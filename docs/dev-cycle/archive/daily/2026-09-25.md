# 2026-09-25

## [INFRA-120] README 의 Market Gate 설명을 코드에 맞춤
- 완료 2026-09-25 15:48 | 티어 문서 (README 는 §2 밖, 실행 코드 변경 0) | 커밋 79b43a9
- 원인: README 가 Gate Closed 를 신호 차단·매매 보류로 설명했으나 `engine/screener.py:201-202` 는 경고만 남기고 `engine/generator.py` 는 판정을 `market_status` 로 저장해 LLM 문맥에만 쓴다. 환율 1480원은 Warning 이 아니라 DANGER 이고 사유에 「[환율 위험]」만 붙는다. `[INFRA-047]` 이후 GET 은 저장값만 읽는데 「접속 즉시 동적 계산」이라고 썼다
- 변경: README.md(흐름도 `Block Signal` → `Warning Log` 뒤 출력, 패턴 표, §1 서술·급락 감점·강세/중립/약세 기준·환율·기본값, §1.1 RSI 구간 셋 추가, §1.2 예시 코드를 `analyze_market_state` 요약으로, §1.3 시나리오 B, 실시간 업데이트 두 줄). 코드 0줄, 문서 +26 -43
- 설계: bounded. 2026-09-25 15:47 「문서를 코드에 맞춤 (Recommended)」. 챗봇 페르소나 지침·예시(「GREEN 일 때만 공격적 진입」 등)는 AI 답변 지침이라 둠. 프런트엔드에 `is_gate_open` 사용이 없어 화면 버튼 서술은 넣지 않음
- 리뷰: tier-rules §5(문서 전용) — 참조 경로 여섯 개 실재, 바꾼 문장마다 코드 줄 대조(TODO 체크에 기록)
- 검증: `git diff --check` exit 0, `git diff --stat` 은 README.md·TODO.md 뿐
- QA: 동적 QA 제외(문서만, 동적 검증 요청 없음)
- 이월: 없음
- 정리: 해당 없음(사본·서버 없음). 운영 동작 변화 없음

## [INFRA-117] 운영 호출자가 없는 KIS 수집기 삭제
- 완료 2026-09-25 15:22 | 티어 T2 (대상이 §2 밖이나 모듈 삭제) | 커밋 e808053
- 원인: `[INFRA-108]` 이 `MarketGate` 의 `KisCollector` 생성을 지운 뒤 `engine/kis_collector.py` 를 import 하는 곳은 그 테스트뿐이었다. `README.md:23` 은 「환율, 수급」이 매수를 원천 차단한다고 썼으나 실제 판정은 시장 점수 40점 하나이고, 닫혀도 `engine/screener.py:201-202` 는 경고만 남긴다
- 변경: engine/kis_collector.py(삭제 229줄), tests/engine/test_kis_collector_refactor.py(삭제 96줄, 토큰 캐시 읽기·쓰기 검사 3건으로 대상 코드와 함께 사라짐), docs/KIS_API_GUIDE.md(삭제), .env.example(`KIS_*` 네 줄), README.md(파일 목록 줄, 23행 Gate 설명). 코드 -325줄(테스트 포함)
- 설계: bounded, T2. 2026-09-25 15:19 「삭제 (Recommended)」. 저장소 밖 호출자는 없다는 전제. 과거 계획·증거 문서의 언급은 기록이라 둠. 필요하면 git 이력에서 되살린다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 Lean · `closing-bet-reviewer` CHANGES_REQUIRED(max medium): 23행 「원천 차단」이 코드와 다름(medium) → 「Closed(약세) 표시와 경고, 신호 생성은 막지 않음」과 데이터 부족 시 50점·Open 기본값 기재 | 「지수·섹터 급락 감점」은 합이 아니라 큰 값(low) → 고침 | README 흐름도·937·946행의 같은 과장(low, 범위 밖) → `[INFRA-120]`
- 검증: `git grep -i` 로 `docs/dev-cycle`·`docs/superpowers` 밖 KIS 참조 0건. `venv/bin/python -m pytest -q -p no:cacheprovider` 2850 passed, 2 skipped(exit 0, 삭제한 3건 차감). 리뷰 반영(README)은 그 뒤이나 README 를 읽는 테스트 없음
- QA: 동적 QA 제외. 삭제한 모듈은 테스트 외 import 가 0건이라 도달하는 실행 경로가 없고 나머지는 문서다
- 이월: `[INFRA-120]` README 의 Market Gate 설명이 신호를 차단한다고 쓴다
- 정리: 해당 없음(사본·서버 없음). ignore 된 `__pycache__` 의 KIS pyc 는 소스가 없으면 import 되지 않아 둠. 운영 반영은 다음 배포에 함께 실리며 동작 변화 없음

## [INFRA-119] 수동 V2 실행의 실행권 저장 실패 거부
- 완료 2026-09-25 14:51 | 티어 T2 (`kr_market_jongga_runtime_service.py`·`kr_market_jongga_execution_routes.py` 는 §2 밖) | 커밋 04bf316
- 원인: 라우트의 `_save_v2_status` 가 `write_v2_status` 의 bool 을 버렸고, `launch_jongga_v2_screener` 는 잠금 안의 `save_v2_status(True)` 가 실패해도 200 을 주고 분석 스레드를 띄웠다. 실행권 없이 분석이 돌고, 그 사이 `[INFRA-116]` 체인이 실행권을 잡으면 끝날 때의 False 가 그것을 덮을 수 있었다. QA 에서 수정 전 사본의 `data/` 쓰기를 막아 200·분석 시작으로 재현
- 변경: services/kr_market_jongga_runtime_service.py(`save_v2_status(True) is False` 면 오류 로그, 잠금 안에서 `save_v2_status(False)` 재시도, 스레드 없이 500), app/routes/kr_market_jongga_execution_routes.py(`_save_v2_status` 가 bool 반환, 타입 힌트), 테스트 두 파일, docs/dev-cycle/qa/INFRA-119.md. 코드 +53줄 순증(테스트 포함, +59 -6)
- 설계: bounded, T2. 2026-09-25 14:45 「이 설계로 진행 (Recommended)」. `is False` 판정은 None 을 돌려주는 기존 테스트 가짜를 그대로 두기 위함. 프론트(`page.tsx` `runUpdate`)는 409 외 오류에서 스피너를 끄므로 바꾸지 않음. 해제 False 실패는 종전처럼 로그만
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 Lean · `closing-bet-reviewer` APPROVE(max low 3건): 교체 뒤 캐시 무효화 예외로 True 가 남은 채 False 를 받는 갈래(low) → 500 갈래에서 False 재시도 | 라우트 bool 반환을 지키는 테스트 없음(low) → 라우트 테스트 추가 | launcher `bool | None` 과 라우트 `bool` 타입 차이(참고) → 테스트 가짜를 받기 위해 둠
- 테스트: `test_launch_jongga_v2_screener_refuses_when_claim_save_fails`(수정 전 실패 확인), `test_manual_run_refuses_when_v2_claim_cannot_be_saved`(실제 라우트·launcher, `atomic_write_text` 가 OSError. 재시도 추가 전 `[True]` 로 실패, 라우트의 `return` 을 지우는 변이에서 실패 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2853 passed, 2 skipped(exit 0, 리뷰 반영 뒤). 첫 커밋의 TODO 체크에 적은 2852 는 리뷰 반영 전 실행의 수치를 잘못 옮긴 것이다
- QA: `docs/dev-cycle/qa/INFRA-119.md` 필수 2/2 통과(격리 사본 둘, 실제 라우트·launcher·원자적 저장, 사본 `data/` 를 0555 로 바꾼 실제 쓰기 실패, 가짜 파이프라인, 소켓 차단). 수정 뒤 500·분석 없음·`isRunning` false, 수정 전 200·분석 시작(재현). 권한 복구 뒤 200 회귀 유지. 원본 실행 요청은 LLM 비용이고 운영 `data/` 권한을 바꿀 수 없어 하네스
- 이월: 없음
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 에서 QA 직전 뒤 수정된 파일 0개. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지)

## [INFRA-118] 수동 V2 실행 종료 시 실행권 해제 단일화
- 완료 2026-09-25 14:42 | 티어 T2 (`kr_market_route_service.py`·`kr_market_jongga_runtime_service.py`·`kr_market_jongga_execution_routes.py` 는 §2 밖) | 커밋 7e3a8b9
- 원인: 라우트가 쓰는 파이프라인(`services/kr_market_route_service.py` 의 `run_jongga_v2_background_pipeline`)이 시작에 True, `finally` 에 False 를 쓰고, 그것을 부르는 `launch_jongga_v2_screener` 의 `_run_wrapper` `finally` 가 False 를 한 번 더 썼다. 둘 다 잠금 밖이라, 첫 False 뒤 `[INFRA-116]` 의 대기 중인 체인이 실행권을 잡으면 두 번째 False 가 그것을 덮어 체인 분석 중 수동 요청이 200 을 받았다. QA 에서 수정 전 사본으로 재현(틈을 넓힌 하네스)
- 변경: services/kr_market_route_service.py·services/kr_market_jongga_runtime_service.py(파이프라인 두 사본에서 `save_status` 매개변수·True/False 저장·try/finally 제거, 「Status reset to False」 로그는 `_run_wrapper` `finally` 로 옮김), app/routes/kr_market_jongga_execution_routes.py(러너 조립에서 `save_status` 전달 제거), 테스트 세 파일, docs/dev-cycle/qa/INFRA-118.md. 코드 +51줄 순증(테스트 포함, +106 -55)
- 설계: bounded, T2. 2026-09-25 14:35 「이 설계로 진행 (Recommended)」. 잠금 없는 단일 False 는 실행권을 쥔 동안 다른 쪽이 True 를 잡을 수 없으므로 남의 실행권을 덮지 않는다. `[INFRA-116]` 심층 리뷰의 「살아 보이는 낡은 True 를 `updated_at` 으로 판정」은 넣지 않기로 결정(운영 재기동은 전체 재기동, 실행 중 갱신이 없어 기준 시간을 정할 수 없음, `scheduler_jobs.py` 가 §2). 한계는 `owned_by_live_sibling` 의 ponytail 주석에 이미 있다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · `/ponytail-review` Lean(두 파이프라인 사본 중복은 범위 밖) · `closing-bet-reviewer` APPROVE(max low): 해제 로그 사라짐(low) → `_run_wrapper` 로 옮김 | True 저장 실패 미확인(low, 기존 결함) → `[INFRA-119]`
- 테스트: `test_manual_run_releases_v2_claim_once_so_a_later_claim_survives`(실제 라우트·launcher·파이프라인, 첫 False 직후 체인 실행권 주입, 수정 전 `[True, True, False, False]` 로 실패 확인), `test_route_service_background_pipeline_error_still_releases_claim_once`(종전 `..._resets_status_on_error` 를 launcher 경로로 바꿈), `test_background_pipeline_does_not_save_twice` 의 상태 단언 제거(파이프라인이 상태를 쓰지 않는 계약). 라우트 테스트 헬퍼의 가짜 logger 에 `info` 추가
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2851 passed, 2 skipped(exit 0, 리뷰 반영 뒤)
- QA: `docs/dev-cycle/qa/INFRA-118.md` 필수 2/2 통과(격리 사본 둘, 체인 프로세스와 라우트 프로세스를 따로 띄움, 가짜 `run_screener`, 소켓 차단). 수정 뒤 체인 분석 중 `isRunning` True·새 수동 요청 409, 수정 전 False·200(재현). 유휴 상태 200 두 번 회귀 유지. 17시 잡은 브라우저로 부를 수 없고 원본 실행 요청은 LLM 비용이라 하네스
- 이월: `[INFRA-119]` 수동 V2 실행이 실행권 True 저장 실패를 확인하지 않음. 범위 밖 참고: 파이프라인 두 사본의 중복
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 에서 QA 직전 뒤 수정된 파일 0개. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지)

## [INFRA-116] 17시 스케줄러 체인과 수동 V2 실행의 실행권 공유
- 완료 2026-09-25 14:31 | 티어 T3 (위험 경로 `services/scheduler_jobs.py`) | 커밋 964a5f8
- 원인: 수동 실행(`launch_jongga_v2_screener`)은 `v2_screener_status.json` 의 `isRunning` 만 확인했고, 스케줄러 `run_jongga_v2_analysis` 는 그 파일을 보지도 쓰지도 않았다. 체인 중 수동 요청은 200 을 받았고 수동 실행 중 시작한 체인도 막히지 않아 LLM 분석이 두 번 돌며 결과 파일을 번갈아 썼다. QA 에서 수정 전 사본으로 두 방향 모두 재현
- 변경: services/scheduler_jobs.py(`V2_STATUS_FILE`·`V2_WAIT_SECONDS`·`V2_POLL_SECONDS`, `_claim_v2_run`·`_wait_and_claim_v2_run`, 분석 전 실행권 획득·`finally` 에서 잡은 경우만 해제), services/kr_market_jongga_runtime_service.py(`write_v2_status`, 저장 성공 여부 반환), app/routes/kr_market_jongga_execution_routes.py(저장 클로저가 `write_v2_status` 호출, 안 쓰는 import 제거), services/common_update_status_service.py(ponytail 주석에 체인 영향 추가), 테스트 두 파일, 계획 `docs/superpowers/plans/2026-09-25-infra-116-scheduler-v2-run-claim.md`, docs/dev-cycle/qa/INFRA-116.md. 코드 +158줄 순증(테스트 포함, +185 -27)
- 설계: bounded, T3. 2026-09-25 14:19 「이 설계로 진행 (Recommended)」(수동 실행 중이면 곧바로 건너뛰지 않고 기다림. 수동 실행이 과거 날짜 분석이거나 17시 수집 전에 시작한 것일 수 있어서). 한계: 주인이 죽었거나 살아 보이는 낡은 True 는 전체 재기동까지 매일 체인의 종가베팅을 30분 대기 뒤 건너뛰게 함, 대기 동안 스케줄러 스레드의 다른 잡도 늦어짐, 대기 중 상태 문구는 「스케쥴링 진행 중」
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 critic ACCEPT-WITH-RESERVATIONS(RED 기대·import·대기 증명·클록 고갈 거짓 통과·409/휴장일 테스트 누락 → 반영, 수동 경로 이중 False → `[INFRA-118]`) · 과잉설계 직접 검토 Lean · `closing-bet-reviewer` APPROVE(max low) · `/review`(T3, `oh-my-claudecode:code-reviewer`) 차단 0: 저장 실패를 실행권 획득으로 처리(두 리뷰 low) → `write_v2_status` bool 반영 | fixture `raising=False`(low) → 제거 | 살아 보이는 낡은 True 가 체인도 막음(중간) → 주석·계획 한계 기록, 낡은 값 판정은 `[INFRA-118]` 설계로 | 대기 중 루프 정지(low)·상태 문구(참고) → 계획 한계 기록
- 테스트: `test_run_jongga_v2_analysis_holds_v2_run_claim_during_analysis`, `..._waits_for_manual_run_then_runs`, `..._skips_after_wait_limit_and_keeps_manual_claim`, `test_manual_run_request_gets_409_while_scheduler_holds_v2_claim`, `test_run_jongga_v2_analysis_does_not_run_when_claim_write_fails`, 휴장일 테스트에 실행권 미획득 단언. 모두 수정 전 코드에서 실패 확인. 라우트 원자적 저장 테스트는 패치 대상 모듈만 옮김
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2850 passed, 2 skipped(exit 0, 리뷰 반영 뒤)
- QA: `docs/dev-cycle/qa/INFRA-116.md` 필수 4/4 통과(격리 사본 둘, 체인 프로세스와 라우트 프로세스를 따로 띄움, 가짜 분석, 소켓 차단). 수정 뒤 체인 중 수동 409, 수동 끝 0.08초 뒤 체인 시작(겹침 없음), 한도 초과 시 분석 없이 수동 실행권 유지, 유휴 상태 200·체인 단독 실행 회귀 유지. 17시 잡은 브라우저로 부를 수 없고 원본 실행 요청은 LLM 비용이라 하네스
- 이월: `[INFRA-118]` 수동 V2 실행이 끝날 때 False 를 잠금 없이 두 번 씀(심층 리뷰의 낡은 True 판정도 그 설계에서 함께). 범위 밖 참고: `scripts/run_full_update.py`·`scripts/verify_collection_logic.py` 의 수동 CLI 는 실행권 없이 분석
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 에서 QA 직전 뒤 수정된 파일 0개. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지)

## [INFRA-115] 종가베팅 V2 실행 요청의 워커 사이 직렬화
- 완료 2026-09-25 14:11 | 티어 T2 (`kr_market_jongga_runtime_service.py`·`kr_market_jongga_execution_routes.py`·`app/__init__.py` 는 §2 밖, 위험 경로 `common_update_status_service.py` 는 import 만) | 커밋 9762439
- 원인: `launch_jongga_v2_screener` 는 `v2_screener_status.json` 의 `isRunning` 을 읽은 뒤 `True` 를 저장했는데 그 사이에 프로세스 사이 잠금이 없었다. 기동 초기화의 읽기-쓰기도 같은 틈으로 다른 워커가 방금 쓴 `True` 를 덮을 수 있었다. QA 에서 수정 전 사본의 두 프로세스 동시 요청이 `[200, 200]`, 백그라운드 시작 2회로 재현
- 변경: services/kr_market_jongga_runtime_service.py(`status_lock` 인자, 기본 `nullcontext`. 확인·저장을 그 안에서, 스레드 시작은 밖. `read_v2_status_uncached` 추가), app/routes/kr_market_jongga_execution_routes.py(실행 라우트가 `_status_file_lock` 과 직접 읽기 로더를 넘김. 상태 조회 GET 은 종전 캐시 로더 유지), app/__init__.py(기동 초기화의 V2 읽기·판정·쓰기를 같은 잠금 안에서, 읽기는 직접), 테스트 세 파일, docs/dev-cycle/qa/INFRA-115.md. 코드 +135줄 순증(테스트 포함, +158 -23)
- 설계: bounded, T2. 2026-09-25 14:03 「이 설계로 진행 (Recommended)」. 잠금 파일을 열지 못하면 `_status_file_lock` 의 폴백대로 잠금 없이 진행해 수정 전 동작으로 돌아간다(500·고착 없음). 백그라운드의 `save_v2_status(False)` 는 잠금 없이 쓰며 끝나는 순간의 새 요청이 받아들여지는 결과만 낸다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 Lean(기존 잠금 헬퍼 재사용) · `closing-bet-reviewer` APPROVE(max low): low 1 잠금 안 판정이 `(mtime, size)` 캐시를 거쳐 같은 틱·같은 크기의 다른 워커 저장을 놓칠 수 있음 → 반영(`read_v2_status_uncached`, 캐시를 채운 뒤 같은 크기·같은 mtime 으로 바꾼 파일을 읽는 테스트가 캐시 로더에서 실패 확인) | low 2 `join` 뒤 스레드 생존 미확인 → 반영. 교착 없음, private 이름 import 는 저장소 관례상 문제없음을 리뷰가 확인
- 테스트: `test_launch_jongga_v2_screener_serializes_concurrent_requests`(실제 파일 잠금, 두 스레드 200·409, 잠금 없는 변형은 `[200, 200]`), `test_run_jongga_v2_screener_route_passes_status_file_lock`, `test_run_jongga_v2_screener_route_checks_running_without_signature_cache`, `test_reset_startup_status_files_waits_for_v2_status_file_lock`. 넷 다 수정 전 코드에서 실패 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2845 passed, 2 skipped(exit 0, 리뷰 반영 뒤)
- QA: `docs/dev-cycle/qa/INFRA-115.md` 필수 3/3 통과(2회차, 격리 사본 둘, 별도 프로세스가 실제 라우트를 Flask 테스트 클라이언트로 호출, 가짜 백그라운드, 소켓 차단). 수정 뒤 동시 요청 `[200, 409]`·시작 1회, 기동 초기화가 잠금을 2.11초 기다린 뒤 초기화, 409→200 회귀 유지. 1회차 S-2 미달(0.13초)은 하네스가 import 시간을 잠금 유지 시간 안에 넣은 측정 결함이라 코드 수정 없이 유지 시간만 늘려 재실행. 원본 서버 실행 요청은 실제 LLM 비용이라 브라우저 대신 하네스
- 이월: 없음(스케줄러 체인과 수동 실행이 겹치는 문제는 기존 `[INFRA-116]`)
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 에서 QA 직전 뒤 수정된 파일 0개. 운영 반영은 gunicorn 재기동(장 중 금지)

## [VCP-060] 가격 동기화의 청산 전용 종목 시세 조회 제외
- 완료 2026-09-25 13:49 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 43effae
- 원인: `update_vcp_signals_recent_price` 는 `df['ticker'].unique()` 전체의 시세를 pykrx·`fetch_stock_price` 로 조회했지만 적용 루프는 `status == 'CLOSED'` 행을 건너뛴다(`[VCP-051]`). 그 값을 쓰던 `kr_ai_analysis.json` 동기화가 `[VCP-059]` 에서 없어져 청산 전용 종목의 조회는 외부 호출 낭비만 남았다. QA 에서 수정 전 사본이 청산 전용 종목을 조회하는 것을 재현
- 변경: scripts/init_data.py(조회 대상을 `status != 'CLOSED'` 행의 티커로, `status` 열이 없으면 전 행), tests/services/test_vcp_signals_log_lock_refactor.py, docs/dev-cycle/qa/VCP-060.md. 코드 +31줄 순증(테스트 포함, +33 -2)
- 설계: bounded, T3. 2026-09-25 13:41 「이 설계로 진행 (Recommended)」. 잠금 밖 조회 중 청산 전용 종목에 새로 생긴 OPEN 행은 다음 동기화에서 갱신되는 한계를 수용(오늘 새로 생긴 종목과 같은 결과)
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 Lean · `closing-bet-reviewer` APPROVE(max low): low status 열 없는 갈래 테스트 부재 → 미반영, 기존 `test_recent_price_update_keeps_rows_added_during_fetch` 가 status 열 없는 로그의 갱신을 단언해 갈래를 지우면 실패 · `/review`(T3, `oh-my-claudecode:code-reviewer`) Critical·Major 0: M1 NaN status 미고정 → 새 테스트에 결측 행 추가 | M2 조회 로그 숫자가 미청산 종목 수 → 의도한 동작이라 미반영. 필터와 적용 루프가 NaN·소문자·열 없음·빈 로그에서 같은 판정임을 두 리뷰가 실측
- 테스트: `test_update_recent_price_skips_quotes_for_closed_only_tickers`(pykrx·폴백 조회 티커를 기록, 수정 전 코드에서 실패 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2841 passed, 2 skipped(exit 0, M1 반영 뒤)
- QA: `docs/dev-cycle/qa/VCP-060.md` 필수 4/4 통과(격리 사본 둘, 가짜 `pykrx`·폴백, 소켓 차단). 수정 뒤 청산 전용 종목은 조회하지 않고, status 결측 행과 status 열 없는 로그는 종전대로 조회·갱신, 전부 청산이면 조회 0건. 로그 결과는 네 경우 모두 수정 전후 동일. 화면 코드·API 형식 불변이라 브라우저 대신 하네스
- 이월: 없음(범위 밖 참고: 전부 청산이어도 잠금 안에서 파일을 다시 쓰지만 원자적 쓰기라 손실이 없어 등록하지 않음)
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 에서 QA 직전 뒤 수정된 파일 0개. 운영 반영은 gunicorn 재기동(장 중 금지)

## [VCP-059] 가격 동기화의 `kr_ai_analysis.json` 쓰기 삭제
- 완료 2026-09-25 13:36 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 9ab1b64
- 원인: `update_vcp_signals_recent_price` 가 부르던 `update_kr_ai_analysis_prices` 는 날짜 없는 `kr_ai_analysis.json` 의 `current_price`·`return_pct` 만 고쳤고, `_merge_ai_analysis_files` 는 날짜 파일 병합 결과로 그 파일을 통째로 덮어 가격이 날짜 파일 값으로 되돌아갔다. 그 가격을 쓰는 소비자는 없었다. VCP 화면 가격은 `signals_log.csv` 에서 오고, 이 파일에서는 판정과 `news` 만 합친다. `/api/kr/ai-analysis` 최신 조회는 이 파일 전체를 응답에 싣지만 그 응답을 쓰는 VCP 페이지와 `KRAIAnalysis` 타입은 가격을 읽지 않는다. 챗봇은 판정만 쓴다. QA 에서 수정 전 사본으로 가격이 동기화 값 → 날짜 파일 값 → 동기화 값으로 오가는 것을 재현
- 변경: scripts/init_data.py(함수·호출·`ai_analysis_lock` import 삭제, 호출 자리에 이유 주석), services/common_env_service.py·services/common_update_ai_analysis_service.py(잠금 주석에서 가격 동기화 제거), tests/services/test_vcp_signals_log_lock_refactor.py, docs/dev-cycle/qa/VCP-059.md. 코드 -56줄 순감(테스트 포함, +4 -60)
- 설계: bounded, T3. 2026-09-25 13:26 「가격 동기화 삭제 (Recommended)」 선택. 병합이 가격 필드를 보존하는 안과 가격 동기화가 날짜 파일도 고치는 안은 읽는 곳 없는 값을 계속 관리하게 되어 택하지 않음. 설계 때 「최신 조회는 `ai_analysis_results.json` 을 읽는다」고 보고한 근거는 틀렸으나(리뷰에서 확인) 소비자가 없다는 결론과 승인 범위는 그대로다. 이 변경 뒤 API 최신 응답의 가격은 AI 판정 병합 때의 값으로 남는다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 Lean(순수 삭제) · `closing-bet-reviewer` APPROVE(max low): 설계 근거 2번 오류 → 소비자를 직접 대조해 결론 유지 | low 병합 잠금 주석의 「가격 동기화」, 새 주석의 「읽는 곳이 없어」, QA 문서 읽는 곳 목록 누락 → 셋 다 반영 · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) Critical·Important 0: M1 CLOSED 행만 남은 종목의 시세 조회가 이제 낭비 → `[VCP-060]` 등록 | M2·M3 QA 기대값을 숫자로, 수정 전 기대값도 → 반영 | M4 API 최신 응답 시나리오 → 선택 S-4 로 반영
- 테스트: 삭제한 함수의 잠금 테스트 `test_ai_price_sync_waits_for_lock_and_writes_atomically`(가격 동기화가 AI 잠금 안에서 다시 읽고 원자적으로 교체하는지 검사)를 대상 코드가 없어져 삭제. 새 테스트는 없음(순수 삭제, QA 하네스가 파일 불변을 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2840 passed, 2 skipped(exit 0, 주석 수정 전), 주석 수정 뒤 관련 4개 파일군 216 passed
- QA: `docs/dev-cycle/qa/VCP-059.md` 필수 3/3·선택 1/1 통과(격리 사본 둘, 가짜 `pykrx`, 소켓 차단). 수정 뒤 가격 동기화는 `kr_ai_analysis.json` 의 바이트·수정 시각을 바꾸지 않고 AI 잠금 파일도 만들지 않으며 `signals_log.csv` 가격은 종전대로 갱신, 병합 뒤 동기화가 가격을 되돌리지 않음, 챗봇 판정과 API 최신 응답의 판정은 전후 동일. 화면 코드·API 형식 불변이라 브라우저 대신 하네스
- 이월: `[VCP-060]` 가격 동기화가 청산 행만 남은 종목의 시세도 조회
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 에서 QA 시작 뒤 수정된 파일 0개. 운영 반영은 gunicorn 재기동(장 중 금지). 운영의 기존 `kr_ai_analysis.json` 가격은 다음 AI 판정 병합 때 날짜 파일 값으로 바뀐다

## [VCP-058] `vcp_signals_latest.json` 을 원자적으로 저장
- 완료 2026-09-25 13:12 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 7694bee
- 원인: `_write_vcp_signals_latest_payload` 가 `open(path, "w")` 로 파일을 비운 뒤 `json.dump` 로 스트리밍해, 직렬화나 쓰기가 도중에 실패하면 잘린 JSON 이 남고 쓰는 동안 읽는 `_count_latest_signals`·`_extract_json_data_metadata` 가 0·예외를 받았다. QA 에서 수정 전 사본으로 두 경우 모두 재현(잘린 220바이트, 동시 읽기 172회 중 145회 오류)
- 변경: scripts/init_data.py(`json.dumps` 로 먼저 직렬화하고 `atomic_write_text` 로 저장, `:2097` 과 같은 방식. `create_signals_log` 바깥 except 갈래의 최신 파일 재저장을 `OSError` 로 감싸 경고 후 `False`), tests/scripts/test_init_data_vcp_scheduler.py, docs/dev-cycle/qa/VCP-058.md. 코드 +50줄 순증(테스트 포함, +56 -6)
- 설계: bounded, T3. 2026-09-25 13:00 「이대로 진행 (Recommended)」. 파일 권한 0600·캐시 무효화는 부수 효과로 수용, 다른 `open('w')` 는 범위 밖. 교체로 최신 파일 저장이 fsync·`os.replace` 를 거치게 되자 `[VCP-030]` 테스트가 except 갈래 재저장의 예외 누출(수정 전에도 `open` 실패면 같음)을 드러내, 13:02 「이번에 함께 막기 (Recommended)」로 범위 추가. fsync 는 교체 전에 일어나므로 실패하면 교체가 일어나지 않고 기존 파일이 남는다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 「Lean already. Ship.」 · `closing-bet-reviewer` CHANGES_REQUIRED(max medium): medium `[VCP-030]` merge 케이스가 최신 파일 저장에서 먼저 실패해 병합 쓰기에 닿지 않음 → 그 테스트에서 최신 파일 저장을 no-op 으로(사본에서 병합 쓰기를 `to_csv` 로 바꾸면 [merge] 실패) | low 새 테스트는 직렬화 단계 실패만 봄 → 최신 파일 `os.replace` 만 실패시키는 테스트 추가. 재검토 APPROVE(max none) · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) Critical·Important 0: Minor 1 merge 갈래 저장 실패 뒤 except 갈래가 빈 결과로 덮는 것은 `[VCP-028]` 설계라 그대로(QA 에서 「이전 결과 유지」 증거로 쓰지 않음) | Minor 2 「교체 뒤 fsync」 설명 오류 → 이 기록에서 바로잡음
- 테스트: `test_write_vcp_signals_latest_keeps_previous_file_when_write_fails`(수정 전 코드에서 잘린 파일로 실패), `test_create_signals_log_returns_false_when_latest_replace_fails`(사본에서 except 보호 제거 시 이 테스트만 실패, 수정 전 코드면 둘 다 실패), `[VCP-030]` 테스트의 최신 파일 저장 no-op
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2841 passed, 2 skipped(exit 0)
- QA: `docs/dev-cycle/qa/VCP-058.md` 필수 4/4 통과(격리 사본 둘, CLI 하네스, 소켓 차단, 가짜 스크리너). 화면 코드·API 형식 불변이라 브라우저 대신 하네스. 수정 뒤 실패해도 기존 파일 보존, 동시 읽기 543회 오류 0, 정상 저장 내용 불변(권한만 0644→0600), 분석 실패 갈래에서 저장까지 실패해도 `False`
- 이월: 없음
- 정리: 사본 셋 삭제, 하네스 프로세스 0, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 재기동(장 중 금지). 운영의 기존 파일 권한은 다음 저장부터 0600

## [INFRA-110] 데이터 상태 화면의 늦게 도착한 마운트 조회가 새 진행 폴링을 끊지 않게 함
- 완료 2026-09-25 12:48 | 티어 T2 (프론트엔드, §2 위험 경로 아님) | 커밋 462c506
- 원인: `pollUpdateStatus` 는 `!status.isRunning && pollingRef.current` 이면 interval 을 끊는다. 마운트 때 보낸 조회가 전체 업데이트 시작보다 늦게 `isRunning:false` 로 도착하면 시작 뒤에 만든 interval 까지 끊고 1초 뒤 `setUpdating(false)` 로 진행 표시를 지웠다. 시작 요청의 응답이 오기 전에 도착해도 진행 상태가 잠깐 지워지고 전체 버튼이 다시 활성화됐다. 백엔드 `api_start_update` 는 응답 전에 실행 중을 저장하므로 문제는 시작 전에 보낸 조회뿐이다. QA 에서 수정 전 사본으로 두 경우 모두 재현
- 변경: frontend/src/app/dashboard/data-status/page.tsx(`pollSessionRef`, `startPolling`·`handleUpdateAll` 이 번호를 올리고 보낸 시점과 번호가 다른 응답은 성공·실패 모두 무시), 회귀 테스트 `page.regression-INFRA-110.test.tsx`, docs/dev-cycle/qa/INFRA-110.md. 코드 +96줄 순증(테스트 포함, +96 -0)
- 설계: bounded, T2. 2026-09-25 12:34 「세션 번호로 늦은 응답 버림 (Recommended)」 선택. 시작 직후 일정 시간 `false` 를 무시하는 안은 기준 시간이 임의라 택하지 않음. 리뷰 low 1(시작 응답 전에 도착한 조회)을 대화에서 「이번에 함께 막기 (Recommended)」로 확인받아 클릭 때도 번호를 올림
- 리뷰: `.claude/skills/closing-bet-nextjs/SKILL.md`·`vendor/skills/vercel-react-best-practices/rules/rerender-use-ref-transient-values.md`·번들 문서 `01-app/01-getting-started/05-server-and-client-components.md` · ponytail(직접 검토, 줄일 것 없음) · `closing-bet-reviewer` APPROVE(max low): low 1 시작 응답 전에 도착한 조회 → 범위 확장으로 반영 | low 2 마운트 조회가 느리면 새 세션 폴링이 그동안 멈춤 → `[INFRA-103]` 겹침 방지의 기존 동작 | 참고 완료 뒤 1초 지연 콜백·언마운트 정리 → 범위 밖·영향 없음 · 확장분 재검토 APPROVE, 지적 없음
- 테스트: vitest 회귀 2건(늦은 `false` 가 새 폴링을 끊지 않음, 시작 응답 전의 `false` 가 「업데이트 중...」을 지우지 않음). 구현 전 두 건 모두 RED 확인
- 검증: `(cd frontend && npx vitest run)` 103 files·697 tests(exit 0), `npm run type-check` exit 0, `npm run lint` 오류 0(경고 183, 변경 파일의 경고는 기존 줄). 주석 수정 뒤 data-status 5 files·13 tests·tsc 재확인
- QA: `docs/dev-cycle/qa/INFRA-110.md` 필수 4/4 통과(격리 사본 둘, 사본 Next dev 58102 + 가짜 백엔드 58101, agent-browser). 실제 Flask 의 시작 요청은 수집·LLM 을 돌리므로 가짜 백엔드가 상태만 바꿈. 수정 뒤 늦은 `false` 뒤에도 폴링·진행 표시 유지, 시작 응답 전에도 「업데이트 중...」 유지, 끝나면 폴링 정지, 시작하지 않으면 폴링 없음
- 이월: 없음
- 정리: agent-browser 세션 닫음, 사본 둘·더미 토큰 삭제, 58101·58102 리스너 0. 운영 반영은 Next 재빌드·재기동(`restart_all.sh`, 장 중 금지)

## [INFRA-109] Toss 투자자 추이의 빈 순매수 수량을 날짜 단위 결측으로 보존
- 완료 2026-09-25 12:04 | 티어 T3 (위험 경로 `services/investor_trend_5day_service.py`) | 커밋 e162b56
- 원인: `parse_investor_trend` 는 종가가 있는 행의 외국인·기관 수량이 비면 `to_float` 으로 0 을 더했고, `_normalize_external_trend_payload` 는 `_safe_int` 로 하루 값을 0 으로 채웠으며, `_normalize_toss_supply_payload` 는 값이 None 인 행을 버려 전날 행이 `details[0]`(1일 순매수)이 됐다. 파서가 원본 `trends` 를 `details` 로 넘겨 종가 없는 행도 정규화기의 `[:5]` 에 섞였다. QA 에서 수정 전 사본의 참조 경로가 종가 없는 최신 행을 1일 값 0 으로 읽는 것을 재현
- 변경: engine/toss_collector_numeric_helpers.py(공용 `optional_volume`: None·bool·빈 문자열·숫자 아님·무한대는 결측, 쉼표 제거), engine/toss_collector_metric_parsers.py(값 있는 날만 합산, 다섯 날 모두 비면 합계 None, `details` 는 종가 있는 행만), engine/screener_supply_helpers.py·services/investor_trend_5day_service.py(하루 결측은 제자리 None, 합계 결측·키 없음이면 페이로드 None), 테스트 셋, 계획 `docs/superpowers/plans/2026-09-25-infra-109-toss-supply-missing-per-day.md`, docs/dev-cycle/qa/INFRA-109.md. 코드 +148줄 순증(테스트 포함, +172 -24)
- 설계: bounded, T3. 2026-09-25 11:48 「추이 전체를 결측 처리」를 골랐다가 사용자가 「결측이 하나라고 5일 추이 전체를 결측으로 보기엔 애매하다」고 되물어 철회, 11:51 「날짜 단위 보존 (Recommended)」 선택. 행만 버리는 안, 최신일 결측만 전체 결측으로 보는 안, 실측까지 보류하는 안은 택하지 않음
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` REVISE: 종가 없는 행이 정규화기에서 None 행으로 남음 → 파서 `details` 를 종가 있는 행으로 좁혀 반영 | `_serialize_trend_map` 변경 불필요 → 철회 | 권고 테스트 (a)(b) → 반영, (c) → 기존 테스트로 갈음 | 상세 API `null`→화면 0 → `[FE-048]` · `/ponytail-review` shrink 1(`valid_rows` 카운터) → 반영 · `closing-bet-reviewer` APPROVE(max low): 종가 없고 수량만 있는 행도 빠지는 절충 → 계획 기록 | 상세 API `null` → `[FE-048]` | 개인 합계 `days != 5`·캐시 스키마 버전 → 범위 밖·영향 미미 · `/review`(T3, `oh-my-claudecode:code-reviewer`) APPROVE, Critical·Major 0: M1 종가 없는 행이 있으면 Toss 참조가 `insufficient_days` 로 버려짐 → 보수적 변화로 계획 기록 | M2 최신일 빈 참조의 하루 캐시 → 종전과 결과 같아 알려진 한계 기록 | N2 줄바꿈 → 반영 | N1·N3 → 그대로
- 테스트: pytest 8건 추가·1건 기대값 변경(파서 빈 날 합산·전부 공란 합계 None·실제 0·종가 없는 최신 행 제외, 스크리너 정규화기 제자리 None·합계 결측, 폴백 뒤 캐시 미저장, Toss SQLite 캐시 왕복, 외부 정규화기 JSON 왕복·합계 결측). 기존 `test_parse_investor_trend_ignores_non_dict_or_zero_close_items` 의 `len(details) == 3` 은 의도된 변경에 맞춰 종가 있는 행 하나로 바꿨고, 기존 픽스처 `{"foreign": 10}` 에 `"institution": 0` 을 넣었다(기대값 불변). 구현 전 5건 RED 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2839 passed 2 skipped(exit 0, 리뷰 반영 뒤). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-109.md` 필수 4/4 통과(격리 사본 둘, `TossCollector._safe_request` 만 가짜, 소켓 차단). 최신일 수량만 빈 종목은 1일 값 None·합계 네 날(수정 전 스크리너는 전날 값이 밀려 올라와 35점, 수정 뒤 15점), 다섯 날 공란은 스크리너 폴백·참조 없음, 종가 없는 최신 행은 1일 자리를 차지하지 않음, 정상 종목과 `init_data` 저장 행은 전후 동일. 화면은 저장된 점수를 읽을 뿐이고 실제 신호 생성은 외부 조회·LLM 이라 하네스로 대체
- 이월: `[FE-048]` 종가베팅 상세 모달이 결측인 Toss 5일 순매수를 0 으로 표시
- 정리: 사본 둘 삭제, 하네스 프로세스 0, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지). 배포 직전 15분 슬롯의 Toss 수급 캐시는 종전 방식 값으로 남을 수 있다

## [INFRA-108] Market Gate 의 쓰이지 않는 수급 점수 경로 삭제
- 완료 2026-09-25 11:43 | 티어 T3 (위험 경로 `engine/market_gate*.py`) | 커밋 9e31972
- 원인: TODO 는 `load_supply_data` 가 수급 파일을 날짜로만 정렬해 마지막 날짜의 임의 종목 한 행을 시장 수급으로 쓰고 `score_supply` 가 그 값으로 최대 15점을 준다고 적었다. 설계 조사에서 `analyze_market_state`(`engine/market_gate_analysis.py:106-150`)의 `total_score` 는 기술 점수 다섯 개와 급락 벌점만 쓰며, 수급 호출은 `45ad2a67`(2026-02-22) 리팩터링에서 빠졌음을(그 전에도 "Just for display") 확인했다. 결함 코드는 판정·화면에 영향이 없는 죽은 코드였다
- 변경: engine/market_gate_fetchers_local.py(`load_supply_data`·쓰지 않는 typing import 삭제), engine/market_gate_logic_fetchers.py·engine/market_gate_logic.py(재수출 삭제), engine/market_gate_logic_scoring.py(`score_supply` 삭제), engine/market_gate.py(`KisCollector` 생성·`_load_supply_data`·`_score_supply`·폴백 설정 필드 삭제), engine/config.py·config.py(`foreign_net_buy_threshold` 삭제), 테스트 셋 정리, scripts/test_market_gate_crash.py·tests/test_empty_supply.csv 삭제, docs/KIS_API_GUIDE.md(market_gate 연동 서술을 현행으로), 계획 `docs/superpowers/plans/2026-09-25-infra-108-market-gate-dead-supply-path.md`, docs/dev-cycle/qa/INFRA-108.md. 코드 -213줄 순감(테스트 포함, +4 -217)
- 설계: bounded, T3. 2026-09-25 사용자 「죽은 경로 삭제 (Recommended)」 선택(확인 시각 11:34). 합계로 고쳐 점수에 편입하는 안, TODO 만 정정하는 안은 택하지 않음. `engine/kis_collector.py` 모듈은 유지
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS: 1 남는 typing import → 반영 | 2 중복 테스트 → 삭제 | 3 grep 완료 기준 → 허용 두 줄 명시 | 4 `kis_collector.py` 죽은 코드·README → `[INFRA-117]` | 5 QA 방식 → QA 문서에 반영 · ponytail(삭제만 있는 diff, Lean already) · `closing-bet-reviewer` APPROVE(max low): KIS 가이드 불일치·import 빈 줄·중복 테스트 → 반영 | `README.md:23` → `[INFRA-117]` · `/review`(T3, `oh-my-claudecode:code-reviewer`) 차단 0: Minor KIS 가이드 → 반영 | Nit 빈 줄 → 반영 | Nit `docs/reference/` 스냅샷·테스트 이름 → 그대로
- 테스트: 삭제 3건(`test_load_supply_data_reads_latest_values`, `score_supply` 단언, 중복된 `test_analyze_market_state_does_not_require_supply_side_path`). 모두 삭제한 함수만 검사했거나 `test_analyze_market_state_builds_expected_payload` 와 같은 단언이었다. 가짜 게이트에서 수급 메서드를 뺐으므로 수급 호출을 되살리면 기본 결과(50)가 나와 기존 `total_score == 75` 단언이 실패한다
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2830 passed 2 skipped(exit 0, 리뷰 반영 뒤). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-108.md` 필수 3/3 통과(격리 사본 둘, 합성 가격·수급 파일, 외부 조회 고정값·소켓 차단). 두 날짜의 `analyze` 결과와 저장 JSON 이 수정 전후 동일, 9,000억 수급 행은 전후 모두 점수에 없음, 수정 뒤 `kis` 속성·수급 메서드 없음. 화면은 저장 JSON 을 읽을 뿐이고 실제 갱신 POST 는 금지 조작이라 하네스로 대체
- 이월: `[INFRA-117]` `engine/kis_collector.py` 에 운영 호출자가 없음(README·`.env.example`·KIS 가이드·`README.md:23` 정리 포함)
- 정리: 사본 둘 삭제, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지), 반영 뒤 달라지는 것은 `MarketGate()` 생성 때 KIS 토큰 파일을 읽던 로그 한 줄뿐

## [INFRA-114] 워커 하나만 다시 떠도 다른 워커의 V2 실행 상태와 스케줄러 실행 표시를 지우지 않음
- 완료 2026-09-25 11:29 | 티어 T3 (위험 경로 `services/scheduler_runtime_status_service.py`) | 커밋 803d298
- 원인: `_reset_startup_status_files` 는 워커가 뜰 때마다 `v2_screener_status.json` 을 `{"isRunning": false}` 로 쓰고 `reset_scheduler_runtime_status` 로 세 플래그를 내렸다. V2 `isRunning` 은 `launch_jongga_v2_screener` 의 409 중복 실행 잠금이라, 워커 하나만 다시 떠도 다른 워커의 실행 중에 두 번째 V2 분석(LLM 비용)이 허용됐다. 스케줄러 플래그는 리더 워커의 17시 체인 표시를 끄는 데 그친다. QA 에서 수정 전 사본의 워커 B 를 `kill -9` 해 재현
- 변경: services/common_update_status_service.py(공용 `owned_by_live_sibling`, `reset_orphaned_update_status` 가 사용), services/scheduler_runtime_status_service.py(`set_scheduler_runtime_status` pid 기록, `reset_scheduler_runtime_status` 가 실행 중이고 형제 소유면 보존·깨진 파일은 초기화), app/routes/kr_market_jongga_execution_routes.py(`_save_v2_status` pid 기록), app/__init__.py(V2 보존 판정), services/kr_market_jongga_runtime_service.py(실행 표시를 스레드 시작 전에 저장, 시작 실패 때 `False` 로 되돌림), 테스트 넷(새 파일 `tests/services/test_kr_market_jongga_runtime_service_refactor.py`), 계획 `docs/superpowers/plans/2026-09-25-infra-114-startup-reset-v2-scheduler-owner.md`, docs/dev-cycle/qa/INFRA-114.md. 코드 +205줄 순증(테스트 포함, +219 -14)
- 설계: bounded, T3. 2026-09-25 사용자 「승인 (Recommended)」 선택(확인 시각 11:13). V2 만 고치는 안은 택하지 않음
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS: 1 스레드 시작 뒤 `True` 저장 → 순서 변경 반영 | 2·3 HUP 뒤 스케줄러 플래그·프로세스 사이 잠금 없음 → 계획 알려진 한계 기록 | 4 import 누락 → 반영 | 5 테스트 공백 둘 → 반영 · `/ponytail-review` shrink 2(중복 `isinstance`) → 반영 · `closing-bet-reviewer` APPROVE(max low): low 1 `thread.start()` 실패 시 `True` 고착 → 되돌림 반영 | low 2 공용 주석의 해제 수단 → 파일별로 고침 | low 3 기동 초기화 읽기-쓰기 창 → `[INFRA-115]` | 관찰 테스트 위치 → 새 파일로 옮김 · `/review`(T3, `oh-my-claudecode:code-reviewer`) 차단 0: 중 1 같은 고착 → 반영 | 2 주석 → 반영 | 3 파일 잠금 부재 → `[INFRA-115]` | 4 `False` 기록 실패 때 복구 경로 좁아짐 → 의도된 트레이드오프, 기록만
- 테스트: pytest 7건 추가·1건 확장(두 파일 형제 소유 보존, 죽은·자기·마스터 불일치·옛·깨진 파일 초기화, 스케줄러 pid 기록과 조회 제외, 옛 소유자 덮기, 플래그 없으면 기본값, V2 pid 기록과 공개 응답 제외, 저장 순서, 스레드 시작 실패 되돌림). 구현 전 3건 RED, 옛 저장 순서에서 순서 테스트 3/3 실패 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2832 passed 2 skipped(exit 0, 리뷰 반영 뒤). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-114.md` 필수 4/4 통과(격리 사본 둘, 실제 gunicorn 워커 둘·`SCHEDULER_ENABLED=false`, 포트 5613·5614, 워커 `kill -9`, GET 셋). 수정 전 S-1 에서 형제 워커 교체만으로 두 상태 초기화(결함 재현), 수정 뒤 보존·「V2 reset skipped」·세 응답 pid 없음. 소유 워커 사망·전체 재기동·옛 파일은 전후 모두 초기화. 화면 조작이 계기가 아니라 하네스로 대체
- 이월: `[INFRA-115]` V2 실행 요청의 409 잠금이 워커 사이에서 원자적이지 않음, `[INFRA-116]` 17시 체인이 수동 V2 실행 중인지 확인하지 않음
- 정리: 사본 둘 삭제, 포트 5613·5614 LISTEN 0, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지). HUP 나 같은 마스터 안 pid 재사용으로 V2 `isRunning` 이 남으면 다음 전체 재기동이 푼다

## [INFRA-107] 워커 하나만 다시 떠도 다른 워커에서 도는 수동 업데이트 상태를 지우지 않음
- 완료 2026-09-25 11:09 | 티어 T3 (위험 경로 `services/common_update_status_service.py`, 저장소 스키마 목록) | 커밋 8f83b55
- 원인: gunicorn 은 `--preload` 없이 워커 둘을 띄워 워커마다 `create_app` → `_reset_startup_status_files` 가 돈다. 이 함수는 `update_status.json` 이 `isRunning` 이면 잠금 없이 `isRunning=False`·`items=[]` 로 덮었으므로, 워커 하나만 비정상 종료돼 다시 뜨면 다른 워커에서 실제로 도는 실행 상태가 지워졌다. QA 에서 수정 전 사본의 워커 B 를 `kill -9` 해 재현
- 변경: services/common_update_status_service.py(`start_update` 가 `ownerPid`·`ownerPpid` 기록, `_owner_process_alive`(bool·0 이하·자기 pid·`OverflowError` 는 죽음, `PermissionError` 는 살아 있음), `reset_orphaned_update_status`(`_status_file_lock` 안에서 같은 마스터의 살아 있는 다른 워커 소유면 보존, 아니면 초기화해 `save_update_status`)), app/__init__.py(기동 초기화가 이 함수를 부름, 안쪽 `try/except` 유지), app/routes/common_update_routes.py(공개 GET 에서 두 pid 제외), 테스트 셋, 계획 `docs/superpowers/plans/2026-09-25-infra-107-startup-reset-owner-pid.md`, docs/dev-cycle/qa/INFRA-107.md. 코드 +127줄 순증(테스트 포함, +146 -19)
- 설계: bounded, T3. 2026-09-25 사용자 「승인 (Recommended)」 선택(확인 시각 10:56). gunicorn master 훅 안과 v2·스케줄러 상태까지 포함하는 안은 택하지 않음
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS: 1 재부팅 뒤 pid 재사용 → `ownerPpid` 마스터 일치 반영 | 2 HUP 고착 → 알려진 한계·QA 문서 기록 | 3 `OverflowError` → 반영 | 4 계획·구현 불일치 → 계획 수정 | 5 잠금 미검증 → 잠금 대기 테스트 추가(사본에서 잠금 제거 시 그 테스트만 실패) · `/ponytail-review` 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): low 1 HUP → QA 문서 기록 | low 2 공개 GET pid 노출 → 제외 반영 | low 3 스냅샷 대체로 파일 신규 작성 → QA 문서 기록 · `/review`(T3, `oh-my-claudecode:code-reviewer`) 차단 0: Minor 2 pid 노출 → 반영 | Minor 6 EPERM 갈래 테스트 → 반영 | Minor 1 flock 실패·3 같은 마스터 pid 재사용과 USR2·4 다른 프로세스의 `create_app`·5 컨테이너 PID 1 테스트 → 기록만(사유는 TODO 체크 원문)
- 테스트: pytest 5건 추가(살아 있는 다른 워커·EPERM pid 보존, 죽은·자기·bool·overflow·마스터 불일치 초기화, `ownerPid`·`ownerPpid` 기록, 잠금 대기, 공개 응답 pid 제외). 구현 전 2건 RED 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2825 passed 2 skipped(exit 0, 리뷰 반영 뒤). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-107.md` 필수 4/4 통과(격리 사본 둘, 실제 gunicorn 워커 둘·`SCHEDULER_ENABLED=false`, 포트 5611·5612, 워커 `kill -9`). 수정 전 S-1 에서 형제 워커 교체만으로 `isRunning:false`·`items:[]`(결함 재현), 수정 뒤 보존과 「Startup reset skipped」. 소유 워커 사망·전체 재기동·옛 파일은 전후 모두 초기화. 화면 조작이 계기가 아니라 하네스로 대체
- 이월: `[INFRA-114]` 기동 초기화가 `v2_screener_status.json`·스케줄러 런타임 상태를 조건 없이 지움
- 정리: 사본 둘 삭제, 포트 5611·5612 LISTEN 0, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지). HUP 이나 같은 마스터 안 pid 재사용으로 `isRunning` 이 남으면 관리자 중단 요청으로 푼다

## [INFRA-113] 수동 갱신 stale 검증이 결측·형식 오류 날짜를 비교에서 빼고 유효 날짜로 판정함
- 완료 2026-09-25 10:53 | 티어 T1 (구현 13줄, 위험 경로 없음) | 커밋 f4fd0e5
- 원인: `_validate_latest_date_not_stale` 은 `read_csv(dtype=str)` 의 날짜를 `astype(str)` 로 바꿔 문자열 최댓값을 구했다. 결측은 `"nan"`, 형식 오류는 `"abc"` 처럼 되어 숫자로 시작하는 날짜보다 크므로, 한 행만 비어도 최신 날짜가 그 값이 되어 오래된 파일이 통과했다. 날짜 열이 전부 비면 수급 파일 없이도 VCP 게이트가 열린다. QA 하네스의 수정 전 사본에서 재현. 로컬 `data/` 두 파일은 결측·형식 오류 0건이라 실제 발생 기록은 없음
- 변경: services/common_update_pipeline_steps.py(날짜 앞 10자리를 `pd.to_datetime(format="%Y-%m-%d", errors="coerce")` 로 변환해 `dropna`, 유효 날짜가 없으면 「no valid date」 error), 테스트 셋(헬퍼 1), docs/dev-cycle/qa/INFRA-113.md. 코드 +52 -3줄(테스트 포함)
- 설계: bounded, T1. 2026-09-25 10:49 사용자 「승인」 선택(일부 결측에 경고를 더하는 안은 택하지 않음)
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` 기준 · `/ponytail-review` 「Lean already. Ship.」
- 테스트: pytest 2건 추가(날짜 전부 결측 → `error`, 결측·`abc`·오래된 날짜 → stale `error` 이고 최신 날짜와 결측이면 `done`). 구현 전 2 failed 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2820 passed 2 skipped(exit 0). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-113.md` 필수 4/4 통과(격리 사본 둘, 가짜 pykrx·소켓 차단, 실제 `run_daily_prices_step`·`run_institutional_trend_step`). 수정 전 네 경우 모두 `done`(결함 재현), 수정 뒤 전부 결측·stale 은 `error`, 최신+결측은 전후 `done`. 화면 진입점은 실제 수집·LLM 을 일으키는 금지 조작이라 하네스로 대체(`[INFRA-106]` 과 같은 판단)
- 정리: 사본 둘 삭제, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지)

## [INFRA-106] 개장일 확인 실패를 WARNING 으로 남기고 수동 갱신 stale 검증은 날짜 비교만 보류함
- 완료 2026-09-25 10:42 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 773f2fc
- 원인: `get_last_trading_date` 는 KOSPI 지수 조회가 예외를 내거나 빈 결과면 DEBUG 만 남기고 주말만 거른 날짜를 돌려줬다. KRX 미인증·점검 때 추석 같은 평일 휴장일이 기대 날짜가 되어, 관리자 수동 갱신의 stale 검증이 직전 거래일까지 정상인 파일을 「stale data detected (2026-09-23 < 2026-09-24)」로 실패시켰다. QA 하네스에서 재현
- 변경: scripts/init_data.py(`strict=False` 인자, 빈 결과·미설치·예외 세 갈래 WARNING 「개장일 미확인」, `strict` 면 `RuntimeError`, 비strict 폴백은 「주말 처리 날짜로 진행합니다」 WARNING), services/common_update_pipeline_steps.py(`_resolve_expected_trading_date_str` 만 strict 호출, `_validate_latest_date_not_stale` 은 기대 날짜 확인을 파일·날짜 열 검사 뒤 날짜 비교 앞으로 옮기고 「stale check skipped」 WARNING), tests/conftest.py(`services.common_update_pipeline_steps._BASE_DIR` 격리), 테스트 셋, 계획 `docs/superpowers/plans/2026-09-25-infra-106-trading-date-unconfirmed.md`, docs/dev-cycle/qa/INFRA-106.md. 수집기 두 곳은 인자 불변. 코드 +142 -13줄(테스트 포함)
- 설계: bounded, T3. 2026-09-25 10:29 사용자 「WARNING 승격 + stale 검증만 판정 보류」 선택. 「실패 시 전 호출자 판정 보류」안은 KRX 점검 때 평일 수집이 통째로 빠져 제외
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS: 1 strict 성공 경로 테스트 없음 → 반영(사본 변이로 실패 확인) | 2 WARNING 단언이 메시지를 안 봄 → 반영 | 3 미설치 분기 테스트 → 반영 | 4 문구 → 「개장일 미확인」 반영 | 5 단계마다 조회 두 번 → 종전 구조라 기록만 · `/ponytail-review` 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max medium): 1 판정 보류가 파일 없음·날짜 열 검사까지 꺼 수급 파일 없이 VCP 게이트 통과 → 기대 날짜 확인 위치를 옮겨 반영(사본에서 옛 위치로 되돌리면 새 테스트 실패) | 2 계획 문구 → 정정 | 3 보류 로그 없음 → WARNING 반영 | 4 수급 단계 테스트 → 반영 · `/review`(T3, `oh-my-claudecode:code-reviewer`) APPROVE-WITH-FIX: 1 위치 이동으로 가짜 `init_data` 테스트가 저장소 `data/daily_prices.csv` 를 읽게 됨(빈 `_BASE_DIR` 로 재현) → conftest 격리와 그 테스트의 자기 CSV 로 반영 | 2 새 WARNING 단언 없음 → 반영 | 관찰 날짜 열 결측이 「nan」 으로 최신 판정 → `[INFRA-113]` 이월
- 테스트: pytest 5건 추가(세 실패 갈래의 WARNING·strict 예외, strict 성공 경로 09-24, 미확인 때 Daily Prices `done`·보류 WARNING, 미확인이어도 수급 파일 없으면 `error`). 구현 전 3건 RED, 변이 두 건 RED 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2818 passed 2 skipped(exit 0, 리뷰 반영 뒤). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-106.md` 필수 4/4 통과(격리 사본 둘, 가짜 pykrx·소켓 차단, 실제 `run_daily_prices_step`·`get_last_trading_date`). 수정 전 미확인 두 경우 `error`(결함 재현), 수정 뒤 `done`·WARNING. 파일 없음은 전후 모두 `error`, 확인된 날짜의 stale 판정은 전후 같음. 화면 진입점은 실제 수집·LLM 을 일으키는 금지 조작이라 하네스로 대체(`[INFRA-101]` 과 같은 판단)
- 이월: `[INFRA-113]` stale 검증이 날짜 열 결측을 최신으로 봄
- 정리: 사본 둘 삭제, 원본 `data/` 수정 시각 불변. 운영 반영은 gunicorn 워커 전부 재기동(장 중 금지)

## [INFRA-112] Market Gate 섹터 등락률 NaN 이 화면에 「NaN%」 로 보인다는 지적 조사 종결
- 완료 2026-09-25 10:27 | 티어 T1 (테스트만 변경, 위험 경로 없음) | 커밋 011b55d
- 조사: `get_sector_data` 가 NaN 을 그대로 넣으면 `build_sector_signals` 는 `change_pct` NaN·신호 `Neutral` 을 만든다. 저장은 `save_analysis` 의 `sanitize_for_json`(`engine/market_gate_logic_utils.py`)이, 응답은 관리자 POST 응답을 포함해 `app/__init__.py:147` 의 `NaNSafeJSONProvider`(`engine/pandas_utils_safe.sanitize_for_json`)가 null 로 바꾸고, 화면은 `[INFRA-105]` 의 결측 카드 `—` 로 그린다. 섹터 급락 감점은 `isfinite` 로 거른다. 전역 KOSPI 가 없고 KOSPI 200 섹터가 NaN 이면 `_resolve_kospi_change` 가 None 대신 NaN 을 돌려주지만 비교가 모두 거짓이라 장중 감점은 None 과 같은 0 이고 저장값은 null 이다
- 결론: 결함 아님. 코드 불변, 현재 동작을 고정하는 테스트 1개 추가. TODO 의 전제(화면에 「NaN%」)가 틀렸음을 기록
- 변경: tests/engine/test_market_gate_logic_facade.py(NaN 섹터가 `Neutral`, 저장 sanitize 뒤 None, 응답 직렬화에 `"change_pct": null`·`NaN` 없음). 코드 +15줄(테스트)
- 설계: bounded, T1. 2026-09-25 10:24 사용자 「테스트만 추가 후 종결」 선택
- 리뷰: `/ponytail-review` 「Lean already. Ship.」
- 테스트: 격리 사본에서 `NaNSafeJSONProvider.dumps` 가 sanitize 를 건너뛰게 한 mutation 에 새 테스트가 실패(1 failed) 확인, 사본 삭제
- 검증: 해당 파일 6 passed(exit 0), `venv/bin/python -m pytest -q -p no:cacheprovider` 2812 passed 2 skipped(exit 0)
- QA: 테스트만 바꾸는 작업이라 동적 QA 제외(`tier-rules.md` §1-1)
- 사고 기록: 실측 중 로컬 파이썬에서 `create_app()` 을 불러 앱 기동 절차가 원본 저장소에서 실행됐다(10:23:35, 로컬 3500·5501 미기동 상태). 원본 `data/v2_screener_status.json`(`isRunning:false`)·`data/scheduler_runtime_status.json`(플래그 셋 false)·`data/paper_trading.db-wal`(0바이트)·`-shm`·`data/runtime_cache.db` 의 수정 시각과 `services/scheduler.lock` 이 바뀌었다. 스케줄러 첫 실행 예정은 10:24:35 였으나 프로세스가 10:23:35 에 끝나 잡은 돌지 않았고, `paper_trading.db` 본 파일은 9/21 그대로다. 기동 초기화 값만 쓰였으며 백업이 없어 수정 시각은 되돌리지 않았다. 이후 실측은 `create_app` 없이 대상 클래스만 부르거나 사본에서 한다

## [INFRA-111] Market Gate 유효성 판정의 섹터 전부 결측 지적 조사 종결
- 완료 2026-09-25 10:24 | 티어 T1 (테스트만 변경, 위험 경로 없음) | 커밋 ce74742
- 조사: `_is_market_gate_data_structurally_valid`(`services/kr_market_market_gate_validity.py:26`)는 `status != "분석 대기 (Neutral)"` · 섹터 목록 있음 · `total_score != 50` 의 OR 이다. 「분석 대기 (Neutral)」는 분석 실패 때의 기본 결과(`build_default_result`, 섹터 없음)에만 쓰이므로 정상 분석은 상태 조건만으로 유효다. 섹터 조건을 좁혀도 실제 판정은 바뀌지 않는다. 섹터가 전부 결측이어도 기술 점수는 로컬 가격으로 계산되고, 스냅샷 대체는 다른 날짜 자료를 보이게 하므로 유효 판정이 맞다. 표본 하한 `len(changes) < 4` 는 잡음 방지로 유지하며 급락은 `intraday_penalty`(지수 또는 가격 자료의 KOSPI 등락률)가 따로 막는다
- 결론: 결함 아님. 코드 불변, 현재 동작을 고정하는 테스트 1개 추가. TODO 의 전제(섹터 목록만으로 유효 판정)가 틀렸음을 기록
- 변경: tests/services/test_kr_market_market_gate_service_refactor.py(섹터 전부 `change_pct: null` 인 분석이 유효이고 스냅샷 로더를 부르지 않음). 코드 +25줄(테스트)
- 설계: bounded, T1. 2026-09-25 10:17 사용자 「테스트만 추가 후 종결」 선택
- 리뷰: `/ponytail-review` 「Lean already. Ship.」
- 테스트: 격리 사본에서 판정을 「유한한 섹터 값이 하나 이상」으로 바꾼 mutation 에 새 테스트가 `assert False is True` 로 실패(exit 1) 확인, 사본 삭제
- 검증: 해당 파일 11 passed(exit 0), `venv/bin/python -m pytest -q -p no:cacheprovider` 2811 passed 2 skipped(exit 0)
- QA: 테스트만 바꾸는 작업이라 동적 QA 제외(`tier-rules.md` §1-1)

## [INFRA-105] Market Gate 섹터 ETF 조회 실패를 0.0 대신 결측으로 남김
- 완료 2026-09-25 10:16 | 티어 T3 (위험 경로 `engine/market_gate_fetchers_external.py`·`engine/market_gate_logic_scoring.py`) | 커밋 fe532dc
- 원인: `get_sector_data` 가 pykrx 조회 실패·빈 결과·한 줄 결과에 `0.0` 을 넣었다. `_calculate_sector_crash_penalty` 는 결측을 걸러 내도록 되어 있었지만 0 은 결측이 아니어서 평균과 하락 비율을 희석했다. QA 하네스에서 11개 중 5개 -3%·6개 실패로 재현(감점 0, 총점 80 「강세장」, 화면 `+0.00%` 6개). KOSPI 200 조회 실패의 0.0 은 `_resolve_kospi_change` 의 두 번째 우선순위를 차지해 가격 자료 폴백도 막았다
- 변경: engine/market_gate_fetchers_external.py(세 갈래 `None`, 빈 결과 WARNING 로그), engine/market_gate_logic_scoring.py(`build_sector_signals` 가 `None` 을 `Neutral`·null 로), frontend/src/app/dashboard/kr/page.tsx(`change_pct == null` 이면 회색 배경·`—`·`title="조회 실패"`), frontend/src/lib/api.ts(`KRSector.change_pct: number | null`), 테스트 넷, docs/dev-cycle/qa/INFRA-105.md. 과거 저장분의 0.0 은 실패였는지 구분할 수 없어 되돌리지 않음. 코드 +155 -13줄
- 설계: bounded, T3. 2026-09-25 09:57 사용자 「진행해」
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · `.claude/skills/closing-bet-nextjs/SKILL.md` · 번들 문서 `01-app/01-getting-started/05-server-and-client-components.md`(순수 클라이언트 렌더 변경) · `/ponytail-review` 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): 1 결측 카드가 보합 노란 배경 → 회색·툴팁 반영 | 2 감점 하한 4·임계값 리터럴 → 기존 코드라 미반영 | 3 전부 결측이어도 유효 판정 → `[INFRA-111]` | 4 KOSPI 200 결측 폴백 단언 없음 → 반영 | 5 빈 줄 → 반영 | 6 NaN 등락률 → `[INFRA-112]` · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) APPROVE, Critical·Important 0, Minor 4: 1 `=== null` 은 키 누락 undefined 를 놓침 → `== null` 반영 | 2 챗봇 호출자 고정 테스트 없음 → `tests/chatbot/test_payload_service.py` 에 None 섹터 사례 반영 | 3 디버그 스크립트 `None%` → 영향 없어 미반영 | 4 유효 값 셋 이하면 감점 0 → `[INFRA-111]` 에 기록
- 테스트: pytest 새 파일 `tests/engine/test_market_gate_fetchers_external_refactor.py`(실패·빈 결과·한 줄 결과가 `None`, 실제 등락률 반올림 유지), `tests/engine/test_market_gate_analysis_refactor.py` 부분 실패 사례(오류 없음, 감점 30, 결측 6개 `Neutral`, `kospi_change` 폴백 2.1), 챗봇 payload 사례. vitest `page.regression-infra-105.test.tsx`(결측 카드 `—`·회색, 조회된 카드 `-3.21%`). 세 새 테스트 모두 수정 전 RED 확인
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2810 passed 2 skipped(exit 0, 리뷰 반영 뒤), `npx vitest run` 102파일 695 passed(exit 0), `npm run type-check` exit 0, 바꾼 파일 eslint 경고 19(기준과 같음)
- QA: `docs/dev-cycle/qa/INFRA-105.md` 필수 4/4 통과(격리 사본 둘, 더미 비밀, 가짜 pykrx·소켓 차단). 실제 `MarketGate.analyze`·`save_analysis` 로 수정 전 감점 0·총점 80, 수정 뒤 감점 30·총점 50·저장값 null 을 확인. agent-browser 로 대시보드 결측 카드 6개가 수정 전 `+0.00%`, 수정 뒤 회색 `—`. 챗봇 문맥은 수정 뒤 조회된 5개만 담음. 사본 Next 첫 기동은 node_modules 심볼릭 링크를 Turbopack 이 거부해 클론 복사로 바꿔 다시 띄움
- 이월: `[INFRA-111]` 유효성 판정이 전부 결측이어도 유효로 봄(감점 표본 하한 함께), `[INFRA-112]` NaN 등락률이 화면에 「NaN%」
- 정리: 브라우저 세션 둘 닫음, 사본 서버 정지(58101·58102 수신 0), 사본 둘 삭제, 원본 `data/` 수정 시각 불변

## [INFRA-104] 같은 워커의 재시작을 스레드가 파이프라인에 들어가기 전부터 거부함
- 완료 2026-09-25 09:56 | 티어 T3 (위험 경로 `services/common_update_status_service.py`) | 커밋 80978fa
- 원인: `start_update` 는 실행 시각을 워커 공용 `LOCAL_RUN_START_TIME` 에만 남기고, 같은 워커의 재시작을 막는 `LOCAL_PIPELINE_ACTIVE` 는 스레드가 파이프라인에 들어간 뒤에야 켜졌다. 그 틈에 다른 워커의 중단과 같은 워커의 재시작이 겹치면 두 실행이 함께 돌고, 옛 스레드가 새 실행의 시각을 읽어 `[INFRA-097]` 감시가 옛 실행을 멈추지 않고 `[INFRA-101]` 항목 가드와 `[INFRA-098]` 완료 가드가 옛 실행의 쓰기를 통과시켰다. QA 하네스에서 재현(두 번째 시작 `True`, 옛 스레드가 새 실행을 `isRunning:false` 로 내림)
- 변경: services/common_update_status_service.py(`start_update` 가 잠금 안에서 `LOCAL_PIPELINE_ACTIVE = True`, 스레드 시작 실패 시 그 워커가 재기동까지 시작을 거부하는 한계를 `ponytail:` 주석으로), services/common_update_service.py(감시 스레드를 try 안에서 띄우고 시작된 스레드만 `watcher` 에 대입, 감시 주석 갱신), tests/app/test_common_update_service.py, docs/dev-cycle/qa/INFRA-104.md. 다른 워커의 재시작은 종전처럼 `startTime` 가드가 가림. 코드 +68 -9줄
- 설계: bounded, T3. 해법 (B). (A) 실행 시각을 스레드 인자로 넘기는 안은 같은 워커의 두 실행 동시 진행을 막지 못해 제외. 2026-09-25 09:45 사용자 「진ㅇ해」(진행해)
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · `/ponytail-review` 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): 감시 스레드 `start()` 가 try 밖이라 실패 시 이번 변경으로 플래그가 고착됨 → try 안으로 옮기고 시작된 스레드만 대입·테스트 | 주석 「이 갈래」 모호 → 대상 명시. 재검토 APPROVE(max none) · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) Critical·Important 0, Minor 3: 1·2 는 위와 같아 반영 | 3 시작부터 해제까지 잇는 왕복 테스트 없음 → 해제는 기존 `[INFRA-099]` 테스트가 덮어 미반영
- 테스트: pytest 2건 추가(시작 뒤 파이프라인 진입 전 같은 워커의 재시작 거부와 상태·시각 불변, 감시 스레드 시작 실패에도 `finish_update` 호출과 플래그 해제. 각각 수정 전 RED 확인), 기존 두 번 시작 테스트에 앞 파이프라인 종료(플래그 해제) 전제를 명시
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2808 passed 2 skipped(exit 0, 리뷰 반영 뒤). 프런트엔드 변경이 없어 vitest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-104.md` 필수 4/4 통과(격리 사본 둘, 더미 비밀). 서비스 하네스로 틈을 재현해 수정 전 결함, 수정 뒤 거부·옛 스레드의 자기 시각·해제 뒤 재시작을 확인. agent-browser 로 `QA Noop` 항목 시작 두 번이 모두 200 이고 플래그가 남지 않음, 실행 중 「업데이트」 409 중복 모달. 수정 전 사본 브라우저 첫 시도는 하네스 순서 문제로 무효 처리하고 다시 실행
- 이월: 없음
- 정리: 브라우저 세션 둘 닫음, 사본 서버 정지(58101·58102 수신 0), 사본 둘 삭제, 원본 `data/` 수정 시각 불변

## [INFRA-103] 데이터 상태 화면의 진행 폴링이 조회 실패에 멈추고 요청을 겹쳐 보내지 않음
- 완료 2026-09-25 09:43 | 티어 T2 (frontend 페이지 하나, 위험 경로 아님) | 커밋 4a4779b
- 원인: `pollUpdateStatus` 의 catch 가 `console.error` 만 남기고 500ms interval 을 유지했다. 요청마다 30초 timeout 이라 백엔드가 응답하지 않으면 끝나지 않은 요청이 계속 쌓였다. QA 에서 재현(Flask 정지 뒤 3초에 6건 증가, SIGSTOP 5초에 10건)
- 변경: frontend/src/app/dashboard/data-status/page.tsx(`POLL_FAILURE_LIMIT = 3`, 실패 횟수·진행 중 ref, 연속 3회 실패 시 interval 정지·진행 표시 해제·「업데이트 오류」 모달, 성공과 `startPolling` 에서 초기화, 진행 중이면 tick 건너뜀), 회귀 테스트 `page.regression-INFRA-103.test.tsx`, docs/dev-cycle/qa/INFRA-103.md. 간격·timeout·다른 두 폴링·백엔드는 그대로. 코드 +106줄
- 설계: bounded, T2. 2026-09-25 09:32 사용자 「진행해」
- 리뷰: `.claude/skills/closing-bet-nextjs/SKILL.md` · `vendor/skills/vercel-react-best-practices/rules/rerender-use-ref-transient-values.md` · 번들 문서 `01-app/01-getting-started/05-server-and-client-components.md` · `/ponytail-review` 「Lean already. Ship.」, 새 상수가 기존 409 주석과 그 대상 사이에 끼어든 배치만 고침 · `closing-bet-reviewer` APPROVE(max low): low-1 마운트 조회가 걸린 채 시작하면 새 세션이 실패 2회에 멈출 수 있음 → 창이 좁고 결과가 조기 알림뿐이라 미반영 | low-2 늦게 도착한 마운트 응답이 새 interval 을 끊음(종전 동작) → `[INFRA-110]` 이월 | low-3 unmount 뒤 setModal → React 에서 no-op 이라 미반영 | low-4 마운트 실패·409 재부착 테스트 공백과 실제 타이머 의존 → 코드상 맞게 동작하고 409 재부착은 QA S-3 으로 확인해 미반영
- 테스트: vitest 3건(연속 3회 실패 뒤 요청 정지와 모달, 중간 성공 시 횟수 초기화, 진행 중 조회가 있으면 다음 조회 없음. 수정 전 3/3 RED 확인)
- 검증: `npx vitest run` 101파일 694 passed(exit 0), `npm run type-check` exit 0, `npm run lint` exit 0(경고 183, 수정 전과 같음). 파이썬 변경이 없어 pytest 는 돌리지 않음
- QA: `docs/dev-cycle/qa/INFRA-103.md` 필수 4/4 통과(agent-browser, 격리 사본 둘, 더미 비밀). 수정 뒤 Flask 정지 4초 안에 모달과 진행 표시 해제, 이후 요청 증가 0, SIGSTOP 5초 동안 조회 1건, 409 뒤 폴링 재부착. 수정 전 사본에서 두 결함 재현. 첫 시도는 토큰 파일 부재와 S-2 계수 방식 문제로 무효 처리하고 다시 실행
- 이월: `[INFRA-110]` 마운트 조회의 늦은 `isRunning:false` 가 새 폴링을 끊음
- 정리: 브라우저 세션 둘 닫음, 사본 서버 정지(58101·58102 수신 0), 사본 둘 삭제, 원본 `data/` 수정 시각 불변

## [VCP-052] VCP AI JSON 병합과 가격 동기화를 파일 잠금 하나로 직렬화함
- 완료 2026-09-25 09:30 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 4ffb906
- 원인: 수동 갱신·수집·재분석이 함께 쓰는 `_write_ai_analysis_files` 가 날짜 파일과 최신 파일을 잠금 없이 다시 읽고 병합해 교체했다. 겹치면 나중에 쓴 쪽이 먼저 쓴 쪽의 판정을 지웠다. 가격 동기화 `update_kr_ai_analysis_prices` 는 잠금 없이 `open('w')` 로 최신 파일을 직접 썼다. QA 에서 두 유실을 모두 재현
- 변경: services/common_update_ai_analysis_service.py(읽기·병합·저장 전체를 `data/kr_ai_analysis.json.lock` 안에서, 본문은 `_merge_ai_analysis_files` 로 분리), scripts/init_data.py(가격 동기화를 같은 잠금 안에서 다시 읽고 `atomic_write_text` 로 저장, 예외는 종전대로 WARNING), services/common_env_service.py(`_env_file_lock` docstring 에 새 사용처), tests/services/test_vcp_signals_log_lock_refactor.py, docs/dev-cycle/qa/VCP-052.md. 워커 메모리 `vcp_status` 는 범위 밖. 코드 +82 -24줄
- 설계: bounded, T3. 2026-09-25 09:19 사용자 「진행해」
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): low 1 docstring 사용처 → 반영 | low 2 잠금 이름 불일치 → `ai_analysis_lock` 으로 반영 | low 3 JSON 없어도 잠금 파일 생성 → `data/*.lock` 이 덮어 미반영 | low 4 파일 없음 갈래 테스트 → 한 줄 갈래라 미반영 · 심층 리뷰(`oh-my-claudecode:code-reviewer`) 차단 0: Minor 1 0.3초 판정 거짓 통과 가능성 → 기존 헬퍼 방식, import 57ms 라 미반영 | Minor 2 docstring → 반영 | Minor 3 private 이름 가져오기 → 기존 선례와 같아 미반영 · 리뷰어가 결함 주입 셋(병합 잠금 제거·경로 변경·가격 동기화 잠금 제거)을 새 테스트가 잡는 것을 확인
- 테스트: pytest 2건(잠금을 쥔 동안 병합이 기다리고 그 사이 다른 쪽이 더한 종목을 보존, 가격 동기화가 기다리고 다시 읽어 보존하며 `atomic_write_text` 로 저장. 수정 전 둘 다 RED 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2806 passed 2 skipped(exit 0, 리뷰 반영 뒤), `npm run test` 100파일 691 passed(exit 0). 리뷰 반영 전 첫 pytest 실행은 실패 0건인데 exit 1 로 기록되었고, 같은 상태의 재실행은 exit 0 이었다(원인 미확인)
- QA: `docs/dev-cycle/qa/VCP-052.md` 필수 2/2 통과(격리 사본 둘, 실제 함수를 프로세스 둘로 겹쳐 실행). 수정 전 날짜 파일에 `['000660']` 만 남고(005930 판정 유실) 최신 파일에서 가격 동기화가 사라짐(결함 재현), 수정 뒤 `['000660', '005930']` 과 005930 가격 110 보존. 겹침을 화면 조작으로 만들 수 없어 브라우저 대상 아님
- 이월: `[VCP-058]` `vcp_signals_latest.json` 직접 쓰기, `[VCP-059]` 병합이 최신 파일의 가격 동기화 값을 되돌림(종전 동작)
- 정리: 사본 둘 삭제, 서버 기동 없음, 원본 `data/` 수정 시각 불변. 저장소 밖으로 잘못 돌린 vitest 출력 파일 하나(`/Users/freelife/vibe/lecture/hodu/v052_dummy`) 삭제 확인

## [VCP-051] 시그널 시세 갱신이 청산 행의 실현 수익률을 덮지 않음
- 완료 2026-09-25 09:14 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 4779f00
- 원인: `update_vcp_signals_recent_price` 의 잠금 안 적용 루프가 `status` 를 보지 않고 가격을 얻은 종목의 모든 행에 `current_price`·`return_pct` 를 덮었다. `engine/signal_tracker_log_helpers.py` 가 청산 때 기록한 실현 수익률이 다음 갱신에서 사라졌다. QA 에서 재현
- 변경: scripts/init_data.py(`status == 'CLOSED'` 행은 건너뜀, 열 없음·빈 값은 종전대로 갱신, 가격 조회와 `kr_ai_analysis.json` 동기화는 그대로), tests/services/test_vcp_signals_log_lock_refactor.py, docs/dev-cycle/qa/VCP-051.md. 이미 덮인 운영 이력 복구와 과거 날짜 청산 행 표시 정책은 범위 밖. 코드 +28 -1줄
- 설계: bounded, T3. 2026-09-25 09:10 사용자 「진행해」
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): low 1 청산 행만 남은 종목도 시세를 조회함(종전 동작, 그 가격은 `kr_ai_analysis.json` 동기화가 씀) → 미반영 | low 2 `update_kr_ai_analysis_prices` 가 상태를 보지 않음 → 로컬 JSON signals 에 `status`·`return_pct` 키가 없어 해당 없음 · 심층 리뷰(`oh-my-claudecode:code-reviewer`) 차단 0: 참고 1 `PENDING`·`CANCELLED` 도 갱신 → `signals_log.csv` 에 쓰는 경로가 없어 미반영 | 참고 2 빈 status 테스트 없음 → 열 없음은 기존 테스트, 빈 값은 QA 로 확인
- 테스트: pytest 1건(같은 종목의 CLOSED·OPEN 행, CLOSED 값 보존·OPEN 갱신, 수정 전 RED 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2804 passed 2 skipped(exit 0), `npm run test` 100파일 691 passed(exit 0)
- QA: `docs/dev-cycle/qa/VCP-051.md` 필수 2/2 통과(격리 사본 둘, 실제 함수 실행, pykrx 가짜 모듈). 수정 전 청산 두 행이 `110 / 10.0`·`180 / -10.0` 으로 덮임(결함 재현), 수정 뒤 `93 / -7.0`·`230 / 15.0` 보존. OPEN·빈 status 행은 두 사본이 같게 갱신. 화면이 이 값을 보이지 않아 브라우저 대상 아님
- 이월: 없음
- 정리: 사본 둘 삭제, 서버 기동 없음, 원본 `data/` 수정 시각 불변

## [VCP-057] VCP 수급 결측의 남은 0 기본값을 None 으로, 실제 0 을 중립색으로 표기함
- 완료 2026-09-25 08:50 | 티어 T2 (`engine/screener*.py`·`frontend/src/app/dashboard/kr/vcp/page.tsx`, 위험 경로 밖) | 커밋 72e688a
- 원인: `[VCP-050]`·`[VCP-054]` 가 수급 결측을 None 으로 바꾼 뒤에도 남은 곳이다. VCP 표의 `supplyColorClass` 는 0 을 매도와 같은 `text-red-400` 으로 그렸고(QA 에서 재현), `_score_supply_core` 는 details 첫 행에 키가 없으면 1일 값을 0 으로 쓰고 값이 None 이면 `None > 0` 에서 TypeError 가 났다. 두 수동 스크립트는 None 에서 서식 오류가 났고, `engine/screener.py` 의 `ScreenerResult` 는 쓰는 곳이 없었다
- 변경: frontend/src/app/dashboard/kr/vcp/page.tsx(실제 0 은 `text-gray-300`, 결측·매수·매도 불변), engine/screener_scoring_helpers.py(1일 값은 키·값이 없으면 None, 연속 매수는 그 행에서 멈춤, 점수 불변), engine/screener.py(미사용 `ScreenerResult` 삭제), tests/test_vcp.py·scripts/diagnose_screener.py(None·NaN 을 `-` 로 출력), 테스트 두 파일, docs/dev-cycle/qa/VCP-057.md. `engine/kr_ai_data_service.py:436-437` 은 읽는 `KrAiAnalyzer` 가 수급 값을 쓰지 않아 바꾸지 않음. 코드 +44 -30줄
- 설계: bounded, T2. 2026-09-25 08:40 사용자 「진행해」
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · `.claude/skills/closing-bet-nextjs/SKILL.md` · `frontend/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`(`'use client'` 페이지의 클래스만 바뀌는 순수 클라이언트 변경이라 이 문서로 판정, vendor 스킬 해당 없음) · 과잉설계 직접 검토: shrink 1건(1일 값 변환 4줄 → 한 식) 반영 · `closing-bet-reviewer` APPROVE(max low): low 1 `tests/test_vcp.py` 가 DataFrame 을 거친 NaN 을 `nan` 으로 출력 → `pd.isna` 로 판정 | low 2 details 정규화기 둘이 결측을 0 으로 채우거나 행을 버림 → 범위 밖, `[INFRA-109]` 에 추가 | low 3 음수 억 단위 내림(종전 동작) → 미반영
- 테스트: pytest 1건(details 첫 행 값 결측 → 1일 값 None·점수 0, 수정 전 TypeError 로 RED), vitest 1건(`page.regression-vcp-054.test.tsx` 실제 0 칸이 `text-gray-300`·빨강 아님, 수정 전 RED)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2803 passed 2 skipped(exit 0), `npm run test` 100파일 691 passed, `npm run lint`·`npm run type-check` exit 0(모두 커밋 72e688a 상태)
- QA: `docs/dev-cycle/qa/VCP-057.md` 필수 2/2 통과(격리 사본 둘, 가짜 비밀, agent-browser). 수정 전 SK 기관 실제 0 이 `text-red-400`(결함 재현), 수정 뒤 `text-gray-300`. 결측 `-` 회색·매도 빨강·매수 초록과 나머지 행은 두 사본이 같음(칸별 비교 차이 1건)
- 이월: `[INFRA-109]` 에 details 정규화기 두 곳 추가
- 정리: 사본 둘 삭제, 사본 서버 넷 종료·리스너 0, 원본 `data/` 수정 시각 불변, 3500·5501 리스너 0

## [VCP-056] 가격 값이 빈 프레임을 분석 불가로 셈
- 완료 2026-09-25 08:29 | 티어 T2 (`engine/screener.py`, 위험 경로 밖) | 커밋 279e841
- 원인: 스크리너 `_prepare_stock_analysis` 는 원래 행 수만 20 과 비교했다. `engine/vcp.py` `_normalize_price_frame` 은 high·low·close·volume 중 하나라도 빈 행을 버리므로, 값이 빈 프레임은 예외가 아니라 「Invalid or empty price frame」(또는 「Insufficient data」) 실패 결과가 되어 `[VCP-053]` 의 분석 수에 들어갔다. 가격 파일 형식이 전 종목에서 깨지면 「VCP 조건 충족 종목 없음」으로 그 날짜의 기존 시그널이 지워졌다. QA 에서 재현
- 변경: engine/screener.py(사전 검사가 `_normalize_price_frame` 이 남긴 행 수를 `VCP_THRESHOLDS.MIN_DATA_POINTS` 와 비교), tests/engine/test_screener_vcp_gate_refactor.py, tests/engine/test_screener_supply_batch.py, docs/dev-cycle/qa/VCP-056.md. `detect_vcp_pattern` 계약과 위험 경로 Phase1 은 바꾸지 않음. 코드 +41 -4줄
- 설계: bounded, T2. 2026-09-25 08:19 사용자 「진행해」. `detect_vcp_pattern` 이 예외를 올리는 안은 위험 경로 Phase1 의 동작을 바꿔 기각
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 직접 검토 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): low 1 사전 검사의 리터럴 20 이 판정 상수와 따로 놂 → 상수로 바꿈 | low 2 high 가 전부 실제 0 이면 「Invalid recent high」 결과가 분석 수에 들어감(확신도 낮음) → 미반영, 실제 0 은 유효 값이고 0 으로 채우는 수집 경로 없음 | nit 2건 → 수정 불필요
- 테스트: 2건 추가(high 전부 NaN → 예외, 수정 전 RED 확인 / 일부 NaN·유효 20행 → 분석). `[VCP-053]` 판정 예외 테스트는 close 만 있던 프레임이 새 사전 검사에서 걸러져 판정까지 가지 않으므로 네 열 프레임으로 바꾸고 판정 호출을 단언. `test_screener_supply_batch.py` 두 건은 기대값을 두고 입력을 네 열 프레임으로 맞춤
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2802 passed 2 skipped(exit 0, 커밋 279e841 상태)
- QA: `docs/dev-cycle/qa/VCP-056.md` 필수 3/3 통과(격리 사본 둘, 실제 `create_signals_log(run_ai=False)` 를 부르고 `MarketGate.analyze` 만 가짜, 소켓 차단). 전 종목 high 결측: 수정 전 `True`·기존 시그널 삭제(결함 재현), 수정 뒤 `False`·기존 시그널 보존·「분석 가능한 종목이 없습니다」. 일부 결측과 결측 없음은 두 사본 결과 동일. 브라우저는 진입점이 실제 수집·LLM 을 일으키는 금지 조작이고 결함이 가격 파일 값의 체계적 결측에서만 드러나 하네스로 대체
- 이월: 없음
- 정리: 사본 셋(`p056`·`q056old`·`q056new`) 삭제, 서버 기동 없음, 원본 `data/` 수정 시각 불변·새 잠금 파일 없음, 3500·5501 리스너 0

## [INFRA-095] 수급 수집이 결측을 0 으로 저장하지 않음
- 완료 2026-09-25 08:16 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 47e6179
- 원인: pykrx 경로는 외국인·기관 두 프레임의 합집합을 저장해 한쪽에만 있는 종목의 다른 쪽을 0 으로 썼고(한쪽 조회가 통째로 비면 그 날짜 전 종목이 0), 값이 NaN 이면 `int()` 예외로 그 날짜 전체를 잃었다. Toss 백필은 빈 종가·순매수 수량을 `or 0` 으로 0 으로 만들었다. `[INFRA-093]` 뒤로 백필이 기존 행을 덮지 않아 이 0 이 남았다. QA 에서 세 경우 모두 재현
- 변경: scripts/init_data.py(pykrx 는 두 프레임 교집합만 저장하고 NaN 값 종목은 건너뜀, `[INFRA-094]` 의 재수집 날짜 전용 교집합 갈래를 흡수, 한쪽 프레임만 비면 WARNING. Toss 는 종가·수량이 없거나 숫자가 아니거나 유한하지 않거나 종가가 0 이하인 행을 버리고 실제 0 수량은 저장), tests/scripts/test_init_data_vcp_scheduler.py, docs/superpowers/plans/2026-09-25-infra-095-supply-missing-not-zero.md, docs/dev-cycle/qa/INFRA-095.md. 읽는 쪽 코드와 기존 저장분의 0 은 바꾸지 않음. 코드 +131 -26줄
- 설계: bounded, T3. 2026-09-25 07:56 사용자 「진행해」. 빈 칸(NaN) 저장안은 읽는 쪽 여덟 곳 이상의 감사가 필요해 기각
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS, 필수 0: 권장 1 기관 수량 `nan` 변이 생존 → 테스트 행 추가 | 권장 2 계획 Step 5·Review Focus 가 Ruling 과 어긋남 → 정리 | 권장 3 한쪽 조회 실패 날짜가 조용히 영구히 빔 → WARNING·한계 4 | 참고 첫 변이 확인을 작업 트리에서 돌림 → 재확인은 스크래치 사본 · 과잉설계 직접 검토 「Lean already. Ship.」, 변이가 살아남은 `순매수거래대금` 열 검사는 기존 `except` 와 결과가 같아 삭제 · `closing-bet-reviewer` APPROVE(max low): low 1 창이 과거로 밀림 → 한계 2 문구 | low 2 휴장일 무경고 테스트 → 추가 | low 3 중복 인덱스(기존) → 한계 5 | 범위 밖 → `[INFRA-108]` · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) APPROVE, Critical·Important 0: M1 Toss 백필 False 경로 테스트 → 추가 | M2 Toss 잠정 0 → 한계 6 | M3 계획 번호·이름 → 정리 | M4 메모리 파서의 0 기본값 → `[INFRA-109]`
- 테스트: 7건 추가(한쪽 프레임 종목, NaN 값·실제 0, 열 없는 날짜, 한쪽 빈 프레임 경고, 휴장일 무경고, Toss 최신일 결측 시 `False`, Toss 행의 빈 필드·nan·inf·실제 0). 수정 전 코드에서 휴장일 외 6건 FAIL. 변이 9종(교집합→합집합, `pd.isna` 제거, 종가·외국인·기관 `isfinite` 각각 제거, 외국인·기관 수량 `or 0` 복원, `close > 0` 제거, 한쪽 빈 프레임 경고 제거)과 휴장일 조건 `or` 변이 각각 FAIL. 종가 `or 0` 복원은 동등 변이
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2800 passed 2 skipped(exit 0, 커밋 47e6179 상태)
- QA: `docs/dev-cycle/qa/INFRA-095.md` 필수 5/5 통과(격리 사본 둘, 실제 `create_institutional_trend` 를 부르고 pykrx·Toss 만 가짜, 소켓 차단). 한쪽 프레임 종목: 수정 전 (40, 0) 저장(결함 재현)·수정 뒤 없음. Toss 빈 필드: 수정 전 (0, 0)·(0, 2000) 저장(결함 재현)·수정 뒤 실제 0 행만. 한쪽 빈 프레임: 수정 전 전 종목 기관 0·`True`(결함 재현)·수정 뒤 저장 없음·WARNING·`False`. 결측 없는 실행과 휴장일은 두 사본 결과 동일. 브라우저는 진입점이 실제 수집·LLM 을 일으키는 금지 조작이고 결함이 외부 응답의 결측 모양에서만 드러나 하네스로 대체
- 이월: `[INFRA-108]`(Market Gate 시장 수급 점수가 임의 종목 한 행을 씀), `[INFRA-109]`(Toss 메모리 파서의 0 기본값)
- 정리: 사본 넷(`q095old`·`q095new`·`m095`·`i095`) 삭제, 서버 기동 없음, 원본 `data/all_institutional_trend_data.csv` 수정 시각 불변·잠금 파일 없음, 3500·5501 리스너 0

## [INFRA-098] 수동 업데이트 상태 파일의 읽고-수정-쓰기를 워커 사이에서 직렬화함
- 완료 2026-09-25 07:50 | 티어 T3 (위험 경로 `services/common_update_status_service.py`) | 커밋 8c75ba5
- 원인: `update_lock` 은 프로세스 안의 `threading.Lock` 이다. (a) 작업 워커의 `update_item_status` 가 상태를 읽고 쓰는 사이 다른 워커의 `stop_update` 가 쓰면 앞 워커의 저장이 `stopRequested` 를 덮었다. (b) 파이프라인 `finally` 가 `startTime` 을 확인한 뒤 잠금 없이 `finish_update` 를 불러, 그 사이 다른 워커의 새 실행을 끝낼 수 있었다. (c) 상태 변경도 `(mtime_ns, size)` 캐시로 읽어 같은 틱·같은 크기의 저장을 놓칠 수 있었다. (a) 는 QA 에서 중단이 파이프라인까지 사라지는 것으로 재현(S-1b)
- 변경: services/common_update_status_service.py(`_status_file_lock` 이 `<상태 파일>.lock` 에 `fcntl.flock(LOCK_EX)`, 열기 실패 시 로그 뒤 잠금 없이 진행. `_read_status_file` 이 캐시 없이 파일을 읽고 없거나 깨지면 SQLite 스냅샷. 네 함수가 `with update_lock, _status_file_lock(...)`. `finish_update` 에 선택 인자 `start_time`, 잠금 안에서 다른 `startTime` 이면 끝내지 않음), services/common_update_service.py(`finally` 의 바깥 비교 제거), app/routes/common.py(`finish_update` 에 `LOCAL_RUN_START_TIME` 을 묶음, 수동 `/system/finish-update` 는 종전처럼 무조건), tests 두 파일, docs/superpowers/plans/2026-09-25-infra-098-update-status-file-lock.md, docs/dev-cycle/qa/INFRA-098.md. 폴링 GET 은 바꾸지 않음. 코드 +268 -17줄
- 설계: bounded, T3. 2026-09-25 07:34 사용자 「진행해」. 중단 요청 별도 파일안은 `stop_update` 도 같은 파일을 읽고 써야 해서 기각
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` REVISE: 필수 1 잠금 파일 열기 실패가 500·`isRunning` 고착 → 로그 뒤 진행·테스트 | 필수 2 finish·start 경합 테스트 없음(변이 M3·M5 통과) → 테스트 추가 | 권장 3 옛 캐시 테스트 finish 만 → 세 경우 | 권장 4·참고 6 → 계획 한계 | 권장 5 기동 초기화 → `[INFRA-107]` | 참고 7 → 계획 문구 · 과잉설계 직접 검토: 중첩 `with` 를 한 줄로 합쳐 들여쓰기 변경 제거 · `closing-bet-reviewer` APPROVE(max low): low 2건 → 계획 한계 7·Step 3 주석 | info 2건 → 한계 8·아래 I-1 · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) Critical 0: Important I-1 상태 파일이 없으면 기본값(`startTime` 없음)에 중단을 저장해 실행 워커의 감시가 알아보지 못함 → 스냅샷 폴백·테스트 RED→GREEN | M-1 `except` 안 `yield` → 밖으로 | M-2 무타임아웃 flock → 한계 8 | M-3 스레드 생존 미단언 → 단언 | M-4 `start_time=None` 계약 → 단계 추가
- 테스트: 서비스 7건(파라미터 포함) 추가(다른 워커 중단 보존, finish 중 다른 워커 start 보존, 대체된 실행 finish 무시·수동 finish 무조건, 옛 캐시 finish·item·stop, 잠금 파일 불가, 파일 없을 때 스냅샷), 라우트 배선 1건. 파이프라인 테스트 2건은 판정이 `finish_update` 로 옮겨져 기대값을 `[True]` 로 바꾸고 판정은 서비스 테스트가 맡음. 변이 7종(flock 제거, item·stop 캐시 읽기, finish 가드 제거, start 잠금 제거, finish 비교를 잠금 밖으로, 열기 실패 미처리) 각각 해당 테스트 FAIL
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2793 passed 2 skipped(exit 0, 커밋 8c75ba5 상태)
- QA: `docs/dev-cycle/qa/INFRA-098.md` 필수 4/4 통과(격리 사본 둘, 실제 `app.routes.common` 배선을 부르는 프로세스 둘이 사본 `data/` 공유, 가짜 수집기, 소켓 차단). 항목 저장 틈의 중단: 수정 전 파일의 `stopRequested` false 로 덮임, 감시 주기 3초 변형(S-1b)에서 A 가 중단을 보지 못하고 끝까지 돎(결함 재현), 수정 뒤 B 가 1.5초 기다린 뒤 중단이 남고 A 가 멈춤. finish 중 재시작: 수정 전 새 실행 `isRunning` false(결함 재현), 수정 뒤 true·거른 finish 1회. 경합 없는 실행 종전과 같음. S-1 은 감시 주기(1초)가 하네스 틈(1.5초)보다 짧아 수정 전에도 A 가 중단을 봐 기대와 달랐고, 그래서 S-1b 를 더함(기록). 브라우저는 진입점이 실제 수집·LLM 을 일으키는 금지 조작이고 결함이 워커 둘의 틈에서만 드러나 하네스로 대체
- 이월: `[INFRA-107]`(워커 기동 초기화가 다른 워커의 실행 중 상태를 지움)
- 정리: 사본 둘과 critic 의 스크래치 사본 삭제, 서버 기동 없음, 원본 `data/update_status.json` 수정 시각 불변·잠금 파일 없음, 3500·5501 리스너 0

## [INFRA-089] KRX 가 끊은 세션을 곧바로 다시 로그인함
- 완료 2026-09-25 07:29 | 티어 T3 (모든 KRX 조회가 거치는 의존성 수정판 교체, `requirements.txt` 핀 변경) | 커밋 b3de9c6
- 원인: KRX 서버가 세션을 끊으면 데이터 요청은 `400` + 본문 `LOGOUT` 을 받는다(07:07 실측, 잘못된 기간은 `400 INVALIDPERIOD`, 잘못된 요청 종류는 `302`). pykrx 는 클라이언트 쪽 1시간 타이머로만 재로그인해, 끊긴 세션으로 계속 요청했다. 운영 09-23 17:0x 종가베팅의 수급 조회 41건이 이렇게 빈 결과가 됐다(운영 로그, 워커 출력이 종료 때 한꺼번에 기록되어 시각은 같은 프로세스 안의 순서로만 판정). 서버가 끊은 이유는 재현되지 않았다(중복 로그인·만료·동시 재로그인·두 번째 프로세스 로그인 모두 첫 세션 유지)
- 변경: vendor/pykrx/transport.patch(`KRXSession._request` 가 `400`+`LOGOUT` 에 재로그인 1회 뒤 같은 요청 재전송, `_refresh_session` 이 모듈 잠금 안에서 `login_time` 이 바뀌었으면 로그인하지 않음, 실패 뒤 60초 `monotonic` 차단, 로그인 뒤 60초 안의 LOGOUT 은 재로그인하지 않음, 타이머·세션 없음 갈래도 같은 잠금·차단, 실패 출력에 비밀 아닌 범주, `print(flush=True)`, `refresh()` 가 `login_time` 을 마지막에 씀), wheel `+cookie.2` → `+cookie.3`(SHA `4af4baf6…`, 두 번 빌드 동일), vendor/pykrx/rebuild.py·README.md, requirements.txt, restart_all.sh(`PYTHONUNBUFFERED=1`), tests/test_pykrx_logout_relogin.py, docs/superpowers/plans/2026-09-25-infra-089-krx-logout-relogin.md. 호출자 코드는 바꾸지 않음. 코드 +292 -35줄
- 설계: bounded, T3. 2026-09-25 07:09 사용자 「진행해 그리고 실측은 이제 운영에 요구하지말고 여기서 바로바로 진행하면서 돼」. 운영 로그 확인은 사용자가 운영에서 실행해 전달한 결과를 썼다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` REVISE: R1 테스트 import 시 실제 로그인 가능 → 계정을 지운 뒤 늦게 import | R2 변이 복원 명령 → 강제 재설치·`cmp` | 권고 1·2·4·6·8 반영, 3 은 한계 5, 7(`CLAUDE.md` gunicorn 명령)은 피어 제안만으로 `CLAUDE.md` 를 바꾸지 않는 규칙에 따라 미반영·사용자 판단 · 과잉설계 직접 검토 「Lean already」 · 보안 리뷰 보강 `oh-my-claudecode:security-reviewer` 차단급 없음: Medium 성공 재로그인 무제한 → 로그인 뒤 60초 규칙 | Low 첫 응답 미종료 → `close()` | Low 2건 → 한계 5·6 · `closing-bet-reviewer` APPROVE low 4건 반영 뒤 재검토 APPROVE none · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) Critical 없음: Important 새 wheel 미스테이징 → 커밋에 포함 | Minor 2건 → 테스트 추가 | Minor 1건(타이머 동시·세션 없음 성공 갈래) 미반영
- 테스트: 12건 추가(LOGOUT 재로그인 1회·재전송 인자 동일, 동시 4스레드 로그인 1회, INVALIDPERIOD·302 무시, 실패 60초 차단, 재전송도 LOGOUT 이면 반복 없음, 타이머 실패·성공, 세션 없음 실패 차단, 자격 증명 없음, 로그인 직후 LOGOUT, 다른 스레드 재로그인 뒤 LOGOUT). RED 5건. 변이 7종(`login_time` 비교·차단 검사·판별 조건·세션 없음 차단·60초 규칙·간격 기준값·재전송 인자) 각각 해당 테스트 FAIL
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2784 passed 2 skipped(exit 0, 커밋 b3de9c6 상태)
- QA: `docs/dev-cycle/qa/INFRA-089.md` 수정 QA 필수 5/5 통과(로컬, 실제 KRX, 사용자 지시로 운영 대신 로컬 실측). 끊긴 세션에서 재로그인 1회로 6행(수정 전 0행), 동시 4스레드 로그인 1회로 8종목 6행, `INVALIDPERIOD` 재로그인 0, 두 번째 프로세스 로그인(`CD001`) 뒤 첫 세션 유지, `restart_all.sh` 문법·환경 전달. 브라우저는 화면 변경이 없고 결과가 조회 행 수·로그인 횟수라 하네스로 대체
- 이월: `[INFRA-105]`(섹터 ETF 조회 실패 시 0.0), `[INFRA-106]`(`get_last_trading_date` 실패가 DEBUG 로그뿐). `CLAUDE.md` Production 절 gunicorn 명령의 `PYTHONUNBUFFERED` 는 사용자 판단
- 운영 반영: 장 마감 뒤 `git pull` 과 `./restart_all.sh`. `scripts/sync_dependencies.sh` 의 `pip install -r requirements.txt` 가 `+cookie.3` 을 설치한다
- 정리: QA 프로세스 0, 서버 기동 없음, 3500·5501 리스너 0, 원본 `data/` 쓰기 없음, KRX 로그인 QA 5회·원인 확정 실측 9회 모두 성공, LLM 호출 없음

## [INFRA-102] 데이터 상태 화면의 업데이트 시작 실패를 화면에 보이고 409 뒤 진행 상황을 폴링함
- 완료 2026-09-25 06:27 | 티어 T2 (프론트엔드, 위험 경로 아님) | 커밋 f8fea11
- 원인: `performUpdate` 는 409 만 「업데이트 중복」으로 띄우고 나머지는 다시 던졌고, `handleUpdate` 는 `console.error` 만 남겨 403·500 등 시작 실패가 화면에 보이지 않았다. 409 뒤에는 두 버튼 모두 폴링을 시작하지 않아 다른 세션이 시작한 실행의 진행 상황이 보이지 않았다(`[INFRA-100]` 리뷰 L-2)
- 변경: frontend/src/app/dashboard/data-status/page.tsx(`performUpdate` 의 409 분기 제거, 공용 `showStartUpdateError` 추가: 409 면 `DUPLICATE_UPDATE_MODAL` 과 `startPolling()`, 그 밖은 「업데이트 오류」 모달에 `error.message` 또는 기본 문구, `handleUpdate`·`handleUpdateAll` catch 에서 부름), frontend/src/app/dashboard/data-status/page.regression-INFRA-102.test.tsx, docs/dev-cycle/qa/INFRA-102.md. 코드 +113 -22줄
- 설계: bounded, 해법 (a). 2026-09-25 06:20 사용자 「진행해」. (b)(오류 모달만, 폴링 없음)는 L-2 가 남아 제외
- 리뷰: `.claude/skills/closing-bet-nextjs/SKILL.md`·`frontend/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`(순수 클라이언트 변경)·`vendor/skills/vercel-react-best-practices/rules/rerender-move-effect-to-event.md` · `/ponytail-review` `performUpdate` 인라인 -3줄 지적은 기존 함수라 범위 밖으로 미반영 · `closing-bet-reviewer` APPROVE(max low): 409 뒤 `setUpdating(false)` 와 폴링 경쟁 없음, `fetchAPI` 에 401·403 리다이렉트 없음, 표시 문구에 비밀 없음 확인. L-1(`[INFRA-099]` 경로 409 는 진행 표시 없음, 종전과 같음) → QA 기대값 제외 | L-2(`any`) 주변 관례라 미반영 | 범위 밖(폴링 오류 시 무한 요청) → `[INFRA-103]`
- 테스트: vitest 4건 추가(개별 403·500 오류 모달, 개별·전체 409 뒤 「Daily Prices 업데이트 중...」). 수정 전 4건 모두 실패
- 검증: `npx vitest run` 100 files·690 passed(exit 0), `npm run type-check` exit 0, `npm run lint` 0 errors(`page.tsx` 경고 11건, 수정 전과 같음). 파이썬 변경 없음
- QA: `docs/dev-cycle/qa/INFRA-102.md` 필수 4/4 통과(agent-browser, 격리 사본 둘, 더미 관리자 세션). 403 은 화면을 연 뒤 사본 Flask 만 다른 `ADMIN_EMAILS` 로 재기동해, 5xx 는 사본 Flask 를 멈춰 재현했고 모든 클릭은 상태 파일이 실행 중일 때만 했다. 수정 전은 403·500 모달 없음, 409 뒤 폴링 없음(결함 재현). 수정 뒤는 「업데이트 오류 / Forbidden」·「API Error: 500」 모달, 409 모달을 닫으면 진행 표시와 「중지」 버튼, 상태 파일 해시 불변, 파이프라인 로그 0
- 이월: `[INFRA-103]`(진행 폴링이 조회 오류에도 멈추지 않음)
- 정리: 사본 둘·더미 토큰 삭제, 사본 서버 종료 뒤 리스너 0, 원본 `data/update_status.json` 수정 시각 불변, 3500·5501 리스너 0

## [INFRA-101] 대체된 옛 수동 업데이트가 새 실행의 항목 상태를 덮어쓰지 않게 함
- 완료 2026-09-25 06:40 | 티어 T3 (위험 경로 `services/common_update_status_service.py`) | 커밋 61c30c0
- 원인: `update_item_status` 는 상태 파일의 `items` 를 이름으로만 찾아 고치고 자기 실행의 `startTime` 을 확인하지 않았다. 중단 뒤 다른 워커에서 새 실행이 시작되면 옛 실행은 감시가 멈출 때까지 남은 단계를 마치며 `running`·`done`·`error` 를 새 실행의 같은 이름 항목에 썼다(`[INFRA-099]` QA S-3 관찰)
- 변경: services/common_update_status_service.py(`update_item_status` 에 선택 인자 `start_time`, `update_lock` 안에서 읽은 `startTime` 과 다르면 저장하지 않고 INFO 로그), app/routes/common.py(래퍼가 `start_time` 을 넘기고, `run_background_update` 가 진입 때의 `LOCAL_RUN_START_TIME` 을 두 인자 콜백에 묶음), tests/app/test_common_update_service.py, docs/superpowers/plans/2026-09-25-infra-101-item-status-run-guard.md, docs/dev-cycle/qa/INFRA-101.md. 파이프라인·단계 함수·수동 API `api_update_item_status` 는 바꾸지 않음. 코드 +66 -3줄
- 설계: bounded, 해법 (a). 2026-09-25 06:30 사용자 「진행해」. (b)(파이프라인에서 콜백 전에 비교)는 비교와 쓰기가 같은 잠금 안에 있지 않아 제외
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS: 중 시작 처리와 스레드 진입 사이 틈 → 계획 한계 3(범위 밖) | 낮 같은 실행 안 중단 뒤 쓰기는 막지 않음 → 한계 4, QA 판정 한정 | 낮 상태 파일·스냅샷이 없으면 건너뜀 → 한계 5 | 낮 `LOCAL_RUN_START_TIME` 없음 → `None` 전달 assert 추가 · `/ponytail-review` 「Lean already. Ship.」 · `closing-bet-reviewer` APPROVE(max low): 한계 3 의 틈 → `[INFRA-104]` 등록 | 계획 테스트 스케치 정정 · `/review`(T3, `oh-my-claudecode:code-reviewer`) APPROVE: 시그니처 캐시 충돌(기존 결함) → `[INFRA-098]` 근거에 추가 | 건너뛴 `running` 이 `currentItem` 을 안 바꾸는지 → assert 추가 | 통합 테스트 공백 → QA S-1 로 대체 | `getattr` 기본값 → 기존 관용구라 유지 | QA 전제 → QA 문서에 명시
- 테스트: 2건 추가(다른 실행의 `startTime` 이면 `done`·`running` 을 써도 상태와 `currentItem` 불변, 같거나 인자 없음이면 씀, 래퍼가 진입 때 값을 묶고 없으면 `None`). RED `2 failed`. 변이 검사: 비교 분기 삭제·래퍼 미전달·콜백 호출 때 읽기·건너뛰며 `currentItem` 저장 각각 `1 failed`
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2772 passed 2 skipped(exit 0)
- QA: `docs/dev-cycle/qa/INFRA-101.md` 필수 3/3 통과(격리 사본 둘, 실제 `app.routes.common` 배선을 부르는 프로세스 둘이 사본 `data/` 공유, 가짜 수집기, 소켓 차단). 다른 워커 중단·0.5초 뒤 재시작에서 새 실행의 `Daily Prices` 가 수정 전 `done`(결함 재현)·수정 뒤 `pending`, 옛 실행의 거른 쓰기 1회. 대체되지 않은 실행은 `done`·거른 쓰기 0, 옛 실행이 1.03초에 멈추는 종전 동작 유지. 브라우저는 진입점이 실제 수집·LLM 을 일으키는 금지 조작이고 결함이 워커 둘에서만 드러나 하네스로 대체. 첫 시도는 셸 호출 실수로 무효(기록)
- 이월: `[INFRA-104]`(실행 시각을 시작 처리에서 스레드로 넘기지 않아 스레드 진입 전의 틈에서 옛 실행이 새 실행으로 보임), `[INFRA-098]` 근거 추가(시그니처 캐시 충돌)
- 정리: 사본 둘 삭제, 서버 기동 없음, 원본 `data/update_status.json` 수정 시각 불변, 3500·5501 리스너 0

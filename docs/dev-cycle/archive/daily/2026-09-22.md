# 2026-09-22

## [VCP-026] 「최신」 조회가 오늘 자 행만 보아 오늘 스캔 전에는 시그널이 비어 보인다
- 완료 2026-09-22 13:35 | 티어 T3 (계획 T2, 코드 리뷰 반영으로 위험 경로 캐시 모듈 한 줄을 고쳐 상향) | 커밋 9a2ca1c
- 변경: app/routes/kr_market_vcp_signal_helpers.py (+20 -4), services/kr_market_vcp_payload_service.py (+30 -3),
  services/kr_market_vcp_signals_cache.py (+5 -1) 외 6개 (frontend 2 + 회귀 테스트 2 + pytest 2, 합계 +381 -24)
- 리뷰: next 번들 문서 05-server-and-client-components(클라이언트 컴포넌트 경계 확인, 판단 변경 없음) ·
  superpowers:brainstorming(bounded, 매수 잠금은 사용자가 제외) · ponytail-review(oh-my-claudecode:code-reviewer 레인, 6건 전부
  반영: max() 한 줄, 죽은 초기화 삭제, 홈 조건 축약, 무변경 화면의 회귀 테스트 삭제, kwargs 헬퍼, 중복 행 제거) ·
  code-review(feature-dev:code-reviewer, REQUEST_CHANGES: 배포 전 스키마 4 빈 캐시가 대체 로직을 하루 무력화 → 스키마 5 + 회귀
  테스트 → APPROVE) · review(oh-my-claudecode:critic, MAJOR 0 / MINOR 5: 차트 기준일·툴팁·docstring·테스트 두 갈래 반영, 재분석
  스코프 판정 불일치는 [VCP-027] 이월. 판정 ACCEPT-WITH-RESERVATIONS → 반영 재확인 뒤 ACCEPT, 유보는 결함이 아니라 로컬
  자료의 `is_vcp` 공백으로 실측 범위가 좁다는 점) · qa-only → qa(gstack `$B`, 필수 6/6)
- 검증: pytest 2578 통과 2 skipped (직전 2571 + 신규 7) · vitest 644 통과 (86 파일) · npm run type-check 무오류 ·
  QA 시나리오 6/6 통과 (헬스 100, 발견 0건)
- 메모: 3월 커밋 11ad7a9 의 「오늘만」 필터를 되돌린 것이며, 대체 기준은 날짜 목록과 같은 `_is_vcp_signal_row` 다. 로컬
  `signals_log.csv` 는 `is_vcp` 가 전부 비어 대체가 일어나지 않으므로 실측은 scratchpad 사본(`is_vcp=True`)을 cwd 로 삼은
  임시 백엔드로 했다. 그 격리 실행에서도 `runtime_cache.db` 와 상태 파일 둘이 저장소 절대 경로로 원본 `data/` 에 쓰였다
  ([INFRA-078]). 감사 2차 리포트(AUDIT-CHAT·AUDIT-FLOW)와 백로그 등록 12건이 같은 커밋에 있다.

## [FE-044] 개인정보처리방침과 서비스 약관 페이지 신설
- 완료 2026-09-22 14:32 | 티어 T3 (계획 T3, 구현 파일 diff 796줄) | 커밋 9525fe2
- 변경: frontend/src/app/(legal)/privacy/page.tsx (+400), (legal)/terms/page.tsx (+208),
  (legal)/legal-pages.test.tsx (+141), (legal)/layout.tsx (+40), frontend/src/app/page.tsx (+9),
  docs 2개 (TODO +30, qa/FE-044.md +134). 합계 +962
- 리뷰: next 번들 문서 03-layouts-and-pages·14-metadata-and-og-images(라우트 그룹과 정적 metadata 확인) ·
  superpowers:brainstorming(bounded, AskUserQuestion 으로 연락처와 운영자 표기 확정) ·
  ponytail-review(2건 반영: 손으로 그린 글머리표를 `list-disc` 로, 테스트의 `new RegExp` 와 괄호 이스케이프 제거) ·
  code-review(feature-dev:code-reviewer, 3라운드. CHANGES REQUESTED 두 번 → APPROVE) ·
  review(oh-my-claudecode:critic, 3라운드. REJECT 두 번 → ACCEPT)
- 검증: vitest 655 통과 (87 파일, 직전 644 + 신규 11) · npm run type-check 무오류 ·
  npm run lint 신규 파일 경고 0 (저장소 기존 경고 184건은 무관) · QA 시나리오 7/7 통과 (2회 실행)
- 리뷰가 잡은 것: 1라운드에서 방침 본문이 코드와 어긋나는 네 곳을 잡았다. 파일 권한 주장(실제 0644),
  취급자 한 명 주장(`ADMIN_EMAILS` 네 개), 브라우저 저장소 절 누락, 화면의 「계정 삭제」 버튼과의 불일치다.
  심층 리뷰는 「30일 자동 삭제」가 거짓임을 지적했다. `TimedRotatingFileHandler(when='midnight', backupCount=30)`
  은 날짜가 아니라 파일 개수를 세며 기록이 없는 날에는 회전하지 않는다. 실측하면 `logs/` 에
  `user_activity.log.2026-02-22` 부터 20개가 남아 보관 범위가 7개월이다. 그 밖에 약관법 제7조 제1호에 걸리는
  「어떠한 책임도 지지 않습니다」, 제11조 제1호에 걸리는 사전 통지 없는 이용 제한, 제12조 제3호의 고지 절차
  누락, 개인정보 보호법 제30조 제1항 제7호(쿠키)와 제28조의8 제2항(국외 이전의 국가·연락처·기간) 미비를 잡았다.
  2라운드에서는 1라운드 반영으로 새로 넣은 「라. 브라우저에만 저장되는 정보」가 사실과 다름을 두 레인이 함께
  잡았다. 관심 종목은 채팅 요청마다 서버로 가고 Google 로 이전되며, 표시 이름과 대화 성향은 서버에 먼저
  기록된 뒤 로컬에 복사된다. 법적 고지가 수집 범위를 실제보다 좁게 알리는 오류라 차단 사유로 받아들였다.
- 변이 실험: 조항 제목 변경, 문의 이메일 변경, 투자자문 면책 삭제, 푸터 링크 주소 변경은 곧바로 잡혔다.
  보관 기간 서술 삭제만 살아남아(「30일」이 세 곳에 나옴) 단언을 표 셀 문구로 좁힌 뒤 다시 잡혔다.
- 메모: 보호책임자 성명은 사용자가 「Smart Money Bot 운영자」로 표기하기로 결정한 사항이라 그대로 두었다.
  심층 리뷰는 이것이 개인정보 보호법 제30조 제1항 제6호가 직접 요구하는 항목이며 개인 운영이 면제 사유가
  아니라는 점을 보고에 포함하라고 했고 그대로 전달했다. 문서를 쓰다 새로 드러난 문제 세 가지를 백로그에
  등록했다. `[FE-045]`(서버 기록을 한 번에 지우는 수단 부재와 「계정 삭제」 버튼의 거짓 문구),
  `[FE-046]`(활동 로그의 날짜 기준 삭제와 데이터 파일 0600 권한), `[FE-047]`(루트 레이아웃 `lang="en"`).
  방침의 보관 기간 문안과 시스템의 간격은 `[FE-046]` 이 닫을 때까지 열려 있다.

## [VCP-028] VCP 스캔이 예외로 끝나면 `signals_log.csv` 의 누적 이력을 빈 파일로 덮어쓴다
- 완료 2026-09-22 15:21 | 티어 T2 (계획 T1, 구현 파일 diff 65줄로 상향) | 커밋 1fae279
- 변경: scripts/init_data.py (+18 -47), tests/scripts/test_init_data_vcp_scheduler.py (+40),
  docs 2개 (TODO +10, qa/VCP-028.md +74). 코드 합계 +58 -47
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「VCP-028 설계」 추천안 승인) ·
  ponytail-review(자체 검토: 정상 갈래와 예외 갈래의 열 목록을 모듈 상수 하나로 모으고 예외 갈래의 13개 열
  목록을 삭제, 구현 파일 순 -29줄. 그 밖에 덜어낼 것 없음) · code-review(feature-dev:code-reviewer, APPROVE.
  리뷰어가 `git stash` 로 종전 코드에서 두 테스트의 실패를 직접 재현한 뒤 원복, 확신도 높은 지적 0건) ·
  qa-only → qa(대상 화면이 없어 브라우저 단계는 적용하지 않고, 원본 사본 + 예외 주입 재현 스크립트로 두 단계
  수행. 필수 2/2 · 인접 1/1)
- 검증: pytest 2580 통과 2 skipped (직전 2578 + 신규 2) · vitest 655 통과 (87 파일, frontend 무변경) ·
  QA 시나리오 3/3 통과 (원본 `data/signals_log.csv` md5 무변경)
- 메모: 사용자의 「최신 저장분이 화면에 보이지 않는다」 문의를 조사하다 찾았다. 그 증상의 원인은 운영 서버가
  `9a2ca1c` 이전 코드로 돌고 있는 것으로 판단했고(이 컴퓨터의 자료는 05-05 행 15개뿐이라 화면이 말하는 09-21 은
  운영 자료다), 이 결함은 그와 별개로 스케줄러가 매 개장일 17시에 부르는 함수의 실패 경로다. `[VCP-026]` 의
  최신 저장분 대체와 히스토리 목록이 모두 이 파일의 과거 행에 기대므로, 예외 한 번에 화면의 VCP 이력 전체가
  비는 구조였다. 예외 갈래가 `vcp_signals_latest.json` 을 빈 시그널로 덮어쓰는 동작은 설계 승인 때 범위
  밖으로 두었다. QA 는 원본 CSV 사본을 작업 디렉터리로 삼고 스크리너 생성자에서 예외를 주입해 실행했으며,
  회귀 테스트의 주입 지점(`run_screening`)과 달리해 같은 갈래로 떨어지는 것을 함께 확인했다. 같은 조사에서
  `.env` 의 `gemini-3.8-flash` 가 Vertex AI `global` 리전에서 실제 응답하는 것을 프로젝트 클라이언트로 확인했다.

## [VCP-029] VCP 로그의 병합·정리 실패 갈래 두 곳이 누적 이력을 덮어쓴다
- 완료 2026-09-22 17:42 | 티어 T3 (등록 때 T1 로 적었으나 `scripts/init_data.py` 는 위험 경로) | 커밋 6a60b12
- 변경: scripts/init_data.py (+12 -7), tests/scripts/test_init_data_vcp_scheduler.py (+43),
  docs 3개 (TODO +29, qa/VCP-029.md +80, plans/2026-09-22-vcp029-signals-log-preserve.md +358). 코드 합계 +55 -7
- 리뷰: superpowers:brainstorming(bounded, 사용자 「승인」 뒤 AskUserQuestion 「VCP-029 설계」 추천안 「두 갈래 모두 False」) ·
  writing-plans + 계획 검토(oh-my-claudecode:critic `vcp029-plan-critic`, ACCEPT-WITH-RESERVATIONS R1-R8: QA 스크립트 구문
  오류, 호출자 서술, Refresh Data 화면의 error 상태를 제약과 S-4 에 추가, S-3 단언, `data/` 스냅샷, INFO 로그, 커밋 형식을
  반영. R4 → `[VCP-030]`) · ponytail-review(자체 검토, 자를 것 없음) · code-review(feature-dev:code-reviewer
  `vcp029-reviewer`, APPROVE, 지적 0) · review(oh-my-claudecode:critic `vcp029-deep-review`, ACCEPT-WITH-RESERVATIONS
  MAJOR 2 / MINOR 4: m1 두 WARNING 에 파일 경로와 다음 행동, m2 `[VCP-028]` 주석의 「정상 갈래」 정정을 반영. M1 Refresh VCP
  의 False → success 표시는 `[VCP-031]`, M2 손상 파일 회복 경로 부재와 m4 0바이트 때 데이터 상태 「오늘」 표시는 `[VCP-030]`
  확장, m3 결측 ticker 중복 제거는 확신도 낮음으로 기록만) · qa-only → qa(대상 화면의 되돌릴 수 없는 조작을 피해 원본
  사본 + 손상 파일 재현 스크립트로 두 단계 수행. 필수 3/3 · 인접 1/1)
- 검증: pytest 2582 통과 2 skipped (직전 2580 + 신규 2, 로그 문구 수정 뒤 재실행) · vitest 655 통과 (87 파일, frontend
  무변경) · QA 시나리오 4/4 통과 (원본 `data/signals_log.csv` md5 무변경, 원본 `data/` 최상위 변경 파일 없음, 종료 코드 0)
- 메모: `[VCP-028]` 완료 뒤 「덮어쓰기가 남아 있는가」 재점검에서 찾았다. 같은 함수의 안쪽 두 갈래가 기존 CSV 를 읽거나
  병합하지 못하면 오늘 자 행만 남기거나 빈 파일을 썼다. 이제 세 실패 갈래가 모두 「보존 + False」 계약이며, 시그널 없음
  갈래와 바깥 갈래는 빈 최신 payload 를 함께 쓴다. 이 사이클에서 `[VCP-028]` 이 위험 경로 파일을 T2 로 잘못 판정해 심층
  리뷰를 건너뛴 것을 확인하고 그 갈래를 이번 심층 리뷰 범위에 넣었다. 심층 리뷰는 두 파일 안에서 결함을 찾지 못했다. 같은
  조사에서 종가베팅 저장 경로는 예외 시 아무것도 쓰지 않아 안전함을 확인했고, 최신 파일의 비원자적 중복 쓰기는
  `[JONGGA-040]` 으로 등록했다.

## [JONGGA-041] 종가베팅 카드에 실제 시세로 그린 작은 차트를 되살린다
- 완료 2026-09-22 18:40 | 티어 T2 | 커밋 1029f2e
- 변경: frontend/src/app/dashboard/kr/closing-bet/MiniPriceChart.tsx (+90, 신규), MiniPriceChart.test.tsx (+71, 신규),
  page.tsx (+7 -10), docs 2개 (TODO +20, qa/JONGGA-041.md +97). 코드 합계 +168 -10
- 리뷰: superpowers:brainstorming(bounded, 사용자 「작은 차트 되살려주고 진행해」 → AskUserQuestion 「차트 설계」 추천안
  「실제 시세 SVG 선 차트」. 대안으로 네이버 이미지 축소와 lightweight-charts 캔들을 제시) · ponytail-review(자체 검토,
  백엔드가 이미 거르는 `close > 0` 필터 -1줄, 그 외 lean) · code-review(feature-dev:code-reviewer `jongga041-reviewer`,
  APPROVE, 참고 2: 이 화면의 확대 모달은 네이버 이미지라 이 카드가 `/api/kr/stock-chart` 의 첫 호출자 → 주석 정정,
  `data-testid="mini-chart"` 는 테스트가 안 쓰지만 브라우저 실측 선택자라 유지) · qa-only → qa(격리 Next 57821 + 합성
  fixture 57822, browse 실측, 필수 3/3 · 인접 1/1)
- 검증: vitest 전체 exit 0 (closing-bet 디렉터리 14파일 93건, 신규 5) · type-check 0 · eslint 신규 파일 0오류 0경고 ·
  pytest 2582 통과 2 skipped (frontend 만 변경) · QA 4/4 통과, `/_next/mcp` `get_compilation_issues`·`get_errors` 빈 목록,
  스크린샷 3장(데스크톱·모달·390px)
- 메모: `[JONGGA-024]` 가 지운 가짜 polyline 자리를 신호일까지 한 달 종가의 SVG 선으로 채웠다. 상자 전체가 종전과 같은
  「{종목} 차트 크게 보기」 버튼이라 기존 회귀 테스트 세 벌이 그대로 통과했다. 읽은 문서: Next 번들
  `05-server-and-client-components.md`·`06-fetching-data.md`, 스킬 `vercel-react-best-practices`. 격리 Next 를 `127.0.0.1`
  로 열면 Next 16 의 개발 origin 검사에 걸려 수화되지 않아 카드가 영영 그려지지 않으며(콘솔 오류 없음), `localhost` 로
  열어야 한다는 실측 요령을 QA 문서에 남겼다. 격리 사본은 rsync + `node_modules` APFS clone 으로 만들었다(심볼릭 링크는
  Turbopack 이 거부한다).

## [VCP-032] 스케줄러의 VCP 스캔이 어느 날에도 시그널을 내지 못한다 — 합산 점수 문턱 60 이 점수 구성상 도달 불가
- 완료 2026-09-22 19:05 | 티어 T2 | 커밋 42b5a87
- 변경: engine/screener.py (+6 -6), engine/screening_runtime.py (-19), app/routes/kr_market_vcp_signal_helpers.py (+4 -3),
  app/routes/kr_market_data_signals_routes.py (+2 -2), tests 5개 (+53 -24), docs 2개 (TODO +9 -1, qa/VCP-032.md +98).
  코드 합계 +65 -54
- 리뷰: superpowers:brainstorming(bounded, 사용자 「진행해」 → AskUserQuestion 「VCP 설계」 추천안 「패턴 통과가 곧 시그널」.
  대안으로 문턱 60→30(40일 모두 1~28건)과 60→35(34/40일)를 재현 수치와 함께 제시) · ponytail-review(자체 검토, 삭제만
  있는 diff, 날짜 목록 라우트의 불필요해진 `score` usecols -1) · code-review(feature-dev:code-reviewer `vcp032-reviewer`,
  APPROVE, 블로커 0, 참고 2: 프로덕션 호출자가 없어진 `resolve_vcp_min_score` → 함수·전용 `_coerce_float`·단위 테스트 삭제로
  반영, AI 규칙 기반 폴백이 낮은 합산 점수를 SELL 로 모는 문제 → `[VCP-033]`) · qa-only → qa(격리 사본에서 실제 스크리너로
  2026-09-21 스캔 + 같은 사본의 격리 gunicorn·Next 를 browse 로 실측, 필수 2/2 · 인접 2/2)
- 검증: pytest 2583 통과 2 skipped(직전 2582 + 신규 1) → 죽은 resolver 와 그 테스트를 지운 뒤 2582 통과 2 skipped ·
  vitest 전체 exit 0(frontend 무변경) · QA 4/4 통과(사본 `signals_log.csv` 에 2026-09-21 시그널 20건, 점수 27~42 내림차순,
  전부 화면 판정 통과, VCP 화면 20행·홈 카드 「오늘의 시그널 20 최신 저장분(2026-09-21) 기준」·날짜 목록 09-21, 원본
  `data/` 자료 파일 무변경)
- 메모: 등록 때의 40거래일 재현(두 조건 동시 만족 0건, 패턴 통과 하루 1~60건)이 근거다. 저장 조건은 `is_vcp` 하나가 됐고
  합산 점수는 정렬·상위 20·등급에만 남았다. 삭제한 검사: `test_resolve_vcp_min_score_supports_comma_formatted_string`(쉼표
  문자열 파싱)은 대상 함수와 함께 지웠고, `test_build_vcp_signal_from_row_respects_runtime_min_score`(문턱 미만 행 제외)와
  `..._filters_closed_and_low_score` 는 옛 계약을 고정하던 것이라 새 계약 검사로 대체했다. `is_vcp` 가 기존 빈 값 행과
  병합되며 `1.0` 으로 저장되는 것은 기존 동작이고 `safe_bool` 이 참으로 읽어 기록만 남겼다. 운영 반영은 사용자가 pull 과
  재기동으로 하며, 다음 개장일 17시 스캔부터 시그널이 쌓인다.

## [JONGGA-039] 저장분이 비어 있으면 「최신」 이 과거 리포트로 내려가지 않고, 배너가 표시 중인 사실을 감춘다
- 완료 2026-09-22 20:02 | 티어 T1 | 커밋 4b8ee17
- 변경: services/kr_market_jongga_payload_latest.py (+14 -4), frontend/src/app/dashboard/kr/closing-bet/page.tsx (+9 -2),
  tests 2개 (vitest 신규 +81, pytest +39 -7), docs 2개 (TODO +6 -1, qa/JONGGA-039.md +80). 코드 합계 +143 -13
- 리뷰: superpowers:brainstorming(bounded, 사용자 「백로그도 설계해서 진행해」 → 네 항목 일괄 설계 → AskUserQuestion
  「최근 유효 리포트로 대체」 선택) · ponytail-review(자체 검토, 배너 두 갈래가 감싸는 `<div>` 를 중복해 삼항식 둘로 -6줄) ·
  T1 이라 code-review 없음 · qa-only → qa(격리 사본의 실제 gunicorn·Next 를 browse 로 실측, 필수 2/2 · 인접 2/2)
- 검증: pytest 파일 9 통과 · closing-bet vitest 15파일 95 통과 · `npm run type-check` exit 0 · QA 4/4 통과(배너 제목
  「오늘 분석은 아직 없습니다. 최신 저장분(2026-09-21)을 표시합니다.」 와 message 줄, 카드 8장, API `is_stale`·
  `latest_available_date` 2026-09-21·「주말/휴일」 없음, 날짜 선택 시 배너 없음, 유효 파일 없음 → 빈 응답 유지) ·
  `/_next/mcp` `get_compilation_issues`·`get_errors` 빈 목록
- 메모: stale 갈래에서 저장분이 비면 `find_recent_valid_jongga_payload` 로 내려가고 그 리포트의 날짜로 표식을 다시 만들어
  나중에 덮는다(helper 의 「주말/휴일」 message 를 가림). 오늘 자 0건과 비개장일은 종전대로 대체하지 않는다. 대체한 검사:
  `test_build_jongga_latest_payload_stale_without_signals_stays_empty`(빈 저장분 → 빈 응답 고정)를 「대체」와 「유효 파일 없음」
  두 검사로 바꿨다. 읽은 문서: Next 번들 `05-server-and-client-components.md`·`06-fetching-data.md`. closing-bet vitest 를
  `type-check` 와 동시에 돌리면 워커가 충돌해 14파일이 실패로 끝나고 단독 실행은 통과한다(기록만). 운영 판별은
  `data/jongga_v2_latest.json` 의 `date` 와 `signals` 길이다.

## [CHAT-031] 프롬프트가 싣는 시장·시그널 값과 기준일 바로잡기
- 완료 2026-09-22 20:40 | 티어 T2 | 커밋 4462816
- 변경: chatbot/signal_context.py (+56 -23), chatbot/prompts.py (+17 -15), chatbot/payload_service.py (+8 -2),
  chatbot/intent_detail_service.py (+1 -10), chatbot/data_service.py (+4 -5), chatbot/core_data_access_mixin.py (+1 -1),
  tests 3개 (신규 test_prompts_refactor.py +48, test_signal_context.py +66 -6, test_intent_detail_service.py +3 -1),
  docs 2개 (TODO +8 -1, qa/CHAT-031.md +101). 코드 합계 +204 -63
- 리뷰: superpowers:brainstorming(bounded, 사용자 「백로그도 설계해서 진행해」 → 네 항목 일괄 설계 → AskUserQuestion
  「시스템 프롬프트 한 곳」 선택) · ponytail-review(자체 검토, 프로덕션 호출자가 없어진 `load_vcp_ai_signals` 래퍼 삭제, 테스트
  2건은 `load_vcp_ai_payload` 로) · code-review(feature-dev:code-reviewer `chat031-reviewer`, APPROVE, 블로커 0. 회신이
  두 번 모두 유실되어 subagents 기록에서 읽음. 낮은 확신도 3건 가운데 docstring 드리프트 반영, 중복 필드 조회와 `date.today()`
  반복은 미반영) · qa-only → qa(격리 사본에 LLM 스텁을 끼워 실제 챗봇 화면에서 네 메시지 전송, 필수 2/2 · 인접 2/2)
- 검증: `tests/chatbot` 228 통과 · pytest 전체 2589 통과 2 skipped(래퍼 삭제 전후 두 번) · vitest 전체는 frontend 무변경이라
  `[VCP-032]` 의 exit 0 유지 · QA 4/4 통과(「## 시장 현황 (Market Gate 기준 2026-09-22)」·「KOSPI: 7,110.76」·「🟢 반도체:
  +2.93%」 한 번, 「2.93점」 없음, 「분석 4건 (기준일 2026-05-05, 140일 경과), 매수 추천 0건」, 종가베팅·뉴스 문맥의
  「(기준일 2026-09-21, 1일 경과)」) · `/_next/mcp` 빈 목록
- 메모: 섹터는 시스템 프롬프트의 「섹터 등락률」 절 한 곳에 부호 색으로 싣고 시장 의도 문맥의 중복 목록은 지웠다. VCP 문맥은
  「분석 N건, 매수 추천 M건」 머리글로 「분석 없음」과 「매수 추천 없음」을 가른다. 기존 고정 테스트 두 건(`..._overrides_sector_scores_from_market_gate`,
  `..._uses_fallback_when_empty`)은 새 계약에서도 참이라 그대로 두었다. 원본 챗봇으로 전송하지 않고, 격리 사본의
  `qa_flask_app.py` 가 `engine.genai_client.build_genai_client` 를 프롬프트 데이터 절을 되돌리는 가짜 클라이언트로 바꿔
  화면에 보이는 답변이 곧 모델이 받은 데이터가 되게 했다. 쿼터 가드의 「서버 키 있음」 판정은 더미 `GOOGLE_CLOUD_PROJECT` 로
  통과시켰다. 이 스텁 요령은 챗봇 항목의 다음 QA 에도 쓸 수 있다.

## [VCP-031] 「Refresh VCP」가 시그널 저장 실패를 「완료: 조건 충족 종목 없음」 성공으로 보인다
- 완료 2026-09-22 21:25 | 티어 T2 (구현 28줄이라 T1 조건이나 관리자 화면 흐름이라 code-review 를 둠) | 커밋 8ff9ed1
- 변경: services/kr_market_vcp_background_service.py (+20 -8), tests/services/test_kr_market_vcp_background_service_refactor.py
  (신규 +68), docs 2개 (TODO +8 -1, qa/VCP-031.md +103). 코드 합계 +88 -8
- 리뷰: superpowers:brainstorming(bounded, 사용자 「백로그도 설계해서 진행해」 → 네 항목 일괄 설계 → AskUserQuestion
  「vcp_signals_latest.json 의 수」 선택) · ponytail-review(자체 검토, 더 줄일 것 없음. 시그널 수는 캐시 계층 대신 stdlib `json`
  으로 읽어 격리 실행이 원본 `runtime_cache.db` 에 닿는 경로를 만들지 않음) · code-review(feature-dev:code-reviewer
  `vcp031-reviewer`, APPROVE, 블로커 0. 회신 유실로 subagents 기록에서 읽음. 낮은 확신도 2건은 기존 형식·극저확률이라 미반영) ·
  qa-only → qa(격리 사본에 init_data 스텁과 더미 비밀의 관리자 세션을 두고 실제 VCP 화면에서 「Refresh VCP」 를 눌러 실측,
  필수 2/2 · 인접 2/2)
- 검증: 새 테스트 3건 RED→GREEN · pytest 전체 2592 통과 2 skipped · vitest 전체는 frontend 무변경이라 `[VCP-032]` 의 exit 0
  유지 · QA 4/4 통과(실패 모드에서 화면 「❌ 오류: 실패: 시그널 저장 실패. …」 와 API `status: error`, 성공 모드에서
  「✅ 업데이트 완료!」 와 API 「완료: 20개 시그널 감지」, True+0건 하네스 「완료: 조건 충족 종목 없음」, 비관리자는 권한 모달과
  라우트 403)
- 메모: 종전에는 `create_signals_log` 의 False 가 「완료: 조건 충족 종목 없음」 성공으로, True 가 「완료: 성공」 으로 보였다.
  이제 False 는 예외로 올려 기존 except 갈래가 error 를 세우고 가격 동기화를 건너뛰며, True 는 방금 쓴 latest payload 의 시그널
  수를 말한다. 관리자 흐름의 브라우저 실측 요령(사본 Next·Flask 에 더미 `NEXTAUTH_SECRET`·`INTERNAL_IDENTITY_SECRET`·
  `ADMIN_EMAILS` 를 환경 변수로 주고 `next-auth/jwt` 의 `encode` 로 만든 세션 토큰을 쿠키로 넣음)은 QA 문서에 남겼다. 다음
  관리자 화면 항목의 QA 에 그대로 쓸 수 있다.

## [VCP-033] AI 규칙 기반 폴백이 낮은 합산 점수의 VCP 시그널을 일괄 SELL 로 판정한다
- 완료 2026-09-22 23:05 | 티어 T3 (`engine/vcp_ai_analyzer_helpers.py` 위험 경로) | 커밋 6b9cb66
- 변경: engine/vcp_ai_analyzer_helpers.py (+43 -66), engine/vcp.py (+2 -2, 리터럴 0.5·0.7 → 상수), engine/constants_market_scoring.py
  (+2, `STRONG_CONTRACTION_RATIO`), tests 2개 (helpers +83 -41, analyzer +3 -3), docs 3개 (TODO +10 -1, qa/VCP-033.md +101,
  plans/2026-09-22-vcp-033-fallback-verdict.md +315). 코드 합계 +133 -112
- 리뷰: superpowers:brainstorming(bounded, 사용자 「백로그도 설계해서 진행해」 → 네 항목 일괄 설계 → AskUserQuestion 「수급 이탈만
  SELL, 나머지 HOLD」 선택) · writing-plans + 계획 검토(oh-my-claudecode:critic `vcp033-plan-critic`, REVISE: BUY 고정 검사의 대체
  명시, 합성 순매도 행 필수화, 「보수적으로 SELL」 문구, 0.5 상수화, 5일 음수·1일 0 경계, score 없이도 상태 문장, 수축비 0 결측, 하네스
  다듬기 8건 전부 반영) · ponytail-review(자체 검토, 두 갈래를 한 흐름으로 -34줄) · code-review(feature-dev:code-reviewer
  `vcp033-reviewer`, APPROVE, `ai_action` 소비처 16개에 BUY 특별 취급 없음 확인) · review(oh-my-claudecode:critic `vcp033-deep-review`,
  경계 탐침 포함, REVISE → 반영 뒤 재확인 ACCEPT. MAJOR 1: 「`vcp_score` 열이 없다」 전제 오류(0~10 양자화 값이 실림) → 계획·QA
  정정, MINOR 2: 0.5 문턱이 `engine/vcp.py` 리터럴과 중복 → `VCPThresholds.STRONG_CONTRACTION_RATIO` 로 묶음, MINOR 3: 경계
  검사 없음 → 매개변수 검사 5벌 + 음수 검사, MINOR 4: 「0.50」 두 종목의 서술 불일치 → 반올림 값으로 판정, MINOR 5: 600자 절단은 정보)
  · qa-only → qa(사본에서 파싱 실패 주입 하네스, 필수 2/2 · 인접 2/2)
- 검증: 바꾼 검사 7건 RED → 구현 뒤 두 파일 81 통과 → 경계 검사 추가 뒤 `tests/engine -k vcp` + `tests/test_vcp.py` 129 통과 ·
  pytest 전체 2595 → 2601 통과 2 skipped · vitest 전체는 frontend 무변경이라 `[VCP-032]` 의 exit 0 유지 · QA 4/4 통과(실제 20행
  HOLD 20·BUY 0·SELL 0, 합성 순매도 행 SELL 55, 결측 갈래·호출부 pytest). 기준선: 고치기 전 같은 20행 전부 SELL 72%
- 메모: 판정 축을 수급 하나로 바꿨다. 5일·1일 수급이 모두 순매도면 SELL, 그 밖에는 HOLD, 폴백은 BUY 를 내지 않고 신뢰도는 55 하나다.
  합산 점수와 수축비는 근거 문장에만 남기고 라벨을 「VCP 점수」에서 「종합 점수」로 바로잡았다. 대체한 검사: `changes_by_signal_state`
  (폴백 BUY·신뢰도 순서) → `never_buys_and_sells_only_on_outflows`(같은 입력), `preserves_real_zero_and_complete_verdicts` →
  `complete_data_verdicts_ignore_score`(신뢰도 61·78·69 기대를 55 로, BUY 기대를 HOLD 로), analyzer `all_parsing_fails` 의 BUY·≥60
  기대 → HOLD·55. 수축비 0 이하는 결측으로 보고, 소수 둘째 자리로 반올림한 값으로 판정해 문장의 숫자와 서술이 어긋나지 않게 했다.
  이월: 양자화된 `vcp_score` 를 서술에 쓸지, 폴백임을 화면에 구분해 보일지는 별도 판단이라 등록하지 않았다.

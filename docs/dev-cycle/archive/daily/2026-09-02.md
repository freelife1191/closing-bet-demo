# 2026-09-02

## [CHAT-001] 새 대화 스트리밍의 세션 전환 결함 수정
- 완료 2026-09-02 | 티어 T2 | 커밋 3dce4ce
- 변경: frontend/src/app/chatbot/page.tsx (+15 -4),
  frontend/src/app/chatbot/page.regression-chat-001.test.tsx (신규 156줄),
  docs/dev-cycle/TODO.md (+30 -15)
- 리뷰: /ponytail-review → feature-dev:code-reviewer → /review 순서대로 실행.
  코드 리뷰가 첫 수정의 회귀를 잡아냈고 그 지적을 반영해 구현을 바꿨다.
- 검증: vitest 162 통과(16 파일) · pytest 1533 통과 2 skipped · type-check 통과 ·
  lint 오류 0(경고 205는 기존 상태, 변경 파일은 경고 없음) ·
  /qa-only quick 실측 health 94/100, 네 시나리오 전부 통과
- 메모: 감사가 지목한 세 증상, 즉 청크마다 fetchSessions() 를 부르는 것과 done 분기가
  실행되지 않는 것과 isCreatingSessionRef 가 남는 것이 모두 "SSE 루프가 렌더에 고정된
  클로저 값으로 세션을 비교한다" 는 한 원인에서 갈라져 나왔다. 그래서 그 한 곳만 고쳤다.

  첫 수정은 sessionIdRef 하나를 두고 effect 로 currentSessionId 와 동기화했다. 이것이
  회귀를 만들었다. 하나의 ref 에 서로 다른 두 상태, 즉 "화면에 표시된 세션" 과 "이 스트림이
  속한 세션" 을 함께 담았기 때문이다. 세션 A 가 스트리밍되는 동안 사용자가 B 를 클릭하면
  effect 가 ref 를 B 로 덮어쓰고, A 의 다음 청크가 sessionChanged 를 오판해 화면을 A 로
  되돌리며, isCreatingSessionRef 때문에 fetchHistory(A) 도 건너뛰어 세션 ID 와 메시지
  목록이 어긋난다. 코드 리뷰가 이것을 지적했다. ref 를 걷어내고 handleSend 안의 지역 변수
  streamSessionId 로 바꾸니 ref 선언과 effect 동기화가 함께 사라져 구현이 더 짧아졌다.

  회귀 테스트를 세 코드 버전에 각각 돌려 유효성을 확인했다. 원본에서는 1번과 2번이 실패하고
  3번이 통과하며, 첫 수정에서는 3번만 실패하고, 최종 구현에서 셋 다 통과한다. 3번 테스트는
  처음에 첫 수정에서도 통과했는데, 청크가 마이크로태스크만으로 연속 처리되어 React 가
  렌더를 배치하는 바람에 결함이 재현되지 않았기 때문이다. read() 안에 setTimeout 0 을 넣어
  실제 스트림처럼 태스크를 넘기게 하니 정확히 실패하게 되었다.

  jsdom 이 scrollIntoView 를 구현하지 않아 마운트 직후 자동 스크롤 effect 가 죽는다.
  beforeEach 에 Element.prototype.scrollIntoView 스텁을 넣어 막았다. 그리고 이 저장소의
  vitest.setup.ts 는 expect.extend 만 하고 타입 선언을 하지 않으므로 toBeInTheDocument 를
  쓰면 tsc 가 TS2339 를 낸다. 기존 테스트가 어디서도 쓰지 않는 것을 확인하고 toBeTruthy 로
  바꿨다.

  /qa-only 실측에서 이 사이클과 무관한 기존 결함 둘을 발견해 백로그에 넣었다. 대시보드
  진입 시의 하이드레이션 불일치가 [FE-008], 사이드바 대화 항목이 접근성 트리에 노출되지
  않는 문제가 [CHAT-007] 이다. 후자는 실측 도중 세션을 전환하려다 삭제 확인 모달이 뜨면서
  드러났다. 대화 항목이 onclick 을 붙인 div 라서 접근성 트리에는 같은 행의 삭제 버튼만
  button 으로 잡힌다.

  이 사이클이 [INFRA-016] 이 정한 압축 지점 세 곳 가운데 /qa-only 직전 지점을 처음 실제로
  사용한 자리다.

## [FE-003] 모의투자 주문 실패 처리와 정산 금액을 실제와 맞춘다
- 완료 2026-09-02 00:52 | 티어 T2 | 커밋 521ad8b
- 변경: frontend/src/lib/api.ts, frontend/src/app/components/BuyStockModal.tsx,
  SellStockModal.tsx, PaperTradingModal.tsx (구현 4개 +49 -77, 순 28줄 감소),
  BuyStockModal.test.tsx · SellStockModal.test.tsx ·
  PaperTradingModal.trade-result.test.tsx (신규 297줄), docs/dev-cycle/TODO.md (+9 -13)
- 리뷰: /ponytail-review(2건 반영, 순수 함수를 모듈 최상위로 옮기고 중복 요약 행 삭제) →
  feature-dev:code-reviewer(결함 없음, 확인 요청 네 가지에 모두 근거 제시) → /review
- 검증: vitest 175 통과(19 파일, 신규 13개 포함) · pytest 1533 통과 2 skipped ·
  type-check 통과 · lint 오류 0(경고 213은 기존 205 + 신규 테스트의 any 8개) ·
  /qa-only quick 실측 health 93/100
- 메모: 감사 초안은 "fetchAPI 로 옮겨 상태 코드를 확인하라" 고만 적었는데, 그것만으로는
  실패를 잡지 못한다. app/routes/common_portfolio_routes.py:98 의 매수 라우트가 상태 코드
  없이 jsonify(result) 만 돌려주므로 잔고 부족 같은 논리적 거절이 HTTP 200 에
  {status:'error'} 를 실어 온다. response.ok 는 언제나 참이다. 그래서 두 겹으로 고쳤다.
  fetchAPI 가 타임아웃과 4xx·5xx 를 예외로 올리고, 호출부가 응답 본문의 status 를 별도로
  확인한다.

  수수료와 세금은 백엔드에 도입하지 않고 화면 표기에서 걷어냈다. 근거 세 가지는 커밋
  본문에 적혀 있다. 요지는 services/paper_trading*.py 가 위험 경로라 도입하면 T3 이 되고,
  백엔드가 가격 × 수량만 정산하므로 화면에만 얹으면 반드시 어긋나며, 모의투자에 수수료
  모델을 넣는 것이 이 항목의 범위 밖이라는 것이다.

  회귀 테스트 유효성은 git stash 로 구현 4개만 되돌려 확인했다. 신규 13개 가운데 9개가
  실패한다. 통과하는 4개는 이번 결함과 무관한 검사(보유 수량 상한, 성공 응답 표시)다.

  실측 대조에 결제 금액을 쓴 이유가 있다. 당시 예수금 91,733,700원에서는 "가능 수량" 이
  수정 전후 모두 1,054주(87,000원 기준)로 같아 수량으로는 차이가 드러나지 않는다. 결제
  금액은 91,655,400원과 91,669,148원으로 갈리므로 그쪽으로 대조했다.

  /qa-only 실측에서 이 사이클과 무관한 기존 결함 하나를 발견해 [FE-009] 로 등록했다.
  보유 종목 행의 매수·매도 버튼이 행 클릭 핸들러로 전파되어, 주문 모달(z-110) 위에 종목
  상세 패널(z-120)이 함께 열리면서 주문 화면을 완전히 덮는다. 상세를 닫으면 주문 모달도
  같이 사라져 목록에서는 매수도 매도도 할 수 없다. git stash 로 구현을 되돌려도 동일하게
  재현되므로 회귀가 아니다.

  실측 도구에 관한 사실 하나. gstack browse 의 좌표 기반 click @eN 은 이 결함 앞에서
  주문 모달이 아예 열리지 않은 것처럼 보이게 만든다. 상세 패널 오버레이가 버튼을 덮기
  때문이다. browse js 로 querySelector 를 써서 직접 click() 하고 document.body.innerText
  를 확인해야 두 패널이 동시에 열려 있다는 사실이 드러난다.

## [FE-009] 보유 종목의 매수·매도 버튼이 종목 상세를 함께 열지 않도록 만든다
- 완료 2026-09-02 01:05 | 티어 T2 | 커밋 087c8b6
- 변경: frontend/src/app/components/PaperTradingModal.holdings-actions.test.tsx 신규,
  docs/dev-cycle/TODO.md (+94 -10). 구현 코드는 한 줄도 바꾸지 않았다
- 리뷰: ponytail-review(net -23, 세 지적 모두 반영) · code-review(지적 없음)
- 검증: pytest 1533 통과 · vitest 178 통과 · type-check 통과 · lint 오류 0 ·
  agent-browser 실측
- 메모: 이 항목이 기술한 결함은 실재하지 않았다. PaperTradingModal.tsx:923,932 의 두
  버튼에는 e.stopPropagation() 이 처음부터 붙어 있었고, agent-browser 로 프로그램적
  클릭과 실제 마우스 좌표 클릭을 각각 시험한 결과 두 경우 모두 오버레이가
  [{모의투자 포트폴리오, z-110}, {모의 투자 매수/매도, z-110}] 둘뿐이었다. 상세
  패널(z-120)은 나타나지 않는다.

  바로 앞 [FE-003] 항목의 메모가 이 결함을 사실로 적었으나 그 관찰은 틀렸다. 좌표
  클릭이 버튼이 아니라 행을 눌러 상세를 먼저 열었고, 그 상태에서 버튼을 다시 클릭해
  두 패널이 공존한 것이다. 조작 순서가 만든 상태였지 한 번의 클릭이 만든 상태가
  아니었다. git stash 로 되돌려도 재현된 이유도 같다. 되돌린 구현과 무관하게 절차가
  같았기 때문이다.

  여기서 얻을 교훈은 도구 사용법이 아니라 판정 절차다. 좌표 클릭이 의도한 요소를
  맞히지 못하면 그 다음 관찰은 전부 오염된다. 두 패널이 함께 열린 것을 보았을 때,
  결함으로 단정하기 전에 깨끗한 상태에서 버튼만 한 번 눌러 재현하는 대조를 먼저
  했어야 한다. 이번에는 클릭 직전과 직후의 오버레이 목록을 각각 수집해 비교하는
  방식으로 확인했다.

  .gstack/qa-reports/qa-report-localhost-3500-2026-09-02.md 의 ISSUE-001 절에 정정
  블록을 달았다. 다만 .gstack/ 는 .gitignore 대상이라 이 정정은 커밋에 남지 않고
  로컬 파일에만 있다.

  회귀 검사의 유효성은 두 버튼의 stopPropagation 을 임시로 걷어내고 확인했다. 세
  검사 가운데 둘이 실패한다. 남은 하나는 행 클릭이 상세만 여는 것을 보는 검사여서
  전파 차단과 무관하게 통과한다.

## [FLOW-001] 장중 실시간 수급 데이터 KIS API 연동 — 철회
- 철회 2026-09-02 | 티어 T3(착수 안 함) | 커밋 없음(구현 없이 백로그에서 제거)
- 변경: docs/dev-cycle/TODO.md (-8)
- 사유: 사용자가 한국투자증권 계좌를 개설하지 않기로 결정했다. 계좌가 없으면 Open API
  키를 발급받을 수 없고, 키가 없으면 이 항목의 목표인 장중 실시간 수급 수집이 성립하지
  않는다. 선행 조건이 영구히 충족되지 않으므로 대기가 아니라 철회로 닫는다.
- 확인한 사실: `.env` 의 KIS 네 줄(26-29행)은 `#` 으로 주석 처리되어 있어 변수 자체가
  존재하지 않는다. `os.getenv("KIS_APP_KEY")` 가 `None` 을 돌려준다. `.env.production`
  과 `frontend/.env` 는 변수 이름은 있으나 값이 비어 있고, 셸 환경 변수에도 없다.
  `.env.example` 의 세 값은 `your_kis_app_key_here` 형태의 자리표시자다. 즉 항목의 첫
  체크박스인 ".env.example 에 추가" 는 이미 되어 있었고, 실제로 빠져 있던 것은
  발급받은 키 값이다.
  (철회 판단 당시에는 `.env` 도 "빈 값" 으로 관찰했으나, 그 직후 사용자가 해당 네 줄을
  주석 처리했다. 결론은 달라지지 않는다.)
- 코드 정리는 하지 않는다. `engine/market_gate_fetchers_local.py:133` 이
  `if kis and os.getenv("KIS_APP_KEY")` 로 막고 있어서, 빈 문자열이 거짓으로 평가되어
  KIS 분기를 건너뛰고 CSV 경로로 내려간다. 키가 없는 상태가 이미 정상 동작이므로
  걷어낼 죽은 코드도, 고칠 예외 경로도 없다. `engine/kis_collector.py` 229줄과
  `tests/engine/test_kis_collector_refactor.py` 는 나중에 계좌를 열 경우를 위해 남긴다.
- 남는 제약: 장중 수급은 계속 CSV 기반으로만 동작한다. 실시간 값이 필요해지면 이
  항목을 되살리는 대신 새 번호로 등록한다. `archive-format.md` §2 에 따라 `FLOW-001`
  번호는 재사용하지 않는다.


## [FE-004] 사이드바 로그아웃과 사용자 표시를 실제 세션에 연결한다
- 완료 2026-09-02 08:14 | 티어 T2 | 커밋 31d66b8
- 변경: frontend/src/app/components/Sidebar.tsx (+19 -13),
  frontend/src/app/components/SettingsModal.tsx (+7 -11),
  frontend/src/app/components/Sidebar.session.test.tsx 신규 98줄,
  docs/dev-cycle/TODO.md (+9 -11)
- 리뷰: ponytail-review(걷어낼 자리 없음) · code-review(지적 2건, 1건 반영 · 1건 미반영)
- 검증: vitest 21파일 183검사 통과 · pytest 1533 통과 2 스킵 · tsc --noEmit 통과 ·
  시크릿 확인 3종 · qa-only(--quick, 건강 점수 97)
- 표시 기준을 세션으로 정한 근거: 권한 판정(useAdmin)과 사용량 집계가 이미 세션
  이메일을 쓰고 있었다. 사이드바만 localStorage 의 user_profile 을 읽으면 화면에
  보이는 계정과 실제 권한이 붙은 계정이 갈린다. 그래서 세션이 인증 상태이면 세션을
  우선하고, 아니면 기존 프로필로 떨어지도록 했다. 상태로 승격하지 않고 렌더 중에
  계산하므로 useEffect 한 번을 아낀다.
- 미반영 지적: code-review 는 status 가 'loading' 인 구간에 계정 표기가 이전 값으로
  한 프레임 깜빡인다고 지적했다. 없애려면 로딩 전용 표시 상태를 새로 들여야 해서
  항목 범위를 넘는다. 변경 전에는 그 값이 깜빡이는 정도가 아니라 계속 틀린 채로
  남아 있었으므로 이번 변경이 상태를 나쁘게 만들지 않는다. 같은 리뷰가 지적한
  사용량 조회 쪽은 실제 부작용이므로 반영했다. status 가 'loading' 인 동안 조회를
  미루는 가드를 넣어, 이전 사용자의 이메일로 요청이 나가지 않게 했다.
- 실측: qa-only 의 브라우저 검증에서 performance.getEntriesByType('resource') 로
  실제 요청 목록을 읽었다. /api/kr/user/quota 요청은 정확히 한 건이고 email 값이
  비어 있었다. 세션 로딩 구간의 선행 요청이 사라졌음을 이 목록이 뒷받침한다.
  로그아웃 버튼을 누르면 / 로 실제 이동하며, 예전의 "로그아웃 되었습니다" 모달은
  나타나지 않는다. 인증 상태의 계정 표기는 구글 OAuth 로그인이 필요해 브라우저로
  실측하지 못했고, Sidebar.session.test.tsx 의 검사 다섯 개가 그 경로를 덮는다.
- 놓칠 뻔한 자리: `npm run type-check` 의 출력을 `tail` 로 넘겨 읽는 바람에 `$?` 가
  tail 의 종료 코드를 담았고, 실패한 타입 검사를 통과로 오독했다. 신규 테스트 파일의
  `requested` 변수가 `any` 로 추론되어 TS7006 두 건이 나 있었다. `string[]` 로
  명시해 고쳤다. 파이프 뒤의 종료 코드를 종료 코드로 믿지 않는다.
- 부수 발견: 존재하지 않는 API 경로가 404 대신 500 을 돌려주고 logs/backend.log 에
  CRITICAL SERVER ERROR 로 남는다. 전역 예외 처리기가 werkzeug 의 NotFound 까지
  삼키기 때문이다. 이번 변경과 무관한 기존 결함이므로 [INFRA-017] 로 등록했다.

## [JONGGA-002] Gemini 재분석 결과의 AI 사유 유실 수정
- 완료 2026-09-02 08:26 | 티어 T1 | 커밋 1fb8281
- 변경: app/routes/kr_market_jongga_ai_payload_helpers.py (+10 -1),
  app/routes/kr_market_jongga_reanalysis_helpers.py (+1 -0),
  tests/app/test_kr_market_helpers_contract.py (+40 -0),
  docs/dev-cycle/TODO.md (+22 -10)
- 리뷰: ponytail-review(shrink 1건 반영 · 가드 삭제는 근거를 확인하고 기각)
- 검증: pytest 1535 통과 2 스킵 · qa-only(--quick, 건강 점수 97) ·
  주요 경로 7곳 HTTP 200 콘솔 오류 0건

- 고친 내용: `_apply_gemini_reanalysis_results` 가 분석 사유를 `score.llm_reason` 에만
  저장하고 `signal["ai_evaluation"]` 에는 담지 않았다. 그런데
  `_extract_jongga_ai_evaluation` 의 우선순위 체인은 사유가 없는 `signal.ai_evaluation`
  을 먼저 집어 들고 네 번째 폴백까지 내려가지 않는다. 생산 지점과 소비 지점을 함께
  고쳤다. 재분석 헬퍼가 `reason` 을 담고, 추출 함수는 고른 객체의 사유가 비어 있으면
  `score.llm_reason` 으로 한 번 더 내려간다.
- 두 자리를 모두 고친 이유: 소비 지점만 고치면 저장 파일에는 여전히 사유가 빠진
  `ai_evaluation` 이 쌓이고, 생산 지점만 고치면 이 함수의 docstring 이 선언한 네 단계
  우선순위 가운데 마지막 단계가 계속 도달 불가능한 상태로 남는다.
- ponytail-review 의 기각 근거: 소비 지점의 가드를 지울지 따졌다. `data/` 40개 파일을
  훑어 보니 결함 형태(`ai_evaluation` 에 사유가 없고 `score.llm_reason` 에만 있는
  시그널)는 0건이고 `score_details.ai_evaluation` 은 항상 `reason` 을 포함한다.
  그러나 재분석 결과는 `persist_reanalyze_payload` 로 파일에 기록되므로, 이번 수정
  이전에 재분석을 실행한 사용자의 파일에는 결함 형태가 남아 있다. 남기되 중첩을 한 단계
  걷어 다섯 줄로 줄였다.
- 실측: `/api/kr/ai-analysis?date=2026-05-05` 가 시그널 9건을 모두 사유와 함께
  돌려주며 `source` 가 `jongga_v2_integrated_history` 다. 이번에 고친
  `_build_ai_signals_from_jongga_results` → `_extract_jongga_ai_evaluation` 경로를
  실제로 지나온 응답이다. 화면에서 `No analysis available.` 은 0회 나타났다.
- 확인하지 못한 범위: Gemini 재분석을 실제로 실행하지 않았다. 실행하면
  `persist_reanalyze_payload` 가 `data/` 의 결과 파일을 덮어쓰고 API 호출 비용이
  발생한다. 재분석 직후의 사유 유지는 회귀 검사 두 개로 대신 고정했다.
- 부수 발견 두 건: `GET /api/kr/signals/dates` 는 `2026-05-05` 를 제시하는데
  `GET /api/kr/signals?date=2026-05-05` 는 `no_data` 를 돌려준다. 날짜 목록은 종가 계열
  파일에서 만들어지고 시그널 조회는 `vcp_signals_results_*.json` 을 찾기 때문이다.
  화면에서는 유일한 히스토리 날짜를 눌러도 빈 표만 보인다. `[VCP-008]` 로 등록했다.
  종가베팅 화면에서 챗봇 안내 말풍선이 `Trading Tips & Strategy` 카드를 가리는 것은
  `[FE-010]` 으로 등록했다.

## [JONGGA-003] 체크리스트 수급 항목의 키 이름 통일
- 완료 2026-09-02 17:15 | 티어 T1 | 커밋 3a1b379
- 변경: app/routes/kr_market_jongga_normalize_helpers.py (+1 -1),
  tests/app/test_kr_market_helpers_contract.py (+15 -1),
  docs/dev-cycle/TODO.md (+0 -10)
- 리뷰: ponytail-review(중복 단언 한 줄 삭제 · net -1)
- 검증: pytest 1536 통과 2 스킵 · qa-only(--quick, 건강 점수 100 이슈 0건) ·
  주요 경로 7곳 HTTP 200 콘솔 오류 0건

- 고친 내용: `_normalize_jongga_signal_for_frontend` 의 체크리스트 폴백이 수급 키를
  `supply_demand` 로 만들었다. 화면의 `ChecklistDetail` 인터페이스와
  `engine/models.py` 의 같은 이름 데이터클래스는 둘 다 `supply_positive` 를 쓰므로,
  `checklist` 가 없는 시그널이 폴백을 타면 "수급 양호 (외인/기관)" 배지가 켜지지
  않았다. 백엔드를 화면 쪽 이름에 맞췄다.
- 감사 문서보다 범위가 좁았다: `AUDIT-JONGGA §1.2` 는 표기 불일치를 넓게 서술하지만,
  실제로 `supply_demand` 를 만드는 곳은 이 폴백 한 줄뿐이었다. 저장된
  `data/jongga*.json` 180건은 모두 `supply_positive` 를 담고 있고, 정규화가 걸리는
  유일한 경로인 `build_jongga_latest_payload` 도 마찬가지다. 따라서 증상은
  `checklist` 키가 아예 없는 시그널에서만 나타난다.
- 이름이 비슷한 별개 개념은 건드리지 않았다: 챗봇 네 파일의 `supply_demand_score`,
  챗봇의 `supply_demand_stage`, `engine/signal_tracker_supply_helpers.py` 의
  `supply_demand_index` 는 각각 수급 점수·단계·지수라는 다른 개념이다.
- ponytail-review 반영: 회귀 검사에 넣었던 `assert "supply_demand" not in
  signals[0]["checklist"]` 를 지웠다. 바로 다음 줄이 `supply_positive` 를 읽으므로
  옛 키가 되돌아오면 `KeyError` 로 이미 실패한다.
- 폴백 경로를 화면에서 재현하지 않은 이유: 정규화는 `jongga_v2_latest.json` 경로에서만
  걸리는데 그 파일은 `checklist` 를 이미 담고 있고 날짜가 2026-09-01 이라
  `_is_latest_payload_stale` 로 빠진다. 재현하려면 파일의 날짜와 내용을 함께 조작하고
  복원해야 해서 위험 대비 이득이 낮다고 보았다. 대신 둘로 나누어 확인했다. 폴백이 그
  키를 만든다는 것은 회귀 검사로 고정하고, 화면이 그 키를 읽어 배지를 켠다는 것은
  히스토리 날짜로 실측했다.
- 실측: 2026-05-05 히스토리의 아홉 종목을 API 응답값과 대조했다. `supply_positive` 가
  참인 여섯 건은 모두 "수급 양호 (외인/기관)", 거짓인 세 건은 모두 "수급 보통" 으로
  정확히 일치했다. 2026-09-01 히스토리에서도 두 종목이 외국인·기관 모두 순매수여서
  배지가 켜지는 것을 다시 확인했다.

## [JONGGA-004] AI 미분석 종목의 확신도 추정치 표기 정리
- 완료 2026-09-02 09:40 | 티어 T2 | 커밋 ea9ea93
- 변경: frontend/src/app/dashboard/kr/closing-bet/page.tsx (+50 -38),
  frontend/src/app/dashboard/kr/closing-bet/page.jongga-004.test.tsx (신규 165줄),
  docs/dev-cycle/TODO.md (+20 -14)
- 리뷰: ponytail-review(배지 분기 통합 · net -3) → code-review(4건 중 3건 반영)
- 검증: pytest 1536 통과 2 스킵 · vitest 22파일 188건 통과(신규 5건) ·
  npm run type-check 통과 · agent-browser 세 경로 실측 ·
  qa-only(--quick, 건강 점수 100 이슈 0건)

- 고친 내용: 카드의 확신도가 AI 산출값이 아니라 등급에서 만들어진 값이었다. 계산식은
  `score.total * 8 + (S등급이면 10)` 인데 총점 상한이 19점이라 12점만 넘으면 항상
  100% 로 잘렸다. 그 값에 "Gemini AI가 산출한 추천 신뢰도" 라는 툴팁이 붙어 있어서
  AI 가 확신한 값으로 읽힌다. 등급 유도 폴백을 삭제하고, AI 결과가 없는 상태는 값
  없음으로 두어 배지 자리에 "AI 분석 대기", 확신도 자리에 "미산출" 을 표시한다.
- 결함의 실증: `data/jongga_v2_results_20260226.json` 의 35건은 AI 분석을 하나도 받지
  않았는데 화면에는 전부 `BUY 98~100%` 로 표시되었다. 한미반도체는 총점 16점이라
  138 이 100% 로 잘렸고, 현대차는 총점 11점이라 98% 였다.
- 세 곳에 흩어져 있던 `Math.min(aiEval.confidence, 100)` 을 `confidencePct` 하나로
  모았다. 이 정리가 없으면 "값 없음" 판정을 세 곳에 똑같이 넣어야 한다.
- ponytail-review 반영: 배지의 있음/없음을 `Tooltip` 통째로 갈라 놓아 클래스 문자열과
  gray 분기가 두 벌이었다. `aiEval?.action ?? 'AI 분석 대기'` 로 하나에 합쳐 파일이
  3줄 짧아졌다.
- code-review 반영 세 건. ① 재분석 경로(`engine/llm_analyzer_parsers.py:102`)가 LLM
  응답의 confidence 를 형 변환 없이 넘기므로 `"80"` 같은 문자열이 올라온다. 옛 코드는
  `Math.min("80", 100)` 의 암묵 변환으로 80% 를 표시했으므로, 숫자만 받는 새 코드는
  그것을 "미산출" 로 버리는 회귀였다. 숫자로 읽히면 살리도록 고쳤다. ② `Math.min` 이
  상한만 조여 음수가 `width: '-5%'` 로 통과했다. 하한도 함께 조였다. ③ 새로 쓴 툴팁
  문구 "AI가 값을 내지 않았으면 표시하지 않습니다" 가 백엔드의 0 센티널 상황에서
  거짓이 되므로 "값이 없으면 미산출로 표시합니다" 로 바꿨다.
- 반영하지 않은 지적 한 건: 백엔드가 확신도 없음을 `0` 으로 채워 보내는 자리가 다섯
  곳 있다. 화면에서는 그 0 이 AI 가 낸 값인지 값이 없는 상태인지 구분할 수 없으므로
  백엔드에서 고쳐야 한다. `[JONGGA-008]` 로 등록하면서 code-review 가 새로 짚은
  재분석 경로 두 곳(`kr_market_jongga_reanalysis_helpers.py:123`,
  `engine/llm_analyzer_parsers.py:102`)을 항목에 보강했다.
- 회귀 검사의 실효성을 두 번 확인했다. `git stash` 로 구현만 되돌려 초기 3건 중 2건이
  실패하는 것을 보았고, code-review 반영 전 형태로 `confidencePct` 계산부만 임시로
  되돌려 신규 2건이 실패하는 것을 보았다.
- 실측: 세 경로를 모두 확인했다. 2026-02-26(AI 결과 전무, 35건)은 전부 "AI 분석 대기 /
  미산출", 2026-03-06(사유 텍스트만, 13건)은 배지는 BUY 12·HOLD 1 이고 확신도는 전부
  "미산출", 2026-05-05(AI 결과 완전, 9건)은 85·65·75·55·75·72·74·72% 와 68% 로
  저장 데이터 원본값과 정확히 일치했다. 2026-05-05 의 삼성증권은 총점 13점이라 옛
  계산식이었다면 104 가 100% 로 잘려 AI 가 낸 85% 를 덮었을 카드다.

## [INFRA-001] 파이썬 직접 의존성 16개 버전 고정
- 완료 2026-09-02 10:30 | 티어 T3 | 커밋 c58fca7
- 변경: requirements.txt (+34 -14), requirements.updated.txt (삭제 -35),
  docs/dev-cycle/TODO.md (+33 -7),
  docs/superpowers/plans/2026-09-02-infra-001-dependency-pinning.md (신규 223줄)
- 리뷰: ponytail-review(38줄 → 29줄) → code-review(7건 중 3건 반영) →
  review(1건 자동 수정)
- 검증: pytest 1536 통과 2 스킵(기존 venv 와 새 venv 양쪽) · vitest 22파일 188건 통과 ·
  새 venv 에서 pip check 통과와 핀 16개 재현 확인 ·
  qa-only(--quick, 건강 점수 95, 신규 이슈 0건)

- 고친 내용: `requirements.txt` 14줄에 버전 핀이 하나도 없었다. 새 환경에서
  `pip install` 을 돌릴 때마다 다른 버전이 들어오므로 언제 어떤 조합이 깨질지 알 수
  없었다. 직접 의존성 16개를 `==` 정확 핀으로 바꿨다.
- 목록과 실제 사용이 어긋나 있었다. 파이썬 파일 536개를 `ast.walk` 로 훑어 `Import` 와
  `ImportFrom` 노드를 모으고 표준 라이브러리와 로컬 모듈 이름을 뺐다. 목록에 없으면서
  실제로 쓰던 것이 numpy(11곳)·beautifulsoup4(12곳)·httpx(3곳)이고, 검증에 쓰는
  pytest(15곳)도 빠져 있었다. 반대로 apscheduler 와 pytz 는 코드 어디에서도 참조하지
  않았다. 스케줄러는 apscheduler 가 아니라 `schedule` 을 쓴다.
- numpy 를 1.26.4 에 묶은 이유가 두 가지다. `numpy_json_encoder.py:38` 이 numpy 2.0
  에서 제거된 `np.float_` 을 참조하고, `pykrx==1.2.3` 이 `numpy<2.0` 을 요구한다.
  PyPI 메타데이터를 버전별로 조회해 보니 pykrx 는 1.2.6 까지 `numpy<2.0` 이고 1.2.7
  부터 `numpy>=2.0` 으로 뒤집힌다. 즉 `np.float_` 만 고쳐서는 numpy 2.x 로 갈 수
  없고 pykrx 승격이 한 묶음이다. 이 사실로 `[INFRA-018]` 을 새로 등록했다.
- google-genai 승격은 보류했다. 격리 venv 에 2.21.0 을 설치해 이 저장소가 쓰는 API
  표면 아홉 가지(`Client` 의 네 인자, `client.models`·`client.aio.models`·
  `models.list`, `generate_content` 의 세 인자, `GenerateContentConfig`,
  `GenerativeModel` 의 부재, `errors.APIError`·`ClientError`)를 대조했더니 1.62.0 과
  전부 같았다. 그럼에도 확인한 것은 API 표면이지 호출 동작이 아니다. 응답 파싱과 예외
  메시지, 재시도 거동은 Vertex AI 로 실제 과금 호출을 해야 알 수 있어 승격을 분리했다.
- `AUDIT-INFRA.md:208` 이 우려한 "2.x 로 올리면 numpy 가 전이 의존성으로 따라 온다" 는
  반증됐다. 2.21.0 이 끌고 오는 24개에 numpy 는 없다. 필수 의존성은 두 버전 모두
  10개로 같고 실제 차이는 하한 세 개뿐이다. `google-auth` 가 2.47.0 에서 2.56.0 으로
  오르고, `pydantic` 과 `typing-extensions` 의 하한은 현재 환경이 이미 충족한다.
- `requirements.updated.txt` 를 삭제했다. 2026-05-05 Vertex 마이그레이션 때의
  `pip freeze` 산출물인데 PyPI 에 없는 개발 버전(`google-auth==2.49.0.dev0`)이 들어
  있어 그대로는 설치되지 않고, 참조처가 문서 언급뿐이었다.
- 전이 의존성은 고정하지 않는다. 그래서 새 환경에는 peewee 4.4, protobuf 7.36,
  lxml 6.1 처럼 기존 venv 보다 최신인 것이 들어온다. 그 조합에서도 pytest 1536건이
  통과함을 확인했다.
- ponytail-review 반영: 16줄짜리 목록에 `# --- 웹 서버 ---` 같은 섹션 헤더 7개와 빈 줄
  6개를 두는 것이 구조 과잉이라는 지적이다. 헤더를 걷어내고 "갱신은 pip install -U 로
  한다" 는 pip 상식 한 줄을 지웠으며 주석 두 개를 각각 한 줄로 줄였다. 38줄에서
  29줄이 됐다.
- code-review 반영 세 건. ① `[INFRA-018]` 에 적은 numpy 차단 원인이 틀렸다. "pandas 와
  yfinance 가 요구하는 시점이 오면" 이라고 썼는데 실제로는 pykrx 가 지금 막고 있다.
  항목을 다시 썼다. ② `google-genai` 위에 붙인 승격 보류 주석이 바로 아래 `openai`
  핀에도 걸리는 것처럼 읽혀 "아래 openai 핀에는 해당하지 않는다" 를 덧붙였다.
  ③ 머리 주석이 이 파일을 완전한 잠금처럼 읽히게 해서 전이 의존성은 pip 이 그때그때
  결정한다는 사실을 명시했다.
- review 반영 한 건: 주석에 쓴 "전이 의존성 26개" 가 사실과 달랐다. PyPI 의
  `requires_dist` 를 조건절까지 살려 다시 파싱하니 torch·transformers 등은 전부
  extras 였다. 실측값으로 교체했다.
- 반영하지 않은 지적 두 건과 이유. 빈 `## P0` 절의 헤더를 남긴 것은 `archive-format.md`
  의 형식이 세 절 헤더를 모두 두도록 정하기 때문이다. 아카이브 기록이 첫 커밋에 없는
  것은 같은 문서가 두 커밋 분리를 명시하기 때문이다.
- 검증 도중 `npm run test` 가 7분간 멈췄다. `frontend/package.json:10` 의
  `"test": "vitest"` 가 감시 모드라 188건을 다 끝낸 뒤에도 워커가 남고 출력이 버퍼에
  갇힌다. `npx vitest run` 으로 16.98초에 끝냈고 이 사실을 `[FE-011]` 로 등록했다.
- qa-only 결과는 건강 점수 95 이고 신규 발견이 없었다. 대시보드 7개 페이지가 모두 200
  이며 콘솔 에러가 하나도 없고, 등록된 백엔드 API 9개가 전부 200 이다. 의존성 핀
  변경으로 인한 런타임 회귀는 없다. 리포트가 짚은 두 건은 이미 백로그에 있는
  `[INFRA-017]`(404 를 500 으로 반환)과 `[FE-010]`(챗봇 말풍선 겹침)이다. 후자에는
  이번 실측을 보탰다. 뷰포트 폭 900px 에서 말풍선이 필터 줄의 새로고침 버튼을 완전히
  덮어 클릭 자체를 막는다. 처음 기록한 "가려서 잘려 보인다" 보다 심각하다.
- 이 사이클에서 지운 테스트는 없다.

## [VCP-007] AI 캐시 병합이 정상 추천을 지우고 실패 추천을 통과시키는 결함 수정
- 완료 2026-09-02 | 티어 T2 | 커밋 25c30de
- 변경: app/routes/kr_market_vcp_signal_helpers.py (+34 -21),
  tests/app/test_vcp_failed_ai_filter.py (+83 -1),
  docs/dev-cycle/TODO.md (+47 -12)
- 리뷰: /ponytail-review → /code-review 순서대로 실행. ponytail 지적 2건은 전부
  반영해 순 8줄을 줄였고, code-review 7건 중 2건을 반영, 2건은 프론트엔드 결함이라
  분리, 3건은 반영하지 않았다.
- 검증: pytest 1541 통과 2 skipped (기존 1536 + 신규 5) · vitest 22파일 188건 통과 ·
  API 레벨 결함 재현과 해소 대조 · agent-browser 화면 실측 6행 일치 ·
  /qa-only quick 실측 health 95/100
- 메모: 결함의 자리는 한 곳이다. `_merge_ai_data_into_vcp_signals` 가 캐시에 티커가
  있기만 하면 `gemini_recommendation`, `gpt_recommendation`, `perplexity_recommendation`
  세 필드를 조건 없이 대입했다. 캐시에 추천이 없으면 CSV 에서 만든 정상 추천이 `None`
  으로 지워지고, 캐시에 실패 기록이 들어 있으면 `[VCP-001]` 이 CSV 경로에서 막아 둔
  실패 추천이 이 경로로 되살아난다.

  TODO 에 적힌 근거 한 줄이 사실과 달라 정정했다. TODO 는 `data/vcp_signals_results_*.json`
  세 파일이 티커만 있고 추천이 없다는 것을 첫 번째 문제의 실재 근거로 들었다. 실측하니
  그 파일은 다섯 개이고 전부 추천이 없는 것은 맞지만, 병합 경로가 그 파일을 읽지 않는다.
  실제로 읽는 것은 `ai_analysis_results_{날짜}.json`(7개)과 폴백 `kr_ai_analysis.json`
  이고 그 여덟 파일에는 추천이 모두 정상이다. 즉 첫 번째 문제도 지금 데이터로는
  재현되지 않고 구조만 허용한다. 고칠 값어치는 그대로인데, 두 캐시 형식(추천 dict 를
  담는 형식과 `ai_action`/`ai_reason` 만 담는 형식)이 공존하고 `_build_ai_data_map` 이
  형식을 가리지 않기 때문이다.

  재현 조건을 손으로 만들어 대조했다. `data/signals_log.csv` 의 000660·005930·034730
  세 행에 `is_vcp=True` 와 정상 `ai_action`/`ai_reason` 을 넣고, 두 캐시 파일에서 같은
  세 티커의 `gemini_recommendation` 만 지웠다. legacy 파일까지 손대야 했던 이유는
  `_merge_legacy_ai_fields_into_map` 이 `kr_ai_analysis.json` 에서 보강해 결함 조건을
  무력화했기 때문이다. `git stash` 로 구현만 되돌려 두 상태를 비교했다.

  | 티커 | 수정 전 | 수정 후 | 캐시 상태 |
  |---|---|---|---|
  | 034730 SK | `None` | `BUY` | 티커 있음, 추천 없음 |
  | 000660 SK하이닉스 | `None` | `BUY` | 티커 있음, 추천 없음 |
  | 005930 삼성전자 | `None` | `HOLD` | 티커 있음, 추천 없음 |
  | 005380 현대차 | `BUY` | `BUY` | 캐시에 없음 |
  | 003550 LG | `HOLD` | `HOLD` | 캐시에 없음 |
  | 145020 휴젤 | `BUY` | `BUY` | 캐시에 없음 |

  ponytail-review 반영 2건: 모듈 상단에 둔 `_AI_RECOMMENDATION_FIELDS` 상수를 쓰는
  곳이 한 곳뿐이라 호출처에 인라인했고, 주석 3줄을 1줄로 줄였다. 순 8줄이 줄었다.

  code-review 반영 2건. ① 판정 규칙이 두 함수에 각자 구현되어 있었다.
  `_is_valid_ai_verdict(action, reason)` 를 공통 술어로 두고 `_is_vcp_ai_analysis_failed`
  (CSV 행)와 `_is_valid_ai_recommendation`(캐시 dict)이 모두 그것을 부르게 했다.
  ② 정상 추천끼리 캐시가 이기는지 확인하는 검사가 없었다. "덮어쓰지 않는다" 를 "기존
  값이 있으면 건드리지 않는다" 로 잘못 읽으면 재분석 결과가 화면에 반영되지 않으므로
  회귀 검사를 1건 더 추가했다.

  code-review 지적 중 프론트엔드 관련 2건은 실재하지만 영향 범위가 리뷰 설명과 달랐다.
  `AI_ACTION_BADGES` 에 `N/A` 항목이 없어 실패 기록은 배지가 아니라 `-` 로 나간다.
  실제 영향은 데이터가 없는 AI 탭이 활성 상태로 보이는 것이다. `[FE-012]` 로 남겼다.

- 반영하지 않은 지적 3건과 이유. ① `merged_count` 가 실제 병합 없이도 증가한다는
  지적은, 이 값의 기존 의미가 "맵에 티커가 있어 처리한 시그널 수" 이고 news 병합도
  포함하므로 debug 로그 한 줄을 위해 상태 변수를 늘리지 않기로 했다.
  ② `_merge_legacy_ai_fields_into_map` 의 검증 게이트는 호출자가 하나이고 바로 뒤에
  이번에 고친 병합이 오므로 같은 문턱을 두 번 두는 셈이라 넣지 않았다. ③ 정규화 이중
  수행은 리뷰 스스로 correctness 문제가 아님을 밝혔고, 값을 함께 반환하도록 바꾸면
  병합 경로가 쓰지 않는 반환값을 받게 되어 그대로 두었다.
- qa-only 결과는 건강 점수 95 이고 신규 발견이 3건이다. 셋 다 이번 변경과 무관한 기존
  결함이라 백로그로 넘겼다. ① `/api/kr/signals` 가 `stop_loss` 와 `target_price` 를
  `null` 로 내려보내고 화면이 `현재가 × 0.97` 과 `현재가 × 1.05` 로 채운다. 기준가가
  진입가가 아니어서 여섯 행 중 네 행에서 손절가가 진입가보다 높게 표시된다(`[VCP-009]`).
  ② 과거 날짜에서 일괄 매수는 막히는데 행별 매수 버튼 여섯 개는 활성이다(`[FE-013]`).
  ③ 고정된 채팅 위젯이 VCP 표의 오른쪽 아래 셀을 가린다(`[FE-014]`). 콘솔 오류는 여섯
  페이지 전부 0건이다.
- 실측을 위해 조작한 `data/` 의 세 파일은 스크래치패드 백업에서 원복했고 md5 로 일치를
  확인했다. `data/` 는 git 추적 대상이 아니다.
- 이 사이클에서 지운 테스트는 없다.

## [FE-012] VCP 화면이 걸러진 AI 추천을 원시 캐시에서 되살린다
- 완료 2026-09-02 | 티어 T2 | 커밋 984282f
- 변경: frontend/src/app/dashboard/kr/vcp/aiHelpers.ts (+40),
  frontend/src/app/dashboard/kr/vcp/page.tsx (+13 -13),
  frontend/src/app/dashboard/kr/vcp/aiHelpers.test.ts (+35 -2),
  app/routes/kr_market_signal_common.py (+3),
  docs/dev-cycle/TODO.md (+40 -27)
- 리뷰: /ponytail-review → code-review 순서대로 실행. ponytail 지적 4건 중 3건 반영,
  code-review 는 신뢰도 80 이상 결함 없음에 스타일 권고 1건 반영.
- 검증: pytest 1541 통과 2 skipped · vitest 22파일 197건 통과 (기존 188 + 신규 9) ·
  npm run type-check 무오류 · agent-browser 수정 전후 대조 · /qa-only quick
  실측 health 95/100
- 메모: `[VCP-007]` 이 백엔드의 응답 조립 경로에서 실패 추천을 걸러 냈는데도 결함이
  남은 이유는 화면의 폴백 경로에 있다. 병합된 값이 비면 `aiData?.signals` 를 다시
  보는데, 그 자료는 `/api/kr/ai-analysis` 가 내려보내는 원시 캐시라서 실패 기록이
  그대로 들어 있다. 값의 존재만 보고 판정하면 그 기록이 정상 분석으로 통과한다.

  `aiHelpers.ts` 에 유효성 술어 `isValidAIRecommendation` 을 두고, `page.tsx` 의 네
  자리가 그것을 쓰게 했다. TODO 는 세 자리(`secondaryAI`, `hasData`, `getRec`)를
  적었지만 `getAIBadge`(`:923`, `:932`)도 같은 판정을 하고 있어 함께 고쳤다. 판정
  기준은 백엔드의 `_VALID_AI_ACTIONS` 와 `_INVALID_AI_REASONS` 를 그대로 옮긴 것이며,
  두 곳이 갈라지지 않도록 백엔드 상수 앞에 프론트엔드 역참조 주석을 남겼다.

  티어는 T1 로 시작해 T2 로 올렸다. `git diff --stat -- ':!*test*'` 가 구현만 54 추가
  13 삭제로 67줄을 내어 T1 의 50줄 상한을 넘었기 때문이다.

  실측 조건은 CSV 여섯 티커에 `is_vcp=True` 와 정상 AI 값을 채우고,
  `kr_ai_analysis_20260505.json` 의 네 건에 `{"action": "N/A", "reason": "분석 실패"}`
  를 `perplexity_recommendation` 으로 심어 만들었다. 수정 전에는 표의 마지막 두 열이
  PERPLEXITY·GEMINI 였고 perplexity 열 여섯 행이 전부 `-` 였다. 수정 후에는 GPT·GEMINI
  로 바뀌고 GPT 열에 배지 세 개가 나온다. 상세 패널의 AI 탭도 GPT 와 Gemini 두 개만
  나와 `getRec` 자리까지 확인했다.

  `/api/kr/ai-analysis?date=` 는 `jongga_v2_results_{date}.json` 을 가장 먼저 보는데
  변환 함수가 perplexity 필드를 옮기지 않는다. 실측하는 동안 그 파일을 잠시 옮겨
  `kr_ai_analysis_{date}.json` 경로를 타게 했고, 끝난 뒤 제자리로 되돌렸다.
- 반영하지 않은 지적 1건과 이유. ponytail 은 캐시 통합 검사가 프로덕션 코드를 부르지
  않는다며 삭제를 권했다. 그런데 그 검사는 술어를 직접 부르고 있고, TODO 의 두 번째
  체크 항목이 "실패 기록만 든 캐시에서 탭이 활성화되지 않는지 확인하는 회귀 검사" 를
  명시적으로 요구한다. 컴포넌트를 렌더링하지 않고 술어와 탭 결정의 연결을 확인할 수
  있는 가장 작은 형태라서 남겼다.
- qa-only 결과는 건강 점수 95 이고 신규 발견이 2건이다. 둘 다 이번 변경과 무관한 기존
  결함이라 백로그로 넘겼다. ① 종가베팅의 리포트 선택기 기본값 `Latest Report` 가 가장
  최근 저장분이 아니라 오늘 날짜를 조회한다. 배너는 "최신 저장 데이터는 2026-09-01"
  이라고 알리는데 첫 화면은 `NO DATA` 로 비어 있고, `2026-09-01` 을 직접 고르면 카드
  두 장이 나온다(`[JONGGA-009]`). ② VCP 상세 차트가 선택한 과거 날짜를 반영하지 않아
  2026-05-05 시그널을 열어도 가로축이 6월부터 9월까지 이어지고, 수축비율이 표에서는
  0.41 차트에서는 0.77 로 두 가지가 표시된다(`[VCP-010]`). 직전 사이클이 올린
  `[VCP-009]` 는 이번에도 그대로 재현되어 중복 등록하지 않았다. 콘솔 오류는 여섯
  페이지 전부 0건이다.
- 실측을 위해 조작한 `data/` 의 네 파일은 스크래치패드 백업에서 원복했고 md5 로 일치를
  확인했다. `data/` 는 git 추적 대상이 아니다.
- 이 사이클에서 지운 테스트는 없다.

## [VCP-009] VCP 화면의 손절가·목표가가 진입가가 아니라 현재가에서 만들어진다
- 완료 2026-09-02 12:16 | 티어 T2 | 커밋 3b7925a
- 변경: app/routes/kr_market_vcp_signal_helpers.py (+39 -3),
  tests/app/test_kr_market_vcp_signal_helpers_refactor.py (+90),
  frontend/src/app/dashboard/kr/vcp/page.tsx (+5 -14),
  frontend/src/lib/api.ts (+4),
  services/kr_market_vcp_signals_cache.py (+3 -1),
  docs/dev-cycle/TODO.md (+31 -20)
- 리뷰: /ponytail-review → /code-review 순서대로 실행. ponytail 지적 3건 중 1건 반영,
  code-review 지적 6건 중 4건 반영.
- 검증: pytest 1547 통과 2 skipped (직전 1541 + 신규 6) · vitest 22파일 197건 통과 ·
  npm run type-check 무오류 · agent-browser 수정 전후 대조 · /qa-only quick
  실측 health 91/100
- 메모: 근본 원인은 화면이 아니라 자료에 있었다. `data/signals_log.csv` 의 열 21개
  안에 `target_price` 와 `stop_price` 가 없어서 `_build_vcp_signal_from_row` 가 두
  값을 늘 `None` 으로 내려보냈고, 화면이 그 자리를 `현재가 × 0.97` 과 `현재가 × 1.05`
  로 메웠다. `engine/position_sizer.py:41-44` 는 진입가 기준으로 계산하므로 시스템의
  정의와 화면이 어긋나 있었다. 종가베팅은 이미 백엔드에서 진입가 기준으로 채우는
  방식(`kr_market_jongga_normalize_helpers.py:122-126`)을 쓰고 있어 그 관례를 따랐다.

  `_resolve_vcp_exit_prices` 를 백엔드에 두고 저장된 값이 없을 때만 진입가 기준으로
  채운다. 진입가가 없으면 임의로 만들지 않고 비워 둔다. 비율은 하드코딩하지 않고
  `engine/config.py:297` 의 싱글턴 `config` 를 참조한다. 처음에는 행마다
  `SignalConfig()` 를 새로 만들었는데 code-review 가 잡아 싱글턴으로 바꿨다. 그
  생성자는 `os.getenv` 를 11회 호출하므로 행 수만큼 반복될 자리였다.

  `math.isfinite(entry)` 가드도 code-review 지적으로 넣었다. 진입가가 `inf` 인 행이
  하나 있으면 `round()` 가 OverflowError 를 던져 응답 전체가 무너진다. 이 가드가
  NaN 도 함께 잡으므로 앞서 있던 `_none_if_nan` 중복 호출은 지웠다.

  반영하지 않은 지적 두 건이다. ① 종가베팅과 공용 헬퍼로 통합하라는 지적은 두 곳의
  비율이 다르고(1.09/0.95 대 1.05/0.97) `[JONGGA-007]` 이 이미 종가베팅 쪽 리터럴
  정리를 담당하는 백로그 항목이라 미뤘다. ② `if not resolved_target:` 이 0 을 결측으로
  취급한다는 지적은 주가 0 이 유효한 값이 아니므로 재계산이 맞다고 판단했다.

  응답 스키마가 실제로 바뀌었으므로 `_VCP_SIGNALS_CACHE_SCHEMA_VERSION` 을 3 에서 4 로
  올렸다. 3 으로 굳어 있던 캐시에는 두 값이 `null` 로 들어 있어 무효화가 필요하다.

  agent-browser 실측은 `data/signals_log.csv` 를 백업한 뒤 여섯 종목의 `is_vcp` 를
  `True` 로 세워 만들었다. 수정 전에는 SK 의 손절가가 ₩568,420 으로 진입가 ₩475,500
  보다 19.5% 위였고 현대차의 목표가가 ₩420,525 로 진입가 ₩609,000 보다 31.0% 아래였다.
  수정 후에는 여섯 행 전부 손절가 < 진입가 < 목표가 가 성립한다. 자료는 백업본에서
  원복했고 md5 `9337b9db00b1fd2c58f57104527ae65c` 일치를 확인했다.

  qa-only 결과는 건강 점수 91 이고 신규 발견이 2건이다. 둘 다 모바일 뷰포트에서
  나왔고 이번 변경과 무관하다. ① 375px 에서 빈 상태 문구를 담은 `td` 의 폭이 995px
  인데 가로 스크롤 영역의 표시 폭은 341px 이라 문구가 화면 밖으로 밀려난다. 과거
  날짜를 고르면 경고 배너까지 사라져 설명 없는 빈 상자만 남는다(`[FE-015]`).
  ② `VCP 전체 10주 매수` 버튼의 좌우 좌표가 -47 부터 99 까지라서 이름이 잘리고,
  문서의 `scrollWidth` 가 뷰포트와 같아 가로로 밀어서 되돌릴 수도 없다(`[FE-016]`).
  채팅 위젯이 종가베팅·누적 성과·데이터 상태의 본문도 덮고 있어 `[FE-014]` 의 대상
  범위를 VCP 화면 하나에서 네 화면으로 넓혔다.

  qa-only 로는 이번 수정의 STOP·TARGET 열을 확인하지 못했다. 실제 자료에 VCP 조건을
  통과하는 시그널이 0건이라 표가 비어 있기 때문이다. `데이터 상태` 화면은
  `signals_log.csv` 가 15행이고 기준일이 2026-05-05 라고 표시하는데 `과거` 에서 그
  날짜를 골라도 `No signals found.` 가 나온다. 날짜 목록은 저장분 전체를 보고 표는
  VCP 조건 통과분만 보기 때문이며, 이미 `[VCP-008]` 로 등록되어 있다.

  콘솔 오류는 페이지별로 버퍼를 비우고 개별 측정해 일곱 자리 모두 0건이었다. 측정
  도중 하이드레이션 불일치를 한 차례 관찰했으나 8회 재시도로 재현되지 않았고
  `[FE-008]` 이 이미 다루고 있어 중복 등록하지 않았다.

  이 사이클에서 지운 테스트는 없다.

## [FE-013] 과거 날짜 VCP 화면에서 행별 매수 버튼만 살아 있다
- 완료 2026-09-02 12:41 | 티어 T1 | 커밋 d0f23a8
- 변경: frontend/src/app/dashboard/kr/vcp/page.tsx (+16 -10),
  frontend/src/app/dashboard/kr/vcp/page.regression-fe-013.test.tsx (+86),
  docs/dev-cycle/TODO.md (+21 -7)
- 리뷰: /ponytail-review 실행. 지적 2건 중 1건 반영.
- 검증: vitest 23파일 200건 통과 (직전 197 + 신규 3) · npm run type-check 무오류 ·
  npm run lint 오류 0건 경고 219건(기존 수준) · /qa-only quick 실측 health 98/100
- 메모: 근본 원인은 날짜 조건 판정이 일괄 매수 경로 안에만 있었다는 점이다.
  `activeDateTab !== 'latest'` 라는 같은 판정이 흩어져 있고 행별 버튼에는 아예 없어서,
  개별 주문을 종목 수만큼 반복하면 일괄 매수가 막으려던 결과에 그대로 도달했다.
  ponytail 사다리 2번에 따라 새 상태나 훅을 만들지 않고 기존 `activeDateTab` 으로
  `buyDisabledReason` 파생 변수 하나를 만들어 경고 모달·일괄 버튼·행별 버튼 세 곳이
  함께 참조하게 했다. 문구도 한 곳에만 존재하므로 한쪽만 고쳐서 어긋나지 않는다.

  ponytail 지적 중 반영한 것은 `isBulkBuyVCPDisabled` 의 네 조건이
  `bulkBuyVCPDisabledReason` 의 네 분기와 정확히 동치라는 지적이다.
  `Boolean(bulkBuyVCPDisabledReason)` 한 줄로 줄이고 선언 순서를 뒤로 옮겼다.
  반영하지 않은 것은 `disabled:hover:bg-rose-500/10 disabled:hover:text-rose-500` 두
  클래스를 `disabled:pointer-events-none` 하나로 줄이라는 지적이다.
  `pointer-events: none` 은 `cursor-not-allowed` 까지 무력화해서 막혀 있다는 신호가
  커서에서 사라지고, 같은 화면의 일괄 매수 버튼이 `disabled:cursor-not-allowed` 를
  쓰고 있어 어긋난다.

  회귀 검사는 처음에 `toBeDisabled` 같은 jest-dom matcher 로 썼는데 `tsc --noEmit` 이
  4건을 막았다. `vitest.setup.ts` 가 `expect.extend(matchers)` 로 런타임에는 등록하지만
  타입 선언은 없기 때문이다. 이 실패가 `tests/smoke/upgrade-smoke.test.ts` 의
  "TypeScript Compilation" 검사까지 무너뜨렸다. 저장소의 다른 테스트가 모두 순수 vitest
  matcher 를 쓰고 있어 `btn.disabled).toBe(true)` 와 `btn.getAttribute('title')` 로
  맞췄다. 가드를 임시로 지우면 두 번째 검사만 실패하는 것까지 확인하고 원복했다.

  qa-only 실측은 `data/signals_log.csv` 를 백업한 뒤 점수 상위 6행에 `is_vcp=True` 를
  세우고 그중 3행의 `signal_date` 를 오늘로 옮겨 만들었다. `/api/kr/signals` 는 date
  파라미터가 없으면 오늘 날짜로 조회하므로, 날짜를 옮기지 않으면 최신 탭이 비어 있어
  활성 상태를 볼 수 없다. 최신 탭에서 행별 버튼 3개가 `disabled=false` 이고 툴팁이
  "모의 계좌로 매수 주문을 실행합니다." 였다. 과거 2026-05-05 를 고르면 3개 모두
  `disabled=true` 에 `cursor: not-allowed`, `opacity: 0.4` 이고 툴팁이 일괄 매수
  버튼과 똑같이 "최신(오늘) VCP 시그널 탭에서만 매수를 실행할 수 있습니다." 로 바뀐다.
  자료는 백업본에서 원복했고 `diff` 로 완전 일치를 확인했다.

  qa-only 건강 점수는 98 이고 신규 발견 2건은 모두 접근성 항목이다. 이 화면의 버튼
  17개 가운데 7개가 텍스트도 `aria-label` 도 없고, 그중 4개는 `title` 조차 없어
  접근성 트리에 이름이 빈 문자열로 올라간다. 나머지 3개가 이번에 손댄 행별 매수
  버튼인데 `title` 하나에만 기대고 있다. 두 건을 묶어 `[FE-017]` 로 등록했다.
  변경 전에도 이 버튼은 아이콘뿐이었으므로 이번 변경이 만든 결함은 아니다.

  실제로 존재하는 내비게이션 경로 7개를 각각 격리해 6초씩 관찰했고 콘솔 오류와 실패
  요청이 모두 0건이었다. 처음에는 `/dashboard/kr/jongga` 처럼 이름을 추측해서 돌렸다가
  네 경로가 전부 404 를 내는 것을 콘솔 오류로 잘못 셀 뻔했다. 사이드바의 실제 경로는
  `/dashboard/kr/closing-bet`, `/dashboard/kr/cumulative`, `/chatbot`,
  `/dashboard/data-status` 다.

  `[FE-016]` 은 이번 실행에서 재현되지 않았다. 375×812 에서 `VCP 전체 10주 매수`
  버튼의 좌우 좌표가 10 부터 157 로 나와 온전히 보이고 `document.body.scrollWidth` 도
  375 로 뷰포트와 같다. 등록 당시 좌표는 -47 부터 99 였다. 착수 전에 어떤 조건에서
  잘리는지 먼저 특정하라는 관찰을 항목에 덧붙였다.

  이 사이클에서 지운 테스트는 없다.

# 2026-09-06

## [CHAT-021] 챗봇 프로필 API 가 소유자 판정 없이 한 벌을 공유한다
- 완료 2026-09-06 07:15 | 티어 T2 | 커밋 40b37a2
- 변경: services/kr_market_chatbot_request_helpers.py, chatbot/runtime_setup_service.py,
  frontend/src/app/chatbot/page.tsx 외 5개 (+165 -28, 문서 +151 -19)
- 리뷰: ponytail-review(같은 사실의 사본 넷을 판정이 일어나는 헬퍼 한 곳으로 줄여 net -25) ·
  feature-dev:code-reviewer(지적 6건 가운데 3건 반영 — 만들어질 수 없는 값을 근거로 들던
  독스트링 수정, `update_user_profile` 의 `owner_id` 를 필수 인자로 승격, 헤더 이름 오타를
  잡는 라우트 수준 검사 추가. 나머지 3건은 범위 밖으로 판정해 백로그로 이월)
- 검증: pytest 1701 통과 2 스킵 · vitest 279 통과 · tsc 오류 없음 ·
  QA 시나리오 9/9 통과 (헬스 100)
- 메모:
  **저장소 마이그레이션이 필요 없었다.** `chatbot_memories` 를 읽기 전용으로 조회해
  `user_profile` 행이 어느 소유자에게도 없음을 확인했다. 공용(`owner_id = ''`)에는
  `daily_suggestions_default_empty` 와 `interest` 두 행뿐인데 둘 다 프로세스 공용 캐시다.
  옮길 사용자 데이터가 없으므로 TODO 의 「공용에 남아 있던 기존 `user_profile` 을 어떻게
  다룰지 정함」 체크박스는 「옮길 데이터가 없다」로 닫았다.

  **조회와 저장의 판정을 갈랐다.** 조회는 소유자를 몰라도 막지 않는다. 사용자가 저장한
  프로필이 공용에 들어갈 경로가 없어 `DEFAULT_PROFILE` 이 나오고 화면이 그대로 그려지기
  때문이다. 저장만 400 으로 막아 공용 오염을 차단했다. 이 판정이 안전한 이유는 공용에
  사용자 데이터가 들어갈 경로가 셋 다 막혀 있기 때문이다. `update_user_profile` 이 400 을
  돌려주고, `/memory` 명령이 소유자 없는 요청을 거부하며, `init_user_profile_from_env` 는
  가드 때문에 발동하지 않는다.

  **프로필이 이제 시스템 프롬프트에 실린다.** `format_for_prompt(owner_id)` 가 소유자
  버킷의 모든 키를 순회하므로, 프로필이 그 버킷에 들어가면서
  `- **user_profile**: {'name': ..., 'persona': ...}` 형태로 새로 실린다. 종전에는 공용에만
  있어 어느 프롬프트에도 실리지 않았다. 의도한 변화이므로 pytest 로 고정해 두었다
  (`test_saved_profile_reaches_only_its_owner_prompt`).

  **프런트엔드를 함께 고쳐야 변경이 동작한다.** 프로필 POST 가 `X-Session-Id` 를 보내지
  않아서 서버만 고치면 소유자가 늘 미상이 되어 아무것도 나뉘지 않는다.
  `page.tsx:290` 의 `headers` 에 `getAuthHeaders()` 를 펼쳐 넣은 한 줄이 그 연결이다.

  **gunicorn 은 `--reload` 없이 돈다.** QA 전에 마스터 8939 에 HUP 을 보내 워커를
  83250·83251 로 재기동했다. 이 절차를 빠뜨리면 워커가 기동 시점의 옛 코드를 들고 있어
  시나리오가 새 동작을 검사하지 못한다.

- 이월: 소유자 사칭 대응은 같은 근본 원인(`X-User-Email` 무검증)을 다루는 `[INFRA-027]` 에
  관찰과 체크박스로 합쳤다. 새 항목을 세우려다 백로그를 훑어 중복을 발견해 되돌렸다.
  대시보드 설정창의 저장 경로 불일치와 `res.ok` 무시는 `[CHAT-026]`, 프로필이 메모리
  영역에 들어오며 생긴 어긋남 셋(`/memory view` 노출, `/clear all` 문구,
  `init_user_profile_from_env` 죽은 코드)은 `[CHAT-027]` 로 신설했다. QA 가 시나리오 밖에서
  발견한 헤딩 깜빡임(첫 페인트에 `안녕하세요, 님`)도 `[CHAT-026]` 에 덧붙였다.

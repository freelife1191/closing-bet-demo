# 2026-09-24

## [INFRA-082] 캐시가 아닌 `data/` 절대 경로가 격리 실행에서도 원본을 가리킨다
- 완료 2026-09-24 00:09 | 티어 T2 (위험 경로 아님) | 커밋 d035905
- 변경: app/routes/kr_market.py (`VCP_STATUS` 를 같은 파일의 `DATA_DIR` 기준으로), scripts/run_full_update.py·scripts/verify_collection_logic.py (`run.py` 와 같은 루트 `os.chdir` 한 줄씩), tests/app/test_kr_market_route_integration.py, docs/dev-cycle/qa/INFRA-082.md
- 분류: 모듈 기준 `data/` 경로 18개 파일, cwd 기준 24개 파일. `scripts/init_data.py`·`engine/screener.py`·`services/common_update_pipeline_steps.py` 는 루트 기준으로 서로 짝이 맞고, 두 종가 알림 가드(`services/kr_market_jongga_runtime_service.py:40`·`scripts/init_data.py:1722`)도 루트 기준으로 같은 파일을 본다. `services/common_update_ai_analysis_service.py:176`(인자 없을 때 기본값)·`services/kr_market_realtime_price_cache.py:125`(대체 경로)·`services/kr_market_vcp_background_service.py`·`services/kr_market_stock_detail_service.py`·`services/paper_trading.py`·`chatbot/core*`·`scripts/debug_*` 는 그대로 둔다. `services/usage_tracker.py`·`engine/services/usage_tracker.py` 는 `[INFRA-079]` 삭제 대상. 결함은 cwd 가 루트가 아닐 때 cwd 로 쓴 파일(`engine/generator.py` 의 `data_dir="data"`)을 루트 기준으로 읽는 경우뿐이며 운영(`restart_all.sh:7` 의 `cd`, `run.py:14` 의 chdir)은 해당하지 않는다
- 설계: bounded(2026-09-24 사용자 AskUserQuestion 「A. 진입점 고정 + 두 곳 (Recommended)」, 승인 확인 00:02). 제시한 3번(런타임 가드 경로를 cwd 로)은 R1 의 읽기 지점이 아니고 한쪽만 바꾸면 가드 파일이 갈라져 중복 발송이 가능해서 빼고, 축소안에 「1·2번만 진행 (Recommended)」 승인
- 테스트: `test_vcp_status_file_follows_cwd_data_dir` 추가, 구현 전 AssertionError 로 실패. CLI 의 chdir 한 줄은 테스트 없이 QA 로 확인
- 리뷰: 과잉설계 리뷰(`infra082-ponytail`, 별도 레인) 「Lean already. Ship.」. `closing-bet-reviewer` APPROVE(max low). 질문 넷(다른 vcp_status 경로 없음, chdir 가 import·인자에 영향 없음, 운영 cwd 루트, 가드 제외 판단) 확인. 발견 1·2 는 `[INFRA-086]` 으로 이월, 발견 3(테스트가 경로 문자열만 봄)은 conftest 세션 끝 검사와 QA S-4 가 계약을 덮어 미반영
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2727 passed 2 skipped(exit 0)
- QA: `docs/dev-cycle/qa/INFRA-082.md` 필수 4/4 통과. 사본에서만 `init_data.py` 를 cwd 기록용 가짜로 바꿔 두 CLI 를 루트 밖에서 실행해 cwd 가 사본 루트임을 확인(chdir 를 뺀 대조본은 루트 밖). /dashboard/kr 의 `/api/kr/signals/status` 가 사본 `data/vcp_status.json` 의 표지 `QA-INFRA-082` 를 돌려줌·콘솔 오류 0. 파일이 없는 트리에서 pytest 가 파일을 만들지 않음(변경 전 코드 대조는 conftest 검사에 걸려 exit 1). 증거 `docs/dev-cycle/evidence/INFRA-082/`
- 정리: 사본 서비스 종료(리스너 0, `pgrep -fl infra082` 0건), browse 정지, 사본·임시 디렉터리 삭제. 원본 `data/`·3500·5501 미접촉
- 삭제한 검사 없음. 이월 `[INFRA-086]`

## [INFRA-085] restart 를 거치지 않는 기동에서 `logs/` 가 0755 로 생긴다
- 완료 2026-09-24 00:19 | 티어 T1 | 커밋 01ebb12
- 변경: services/activity_logger.py, tests/services/test_activity_logger_refactor.py (+15 -2)
- 설계: bounded(2026-09-24 사용자 AskUserQuestion 「A. makedirs 0700 한 줄 (Recommended)」, 승인 확인 00:12). 파이썬에서 `logs/` 를 만드는 곳은 `ActivityLogger` 의 `makedirs` 하나다. 이미 있는 디렉터리는 좁히지 않고 `restart_all.sh:73` 에 맡긴다
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md`(위험 경로 아님 → T1 판정) · 과잉설계 리뷰(`infra085-ponytail`, 별도 레인) 「Lean already. Ship.」(회신 유실, subagents 기록에서 읽음)
- 테스트: `test_activity_logger_creates_a_missing_log_dir_as_0700` 추가, 구현 전 0755 로 AssertionError
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2728 passed 2 skipped(exit 0) · QA 시나리오 3/3 통과
- QA: `docs/dev-cycle/qa/INFRA-085.md`. `logs/` 가 없는 사본에서 gunicorn 과 `npm run dev` 를 직접 띄우고 `/api/system/log-event` POST(curl, /dashboard/kr 페이지 안의 fetch) → `logs` 700·`user_activity.log` 600, 콘솔 오류 0. 이미 있는 0755 `logs/` 는 그대로 755. 변경 전 코드 대조에서 755 로 생김(결함 재현). 증거 `docs/dev-cycle/evidence/INFRA-085/`
- 정리: 사본 프로세스 일곱 개 종료(gunicorn 셋은 TERM 뒤 KILL), 리스너 0, `pgrep -fl infra085` 0건, 사본 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 메모: `logs/` 는 첫 요청이 아니라 기동 때 생긴다. 블루프린트 등록에서 `kr_market_chatbot_http_routes.py:249` 가 `ActivityLogger` 를 import 한다
- 삭제한 검사 없음. 이월 없음

## [INFRA-086] `python scripts/init_data.py` 를 루트 밖에서 실행하면 종가 최신 파일이 `<cwd>/data` 에 생긴다
- 완료 2026-09-24 00:29 | 티어 T3 (`scripts/init_data.py` 위험 경로) | 커밋 4187562
- 변경: scripts/init_data.py (`__main__` 블록 첫 줄 `os.chdir(BASE_DIR)`, +2), docs/dev-cycle/qa/INFRA-086.md
- 설계: bounded(2026-09-24 사용자 AskUserQuestion 「A. init_data.py 한 줄 (Recommended)」, 승인 확인 00:23). 모듈 수준 chdir 는 import 하는 서비스·테스트의 cwd 를 바꾸므로 금지. `scripts/_run_full_update_test.py` 는 영향 미확인이라 제외
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 리뷰(`infra086-ponytail`, 별도 레인) 「Lean already. Ship.」 · `closing-bet-reviewer`(`infra086-reviewer`) APPROVE, 낮음 1건(주석의 알림 호출자는 스케줄러 `services/scheduler_jobs.py:30`) 반영 · 심층 리뷰(`infra086-deep-review`, oh-my-claudecode:critic) ACCEPT, 참고 3건 수정 불요, 선택 제안 AST 계약 테스트는 한 줄 변경이라 미추가. 세 레인 모두 회신 대신 subagents 기록에서 결과를 읽음
- 테스트: 추가 없음(한 줄 변경, 격리 사본 QA 로 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2728 passed 2 skipped(exit 0), 주석 정정 뒤 `py_compile`
- QA: `docs/dev-cycle/qa/INFRA-086.md` 필수 4/4 통과. 사본에서만 작업 다섯 개를 cwd 기록용 가짜로 두고 루트 밖 cwd 에서 인자 없음·`all` 실행 → 기록 cwd 모두 사본 루트, 루트 밖 cwd 비어 있음. import 만 하면 cwd 불변. 변경 전 `__main__` 대조에서 표식이 `<cwd>/data` 에 생김(결함 재현). CLI 만 바뀌어 브라우저 실측 없음
- 정리: 띄운 서비스 없음, `pgrep -fl infra086` 0건, 사본·임시 디렉터리 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 삭제한 검사 없음. 이월 없음. 근본 원인 `engine/generator.py:244` 의 cwd 상대 `"data"` 는 실행 파일 진입부 chdir 관례(`run.py:14`, `scripts/run_full_update.py:9`)에 따라 그대로 둠

## [INFRA-079] 유물 사용량 모듈 삭제 (코드 삭제분)
- 완료 2026-09-24 00:46 | 티어 T3 (두 모듈이 `tier-rules.md` 「저장소 스키마」의 `CREATE TABLE` 모듈, 등록 때 T1 에서 재판정) | 커밋 6efcb93
- 변경: services/usage_tracker.py·engine/services/usage_tracker.py 삭제(-413), tests/services/test_usage_tracker.py·tests/engine/test_engine_usage_tracker_refactor.py 삭제(-455), tests/app/test_kr_market_route_integration.py(+3 -5), README.md·docs/vibe/ARCHITECTURE.md·.claude/skills/dev-cycle/references/tier-rules.md(목록 두 줄, 개수 스물한 → 열아홉), docs/dev-cycle/qa/INFRA-079.md. 코드 -870, 문서 +61 -12
- 설계: bounded(2026-09-24 00:36 사용자 「진행해」). 사용자 「코드 삭제만 먼저 진행해」로 범위를 나눔. 운영 서버의 `data/usage.db` 행 확인·파일 정리는 운영자 단계로 TODO 에 남김. 과거 증거·아카이브는 보존
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 리뷰(`infra079-ponytail`, oh-my-claudecode:code-reviewer 레인) diff 안 지울 것 없음, `docs/vibe/ARCHITECTURE.md` 잔존 두 줄 반영 · `closing-bet-reviewer`(`infra079-reviewer`) APPROVE, low 3건(TODO 티어·범위 불일치 정정, 람다 인자 이름은 가드 테스트 키라 미반영, 운영 파일에 chmod 600·제거 단계 추가) · 심층 리뷰(`infra079-critic`, oh-my-claudecode:critic) ACCEPT-WITH-RESERVATIONS, 코드 결함 없음, QA 계획 유보 4건(S-2 기대 하향, 원본 `data/` 전후 대조, 원본 `data/` 미복제, 사본의 모듈 부재 확인)과 고아 pyc 정리 반영
- 테스트: 삭제한 검사는 두 모듈 전용 테스트 21건(SQLite 인덱스·busy timeout·읽기 전용 접속·이메일 정규화·테이블 복구·잠금 재시도·초기화 single-flight 등). 대상 코드가 없어져 무효. 폐기 라우트 테스트는 삭제된 모듈 가로채기 대신 `_prepare_quota_store` 로 실제 `user_quota.json` 이 생기지 않음을 단언(리뷰어 뮤테이션 대조로 쿼터 소비 퇴행을 잡음을 확인)
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2707 passed 2 skipped(exit 0, 직전 2728 에서 삭제한 21건 감소), `git grep` 잔존 참조는 람다 인자 이름 하나
- QA: `docs/dev-cycle/qa/INFRA-079.md` 필수 3/3 통과. 빈 `data/` 사본에서 gunicorn 기동, 기존 GET 둘 200, 폐기 라우트 410, `usage.db` 미생성. 변경 전 모듈 import 대조에서 사본 `data/usage.db` 생성(결함 재현). 원본 `data/` 전후 목록·`stat` 동일. 화면 흐름이 바뀌지 않아 브라우저 실측 없음
- 정리: 사본 gunicorn 종료(리스너 0, `pgrep -fl infra079` 0건), 사본 트리 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 이월 없음. 남은 운영자 단계: 운영 서버 `data/usage.db` 행 수 확인 → 파일 제거, 제거 전까지 `chmod 600`

## [INFRA-084] `closing-bet-reviewer` 에 일반 Python 보안 검토 항목을 더한다
- 완료 2026-09-24 00:50 | 티어 문서 (`tier-rules.md` §5, 실행 코드 변경 없음. 역할 파일은 §5 의 사이클 규약 문서 목록 밖이라 TODO 대조 생략) | 커밋 e127ef4
- 변경: .claude/agents/closing-bet-reviewer.md(「볼 것」에 명령 주입·경로 조작·안전하지 않은 역직렬화·잠금 없는 공유 상태 네 가지와 출처 ECC `agents/python-reviewer.md`(MIT), description 한 구절, +11 -1), .codex/agents/closing-bet-reviewer.toml(description 과 「볼 것」을 가리키는 한 문장, +2 -2)
- 설계: bounded(2026-09-24 사용자 「진행해 함께고쳐」, 승인 확인 00:49). 설계안은 `.toml` 을 고치지 않는 것이었으나 사용자가 함께 고치도록 정함. 검토 기준 본문은 복제하지 않고 `.md` 를 정본으로 유지. ECC 설치·파일 복사 없음
- 리뷰: §5 에 따라 리뷰 열 미적용
- 검증: 참조 `[INFRA-047]`(`CLAUDE.md:305`)·`--threads 8`(`restart_all.sh:116`) 실재 확인, `git diff --stat` 두 파일만(실행 코드 0), TOML 파싱 확인, 「셸·역직렬화 쓰는 곳 없음」 서술은 `git grep` 으로 `docs/`·`tests/` 밖 `vendor/pykrx/rebuild.py`(빌드 도구, `shell=True` 없음) 하나만 확인, `venv/bin/python -m pytest -q -p no:cacheprovider tests/scripts/test_skill_set.py` 6 passed(exit 0)
- QA: 문서 전용이라 동적 QA 제외
- 삭제한 검사 없음. 이월 없음

## [INFRA-087] 기동 스크립트와 문서에서 systemd 관련 부분을 제거한다
- 완료 2026-09-24 01:08 | 티어 T3 (위험 경로 아님, 실행 코드 스크립트 -82·테스트 -240 으로 300줄 초과) | 커밋 33cf316
- 변경: scripts/service_lifecycle.sh(cgroup·systemd 판정 함수 넷 `lifecycle_cgroup_path`·`lifecycle_cgroup_unit`·`lifecycle_pid_supervisor`·`lifecycle_assert_pid_is_unsupervised` 와 입양 경로의 호출, 계보 출력의 cgroup 줄, `LIFECYCLE_PROC_DIR`, 관련 주석·오류 문구, +3 -85), tests/scripts/test_lifecycle_adversarial.py(+5 -211), tests/scripts/test_service_lifecycle.py(+7 -41), deploy/systemd/README.md → deploy/README.md(유닛 절 제거, 운영 절차 보존, 로그아웃 생존 전제와 병행 금지 문단), README.md·.env.example·deploy/caddy/README.md(참조 경로와 systemd 언급), docs/dev-cycle/qa/INFRA-087.md. 코드 -322, 문서 +180 -215
- 설계: bounded(2026-09-24 00:55 사용자 「systemd 관련된거도 모두 제거해야돼 불필요해졌어」, 문서 처리 「deploy/README.md 로 이동」 선택). 두 스크립트의 OS 이름 분기는 이미 없었음을 먼저 확인. `/proc` cwd 조회와 `lsof`/`ss` 대체는 systemd 와 무관해 유지. `sudo systemctl reload caddy` 는 Caddy 서비스 명령이라 유지. 과거 아카이브·QA·증거는 보존
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · 과잉설계 리뷰(`infra087-ponytail`, general-purpose 레인) 「Lean already. Ship.」 · `closing-bet-reviewer`(`infra087-reviewer`) APPROVE, low 3건(로그아웃 생존 전제 문장 복구와 줄바꿈은 반영, 옛 유닛 이력 포인터·부팅 유닛 경고는 승인 범위라 미반영) · 심층 리뷰(`infra087-critic`, oh-my-claudecode:critic) ACCEPT-WITH-RESERVATIONS, 차단 결함 없음. 보류 1: 재점유 감지는 종료 대기 창 안에서만 성립하므로 이 기록과 커밋 메시지에서 「보호 유지」라고 쓰지 않고, `deploy/README.md` 에 「다른 실행 관리자와 병행하지 않습니다」 문단을 둠. 보류 2: `restart_all.sh` 의 거부 전파 진입점 테스트 공백은 입양 거부 테스트를 두 진입점으로 매개변수화해 메움. 사소 2건(caddy 문단 줄바꿈, 병행 금지 문단) 반영
- 테스트: 삭제한 검사는 systemd·cgroup 전용 12건(유닛 소속 점유자 거부, cgroup 을 읽지 못할 때·빈 파일·해석 불가 시 거부, cgroup 없는 호스트 허용, 호출자와 같은 유닛 허용, v1 하이브리드 계층 선택, Delegate 하위 cgroup, 유닛 이름 제어 문자 제거, 세션 관리자 재기동 권고 금지)과 두 진입점의 systemd 거부 전파 1건. 대상 코드가 없어져 무효. 세션 스코프 입양 테스트는 가짜 `/proc` 없는 `test_legacy_adoption_records_a_matching_pid` 로 바꿔 유지
- 검증: `bash -n scripts/service_lifecycle.sh` 통과, `venv/bin/python -m pytest -q -p no:cacheprovider` 2696 passed 2 skipped(exit 0, 직전 2707 에서 삭제 13·재작성 1·매개변수 1 반영), `git grep` 으로 아카이브·QA·증거 밖의 `systemd|cgroup|LIFECYCLE_PROC_DIR|deploy/systemd` 0건
- QA: `docs/dev-cycle/qa/INFRA-087.md` 필수 2/2 통과, 관측 1건. 격리 사본(포트 58151·58152)에서 PID 기록 없는 gunicorn 을 `stop_all.sh` 가 입양·종료(exit 0), 빈 포트 종료 exit 0. 관측: 약 2초 뒤 되살리는 루프는 대기 창 안에서 「다시 점유했습니다」로 잡혀 exit 1. 창보다 긴 되살림 지연은 잡지 못하는 한계가 남음. 화면 흐름이 없어 브라우저 실측 없음
- 정리: 되살림 루프와 사본 gunicorn 종료(`pgrep -fl infra087` 0건, 58151·58152·3500·5501 리스너 0), 사본 트리 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 이월 없음

## [VCP-040] VCP AI 판정 병합이 Gemini 추천만 보고 다른 프로바이더의 성공 결과를 버린다
- 완료 2026-09-24 08:42 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 3f00f58
- 변경: app/routes/kr_market_vcp_signal_helpers.py(`_extract_vcp_ai_recommendation` 을 gemini → gpt → perplexity 첫 유효 추천으로, `_apply_vcp_reanalysis_updates` 의 `keep_valid_verdicts` 와 Gemini 유효분만 `updated_recommendations`, 신규 `_drop_csv_verdict_copied_from_another_provider`), scripts/init_data.py(수집 병합을 같은 함수 호출로 교체, 전부 실패 시 확신도 결측), services/kr_market_vcp_reanalysis_service.py(자동 모드만 가드 연결), 테스트 세 파일, docs/dev-cycle/qa/VCP-040.md, 계획 문서. 코드 +298 -28, 문서 +329 -7
- 설계: 원인 확정 실측(08:09~08:11, 사용자 승인 「수집+재분석 모두」, 실제 LLM 호출 격리 사본) 뒤 bounded 설계 승인(08:13 「승인, 진행해」). critic 계획 검토 REVISE 뒤 범위 추가 승인(08:22, 자동 모드 한정 가드·화면 Gemini 라벨 교정). 승인 범위의 별도 `_select_vcp_ai_recommendation` 대신 규칙을 `_extract_vcp_ai_recommendation` 안에 둠(결과 동일, 기존 4-튜플 계약 유지). `VCP_AI_PROVIDERS` 순서 정렬·CSV 출처 열·기존 `data/` 복구는 하지 않음
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md`(계층·지연 import 판단) · superpowers:writing-plans(계획 문서) · 계획 검토(`vcp040-plan-critic`, oh-my-claudecode:critic) REVISE → 가드 자동 모드 한정, 화면 라벨 수정 추가, `tests/services` 단계 검증 포함, `[VCP-043]` 등록 · 과잉설계 리뷰(`vcp040-ponytail`, general-purpose 레인) 「Lean already. Ship.」 · `closing-bet-reviewer`(`vcp040-reviewer`) CHANGES_REQUIRED medium 1·low 4 → 강제 Second 뒤 라벨 누출은 구현 때 임의로 좁혔던 판정·사유 대조를 빼고 승인 원안 규칙으로 되돌려 해소, 서비스 연결 테스트 추가, 주석 수정, 대문자 비교·target_indexes 두 건은 사유와 함께 미반영 · 심층 리뷰(`vcp040-deep-critic`, oh-my-claudecode:critic) ACCEPT-WITH-RESERVATIONS, 유보(캐시 미등재 종목 재분석 라벨)는 `[VCP-044]` 로 이월
- 테스트: 추가 12건(헬퍼 9, 수집 2, 서비스 연결 1). 수집 2건은 옛 코드에서 실패, 서비스 1건은 연결을 지우면 실패함을 확인. 삭제한 검사 없음
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2708 passed 2 skipped(exit 0, 리뷰 반영 뒤)
- QA: `docs/dev-cycle/qa/VCP-040.md` 필수 5/5 통과, 관측 1건. 격리 사본(58171·58172, 원인 확정 실측의 실제 응답 기반 가짜 분석기, LLM 호출 없음)에서 수집 CSV·캐시·`/api/kr/signals`·VCP 화면(gstack browse, GPT 열 관망·Gemini 열 비어 있음 스크린샷)·자동 재분석(가드로 유효 판정 보존) 확인. 관측: 캐시에 없는 종목을 재분석이 GPT 로 채우면 Gemini 열에 뜸(`[VCP-044]`)
- 정리: browse 중지, 사본 gunicorn·Next 종료(58171·58172 리스너 0, `pgrep -fl vcp040qa` 0건), 사본 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 메모: 운영 반영은 gunicorn 워커 전부 재기동이 필요하다(장 중 금지). 이미 쌓인 운영 `signals_log.csv` 의 「분석 실패」 행은 되살리지 않으며, 자동 실패 재분석을 누르면 GPT 판정으로 채워진다. Gemini 가 계속 null 인 근본 원인은 `[VCP-041]` 이다
- 이월: `[VCP-042]`(재분석 저장 티커 0 누락), `[VCP-043]`(signal_tracker 선택 규칙), `[VCP-044]`(캐시 미등재 종목·날짜 없는 캐시 파일)

## [VCP-041] VCP 분석기 세션 차단 플래그 10분 만료 (코드 수정분)
- 완료 2026-09-24 09:12 | 티어 T3 (위험 경로 `engine/vcp_ai_analyzer.py`) | 커밋 7470475
- 변경: engine/vcp_ai_analyzer.py(신규 `_expire_session_blocks`, `analyze_stock` 의 시작과 `finally` 에서 호출, `__init__` 에 `_session_blocks_since`), engine/constants_market_system.py(`LLMThresholds.SESSION_BLOCK_TTL_SECONDS = 600`), tests/engine/test_vcp_ai_analyzer_refactor.py(4건), docs/dev-cycle/qa/VCP-041.md, 계획 문서. 코드 +163 -11
- 원인 확정: 실호출 1회(08:48 사용자 승인, 08:49 격리 사본, `gemini-3.8-flash`·Vertex `global`)에서 `STOP`·코드펜스 없는 JSON·파싱 성공. 대안 가설(파싱 실패·`response.text` None·리전 404)은 재현되지 않았다. 운영 `backend.log` 확인(원인 확정 1)은 운영자 단계로 남김
- 설계: 사용자 「지금 설계 진행」(운영 로그 없이 진입) 뒤 bounded 설계 승인(08:54 「승인, 진행」). TODO 기본안(`analyze_batch` 시작 초기화)은 재분석 진행률 경로가 `analyze_stock` 을 직접 불러 빠지므로 `analyze_stock` 한 곳의 10분 만료로 바꿈. 출력 토큰 `config`·타임아웃 정렬은 제외
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · superpowers:writing-plans · 계획 검토(`vcp041-plan-critic`, oh-my-claudecode:critic) REVISE → F1(배치 마지막 종목에서 켠 차단은 시각이 남지 않아 다음 날 배치 전체가 막힘)을 `try/finally` 로 해소, 재현 테스트 교체, `.clear()`·별칭 채택 · 과잉설계 리뷰(`vcp041-ponytail`, general-purpose 레인) 「Lean already. Ship.」 · `closing-bet-reviewer`(`vcp041-reviewer`) APPROVE low 2(L1 창 끝 무렵 새 차단도 함께 풀림은 승인 문구와 일치해 유지, L2 계획 문구 수정), 범위 밖 `zai_disabled_reason` → `[VCP-045]` · 심층 리뷰(`vcp041-deep-critic`, oh-my-claudecode:critic) ACCEPT, 사본에서 예외·취소·동시성 2 실측, O1 은 ponytail 주석에 반영
- 테스트: 추가 4건(1일차 마지막 종목 차단 → 2일차 재호출, TTL 안 유지, GPT·Perplexity 해제, 빈 상태 시각 초기화). 수정 전 코드에서 3건, 시작 가드만 둔 원안에서 1건 실패함을 확인. 삭제한 검사 없음
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2712 passed 2 skipped(exit 0). 이후 주석 한 줄만 바꾸고 대상 파일 36 passed
- QA: `docs/dev-cycle/qa/VCP-041.md` 필수 5/5 통과. 격리 사본의 한 프로세스(오래 사는 워커 흉내, 가짜 Gemini 클라이언트, 모듈 시계 오프셋)에서 1일차 전 모델 429 → 5분 뒤 호출 0회 유지 → 다음 날 수집에서 Gemini BUY 로 CSV·캐시 채움, 재분석 `background: true` 경로도 10분 뒤 재호출로 2건 완료, VCP 화면(gstack browse) GEMINI 열 「▲ 매수」 스크린샷. 기대값 정정 둘(체인 6개, 202)은 결함 아님
- 정리: browse 중지, 사본 소유 프로세스 8개 종료(58171·58172 리스너 0), 사본 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 메모: 운영 반영은 gunicorn 워커 전부 재기동이 필요하다(장 중 금지). 재기동만으로도 지금의 영구 차단은 풀린다. 만료 뒤 429 가 계속되면 창마다 Gemini 재호출이 최대 14회(체인 7 × 동시 슬롯 2) 생기고, GPT 402·Perplexity 401 도 10분마다 한 번 재시도한 뒤 폴백으로 넘어간다. 플래그 전체가 시계 하나를 써서 창 끝 무렵에 새로 켠 차단도 함께 풀린다(재호출 한 번 뒤 다시 막힘)
- 이월: `[VCP-045]`(Z.ai `zai_disabled_reason` 영구 플래그). 남은 운영자 단계: 운영 `backend.log` 문구 확인, 배포와 워커 전부 재기동, 배포 뒤 첫 17:00 실행에서 Gemini 결과 확인

## [INFRA-090] 일별 가격 수집의 휴장일 0원 행 차단 (코드 수정분)
- 완료 2026-09-24 10:38 | 티어 T3 (위험 경로 `scripts/init_data.py`) | 커밋 d4adaa8
- 변경: scripts/init_data.py(신규 `_all_zero_close_dates`, `create_daily_prices` 의 기존 파일 로드 직후 0원 날짜 제외·날짜별 루프 건너뜀·휴장일만 있는 구간의 yfinance 폴백 생략), tests/scripts/test_init_data_vcp_scheduler.py(7건), 계획 문서, docs/dev-cycle/qa/INFRA-090.md
- 원인: pykrx 1.2.9 는 평일 휴장일 전종목 시세를 빈 결과가 아니라 전 종목 0원 행으로 준다(`[INFRA-089]` 실측 09-24 2,870행). 수집은 주말과 `df.empty` 만 걸러 그 행을 저장했다. 로컬 원본에 설 연휴 3일이 각 2,882종목 종가 0 으로 있다
- 설계: bounded 대화 설계, 사용자 선택 「저장 시 자동 정리 (권장)」(10:17 확인). 판정은 「그 날짜의 모든 행 종가 0」(거래정지 종목은 종가가 남아 보존). 휴장일만 있는 구간의 폴백 생략은 계획에서 더한 경계 처리다(폴백은 직전 거래일을 일부 종목으로 덮어씀, `[INFRA-088]` 3차)
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md` · superpowers:writing-plans · 계획 검토(`infra090-plan-critic`, oh-my-claudecode:critic) REVISE → 혼합 구간 대조, 테스트 구간 고정, 정상일 종가 0 종목, 제거 위치, 중단 시 종전 경로, 로그 문구, 불필요 가드 제거 모두 반영 · 과잉설계 리뷰(`/ponytail-review`) 「Lean already. Ship.」 · `closing-bet-reviewer`(`infra090-reviewer`) APPROVE low → 중단 조건 테스트(조건 제거 변이로 실패 확인)·변수명 반영 · 심층 리뷰(`infra090-deep-review`, oh-my-claudecode:code-reviewer) 차단 결함 없음 → L2·L3 테스트 반영, M1 은 `[INFRA-089]` 로 이월, L1·L4 기록
- 테스트: 추가 7건(정상일 종가 0 종목 보존과 휴장일 건너뜀, 휴장일만 있는 구간 폴백 없음, 휴장일+빈 결과 혼합은 폴백, 휴장일 뒤 중단은 폴백, 저장된 0원 날짜 재조회·정리, 전체 0원 파일, 헬퍼 열 결측). 수정 전 코드에서 5건 실패 확인. 삭제한 검사 없음
- 검증: `venv/bin/python -m pytest -q -p no:cacheprovider` 2717 passed 2 skipped(exit 0). 이후 테스트만 바꾸고 `tests/scripts` 160 passed
- QA: `docs/dev-cycle/qa/INFRA-090.md` 필수 3/3 통과. S-1 실제 KRX 로그인 1회(사용자 승인)로 09-22·23 실데이터 저장, 09-24 휴장일 저장 생략·폴백 0, 휴장일만 남은 재실행은 파일 해시 불변. S-2 로컬 원본 복사본의 설 연휴 3일과 덧붙인 0원 날짜가 다음 저장 때 제거되고 다른 날짜 행 수 변화 0. S-3 VCP 화면(agent-browser) CURRENT ₩0·RETURN -100.0% → ₩276,740·+1.0%. 기대값 정정 하나(차트 API 는 원래 0원 봉을 거름)
- 정리: agent-browser 종료, 사본 gunicorn·Next 종료(58190·58191 리스너 0), 사본 삭제. 원본 `data/`·`logs/`·3500·5501 미접촉
- 메모: 이미 운영 파일에 0원 행이 있으면 다음 저장 때 빠진다. 휴장 기간의 실행은 파일을 다시 쓰지 않으므로 그 전까지는 VCP·종가베팅이 마지막 날짜의 0원을 현재가로 읽는다(S-3 의 수정 전 화면). 운영 반영은 gunicorn 재기동이 필요하다(장 중 금지)
- 이월: `[INFRA-089]` 에 심층 리뷰 M1(지수 조회 실패로 기대 날짜가 휴장일이 되면 관리자 수동 갱신의 stale 검증이 실패). 남은 운영자 단계: 운영 `daily_prices.csv` 의 0원 날짜 확인과 배포

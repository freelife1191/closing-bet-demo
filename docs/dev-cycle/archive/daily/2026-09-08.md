# 2026-09-08

## [INFRA-044] 인가 근거가 된 `ADMIN_EMAILS` 가 워커마다 갈릴 수 있다
- 완료 2026-09-08 01:00 | 티어 T1 | 커밋 b0ee535, 4d82f31
- 변경: services/common_env_service.py, tests/app/test_common_env_service.py (+128 -8)
- 리뷰: ponytail-review(Lean already. Ship.) · security-reviewer(F1 실제 결함 발견,
  범위에 넣어 수정 · F2·F3 이월) · code-reviewer(개행 필터 판정 통과, 명령 치환 결함을
  새로 찾아 이월)
- 검증: pytest 1799 통과 · 2 skip · QA 시나리오 15/15 통과
- 메모: 항목이 적고 있던 전제 둘이 이미 거짓이었다. 「키 허용 목록이 없다」는
  `[INFRA-025]`(`95b022c`)가 이미 해소했고, 그 결과 「워커마다 갈린다」도 그 경로로는
  닫혀 있었다. 이 항목의 근거가 된 `[INFRA-037]` 사이클 보안 리뷰가 **바로 직전
  커밋인** 그 필터를 보지 못한 것이다. 리뷰가 지적한 파일을 열어 볼 때는 그 리뷰가
  어느 시점의 코드를 봤는지 먼저 확인한다.
- 메모: 그럼에도 항목의 결론은 옳았다. **인가 키를 덮어쓸 경로가 실제로 남아 있었다.**
  필터가 `data` 의 키만 검사하고 값은 그대로 `f"{key}={value}\n"` 으로 내보냈으므로,
  값에 개행을 넣으면 한 항목이 두 줄로 나뉘어 목록 밖의 키를 쓸 수 있었다. 기존 줄
  갱신 경로(137행)와 새 키 추가 경로(148행) 둘 다 뚫렸다. 내가 「값을 바꿀 경로가
  없다」고 판단한 것은 `os.environ` 대입만 세고 파일 쓰기의 내용을 보지 않은 탓이다.
- 메모: **막는 쪽만 세면 경로를 놓친다.** `grep` 으로 `os.environ[...] = ...` 를 훑어
  세 자리를 찾고 그중 하나가 걸러진다는 것까지 확인했지만, 그 함수가 파일에 무엇을
  어떻게 쓰는지는 읽지 않았다. 인가 근거를 다룰 때는 「누가 이 값을 바꾸는가」뿐 아니라
  「이 값이 어디에 어떤 형식으로 저장되고 누가 그것을 어떻게 해석하는가」까지 따라간다.
- 메모: `frontend/.env` 는 루트 `.env` 의 심볼릭 링크다(`restart_all.sh:17`). 그래서
  `route.ts:28` 의 `process.env.ADMIN_EMAILS` 와 Flask 의 `is_admin_email` 이 같은 파일을
  근거로 삼고, 이 주입은 두 게이트를 한꺼번에 오염시킨다. 보안 리뷰가 알려 주었다.
- 메모: 코드 리뷰가 **개행 없이도 성립하는 더 큰 결함**을 찾았다. `.env` 를 bash 가
  `source` 하므로 허용된 키의 값에 `$(...)` 를 넣으면 다음 기동에서 임의 명령이
  실행된다. 재현해 확인했다. 이번 변경이 만든 것이 아니고 근본 수정이 쓰기 인용과
  기동 스크립트 양쪽에 걸쳐 `[INFRA-049]`(P0) 로 이월했다. **개행을 막았다고 값이
  안전해진 것이 아니다.** 이 파일에 쓰는 값은 여전히 셸이 해석한다.
- 메모: 허용 목록 방식이 차단 목록보다 나은 이유가 이 사이클에서 드러났다. 키 이름의
  대소문자·공백·유니코드 변형을 보안 리뷰가 직접 실행해 확인했는데 전부 「거부」로
  떨어졌다. `[INFRA-025]` 이전의 「키 이름에 KEY·SECRET 이 있는지 보는」 방식이었다면
  변형마다 구멍이 났을 자리다.
- 메모: 설정 화면의 저장 버튼은 운영 `.env` 를 실제로 다시 쓰므로 QA 에서 누를 수 없다.
  `tier-rules.md` §1-1 의 「화면이 없는 경로는 안전한 하네스를 검사 대상으로 삼는다」에
  따라 `update_env_file` 을 임시 디렉터리에 대해 직접 불렀다. HTTP 계층은 `.env` 를
  건드리지 않는 403 거부 경로만 실제로 쳤다.
- 메모: 마감 뒤에 도착한 보안 리뷰가 **개행 필터로도 `source` 제거로도 막히지 않는**
  경로를 하나 더 찾았다. python-dotenv 와 `@next/env` 가 값 안의 `${VAR}` 를 보간하므로,
  편집 가능한 키의 값에 `${ADMIN_API_TOKEN}` 을 넣으면 읽기 필터로 가려 둔 값이 그 키의
  실제 값이 된다. `engine/messenger_config.py:38` 이 그 값을 읽어 서버가 발송할 때마다
  공격자 서버의 액세스 로그에 토큰이 남고, 장 마감 분석 체인이 자동 발송하므로 공격자는
  기다리기만 하면 된다. 순서가 관건인데 **빈 값으로 한 번 저장해 줄을 지운 뒤 다시
  저장하면 파일 끝으로 옮겨진다.** S-9 이 정상 동작으로 확인한 「빈 값은 키를 지운다」가
  공격자에게 순서 결정 수단이 된다. 정상 동작 하나가 다른 결함의 발판이 되는 자리를
  시나리오만으로는 볼 수 없다.
- 메모: 그 유출을 **읽는 쪽에서는 막을 수 없다.** python-dotenv 에는
  `load_dotenv(interpolate=False)` 가 있지만 호출 자리가 다섯 곳이고 기본값이 참이라 한 줄만
  새로 추가되면 조용히 되돌아간다. `@next/env` 는 `processEnv` 가 expand 를 무조건 부르고
  끄는 인자가 없다. 그래서 쓰기 경로 한 자리에서 막았다. 처음에 `[INFRA-052]` 로 이월했다가
  리뷰의 권고와 사용자 판단에 따라 이번 범위로 되돌렸다. 고치는 자리가 방금 편집한 그 한
  줄이라 미루면 같은 세 줄을 두 번 열게 되기 때문이다. **`[INFRA-052]` 번호는 재사용하지
  않는다.** 다음 인프라 항목은 `[INFRA-053]` 부터다.
- 메모: 정규식이 `$` 전체를 막지 않는 근거를 실측으로 정했다. `@next/env` 로
  `my$ecret123` 을 읽으면 **`my` 로 잘린다**(정의되지 않은 변수가 빈 문자열이 된다).
  즉 `$` 뒤에 글자가 오는 값은 지금도 이미 Next 쪽에서 조용히 깨져 있었고, 저장을 거부하는
  편이 정직하다. 반대로 `$` 로 끝나거나 `$!` 처럼 기호가 이어지면 어느 파서도 보간하지
  않으므로 막을 이유가 없다. 리뷰가 준 정규식 `\$[{\w]` 는 `$(` 를 잡지 못해 `(` 를
  더했다. 받은 제안이라도 실제로 무엇을 잡는지 직접 확인한다.
- 메모: 같은 리뷰가 세 파서(`bash`·python-dotenv·`@next/env`)를 실측해 `[INFRA-049]` 의
  수정 방향을 정해 주었다. **따옴표 방식은 안 된다.** 세 파서가 따옴표를 다르게 읽고,
  python-dotenv 는 `'\''` 이스케이프가 든 줄을 통째로 버린다. 그리고 bash 는 **공백
  하나만으로도** 뒤 토큰을 실행하는데 Google 앱 비밀번호에 공백이 들어가므로 문자 필터로는
  풀 수 없다. `source` 를 걷어내는 쪽이며, 두 스크립트가 `.env` 에서 실제로 쓰는 값은
  포트 둘과 호스트 하나뿐이라 비용도 작다.
- 메모: 리뷰 회신이 두 건 모두 중간에서 잘렸다. `SendMessage` 로 이어 받았는데
  `infra044-security` 는 세 번 잘려 네 번에 나눠 받았고 마지막 조각은 아직 오지 않았다. 긴 표나 목록이 들어가는 리뷰는
  잘릴 것을 예상하고 처음부터 절을 나눠 요청하는 편이 낫다.

## [INFRA-049] `.env` 의 값이 다음 기동에서 셸 명령으로 실행된다
- 완료 2026-09-08 01:40 | 티어 T3 | 커밋 8491e18, 3e6a3f6
- 변경: scripts/env_value.sh(신규 66), restart_all.sh, stop_all.sh,
  tests/scripts/test_env_value_sh.py(신규 238) — 코드 +327 -14
- 리뷰: critic(REVISE, 지적 7 전부 반영) · ponytail-review(2줄 축약) ·
  code-reviewer(지적 2 재현 후 반영) · /review(0 critical) ·
  security-reviewer(지적 3 재현 후 반영, 그중 하나는 커밋 차단 사유였다)
- 검증: pytest 1809 통과 · 2 skip · bash -n 셋 · 임시 포트 5599 기동 200 ·
  QA 시나리오 18/18 통과
- 메모: 고친 것은 `set -a; source .env` 다. bash 는 대입의 오른쪽에서 명령 치환을 항상
  평가하므로 설정 화면으로 저장할 수 있는 값이 다음 기동에서 임의 명령이 되었다.
  `[INFRA-044]` 가 넣은 값 필터는 **설정 화면을 지나는 값만** 보므로 이미 파일에 있는
  값과 손으로 적은 값은 그대로 셸을 지났다.
- 메모: **export 는 이미 중복이었다.** `os.environ` 을 추적 매핑으로 감싸고 `import app` 을
  돌리니 `load_dotenv()` 이전에 읽히는 키는 로케일 넷뿐이었고, `env -i` 로 띄운 node 에서
  `@next/env` 가 `.env` 키 58개를 채웠다. 두 런타임이 각자 파일을 읽고 있었으므로 셸이
  미리 넣어 주던 값은 같은 파일의 같은 값이었다. 걷어내도 잃는 것이 없다는 근거가 이것이다.
- 메모: **계획이 적은 사실 하나가 거짓이었다.** 「sed 방식이 세 파서와 같은 값을 준다」는
  지금 세 키의 값 모양에서만 참이다. 따옴표·인라인 주석·후행 공백을 벗기지 않고
  `export KEY=값` 도 못 읽는다. critic 이 실측 표로 보였고 나도 재현했다. **인라인 주석은
  이 저장소의 집필 관례다** — `.env` 159줄 중 21줄, `.env.example` 24줄이 그 모양이다.
  「지금 값이 같다」와 「같은 값을 준다」는 다른 주장이고, 전자를 확인하고 후자를 적었다.
- 메모: 그 한계가 물리는 자리를 재현했다. 포트가 `5501 # 백엔드 포트` 가 되면
  `stop_all.sh:38` 의 `lsof -ti :$port` 가 단어 분리로 깨져 PID 를 하나도 못 찾고, 58행의
  최종 확인까지 함께 실패해 **포트가 살아 있는데 「✅ Port freed」를 출력한다.** 옛 방식에는
  없던 실패 모드다. 내가 「형식 검사를 넣지 않는다」고 판단한 근거가 「gunicorn 기동 실패로
  드러난다」였는데, `stop_all.sh` 에는 gunicorn 이 없다. **한 스크립트에서 참인 근거를 두
  스크립트에 적용했다.**
- 메모: **회귀 검사가 자기가 걷어낸 코드를 잡지 못했다.** 정규식에 `^\s*` 를 두었는데 옛
  `restart_all.sh:11` 은 한 줄이라 `source` 와 `set -a` 가 행 중간에 있었다. 검사는 통과하는데
  막는 것은 없는 상태였다. 보안 리뷰가 실측으로 찾았다. **검사를 쓸 때 「고치기 전 코드」를
  입력으로 넣어 실패를 확인하는 절차가 있었다면 이 자리에서 걸렸다.** 나는 새 코드에 대해
  통과하는 것만 확인했다.
- 메모: 그 수정에서 **리뷰가 준 형태가 그대로는 동작하지 않았다.** 「행 앞 앵커 제거 + 주석
  제거」로 하면 내가 쓴 오류 메시지 `"…아니다. .env 의 해당 줄을…"` 의 `다. .env` 가 걸린다.
  리뷰는 문자열 리터럴에 `#` 이 없는 것만 확인했고 마침표는 보지 못했다. 앵커를 셸의 명령
  시작 자리(`^`·`;`·`&&`·`||`·`{`·`(`·`|`)로 옮겨 잡을 것 여섯과 오탐 여섯을 함께 쟀다.
  `[INFRA-044]` 에서도 리뷰가 준 정규식이 `$(` 를 놓쳤다. **두 사이클 연속으로 같은 일이
  일어났으므로 받은 정규식은 반드시 양쪽 표본으로 잰다.**
- 메모: `${!1}` 간접 확장이 **실제로 명령을 실행한다.** 이름 안의 배열 첨자를 산술 평가하기
  때문이며 `f 'a[$(touch X)]'` 로 bash 3.2 와 5.x 양쪽에서 확인했다. GNU sed 의 `s///e` 도
  같은 성질이다. 호출자가 리터럴 셋만 넘겨 지금은 닿지 않지만, 이 항목이 고치는 결함이
  바로 「데이터가 코드로 평가되는 자리」라서 같은 자리를 새로 남기지 않기로 했다. 키 이름
  검사 한 자리가 둘을 함께 막는다.
- 메모: 코드 리뷰가 **확신도 낮음으로 준 관찰이 실재했다.** pytest 의 assertion rewriting 이
  실패 시 비교된 두 값을 출력에 남긴다(리터럴 3회 등장을 재현). 「`.env` 값을 출력하지
  않는다」는 규약이 검사 실패 경로에도 걸린다는 것을 그전에는 생각하지 못했다. 보안 리뷰가
  거기에 하나를 더했다 — `dotenv_values` 반환값을 지역 변수로 붙들면 `pytest -l` 이 `.env`
  전체를 덤프한다. 임시 객체로 바꾸었다.
- 메모: `${!1}` 이 함수의 `local value` 와 이름이 겹치면 상속값이 조용히 무시된다.
  `env_port value 7` 로 재현했다. 지역 변수 이름을 `_env_port_value` 로 두었다. 키 이름
  검사를 넣어도 `value` 는 그 검사를 통과하므로 두 방어는 서로 다른 것을 막는다.
- 메모: 사용자가 서비스를 방금 기동한 상태여서 `./restart_all.sh` 를 돌릴 수 없었다.
  임시 포트 5599 에 `env -i` 환경으로 gunicorn 을 따로 띄워 `/health` 200 을 확인했고,
  `SCHEDULER_ENABLED=false` 와 **`NOTIFICATION_ENABLED=false`** 를 함께 넘겼다. 뒤의 것은
  critic 이 짚어 준 것으로, 운영 `.env` 의 값이 `true` 이고 디스코드·텔레그램·SMTP 자격
  증명이 실제 값이다. Next 는 `.next` 를 공유해 서로를 깨뜨리므로 전체 기동 대신
  `@next/env` 로더를 같은 조건으로 불렀다.
- 메모: `services/scheduler.lock` 의 수정 시각이 임시 기동 시각으로 갱신되었으나 지우지
  않았다. `lsof` 로 보니 그 파일을 잡고 있는 것은 운영 워커였고, `.gitignore` 대상이며
  `restart_all.sh:83` 이 매 기동에서 지운다. **정리해야 할 흔적과 남의 것을 구분한다.**
- 메모: 범위 밖 발견 둘을 이월했다. `.env` 모드가 0644 라 같은 호스트의 모든 로컬 계정이
  읽는다(`[INFRA-053]`, P1). `restart_all.sh:36` 의 `pkill` 이 패턴을 셋 넘겨 사용법 오류로
  끝나고 아무것도 죽이지 않는다(`[INFRA-054]`, P2). 뒤의 것은 처음에 「뒤의 둘이 무시된다」로
  적었는데 macOS `pgrep` 실측과 보안 리뷰의 종료 코드 확인을 합쳐 「줄 전체가 무동작」으로
  고쳤다. 이월 문구가 다음 사람의 우선순위를 정한다.
- 메모: 마감 뒤에 도착한 보안 리뷰의 남은 절이 확신도 중간으로 「bash 파싱과 python-dotenv
  파싱이 갈리는 키가 `.env` 에 있으면 export 제거로 그 키의 실효값이 조용히 바뀐다」를
  지적했다. 세 키만 검사로 묶여 있고 나머지 55개는 재지 않은 상태였다. **57개 키 전부를
  두 파서로 읽어 값의 길이를 비교했더니 갈리는 키가 하나도 없었다.** 「영향 없음」으로
  확정했고 그 확인 자체를 QA 시나리오(S-5b)로 남겼다. 확신도 중간 관찰을 「아마 괜찮다」로
  닫지 않고 실제로 재는 데 명령 하나가 들었다.
- 메모: 같은 리뷰가 `FLASK_HOST` 에 인라인 주석이 붙으면 `--bind` 문자열이 깨져 gunicorn 이
  기동하지 못한다고 짚었다. 포트 둘은 `env_port` 가 큰 소리로 막지만 이 값은 검사를 받지
  않아 로그에만 흔적이 남는다. **닫히는 쪽으로 실패하므로 보안 문제는 아니고 가용성
  문제다.** `.env.example` 의 그 줄에 주의를 세 줄 적었다. 인라인 주석이 이 파일의 집필
  관례라서, 규칙을 코드로 강제하기 어려운 자리에는 파일 자신이 경고를 들고 있어야 한다.
- 메모: 보안 리뷰가 `source scripts/env_value.sh` 자체를 평가해 공격면 순감이라고 판정했다.
  근거는 쓰기 주체가 「관리자 화면을 지나는 사람」에서 「파일시스템 쓰기 권한을 가진
  사람」으로 좁아졌다는 것이다. `.env` 는 `POST /api/system/env` 가 쓰지만 이 파일은 어느
  애플리케이션 경로도 쓰지 않는다. 새 파일을 들이는 변경에서 「무엇이 이 파일을 쓸 수
  있는가」를 먼저 묻는 방식이다.

## [INFRA-053] `.env` 가 같은 호스트의 모든 로컬 계정에 읽힌다
- 완료 2026-09-08 08:05 | 티어 T3 | 커밋 4848f42
- 변경: services/common_env_service.py, tests/app/test_common_env_service.py, restart_all.sh 외 5개 (코드 +115, 문서 +632 -20)
- 리뷰: critic(REVISE, 지적 7 전부 반영) · ponytail-review(Lean already) · code-reviewer(지적 1 반영, 확신도 중하가 실재) · review(구조화 패스 0건 + Codex 적대 P1 3건·P2 1건 + Claude 적대 INVESTIGATE 2·FIXABLE 1) · security-reviewer(H1 1건·M1 1건, 둘 다 반영)
- 검증: pytest 1812 통과·2 skip · vitest 335 통과(12회 중 10회, 간헐 실패는 `[FE-042]` 로 분리) · bash -n 4개 0 · QA 시나리오 10/10 통과
- 메모: `os.chmod` 를 쓰기 뒤에 두면 기존 0644 파일에서도 새 시크릿이 0644 아래 쓰이는 창이 남는다. `open(w)` 가 이미 잘라낸 뒤라 `os.fchmod` 를 블록 맨 앞에 두면 그 창이 없다(실측).
- 메모: 모드 변경은 **이미 열린 파일 기술자를 회수하지 않는다.** 0644 이던 동안 열어 둔 기술자는 같은 inode 에 이어 쓰이는 값을 계속 읽는다. 보안 리뷰와 Codex 가 독립적으로 같은 지적을 냈다. 근본 해법은 새 inode + `os.replace` 이며 `[INFRA-050]` 이 맡는다.
- 메모: `atomic_write_text` 를 쓰는 다른 `.env` 쓰기 경로는 `NamedTemporaryFile`(0600) + `os.replace` 라 **이미 0600 을 남긴다.** 「일관성」을 이유로 함께 고치지 않았고 그 전제를 QA S-7 로 확인했다.
- 메모: **`.env` 를 0600 으로 좁혀도 같은 값이 `frontend/.next` 에서 읽혔다.** Turbopack 의 FileSystem Cache 가 프로세스 환경을 직렬화하며, `restart_all.sh` 의 심볼릭 링크 때문에 백엔드 전용 시크릿까지 그 환경에 오른다. 실측으로 키 16개가 파일 3,222개에 평문·0644 로 있었다. 캐시는 실행마다 새로 만들어져 파일 모드로 막을 수 없어 `chmod 700 frontend/.next` 로 순회를 끊었다.
- 메모: 검사가 **최종 상태만 재면 순서 회귀를 놓친다.** `os.fchmod` 를 블록 밖 `os.chmod` 로 옮겨도 최종 모드와 내용이 같아 통과한다. 좁히는 시점의 파일 크기를 재는 검사를 더했고, 돌연변이 둘로 그 검사만 실패하는 것을 확인했다.
- 메모: 워커가 `--reload` 없이 도는 탓에 **화면 저장 경로는 다음 재기동 전까지 옛 코드를 쓴다.** 5501 워커는 00:39 기동, 변경은 07:37 이었다. QA 를 하네스로 설계한 이유이며 저장소 learning `gunicorn-needs-hup-before-qa` 와 같은 사실이다.
- 메모: 「파일 소유자와 워커가 같으니 `fchmod` 는 실패하지 않는다」는 단정을 적었다가 적대적 리뷰가 반례를 냈다. macOS `chflags uchg` 는 소유자가 스스로 걸 수 있고, POSIX 권한 비트를 지원하지 않는 마운트에서는 언제나 실패한다. 계정 불일치는 실패 원인 가운데 하나일 뿐이다.

## [INFRA-050] 겹치는 `.env` 저장이 서로를 깨뜨린다
- 완료 2026-09-08 09:30 | 티어 T3 | 커밋 ed502d9
- 변경: services/common_env_service.py, services/kr_market_data_cache_core.py, tests/app/test_common_env_service.py, tests/services/test_kr_market_data_cache_service_refactor.py (코드 +450 -114, 문서 +1107 -32)
- 리뷰: critic(REVISE, 지적 6 전부 반영) · ponytail-review(-16줄 가능, 9줄 적용) · code-reviewer(결함 없음) · security-reviewer(M1·L3 반영, L1·L2 는 주석에 한계로 명시) · 적대적 리뷰(H1·H2 와 조회 경로 불일치 반영)
- 검증: pytest 1818 통과·2 skip · QA 시나리오 12/12 통과 · 돌연변이 5종으로 검사 실효 확인
- 메모: **항목이 적은 경합 주체가 성립하지 않았다.** TODO 는 `update_env_file` 과 `persist_market_gate_interval_to_env` 가 겹친다고 했으나, 뒤엣것이 받는 경로가 `project_env_path(kr_market.__file__)` 로 계산돼 존재하지 않는 `<루트>/app/.env` 를 가리킨다. 그 함수는 첫 줄의 존재 검사에서 조용히 반환한다. 근거가 된 리뷰가 두 함수에 같은 경로를 직접 넘긴 모형을 세우고 배선을 건너뛴 것이다. **함수 시그니처가 같아 보여도 호출자가 무엇을 넘기는지 따라가야 한다.** 경로 버그는 `[INFRA-056]` 으로 이월했다. 고치면 인증 없는 엔드포인트가 운영 `.env` 를 쓰게 되므로 `[INFRA-042]` 뒤에 둔다.
- 메모: 실재하는 경합은 `update_env_file` 끼리였다. 설정 화면이 같은 API 를 내는 자리가 둘이고(`SettingsModal.tsx:167` 의 저장, `:279` 의 알림 테스트), gunicorn 이 `--workers 2 --threads 8` 로 뜬다. 주체를 정정하니 심각도도 달라졌다. **`open(w)` 는 여는 시점에만 잘라내고 닫는 시점에는 잘라내지 않는다.** 두 요청이 각자 offset 0 에서 쓰면 짧은 쪽 뒤에 긴 쪽의 꼬리가 키 없는 줄로 남고, 이 파일의 파서가 `=` 없는 줄을 보존해 영구히 남는다. 실측으로 재현했다. 즉 원자적 교체가 P0 급을 닫고 잠금이 P2 급(갱신 소실)을 닫는다.
- 메모: 잠금은 `.env` 가 아니라 `.env.lock` 에 건다. `os.replace` 가 inode 를 갈아 끼우므로 `.env` 에 걸면 교체 순간 잠금이 경로에서 분리된 옛 inode 에 남는다. `fcntl.flock` 은 기술자 단위라 워커 사이에서도, 같은 프로세스의 서로 다른 기술자 사이에서도 배타적이다. 뒤엣것 덕분에 스레드로 재현하는 검사를 쓸 수 있다. `fcntl.lockf` 는 프로세스 단위라 그 성질이 없다.
- 메모: **`gunicorn --timeout 120` 은 이 대기를 끊지 못한다.** 계획에 그렇게 적었다가 Codex 가 짚어 소스로 확인했다. gunicorn 25.0.3 `gthread` 워커의 `run()` 은 요청 스레드 풀과 별개로 `while self.alive` 루프에서 `self.notify()` 를 부른다. 요청이 잠금에서 막혀 있어도 중재자는 그 워커를 살아 있는 것으로 본다. 상한을 두지 않기로 한 결정의 대가를 정확히 적어 두어야 했다.
- 메모: **제 주석이 거짓을 주장했다.** 「`os.replace` 가 링크를 따라가지 않으므로 링크 경로가 닫힌다」고 적었는데 그것은 쓰기에만 참이다. 읽기는 여전히 따라가 대상의 `ADMIN_API_TOKEN` 과 `ADMIN_EMAILS` 를 읽고, 줄 루프가 `data` 에 없는 키를 무조건 보존하므로 그 값들이 그대로 새 `.env` 로 옮겨 붙었다. `EDITABLE_ENV_KEYS` 필터가 막으려던 바로 그 키들이 파일 경로로 주입된 것이다.
- 메모: **그 결함을 제가 쓴 검사가 통과시켰다.** 대상 파일이 안 바뀐 것만 재고 대상 내용이 어디로 갔는지는 보지 않았다. 지금은 셋을 함께 본다. 예외가 나는가, 대상이 보존되는가, 그리고 `.env` 가 여전히 링크인가. 마지막 것이 「아무것도 하지 않았다」를 잰다. **「막았다」를 재는 검사는 막힌 쪽만이 아니라 우회로가 열렸는지도 함께 봐야 한다.**
- 메모: 그 수정을 저장 경로에만 하니 **관리자가 보는 파일과 저장이 닿는 파일이 갈렸다.** 조회는 링크를 따라가 대상 값을 보여 주는데 `PLAIN_ENV_KEYS` 인 `SMTP_HOST`·`SMTP_PORT`·`AI_PROVIDER` 는 마스킹 없이 나가고, 저장을 누르면 `ELOOP` 로 500 이 난다. 읽기 자리가 둘이면 헬퍼 하나로 묶는다. `_read_env_lines` 로 통일했다.
- 메모: **제 변경이 없던 유출 경로를 새로 만들었다.** `tmp_path = tmp_file.name` 대입을 `with` 블록 마지막에 두었더니 `fsync` 가 실패할 때 그 변수가 빈 문자열이라 `finally` 의 `if tmp_path` 가 거짓이 되어 임시 파일을 지우지 못했다. 재현하면 `SMTP_PASSWORD=secret` 을 담은 0600 파일이 그대로 남았다. 대입을 첫 줄로 옮겼다. 성공 경로만 재는 검사로는 이 회귀를 잡을 수 없어 `os.fsync` 를 ENOSPC 로 만드는 검사를 따로 두었다.
- 메모: `NamedTemporaryFile` 의 기본 이름 `tmpXXXXXXXX` 는 어느 `.gitignore` 규칙에도 걸리지 않는다. 실측으로 `git check-ignore -v tmpAB12cd34` 가 안 걸리고 `.env.tmpAB12cd34` 는 `.gitignore:23` 의 `.env.*` 가 덮는 것을 확인했다. 대상 파일명을 `prefix` 로 붙였다. 같은 함수가 쓰는 `data/` 아래 캐시 파일은 `.gitignore` 가 확장자로 걸어 두어 붙이기 전이나 후나 걸리지 않는데, 그 파일들은 시크릿이 아니라 무해하다. 보안 리뷰가 「`data/` 는 이미 덮인다」고 추정한 것을 실측이 뒤집었지만 결론은 같았다.
- 메모: **돌연변이 측정에 경로 함정이 있다.** 사본을 `PYTHONPATH` 에만 얹고 저장소 루트에서 `pytest` 를 돌리면 `sys.path[0]` 이 빈 문자열(= 저장소 루트)이라 원본 모듈이 먼저 잡히고, 돌연변이를 심어도 전부 통과한다. 1회차 수치가 그렇게 틀렸다. cwd 를 사본으로 옮기고 `PYTHONPATH=<저장소>` 를 뒤에 두어 `__file__` 로 로드 위치를 확인한 뒤 다시 쟀다. **돌연변이가 하나도 안 잡히면 검사가 강한 것이 아니라 측정이 틀린 것이다.**
- 메모: 잠금이 있는 코드에서 경합 검사를 짜는 법. 적대적 리뷰가 `threading.Barrier(2)` 를 제안했으나 두 번째 스레드가 잠금에서 막혀 barrier 에 닿지 못하므로 통과 상태에서 교착한다. `Event` 로 첫 스레드의 쓰기 진입만 확인하고 두 번째를 띄웠다. 남는 가정(두 번째가 0.3 초 지연 안에 읽기에 닿는다)은 없앨 수 없다. 잠금이 바로 그 순서를 막는 것이 목적이기 때문이다. `ponytail:` 마커로 한계를 적고 실효는 돌연변이로 증명했다.
- 메모: `[INFRA-053]` 이 넣은 `os.fchmod` 가 이번 변경으로 사라졌다. `NamedTemporaryFile` 이 0600 으로 만들고 `os.replace` 가 그 모드째 옮기므로 성질은 유지된다. 함께 지운 검사는 좁히는 시점의 파일 크기를 재던 것인데, 임시 파일이 처음부터 0600 이고 교체 뒤에야 경로에 나타나므로 **그 검사가 막던 회귀 자체가 성립하지 않는다.** 모드 보호는 동작을 재는 `test_update_env_file_narrows_file_mode` 가 계속 맡는다.

## [INFRA-042] 관리자 전용 라우트 열둘이 권한 검사 없이 열려 있다
- 완료 2026-09-08 11:10 | 티어 T3 | 커밋 388755a
- 변경: app/routes/route_guards.py(신규), app/routes/kr_market_jongga_execution_routes.py, app/routes/kr_market_data_signals_routes.py, app/routes/kr_market_system_http_routes.py, app/routes/common_update_routes.py, tests/app/test_route_guards.py(신규), tests/app/test_admin_gated_routes.py(신규), tests/services/test_admin_helpers.py(신규), tests/fixtures/admin_email_cases.json(신규), 기존 검사 셋, frontend/src/lib/adminEmails.test.ts (코드 +722 -3, 문서 +1625 -8)
- 리뷰: critic(REVISE, 지적 10 전부 반영 후 ACCEPT) · ponytail-review(-18줄 가능, 넷 전부 적용해 54→49줄) · code-reviewer(결함 없음, 확신도 낮은 관찰 셋 가운데 둘 반영) · security-reviewer(4-2 반영해 게이트 둘 추가) · 적대적 리뷰(F1 이월, F2·F7 반영) · Codex 교차 검토(OPTIONS 갈래 수정)
- 검증: pytest 1850 통과·2 skip(사이클 전 1818, +32) · type-check 종료 코드 0 · vitest 54파일 349건 통과 · QA 시나리오 11/11 통과 · 돌연변이 7종으로 검사 실효 확인
- 메모: **승인 범위는 여섯 자리였고 최종은 열둘이다.** 두 번 늘었고 두 번 모두 원인이 같다. 제가 라우트를 「화면이 그 버튼을 누구에게 보이는가」로 갈랐는데, 그 기준은 **화면에 없더라도 서버에서 같은 함수와 같은 상태 파일을 공유하는 형제 라우트**를 세지 못한다. 적대적 리뷰가 중단 계열 셋(`stop-update`·`finish-update`·`update-item-status`)이 `start-update` 와 같은 상태 파일을 쓴다는 것을, 보안 리뷰와 뒤늦은 전수 조사가 `signals/reanalyze-failed-ai` 계열 셋을 찾았다. **들어가는 문만 잠그고 나오는 문을 열어 두면 잠근 의미가 없다.**
- 메모: **적대적 리뷰의 F1 은 앞선 세 리뷰가 모두 놓쳤다.** `/api/kr/refresh` 에 게이트가 없는데 `_resolve_common_update_handlers()` 로 방금 잠근 `/init-data`·`/start-update` 와 같은 핸들러에 닿는다. 익명이 LLM 비용을 유발하고 `isRunning` 을 세워 관리자 전용 자리를 거꾸로 잠글 수 있다. `[INFRA-059]` 로 P0 에 세웠고, 그 항목에는 「화면이 아니라 핸들러와 상태 파일을 기준으로 호출 그래프를 훑으라」고 적었다.
- 메모: **TODO 의 목록을 그대로 믿은 것이 누락의 뿌리였다.** 마지막에야 POST 라우트 30개를 전수 조사해 게이트 없는 19개를 가려냈다. 그 조사가 오판 하나도 막았다. `/user/quota/recharge` 는 관리자 전용으로 보였지만 `Sidebar.tsx:286` 의 `{!isAdmin && ...}` 안이라 **비관리자에게만 보이는** 버튼이다. 게이트를 두르면 기능이 죽는다. 모의투자도 「넷」이 아니라 `deposit` 을 포함한 다섯이었다.
- 메모: **목록을 지키는 검사가 없으면 목록은 조용히 어긋난다.** 적대적 리뷰 F7 이 짚었다. 라우트를 이름으로 하나씩 여는 검사만 두면 열셋째 라우트가 무검사로 들어온다. `url_map` 을 훑어 `is_admin_email` 로 판정하는 POST 라우트를 모아 선언 목록과 양방향 대조하는 검사를 두었다. 게이트 하나를 떼는 돌연변이가 `목록에 있는데 게이트가 없다: ['/api/kr/jongga-v2/message']` 로 정확히 실패한다.
- 메모: **그 검사의 첫 구현이 틀렸다.** `__wrapped__` 가 없으면 건너뛰게 짜 놓고 docstring 에는 「`__wrapped__` 에 기대지 않는다」고 적었다. 그 조건을 빼자 종전부터 인라인 `if` 로 닫혀 있던 `/api/notification/send` 가 새로 걸렸다. 목록이 뜻하는 것은 「데코레이터가 붙은 자리」가 아니라 「관리자 전용 라우트」여야 하므로 그 경로를 목록에 넣었다. 그 자리의 게이트를 무동작으로 바꾸는 돌연변이가 잡히는 것도 확인했다. **docstring 이 구현과 어긋나면 둘 중 하나가 아니라 대개 둘 다 틀렸다.**
- 메모: **OPTIONS 를 뷰로 넘기면 안 된다.** 1회차에는 통과시키고 「그 뷰가 첫 줄에서 빠지니 안전하다」를 근거로 삼았다. 보안·적대적·Codex 세 리뷰가 각각 독립적으로 같은 지적을 냈다. Flask 는 `methods` 에 OPTIONS 를 적은 라우트에 한해 자동 응답을 끄고 뷰를 부르는데 **그 요청에 본문을 실을 수 있어** `curl -X OPTIONS -d ...` 한 줄이 무인증으로 뷰에 닿는다(실측 재현). 불변식을 지키는 것이 주석뿐이면 다음에 조기 반환을 빠뜨린 라우트가 생기는 순간 그 자리가 열린다. 발송 핸들러가 같은 파일에 있다. `make_default_options_response()` 로 갈래 자체를 없앴다.
- 메모: 그렇다고 OPTIONS 를 막아도 안 된다. `app/__init__.py:171` 의 `before_request` 가 OPTIONS 에서 일찍 반환해 `g.user_email` 을 세우지 않으므로, 막으면 preflight 가 403 을 받아 브라우저가 본 요청을 아예 보내지 않는다. **관리자에게도 화면이 멈춘다.** 통과도 차단도 아닌 세 번째 답이 필요했다.
- 메모: **`CLAUDE.md` 를 두 번 틀렸고, 두 번째는 첫 번째를 고치면서 새로 만든 오류다.** 처음에는 「Next 라우트 핸들러가 기동 시점의 값을 들고 있다」고 적었는데 `route.ts:31` 은 함수 안이라 매번 읽는다. 그것을 고치며 쓴 「설정 화면으로 바꾸면 워커 하나만 바뀐다」도 틀렸다. `ADMIN_EMAILS` 와 `ADMIN_API_TOKEN` 은 **둘 다 `EDITABLE_ENV_KEYS` 밖**이라 그 화면으로 아예 바꿀 수 없다(관리자가 화면에서 자기 자신을 잠그는 것을 막는 의도적 설계). 막다른 길로 안내하는 서술이었다.
- 메모: 두 키의 실제 차이는 **폐기 수단이 하나인가 둘인가**다. 둘 다 `os.environ` 을 바꾸는 수단은 재기동뿐이지만, `ADMIN_EMAILS` 에는 NextAuth 로그인을 끊는 두 번째 길이 있고 `proxy.ts` 가 매 요청 `getToken` 하므로 **재기동 없이 즉시 듣는다.** 토큰 뒤에는 계정이 없어 그 길이 없다. `IDENTITY_TTL_SECONDS`(120초)를 폐기 수단으로 오해하지 말라는 경고도 적었다. 그것은 서명의 신선도만 정하고 인가와 무관하며, 만료되면 `proxy.ts` 가 다시 서명하는데 그 경로는 `ADMIN_EMAILS` 를 보지 않는다.
- 메모: **`[INFRA-042]` 이후로 회전 대상이 넓어졌다.** 관리자 전용으로 닫은 라우트 열둘의 판정이 `INTERNAL_IDENTITY_SECRET` 으로 서명된 신원에 걸려 있고 그 안에 구독자에게 실제 메시지를 보내는 경로가 들어 있다. 보안 리뷰가 짚어 `CLAUDE.md` 에 적었다. 그 서명이 경로·메서드에 묶이지 않아 재생 가능한 성질은 `[INFRA-062]` 로 세웠다.
- 메모: **`git add` 는 목록에 없는 경로가 하나라도 섞이면 아무것도 스테이징하지 않으면서 종료 코드 0 을 낸다.** 계획 검토가 잡았고 실측으로 재현했다. 이어지는 `git diff --cached --check` 도 빈 인덱스를 보고 `exit=0` 을 내므로 **두 신호가 모두 통과로 보인다.** `--name-only` 의 줄 수를 함께 세도록 고쳤고, 이번 커밋에서 17개가 담긴 것을 눈으로 확인했다.
- 메모: **같은 종류의 거짓 통과를 검증 명령에서 또 만들 뻔했다.** `npm run type-check 2>&1 | tail -20` 뒤에 `${PIPESTATUS[0]}` 를 찍었더니 zsh 에서 빈 값이 나왔고(zsh 는 소문자 `pipestatus` 이며 첨자가 1부터다), 파이프라인 마지막이 `echo` 라 전체 종료 코드는 언제나 0 이었다. 출력이 깨끗해 통과로 보였지만 **종료 코드를 실제로 본 적이 없었다.** 리다이렉트로 바꿔 `exit=0` 을 직접 확인했다.
- 메모: **`[INFRA-050]` 에서 겪은 돌연변이 경로 함정을 이번에는 피했다.** 사본을 `PYTHONPATH` 에만 얹고 저장소 루트에서 돌리면 `sys.path[0]` 이 빈 문자열이라 원본이 먼저 잡혀 돌연변이가 전부 통과한다. 매번 cwd 를 사본으로 옮기고 `__file__` 로 로드 위치를 찍어 확인한 뒤 쟀다. 사본에 `engine` 을 빠뜨려 `ModuleNotFoundError` 가 난 것도 그 확인 덕분에 곧바로 드러났다.
- 메모: **막는 것만 재면 통과 판정이 무의미하다.** 게이트가 전부를 막아도 거부 검사는 전부 통과한다. 각 시나리오에 정상 경로 어서트를 함께 두고, 의존성을 「불리면 `AssertionError` 를 내는 가짜」로 넣어 **게이트가 없을 때 호출이 실제로 일어났다는 것을 검사가 증명**하게 했다. 다만 `start-update` 는 `load_update_status` 만 통과시킨다. 그것까지 실패 가짜로 두면 첫 호출에서 터져 「403 이 났다」는 재지만 「스레드가 뜨지 않았다」는 증명하지 못한다.
- 메모: 두 언어에 두 벌 있는 관리자 판정을 공유 케이스 파일로 묶었다. 케이스 셋이 각각 다른 오구현을 겨냥한다. 「부분 일치」는 접두사 비교를, 「다른 항목에 붙어도」는 쪼개지 않은 원본 문자열 비교를, 「줄바꿈은 구분자가 아니다」는 한쪽만 `split(/[,\n]/)` 로 고치는 변경을 잡는다. **알려진 천장이 하나 있다.** `str.strip()` 과 `trim()` 의 공백 집합이 달라 U+001C·U+0085 는 파이썬만, U+FEFF(BOM) 는 자바스크립트만 제거한다(양쪽 런타임 실측). 그런 입력은 공유 파일로 표현할 수 없어 `_ceiling` 에 적었다.
- 메모: 게이트를 붙이자 기존 검사 일곱이 403 을 받아 깨졌다. 검사마다 고치는 대신 클라이언트를 만드는 자리에 관리자 신원을 세웠다. `test_kr_market_data_signals_routes_refactor.py` 는 블루프린트를 14곳에서 등록해 `_seed_admin_identity(app)` 헬퍼를 만들어 넣었다. **그 `before_request` 도 OPTIONS 에서 일찍 반환하게 해** 기존 OPTIONS 검사가 신원 없이 200 을 받는 상태를 그대로 유지했다.
- 메모: **두 리뷰의 회신 일부를 받지 못했다.** 보안 리뷰가 6-2(`INTERNAL_IDENTITY_SECRET` 회전) 도중에, 적대적 리뷰가 F5 답변 끝에서 잘렸고 각각 나머지를 요청했으나 회신이 오지 않았다. 번호가 이어지는 뒤쪽 항목(F6·F7)은 이미 받아 반영했고 잘린 6-2 의 내용도 `CLAUDE.md` 갱신으로 다루었으므로 못 받은 것은 두 문단의 마무리뿐이다. 새 결함이 그 안에 있었을 가능성은 남으며 `[INFRA-059]`~`[INFRA-062]` 를 다룰 때 같은 파일을 다시 본다.

# 2026-09-23

## [FE-045] 서버에 저장한 개인정보를 사용자가 실제로 지울 수 있게 한다
- 완료 2026-09-23 00:35 | 티어 T3 | 커밋 8bfd76e
- 변경: app/routes/kr_market_user_data_routes.py (+82, 신규 `DELETE /api/kr/user/data`, 단계별 예외 수집), chatbot/storage.py (+10 -3,
  `_save()` 가 `bool`, `clear_for_owner` 는 강제 재적재 뒤 저장 실패에 `RuntimeError`), frontend/src/app/components/SettingsModal.tsx
  (+39 -13, 서버 삭제 선행·`isDeleting`·`signOut` 실패 안내·문구), app/routes/kr_market.py (+31, `delete_user_usage` 래퍼와 등록) 외 5개
  (services/kr_market_quota_runtime_service.py +29, services/paper_trading_trade_account_mixin.py +22 -3, chatbot/storage_memory_manager.py
  +15, chatbot/command_service.py +8 -1, (legal)/privacy/page.tsx +4 -4), tests 7개 (+354), docs 3개 (계획 +927, qa/FE-045.md +111, TODO
  +19 -2). 코드 합계 +240 -24, 검사 +354
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「INFRA-077 승인, 끝나면 FE-045」) · closing-bet-python·closing-bet-nextjs·
  closing-bet-verify(위험 경로 grep 대조로 `paper_trading_trade_account_mixin.py` → T3, `fetchAPI` 재사용, 격리 사본·가짜 클라이언트·
  비관리자 세션 토큰) · frontend-skills §2 + next 번들 문서 05-server-and-client-components(모달은 순수 클라이언트 변경, 바꾼 판단 없음)
  ·03-layouts-and-pages(방침 페이지는 서버 컴포넌트 문구만, 바꾼 판단 없음) · superpowers:writing-plans(계획 7 과제) ·
  oh-my-claudecode:critic `fe045-critic`(REVISE: getter 선평가로 부분 실패 설계가 깨짐 → 단계 안 호출·실패 검사, 다중 워커 서술
  정정 → `[CHAT-033]` 범위 추가, `signOut` 까지 감싼 `try`, `api_usage` 유물 추가 → `[INFRA-079]`, `GET /api/portfolio` 의 잔고
  재생성 → QA 기대값 완화, 세션 0건 검사, 경미 4·관찰 3 반영, m5 주입 제거는 미반영) · ponytail-review(자체 검토, net -2:
  중복 실패 로그) · code-review(closing-bet-reviewer `fe045-reviewer`, 1차 CHANGES_REQUIRED → 2차 APPROVE. medium: `_save()` 가
  저장 실패를 삼켜 세션 삭제가 커밋되지 않아도 200 → `bool` 반환·강제 재적재·`RuntimeError`·`/clear all` 경고, low: 스냅샷
  실패 무시·실패 문구·계획 S-3 문구) · oh-my-claudecode:security-reviewer `fe045-security`(1차 CHANGES_REQUIRED → 2차 APPROVE.
  같은 medium 둘, 로그인 판정이 좁아 만료 순간 서버 호출 생략 → 익명 분기 삭제로 항상 서버 삭제 선행, 다른 기기 세션의 잔고
  재생성 → 확인 대화 안내, 정보: 레이트 리밋 부재·활동 로그 기록) · /review(oh-my-claudecode:critic `fe045-deep`, scratchpad
  재현 포함, ACCEPT-WITH-RESERVATIONS: 요청 중 버튼 비활성화·「삭제 중…」, `signOut` 실패 안내, Danger Zone 문구 「계정 삭제」.
  스냅샷 예외의 안전한 500 은 유지) · qa-only → qa(격리 사본 `qa_app.py` 가짜 Gemini, 비관리자 세션 토큰, browse, 필수 5/5)
- 검증: pytest RED 12(9 계획 + 3 리뷰 반영) → GREEN · pytest 전체 2627 통과 2 skipped(exit 0) · vitest RED 2 → GREEN, 전체
  91 파일 673 통과(자정 경계에서 `[JONGGA-037]` 시간 의존 검사 1건이 잠시 실패했으나 이 항목과 무관, 재실행 통과) · tsc 통과 ·
  lint 오류 0(경고 184 전부 기존. 중간의 `window.location.assign` 경고는 모달 안내로 바꿔 해소) · QA 5/5 통과(S-1 대화·프로필·
  매수 fixture 뒤 삭제 → `DELETE` 200 `{"deleted":{"chat_memories":true,"chat_sessions":1,"paper_trading":true,"usage":true}}`,
  로그아웃과 `/` 이동, 세 저장소 그 이메일 행 0·고아 메시지 0·사용량 키 없음, 콘솔 오류 0. S-2 재로그인 시 대화 0·기본
  프로필·사용량 0/10·잔고 초기값. S-3 익명 모달에 삭제 진입점 없음·DELETE 0건. S-4 신원 없음·위조 헤더 401. S-5 정적 전체.
  `/_next/mcp` 이슈 0·오류 0)
- 메모: 무료 사용량은 `usage.db` 가 아니라 `data/user_quota.json`(`kr_market.py` 의 `QUOTA_FILE`, 이메일 키와 `recharge_day:` 키)에
  있다. `services/usage_tracker.py`(`usage_log`)와 `engine/services/usage_tracker.py`(`api_usage`)는 운영 코드가 부르지 않는 유물이며
  로컬 `data/usage.db` 는 두 테이블 모두 행 0, 운영 서버 확인은 `[INFRA-079]`. 세 저장소는 한 트랜잭션으로 묶이지 않아 앞이 실패해도
  뒤를 계속 지우고 500 으로 알리며 재시도는 멱등이다. `HistoryManager._save()` 는 이제 성패를 돌려주고 다른 호출자는 값을 쓰지
  않는다. `clear_for_owner` 의 강제 재적재는 호출당 비용을 늘리고 스레드 경합 창의 빈도를 조금 넓힌다(`[CHAT-033]` 범위에 기록).
  저장 실패 뒤 그 워커는 다음 재적재 전까지 빈 대화 목록을 보이지만 SQLite 에는 남아 있고 재시도로 해소된다. SQLite 삭제가
  커밋된 뒤 레거시 스냅샷 쓰기가 예외를 던지면 500 이 나가지만 안전한 방향이라 그대로 둔다. `GET /api/portfolio` 는 잔고 행을
  다시 만들고 자산 이력을 기록하므로(`CLAUDE.md` Important Notes 4) 다른 기기의 남은 세션이 빈 계좌를 되살릴 수 있고, 확인
  대화가 그 기기의 로그아웃을 안내한다. 이 `DELETE` 자체는 활동 로그에 이메일과 함께 남으며 `[FE-046]` 범위다. `useQuota` 도
  `fetchAPI` 를 쓰므로 모달 검사는 URL 로 응답을 가른다. 두 진입점(프로필 탭 「계정 삭제」는 로그인, Danger Zone 은 관리자)
  모두 익명에게 보이지 않아 익명 분기는 두지 않았다. 계획 초판의 「익명은 브라우저 저장소만 비움」 S-3 은 QA 문서에서 「익명
  진입점 없음」으로 바뀌었다. 레이트 리밋 부재는 저장소 전체의 성질이다.

## [FLOW-016] 누적성과 표의 순번을 전체 기준으로 매긴다
- 완료 2026-09-23 07:58 | 티어 T1 | 커밋 8b0534f
- 변경: frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, CumulativeClientPage.regression-flow-016.test.tsx (+101 -7)
- 리뷰: closing-bet-nextjs(`fetchAPI`·신원 경계와 무관한 표시 계산임을 확인, 해당 없음) · frontend-skills §2(`useEffect`·`useMemo`·프롭 과다가 없어 vendor 스킬 해당 없음) · Next 번들 문서 05-server-and-client-components(순수 클라이언트 상태 변경, 해당 없음) · ponytail-review(Lean already, 지적 0건) · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: vitest 92파일 676건 통과(RED 2건 → GREEN) · type-check exit 0 · 변경 파일 eslint 오류 0 경고 1(기존 `any`, 2건에서 1건으로 감소) · QA 시나리오 5/5 통과
- 메모: 순번은 서버 `pagination` 의 `total - (page - 1) * limit - idx` 이며 필터 적용 전에 매긴다. 설계 승인에서 필터 중에도 원래 번호를 유지해 건너뛰게 두기로 정했다. `[FLOW-017]` 이 필터를 서버로 옮기면 `pagination.total` 이 필터 결과 건수가 되어 번호가 「필터 결과 안의 순번」으로 바뀐다. 그때 이 결정을 다시 볼 것

## [FLOW-017] 결과·등급 필터를 전체 기간에 건다
- 완료 2026-09-23 08:18 | 티어 T2 | 커밋 7b808db
- 변경: app/routes/kr_market_data_ai_routes.py, frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, tests/app/test_kr_market_data_ai_routes_refactor.py, CumulativeClientPage.regression-flow-017.test.tsx(신규), regression-005·regression-performance-batch(계약 교체), regression-flow-016(삭제) (코드 +264 -168)
- 리뷰: closing-bet-python(라우트는 HTTP 변환과 가드만, 입력 허용 목록 검증) · closing-bet-nextjs(신원·환경 변수 경계와 무관, 기존 맨 `fetch` 한 곳의 URL 만 바꿈) · frontend-skills §2 와 Next 번들 문서 05-server-and-client-components(상태·클릭 핸들러만 바뀌는 순수 클라이언트 변경) · ponytail-review(Lean already, 지적 0건) · closing-bet-reviewer `flow017-reviewer` APPROVE(최고 낮음, L3·L5·L6 반영, L1 은 설계대로, L2 는 `[FLOW-021]` 로 이월, L4 는 `[FLOW-019]` 에 연결) · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: 라우트 pytest RED 3건 → GREEN, 화면 vitest RED 3건 → GREEN · 전체 pytest 2630 통과 2 skip · 전체 vitest 92파일 674건 통과 · type-check exit 0 · 변경 디렉터리 eslint exit 0 · 리뷰 반영 뒤 범위 pytest 7·vitest 14·type-check·lint 재통과 · QA 시나리오 6/6 통과
- 삭제한 검사: `regression-flow-016` 은 클라이언트의 `total - (page - 1) * limit - idx` 순번 계산과 pagination 부재 시 배열 기준 대체를 검사했다. 그 코드가 사라졌고 번호 규칙은 라우트 pytest(`..._keeps_original_numbers`), 서버 `no` 표시는 flow-017 이 검사한다. `regression-performance-batch` 의 「현재 페이지 내」 표시와 「필터가 다시 조회하지 않음」 두 건은 이 항목이 뒤집은 계약이라 지웠고, `regression-005` 의 칩 기준은 서버 counts 로 바꿨다
- 메모: `[FLOW-016]` 메모가 예고한 번호 의미 변화는 설계 승인에서 「원래 번호 유지」로 정해, 서버가 필터 전 전체 목록 기준 `no` 를 붙인다. 버튼 건수는 필터와 무관한 전체 건수라 두 필터를 함께 켜면 칩과 표 total 이 다르다(승인된 설계). QA 중 원본 `data/runtime_cache.db` 수정 시각이 바뀐 것은 `[INFRA-078]` 경로

## [FLOW-019] 세 화면의 모집단 규칙을 맞추고 화면에 적는다
- 완료 2026-09-23 08:55 | 티어 T2 | 커밋 9506f23
- 변경: app/routes/kr_market_data_ai_routes.py, services/kr_market_analytics_service.py, frontend/src/app/dashboard/kr/closing-bet/page.tsx, frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, frontend/src/app/dashboard/kr/page.tsx, tests/app/test_kr_market_data_ai_routes_refactor.py, closing-bet/page.regression-flow-019.test.tsx(신규), CumulativeClientPage.regression-flow-019.test.tsx(신규) (코드 +182 -10)
- 설계: 현행 모집단 유지(누적성과·백테스트 요약은 D 포함, 종가베팅 화면은 D 를 목록과 일괄 매수에서 제외)와 30개 상한 유지를 사용자가 골랐다. 규칙은 `services/kr_market_analytics_service.py` 의 `JONGGA_SUMMARY_HISTORY_LIMIT` 주석 한 곳에 적고 세 화면에 문구로 드러냈다. 누적성과 라우트는 등급 집합 밖 건수를 `counts.other` 로 보내 칩 합이 전체와 맞게 했다(`[FLOW-017]` 리뷰 L4, 감사 §5.2). 종가베팅 목록의 D 판정을 일괄 매수와 같은 대소문자 무시로 맞췄다
- 리뷰: closing-bet-python(라우트는 HTTP 변환, 규칙은 services 에) · closing-bet-nextjs(신원·환경 변수 경계와 무관, 문구·조건부 렌더링만) · frontend-skills §2 와 Next 번들 문서 05-server-and-client-components · ponytail-review 1건 반영 · closing-bet-reviewer `flow019-reviewer` 1차 CHANGES_REQUIRED(MAJOR 2: 「30거래일」은 실제로 파일 30개, 「D 는 현행 판정기가 내지 않는 과거 등급」은 틀림) → 반영 뒤 APPROVE, 남은 MINOR(D 카드 설명) 반영, 소문자 d 처리 MINOR 는 근거를 들어 미반영 · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: 라우트 pytest RED 2건 → GREEN, 화면 vitest RED 3건 → GREEN · 전체 pytest 2631 통과 2 skip · 전체 vitest 94파일 678건 통과 · type-check·eslint exit 0 · 리뷰 반영 뒤 dashboard/kr vitest 50·cumulative 16·type-check·lint 재통과 · QA 시나리오 5/5 통과(S-5 는 2회차)
- 정정(감사 전제): AUDIT-FLOW(2차) §2.2 와 TODO 근거의 「현행 판정기는 S·A·B 만 내므로 D 는 2월 자료에만 있는 과거 등급」은 부분적으로만 맞다. `engine/grade_decider.py` 는 미달이면 None 을 돌려주지만, 읽기 경로의 재판정(`app/routes/kr_market_jongga_grade_helpers.py:138-146`)이 그 None 에 D 를 붙이고, 최근 유효 자료 경로(`services/kr_market_jongga_payload_helpers.py:468`)는 그 결과를 결과 파일에 되쓴다. 따라서 새 D 가 생길 수 있고 화면 문구는 「현행 기준 미달 또는 과거 등급」으로 적었다
- 메모: 요약과 누적성과의 모집단은 결과 파일이 30개를 넘기 전까지 같다(지금 18개, 둘 다 234건·승률 38.1%). 카드 문구의 30 은 백엔드 상수와 따로 적혀 있어 상수 주석에 함께 바꾸라고 적었다. QA S-5 1회차의 차트 요청 ECONNRESET 500 은 변경 범위 밖이고 재현되지 않아 `[INFRA-080]` 으로 이월했다. QA 중 원본 `data/runtime_cache.db` 수정 시각이 바뀐 것은 `[INFRA-078]` 경로

## [CHAT-032] 종목 질의 문맥이 언제나 비는 경로 복구
- 완료 2026-09-23 09:10 | 티어 T3 (계획 T2, 구현 diff 가 300줄을 넘어 상향. 위험 경로 파일 없음) | 커밋 55919c1
- 변경: chatbot/core_data_access_mixin.py, chatbot/stock_query_service.py, chatbot/payload_service.py 외 9개(chatbot 12개), tests/chatbot 11개(test_core_data_access_mixin.py 신규), tests/app/test_kr_market_route_integration.py (코드 +91 -486)
- 설계: 사용자가 「죽은 경로 제거」를 골랐다. `_detect_stock_query` 를 전체 종목 맵(`detect_stock_query_from_stock_map`)에 연결해 종목명·티커 질의와 VCP 상담 모드의 `[종목명(티커)]` 접두가 최근 5일 주가·수급·VCP 시그널 이력을 싣게 했다(접두는 6자리 티커 정규식이 그대로 잡아 별도 파싱 없음). `get_chatbot()` 이 `data_fetcher` 를 넘기지 않아 늘 `fetch_mock_data()` 의 빈 `vcp_stocks` 만 보던 경로 전체를 지웠다: 데이터 캐시와 mock, vcp 감지·포맷 함수, `fallback_response`, `vcp_data` 인자 사슬(`collect_market_context` 는 값 3개 반환), 프롬프트의 「## VCP 상위 종목」 절, 웰컴 Top 3, 관심종목 VCP 요약과 상세의 「VCP 상태: 현재 시그널 데이터 없음」 줄, 그 캐시를 비우는 일만 하던 챗봇 `/refresh` 명령(프론트 명령 목록에는 원래 없음, 이제 「알 수 없는 명령어」). 두 글자 이하 종목명은 한글·영숫자 경계에서만 잡고 조사 한 글자를 허용한다. 티커 정규식은 `(?<!\d)\d{6}(?!\d)` 로 바꿔 「005930은」도 잡는다
- 리뷰: closing-bet-python(chatbot 믹스인이 서비스 `*_impl` 로 위임하는 기존 구조를 유지, 테스트는 `tmp_path`·가짜 객체로 원본 `data/` 에 닿지 않음) · ponytail-review(`chat032-ponytail`, oh-my-claudecode:code-reviewer 레인) 2건 중 `payload_service` 의 getattr 방어 제거 반영, 모듈 인라인은 기존 위임 구조라 미반영 · closing-bet-reviewer `chat032-code-review` APPROVE(T3 상향 조건 반영, 짧은 종목명 부분 문자열 오탐을 경계 규칙으로 반영, 맵에 없는 티커 갈래 테스트 추가, 기록용·토큰 중복·참고 덤프 문서 3건 미반영) · oh-my-claudecode:critic `chat032-deep-review`(T3 심층) REVISE → 경계 반영 뒤 ACCEPT-WITH-RESERVATIONS(조사 붙은 티커 반영, 가장 긴 이름 하나만 싣는 한계에 `ponytail:` 표시, 유보 사항은 `[CHAT-038]`) · closing-bet-verify(격리 사본에서 브라우저와 `/_next/mcp` 두 관점, 가짜 genai 클라이언트가 모델이 받은 문맥을 되돌려 보여 줌)
- 검증: `test_core_data_access_mixin.py` RED(옛 경로가 `_get_cached_data` 로 빠짐) → GREEN · 전체 `pytest -q -p no:cacheprovider` 2622 통과 2 skip exit 0(리뷰 반영 전후 두 번) · frontend 변경 없음이라 type-check·vitest 생략 · QA 시나리오 5/5 통과(`docs/dev-cycle/qa/CHAT-032.md`)
- 삭제한 테스트: 지운 함수만 검사하던 10건. `get_cached_data` 3건(fetcher 캐시, 실패 시 mock 폴백, 하루 지난 캐시 갱신), `fetch_mock_data` 모양 1건, `detect_stock_query_from_vcp_data` 매칭 1건, `detect_stock_query` 의 vcp 경로 1건, `fallback_response` 2건(추천 목록, 이해 못함 문구), `format_stock_info` 필드 1건, `build_watchlist_summary_context` 빈 값·일치 1건. `/refresh` 는 `test_core_command_mixin` 의 상태 변경 명령 목록과 라우트 통합 테스트의 슬래시 명령 매개변수에서 뺐다(알 수 없는 명령 갈래는 `/unknown` 이 검사)
- 메모: VCP 상담 QA(S-2)는 사본 `data/` 에 오늘 VCP 시그널이 없어 종목을 고를 수 없었으므로 사본의 `signals_log.csv` 에만 005930 합성 행 하나를 넣어 실행했다. 원본 `data/runtime_cache.db` 수정 시각은 QA 전후로 바뀌지 않았다. 독립 단어로 쓰인 짧은 종목명 오탐, 가격과 티커의 우선순위, 두 글자 조사는 `[CHAT-038]` 로 이월했다

## [CHAT-033] 챗봇 저장소의 스레드 동시성 확보
- 완료 2026-09-23 09:34 | 티어 T2 (위험 경로 파일 없음, 구현 diff 292줄) | 커밋 bd5adf1
- 변경: chatbot/storage.py, chatbot/storage_memory_manager.py, chatbot/storage_history_helpers.py, tests/chatbot/test_storage_concurrency.py(신규) (코드 +261 -31)
- 설계: bounded. 워커 하나의 스레드 여덟 개가 챗봇 인스턴스 하나를 공유하므로 공용 데코레이터 `locked`(인스턴스 `RLock`)로 `MemoryManager`·`HistoryManager` 의 공개 메서드를 직렬화했다. `_save_single_entry` 는 레코드를 인자로 받고 `view()` 는 사본을 돌려준다. TODO 가 정하라고 남긴 「폴백 전 재적재냐, 폴백 제거냐」는 **제거**로 정했다. `_save()` 의 전체 동기화 폴백과 무델타 전체 동기화를 없애 삭제 절이 다른 워커의 세션을 지우거나 낡은 사본의 upsert 가 지운 세션을 되살리는 경로를 닫았다. 코드 리뷰를 거쳐 세 갈래를 더했다: 저장 실패 뒤 서명을 비워 다음 접근이 DB 를 다시 읽는다, SQLite 읽기 실패(기동 시점·재적재 중 모두)에서는 최대 15초 늦은 JSON 스냅샷을 쓰지 않고 `_reload_failed` 동안 SQLite·JSON 쓰기를 모두 거부한다, 레거시 JSON 이관은 DB 가 없거나 표가 빈 경우에만 일어난다. `save_history_sessions_to_sqlite` 와 스키마 파일은 바꾸지 않았다
- 리뷰: closing-bet-python(테스트는 `tmp_path` 로 원본 `data/` 에 닿지 않음) · ponytail-review 는 작성자가 먼저 메모리 쪽 `_*_locked` 보조 메서드 네 개를 공용 데코레이터로 통일한 뒤 Lean already · closing-bet-reviewer `chat033-reviewer` CHANGES_REQUIRED(medium 2: 실패 뒤 서명 갱신, 재적재 중 읽기 실패의 늦은 JSON) → APPROVE(low 1: 기동 시점 읽기 실패) → APPROVE(none). 반영 뒤 늘 참이 된 이관 조건은 지웠다. 잠금 안의 SQLite busy 대기와 잠금 밖에서 세션 사전을 고치는 외부 호출자(`command_service`·`session_access`)는 `ponytail:` 주석으로 한계만 표시했다 · closing-bet-verify(격리 사본, 브라우저·저장소·로그·`/_next/mcp`)
- 검증: RED 에서 메모리 update 경합(`datetime.now` 자리에 다른 스레드의 재적재를 끼움)·델타 실패 뒤 다른 워커 세션 보존·읽기 실패 뒤 이관 금지는 결정적 실패, 히스토리 두 스레드 테스트는 확률적 실패(작성자 3/4, 리뷰어 2/6) → GREEN · 리뷰 반영분 변이 4종(서명 유지, 거부 무력화, 재적재 JSON 교체, 기동 조건 복원) 각각 해당 테스트 실패 · 전체 `pytest -q -p no:cacheprovider` 2629 통과 2 skip exit 0 · frontend 변경 없음이라 type-check·vitest 생략 · QA 시나리오 4/4 통과(`docs/dev-cycle/qa/CHAT-033.md`, gunicorn `--workers 2 --threads 8`, 가짜 genai 3초 지연으로 두 탭의 요청이 서버 안에서 겹침을 로그 시각으로 확인)
- 갱신한 테스트: 없음. 「폴백이 돈다」를 전제한 기존 검사(`test_clear_for_owner_raises_when_delete_is_not_persisted`)는 패치 대상 함수가 남아 있어 그대로 통과했다
- 메모: 격리 환경에서는 Google 로그인을 할 수 없어 「같은 계정」은 같은 브라우저 두 탭의 서명된 익명 신원으로 대신했다. S-3 은 브라우저 드라이버 조작 문제(확인 모달 선택자 중복, 탭이 연 대화)로 세 번 만에 의도대로 실행했다. 사용자 메시지와 모델 응답을 따로 저장해 같은 세션에 동시에 쓰는 두 요청의 줄이 섞일 수 있는 것은 원래 있던 동작이며 이번 범위 밖이다. `[CHAT-035]`(HistoryManager 책임 분리)의 선행 조건이 충족되었다

## [FE-046] 활동 로그의 보관 기간을 날짜 기준으로 만들고 데이터 파일 권한을 좁힌다
- 완료 2026-09-23 10:30 | 티어 T3 (등록 시 T2, 위험 경로 `services/sqlite_utils.py` 로 상향) | 커밋 4eba1ee
- 변경: services/activity_logger.py, services/sqlite_utils.py, chatbot/storage_history_helpers.py, frontend/src/app/(legal)/privacy/page.tsx, frontend/src/app/(legal)/legal-pages.test.tsx, tests/services/test_activity_logger_refactor.py(신규), tests/chatbot/test_storage_history_helpers_refactor.py(신규), tests/services/test_sqlite_utils.py (코드 +362 -14)
- 설계: bounded, 사용자 승인 「30일로 진행」. 활동 로그는 `RetentionTimedRotatingFileHandler` 가 회전 파일을 이름의 날짜가 아니라 **마지막 수정 시각** 기준 30일(`ACTIVITY_LOG_RETENTION_DAYS`)로 지운다. 삭제는 기동 때(챗봇 블루프린트 등록이 전역 로거를 만듦)와 자정 뒤 첫 기록(회전) 때이며, 기동 때 기준 파일의 날짜가 이미 지났으면 곧바로 회전해 기록이 없던 파일도 지운다. 계획 검토 B1 뒤 사용자가 범위 확장을 승인해 다중 워커 회전 경합도 고쳤다: 늦게 회전하는 워커는 표준 구현이 「이미 회전됨」으로 돌아가 날짜 붙은 어제 파일에 재기동 때까지 계속 썼고(로컬 `user_activity.log.2026-09-09` 의 수정 시각이 09-21), 그 파일은 수정 시각이 계속 새로워 영원히 지워지지 않았다. `doRollover` 가 조기 반환 뒤 기준 파일을 다시 열고, `rotate` 는 `os.link`+`os.unlink` 로 이미 있는 날짜 파일을 덮어쓰지 않는다. 쓰기용 SQLite 연결(`connect_sqlite`)은 DB 와 남아 있던 -wal·-shm 을 0600 으로, 활동 로그는 0600 으로 열고 남은 회전 파일도 좁히며, 챗봇 JSON 스냅샷은 `os.open(..., 0o600)` 으로 만든다. 방침 3항은 삭제 시점·「이틀가량」 상한·접속과 재시작이 없을 때의 지연을, 10항은 「활동 로그와 데이터베이스 파일」의 권한 제한을 적고 시행일을 2026-09-23 으로 바꿨다
- 리뷰: closing-bet-python · closing-bet-nextjs · Next 번들 문서 03-layouts-and-pages(서버 컴포넌트 page 의 문안만 바뀜) · 계획 검토 oh-my-claudecode:critic `fe046-plan-critic` REVISE(B1 다중 워커 경합, B2 삭제 시점, B3 회전 경로 테스트, R1~R7) → ACCEPT-WITH-RESERVATIONS · ponytail-review(자체 수행, Lean already) · closing-bet-reviewer `fe046-reviewer` CHANGES_REQUIRED(medium: 삭제 시점은 첫 기록이 아니라 기동 시. 계획 검토 B2 의 판단이 틀렸음을 `create_app()` 실측으로 정정, low: 「조금 더」 상한, 생성 뒤 chmod 틈은 `ponytail:` 주석, 이미 0600 인 DB 의 남은 -wal·-shm 은 QA 실측, 표준 라이브러리의 동시 회전은 기록) → APPROVE · /review(oh-my-claudecode:critic `fe046-deep-review`, review 스킬 절차) REVISE(MEDIUM-1: 30일 넘게 기록 없던 기준 파일은 기동 정리에서 빠짐. GET 은 기록되지 않아 실제로 생김) → 기동 시 회전 → ACCEPT-WITH-RESERVATIONS(LOW-A: 기동 시 회전으로 동시 회전 창이 재기동마다 열림 → `rotate` 재정의) → ACCEPT-WITH-RESERVATIONS(LOW: 원본이 이미 옮겨진 경우 `FileNotFoundError` → 한 줄 반영) · closing-bet-verify(격리 사본, 브라우저·파일 권한·`lsof`·`/_next/mcp`)
- 검증: 신규 테스트 14건 RED → GREEN(활동 로그 9, SQLite 4 중 3, 챗봇 JSON 2. 읽기 전용 권한 불변 1건은 처음부터 통과) · 변이 6종(이름 날짜 기준, 재열기 제거, 회전 삭제 제거, `_open` chmod 제거, SQLite 호출 제거, JSON `open()` 복귀) 각각 해당 테스트 실패 · 리뷰 반영분 3건(기동 시 회전, 덮어쓰기 방지, 원본 없음)은 테스트를 먼저 써서 실패를 확인 · 최종 `pytest -q` 2644 통과 2 skip exit 0 · `npm run type-check` exit 0 · `npm run test` 679 통과 · QA 4/4 통과(`docs/dev-cycle/qa/FE-046.md`, gunicorn `--workers 2 --threads 8`, 요청 없이 기동만으로 30일 지난 회전 파일 삭제, 이름이 아니라 수정 시각 기준, 쓰기로 열린 DB 0600, 두 워커가 모두 기준 파일만 엶, 방침 문안)
- 갱신한 테스트: `legal-pages.test.tsx` 의 보관 기간 단언. 옛 동작(파일 개수 30개, 「달력상 30일보다 길어질 수 있다」)을 고정하던 두 문장을 새 문안 세 문장과 10항 한 문장으로 바꿨다
- 메모: 테스트 모듈 import 와 전체 pytest 가 원본에 영향을 주었다(설계 때 알린 것 포함). 원본 `logs/` 의 2026-02~05 회전 파일 9개가 지워지고 남은 파일이 0600 이 되었으며, `[INFRA-078]` 의 절대 경로로 원본 `data/` 의 `runtime_cache.db`·`paper_trading.db`(-wal·-shm)·`usage.db` 가 0600 으로 좁혀졌다(내용·수정 시각 불변). 운영 서버에는 다음 배포 뒤 첫 기동 때 같은 삭제와 권한 변경이 일어난다. QA 에서 `paper_trading.db` 는 익명으로 열리지 않아 서비스 하네스로 확인했고, `usage.db` 는 운영 코드가 열지 않는 유물이라 0644 로 남는다(`[INFRA-079]`). 수정 전 늦은 워커가 12일간 쓴 `user_activity.log.2026-09-09` 는 그 안의 09-09 기록이 약 42일 남는 과도기 파일이며 10-21 에 지워진다. 기록 없던 날 재기동하면 0바이트 날짜 파일이 하나 생긴다(0600, 30일 뒤 삭제). 새 DB·로그 파일은 umask 로 만든 뒤 좁히므로 비어 있는 잠깐 동안 0644 다. 하드 링크를 지원하지 않는 파일 시스템에서는 회전마다 실패하므로 배포 환경이 바뀌면 다시 본다. 0644 로 남는 다른 로그 세 개는 `[INFRA-081]` 로 이월했다

## [FE-047] 루트 레이아웃의 `lang` 을 한국어로 바로잡는다
- 완료 2026-09-23 11:05 | 티어 T1 | 커밋 bc7ac3d
- 변경: frontend/src/app/layout.tsx, frontend/src/app/layout.test.tsx(신규) (코드 +13 -1)
- 리뷰: closing-bet-nextjs(신원·환경 변수·API 경계와 무관한 속성 값 하나) · frontend-skills §2 의 `layout` 줄과 Next 번들 문서 `03-file-conventions/layout.md`(Root Layout 은 `<html>`·`<body>` 를 직접 정의하며 `lang` 은 그 속성이다) · vendor 스킬 해당 없음 · ponytail-review(Lean already, 지적 0건) · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: vitest RED 1건(`expected 'en' to be 'ko'`) → GREEN · 전체 vitest 95파일 680건 통과 · type-check exit 0 · QA 시나리오 2/2 통과
- 메모: 테스트는 `RootLayout` 을 함수로 불러 반환 요소의 `props.lang` 을 본다. `(legal)/layout.tsx` 는 위에 `app/layout.tsx` 가 있어 루트 레이아웃이 아니므로 `<html>` 을 두지 않는다. QA 는 Flask 없이 Next 만 띄웠고, 대시보드의 API 500 은 그 때문이다

## [INFRA-078] 격리 실행이 원본 `runtime_cache.db` 에 쓰는 절대 경로
- 완료 2026-09-23 11:29 | 티어 T1 | 커밋 55ea40a
- 변경: services/file_row_count_cache.py, services/common_update_status_service.py, services/kr_market_data_cache_jongga.py, services/kr_market_cumulative_cache.py, services/kr_market_backtest_summary_cache.py, tests/services/test_runtime_cache_path_refactor.py(신규) (코드 +64 -10)
- 리뷰: closing-bet-python(`.claude/skills/closing-bet-python/SKILL.md`, 위험 경로 해당 없음) · ponytail-review(Lean already, 지적 0건) · closing-bet-verify(`.claude/skills/closing-bet-verify/SKILL.md`, 대조군을 둔 격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: pytest RED 5건(상수 검사) → GREEN 6건 · 전체 pytest 2650 passed 2 skipped exit 0 · QA 시나리오 3/3 통과(수정 전 커밋 대조군 재현 포함)
- 메모: TODO 의 세 곳은 조사로 다섯 곳이 되었다(누적 성과·백테스트 요약 캐시 추가). 사용자 결정으로 캐시만 고치고, 상태 파일·파이프라인 출력의 절대 경로 여섯 곳은 `[INFRA-082]` 로, 전체 pytest 가 원본 `runtime_cache.db` 에 쓰는 34개 테스트는 `[INFRA-083]` 으로 이월했다. 동작 테스트는 구현 전에 돌리면 원본 DB 에 쓰므로 RED 에서 뺐다. QA 는 원본 앱을 scratch cwd 로 띄우지 말라는 browser-notes 에 따라 `git archive` 코드 사본 두 벌(수정 전·후)과 `data/` 사본 cwd 로 VCP-026 구조를 재현했고, 수정 전 코드만 코드 사본 쪽 표식 DB 에 다섯 테이블을 만들었다. 두 사본 모두 `data/vcp_status.json` 이 생긴 것은 `[INFRA-082]` 의 `app/routes/kr_market.py:191` 경로다. 운영은 `restart_all.sh` 가 저장소 루트로 `cd` 하므로 같은 파일을 쓴다

## [VCP-027] 실패 AI 재분석의 대상 날짜를 화면과 같은 판정으로 정한다
- 완료 2026-09-23 12:00 | 티어 T1 | 커밋 577e25c
- 변경: services/kr_market_vcp_reanalysis_service.py, app/routes/kr_market_data_signals_routes.py, tests/services/test_kr_market_vcp_service.py, tests/services/test_vcp_reanalysis_scope_refactor.py(신규) (코드 +77 -7)
- 리뷰: closing-bet-python(`.claude/skills/closing-bet-python/SKILL.md`, 위험 경로 해당 없음) · ponytail-review(Lean already, shrink 1건은 줄 수가 같아 미반영) · closing-bet-verify(`.claude/skills/closing-bet-verify/SKILL.md`, 스코프 날짜는 하네스·화면 날짜는 브라우저로 등급 구분)
- 검증: pytest RED 5건 → GREEN 5건 · 관련 4모듈 94 passed · 전체 pytest 2655 passed 2 skipped exit 0 · QA 시나리오 3/3 통과(수정 전 커밋 대조군 재현 포함)
- 메모: 결정 「날짜만 맞춤」으로 날짜 없는 갈래만 화면의 `_filter_signals_dataframe_by_date` 를 그대로 부르고, 고른 날짜의 CLOSED 행은 종전처럼 대상에 둔다. 구현 중 재분석 라우트의 읽기 열에 판정 열 `status`·`is_vcp` 가 없어 모든 행이 탈락하는 것을 찾아 두 열을 더했다(저장은 AI 세 열 병합 그대로). 옛 동작을 고정하던 `test_prepare_vcp_signals_scope_uses_latest_date_when_target_missing` 은 판정 열과 `today` 를 넣어 새 계약으로 갱신했다(삭제한 검사 없음). 재분석 POST 는 AI 비용 때문에 보내지 않고, 같은 자료·같은 읽기 경로의 하네스 날짜를 브라우저가 읽은 화면 날짜와 대조했다. 정적 검증의 전체 pytest 가 사본 생성 실패로 원본 작업 트리에서 돌아 원본 `data/`·`logs/` 를 바꿨으며, 그 범위를 `[INFRA-083]` 에 추가했다. QA 구간에는 원본 변경 0개

## [FLOW-018] 겹치는 재추천을 어떻게 셀지 정하고 화면에 드러낸다
- 완료 2026-09-23 12:42 | 티어 T1 | 커밋 7263a18
- 변경: frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, tests/services/test_cumulative_overlap_refactor.py(신규) (코드 +49 -3)
- 리뷰: closing-bet-nextjs(`.claude/skills/closing-bet-nextjs/SKILL.md`, 번들 문서 `05-server-and-client-components.md`) · closing-bet-python(`.claude/skills/closing-bet-python/SKILL.md`, 위험 경로 해당 없음) · ponytail-review(delete 1건 반영, net -1) · closing-bet-verify(`.claude/skills/closing-bet-verify/SKILL.md`, 문구는 브라우저·값 불변은 API 대조로 등급 구분)
- 검증: 특성화 테스트 1건 통과(현행 정의 고정이라 RED 없음) · 관련 2모듈 36 passed · 전체 pytest 는 저장소 사본에서 2675 passed 3 skipped(frontend 의존 세 파일은 frontend 를 넣은 사본에서, `git check-ignore` 1건은 원본에서 읽기 전용 재실행) · vitest 680 passed · type-check exit 0 · QA 시나리오 3/3 통과
- 메모: 결정 「현행 유지 + 문구」로 집계는 바꾸지 않고 추천 단위 정의를 확정했다. 근거는 이 화면이 재는 것이 추천의 성과이고 사용자가 추천마다 따로 진입할 수 있다는 점이다. 처음에는 `interpretation` 필드에 문구를 넣었으나 `renderStatTooltip` 이 승률·손익비에서 그 필드를 그리지 않아 `desc` 로 옮겼다. 회귀 테스트는 AUDIT-FLOW §2.1 의 005935 구조(앞 건 익절 전 재추천, 뒤 건 손절)를 실제 레코드 빌더로 만들어 두 거래·1승 1패로 세는지 고정한다. 전체 pytest 는 `[INFRA-083]` 때문에 사본에서 돌렸다. QA 구간의 원본 `data/`·`logs/` 변경 0개(정적 검증 구간은 측정하지 않음)

## [FLOW-020] 「최고가」 열이 값을 숨기지 않게 한다
- 완료 2026-09-23 13:06 | 티어 T1 | 커밋 4f47dd1
- 변경: frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.regression-flow-020.test.tsx(신규) (코드 +60 -2)
- 리뷰: closing-bet-nextjs(`.claude/skills/closing-bet-nextjs/SKILL.md`, 번들 문서 `05-server-and-client-components.md`, vendor 스킬 해당 코드 없음) · ponytail-review(Lean already, 지적 0건)
- 검증: 새 vitest 가 수정 전 `Expected "-1.5%" Received "-"` 로 실패하고 수정 뒤 통과 · vitest 전체 681 passed · type-check exit 0 · 변경 파일 eslint 오류 0(589행 `any` 경고는 기존 코드) · QA 시나리오 3/3 통과(격리 서버 전 5쪽 234행을 API 와 대조해 불일치 0, 177번 삼성생명 `-1.5%`)
- 메모: 서버가 일봉이 없을 때도 `max_high` 에 0 을 보내므로 값으로는 결측과 실제 0% 를 가를 수 없다. 일봉이 없는 갈래는 모두 `days` 0 이라 하이픈을 그때로 한정했고 백엔드는 바꾸지 않았다. `formatSignedPercent` 는 페이지 컴포넌트 안에 있어 표에서 부를 수 없었으므로 모듈 최상위로 옮겨 재사용했다. 색상 규칙은 그대로라 음수는 회색이다. 실자료에 `days > 0` 이면서 `maxHigh` 가 정확히 0 인 거래가 없어 `0%` 표기는 vitest 로만 확인했다. 파이썬 파일을 건드리지 않아 pytest 는 돌리지 않았다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [FLOW-021] 누적성과 필터·페이지 전환 중 화면 전체가 로딩 표시로 바뀐다
- 완료 2026-09-23 13:12 | 티어 T1 | 커밋 bb95a95
- 변경: frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.regression-flow-021.test.tsx(신규) (코드 +70 -4)
- 리뷰: closing-bet-nextjs(`.claude/skills/closing-bet-nextjs/SKILL.md`, 번들 문서 `05-server-and-client-components.md`, vendor 스킬 해당 코드 없음) · ponytail-review(Lean already, 지적 0건)
- 검증: 새 vitest 가 수정 전 재조회 중 「데이터 불러오는 중...」이 보여 실패하고 수정 뒤 통과 · vitest 전체 682 passed · type-check exit 0 · 변경 파일 eslint 오류 0(589행 `any` 경고는 기존 코드) · QA 시나리오 3/3 통과(요청을 1.5초 늦춘 격리 브라우저에서 필터·페이지 재조회 중 KPI·필터 유지와 `aria-busy`, `보유`+`D` 0건 문구, 오류 0)
- 메모: 전체 화면 스피너의 조건을 `loading && pagination === null` 로 좁혀 첫 응답 전에만 쓰고, 상태 변수를 새로 두지 않았다. 재조회 중에는 표를 감싼 영역에 `aria-busy` 와 `opacity-50` 을 준다. 첫 응답이 실패하면 `pagination` 이 null 로 남아 다음 재조회도 전체 스피너가 되는데, 수정 전과 같은 동작이라 수용했다. 파이썬 파일을 건드리지 않아 pytest 는 돌리지 않았다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [CHAT-034] SQLite 누락 테이블 복구 래퍼 통합
- 완료 2026-09-23 13:30 | 티어 T3 (위험 경로 `chatbot/storage_sqlite_common.py`) | 커밋 23a39f8
- 변경: chatbot/storage_sqlite_common.py, chatbot/storage_sqlite_history.py, chatbot/storage_sqlite_memory.py, tests/chatbot/test_storage_sqlite_common_refactor.py(신규) (코드 +244 -282)
- 설계: bounded, 2026-09-23 사용자 「응 진행해」로 승인. 계획 `docs/superpowers/plans/2026-09-23-chat-034-sqlite-recovery-wrapper.md`. 열네 함수가 복제하던 「누락 테이블 → `force_recheck` → `_retried=True` 재귀」를 저장소에 이미 있던 `services/sqlite_ready_gate.run_with_schema_recovery` 를 감싼 `run_chatbot_sqlite_with_recovery` 한 곳으로 모았다. `services/` 는 고치지 않았다. 공개 인자·로그 문구·기본값은 그대로이고 private `_retried` 와 두 모듈의 로컬 `_is_missing_table_error`·재시도 상수를 지웠다. 수용한 차이: prune 의 probe·삭제를 한 operation 으로 묶어 일시 잠금 재시도 예산이 합계 2회가 됨, 복구 뒤 재시도 실패 로그에 `schema recovery retry failed:` 접두, 재시도 때 `exists`·비강제 확인 생략
- 리뷰: closing-bet-python · 계획 검토 critic `chat034-plan-critic` ACCEPT-WITH-RESERVATIONS(필수 2: `load_memories` 는 prune 이 먼저 복구해 자기 래퍼를 검증 못 함, 복구 실패 테스트는 세 모듈을 함께 패치해야 함 · 권고 3 모두 반영) · ponytail-review(RED 용 래퍼 단위 테스트 1건이 매개변수화 14건과 중복 → 삭제) · closing-bet-reviewer `chat034-code-review` APPROVE(low 2: 테스트 건수 기록, prune 재시도 예산 기록 → 계획에 기록) · /review T3 심층 `chat034-deep-review`(architect) APPROVE(low: 복구 성공 뒤 재시도 실패 경로 검사 공백 → 테스트 추가, 기존 동작 1건 → `[CHAT-039]` 이월) · closing-bet-verify(격리 사본 브라우저·SQLite·로그·`/_next/mcp`)
- 검증: RED 는 래퍼 import 실패, 특성 테스트 16건은 전환 전 코드로 통과 → 전환 뒤 `tests/chatbot` 256 passed · pyflakes 0 · 변이 3종(history 테이블 목록에서 messages 누락, memory 테이블 오기, `load_memories` 래퍼 제거) 모두 검출 · 전체 pytest 는 격리 `git archive` 사본에서 2609 passed 55 failed 8 errors 이며 변경 전 HEAD 사본의 실패·오류 63건과 목록이 같아 새 실패 0(node·frontend `node_modules` 가 없는 사본 환경의 scripts·identity cross-runtime 테스트) · QA 시나리오 4/4 통과(`docs/dev-cycle/qa/CHAT-034.md`, 게이트 ready 뒤 사본 DB 테이블 DROP → 질문 전송·대화 삭제·프로필 저장이 오류 없이 반영되고 테이블이 다시 생김)
- 갱신한 테스트: 없음. 기존 복구 테스트 3건(`test_storage_sqlite.py`)은 그대로 통과했다. 그중 `load_memories` 경우는 prune 이 먼저 복구해 자기 경로를 검사하지 못한다는 사실을 계획 검토가 밝혔고, 신규 파일이 prune 을 우회해 그 경로를 덮는다
- 메모: TODO 의 QA 초안(DB 를 지우고 질문 전송)은 설계 때 「챗봇 전송 금지라 세션 생성 경로로 대체」로 바꿨으나, 세션 목록이 사용자 메시지 있는 세션만 보여 주어 빈 세션으로는 사이드바를 검증할 수 없었다. 그래서 `[CHAT-031]`~`[CHAT-033]` 과 같은 사본 전용 가짜 genai 진입점으로 원래 초안대로 실행했다(실제 LLM 호출 0). DB 파일을 지우는 대신 게이트 ready 뒤 테이블을 DROP 한 것은, 파일이 없으면 게이트가 처음부터 만들어 복구 경로를 타지 않기 때문이다. S-2 첫 조작은 확인 모달의 「삭제」 버튼 중복으로 `DELETE` 가 나가지 않아 무효로 하고 다시 했다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [CHAT-039] 메시지 테이블 유실 뒤 JSON 스냅샷으로 메시지 되살리기
- 완료 2026-09-23 13:55 | 티어 T2 (위험 경로 파일 없음) | 커밋 182b67e
- 변경: chatbot/storage.py, chatbot/storage_sqlite_history.py, chatbot/storage_sqlite_helpers.py, tests/chatbot/test_history_manager_sync.py (코드 +214 -0)
- 설계: bounded, 2026-09-23 사용자 「응 진행해」로 승인. 순서는 사용자 선택 「CHAT-039 먼저」로 `[CHAT-035]` 앞에 뒀다(같은 `_load` 를 건드리므로 결함을 먼저 고정). 「스냅샷 강제 동기화 건너뛰기」 안은 다음 `_save` 의 15초 주기 스냅샷이 결국 JSON 을 덮고 재적재 경로에서도 같은 일이 생겨 기각했다. `_load` 가 SQLite 세션은 있는데 모든 세션의 메시지가 0개일 때만 스냅샷을 읽고, `updated_at` 이 같은 세션만 되살린다(메시지를 지우면 `updated_at` 이 바뀌어 지운 메시지는 살아나지 않음). 되살린 메시지는 `restore_lost_history_messages_in_sqlite` 가 `BEGIN IMMEDIATE` 안에서 「판이 같고 메시지 행이 없을 때만」 INSERT 하고, 쓴 뒤 DB 를 다시 읽는다. 쓰기 실패는 읽기 실패로 다뤄 쓰기를 막고 다음 접근에서 다시 시도한다. 공용 래퍼(`storage_sqlite_common.py`, T3) 서명은 바꾸지 않았다
- 리뷰: closing-bet-python · ponytail-review(oh-my-claudecode:code-reviewer 레인 `chat039-ponytail`) 「Lean already. Ship.」 · closing-bet-reviewer `chat039-reviewer` CHANGES REQUESTED(medium 2: 되살리기 쓰기 실패를 무시해 일시 오류가 영구 유실, 조건 없는 upsert 가 다른 워커의 새 쓰기를 덮음. 둘 다 재현 스크립트로 확인 → 조건부 쓰기·DB 재적재·실패 시 None / low 2: 스냅샷 `messages` 형식 → `isinstance`, 재적재 경로의 JSON 쓰기 → `ponytail:` 주석) → 재판정 APPROVE(low 2 기록만: 되살린 직후 다른 워커가 모두 지우면 스냅샷이 `{}` 로 맞춰지나 DB 와 같아 유실 아님, 리뷰어 재현 스크립트 하나가 옛 함수를 가로챔. 되살리기 실패 재현은 가로챌 함수를 새 함수로 바꾼 사본으로 다시 확인) · closing-bet-verify(격리 사본 브라우저·SQLite·JSON·`/_next/mcp`)
- 검증: RED 는 되살리기 테스트 1건 실패(판이 다른 경우는 종전에도 되살리지 않아 처음부터 통과) → 최종 `tests/chatbot` 262 passed, 신규 테스트 6건 · 변이 7종(`updated_at` 조건 제거, SQLite 재기록 생략, 호출 제거, 조건부 쓰기 제거, 실패 시 사본 반환, 재적재 대신 메모리 사본 반환, 재적재 경로 호출 제거) 모두 검출. 여섯 번째는 처음에 살아남아 「다른 워커가 먼저 쓴 판을 보여 준다」 테스트를 추가해 잡았다 · 전체 pytest 는 격리 `git archive` 사본에서 25 failed 2646 passed 3 skipped 8 errors 이며 HEAD 사본의 실패·오류 33줄과 목록이 같아 새 실패 0(node·frontend `node_modules` 가 없는 사본 환경) · QA 3/3 통과(`docs/dev-cycle/qa/CHAT-039.md`, 사본 DB 에서 `chatbot_messages` DROP 뒤 새로고침하면 A 본문 4개가 화면·DB 에 되살아나고 이후 질문도 저장됨)
- 갱신한 테스트: 없음(기존 테스트는 그대로 통과)
- 메모: QA 가 설계 한계를 그대로 드러냈다. 한 대화의 마지막 저장이 15초 주기에 걸려 스냅샷에 실리지 않으면 그 대화는 되살리지 못한다(QA 의 B). 다음 저장이 15초 뒤에 오면 스냅샷이 따라잡는다(QA 의 A). 기대에 적은 `Restored messages of` 로그 줄은 격리 구성에서 앱 경고가 gunicorn 오류 로그로 오지 않아 관측하지 못했고, DB 행 수와 화면으로 대신 판정했다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [JONGGA-040] 종가베팅 최신 파일을 원자적 저장 한 번으로만 쓴다
- 완료 2026-09-23 14:20 | 티어 T3 (`scripts/init_data.py` 위험 경로, 등록 시 T1 에서 상향) | 커밋 09dca99
- 변경: scripts/init_data.py, services/kr_market_route_service.py, services/kr_market_jongga_runtime_service.py, tests/scripts/test_jongga_latest_single_write.py (코드 +71 -31)
- 설계: bounded, 2026-09-23 사용자 「응 진행해」로 승인. 순서는 사용자 선택 「JONGGA-040 (Recommended)」(P0 세 항목은 운영 적용·QA 재시도가 막혀 로컬에서 끝낼 수 없음). `run_screener`(`engine/generator.py:170`)가 이미 `atomic_write_text` 로 일자·최신 파일을 저장하므로 호출자의 중복 쓰기 세 곳만 지웠다. 스케줄러 진입 `create_jongga_v2_latest` 의 `open('w')` 재쓰기, 두 백그라운드 파이프라인의 두 번째 `save_result_to_json`. 엔진은 고치지 않았다. 계획 `docs/superpowers/plans/2026-09-23-jongga-040-single-latest-write.md`
- 리뷰: closing-bet-python · 계획 검토(oh-my-claudecode:critic `critic-jongga040`) ACCEPT-WITH-RESERVATIONS(R1: `send_jongga_notification` 이 `BASE_DIR` 절대 경로로 최신 파일을 읽는데 계획이 빠뜨림 → 수용 차이에 명시하고 `[INFRA-082]` 에 추가 / R2: init_data 테스트가 cwd 기준 쓰기·저장 재호출을 못 잡음 → `monkeypatch.chdir` 와 `saves == []` 추가) · ponytail-review 「Lean already. Ship.」 · closing-bet-reviewer `review-jongga040` APPROVE(low 1: CLI 호출자 두 곳을 `[INFRA-082]` 에 추가 / low 2: None·예외 갈래 미고정은 이 diff 가 바꾸지 않아 미반영) · 심층 리뷰(oh-my-claudecode:code-reviewer `deep-review-jongga040`, review 스킬 절차) CHANGES REQUESTED(H1: 이 항목의 리뷰 기록을 넣을 때 문자열 전체 치환이 `[CHAT-035]`·`[VCP-030]` 의 체크 줄까지 덮음 → 원문으로 되돌리고 diff 로 확인. 코드 지적 없음: 캐시 무효화·다른 쓰기 주체와의 lost-update 창 소멸·동시 실행 시 일자·최신 파일 불일치 감소 확인) · closing-bet-verify(격리 사본 harness·브라우저·`/_next/mcp`)
- 검증: RED 3건(파일 생성, 두 서비스의 save 1회 호출)이 구현 전 의도한 이유로 실패 → 통과, 계획 검토가 HEAD 사본에서 RED 를 다시 확인 · 관련 범위 284 passed · 전체 pytest 는 격리 사본에서 2649 passed 3 skipped, 실패·오류 33건은 사본 환경 원인(`@next/env` 없음 32건은 `node_modules` clone 뒤 재실행 통과, git 저장소가 아니라 실패한 1건은 원본 트리에서 통과)으로 새 실패 0 · QA 4/4 통과(`docs/dev-cycle/qa/JONGGA-040.md`, 쓰기 계측 harness 로 최신 파일 쓰기가 원자적 1회임을 확인하고 변경 전 커밋 대조군에서는 2회였음을 확인, 알림이 방금 쓴 파일을 읽음, 격리 화면에 새 결과 카드 표시)
- 갱신한 테스트: 없음(신규 `tests/scripts/test_jongga_latest_single_write.py` 3건)
- 메모: 이제 최신 파일은 cwd 기준 `data/` 에만 쓰인다. 운영은 `restart_all.sh`·`run.py` 가 루트로 옮기므로 같은 파일이지만, 알림과 중복 알림 가드는 `BASE_DIR/data` 를 쓰므로 cwd 가 루트가 아닌 실행에서는 어긋날 수 있다. 이 경로 통일은 `[INFRA-082]` 로 넘겼다. QA 계획 초안의 S-4 기대값을 S-3 뒤 순서에 맞게 정정했다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [VCP-030] `signals_log.csv` 쓰기를 원자적으로 바꾸고 0바이트 파일에서 회복한다
- 완료 2026-09-23 14:33 | 티어 T3 (`scripts/init_data.py` 위험 경로) | 커밋 b171416
- 변경: scripts/init_data.py, tests/scripts/test_init_data_vcp_scheduler.py (코드 +77 -7)
- 설계: bounded, 2026-09-23 사용자 「응 진행해 (Recommended)」로 승인. 순서는 사용자 선택 「VCP-030 (Recommended)」(P0 세 항목은 운영 적용·QA 재시도가 막혀 로컬에서 끝낼 수 없음). `create_signals_log` 의 `to_csv` 다섯 곳(병합·새 파일·당일 정리·시그널 없음 빈 파일·예외 갈래 빈 파일)을 이미 있는 `services/kr_market_vcp_reanalysis_service.write_vcp_signals_csv_atomic`(BOM 포함 원자적 교체와 캐시 무효화)으로 바꾸고, 기존 로그를 읽는 두 곳은 새 도우미 `_read_signals_log` 가 `EmptyDataError`(0바이트·BOM 만·줄바꿈만·공백만 든 파일)를 23열 빈 로그로 이어간다. 열 수가 어긋난 파일(`ParserError`)은 사용자 결정대로 `[VCP-029]` 의 보존 + False 를 유지하고 `.corrupt-<시각>` 격리는 하지 않았다. 계획 `docs/superpowers/plans/2026-09-23-vcp-030-signals-log-atomic-write.md`
- 리뷰: closing-bet-python · 계획 검토(oh-my-claudecode:critic `vcp030-plan-critic`) ACCEPT-WITH-RESERVATIONS(R1: 당일 정리 갈래의 원자성을 잡는 테스트 없음 → 쓰기 실패 테스트를 병합·정리 parametrize / R2: 캐시 무효화 DELETE 는 `dirname(csv)/runtime_cache.db` 로 가서 테스트가 원본에 닿지 않음 → 문장 정정 / R3: 파일 권한 0644→0600 → 수용 차이에 기록 / m1: ENOSPC·파일 없음에서 False 대신 OSError → 기록) · ponytail-review 「Lean already. Ship.」 · closing-bet-reviewer `vcp030-code-review` APPROVE(medium, 범위 밖: `SignalTracker` 쓰기 세 곳이 여전히 비원자 → `[VCP-034]` 등록, 원자성 주장을 `create_signals_log` 로 한정 / low: 예외 갈래 빈 파일 쓰기 실패 시 최신 JSON 도 건너뜀 → 계획 기록 / low: 병합 실패 때도 최신 JSON 은 먼저 쓰임, `[VCP-029]` 부터의 기존 동작) · 심층 리뷰(oh-my-claudecode:critic `vcp030-deep-review`, review 스킬 절차) APPROVE(Minor: `[VCP-034]` 근거가 「막힘」으로 적혔으나 실측은 행 중간 잘림이 예외 없이 손상 행으로 굳음 → 근거 정정. TODO 편집 hunk 가 VCP-030·034 구간 하나뿐임을 확인) · closing-bet-verify(격리 사본 하네스·대조군·브라우저·`/_next/mcp`)
- 검증: RED 4건(0바이트 두 건이 False, fsync 실패 두 경우가 True)이 구현 전 의도한 이유로 실패 → 통과 · 관련 6모듈 60 passed · 변이 3종(except 제거, 병합·정리 쓰기 `to_csv` 복원)이 각각 해당 테스트를 실패시킴 · 전체 pytest 는 격리 사본에서 2685 passed 1 failed 3 skipped, 실패 1건은 사본이 git 저장소가 아니라서 실패하는 gitignore 테스트로 원본 트리에서 통과해 새 실패 0 · QA 4/4 통과(`docs/dev-cycle/qa/VCP-030.md`, 하네스로 0바이트 로그 회복과 원자적 쓰기 1회, fsync 실패 시 병합·정리 두 갈래의 바이트 보존, 손상 로그 보존 + False 를 확인하고 변경 전 커밋 대조군과 비교, 격리 화면에 CSV 에서 읽은 「QA종목030」 행 표시)
- 갱신한 테스트: 없음(기존 `tests/scripts/test_init_data_vcp_scheduler.py` 에 3건 추가, 마지막은 두 경우 parametrize)
- 메모: 원자성은 `create_signals_log` 에만 해당하고 같은 파이프라인의 `SignalTracker` 쓰기 세 곳은 `[VCP-034]` 로 넘겼다. 파일 권한은 0600 이 되며 운영 원본도 이미 0600 이었다(QA 사본 관찰). 운영 `data/signals_log.csv` 가 symlink 라면 `os.replace` 가 일반 파일로 바꾸는 점은 확인하지 못했다. 첫 하네스 실행은 f-string 문법 오류로 무효가 되어 다시 돌렸다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [VCP-034] `SignalTracker` 의 `signals_log.csv` 쓰기도 원자적으로 바꾼다
- 완료 2026-09-23 15:58 | 티어 T2 (`engine/signal_tracker_analysis_mixin.py` 는 위험 경로 밖) | 커밋 af4f4d6
- 변경: engine/signal_tracker_analysis_mixin.py, tests/engine/test_signal_tracker_refactor.py (코드 +66 -59)
- 설계: bounded, 2026-09-23 사용자 선택 「VCP-034 (Recommended)」(P0 세 항목은 운영 적용·QA 재시도가 막혀 로컬에서 끝낼 수 없음)와 append 경로 질문의 「분기 삭제 (Recommended)」로 승인. `_append_to_log` 의 새 파일·병합 쓰기와 `update_open_signals` 의 청산 갱신 쓰기를 `write_vcp_signals_csv_atomic` 으로 바꿨다. 조사에서 네 번째 쓰기인 빠른 append 분기(`mode="a"` 이어 쓰기)를 찾았고, 원자적 쓰기가 어차피 파일 전체를 다시 쓰므로 분기를 삭제해 항상 `append_signals_log` 병합 경로로 가게 했다. 캐시 순서는 종전과 같다(`os.replace` → sqlite 무효화 → `_refresh_signals_log_source_cache`)
- 리뷰: closing-bet-python · ponytail-review(테스트 도우미 인라인, 매개변수를 날짜 하나로 축소 2건 반영) · closing-bet-reviewer `vcp034-reviewer` APPROVE(max low. 캐시 순서와 inode 시그니처, fast-append 삭제 뒤 결과, 순환 import, 호출자 예외 처리 네 가지 모두 문제 없음 / low: 기존 행 전체를 다시 써서 숫자 열 표기가 바뀔 수 있음 → QA S-3 허용 차이로 기록 / low: 0644→0600 → VCP-030 과 같은 방향이라 수정 없음 / low: 읽기·병합·교체 사이 잠금 없음 → `[VCP-035]` 등록) · closing-bet-verify(격리 사본 하네스·대조군·브라우저·`/_next/mcp`)
- 검증: RED 4건(fsync 실패 시 예외가 나지 않음)이 구현 전 「DID NOT RAISE」로 실패 → 통과 · 관련 테스트 700 passed · 변이 3종(새 파일·병합·청산 갱신 쓰기를 `to_csv` 로 복원)이 각각 해당 테스트를 실패시킴 · 전체 pytest 는 격리 사본에서 2657 passed 3 skipped, 사본에 `frontend/node_modules` 가 없어 난 25 failed 8 errors 는 링크 뒤 세 파일 재실행에서 78 passed, 남은 gitignore 테스트 1건은 사본이 git 저장소가 아니라 실패했고 원본 트리에서 통과해 새 실패 0 · vitest 682 passed · QA 4/4 통과(`docs/dev-cycle/qa/VCP-034.md`, 하네스로 청산 갱신과 추가의 원자적 쓰기 1회, fsync 실패 시 세 경로와 파이프라인 단계의 바이트 보존을 변경 전 커밋 대조군과 비교, append 분기 삭제 뒤 행 집합과 값이 같음, 격리 화면과 API 에서 청산된 행이 사라지고 「QA종목034」 만 남음)
- 갱신한 테스트: `test_append_to_log_uses_fast_append_when_no_same_day_overlap` 을 `test_append_to_log_keeps_both_rows_without_same_day_overlap` 으로 바꿨다. 삭제한 분기의 `mode="a"` 사용과 병합 경로 미호출을 검사하던 것을, 두 행이 모두 남고 BOM 이 하나뿐인지 검사하도록 바꿨다. `test_update_open_signals_loads_log_with_ticker_dtype` 은 가짜로 바꾸는 대상을 `DataFrame.to_csv` 에서 새 쓰기 함수로 옮겼다(검사 내용은 같음). 새 테스트 2개(첫째는 세 경우 parametrize)
- 메모: S-4 첫 시도에서 하네스 QA 행에 `is_vcp` 가 없어 API 가 `no_data` 였고, 사본 로그와 QA 행에 `is_vcp=True` 를 넣어 다시 구성했다. 그 과정에서 `SignalTracker` 가 저장한 시그널이 화면에 나오지 않는 문제를 찾아 `[VCP-036]` 으로 등록했다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [VCP-036] `SignalTracker` 가 저장한 시그널에 `is_vcp` 를 기록해 화면에 나오게 한다
- 완료 2026-09-23 16:06 | 티어 T2 (`engine/signal_tracker_analysis_mixin.py` 는 위험 경로 밖) | 커밋 584c734
- 변경: engine/signal_tracker_analysis_mixin.py, tests/engine/test_signal_tracker_refactor.py (코드 +4 -1)
- 설계: bounded, 2026-09-23 사용자 선택 「VCP-036 (Recommended)」로 승인(항목 선택과 설계안을 한 질문으로 제시). `scan_today_signals` 가 `detect_vcp_forming` 을 통과한 행에 `is_vcp=True` 를 쓴다. 이미 저장된 빈 `is_vcp` 행은 그대로 두고 화면 필터 `_is_vcp_signal_row` 는 바꾸지 않았다. 필터를 느슨하게 하면 스크리너가 탈락시킨 행까지 보일 수 있기 때문이다. 호출자는 `run.py` 메뉴 2 와 수동 스크립트 `tests/verify_vcp_pipeline.py` 뿐이고 스케줄러 경로(`create_signals_log`)는 이미 `is_vcp` 를 쓴다
- 리뷰: closing-bet-python · ponytail-review(반환 프레임 단언이 저장 로그 단언과 겹침 1건 반영) · closing-bet-reviewer `vcp036-reviewer` APPROVE(max low. 메뉴 2 재저장에서 `is_vcp` 유지, `is_vcp` 열 없는 기존 로그와의 병합 판정 `[False, True]`, 결측을 거짓과 같이 숨기는 동작이 설계와 일치 / low: 저장 질문에 N 을 골라도 스캔 단계 저장분이 화면에 보임 → `[VCP-037]` 등록 / low: 병합 갈래 단위 테스트 없음 → QA S-1·S-2 가 실측해 미반영) · closing-bet-verify(격리 사본 하네스·대조군·브라우저·`/_next/mcp`)
- 검증: RED 는 `KeyError: 'is_vcp'` → 통과 · `tests/engine` 668 passed · pyflakes 는 HEAD 에도 있던 미사용 import 1건만 · 전체 pytest 는 격리 `git archive` 사본에서 2689 passed 1 failed 3 skipped, 실패 1건은 사본이 git 저장소가 아니라 실패하는 gitignore 테스트로 원본 트리에서 통과해 새 실패 0 · QA 3/3 통과(`docs/dev-cycle/qa/VCP-036.md`, 원본 자료 사본으로 실제 스캔해 오늘 자 49행이 모두 `is_vcp` 참이고 기존 행은 그대로, 메뉴 2 저장 단계 재현 뒤에도 유지, 격리 API·화면에 오늘 자 시그널 20건이 보이고 변경 전 커밋 대조군 로그는 날짜 목록이 비어 화면 0행)
- 갱신한 테스트: `test_scan_today_signals_builds_signal_and_applies_name_map` 에 저장 로그의 `is_vcp` 가 `[True]` 인지 보는 단언을 더했다. 삭제한 검사 없음
- 메모: 전체 pytest 첫 시도는 사본을 `git ls-files | xargs tar` 로 만들다가 `xargs` 가 `tar` 를 나눠 실행해 사본이 불완전했다(받는 쪽은 첫 아카이브만 풂). 그 실행은 무효로 하고 `git archive` 로 다시 만들었다. QA 에서 병합 뒤 `is_vcp` 가 CSV 에 `1.0` 으로 적히는 것을 관찰했다. 현재 소비자는 실수로 읽어 문제없으나 문자열로 읽으면 `safe_bool` 이 거짓으로 본다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [VCP-037] 스캔 단계의 `signals_log.csv` 저장을 없애 메뉴 2 의 저장 질문에서만 쓴다
- 완료 2026-09-23 16:25 | 티어 T2 (`engine/signal_tracker_analysis_mixin.py` 는 위험 경로 밖) | 커밋 f528ab1
- 변경: engine/signal_tracker_analysis_mixin.py, tests/engine/test_signal_tracker_refactor.py (코드 +10 -3)
- 설계: bounded, 2026-09-23 사용자 선택 「VCP-037 (Recommended)」로 승인(항목 선택과 설계안을 한 질문으로 제시). `scan_today_signals` 가 AI 분석 전에 부르던 `_append_to_log` 를 지웠다. 스캔은 결과만 돌려주고 저장은 `run.py` 메뉴 2 에서 y 를 골랐을 때만 한다. 수동 검증 스크립트 `tests/verify_vcp_pipeline.py` 도 로그를 쓰지 않게 되었으며 의도한 결과다. 질문 문구를 실제 동작에 맞추는 안은 N 을 골라도 AI 추천 없는 행이 화면에 남으므로 택하지 않았다
- 리뷰: closing-bet-python · ponytail-review(Lean already, 0건) · closing-bet-reviewer `vcp037-reviewer` APPROVE(max low. 다른 호출자와 문서 서술 없음, 캐시 갱신은 파일을 쓸 때만 필요해 빠진 부수 효과 없음, y 로 저장한 행에 `is_vcp` 유지 / low: `run.py:63` 이 `ai_reason` 을 잘라 저장, 기존 동작 → `[VCP-038]` 등록 / low: `is_vcp` 의 CSV 왕복 단위 검사가 사라짐 → 반영) · closing-bet-verify(격리 사본 하네스·대조군·브라우저)
- 검증: RED 는 스캔 뒤 `signals_log.csv` 존재 단언의 AssertionError → 통과 · `tests/engine` 668 passed · pyflakes 는 HEAD 에도 있던 미사용 import 1건만 · 전체 pytest 는 격리 `git archive` 사본에서 2657 passed 25 failed 8 errors 3 skipped, 실패·오류는 사본에 `frontend/node_modules` 가 없어서 난 Next 실행기·신원 교차 테스트와 gitignore 테스트이며 `node_modules` 를 링크해 세 파일을 다시 돌리자 78 passed 1 failed(gitignore, 원본 트리에서 통과)로 새 실패 0 · QA 3/3 통과(`docs/dev-cycle/qa/VCP-037.md`, 원본 자료 사본으로 실제 스캔해 49행을 돌려주고 로그 sha256 불변, 대조군은 49행 저장. 저장 단계 재현 뒤 AI 열과 `is_vcp` 가 함께 기록. 격리 화면은 스캔만 한 상태에서 0행, 저장한 상태에서 오늘 자 20행)
- 갱신한 테스트: `test_scan_today_signals_builds_signal_and_applies_name_map` 의 저장 로그 단언을 반환 프레임 `is_vcp` 단언과 로그 미생성 단언으로 바꿨다. `test_append_to_log_keeps_both_rows_without_same_day_overlap` 에 `is_vcp` 열 없는 로그와 병합한 뒤 `safe_bool` 판정 단언을 더해, 사라진 CSV 왕복 검사를 대신한다. 삭제한 검사 없음
- 메모: 전체 pytest 사본과 QA 사본에서 `node_modules` 를 심볼릭 링크로 넣으면 Turbopack 이 루트 밖 링크를 거부한다. QA 사본은 APFS 복제로 넣었다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [VCP-038] `run.py` 메뉴 2 가 `ai_reason` 을 출력용 사본에서만 잘라 로그에는 원문을 저장한다
- 완료 2026-09-23 16:50 | 티어 T2 (`run.py` 는 위험 경로 밖) | 커밋 d933bfe
- 변경: run.py, tests/test_run_menu_refactor.py (코드 +58 -4)
- 설계: bounded, 2026-09-23 사용자 선택 「VCP-038 (Recommended)」로 승인(항목 선택과 설계안을 한 질문으로 제시). 메뉴 2 가 출력용으로 `ai_reason` 을 50자 + "..." 로 자르던 것을 `analyzed_df[cols]` 사본 `shown` 에만 적용하고, `_append_to_log(analyzed_df)` 는 분석 결과 원문을 저장한다. 결측 사유는 문자열 "nan..." 대신 결측으로 남아 API 가 그 행의 추천을 싣지 않는다
- 리뷰: closing-bet-python · ponytail-review(`shown` 두 줄을 `assign` 한 줄로 줄이는 shrink 1건, 한 줄이 길어져 미반영) · closing-bet-reviewer `vcp038-reviewer` APPROVE(max low. 저장 프레임의 다른 열 불변, 요구 열 변화 없음, `run` import 의 chdir 은 monkeypatch 가 되돌리고 sys.path 추가는 이미 있는 루트, 긴 사유와 결측을 소비자가 올바로 다룸 / low: 사유 열만 단언 → `assert_frame_equal` 반영 / low: 출력 사본의 NaN 이 "nan..." 으로 보임, 화면 출력만이라 범위 밖) · closing-bet-verify(격리 사본 하네스·대조군·브라우저)
- 검증: RED 는 저장 사유가 50자 + "..." 라 원문과 달라 AssertionError → 통과, 리뷰 반영 뒤에도 옛 `run.py` 에서 `assert_frame_equal` 이 ai_reason 열 차이로 실패 · `tests/test_run_menu_refactor.py`·`tests/engine/test_signal_tracker_refactor.py` 25 passed · pyflakes 는 `run.py` 에 원래 있던 미사용 import·빈 f-string 2건만 · 전체 pytest 는 격리 `git archive` 사본(`frontend/node_modules` 링크)에서 2690 passed 1 failed 3 skipped, 실패 1건은 사본이 git 저장소가 아니라 실패하는 gitignore 테스트로 새 실패 0 · QA 2/2 통과(`docs/dev-cycle/qa/VCP-038.md`, 원본 자료 사본으로 실제 스캔하고 AI 만 가짜로 바꿔 `run.main()` 을 2·y 로 실행. 로그에 89자 원문과 결측이 그대로 남고 대조군은 잘린 문자열과 "nan..." 을 저장. 격리 API·화면은 원문 사유를 보이고 결측 행은 추천을 싣지 않으며, 대조군 화면은 배지 title 에 "nan..." 을 보임)
- 새 테스트: `tests/test_run_menu_refactor.py::test_menu2_saves_untruncated_ai_reason`(입력 2·y·"" 와 가짜 tracker 로 `run.main()` 을 실행해 저장 프레임이 분석 결과와 같은지 본다). 삭제한 검사 없음
- 메모: QA 의 대조군 화면 첫 로드에서 Next 프록시 ECONNRESET 500 이 재현되어 `[INFRA-080]` 에 관찰을 더했다. Next dev 는 `127.0.0.1` 로 열면 `/_next` 자원을 교차 출처로 막으므로 격리 화면은 `localhost` 로 연다. QA 구간의 원본 `data/`·`logs/` 변경 0개

## [INFRA-083] 전체 pytest 가 저장소 `data/`·`logs/` 를 쓰지 않게 하고 세션 끝에 변경을 검사한다
- 완료 2026-09-23 17:28 | 티어 T1 (`tests/` 와 스킬 문서만, 위험 경로 밖) | 커밋 76cc79b
- 변경: tests/conftest.py, tests/app/test_kr_market_route_integration.py, tests/engine/test_collectors_package_contract.py, tests/engine/test_krx_local_cache_helpers_refactor.py (코드 +87 -3), .claude/skills/closing-bet-python/SKILL.md
- 설계: bounded, 2026-09-23 사용자 선택 「INFRA-083 (Recommended)」로 승인(항목 선택과 설계안을 한 질문으로 제시). 구현 중 cwd 이동만으로는 모듈 기준 절대 경로로 원본 쓰기 19개가 남아 다시 질문했고, 사용자가 「테스트 쪽에서 막기 (Recommended)」를 골라 운영 코드는 바꾸지 않고 `tests/` 에서만 막았다. conftest 가 세션 시작 때 cwd 를 세션 임시 디렉터리로 옮기고(수집 단계 import 가 `logs/user_activity.log` 를 원본에 고정하던 것), autouse fixture 가 테스트마다 cwd 를 `tmp_path` 로 옮기며 이미 import 된 모듈의 `BASE_DIR`(수집기 셋)·`__file__`(`engine/market_schedule.py`·`services/paper_trading.py`·`services/kr_market_realtime_price_cache.py`)·`app/routes/common.py` 의 `UPDATE_STATUS_FILE` 을 임시 저장소 루트로 돌린다. 세션 끝에 저장소 `data/`·`logs/` 의 수정 시각을 시작 때와 비교해 바뀌었으면 목록을 출력하고 실패로 끝낸다
- 리뷰: closing-bet-python · ponytail-review(`if before is None` 가드 삭제, 값이 모두 `BASE_DIR` 인 dict → 튜플, 둘 다 반영). T1 이라 코드 리뷰 역할은 부르지 않았다
- 검증: RED 는 세션 끝 검사만 넣은 격리 사본 전체 실행에서 `data/`·`logs/` 20개 변경으로 실패 → 테스트마다 수정 시각을 비교하는 임시 탐지 플러그인(사본에만 두고 삭제)으로 원인 12파일 27건을 찾아 고친 뒤 0개. 원본 트리 첫 실행에서 검사가 `data/runtime_cache.db` 1개를 잡아(원본에 `update_status.json` 이 있어야 타는 경로) `UPDATE_STATUS_FILE` 격리를 더했다. 최종: 원본 트리 `venv/bin/python -m pytest -q -p no:cacheprovider` exit 0, 2692 passed 2 skipped, 전후 `data/`·`logs/` 27,564개 파일 수정 시각 비교 변화 0, 세션 임시 디렉터리 잔존 0 · 원본 `data/`·`logs/` 를 APFS 복제한 사본에서도 변화 0(2690 passed 1 failed, 사본이 git 저장소가 아닌 기존 gitignore 테스트) · `tests/scripts/test_skill_set.py` 6 passed · `git diff --cached --check` 통과. 동적 QA 는 테스트 인프라만 바뀌어 제외
- 새 테스트: 별도 테스트 파일 없이 `tests/conftest.py` 의 세션 끝 검사가 회귀 검사를 맡는다. 삭제한 검사 없음. `test_reanalyze_vcp_failed_ai_filters_failed_rows_and_updates_csv` 는 `VCP_STATUS` 에 대한 `monkeypatch.setitem` 을 뺐다(`setattr` 이 먼저 되돌려진 뒤 `setitem` 복원이 원본 파일에 쓰였다. 새 임시 파일의 기본값이 이미 `running=False`)
- 메모: 개발 중 원본 트리 실행 한 번이 `data/runtime_cache.db` 에 캐시 행을 썼다(검사가 잡은 그 1건). 새 클론처럼 `data/usage.db`·`vcp_status.json` 이 없으면 import 시점에 두 파일이 생겨 검사가 실패하며, 이 점과 목록 밖 모듈 경로는 `[INFRA-082]` 에 추가 관찰로 남겼다. 이제 전체 pytest 를 `git archive` 사본이 아니라 원본 트리에서 돌려도 된다

## [CHAT-035] HistoryManager 의 레거시 스냅샷·읽기 캐시·델타 장부를 부품으로 분리한다
- 완료 2026-09-23 18:02 | 티어 T3 (TODO 기재 티어 유지, `chatbot/storage.py` 는 `tier-rules.md` §2 목록 밖) | 커밋 87c46cb
- 변경: chatbot/storage.py, chatbot/storage_history_parts.py(신규), tests/chatbot/test_history_manager_sync.py, tests/chatbot/test_storage_history_parts_refactor.py(신규) (코드 +352 -190). 설계 문서 `docs/superpowers/specs/2026-09-23-chat-035-history-manager-split-design.md`(커밋 ef98ed2), 구현 계획 `docs/superpowers/plans/2026-09-23-chat-035-history-manager-split.md`
- 설계: architectural. 2026-09-23 사용자 선택 「CHAT-035 (Recommended)」「A. 부품 세 개로 조립 (Recommended)」「테스트의 접근 경로만 고침 (Recommended)」, 채팅 설계 두 절 「승인 (Recommended)」, spec 검토 「승인, 계획 작성 (Recommended)」. 새 모듈에 잠금 없는 `LegacySnapshot`·`HistoryReadCache`·`SessionDeltaLedger` 와 함수 `storage_signature` 를 두고, `HistoryManager` 는 유일한 `RLock` 을 쥔 채로만 부품을 부른다. 외부가 `hasattr` 로 부르는 `_mark_session_changed`·`_invalidate_message_cache`·`_invalidate_session_list_cache`·`_save` 는 이름과 `@locked` 를 유지한 위임으로 남겼다. 호출자가 없던 `_backup_corrupt_history` 는 지웠다. 동작 변화 0 이 목표이며, 캐시 상한 상수를 생성 때 한 번 읽는 것과 서명 stat 실패 로그의 logger 이름이 바뀐 것만 spec 에 예외로 적었다
- 리뷰: closing-bet-python · critic(계획, ACCEPT-WITH-RESERVATIONS: grep 기대값 범위, 계획 파일 스테이징·커밋 트레일러, 상한 상수 시점 기록 반영) · ponytail-review(`is_due` 를 `sync` 에 합침, net -3) · closing-bet-reviewer(APPROVE, low 4 반영: spec 예외 기록·문서 정정·서명 검사 단언 보강) · `/review`(`oh-my-claudecode:code-reviewer`, APPROVE. 종전판과 무작위 연산 120회×8시드 차분이 반환값·사본·JSON·SQLite 전 행에서 동일, 스레드 경합 검사 낡은 캐시 0. L1 세션 목록 docstring 을 얕은 사본으로 정정, L2 도달할 수 없던 목록 버전 삭제, L3 전역 `time` 대신 모듈 참조 패치, L4 wal 단독·stat 오류 서명 검사 추가, import 순서 반영. L4(c)는 매니저 테스트가 덮어 미반영, P1 은 `[CHAT-040]` 으로 등록, P2 는 레거시 테스트 전용 경로라 미등록)
- 검증: RED 는 새 부품 검사가 모듈 없음으로 수집 실패, 접근 경로를 옮긴 여섯 테스트가 `AttributeError` 로 실패 → `tests/chatbot` 271 passed · 원본 트리 `venv/bin/python -m pytest -q -p no:cacheprovider` exit 0, 2701 passed 2 skipped(`[INFRA-083]` 세션 끝 검사 포함) · `git diff --cached --check` 통과 · QA `docs/dev-cycle/qa/CHAT-035.md` 필수 4/4 통과(격리 gunicorn 2 workers 8 threads·Next dev, 가짜 LLM, gstack `browse`: 대화 둘 생성·새로고침, 메시지 하나 삭제, 대화 하나 삭제 뒤 목록·본문·사본 SQLite·JSON 스냅샷 일치, 콘솔·MCP·서버 로그 오류 0). 정리 완료, 원본 `data/`·`logs/` 변화 0. frontend 변경이 없어 type-check·vitest 생략
- 새 테스트: `tests/chatbot/test_storage_history_parts_refactor.py` 9건(장부 상태 전이, 스냅샷 간격 판정, 환경 변수 해석 3, 읽기 캐시 복제·LRU·빈 목록 적중, 서명 셋: JSON·SQLite 선택, wal 단독, stat 오류). 삭제한 검사 없음. `test_history_manager_sync.py` 는 내부 접근 경로 19줄만 바꾸고 기대값은 그대로다
- 메모: 계획 문서의 코드 블록은 critic 검토 시점의 판이다. 이후 리뷰로 `is_due` 합침은 계획에도 반영했지만, 목록 버전 삭제와 서명 검사 두 건 추가는 spec·코드에만 있다. QA 중 Next 텔레메트리 전송 프로세스가 사본 경로에 남아 따로 종료했다

## [VCP-035] `signals_log.csv` 의 읽기·병합·교체 사이에 잠금이 없다
- 완료 2026-09-23 19:58 | 티어 T3 (TODO 기재 T2 에서 상향. `scripts/init_data.py` 가 `tier-rules.md` §2 「스케줄러와 데이터 적재」) | 커밋 25036ff
- 변경: scripts/init_data.py, services/kr_market_vcp_reanalysis_service.py, engine/signal_tracker_analysis_mixin.py, services/common_env_service.py(docstring 두 줄), tests/services/test_vcp_signals_log_lock_refactor.py(신규), tests/engine/test_signal_tracker_refactor.py, tests/scripts/test_init_data_vcp_scheduler.py (코드 +367 -123, 대부분 잠금 블록 안으로 들여쓴 줄). 구현 계획 `docs/superpowers/plans/2026-09-23-vcp-035-signals-log-lock.md`
- 설계: bounded(T3 라 계획 문서 동반). 2026-09-23 사용자 선택 「VCP-035 (Recommended)」, 설계 「승인 (Recommended)」, 재분석·시세 갱신 범위 수정 「승인 (Recommended)」. 동시 실행 확인: 17시 스케줄러, 관리자 Refresh VCP·데이터 갱신·실패 AI 재분석, `run.py` 메뉴 2 가 서로를 배타하지 않고 워커가 둘이다. `signals_log.csv` 를 쓰는 다섯 경로에 `fcntl` 잠금 하나(`services/common_env_service.py` 의 `_env_file_lock` 을 `signals_log_lock` 별칭으로 재사용)를 건다. 생성·열린 시그널 갱신·`_append_to_log` 는 읽기부터 교체까지 잠그고, 시세 갱신과 재분석은 느린 작업을 잠금 밖에서 한 뒤 잠금 안에서 다시 읽어 병합·교체한다. 재분석은 실제로 갱신한 `apply_rows` 의 행만 병합하고, 그 행의 `ticker`·`signal_date` 가 다르면 덮지 않고 오류로 끝낸다. 시세 갱신의 비원자적 `to_csv` 를 원자적 쓰기로 바꿨다. `create_signals_log` 는 잠금 파일을 열지 못해도 False 계약을 지킨다
- 리뷰: closing-bet-python · critic(계획, ACCEPT-WITH-RESERVATIONS: R1 테스트 열 확정, R2 daemon 스레드, R3 표지 행 단언, R5 서술 정정 반영, R4 는 오류 문구에 재실행 안내) · ponytail-review(자체 잠금 도우미를 `_env_file_lock` 재사용으로 대체, 겹치는 테스트 삭제, net -26) · closing-bet-reviewer(APPROVE, max low: L3 docstring·L4 `update_open_signals` 대기 검사·L6 계획 메모 반영, L1 파일 모드 0600·L2 다른 계정 CLI 의 EACCES·L5 영어 오류 문구는 기록) · `/review`(`oh-my-claudecode:code-reviewer` opus, 1차 CHANGES REQUESTED → M1 재분석 병합이 비대상 행을 옛 스냅샷으로 되돌리는 문제를 `target_indexes` 로 반영, L1 예외 갈래 잠금 실패를 `try/except OSError` 로 반영, L2 재분석 잠금 대기 검사는 미반영 → 재검토 APPROVE)
- 검증: RED 는 잠금 도우미만 둔 상태에서 신규 경로 검사 4건이 의도한 이유로 실패, M1 테스트는 TypeError, L1 테스트와 mixin 잠금 두 곳은 변이로 실패 확인 → 신규 8 통과 · 원본 트리 `pytest -q -p no:cacheprovider` exit 0, 2709 passed 2 skipped(`[INFRA-083]` 세션 끝 검사 포함) · `git diff --cached --check` 통과 · QA `docs/dev-cycle/qa/VCP-035.md` 필수 4/4 통과(서비스 하네스, 화면 경로는 전부 LLM 비용 조작이라 정책상 차단: 프로세스 네 개 동시 추가 10회 모두 100행 유지(기준 28~46), 시세 갱신 중 추가 5회 모두 25행 유지(기준 10~15), 시세 갱신 저장 바이트 변경 전과 동일). 정리 완료, 원본 `data/`·`logs/` 변화 0. frontend 변경이 없어 type-check·vitest 생략
- 새 테스트: `tests/services/test_vcp_signals_log_lock_refactor.py` 8건(재분석 행 불일치 거부·일치 시 병합·비대상 행 보존, 생성·`_append_to_log`·`update_open_signals` 잠금 대기와 표지 행 보존, 시세 갱신 재읽기, 잠금 파일을 열지 못할 때 False). 기존 세 단언은 원자적 쓰기 실패 뒤 임시 파일 검사에서 새 잠금 파일 `signals_log.csv.lock` 만 제외했다. 삭제한 검사 없음(ponytail 지적으로 지운 잠금 배타 단독 검사는 커밋 전 초안이었다)
- 이월: `[VCP-039]`(취소한 재분석이 아직 돌지 않은 행에 실패 값을 기록한다, 심층 리뷰 참고 사항)
- 메모: 계획 문서의 Task 1 코드 블록은 자체 도우미 판이다. 재사용으로 바꾼 사실은 계획 상단 「구현 메모」에 있다

## [VCP-039] 취소한 재분석이 아직 돌지 않은 행에 실패 값을 기록한다
- 완료 2026-09-23 20:28 | 티어 T2 (위험 경로 밖, 코드 +16 -1) | 커밋 b89d577
- 변경: services/kr_market_vcp_reanalysis_service.py, tests/services/test_vcp_reanalysis_cancel_refactor.py(신규) (코드 +85 -1)
- 설계: bounded. 2026-09-23 사용자 선택 「VCP-039 (Recommended)」, 설계 「승인 (Recommended)」(20:20 확인). 재현 결과 TODO 기재보다 넓었다. 다른 실행이 끼지 않아도 취소 시점에 닿지 않은 행의 기존 정상 AI 값이 `분석 실패` 로 지워지고 실패로 집계되었다. `execute_vcp_failed_ai_reanalysis` 가 진행 콜백을 감싸 배치 루프가 처리한 티커를 모으고(루프는 예외 종목도 `finally` 에서 콜백을 부른다), 취소된 경우에만 `apply_rows`·`second_only_rows` 를 그 티커로 좁힌다. 배치 헬퍼 시그니처·취소하지 않은 경로·응답 형식은 그대로다
- 리뷰: closing-bet-python · ponytail-review(oh-my-claudecode:code-reviewer 레인, 한 번만 쓰는 중첩 함수를 컴프리헨션으로, net -2 반영) · closing-bet-reviewer(APPROVE, max low: 판정이 티커 단위라는 점은 주석으로 명시, `second_only_rows` 갈래 테스트 부재는 `force_provider="second"` 테스트 추가로 반영. 첫 종목 전·마지막 종목 뒤 취소, 콜백 예외, 처리 0행의 캐시 경로를 탐침으로 확인)
- 검증: RED 는 신규 테스트가 `assert (1, 2) == (1, 1)` 로 실패, `second_only_rows` 필터를 지우는 변이로 second 테스트 실패 확인 → 신규 2 통과 · 원본 트리 `pytest -q` exit 0, 2711 passed 2 skipped · `git diff --cached --check` 통과 · QA `docs/dev-cycle/qa/VCP-039.md` 필수 5/5 통과(서비스 하네스, 화면 경로는 LLM 비용 조작이라 정책상 차단: 첫 종목 뒤 취소 시 미처리 두 행 기존 값 유지(기준은 `분석 실패`로 덮음), 처리 후 실패 행은 실패 기록, 첫 종목 전 취소 시 변화 없음, 취소하지 않은 경로의 결과 CSV 바이트 동일). 정리 완료, 원본 `data/`·`logs/` 변화 0. frontend 변경이 없어 type-check·vitest 생략
- 새 테스트: `tests/services/test_vcp_reanalysis_cancel_refactor.py` 2건(gemini 강제 재분석 취소 시 미처리 행 보존·처리 후 실패 행 기록·집계, second 강제 재분석 취소 시 집계). 삭제한 검사 없음
- 이월: 없음. 처리 0행이어도 CSV 를 잠금 안에서 전체 재기록하는 동작은 종전부터 있던 것으로 값이 바뀌지 않아 등록하지 않았다

## [CHAT-040] 스냅샷 쓰기 실패가 이미 저장된 대화 삭제를 실패로 보고한다
- 완료 2026-09-23 20:53 | 티어 T2 (위험 경로 밖, 코드 +17 -6) | 커밋 2fed790
- 변경: chatbot/storage.py, tests/chatbot/test_history_snapshot_failure_refactor.py(신규) (코드 +79 -6)
- 설계: bounded. 2026-09-23 사용자 선택 「CHAT-040 (Recommended)」, 설계 「승인 (Recommended)」(20:40 확인). 테스트 작성 전 재현에서 그 소유자의 세션이 유일해 SQLite 가 비면 `_load` 가 낡은 JSON 을 다시 이관해 지운 대화가 되살아남을 확인했고(종전 동작에서도 동일), 범위 변경 「추가해 진행 (Recommended)」(20:42 확인)을 받았다. `_save` 가 스냅샷 쓰기의 `OSError` 만 따로 잡아 로그를 남기고 `last_monotonic` 을 비운다. 반환·서명·장부 초기화는 SQLite 결과로 정한다. 삭제가 든 저장이면 낡은 JSON 을 `unlink` 하고, 그것도 실패하면 반환만 False 로 두고 장부 초기화·서명 None 경로를 탄다. `_load` 의 강제 스냅샷은 범위 밖
- 리뷰: closing-bet-python · ponytail-review(oh-my-claudecode:code-reviewer 레인, 「Lean already. Ship.」, OSError 밖 직렬화 예외는 승인 범위 밖이라 미반영) · closing-bet-reviewer(APPROVE, max low, `_save` 변이 다섯 판 모두 새 테스트가 잡음. low1 unlink 실패 갈래의 남은 장부가 되살아난 세션을 나중에 몰래 지워 SQLite 와 메모리·JSON 이 어긋남·정보4 암묵 반환은 반영, low2·low3 은 이월)
- 검증: RED 는 신규 3건 가운데 2건 실패(삭제가 RuntimeError·되살아남, 삭제 없는 저장이 False) → 신규 3 통과 · `tests/chatbot` 274 passed · 원본 트리 `pytest -q -p no:cacheprovider` exit 0, 2714 passed 2 skipped(리뷰 반영 뒤 최종 코드) · `git diff --cached --check` 통과 · QA `docs/dev-cycle/qa/CHAT-040.md` 필수 5/5 통과(서비스 하네스, 화면 경로는 되돌릴 수 없는 삭제라 정책상 차단: 스냅샷 실패 시 삭제 200·되살아나지 않음(기준은 500·되살아남), JSON 삭제까지 실패 시 500 후 복구 재시도로 삭제, 삭제 없는 저장은 True(기준 False), 장애 없는 경로 응답 동일). 정리 완료, 원본 `data/`·`logs/` 변화 0. frontend 변경이 없어 type-check·vitest 생략
- 새 테스트: `tests/chatbot/test_history_snapshot_failure_refactor.py` 3건(스냅샷 실패에도 삭제 성공·되살아나지 않음·다음 저장이 사본 재작성, JSON 삭제 실패 시 RuntimeError·장부·서명, 삭제 없는 저장 성공·사본 유지). 삭제한 검사 없음
- 이월: `[CHAT-041]` 재시도가 0 을 돌려준 뒤에도 낡은 JSON 에 지운 대화가 남는 창과 워커 사이 스냅샷 경합(둘 다 종전 동작)

## [CHAT-041] 삭제한 대화가 레거시 JSON 스냅샷에 남는 창
- 완료 2026-09-23 21:12 | 티어 T2 (위험 경로 밖, 코드 +41 -12) | 커밋 a3bdbee
- 변경: chatbot/storage.py, chatbot/storage_history_parts.py, tests/chatbot/test_history_snapshot_failure_refactor.py, tests/chatbot/test_storage_history_parts_refactor.py (코드 +146 -18)
- 설계: bounded. 2026-09-23 사용자 선택 「CHAT-041 (Recommended)」, 설계 「세 개 모두 승인 (Recommended)」(20:59 확인). (1) `clear_for_owner` 가 대상 수와 무관하게 `_save(force_snapshot=True)` 로 사본을 다시 쓰고 실패하면 CHAT-040 의 unlink 경로를 탄다(재적재 실패 상태의 0 건도 RuntimeError). (2) `LegacySnapshot.sync` 가 메모리 사본 대신 콜백으로 SQLite 를 다시 읽어(`_read_sqlite_sessions`, [CHAT-039] 복구를 거침) 쓰고, 그 구간을 `chatbot_history.json.lock` 의 `fcntl.flock` 으로 워커 사이 직렬화한다(`storage_memory_manager.py` 와 같은 관용구). SQLite 저장 실패 때 미저장 변경은 JSON 에 남지 않는다. (3) `_load` 의 강제 스냅샷 OSError 를 로그로 흡수하고 `last_monotonic` 을 비운다
- 리뷰: closing-bet-python · ponytail-review(oh-my-claudecode:code-reviewer 레인, shrink 2 반영) · closing-bet-reviewer 1차(APPROVE, max low, 4건 반영: 문구와 `force=has_deletion`, flock 직렬화 테스트, 스냅샷 읽기의 [CHAT-039] 복구, 재적재 실패 0 건·`last_monotonic` 단언) · 재검토(APPROVE, max low 1: 정본 읽기의 복구 쓰기 실패가 삭제와 겹치면 unlink 가 복구 원천을 지움 → 재적재와 저장 사이 창에서만 생겨 `ponytail:` 주석으로 한계 표시, 미수정)
- 검증: RED 는 신규 3건 모두 실패(AttributeError, 지운 소유자가 JSON 에 남음, OSError 28) → 통과 · 변이 검사(flock no-op, 복구 제거, force_snapshot 제거) 각각 신규 테스트가 실패 · `tests/chatbot` 280 passed · 원본 트리 `pytest -q -p no:cacheprovider` exit 0, 2720 passed 2 skipped(최종 코드, 이후 주석 한 줄만 추가하고 `tests/chatbot` 재실행) · `git diff --cached --check` 통과 · QA `docs/dev-cycle/qa/CHAT-041.md` 필수 6/6 통과(서비스 하네스, 화면 경로는 되돌릴 수 없는 삭제라 정책상 차단: 다른 워커의 재시도가 사본 정리(기준은 alice 잔존), 낡은 메모리가 지운 세션을 되살리지 않음(기준은 되살아남), 기동 시 사본 실패에도 생성(기준 OSError), 겹친 쓰기 직렬화, 장애 없는 경로 응답 동일). 정리 완료, 원본 `data/`·`logs/` 변화 0. frontend 변경이 없어 type-check·vitest 생략
- 새 테스트: `test_history_snapshot_failure_refactor.py` 5건(재시도가 사본 정리, 다른 워커의 낡은 메모리, 기동 시 사본 실패, 재적재 실패 0 건 RuntimeError, 메시지 표가 빈 채 복구된 DB 로 사본을 덮지 않음), `test_storage_history_parts_refactor.py` 1건(쓰기 직렬화). 기존 `sync` 테스트는 콜백 인자로 바꿨다. 삭제한 검사 없음
- 이월: 없음. 범위 밖 관찰(`delete_message` 가 사본을 강제로 쓰지 않아 지운 메시지가 최대 15초 JSON 에 남음, 종전 동작)은 기록만 남긴다

## [INFRA-074] 재시작 충돌·로그인 실패·반복 설치 로그 보완
- 완료 2026-09-23 21:25 | 티어 T3 (재시작·중지 수명주기와 KRX 인증 경계, 코드 +1745 -180) | 커밋 6569c60, QA 기록 1a05aeb
- 변경: restart_all.sh, stop_all.sh, scripts/service_lifecycle.sh, scripts/sync_dependencies.sh, requirements.txt, vendor/pykrx/(rebuild.py, transport.patch, README.md, cookie.2 wheel), tests/scripts/test_{service_lifecycle,lifecycle_adversarial,sync_dependencies,next_environment}.py, tests/test_pykrx_login_guard.py, tests/fixtures/pykrx_login_guard_probe.py, tests/test_restart_cleanup_contract.py, AGENTS.md·CLAUDE.md·README.md
- 설계: bounded. 2026-09-21 사용자 「확인 된 부분들 모두 보완하고 … 의존성 … 로그 … 보완해줘」와 자율 진행 위임. 공유 flock 잠금·PID 소유권 재검증·HTTP 준비 확인으로 거짓 성공과 중복 기동을 막고, 정상 의존성 출력을 축약하며, KRX 로그인의 HTTP 오류·비JSON·리다이렉트를 인증 실패로 처리한다. 원격 운영 서버는 변경하지 않음
- 리뷰: 과잉설계 2건 반영 · code APPROVE · architect CLEAR · deep ACCEPT · security APPROVE(`docs/dev-cycle/evidence/INFRA-074/*-review.md`)
- 검증: iteration 2(2026-09-22) fresh venv 전체 pytest 2527 passed 2 skipped, 수명주기 적대적 17 passed, 관련 81 passed, Vitest 641, 실제 시작·중지·재시작과 KRX HTML 주입 import. 그때 B1(스크린샷 3회 실패)과 C1(하위 에이전트가 원본 트리에서 전체 pytest 실행)이 미통과여서 이월. iteration 3(2026-09-23, 기준 727b2e0) 재시도: 격리 사본 `restart_all.sh` exit 0 → `/dashboard/kr` 스크린샷에서 Market Gate 55 Neutral 이 API 와 일치, `/api/` 13건 200, 콘솔·Next 오류 0(B1 통과). 원본 27,572개 파일 해시 전후 동일, 새 파일은 무시된 `.gstack/` 도구 로그뿐, `git status` 0줄(C1 통과). 보강으로 떠 있는 상태 재시작 9초·중지 3초 exit 0, 사본 `tests/scripts`+KRX 159 passed(1 failed 는 사본에 `.git` 이 없는 환경 차이). QA `docs/dev-cycle/qa/INFRA-074.md` 필수 9/9. 정리 완료
- 새 테스트: 수명주기·적대적·의존성·KRX 로그인 가드(위 변경 목록). 삭제한 검사 없음
- 이월: 없음. iteration 2 의 경계 위반 사건 기록은 QA 문서에 그대로 보존

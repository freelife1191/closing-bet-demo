# 2026-09-23

## [FE-045] 서버에 저장한 개인정보를 사용자가 실제로 지울 수 있게 한다
- 완료 2026-09-23 00:35 | 티어 T3 | 커밋 8bfd76e
- 변경: app/routes/kr_market_user_data_routes.py (+82, 신규 `DELETE /api/kr/user/data`, 단계별 예외 수집), chatbot/storage.py (+10 -3,
  `_save()` 가 `bool`, `clear_for_owner` 는 강제 재적재 뒤 저장 실패에 `RuntimeError`), frontend/src/app/components/SettingsModal.tsx
  (+39 -13, 서버 삭제 선행·`isDeleting`·`signOut` 실패 안내·문구), app/routes/kr_market.py (+31, `delete_user_usage` 래퍼와 등록) 외 5개
  (services/kr_market_quota_runtime_service.py +29, services/paper_trading_trade_account_mixin.py +22 -3, chatbot/storage_memory_manager.py
  +15, chatbot/command_service.py +8 -1, (legal)/privacy/page.tsx +4 -4), tests 7개 (+354), docs 3개 (계획 +927, qa/FE-045.md +111, TODO
  +19 -2). 코드 합계 +240 -24, 검사 +354
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「INFRA-077 승인, 끝나면 FE-045」) · closing-bet-python·closing-bet-nextjs·
  closing-bet-verify(위험 경로 grep 대조로 `paper_trading_trade_account_mixin.py` → T3, `fetchAPI` 재사용, 격리 사본·가짜 클라이언트·
  비관리자 세션 토큰) · frontend-skills §2 + next 번들 문서 05-server-and-client-components(모달은 순수 클라이언트 변경, 바꾼 판단 없음)
  ·03-layouts-and-pages(방침 페이지는 서버 컴포넌트 문구만, 바꾼 판단 없음) · superpowers:writing-plans(계획 7 과제) ·
  oh-my-claudecode:critic `fe045-critic`(REVISE: getter 선평가로 부분 실패 설계가 깨짐 → 단계 안 호출·실패 검사, 다중 워커 서술
  정정 → `[CHAT-033]` 범위 추가, `signOut` 까지 감싼 `try`, `api_usage` 유물 추가 → `[INFRA-079]`, `GET /api/portfolio` 의 잔고
  재생성 → QA 기대값 완화, 세션 0건 검사, 경미 4·관찰 3 반영, m5 주입 제거는 미반영) · ponytail-review(자체 검토, net -2:
  중복 실패 로그) · code-review(closing-bet-reviewer `fe045-reviewer`, 1차 CHANGES_REQUIRED → 2차 APPROVE. medium: `_save()` 가
  저장 실패를 삼켜 세션 삭제가 커밋되지 않아도 200 → `bool` 반환·강제 재적재·`RuntimeError`·`/clear all` 경고, low: 스냅샷
  실패 무시·실패 문구·계획 S-3 문구) · oh-my-claudecode:security-reviewer `fe045-security`(1차 CHANGES_REQUIRED → 2차 APPROVE.
  같은 medium 둘, 로그인 판정이 좁아 만료 순간 서버 호출 생략 → 익명 분기 삭제로 항상 서버 삭제 선행, 다른 기기 세션의 잔고
  재생성 → 확인 대화 안내, 정보: 레이트 리밋 부재·활동 로그 기록) · /review(oh-my-claudecode:critic `fe045-deep`, scratchpad
  재현 포함, ACCEPT-WITH-RESERVATIONS: 요청 중 버튼 비활성화·「삭제 중…」, `signOut` 실패 안내, Danger Zone 문구 「계정 삭제」.
  스냅샷 예외의 안전한 500 은 유지) · qa-only → qa(격리 사본 `qa_app.py` 가짜 Gemini, 비관리자 세션 토큰, browse, 필수 5/5)
- 검증: pytest RED 12(9 계획 + 3 리뷰 반영) → GREEN · pytest 전체 2627 통과 2 skipped(exit 0) · vitest RED 2 → GREEN, 전체
  91 파일 673 통과(자정 경계에서 `[JONGGA-037]` 시간 의존 검사 1건이 잠시 실패했으나 이 항목과 무관, 재실행 통과) · tsc 통과 ·
  lint 오류 0(경고 184 전부 기존. 중간의 `window.location.assign` 경고는 모달 안내로 바꿔 해소) · QA 5/5 통과(S-1 대화·프로필·
  매수 fixture 뒤 삭제 → `DELETE` 200 `{"deleted":{"chat_memories":true,"chat_sessions":1,"paper_trading":true,"usage":true}}`,
  로그아웃과 `/` 이동, 세 저장소 그 이메일 행 0·고아 메시지 0·사용량 키 없음, 콘솔 오류 0. S-2 재로그인 시 대화 0·기본
  프로필·사용량 0/10·잔고 초기값. S-3 익명 모달에 삭제 진입점 없음·DELETE 0건. S-4 신원 없음·위조 헤더 401. S-5 정적 전체.
  `/_next/mcp` 이슈 0·오류 0)
- 메모: 무료 사용량은 `usage.db` 가 아니라 `data/user_quota.json`(`kr_market.py` 의 `QUOTA_FILE`, 이메일 키와 `recharge_day:` 키)에
  있다. `services/usage_tracker.py`(`usage_log`)와 `engine/services/usage_tracker.py`(`api_usage`)는 운영 코드가 부르지 않는 유물이며
  로컬 `data/usage.db` 는 두 테이블 모두 행 0, 운영 서버 확인은 `[INFRA-079]`. 세 저장소는 한 트랜잭션으로 묶이지 않아 앞이 실패해도
  뒤를 계속 지우고 500 으로 알리며 재시도는 멱등이다. `HistoryManager._save()` 는 이제 성패를 돌려주고 다른 호출자는 값을 쓰지
  않는다. `clear_for_owner` 의 강제 재적재는 호출당 비용을 늘리고 스레드 경합 창의 빈도를 조금 넓힌다(`[CHAT-033]` 범위에 기록).
  저장 실패 뒤 그 워커는 다음 재적재 전까지 빈 대화 목록을 보이지만 SQLite 에는 남아 있고 재시도로 해소된다. SQLite 삭제가
  커밋된 뒤 레거시 스냅샷 쓰기가 예외를 던지면 500 이 나가지만 안전한 방향이라 그대로 둔다. `GET /api/portfolio` 는 잔고 행을
  다시 만들고 자산 이력을 기록하므로(`CLAUDE.md` Important Notes 4) 다른 기기의 남은 세션이 빈 계좌를 되살릴 수 있고, 확인
  대화가 그 기기의 로그아웃을 안내한다. 이 `DELETE` 자체는 활동 로그에 이메일과 함께 남으며 `[FE-046]` 범위다. `useQuota` 도
  `fetchAPI` 를 쓰므로 모달 검사는 URL 로 응답을 가른다. 두 진입점(프로필 탭 「계정 삭제」는 로그인, Danger Zone 은 관리자)
  모두 익명에게 보이지 않아 익명 분기는 두지 않았다. 계획 초판의 「익명은 브라우저 저장소만 비움」 S-3 은 QA 문서에서 「익명
  진입점 없음」으로 바뀌었다. 레이트 리밋 부재는 저장소 전체의 성질이다.

## [FLOW-016] 누적성과 표의 순번을 전체 기준으로 매긴다
- 완료 2026-09-23 07:58 | 티어 T1 | 커밋 8b0534f
- 변경: frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, CumulativeClientPage.regression-flow-016.test.tsx (+101 -7)
- 리뷰: closing-bet-nextjs(`fetchAPI`·신원 경계와 무관한 표시 계산임을 확인, 해당 없음) · frontend-skills §2(`useEffect`·`useMemo`·프롭 과다가 없어 vendor 스킬 해당 없음) · Next 번들 문서 05-server-and-client-components(순수 클라이언트 상태 변경, 해당 없음) · ponytail-review(Lean already, 지적 0건) · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: vitest 92파일 676건 통과(RED 2건 → GREEN) · type-check exit 0 · 변경 파일 eslint 오류 0 경고 1(기존 `any`, 2건에서 1건으로 감소) · QA 시나리오 5/5 통과
- 메모: 순번은 서버 `pagination` 의 `total - (page - 1) * limit - idx` 이며 필터 적용 전에 매긴다. 설계 승인에서 필터 중에도 원래 번호를 유지해 건너뛰게 두기로 정했다. `[FLOW-017]` 이 필터를 서버로 옮기면 `pagination.total` 이 필터 결과 건수가 되어 번호가 「필터 결과 안의 순번」으로 바뀐다. 그때 이 결정을 다시 볼 것

## [FLOW-017] 결과·등급 필터를 전체 기간에 건다
- 완료 2026-09-23 08:18 | 티어 T2 | 커밋 7b808db
- 변경: app/routes/kr_market_data_ai_routes.py, frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, tests/app/test_kr_market_data_ai_routes_refactor.py, CumulativeClientPage.regression-flow-017.test.tsx(신규), regression-005·regression-performance-batch(계약 교체), regression-flow-016(삭제) (코드 +264 -168)
- 리뷰: closing-bet-python(라우트는 HTTP 변환과 가드만, 입력 허용 목록 검증) · closing-bet-nextjs(신원·환경 변수 경계와 무관, 기존 맨 `fetch` 한 곳의 URL 만 바꿈) · frontend-skills §2 와 Next 번들 문서 05-server-and-client-components(상태·클릭 핸들러만 바뀌는 순수 클라이언트 변경) · ponytail-review(Lean already, 지적 0건) · closing-bet-reviewer `flow017-reviewer` APPROVE(최고 낮음, L3·L5·L6 반영, L1 은 설계대로, L2 는 `[FLOW-021]` 로 이월, L4 는 `[FLOW-019]` 에 연결) · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: 라우트 pytest RED 3건 → GREEN, 화면 vitest RED 3건 → GREEN · 전체 pytest 2630 통과 2 skip · 전체 vitest 92파일 674건 통과 · type-check exit 0 · 변경 디렉터리 eslint exit 0 · 리뷰 반영 뒤 범위 pytest 7·vitest 14·type-check·lint 재통과 · QA 시나리오 6/6 통과
- 삭제한 검사: `regression-flow-016` 은 클라이언트의 `total - (page - 1) * limit - idx` 순번 계산과 pagination 부재 시 배열 기준 대체를 검사했다. 그 코드가 사라졌고 번호 규칙은 라우트 pytest(`..._keeps_original_numbers`), 서버 `no` 표시는 flow-017 이 검사한다. `regression-performance-batch` 의 「현재 페이지 내」 표시와 「필터가 다시 조회하지 않음」 두 건은 이 항목이 뒤집은 계약이라 지웠고, `regression-005` 의 칩 기준은 서버 counts 로 바꿨다
- 메모: `[FLOW-016]` 메모가 예고한 번호 의미 변화는 설계 승인에서 「원래 번호 유지」로 정해, 서버가 필터 전 전체 목록 기준 `no` 를 붙인다. 버튼 건수는 필터와 무관한 전체 건수라 두 필터를 함께 켜면 칩과 표 total 이 다르다(승인된 설계). QA 중 원본 `data/runtime_cache.db` 수정 시각이 바뀐 것은 `[INFRA-078]` 경로

## [FLOW-019] 세 화면의 모집단 규칙을 맞추고 화면에 적는다
- 완료 2026-09-23 08:55 | 티어 T2 | 커밋 9506f23
- 변경: app/routes/kr_market_data_ai_routes.py, services/kr_market_analytics_service.py, frontend/src/app/dashboard/kr/closing-bet/page.tsx, frontend/src/app/dashboard/kr/cumulative/CumulativeClientPage.tsx, frontend/src/app/dashboard/kr/page.tsx, tests/app/test_kr_market_data_ai_routes_refactor.py, closing-bet/page.regression-flow-019.test.tsx(신규), CumulativeClientPage.regression-flow-019.test.tsx(신규) (코드 +182 -10)
- 설계: 현행 모집단 유지(누적성과·백테스트 요약은 D 포함, 종가베팅 화면은 D 를 목록과 일괄 매수에서 제외)와 30개 상한 유지를 사용자가 골랐다. 규칙은 `services/kr_market_analytics_service.py` 의 `JONGGA_SUMMARY_HISTORY_LIMIT` 주석 한 곳에 적고 세 화면에 문구로 드러냈다. 누적성과 라우트는 등급 집합 밖 건수를 `counts.other` 로 보내 칩 합이 전체와 맞게 했다(`[FLOW-017]` 리뷰 L4, 감사 §5.2). 종가베팅 목록의 D 판정을 일괄 매수와 같은 대소문자 무시로 맞췄다
- 리뷰: closing-bet-python(라우트는 HTTP 변환, 규칙은 services 에) · closing-bet-nextjs(신원·환경 변수 경계와 무관, 문구·조건부 렌더링만) · frontend-skills §2 와 Next 번들 문서 05-server-and-client-components · ponytail-review 1건 반영 · closing-bet-reviewer `flow019-reviewer` 1차 CHANGES_REQUIRED(MAJOR 2: 「30거래일」은 실제로 파일 30개, 「D 는 현행 판정기가 내지 않는 과거 등급」은 틀림) → 반영 뒤 APPROVE, 남은 MINOR(D 카드 설명) 반영, 소문자 d 처리 MINOR 는 근거를 들어 미반영 · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: 라우트 pytest RED 2건 → GREEN, 화면 vitest RED 3건 → GREEN · 전체 pytest 2631 통과 2 skip · 전체 vitest 94파일 678건 통과 · type-check·eslint exit 0 · 리뷰 반영 뒤 dashboard/kr vitest 50·cumulative 16·type-check·lint 재통과 · QA 시나리오 5/5 통과(S-5 는 2회차)
- 정정(감사 전제): AUDIT-FLOW(2차) §2.2 와 TODO 근거의 「현행 판정기는 S·A·B 만 내므로 D 는 2월 자료에만 있는 과거 등급」은 부분적으로만 맞다. `engine/grade_decider.py` 는 미달이면 None 을 돌려주지만, 읽기 경로의 재판정(`app/routes/kr_market_jongga_grade_helpers.py:138-146`)이 그 None 에 D 를 붙이고, 최근 유효 자료 경로(`services/kr_market_jongga_payload_helpers.py:468`)는 그 결과를 결과 파일에 되쓴다. 따라서 새 D 가 생길 수 있고 화면 문구는 「현행 기준 미달 또는 과거 등급」으로 적었다
- 메모: 요약과 누적성과의 모집단은 결과 파일이 30개를 넘기 전까지 같다(지금 18개, 둘 다 234건·승률 38.1%). 카드 문구의 30 은 백엔드 상수와 따로 적혀 있어 상수 주석에 함께 바꾸라고 적었다. QA S-5 1회차의 차트 요청 ECONNRESET 500 은 변경 범위 밖이고 재현되지 않아 `[INFRA-080]` 으로 이월했다. QA 중 원본 `data/runtime_cache.db` 수정 시각이 바뀐 것은 `[INFRA-078]` 경로

## [CHAT-032] 종목 질의 문맥이 언제나 비는 경로 복구
- 완료 2026-09-23 09:10 | 티어 T3 (계획 T2, 구현 diff 가 300줄을 넘어 상향. 위험 경로 파일 없음) | 커밋 55919c1
- 변경: chatbot/core_data_access_mixin.py, chatbot/stock_query_service.py, chatbot/payload_service.py 외 9개(chatbot 12개), tests/chatbot 11개(test_core_data_access_mixin.py 신규), tests/app/test_kr_market_route_integration.py (코드 +91 -486)
- 설계: 사용자가 「죽은 경로 제거」를 골랐다. `_detect_stock_query` 를 전체 종목 맵(`detect_stock_query_from_stock_map`)에 연결해 종목명·티커 질의와 VCP 상담 모드의 `[종목명(티커)]` 접두가 최근 5일 주가·수급·VCP 시그널 이력을 싣게 했다(접두는 6자리 티커 정규식이 그대로 잡아 별도 파싱 없음). `get_chatbot()` 이 `data_fetcher` 를 넘기지 않아 늘 `fetch_mock_data()` 의 빈 `vcp_stocks` 만 보던 경로 전체를 지웠다: 데이터 캐시와 mock, vcp 감지·포맷 함수, `fallback_response`, `vcp_data` 인자 사슬(`collect_market_context` 는 값 3개 반환), 프롬프트의 「## VCP 상위 종목」 절, 웰컴 Top 3, 관심종목 VCP 요약과 상세의 「VCP 상태: 현재 시그널 데이터 없음」 줄, 그 캐시를 비우는 일만 하던 챗봇 `/refresh` 명령(프론트 명령 목록에는 원래 없음, 이제 「알 수 없는 명령어」). 두 글자 이하 종목명은 한글·영숫자 경계에서만 잡고 조사 한 글자를 허용한다. 티커 정규식은 `(?<!\d)\d{6}(?!\d)` 로 바꿔 「005930은」도 잡는다
- 리뷰: closing-bet-python(chatbot 믹스인이 서비스 `*_impl` 로 위임하는 기존 구조를 유지, 테스트는 `tmp_path`·가짜 객체로 원본 `data/` 에 닿지 않음) · ponytail-review(`chat032-ponytail`, oh-my-claudecode:code-reviewer 레인) 2건 중 `payload_service` 의 getattr 방어 제거 반영, 모듈 인라인은 기존 위임 구조라 미반영 · closing-bet-reviewer `chat032-code-review` APPROVE(T3 상향 조건 반영, 짧은 종목명 부분 문자열 오탐을 경계 규칙으로 반영, 맵에 없는 티커 갈래 테스트 추가, 기록용·토큰 중복·참고 덤프 문서 3건 미반영) · oh-my-claudecode:critic `chat032-deep-review`(T3 심층) REVISE → 경계 반영 뒤 ACCEPT-WITH-RESERVATIONS(조사 붙은 티커 반영, 가장 긴 이름 하나만 싣는 한계에 `ponytail:` 표시, 유보 사항은 `[CHAT-038]`) · closing-bet-verify(격리 사본에서 브라우저와 `/_next/mcp` 두 관점, 가짜 genai 클라이언트가 모델이 받은 문맥을 되돌려 보여 줌)
- 검증: `test_core_data_access_mixin.py` RED(옛 경로가 `_get_cached_data` 로 빠짐) → GREEN · 전체 `pytest -q -p no:cacheprovider` 2622 통과 2 skip exit 0(리뷰 반영 전후 두 번) · frontend 변경 없음이라 type-check·vitest 생략 · QA 시나리오 5/5 통과(`docs/dev-cycle/qa/CHAT-032.md`)
- 삭제한 테스트: 지운 함수만 검사하던 10건. `get_cached_data` 3건(fetcher 캐시, 실패 시 mock 폴백, 하루 지난 캐시 갱신), `fetch_mock_data` 모양 1건, `detect_stock_query_from_vcp_data` 매칭 1건, `detect_stock_query` 의 vcp 경로 1건, `fallback_response` 2건(추천 목록, 이해 못함 문구), `format_stock_info` 필드 1건, `build_watchlist_summary_context` 빈 값·일치 1건. `/refresh` 는 `test_core_command_mixin` 의 상태 변경 명령 목록과 라우트 통합 테스트의 슬래시 명령 매개변수에서 뺐다(알 수 없는 명령 갈래는 `/unknown` 이 검사)
- 메모: VCP 상담 QA(S-2)는 사본 `data/` 에 오늘 VCP 시그널이 없어 종목을 고를 수 없었으므로 사본의 `signals_log.csv` 에만 005930 합성 행 하나를 넣어 실행했다. 원본 `data/runtime_cache.db` 수정 시각은 QA 전후로 바뀌지 않았다. 독립 단어로 쓰인 짧은 종목명 오탐, 가격과 티커의 우선순위, 두 글자 조사는 `[CHAT-038]` 로 이월했다

## [CHAT-033] 챗봇 저장소의 스레드 동시성 확보
- 완료 2026-09-23 09:34 | 티어 T2 (위험 경로 파일 없음, 구현 diff 292줄) | 커밋 bd5adf1
- 변경: chatbot/storage.py, chatbot/storage_memory_manager.py, chatbot/storage_history_helpers.py, tests/chatbot/test_storage_concurrency.py(신규) (코드 +261 -31)
- 설계: bounded. 워커 하나의 스레드 여덟 개가 챗봇 인스턴스 하나를 공유하므로 공용 데코레이터 `locked`(인스턴스 `RLock`)로 `MemoryManager`·`HistoryManager` 의 공개 메서드를 직렬화했다. `_save_single_entry` 는 레코드를 인자로 받고 `view()` 는 사본을 돌려준다. TODO 가 정하라고 남긴 「폴백 전 재적재냐, 폴백 제거냐」는 **제거**로 정했다. `_save()` 의 전체 동기화 폴백과 무델타 전체 동기화를 없애 삭제 절이 다른 워커의 세션을 지우거나 낡은 사본의 upsert 가 지운 세션을 되살리는 경로를 닫았다. 코드 리뷰를 거쳐 세 갈래를 더했다: 저장 실패 뒤 서명을 비워 다음 접근이 DB 를 다시 읽는다, SQLite 읽기 실패(기동 시점·재적재 중 모두)에서는 최대 15초 늦은 JSON 스냅샷을 쓰지 않고 `_reload_failed` 동안 SQLite·JSON 쓰기를 모두 거부한다, 레거시 JSON 이관은 DB 가 없거나 표가 빈 경우에만 일어난다. `save_history_sessions_to_sqlite` 와 스키마 파일은 바꾸지 않았다
- 리뷰: closing-bet-python(테스트는 `tmp_path` 로 원본 `data/` 에 닿지 않음) · ponytail-review 는 작성자가 먼저 메모리 쪽 `_*_locked` 보조 메서드 네 개를 공용 데코레이터로 통일한 뒤 Lean already · closing-bet-reviewer `chat033-reviewer` CHANGES_REQUIRED(medium 2: 실패 뒤 서명 갱신, 재적재 중 읽기 실패의 늦은 JSON) → APPROVE(low 1: 기동 시점 읽기 실패) → APPROVE(none). 반영 뒤 늘 참이 된 이관 조건은 지웠다. 잠금 안의 SQLite busy 대기와 잠금 밖에서 세션 사전을 고치는 외부 호출자(`command_service`·`session_access`)는 `ponytail:` 주석으로 한계만 표시했다 · closing-bet-verify(격리 사본, 브라우저·저장소·로그·`/_next/mcp`)
- 검증: RED 에서 메모리 update 경합(`datetime.now` 자리에 다른 스레드의 재적재를 끼움)·델타 실패 뒤 다른 워커 세션 보존·읽기 실패 뒤 이관 금지는 결정적 실패, 히스토리 두 스레드 테스트는 확률적 실패(작성자 3/4, 리뷰어 2/6) → GREEN · 리뷰 반영분 변이 4종(서명 유지, 거부 무력화, 재적재 JSON 교체, 기동 조건 복원) 각각 해당 테스트 실패 · 전체 `pytest -q -p no:cacheprovider` 2629 통과 2 skip exit 0 · frontend 변경 없음이라 type-check·vitest 생략 · QA 시나리오 4/4 통과(`docs/dev-cycle/qa/CHAT-033.md`, gunicorn `--workers 2 --threads 8`, 가짜 genai 3초 지연으로 두 탭의 요청이 서버 안에서 겹침을 로그 시각으로 확인)
- 갱신한 테스트: 없음. 「폴백이 돈다」를 전제한 기존 검사(`test_clear_for_owner_raises_when_delete_is_not_persisted`)는 패치 대상 함수가 남아 있어 그대로 통과했다
- 메모: 격리 환경에서는 Google 로그인을 할 수 없어 「같은 계정」은 같은 브라우저 두 탭의 서명된 익명 신원으로 대신했다. S-3 은 브라우저 드라이버 조작 문제(확인 모달 선택자 중복, 탭이 연 대화)로 세 번 만에 의도대로 실행했다. 사용자 메시지와 모델 응답을 따로 저장해 같은 세션에 동시에 쓰는 두 요청의 줄이 섞일 수 있는 것은 원래 있던 동작이며 이번 범위 밖이다. `[CHAT-035]`(HistoryManager 책임 분리)의 선행 조건이 충족되었다

## [FE-046] 활동 로그의 보관 기간을 날짜 기준으로 만들고 데이터 파일 권한을 좁힌다
- 완료 2026-09-23 10:30 | 티어 T3 (등록 시 T2, 위험 경로 `services/sqlite_utils.py` 로 상향) | 커밋 4eba1ee
- 변경: services/activity_logger.py, services/sqlite_utils.py, chatbot/storage_history_helpers.py, frontend/src/app/(legal)/privacy/page.tsx, frontend/src/app/(legal)/legal-pages.test.tsx, tests/services/test_activity_logger_refactor.py(신규), tests/chatbot/test_storage_history_helpers_refactor.py(신규), tests/services/test_sqlite_utils.py (코드 +362 -14)
- 설계: bounded, 사용자 승인 「30일로 진행」. 활동 로그는 `RetentionTimedRotatingFileHandler` 가 회전 파일을 이름의 날짜가 아니라 **마지막 수정 시각** 기준 30일(`ACTIVITY_LOG_RETENTION_DAYS`)로 지운다. 삭제는 기동 때(챗봇 블루프린트 등록이 전역 로거를 만듦)와 자정 뒤 첫 기록(회전) 때이며, 기동 때 기준 파일의 날짜가 이미 지났으면 곧바로 회전해 기록이 없던 파일도 지운다. 계획 검토 B1 뒤 사용자가 범위 확장을 승인해 다중 워커 회전 경합도 고쳤다: 늦게 회전하는 워커는 표준 구현이 「이미 회전됨」으로 돌아가 날짜 붙은 어제 파일에 재기동 때까지 계속 썼고(로컬 `user_activity.log.2026-09-09` 의 수정 시각이 09-21), 그 파일은 수정 시각이 계속 새로워 영원히 지워지지 않았다. `doRollover` 가 조기 반환 뒤 기준 파일을 다시 열고, `rotate` 는 `os.link`+`os.unlink` 로 이미 있는 날짜 파일을 덮어쓰지 않는다. 쓰기용 SQLite 연결(`connect_sqlite`)은 DB 와 남아 있던 -wal·-shm 을 0600 으로, 활동 로그는 0600 으로 열고 남은 회전 파일도 좁히며, 챗봇 JSON 스냅샷은 `os.open(..., 0o600)` 으로 만든다. 방침 3항은 삭제 시점·「이틀가량」 상한·접속과 재시작이 없을 때의 지연을, 10항은 「활동 로그와 데이터베이스 파일」의 권한 제한을 적고 시행일을 2026-09-23 으로 바꿨다
- 리뷰: closing-bet-python · closing-bet-nextjs · Next 번들 문서 03-layouts-and-pages(서버 컴포넌트 page 의 문안만 바뀜) · 계획 검토 oh-my-claudecode:critic `fe046-plan-critic` REVISE(B1 다중 워커 경합, B2 삭제 시점, B3 회전 경로 테스트, R1~R7) → ACCEPT-WITH-RESERVATIONS · ponytail-review(자체 수행, Lean already) · closing-bet-reviewer `fe046-reviewer` CHANGES_REQUIRED(medium: 삭제 시점은 첫 기록이 아니라 기동 시. 계획 검토 B2 의 판단이 틀렸음을 `create_app()` 실측으로 정정, low: 「조금 더」 상한, 생성 뒤 chmod 틈은 `ponytail:` 주석, 이미 0600 인 DB 의 남은 -wal·-shm 은 QA 실측, 표준 라이브러리의 동시 회전은 기록) → APPROVE · /review(oh-my-claudecode:critic `fe046-deep-review`, review 스킬 절차) REVISE(MEDIUM-1: 30일 넘게 기록 없던 기준 파일은 기동 정리에서 빠짐. GET 은 기록되지 않아 실제로 생김) → 기동 시 회전 → ACCEPT-WITH-RESERVATIONS(LOW-A: 기동 시 회전으로 동시 회전 창이 재기동마다 열림 → `rotate` 재정의) → ACCEPT-WITH-RESERVATIONS(LOW: 원본이 이미 옮겨진 경우 `FileNotFoundError` → 한 줄 반영) · closing-bet-verify(격리 사본, 브라우저·파일 권한·`lsof`·`/_next/mcp`)
- 검증: 신규 테스트 14건 RED → GREEN(활동 로그 9, SQLite 4 중 3, 챗봇 JSON 2. 읽기 전용 권한 불변 1건은 처음부터 통과) · 변이 6종(이름 날짜 기준, 재열기 제거, 회전 삭제 제거, `_open` chmod 제거, SQLite 호출 제거, JSON `open()` 복귀) 각각 해당 테스트 실패 · 리뷰 반영분 3건(기동 시 회전, 덮어쓰기 방지, 원본 없음)은 테스트를 먼저 써서 실패를 확인 · 최종 `pytest -q` 2644 통과 2 skip exit 0 · `npm run type-check` exit 0 · `npm run test` 679 통과 · QA 4/4 통과(`docs/dev-cycle/qa/FE-046.md`, gunicorn `--workers 2 --threads 8`, 요청 없이 기동만으로 30일 지난 회전 파일 삭제, 이름이 아니라 수정 시각 기준, 쓰기로 열린 DB 0600, 두 워커가 모두 기준 파일만 엶, 방침 문안)
- 갱신한 테스트: `legal-pages.test.tsx` 의 보관 기간 단언. 옛 동작(파일 개수 30개, 「달력상 30일보다 길어질 수 있다」)을 고정하던 두 문장을 새 문안 세 문장과 10항 한 문장으로 바꿨다
- 메모: 테스트 모듈 import 와 전체 pytest 가 원본에 영향을 주었다(설계 때 알린 것 포함). 원본 `logs/` 의 2026-02~05 회전 파일 9개가 지워지고 남은 파일이 0600 이 되었으며, `[INFRA-078]` 의 절대 경로로 원본 `data/` 의 `runtime_cache.db`·`paper_trading.db`(-wal·-shm)·`usage.db` 가 0600 으로 좁혀졌다(내용·수정 시각 불변). 운영 서버에는 다음 배포 뒤 첫 기동 때 같은 삭제와 권한 변경이 일어난다. QA 에서 `paper_trading.db` 는 익명으로 열리지 않아 서비스 하네스로 확인했고, `usage.db` 는 운영 코드가 열지 않는 유물이라 0644 로 남는다(`[INFRA-079]`). 수정 전 늦은 워커가 12일간 쓴 `user_activity.log.2026-09-09` 는 그 안의 09-09 기록이 약 42일 남는 과도기 파일이며 10-21 에 지워진다. 기록 없던 날 재기동하면 0바이트 날짜 파일이 하나 생긴다(0600, 30일 뒤 삭제). 새 DB·로그 파일은 umask 로 만든 뒤 좁히므로 비어 있는 잠깐 동안 0644 다. 하드 링크를 지원하지 않는 파일 시스템에서는 회전마다 실패하므로 배포 환경이 바뀌면 다시 본다. 0644 로 남는 다른 로그 세 개는 `[INFRA-081]` 로 이월했다

## [FE-047] 루트 레이아웃의 `lang` 을 한국어로 바로잡는다
- 완료 2026-09-23 11:05 | 티어 T1 | 커밋 bc7ac3d
- 변경: frontend/src/app/layout.tsx, frontend/src/app/layout.test.tsx(신규) (코드 +13 -1)
- 리뷰: closing-bet-nextjs(신원·환경 변수·API 경계와 무관한 속성 값 하나) · frontend-skills §2 의 `layout` 줄과 Next 번들 문서 `03-file-conventions/layout.md`(Root Layout 은 `<html>`·`<body>` 를 직접 정의하며 `lang` 은 그 속성이다) · vendor 스킬 해당 없음 · ponytail-review(Lean already, 지적 0건) · closing-bet-verify(격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: vitest RED 1건(`expected 'en' to be 'ko'`) → GREEN · 전체 vitest 95파일 680건 통과 · type-check exit 0 · QA 시나리오 2/2 통과
- 메모: 테스트는 `RootLayout` 을 함수로 불러 반환 요소의 `props.lang` 을 본다. `(legal)/layout.tsx` 는 위에 `app/layout.tsx` 가 있어 루트 레이아웃이 아니므로 `<html>` 을 두지 않는다. QA 는 Flask 없이 Next 만 띄웠고, 대시보드의 API 500 은 그 때문이다

## [INFRA-078] 격리 실행이 원본 `runtime_cache.db` 에 쓰는 절대 경로
- 완료 2026-09-23 11:29 | 티어 T1 | 커밋 55ea40a
- 변경: services/file_row_count_cache.py, services/common_update_status_service.py, services/kr_market_data_cache_jongga.py, services/kr_market_cumulative_cache.py, services/kr_market_backtest_summary_cache.py, tests/services/test_runtime_cache_path_refactor.py(신규) (코드 +64 -10)
- 리뷰: closing-bet-python(`.claude/skills/closing-bet-python/SKILL.md`, 위험 경로 해당 없음) · ponytail-review(Lean already, 지적 0건) · closing-bet-verify(`.claude/skills/closing-bet-verify/SKILL.md`, 대조군을 둔 격리 사본과 브라우저·`/_next/mcp` 두 관점)
- 검증: pytest RED 5건(상수 검사) → GREEN 6건 · 전체 pytest 2650 passed 2 skipped exit 0 · QA 시나리오 3/3 통과(수정 전 커밋 대조군 재현 포함)
- 메모: TODO 의 세 곳은 조사로 다섯 곳이 되었다(누적 성과·백테스트 요약 캐시 추가). 사용자 결정으로 캐시만 고치고, 상태 파일·파이프라인 출력의 절대 경로 여섯 곳은 `[INFRA-082]` 로, 전체 pytest 가 원본 `runtime_cache.db` 에 쓰는 34개 테스트는 `[INFRA-083]` 으로 이월했다. 동작 테스트는 구현 전에 돌리면 원본 DB 에 쓰므로 RED 에서 뺐다. QA 는 원본 앱을 scratch cwd 로 띄우지 말라는 browser-notes 에 따라 `git archive` 코드 사본 두 벌(수정 전·후)과 `data/` 사본 cwd 로 VCP-026 구조를 재현했고, 수정 전 코드만 코드 사본 쪽 표식 DB 에 다섯 테이블을 만들었다. 두 사본 모두 `data/vcp_status.json` 이 생긴 것은 `[INFRA-082]` 의 `app/routes/kr_market.py:191` 경로다. 운영은 `restart_all.sh` 가 저장소 루트로 `cd` 하므로 같은 파일을 쓴다

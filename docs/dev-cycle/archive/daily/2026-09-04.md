# 2026-09-04

## [JONGGA-017] 영문자가 든 종목코드가 조회 경로에 따라 다른 코드가 되던 문제
- 완료 2026-09-04 07:14 | 티어 T2 | 커밋 5952ca5
- 변경: app/routes/kr_market_signal_common.py,
  tests/app/test_kr_market_jongga_helpers_refactor.py, docs/dev-cycle/qa/JONGGA-017.md
  외 1개 (+226 -27, 구현만 1파일 +25 -5)
- 리뷰: ponytail-review(5건 중 4건 반영, net -4) · code-reviewer(응답이 늦어 같은 다섯
  관점을 직접 검토했고, 그 검토에서 긴 숫자열 결함 1건을 추가로 찾아 반영)
- 검증: pytest 1605 통과 · vitest 208 통과 · tsc 오류 없음 ·
  QA 시나리오 7/7 통과 (헬스 98, 실패 없음)
- 메모: `_normalize_ticker` 는 공유 함수가 아니다. 같은 이름의 함수를 다섯 서비스가 각자
  정의하고 있으며, 이번에 고친 것은 라우트 계층의 정의 하나다. 실제 소비자는
  `kr_market_jongga_grade_helpers.py` 와 `kr_market_jongga_normalize_helpers.py` 둘뿐이다.
  나머지 다섯 곳은 `[JONGGA-030]` 으로 이월했다. 이력 조회 경로에 정규화를 끼우자는 리뷰
  지적은 반영하지 않았다. `_normalize_jongga_signals_for_frontend` 가 `score` 재구성과
  `target_price`·`stop_price` 계산을 함께 하므로, 이력 응답에 끼우면 저장된 자료의 값이
  화면에서 달라진다. 회귀를 다시 볼 자리는 2026-02-20 이다. 아로마티카(`0015N0`)와
  두산(`000150`)이 한 화면에 함께 뜨므로, 정규화가 영문자를 버리면 같은 코드를 단 카드가
  두 장 생긴다.

# 2026-09-03

## [CHAT-002] 계산해 놓고 버려지는 세 값 복구
- 완료 2026-09-03 00:02 | 티어 T2 | 커밋 9521c6e
- 변경: chatbot/response_flow_stream.py, chatbot/session_access.py,
  chatbot/payload_service.py 외 14개 (+216 -66, 구현만 9파일 +38 -34)
- 리뷰: ponytail-review(3건 전부 반영, net -12) · code-reviewer(확신도 80 이상 0건,
  낮은 확신도 관찰 1건 반영)
- 검증: pytest 1582 통과 2 skipped · vitest 23파일 168건 통과 · 변이 5갈래 전부 검출 ·
  qa-only 헬스 98
- 메모: 세 결함 모두 값을 만들어 놓고 쓰지 않아 기능이 조용히 죽어 있던 자리다.

  첫째, 스트리밍 경로의 토큰 사용량이 늘 비어 있었다. `stream_single_model_response` 가
  청크에서 `text` 만 꺼내고 `usage_metadata` 를 보지 않아 `chat_handlers.py:186` 의
  `if usage_metadata:` 가 항상 거짓이 되었다. 함께 `extract_usage_metadata` 도 고쳤다.
  `hasattr` 만 검사해서 `usage_metadata=None` 인 응답에 값이 전부 0 인 딕셔너리를
  돌려주고 있었는데, 스트리밍 청크가 대부분 그 형태로 온다. 한 곳을 고쳐 스트리밍과
  비스트리밍 두 경로가 함께 나았다. `google-genai==1.62.0` 의 `total_token_count` 는
  "the entire request" 의 합계이므로 증분이 아니라 누적값이다. 그래서 마지막 유효값을
  남기도록 했다.

  둘째, `is_ephemeral_command` 가 바로 위에 선언된 `_EPHEMERAL_COMMANDS` 를 참조하지
  않고 `startswith("/")` 만 검사했다. `command_service.handle_command` 와 같은
  `split()` 으로 첫 토큰을 뽑아 집합과 대조하도록 고쳤다.

  셋째, 의도 지시문이 종가베팅 질문에만 실렸다. TODO 는 네 의도에도 실을지 네 의도의
  지시문 생성을 걷어낼지 정하라고만 적혀 있었는데, 싣는 쪽으로 정했다. 근거가 셋이다.
  네 의도의 지시문이 각각 의미 있는 내용을 담고 있어 걷어내면 기능이 줄고, 관심종목
  지시문까지 함께 버려지고 있었으며, 싣는 쪽이 코드도 짧아진다. 조건 하나와 파라미터
  하나가 사라지고 두 함수의 반환이 3튜플에서 2튜플로 줄었다.

  `_EPHEMERAL_COMMANDS`(`/status`, `/help`)와
  `storage_history_helpers.has_meaningful_user_message` 의 목록(`/status`, `/help`,
  `/memory view`, `/clear`)이 다른데, 서로 다른 질문에 답하므로 결함이 아니다. 앞의 것은
  저장할지를 정하고 뒤의 것은 세션 목록에 표시할지를 정한다. `/clear` 만 있는 세션은
  저장은 되되 목록에는 뜨지 않는다. qa-only 실측에서 이 맞물림이 설계대로 동작하는 것을
  확인했다. `/clear` 만 있던 세션이 목록에 없다가 실제 질문이 더해진 뒤 나타났다.

  `tests/test_chatbot_feature.py::test_slash_commands` 가 전체 pytest 에서 실패했다.
  모든 슬래시 명령이 임시라는 이전 동작을 못 박고 있었기 때문이며, 결함이 아니라 의도한
  동작 변경이다. 실제 값을 프로브로 확인한 뒤 새 사양을 못 박도록 갱신했다.

  지운 검사가 하나다. `tests/chatbot/test_session_access.py` 의 상태 변경 명령 판정만
  보던 검사이며, 새로 넣은 `tests/chatbot/test_core_command_mixin.py` 가 같은 판정을
  부른 뒤 저장 결과까지 확인해 포함 관계였다. 같은 파일의 다른 검사
  `test_extract_usage_metadata_returns_empty_when_usage_is_none` 은 중복으로 보였으나
  남겼다. 통합 검사 쪽은 `usage_metadata=None` 을 첫 청크에 두었는데, None 처리가 깨지면
  그 자리에서 값이 전부 0 인 딕셔너리가 나왔다가 두 번째 청크의 유효값에 덮여 통과한다.

  qa-only 실측을 하기 전에 gunicorn 워커를 `kill -HUP` 으로 재기동해야 했다. 워커가
  15:48 에 뜬 것이라 22:27 의 변경을 싣지 않았고, 그대로 돌렸다면 이전 코드를 측정하게
  된다. 실측 결과 세 결함이 모두 확인되었다. 실제 스트리밍 응답이
  `prompt_token_count=1421, candidates_token_count=158, total_token_count=1766` 을
  활동 로그에 남겼고, `data/chatbot_storage.db` 직접 조회로 `/status` 와 `/help` 는
  0건, `/clear` 는 2건이 저장되는 것을 확인했다. 셋째 결함은 프롬프트 내용이라 화면으로
  볼 수 없고 다섯 갈래를 각각 보려면 유료 호출이 다섯 번 필요해 실행하지 않았다.

  qa-only 가 두 건을 새로 발견해 등록했다. `[CHAT-008]`(P1)은 모델을 부르지 않는 슬래시
  명령이 무료 사용량을 차감하는 문제이고 이번 변경과 무관한 기존 동작이다. 다만
  `/status` 와 `/help` 가 이제 기록에도 남지 않게 되어 "차감은 되는데 흔적이 없는" 조합이
  새로 생겼다. `[CHAT-009]`(P2)는 `/clear` 로 시작한 대화의 제목이 `/clear` 로 붙는
  문제이며 이번 변경이 직접 만든 것이다.

  활동 로그의 `token_usage` 키 이름이 과거 항목과 달라 결함으로 의심했으나 아니었다.
  과거 476건은 전부 사용자 메시지가 `테스트 메시지` 이고 값이 `(12, 8)` 로 고정된 테스트
  스텁이며, 그 키 이름을 읽는 코드가 저장소에 없다. 실제 트래픽이 사용량을 남긴 것은
  이번이 처음이다.

## [VCP-002] VCP 화면의 현재가 갱신 대상과 폴링 정리 결함 수정
- 완료 2026-09-03 01:11 | 티어 T2 | 커밋 af23211
- 변경: frontend/src/app/dashboard/kr/vcp/page.tsx (+25 -13),
  frontend/src/app/dashboard/kr/vcp/page.regression-vcp-002.test.tsx (신규 +172)
- 리뷰: ponytail-review(2건 전부 반영, net -3) · code-reviewer(확신도 80 이상 1건 반영,
  낮은 확신도 1건 미반영)
- 검증: pytest 1582 통과 2 skipped · vitest 24파일 172건 통과 · tsc 오류 0 ·
  변이 4갈래 중 3갈래 검출 · next-dev-loop 실측 · qa-only 헬스 99
- 메모: 두 결함 모두 코드가 값을 옳게 만들어 놓고 그 값을 쓰지 않는 형태였다.

  첫째, 현재가 갱신 effect 의 의존성이 `[signals.length, loading]` 인데 본문은
  `signals.map(s => s.ticker)` 로 목록의 내용을 읽었다. 조회 날짜를 바꿔 목록이
  통째로 교체되어도 종목 수가 같으면 effect 가 다시 실행되지 않아 클로저가 이전
  목록을 붙든다. 티커 목록을 이어 붙인 문자열을 effect 밖에서 파생시켜 그것에
  의존하도록 바꿨다. `signals` 를 통째로 두면 effect 안의 `setSignals` 가 스스로를
  다시 불러 무한 루프가 되기 때문에 배열이 아니라 문자열로 파생시켰다.

  이 결함이 지금 화면에 드러나지 않는다는 사실을 변이 검사로 규명했다. 의존성을 옛
  형태로 되돌려도 새 검사가 통과했는데, 조사해 보니 `loadSignals` 가 매번
  `setLoading(true)` 로 시작하고 `finally` 에서 `false` 로 돌리므로 목록이 교체되는
  모든 경로에서 loading 전환이 effect 를 대신 재실행시키고 있었다. 옛 의존성에서
  `setLoading(true)` 를 함께 뺀 변이에서는 검사가 실패하고 새 의존성에서는 같은
  조건에서도 통과하는 것을 확인했다. 이번 변경이 걷어낸 것은 우연한 방어다. 이
  경위를 page.tsx 와 테스트 파일의 머리 주석에 남겼다.

  둘째, 스크리너 상태 폴링이 만드는 `setInterval` 핸들이 지역 변수에 담겨 언마운트
  정리에서 빠져 있었다. 감사 문서 §4.1 은 `checkRunningStatus` 한 곳만 지목했으나
  `grep -n 'setInterval'` 로 네 곳을 훑어 스크리너 실행 버튼의 `onClick` 에도 같은
  결함이 있음을 찾았다. 두 폴링은 `screenerRunning` 가드 때문에 동시에 존재할 수
  없으므로 `screenerPollRef` 하나와 `clearScreenerPolling` 하나로 모으고 언마운트
  정리에 더했다. 기존 `reanalysisPollRef` 와 `clearReanalysisPolling` 이 같은 형태로
  있어서 그 짝을 따랐다.

  `clearScreenerPolling` 을 별도 함수로 둔 것은 ponytail-review 에서 과한지 물었고
  유지로 판정했다. `clearInterval` 만 부르면 ref 에 죽은 핸들이 남아 `null` 판정이
  무의미해진다. `join`/`split` 왕복도 유지했다. 배열은 매 렌더 새 참조라
  `Object.is` 비교가 통하지 않는다. 반영한 두 건은 `split` 결과를 사용처에 인라인한
  것과 주석 네 줄을 두 줄로 줄인 것이다.

  code-reviewer 의 확신도 82 지적을 반영해 검사를 하나 더 넣었다. 회귀 검사가 두
  폴링 자리 가운데 `checkRunningStatus` 만 보고 버튼 쪽은 보지 않는다는 지적이며,
  `Refresh VCP` 를 누른 뒤 언마운트하는 검사를 더하고 버튼 쪽만 옛 형태로 되돌리는
  변이로 검출력을 확인했다. 확신도 40 짜리 지적 하나는 반영하지 않았다. 마운트 직후
  스크리너가 중복 실행될 수 있는 좁은 구간을 지적했으나
  `services/kr_market_flow_service.py:188` 이 `running` 상태에서 409 를 돌려주므로
  중복 실행되지 않는다.

  브라우저 실측은 결함 조건을 직접 만들어 확인했다. `data/signals_log.csv` 의
  `is_vcp` 열이 전부 비어 있어 어느 날짜에서도 목록이 비므로, 백업한 뒤 개수가 같은
  두 날짜를 만들었다. 2026-05-05 에 `000660`·`005380`·`003550`, 2026-05-04 에
  `034730`·`005930`·`064350` 이다. 처음에는 인덱스로 3개씩 나눴는데 백엔드가 점수로
  거르는 탓에 한쪽이 1종목이 되어, 점수 70 이상인 티커를 골라 3종목씩 맞췄다.
  브라우저에서 두 날짜를 차례로 골라 `POST /api/kr/realtime-prices` 의 요청 본문을
  읽었더니 개수가 같은데도 티커가 새 목록을 따라갔고, 전환마다 요청이 1건씩만
  나갔다. 관찰 뒤 백업본으로 되돌려 md5 가 원본과 일치하는 것을 확인했다.

  `실패 AI 재분석` 과 `Refresh VCP` 버튼은 누르지 않았다. 실제 LLM API 비용이
  발생하며 사용자가 이 자리에서 지출을 승인하지 않았다. 스크리너 폴링의 정리 동작은
  vitest 두 건과 변이로 대체 검증했다.

  qa-only 는 헬스 99 를 냈고 새 발견이 한 건이다. 아이콘만 있는 컨트롤 다섯 개에
  접근 가능한 이름이 없는데, 이미 `[FE-017]` 이 같은 결함을 담고 있어 새 항목을
  만들지 않고 그 항목에 두 개를 덧붙였다. 브레드크럼의 홈 링크와 채팅 위젯 열기
  버튼이며, 앞의 것은 `button` 이 아니라 `a` 여서 버튼만 세던 이전 조사에서 빠져
  있었다. 이번에는 `aria-label` 과 `title` 과 텍스트가 모두 빈 `button` 과 `a` 를
  함께 조회했다.

  `[FE-016]`(375px 에서 매수 버튼 잘림)은 이번 qa-only 에서 재현되지 않았으나
  해소 근거로 삼지 않았다. 목록이 비어 버튼 행이 vcp-003 실측 때보다 짧아진 상태다.

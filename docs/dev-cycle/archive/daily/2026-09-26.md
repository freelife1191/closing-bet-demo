# 2026-09-26

## [FLOW-029] pykrx 수급 참조가 끝 날짜를 기준일과 대조하지 않고, 거부될 값까지 기준일 키로 영구 캐시하던 결함
- 완료 2026-09-26 00:09 | 티어 T3 (공유 검토, 위험 경로 `services/investor_trend_5day_service.py`) | 커밋 cea9cb0
- 원인: `_fetch_pykrx_reference_trend` 는 기준일까지 14일을 받아 `tail(5)` 를 썼지만 마지막 행이 기준일인지 보지 않았다. 그날 자료가 아직 없으면 전날까지의 5일이 기준일의 5일 값이 되었고, `_get_reference_trend_cached` 가 그 값을 기준일 키로 SQLite 에 저장해 같은 기준일에는 다시 묻지 않았다. `_reference_reject_reason` 이 거부하는 값(NaN 인 날의 `insufficient_days`, `zero_total`)도 저장되어 새 워커도 다시 묻지 않았고(`[FLOW-025]` 묶음 `/review` M2), `930525be` 이전에 NaN 을 0 으로 저장한 항목을 가를 버전이 서명에 없었다(F3). `[FLOW-026]` 이후 종가베팅 기준일 실행이 이 경로를 1순위로 쓴다
- 변경: services/investor_trend_5day_service.py(+15 -3), engine/collectors/naver.py(+5 -2), tests/services/test_investor_trend_5day_service.py(+62), tests/engine/test_naver_collector_refactor.py(+8 -5). `[JONGGA-043]` 과 한 커밋을 공유하며 증감은 이 항목에 한 번 반영: 운영 코드 +20 -5, 테스트 +70 -5(코드 합 +90 -10), 문서 +302(계획 189·QA 113)
- 설계: bounded, T3. 2026-09-25 사용자 「FLOW-029 JONGGA-043 한번에 진행해」 뒤 설계를 제시했고 23:26 `/effort high` + 「승인」: (1) pykrx 표의 마지막 날이 조회 끝 날짜(기준일, 없으면 캐시 키인 pykrx 최근 거래일)와 다르면 None — 60초 실패 캐시, 저장 없음 (2) `_reference_reject_reason` 에 걸리는 값은 메모리·SQLite 에 두지 않고 60초 실패 캐시, 조회한 호출에는 값을 돌려줘 사유 기록 유지 (3) 참조 SQLite 서명에 `v2`(v1 행은 무시되고 같은 키에서 덮어써짐). 계획 `docs/superpowers/plans/2026-09-25-flow-029-reference-cache-end-date.md`(태스크 2개)
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md`(결측과 실제 0 경계, 해당 없음) · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS(Minor-1 사유 기록 범위·Nit-1·Info-1 JONGGA-043 영향 정정 반영, Minor-2·Minor-3 은 아래 한계로 기록, Info-2 는 `[FLOW-030]`) · `/ponytail-review` Lean already. Ship.(코드 +18 −3) · `closing-bet-reviewer` APPROVE, 최대 low(F3 docstring·F2 `[FLOW-030]` 문구 반영, F1 → `[FLOW-031]`, F4·F5 는 운영 호출자에 없어 조치 없음) · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) APPROVE, Critical·Important 0(Minor-1 시각이 붙은 끝 날짜 단언 추가 — 변이 S2 가 새 단언에 걸림을 확인 후 원복, Minor-2 `[FLOW-030]` 범위, Nit-1 v2 주석, Nit-2 docstring 반영) · 보안 리뷰 보강은 인증·시크릿 경로가 없어 해당 없음
- 검증: 새 검사 3건 RED(dict 반환, SQLite 행 `True`, v1 의 4_995) → GREEN · 리뷰 반영 뒤 pytest 전체 2862 passed·2 skipped(exit 0, `KRX_ID= KRX_PW=`, 「KRX 로그인」 0회) · vitest 104 files·701 passed(09-26 00:01, frontend 변경 없음. 23:5x 실행의 1건 실패는 시각 의존 검사 결함으로 `[JONGGA-044]`) · QA 시나리오 7/7 통과(`qa/FLOW-029.md`. 격리 사본의 실제 Flask 라우트와 Next, Toss·Naver HTTP 와 pykrx 모듈만 대역, 소켓 차단 0건. 브라우저 gstack browse 로 두 모달, 전날로 끝나는 pykrx 참조를 버리고 Toss 참조 「-10억」·「-20억」, 수정 전 코드 `13dac910` 은 5억·10억과 `900271__20260925` 행. CLI 하네스로 두 단계 실행: 전날 끝·NaN 참조를 저장하지 않아 단계 B 가 다시 물어 10억·15억, 수정 전은 단계 B 호출 0회에 5억·None, 수정 전 v1 행 위에서 새 코드가 다시 물어 v2 로 덮어씀. 브라우저 1차 실측은 고정 자료의 카드가 D 로 재분류되어 자료를 고쳐 다시 돌림)
- 정리: 격리 gunicorn(5755·5756)·Next(3755)·browse 종료, 00:07:16 리스너 0, 00:07:20 QA 프로세스 0, 사본과 CLI 자료 디렉터리를 리터럴 절대 경로로 삭제, 원본 `data/` 변화 0. gunicorn 종료 첫 명령이 zsh 에서 PID 목록을 나누지 않아 실패해 작업 디렉터리를 확인한 뒤 마스터를 다시 종료
- 이월: `[FLOW-030]`(pykrx·Toss 참조가 확정 전 당일 값을 당일 키로 저장할 수 있음, Toss 참조에는 끝 날짜 대조가 없음) · `[FLOW-031]`(기준일 실행이 pykrx 를 못 쓰면 CSV 폴백이 전날 끝 5거래일을 플래그 없이 씀) · `[JONGGA-044]`(`[JONGGA-037]` 회귀 검사가 KST 자정 전 약 6분 동안 실패)
- 메모: 이 변경은 pykrx 참조 경로에서만 전날 끝 창을 막는다. 기준일 실행의 CSV 폴백은 여전히 전날 끝 창을 쓸 수 있다(`[FLOW-031]`). 알려진 한계: 끝 날짜가 휴장일이면 pykrx 참조를 늘 거부하고 60초마다 다시 묻는다(Minor-2). 늘 거부되는 참조(전부 0 인 거래정지 종목 등)도 하루 한 번이 아니라 60초 간격으로 다시 조회한다(Minor-3). 버린 참조의 사유는 조회한 호출의 `quality.discarded_references` 에만 남고 60초 안의 뒤따른 호출에는 남지 않는다. 운영 반영은 gunicorn 재기동이 필요하다(장 중 금지). 서명이 v2 로 바뀌어 기존 참조 캐시 행은 첫 조회에서 다시 받는다

## [JONGGA-043] Naver 상세 캐시가 영문자로 끝나는 종목코드를 저장하지 않던 결함
- 완료 2026-09-26 00:09 | 티어 T3 (공유 검토) | 커밋 cea9cb0
- 원인: `NaverFinanceCollector._normalize_stock_detail_payload` 가 코드가 숫자 여섯 자리가 아니면 None 을 돌려줬다. `[FLOW-028]` 이후 `00680K` 같은 코드가 자기 코드로 상세 API 에 오므로, Toss 상세가 없어 Naver 로 넘어가면 결과가 메모리·SQLite 에 저장되지 않았다. 상세 서비스의 15분 캐시가 앞에 있어 모달을 열 때마다가 아니라 15분마다 다시 스크랩했다(계획 검토 Info-1)
- 변경: `[FLOW-029]` 와 같은 공유 커밋(engine/collectors/naver.py 에서 숫자 검사를 `normalize_ticker` 로 교체, 코드가 없으면 debug 로그와 None). 증감은 `[FLOW-029]` 에 합산
- 설계: bounded, T3 공유 검토. 2026-09-25 23:26 「승인」(`[FLOW-029]` 와 같은 응답)
- 리뷰·검증·정리: `[FLOW-029]` 와 같음. 기존 Naver 캐시 재사용 검사를 `5930`·`00680K` 로 매개변수화해 `00680K` 만 RED(`second is None`) → GREEN. QA S-2(모달이 QA우선네이버·00680K 로 그려지고 `detail_info` 캐시에 `00680K__202609260000` 행, 수정 전은 행 없음)·H-4(메모리 캐시를 비운 뒤 다시 조회해도 새 코드 `requests=1`, 수정 전 `requests=2`)

## [FLOW-030] pykrx·Toss 수급 참조가 확정 전 당일 값을 당일 키로 저장할 수 있던 결함
- 완료 2026-09-26 01:27 | 티어 T3 (공유 검토, 위험 경로 `services/investor_trend_5day_service.py`) | 커밋 46f569a
- 원인: `_get_reference_trend_cached` 는 쓸 만한 참조를 키 날짜와 무관하게 메모리·SQLite 에 두었다. 17시 기준일 실행(기준일=당일)과 장중에 최근 거래일이 당일로 확정된 상세 모달의 최신 창은 같은 키를 쓰므로, 확정 전 당일 값이 저장되면 그날 뒤의 실행과 다음 날 이후에도 확정 값처럼 읽혔다. Toss 참조는 키가 늘 달력상 오늘이라 아침 스냅숏이 그날 내내 남았다
- 변경: services/investor_trend_5day_service.py(+49 -15), engine/market_schedule.py(+11), tests/services/test_investor_trend_5day_service.py(+152 -4), tests/engine/test_market_schedule_refactor.py(+6), frontend/.../page.regression-jongga-037.test.tsx(+3). `[FLOW-031]`·`[JONGGA-044]` 와 한 커밋을 공유하며 증감은 이 항목에 한 번 반영: 운영 코드 +60 -15, 테스트 +161 -4(코드 합 +202), 문서 +461(계획 334·QA 127)
- 설계: bounded, T3. 2026-09-26 사용자 「FLOW-030 FLOW-031 JONGGA-044 한번에 진행해줘」 뒤 설계를 제시했고 00:25 `/effort high` + 「승인 A로 해」: 키 날짜가 오늘보다 이전일 때만 메모리·SQLite 에 둔다. 오늘 키 값은 조회한 호출과 기다린 호출에만 돌려준다. 실패·거부 값은 오늘 키도 60초 실패 캐시. 참조 SQLite 서명 `v3`. 계획 `docs/superpowers/plans/2026-09-26-flow-030-031-jongga-044.md`
- 리뷰: `.claude/skills/closing-bet-python/SKILL.md`(결측과 실제 0 경계, 해당 없음) · `.claude/skills/closing-bet-nextjs/SKILL.md`·`frontend-skills.md`(테스트 파일만이라 Next 번들 문서는 해당 없음) · 계획 검토 `oh-my-claudecode:critic` ACCEPT-WITH-RESERVATIONS(M1 추세 맵 접미사 `unified_v3`, m2 목록 휴장일 매개변수, m3 탐침 판단 기준 반영, m1 은 아래 메모) · `/ponytail-review` Lean already. Ship. · `closing-bet-reviewer` APPROVE, 최대 medium(F1 → 사용자 선택으로 기준일 호출의 끝날 트리밍 제거, F2 docstring, F3 오늘 키 실패 캐시 검사 추가, F4·F5·F6 조치 없음) · `/review`(T3, `oh-my-claudecode:code-reviewer` opus) APPROVE, Critical·Important 0(M-1 → 사용자 선택으로 휴장일 목록 보충, M-2 주석·메모, N-1~N-3 반영) · 보안 리뷰 보강은 인증·시크릿 경로가 없어 해당 없음
- 검증: FLOW-030 새 검사 RED 3건 → GREEN 4건 · pytest 전체 2873 passed·2 skipped(exit 0, `KRX_ID= KRX_PW=`, 「KRX 로그인」 0회) · vitest 104 files·701 passed · type-check·lint exit 0 · QA 시나리오 8/8 통과(`qa/FLOW-030.md`, 세 항목 공통. 격리 사본의 실제 Flask 라우트와 Next, Toss·Naver HTTP 와 pykrx 모듈만 대역, 소켓 차단 0건. 브라우저 gstack browse: Toss 참조 모달 「-10억」·「-20억」을 그리되 새 사본 toss 캐시 행 0, 수정 전 `f6708cbc` 은 `900271__20260926` 행. 지난 날짜 키 pykrx 참조 「+5억」·「+10억」은 두 코드 모두 저장(서명 v3·v2). CLI H-1: 09-23 17:05 의 오늘 키 값 5억은 저장하지 않고 다음 날 새 프로세스가 7억을 받아 저장, 수정 전은 다음 날 호출 0회에 5억. H-5: 수정 전 장중 행 위에서 새 코드가 다시 물어 7억)
- 정리: 격리 gunicorn(5757·5758)·Next(3757)·browse 종료, 01:25:40 리스너 0·QA 프로세스 0, 사본과 CLI 자료 디렉터리를 리터럴 절대 경로로 삭제, 원본 `data/` 변화 0
- 메모: 오늘 키의 Toss 참조는 저장하지 않으므로 상세 15분 슬롯과 60초 실패 캐시 사이에서 다시 묻는다(F4, 의도한 비용). 저장 판정의 오늘 날짜는 조회를 시작한 시각이라 자정을 넘긴 조회도 두지 않는다. 오늘 키의 60초 재조회(공표 전 당일 행 없음)는 유지했다. 장중·17시의 실제 KRX·Toss 응답은 확인하지 않았다(두 경우 모두 맞게 동작하는 설계). 운영 반영은 gunicorn 재기동이 필요하다(장 중 금지). 참조 서명 `v3`·추세 맵 접미사 `unified_v3` 로 바뀌어 기존 캐시는 첫 조회에서 한 번 다시 받는다. 저장 판정의 시점 이동(N-2)은 `/review` 뒤 변경이라 리뷰어가 보지 않았다(검사로만 확인)

## [FLOW-031] 기준일 실행의 CSV 폴백이 기준일 행 없이 전날로 끝난 5거래일을 플래그 없이 쓰던 결함
- 완료 2026-09-26 01:27 | 티어 T3 (공유 검토) | 커밋 46f569a
- 원인: 기준일 D 실행에서 pykrx 참조가 거부되면 통합 서비스로 넘어가고, `_build_trend_map` 은 `date <= D` 로 거른 CSV 에서 D-1 로 끝나는 5거래일을 만들었다. 영업일 간격이 1 이라 `stale_csv` 도 붙지 않아 그 값이 기준일 5일 값으로 채택되었다. 스크리너의 과거 기준일 배치도 같은 경로다
- 변경: `[FLOW-030]` 과 같은 공유 커밋. 기준일을 준 `_build_trend_map` 은 창 끝 다음 날부터 기준일까지 빈 영업일(`MarketSchedule.known_holidays` 제외)이 있으면 warning 과 빈 맵을 돌려 종목마다 기준일 참조로 가고, 끝쪽 부분 수집일을 빼지 않는다. 추세 맵 접미사 `unified_v3`. `engine/market_schedule.py` 에 2026 하반기 휴장일 9일. 증감은 `[FLOW-030]` 에 합산
- 설계: bounded, T3 공유 검토. 00:25 「승인 A로 해」(안 A 버림). 범위 변경 승인 두 건: 00:5x 「당일 끝날 유지 (권장)」(reviewer F1), 01:0x 「이번에 채움 (권장)」(`/review` M-1)
- 리뷰·검증·정리: `[FLOW-030]` 과 같음. RED 3건 → GREEN 6건, 휴장일 검사 RED(`2026-06-03 True is False`) → GREEN. QA H-2(CSV·pykrx 가 09-22 로 끝나면 새 코드 None·경고 1줄, 수정 전 5억·10억)·H-3(09-23 부분 수집일: 그날 행 있는 종목은 CSV 9억·14억, 없는 종목은 참조 15억, 수정 전 둘 다 전날 끝 5억·10억)·H-4(기준일 온전하면 두 코드 CSV 값)·H-5(수정 전 `unified_v2` 맵을 새 코드가 읽지 않음). CLI 하네스는 스크리너 경로(`get_investor_trend_5day_for_ticker(target_datetime=...)`)를 불렀다. 수집기 경로는 pykrx 를 먼저 봐 창 판정이 가려지기 때문이다
- 메모: 비거래일 기준일은 참조의 끝 날짜 대조에서 늘 실패해 None 이다(`/review` M-2). 스크리너 과거 기준일 배치에서 창 끝과 기준일 사이가 비면 전 종목이 pykrx 를 한 번씩 조회한다. 과거 키라 한 번 뒤에는 저장된다(critic m1). 그날 CSV·pykrx 가 모두 비면 종가베팅은 수급 결측으로 S·A·B 가 없고 「신호 없음」 알림이 나갈 수 있다. 휴장일 목록 가운데 6/3·7/17·8/17 은 로컬 가격 CSV 로 확인했고 9/24·9/25·10/5·10/9·12/25·12/31 은 공휴일 달력으로 채웠다(KRX 휴장일 공고와 대조하지 않음, 원본 CSV 가 09-21 로 끝나 추석도 확인 못함). 목록은 해마다 채워야 한다. 휴장일 목록 보충은 `/review` 뒤 변경이라 리뷰어가 보지 않았다(검사로만 확인)

## [JONGGA-044] 종가베팅 화면의 `[JONGGA-037]` 회귀 검사가 KST 자정 전 약 6분 동안 실패하던 결함
- 완료 2026-09-26 01:27 | 티어 T1 (테스트 파일만, 공유 커밋) | 커밋 46f569a
- 원인: 검사가 `vi.useFakeTimers()` 뒤 350초를 앞당기는데 고정 자료의 날짜는 시작 시각의 KST 오늘이라, 23:54 이후에 돌면 앞당긴 시각이 다음 날이 되어 화면이 「OLD DATA」를 그렸다
- 변경: `beforeEach` 첫 줄에 `vi.setSystemTime(new Date(TODAY()))`(KST 정오 고정, +3). 증감은 `[FLOW-030]` 에 합산
- 설계: bounded. 00:25 `[FLOW-030]` 과 같은 응답
- 검증: `beforeEach` 에 23:58 을 임시로 넣어 1 failed(`UPDATED` 없음) 재현 → 정오 고정 줄을 더해 5 passed → 임시 줄 제거 뒤 5 passed. vitest 전체 104 files·701 passed. 테스트만 바꿔 동적 QA 제외(`tier-rules.md` §1-1)

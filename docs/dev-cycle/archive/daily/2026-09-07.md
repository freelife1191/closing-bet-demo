# 2026-09-07

## [INFRA-034] 감사 초안과 설계의 경계를 밝히고 oh-my-claudecode 를 사이클에 연결한다
- 완료 2026-09-07 00:05 | 티어 문서 | 커밋 b7a2d2e
- 변경: .claude/skills/dev-cycle/SKILL.md, .claude/skills/dev-cycle/references/tier-rules.md,
  .claude/agents/dev-workflow.md 외 3개 (+66 -8, 실행 코드 변경 없음)
- 리뷰: superpowers:brainstorming(bounded 대화 설계, 네 후보 적용 뒤 trace·session_search 제거를
  「추천대로 진행해」로 승인) · oh-my-claudecode:critic(`infra034-critic`, REVISE →
  ACCEPT-WITH-RESERVATIONS, 12건 가운데 11건 반영, 1건은 검토자 철회)
- 검증: 참조 경로·절 번호·frontmatter·TOML·심볼릭 링크 실재 확인 · git diff --stat 실행 코드 0줄 ·
  git diff --check exit 0 · 규약 네 자리 교차 확인 · TODO 티어 재판정 불필요 ·
  QA 2단계 제외(문서 전용, tier-rules §1-1)
- 메모: oh-my-claudecode 5.3.0 의 키워드 감지기는 `team` 을 never-match 로 꺼 두었으므로 감지 목록에
  넣지 않는다. Codex `critic.toml` 은 OKAY·REJECT 둘만 낸다. 이 항목의 TODO 블록은 병렬 codex
  세션의 664b6cc 에 휩쓸렸다가 0eee1ca 에서 지워져 다시 넣은 이력이 있다.

## [CHAT-022] 메모리 전체 동기화가 다른 워커가 저장한 행을 지운다
- 완료 2026-09-07 06:58 | 티어 T2 | 커밋 d990650
- 변경: chatbot/storage_memory_manager.py, chatbot/storage_sqlite_memory.py,
  tests/chatbot/test_memory_owner_access.py 외 3개 (+83 -103, 문서 +133 -3)
- 리뷰: superpowers:brainstorming(bounded 대화 설계, 호출 시 재적재·파괴적 폴백 제거·upsert 전용화를
  「추천안으로 진행」으로 승인) · ponytail-review(호출자 하나뿐인 `_delete_single_entry`·`_clear_storage`
  인라인과 설명 축약, net -15 반영) · feature-dev:code-reviewer(`chat022-reviewer`, 결함 0건. 관찰 A
  실패 시 성공 문구를 돌려주는 한계 → `ponytail:` 주석 반영, 관찰 B diff 밖 `_load()` 의 `None`/`{}`
  미구분 → `[CHAT-028]` 이월, 관찰 C 요청 안 복수 `_reload` → 주석의 상한과 같아 미조치) ·
  qa-only(두 화면 콘솔 0건, 결함 0) · qa(시나리오 5건 실행)
- 검증: pytest 1703 통과 · 2 skip (ponytail 반영 후 재실행) · 새 검사 3건이 구현 전 코드에서 실패함을
  `git stash` 로 대조 · git diff --cached --check exit 0 · QA 시나리오 5/5 통과 (gunicorn 워커 2개,
  add 는 54161, view 8건은 54161·54173 4:4, `/clear all` 은 54173 이 처리)
- 메모: 다중 워커 회귀는 gunicorn 을 `--access-logformat '%(p)s ...'` 로 띄우면 요청별 처리 워커 PID 가
  접근 로그에 남아 증명할 수 있다. `/memory` 명령은 경량 명령이 아니라 시험 신원의 세션과 무료
  사용량(상한 10회)을 소비하므로 QA 는 전용 신원으로 9~10회 안에 짜고 `/clear all` 로 정리한다.
  `/chatbot` 초기 로드는 메모리 경로에 닿지 않으므로 화면 로드만으로는 이 변경이 검사되지 않는다.

## [FE-008] 대시보드 진입 시의 하이드레이션 불일치 제거
- 완료 2026-09-07 07:40 | 티어 문서 | 커밋 190a43b
- 변경: docs/dev-cycle/TODO.md, docs/dev-cycle/qa/FE-008.md (실행 코드 0, 문서 +227 -82)
- 리뷰: superpowers:brainstorming(bounded 대화 설계. 실측으로 서브트리를 특정하고 재현되지 않으면
  증거로 마감하는 안을 「추천안으로 진행」으로 승인) · next 번들 문서 02-guides/streaming.md 와
  01-getting-started/05-server-and-client-components.md(`loading.tsx` 경계의 숨김 세그먼트 교체 흐름을
  0초 DOM 부풀림의 해석에 씀) · tier-rules §5 문서 전용 검증(참조 경로 11개 실재, `git diff --stat`
  실행 코드 0줄) · qa-only(건강 98, 발견 4건 전부 기존 백로그 `[CHAT-007]`·`[FE-029]`·`[FE-022]`·
  `[FE-010]`) · qa(시나리오 8건 실행, 고침 0)
- 검증: browse 실측 — 설계 단계에서 `/dashboard/kr` 전체 로드 5회·클라이언트 전환 10회, `/chatbot`
  전체 로드 30회(개발 서버 재기동, `.next/dev` 삭제 뒤 냉간 컴파일, browse 데몬 재기동, 라우트 첫
  컴파일 조건 포함)에서 하이드레이션 경고 0건 · 운영 빌드(`next start` 3501) 챗봇 5회·대시보드 3회
  0건 · QA 1단계 두 화면 전체 로드 8회·전환 11회·모바일 2회 0건 · QA 2단계 시나리오 8/8 통과
  (필수 6/6: 전체 로드 13회·전환 13회·모바일 2회 0건, `/_next/mcp` 두 조회 빈 배열)
- 메모: 근거로 적혀 있던 「QA 리포트 2026-09-02 ISSUE-001」은 VCP 손절가 문제였고 실제 관찰은 09-02
  `[FE-002]` 실측 1회와 `[VCP-009]` 리포트의 간헐 1회뿐이었다. 챗봇 「안녕하세요, 님」은
  `[CHAT-026]`(b7f790c) 이 프로필 초기값을 서버 렌더와 같은 값으로 바꾸며 이미 사라졌다. 09-05 의
  「`h1` 두 개·버튼 43개」는 Next.js 가 `loading.tsx` 경계 안의 화면을 `<div hidden id="S:0">` 에 실어
  보내고 `$RC` 가 제자리로 옮기기 전에 DOM 을 읽은 것이다(오늘 대시보드 0초 버튼 27 = 보이는 15 +
  숨김 12). 0초 DOM 을 셀 때는 `e.closest("[hidden]")` 로 숨김 조상을 빼거나 `wait --networkidle`
  뒤에 읽는다. 07:20 의 첫 두 로드에서 챗봇 `h1` 이 0초에 둘이었던 것은 30회 중 2회였고 콘솔
  무경고에 어느 조건에서도 재현되지 않았다. 원본 로그는 `.gstack/qa-reports/fe008-hydration-measure-2026-09-07.log`
  와 `fe008-qa-run-2026-09-07.log` 다.

## [CHAT-007] 사이드바 대화 항목을 키보드로 열 수 있게 만든다
- 완료 2026-09-07 08:25 | 티어 T2 | 커밋 d7ab689
- 변경: frontend/src/app/chatbot/page.tsx (+41 -27), frontend/src/app/chatbot/page.regression-chat-007.test.tsx (+103), docs/dev-cycle/TODO.md, docs/dev-cycle/qa/CHAT-007.md, .claude/skills/dev-cycle/references/browser-notes.md (실행 코드 +144 -27, 문서 +180 -7)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「CHAT-007 설계」 추천안 승인) · next 번들 문서 05-server-and-client-components(클라이언트 마크업만 바꿔 경계 변경 없음, 해당 없음) · tier-rules §3(예상 diff 70줄로 TODO 의 T1 을 T2 로 상향, 실제 68줄) · ponytail-review(page.tsx Lean, 테스트 shrink 2·delete 1 net -8 반영) · feature-dev:code-reviewer `chat007-reviewer`(수정 후 승인: 죽은 stopPropagation 제거 반영, z-10 제거는 의도한 정리, 범위 밖 2건 이월) · qa-only(건강 97, 발견 4건 모두 이월) · qa(7/7 통과, 수정 0)
- 검증: vitest 45 파일 288 통과 · tsc 통과 · 새 검사 3/3 · QA 시나리오 7/7 통과 (필수 6/6, 건강 97→97) · /_next/mcp get_errors·get_compilation_issues 빈 배열 · 같은 출처 링크 7/7 200
- 메모: TODO 의 근거 「QA 리포트 2026-09-02 ISSUE-002」는 종가베팅 「Latest Report」 문제라 무관했고 실제 근거는 09-05 네 사이클의 `/qa-only` 관찰이었다. 「답변 위 탭 둘」은 `ChatMessage.tsx` 가 아니라 `page.tsx` 상단 바의 「스마트머니봇」·아바타였다. 대화 항목은 `GET /chatbot/sessions` 가 사용자 메시지가 있는 세션만 돌려주고(`has_meaningful_user_message`), `POST /chatbot/sessions` 로 만든 빈 세션은 목록에 오르지 않으며, 다른 소유자의 `browser_session_id` 로 바꾸는 것은 browser-notes 가 금지하므로 브라우저 프로필에 저장된 대화가 없으면 항목 UI 는 vitest(jsdom) 회귀 테스트로만 검증할 수 있다. 실제 브라우저에서 보려면 본인 프로필로 `/chatbot` 을 열어 Tab·Enter 를 눌러 보면 된다. 설정 대화상자는 닫기 뒤 약 300ms 닫힘 애니메이션이 지나야 `[role=dialog]` 가 사라진다. 이월: 첨부 파일 제거·답변 중단 버튼 이름 `[CHAT-029]`, 접힌 「메뉴」 링크의 탭 순서·「메뉴 닫기」 뒤 초점 소실 `[CHAT-030]`, 공용 사이드바 「+」 버튼 이름 `[FE-029]`, 설정 모달 초점·Escape 는 기존 `[FE-030]`. 원본 로그는 `.gstack/qa-reports/chat007-*.log` 다.

## [VCP-008] 히스토리 날짜 목록과 시그널 조회가 서로 다른 자료를 본다
- 완료 2026-09-07 09:40 | 티어 T2 | 커밋 801688d
- 변경: app/routes/kr_market_data_signals_routes.py, app/routes/kr_market_vcp_signal_helpers.py, services/kr_market_vcp_payload_service.py (실행 코드 +62 -38), tests/app/test_kr_market_data_signals_routes_refactor.py, tests/app/test_kr_market_route_integration.py, tests/services/test_kr_market_vcp_payload_service_refactor.py (+110 -8), docs/dev-cycle/TODO.md, docs/dev-cycle/qa/VCP-008.md, .claude/skills/dev-cycle/references/browser-notes.md (문서 +272 -49)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「VCP-008 설계」 추천안 승인) · frontend-skills(프론트엔드를 건드리지 않아 적용 대상 아님) · tier-rules §2·§3(건드린 세 파일이 위험 경로 목록 밖, 실행 코드 100줄로 T2 유지) · ponytail-review(자체 검토, `bool(safe_bool(...))` 이중 변환 1건 shrink 반영, net -1) · feature-dev:code-reviewer `vcp008-reviewer`(결함 없음·승인 권고, 낮은 확신도 관찰 2건: itertuples 의 비식별자 열 이름 위험은 기존 관례와 맞추어 유지, 오늘 조회 사각지대는 `[VCP-019]` 로 이월) · qa-only(건강 98, 발견 3건 모두 기존 백로그) · qa(8/8 통과, 수정 0)
- 검증: pytest 1706 통과·2 스킵 exit 0 · vitest 45 파일 288 통과 exit 0 · 새 검사만 골라 돌린 `-k` 실행 9 통과 · QA 시나리오 8/8 통과 (필수 7/7, 건강 98→98) · 같은 오리진 링크 7/7 200 · 콘솔 오류 0건
- 메모: TODO 에 적혀 있던 원인 「날짜 목록은 종가 계열 파일에서 만들어지고 시그널 조회는 VCP 파일을 찾는다」는 사실이 아니었다. 두 경로 모두 `data/signals_log.csv` 를 읽으며, 어긋나는 것은 파일이 아니라 판정 기준이었다. 날짜 리더는 `signal_date` 열만 보고, 시그널 변환은 `status != "OPEN"`·`score < 60`·`is_vcp` 거짓인 행을 버린다. 실제 CSV 15행이 전부 `is_vcp` 가 빈 값이라 조회가 늘 0건이었다. 세 조건을 `_is_vcp_signal_row` 로 뽑아 양쪽이 공유하게 했고, 그 결과 날짜 목록이 `["2026-05-05"]` 에서 `[]` 로 바뀌었다. 두 번째 결함인 「히스토리 탭에 이유가 없다」는 `_resolve_stale_warning_message` 가 `req_date` 가 있으면 곧바로 `None` 을 돌려준 탓이었고, 같은 함수의 판정 입력을 「필터를 통과한 행」에서 「변환된 시그널」로 옮겨 함께 고쳤다. 행이 남아도 판정에서 전부 떨어지는 경우가 실제 자료의 상태이기 때문이다. 화면은 이미 `stale_warning` 을 배너로 렌더하고(`page.tsx:1358`) 드롭다운에도 「데이터 파일 없음」 이 있어서(`:1451`) 프론트엔드는 한 줄도 바꾸지 않았다. 실측 중 `date=not-a-date` 가 문구에 그대로 실리는 것을 발견해 「요청한 날짜에 …」 로 바꾸고 회귀 검사를 더했다. QA 에서 히스토리 날짜를 눌러 보는 조작은 만들 수 없었다. 목록에 날짜를 만들려면 CSV 에 판정을 통과하는 행이 있어야 하는데 그 상태를 만드는 화면 수단이 「Refresh VCP」 와 「실패 AI 재분석」 뿐이고 둘 다 되돌릴 수 없는 조작이며 `data/` 는 읽기 전용이다. 그래서 API 응답으로 검사하고 그 전제를 QA 문서에 남겼다. 이월: 오늘 조회의 같은 사각지대 `[VCP-019]`, 헤더 홈 링크와 사이드바 「+」 버튼 이름은 기존 `[FE-029]`, 모바일 빈 상태 문구는 기존 `[FE-015]`. 원본 리포트는 `.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-008.md` 다.

## [VCP-019] 날짜를 지정하지 않는 시그널 조회에는 빈 이유를 알리지 못한다
- 완료 2026-09-07 10:10 | 티어 T2 | 커밋 4197e07
- 변경: services/kr_market_vcp_payload_service.py (실행 코드 +8 -3), tests/services/test_kr_market_vcp_payload_service_refactor.py (+53), docs/dev-cycle/TODO.md, docs/dev-cycle/qa/VCP-019.md, .claude/skills/dev-cycle/references/browser-notes.md (문서 +200 -35)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「VCP-019 설계」 추천안 승인) · frontend-skills(프론트엔드를 건드리지 않아 적용 대상 아님) · tier-rules §2·§3(건드린 파일이 위험 경로 목록 밖, TODO 의 T2 유지) · ponytail-review(자체 검토, 테스트 람다의 `**_kwargs` 를 파일 관례에 맞춰 제거) · feature-dev:code-reviewer `vcp019-reviewer`(차단 결함 없음·승인 권고, 관찰 2건 중 확신도 중간인 `>=` 갈래 지적을 반영) · qa-only(건강 98, 발견 3건 모두 기존 백로그) · qa(8/8 통과, 수정 0)
- 검증: pytest 1708 통과·2 스킵 exit 0 · vitest 45 파일 288 통과 exit 0 · 새 검사만 골라 돌린 `-k` 실행 2 통과 · QA 시나리오 8/8 통과 (필수 7/7, 건강 98→98) · 같은 오리진 링크 7/7 200 · 콘솔 오류 0건
- 메모: `_resolve_stale_warning_message` 는 호출부가 시그널 0건을 확인한 뒤에 부르는데도, 날짜를 지정하지 않은 갈래에서 원본 CSV 의 최신 `signal_date` 만 보고 `latest >= today` 이면 곧바로 `None` 을 돌려주었다. 그래서 오늘 자 행이 있는데 `status`·`score`·`is_vcp` 판정에서 전부 떨어지면 화면이 이유 없이 빈 표만 보였다. `[VCP-008]` 이 날짜를 지정한 갈래에서 같은 구조를 고쳤고 남은 것이 이 갈래였다. code-review 가 확신도 중간으로 지적한 대로 `>=` 를 `==` 로 좁혀 오늘과 미래를 갈랐다. 미래 날짜를 오늘과 묶으면 뒷절이 사라지는데, 같은 판정을 쓰는 `/api/kr/signals/dates` 는 그 날짜를 드롭다운에 보이므로 배너만 침묵하면 둘이 어긋난다. 확신도 낮은 다른 관찰(`pd.to_datetime` 과 `_format_signal_date` 의 파서 차이)은 `signal_date` 가 `YYYYMMDD` 정수일 때만 어긋나는데 저장 경로 두 곳이 모두 `strftime("%Y-%m-%d")` 를 쓰므로(`engine/screener_result_builders.py:51`, `engine/signal_tracker_analysis_mixin.py:310`) 재현 경로가 없어 이월하지 않았다. QA 는 `data/signals_log.csv` 의 `signal_date` 열만 바꾼 사본을 scratchpad 에 만들고 그 디렉터리를 작업 디렉터리로 삼아 백엔드를 다시 띄우는 방법으로 오늘·미래 갈래를 실측했다. `app/routes/kr_market.py:98` 의 `DATA_DIR` 이 `'data'` 로 고정되어 있어 `engine/config.py` 의 `DATA_DIR` 환경변수로는 바꿀 수 없다는 것을 그 과정에서 확인했고, 그 방법을 browser-notes 에 남겼다. 미래 날짜 행 10개가 판정을 통과하는 자료에서는 드롭다운에 `2026-12-01` 이 나타나고 배너도 같은 날짜를 말하는 것을 화면으로 확인했다. 화면 코드는 한 줄도 바꾸지 않았다. 이월: 헤더 홈 링크와 사이드바 「+」 버튼 이름은 기존 `[FE-029]`, 모바일 빈 상태 문구는 기존 `[FE-015]`. 원본 리포트는 `.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-019.md` 다.

## [VCP-018] legacy 분석 파일의 GPT 추천이 VCP 화면에 닿지 못한다
- 완료 2026-09-07 11:40 | 티어 T2 | 커밋 3d7332d
- 변경: app/routes/kr_market_vcp_signal_helpers.py (실행 코드 +18 -8), tests/app/test_kr_market_vcp_signal_helpers_refactor.py (+101), docs/dev-cycle/TODO.md, docs/dev-cycle/qa/VCP-018.md (문서 +285 -4)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「보강 함수를 어느 범위까지 고칠까요」 추천안 승인) · frontend-skills(프론트엔드를 건드리지 않아 적용 대상 아님) · tier-rules §2·§3(건드린 파일이 위험 경로 목록 밖, TODO 의 T2 유지) · ponytail-review(자체 검토, 주석 중복과 낡은 docstring 두 곳을 다듬음) · feature-dev:code-reviewer `vcp018-reviewer`(차단 결함은 없으나 커밋 전 판단이 필요한 지적 2건, 확신도 중간짜리 회귀를 재현해 반영) · qa-only(건강 94, 발견 2건 모두 기존 백로그) · qa(8/8 통과, 수정 0)
- 검증: pytest 1711 통과·2 스킵 exit 0 · vitest 288 통과 exit 0 · 새 검사 3개를 종전 구현과 회귀 구현에 각각 걸어 방벽 확인 · QA 시나리오 8/8 통과 (필수 7/7, 건강 94→94) · 같은 오리진 링크 7/7 200 · 콘솔 오류 0건
- 메모: `_merge_legacy_ai_fields_into_map` 이 `perplexity` 와 `gemini` 두 필드만 보강하고 `gpt_recommendation` 을 빠뜨리고 있었다. `data/` 의 분석 파일에서 프로바이더별 판정 건수를 세어 보면 2026-02-11 은 gpt 2건, 02-12~02-20 은 perplexity 3~5건, 02-26 은 둘 다 0건, 05-05 와 최신 legacy 는 gpt 4건이다. 두 번째 프로바이더가 한 방향으로 이주한 것이 아니라 `VCP_SECOND_PROVIDER` 와 `PERPLEXITY_API_KEY` 를 읽어 실행 시점에 정해지고 그에 따라 오간다. 어느 필드가 쓰일지 설정이 정하므로 보강 목록이 부분집합이면 안 되는데, 그 목록만 함수가 만들어진 시점(`45ad2a6`)에 멈춰 gpt 가 빠져 있었다. perplexity 도 과거 자료와 화면 탭이 아직 쓰므로 죽은 코드가 아니다. (첫 커밋 `3d7332d` 의 메시지는 이 대목을 「perplexity 에서 gpt 로 바뀌었다」 로 적었는데, 그것은 각 파일의 키 존재만 세고 값이 채워졌는지 세지 않은 집계였다. code-review 의 정정을 받아 다시 세어 바로잡는다.) 세 필드를 모듈 상수 `_AI_RECOMMENDATION_FIELDS` 하나로 모아 보강 함수와 병합 함수가 같은 목록을 보게 했다. 처음에는 넣을지 판정하는 기준도 `_is_valid_ai_recommendation` 으로 통일했는데, code-review 가 그것이 회귀를 낳는다고 확신도 중간으로 지적했다. 시그널의 `gemini_recommendation` 은 `_build_vcp_signal_from_row:282` 가 CSV 행에서 만들기 때문에 오늘 JSON 이 실패 기록이어도 시그널은 정상인 상태가 성립하고, 그때 보강이 legacy 의 지난 판정을 맵에 넣으면 이어지는 병합이 그 값으로 오늘 판정을 덮어쓴다. 재현하니 오늘 BUY 가 지난 달 SELL 로 바뀌었다. 사용자 승인을 받아 판정 기준을 종전 「비어 있을 때만」 으로 되돌리고 세 필드 대칭만 남겼다. 화면의 `getAIBadge` 는 병합이 끝난 값을 보고 legacy 를 고르므로 그 자리에서 이미 같은 일을 안전하게 한다. QA 는 원본 `data/` 를 심볼릭 링크로 건 사본 디렉터리를 만들고 세 파일만 실제 사본으로 바꾸어 검사했으며, 「고치기 전」 값은 `git worktree` 로 `ccd15d5` 를 체크아웃해 그 디렉터리를 작업 디렉터리로 삼아 읽었다. `PYTHONPATH` 만 바꾸는 방법은 gunicorn 에서 적용되지 않아 원본 코드가 그대로 적재된다는 것을 표식 로그로 확인했다. 고치기 전에는 상세 모달의 AI 탭이 「Gemini」 하나뿐이고 표의 GPT 열이 전부 `-` 였다. 화면 코드는 한 줄도 바꾸지 않았다. 이월: 교차 날짜 유입은 새 `[VCP-020]`, 같은 필드 목록이 저장소 네 곳에 더 있는 것은 새 `[VCP-021]`, VCP 상세 모달의 Escape·`role`·닫기 버튼 이름은 기존 `[FE-030]`·`[FE-017]` 에 덧붙였고, 모바일 빈 상태 문구는 기존 `[FE-015]`. 원본 리포트는 `.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-018.md` 다.

## [JONGGA-008] 백엔드가 확신도 없음을 0 으로 표현하는 자리를 정리
- 완료 2026-09-07 12:50 | 티어 T2 | 커밋 298306c
- 변경: engine/pandas_utils_safe.py, engine/llm_analyzer_parsers.py, engine/signal_tracker_ai_helpers.py, app/routes/kr_market_jongga_ai_payload_helpers.py, app/routes/kr_market_jongga_normalize_helpers.py, app/routes/kr_market_jongga_reanalysis_helpers.py, app/routes/kr_market_vcp_signal_helpers.py, frontend/src/lib/aiConfidence.ts(신규), frontend/src/lib/api.ts, frontend/src/app/dashboard/kr/vcp/page.tsx, frontend/src/app/dashboard/kr/closing-bet/page.tsx (실행 코드 +100 -45), tests/app/test_ai_confidence_missing_value.py(신규 +235), frontend/src/lib/aiConfidence.test.ts(신규 +22), frontend/src/app/dashboard/kr/vcp/page.regression-jongga-008.test.tsx(신규 +89), 기존 검사 3건 기대값 갱신, docs/dev-cycle/TODO.md, docs/dev-cycle/qa/JONGGA-008.md(신규 +194)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 「확신도 정규화 규칙을 한 벌로 합칠까요」 추천안 「위험 경로 밖에 새 헬퍼」 승인) · frontend-skills(번들 문서 `01-app/01-getting-started/05-server-and-client-components.md` — 두 화면 모두 `'use client'` 컴포넌트이고 변경이 그 경계 안쪽의 렌더 로직이다. 프롭 개수·`useEffect`·버전 어느 조건에도 걸리지 않아 별도 스킬 없음) · tier-rules §2·§3(건드린 파일 전부가 위험 경로 목록 밖, T2 유지) · ponytail-review(자체 검토 3건 — 잉여 `None` 검사 삭제, 코드를 되풀이하던 주석 축약, 두 화면에 두 벌이던 확신도 파싱을 `frontend/src/lib/aiConfidence.ts` 로 통합) · feature-dev:code-reviewer `jongga008-reviewer`(차단 1건·결함 2건·놓친 자리 1건, 전부 처리) · qa-only·qa(필수 8/8, 선택 1/1 통과, 수정 0)
- 검증: pytest 1741 통과·2 스킵 exit 0 · vitest 47 파일 299 통과 exit 0 · type-check exit 0 · 새 검사를 `3aa0ef7` 코드에 걸어 파이썬 9건과 화면 1건이 실패하는 것을 확인 · QA 시나리오 9/9 통과 · 콘솔 오류 0건
- 메모: `[JONGGA-004]` 가 종가베팅 화면 쪽만 고치고 백엔드는 그대로 두어, 확신도가 없는 종목에 화면이 「AI 가 0% 확신한다」는 막대를 그렸다. TODO 는 자리를 다섯 곳으로 적었으나 조사에서 두 곳을 더 찾아 일곱 곳이 되었다. 추가한 것은 `kr_market_vcp_signal_helpers.py:121`(재분석 결과를 CSV 로 되쓰는 자리)과 `engine/signal_tracker_ai_helpers.py:107`(`apply_ai_results`)이다. 뒤엣것이 signals_log 의 `ai_confidence` 열을 만드는 원천이라, 놓치면 나머지 여섯 자리는 이미 0 이 적힌 CSV 를 읽게 된다. TODO 의 서술 셋도 실제와 달랐다. 적힌 행 번호가 전부 밀렸고, 재사용하라던 `_normalize_confidence_value` 는 변환 실패 시 `int(default)` 를 실행해 `default=None` 을 받지 못하며, 「실패 판정 기준이 어긋날 수 있다」는 우려는 `_is_valid_ai_verdict` 가 `ai_action` 과 `ai_reason` 만 읽고 `ai_confidence` 를 아예 보지 않으므로 근거가 없었다. 기존 `_safe_int` 도 쓸 수 없다. 본문이 `int(float(value or 0))` 이라 `_safe_int(None, default=None)` 이 None 이 아니라 0 을 돌려준다. 헬퍼를 `engine/pandas_utils_safe.py` 에 둔 것은 `engine/vcp_ai_analyzer_helpers.py` 가 위험 경로 「VCP 판정」이라 한 줄만 고쳐도 T3 이 되기 때문이며, 사용자가 T2 유지를 골랐다. 그 대가로 확신도 정규화 규칙이 두 벌 남았고 `[VCP-022]` 로 이월했다. code-review 가 잡은 결함 하나가 중요하다. `safe_confidence` 가 `inf` 에서 `OverflowError` 를 냈는데 그것은 `ValueError` 가 아니라 `ArithmeticError` 의 하위라 예외 목록에 걸리지 않았다. `json.loads` 가 `Infinity` 를 예외 없이 통과시키므로 LLM 응답으로 실제 들어올 수 있고, 새던 자리가 `engine/llm_analyzer_parsers.py` 라 종목 하나 때문에 배치 전체가 AI 분석을 잃는 경로였다. 종전 `item.get("confidence", 0)` 은 결코 예외를 내지 않았으므로 이번에 새로 생긴 실패 방식이었다. 예외 목록을 넓히고 회귀 검사 4건을 더했다. 기존 검사 세 건의 기대값을 갱신했는데 셋 다 「실패하면 확신도 0」이라는 옛 계약을 확인하던 것이다. `test_kr_market_route_integration.py` 는 CSV 의 확신도를 `int()` 로 읽다가 실패 행이 결측을 담으면서 열이 float 이 되어 깨졌는데, 실제 `data/signals_log.csv` 는 이 열 15 행이 전부 비어 있어 이미 float 으로 읽히므로 정수 열을 만들던 것은 검사 픽스처 쪽이었다. QA 는 원본 `data/` 를 링크로 건 사본 디렉터리에 `signals_log.csv` 와 `jongga_v2_latest.json` 두 파일만 실제 사본으로 두고 그 디렉터리를 작업 디렉터리로 삼았다. VCP 픽스처 종목은 `ai_analysis_results_20260505.json` 밖에서 골라야 했다. 그 파일이 담은 네 종목은 `_merge_ai_data_into_vcp_signals` 가 파일의 판정으로 CSV 판정을 덮어써서 CSV 갈래를 검사할 수 없다. 「고치기 전」 값은 `git worktree` 로 `3aa0ef7` 을 체크아웃하고 같은 자료 사본을 그 코드에 물려 읽었으며, 갈래를 전환할 때마다 사본의 `data/runtime_cache.db` 를 지웠다. 지우지 않은 첫 측정에서 옛 코드가 `confidence=None` 을 돌려주어 캐시를 의심했고 지운 뒤 `0` 으로 바뀌었다. 실측 대조는 VCP 상세 모달이 `0%` → `미산출`, 종가베팅 S-Oil 카드가 `0%` → `미산출` 이다. 계획한 시나리오 중 하나는 실측과 달랐다. 사유 문자열 갈래(로보티즈)를 회귀로 잡았으나 고치기 전에도 「미산출」이었으므로 인접으로 고쳐 적었다. 그 자리를 고친 것은 유지했다. 응답을 만드는 다른 소비자가 같은 값을 읽기 때문이다. 이월: 탭에 따라 게이지가 갈리는 문제와 `RecommendationCombiner.combine` 의 0 은 새 `[VCP-022]`.

## [FE-015] 모바일에서 VCP 표의 빈 상태 문구가 화면 밖에 놓인다
- 완료 2026-09-07 14:20 | 티어 T1 | 커밋 e5ea758
- 변경: frontend/src/app/dashboard/kr/vcp/page.tsx (실행 코드 +15 -14), frontend/src/app/dashboard/kr/vcp/page.regression-fe-015.test.tsx (신규 +59), docs/dev-cycle/TODO.md, docs/dev-cycle/qa/FE-015.md (신규)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 두 건 승인 — 「수정 범위」에 「VCP 표만 고치고 나머지는 이월」, 「문구 번역」에 「영어 그대로 둔다」) · frontend-skills(번들 문서 `01-app/01-getting-started/11-css.md` — Tailwind 유틸리티만 쓰는 변경이라 PostCSS 설정과 무관함을 확인. 프롭 개수·`useEffect`·버전 어느 조건에도 걸리지 않아 별도 스킬 없음) · tier-rules §2·§3(위험 경로 63 개 파일이 전부 파이썬이고 화면 코드는 목록 밖, 실행 코드 29줄로 T1 유지) · ponytail-review(자체 검토 2건 — 검사의 래핑 람다를 `vi.mocked` 로, 토큰 루프를 `expect.arrayContaining` 으로, net -5) · qa-only·qa(필수 7/7, 선택 1/1 통과, 수정 0)
- 검증: vitest 48 파일 302 통과 exit 0 · type-check exit 0 · 새 검사 2건을 고치기 전 코드에 걸어 둘 다 실패하는 것을 확인 · QA 시나리오 8/8 통과 · 콘솔 오류 0건 · 페이지 가로 스크롤 없음(375·1440 양쪽)
- 메모: 표가 `min-w-[1000px]` 이라 `colSpan` 셀도 그 폭을 차지하고, `text-center` 의 중심이 x 좌표 515 에 놓여 375px 화면 밖으로 나갔다. TODO 가 적은 빈 상태 옆에 로딩 상태가 같은 구조로 하나 더 있어 함께 고쳤다. **첫 구현을 브라우저 실측이 물리쳤다.** 안쪽을 `sticky left-0 w-screen max-w-full` 로 묶어 스크롤 컨테이너 왼쪽에 붙이는 방법이 모바일에서는 통했지만(중심 205, 화면 안), 데스크톱 1440px 에서 문구가 오른쪽으로 잘렸다(중심 1009, `visible: false`). `w-screen` 은 사이드바를 포함한 100vw 라 `td` 가 1440px 로 늘어나고, 그 결과 `max-w-full` 의 기준까지 함께 늘어나 제한이 무력해진다. TODO 가 제시한 첫 번째 선택지대로 안내 문구를 `overflow-x-auto` 영역 밖으로 옮겨 카드 폭을 기준으로 정렬되게 고쳤다. `tbody` 의 3항 연산자를 `{loading || signals.length === 0 ? null : (` 로 바꾸어 `signals.map` 블록의 들여쓰기를 건드리지 않았고, 그래서 diff 가 29줄에 머물렀다. 회귀 검사의 계약도 방법을 바꾸면서 함께 바꿨다. 처음에는 클래스 문자열 네 개를 확인했으나 지금은 `text.closest('.overflow-x-auto')` 가 `null` 인지만 본다. 결함에 더 가까운 계약이다. **QA 준비 중 원본 `data/signals_log.csv` 를 훼손했다가 복구했다.** 사본 디렉터리에 `ln -sf` 로 건 링크에 pandas 로 썼고 그 쓰기가 링크를 따라갔다. 이 파일은 `.gitignore` 대상이라 Git 으로 되돌릴 수 없어, 지난 라운드가 scratchpad 에 남긴 QA 사본에서 `ticker` 의 여섯 자리 zero-padding 과 `signal_date` 를 되찾고 훼손된 파일과 나머지 15 개 열이 일치하는 것을 대조해 복구했다. `signal_date` 가 15 행 전부 `2026-05-05` 라는 사실은 `qa-report-...-vcp-019.md:64` 가, `is_vcp` 가 전부 비어 있다는 사실은 09-01·09-02·09-07 리포트 네 건이 기록해 두었다. 복구 뒤 백엔드가 훼손 전과 같은 경고 문구를 돌려주는 것으로 확인했다. **사본을 만들 때는 링크를 먼저 지우고 `cp` 로 실제 파일을 만든 뒤 편집해야 하고, 편집 직전에 `os.path.islink` 로 확인하고 전후 SHA-256 을 대조해야 한다.** 두 번째 시도는 그렇게 해서 원본을 건드리지 않았다. 사본 백엔드는 `gunicorn --chdir` 로 띄운다. `restart_all.sh` 가 gunicorn 을 쓰므로 `pkill -f flask_app.py` 로는 잡히지 않는다. 이월: 같은 결함이 남은 다른 화면 네 자리는 새 `[FE-040]`, `data/` 에 쌓인 SQLite WAL·SHM 11,569 쌍은 새 `[INFRA-035]`.

## [VCP-011] VCP 상세의 AI 요약이 표·차트와 다른 수축비율과 점수를 말한다
- 완료 2026-09-07 15:00 | 티어 T2 | 커밋 16f8b2b
- 변경: engine/pandas_utils_safe.py, engine/signal_tracker_ai_helpers.py, app/routes/kr_market_vcp_signal_helpers.py, frontend/src/app/dashboard/kr/vcp/page.tsx, frontend/src/lib/api.ts (실행 코드 +92 -29), tests/app/test_kr_market_helpers_contract.py, tests/engine/test_signal_tracker_ai_helpers_refactor.py, frontend/src/app/dashboard/kr/vcp/page.regression-vcp-011.test.tsx (신규) (검사 +191 -2), docs/dev-cycle/TODO.md, docs/dev-cycle/qa/VCP-011.md (신규)
- 리뷰: superpowers:brainstorming(bounded, AskUserQuestion 두 건 승인 — 「프롬프트 표기」에 「payload 에서 키를 빼 N/A 로」, 「고정 서술문」에 「함께 고친다」) · frontend-skills(번들 문서 `01-app/01-getting-started/05-server-and-client-components.md` — 대상이 `'use client'` 컴포넌트이고 변경이 그 경계 안쪽의 렌더 로직이다. 프롭 개수·`useEffect`·버전 어느 조건에도 걸리지 않아 별도 스킬 없음) · tier-rules §2·§3(건드린 다섯 파일 전부 위험 경로 목록 밖, 실행 코드 121줄로 T2) · ponytail-review(지적 3건 전부 반영 — 필드 목록 사본을 지우고 engine 에서 import, `safe_optional_float` 의 죽은 문자열 분기 삭제, 목록 동등성 검사 삭제. 삽입 269줄 → 240줄) · feature-dev:code-reviewer `vcp011-reviewer`(잔여 결함 1건 확신도 상, 이월 판정. 나머지 네 확인은 문제 없음) · qa-only·qa(필수 7/7, 선택 2/2 통과, 수정 0)
- 검증: pytest 1745 통과·2 스킵 exit 0 · vitest 49 파일 304 통과 exit 0 · type-check exit 0 · 새 검사를 `7bcdb15` 코드에 걸어 파이썬 6건과 화면 2건이 실패하는 것을 확인하고 복구 후 SHA-256 대조 · QA 시나리오 9/9 통과 · 콘솔 오류 0건(애플리케이션 기준) · 페이지 가로 스크롤 없음(375·1440 양쪽)
- 메모: 원인이 셋이었다. **첫째, 프롬프트가 없는 값을 0 으로 지어냈다.** `data/signals_log.csv` 의 열 21 개에 `foreign_1d`·`inst_1d` 가 없는데 `_build_vcp_stock_payload` 가 `row.get("foreign_1d", 0)` 으로 읽어 언제나 `0.0` 을 실었고, `build_vcp_prompt` 가 그것을 「외국인 1일(오늘) 순매수: 0.0주」라고 적은 뒤 바로 다음 줄에서 「오늘의 변화를 중요하게 고려하십시오」라고 지시했다. `data/kr_ai_analysis_20260505.json` 의 Gemini 사유가 실제로 「당일 외국인과 기관의 순매수 규모가 0.0으로 집계되어 … 단기적인 수급 공백」이라며 관망을 권했다. 잠재 문제가 아니라 이미 판정을 바꾼 문제다. **둘째, payload 생성기가 두 벌인데 서로 달랐다.** 배치 경로(`build_ai_batch_payload`)는 그 두 열을 아예 싣지 않아 `N/A` 가 되고 `current_price` 자리에 `entry_price` 를 넣었다. 같은 화면인데 어느 경로가 돌았느냐로 프롬프트 입력이 달랐다. 게다가 `vcp_score` 를 기본값 0 으로 실어 「VCP 패턴 보조 점수: 0점」이라는 거짓 문장을 만들었다(`build_vcp_prompt` 는 `("", None, "N/A")` 만 걸러내므로 0 은 통과한다). **셋째, 점수 어긋남의 절반은 이름 문제였다.** `score`(종합 시그널 점수 0~100)와 `vcp_score`(VCP 패턴 보조 점수 0~20)는 다른 값인데 모달 카드가 `score` 를 「VCP Score」라고 불렀다. SK 의 CSV 행은 `score` 100, `vcp_score` 17.0 이므로, 요약문의 「VCP 패턴 보조 점수 3.0점」과 카드의 `100.0` 은 애초에 다른 값을 가리키고 있었다. `safe_optional_float` 을 `engine/pandas_utils_safe.py` 에 두어 「값이 없음」과 「값이 0」을 구분하고, 두 생성기가 `AI_PROMPT_NUMERIC_FIELDS` 한 목록을 함께 쓰게 했다. 헬퍼를 그 파일에 둔 것은 `[JONGGA-008]` 이 `safe_confidence` 를 둔 것과 같은 이유다. `engine/vcp_ai_analyzer_helpers.py` 가 위험 경로라 한 줄만 고쳐도 T3 이 되는데, 사용자가 이번에도 T2 유지를 골랐다. **`1.8811` 의 출처는 복구하지 못했다.** `data/` 를 전수로 훑었으나 SK 의 수축비율로 그 값을 가진 자료 파일이 없고 그 숫자는 AI 사유 본문에만 남아 있다. 프롬프트에 넘긴 값을 어디에도 남기지 않기 때문이며, 그것이 TODO 의 두 번째 체크박스가 가리킨 구멍이다. 새 검사 `test_vcp_prompt_numbers_match_the_values_the_screen_shows` 가 화면 payload 와 프롬프트 문자열을 직접 대조해 그 구멍을 막는다. **이미 저장된 옛 사유 본문은 그대로 남는다.** `data/` 는 읽기 전용이고 다시 만들려면 AI 호출이 필요한데 그것은 금지된 조작이다. 그래서 이번 라운드가 보장하는 것은 「앞으로 만들어지는 프롬프트가 표와 같은 숫자를 싣는다」와 「화면이 그 숫자를 제 이름으로 부른다」 둘이다. **QA 를 위해 사본 환경이 필요했다.** 현재 `signals_log.csv` 는 `is_vcp` 가 15 행 전부 비어 있어 `_is_vcp_signal_row` 가 모두 걸러내고 `/api/kr/signals` 가 `count: 0` 을 돌려준다. 그 상태로는 상세 패널을 열 수 없다. 지난 라운드에서 링크에 써서 원본을 훼손한 사고가 있었으므로, 이번에는 링크를 아예 만들지 않고 `cp` 로 실제 파일을 만들었고 편집 직전에 `os.path.islink` 로 확인했으며 원본 SHA-256 을 세 번 대조했다. 사고는 없었다. 실측 대조값은 표의 `Cont.` 열 `0.41`, 상세 패널 수축비율 `0.41`, 카드 「종합 시그널 점수 100.0」·「VCP 패턴 보조 점수 17.0」, 프롬프트 `수축 비율: 0.406`·`외국인 1일(오늘) 순매수: N/A주` 다. **code-review 가 잔여 결함 하나를 확신도 상으로 지적했다.** `build_vcp_rule_based_recommendation` 이 같은 방식으로 없는 값을 0 으로 채워 판정과 사유 문장에 쓴다. 같은 파일이 위험 경로라 이번 라운드에서 열지 않기로 하고 `[VCP-023]` 으로 이월했다. 그 라운드가 이 결함을 악화시키지 않았다는 것도 확인했다. 고치기 전에도 폴백이 보는 값은 `0.0` 이었다. 예비로 띄운 두 번째 리뷰어(`oh-my-claudecode:code-reviewer`)는 두 번 모두 회신 없이 잠들어 결과를 얻지 못했으나, 지정 도구인 `feature-dev:code-reviewer` 가 완전한 검토를 냈으므로 리뷰 단계는 충족되었다. 이월: 폴백의 지어낸 0 은 새 `[VCP-023]`, 스크리너가 구한 1일 수급이 `signals_log.csv` 까지 오지 못하는 것은 새 `[VCP-024]`.

### [VCP-011] 아카이브 뒤 보정 — 커밋 5d3b8f5
- 보정 2026-09-07 15:10 | 티어 T2 유지
- 아카이브 커밋 `50a8e3a` 를 남긴 **뒤에** 예비 리뷰어(`oh-my-claudecode:code-reviewer` `vcp011-review2`)의 결과가 도착했다. 앞의 기록에 「결과를 얻지 못했다」고 적었는데, 그 뒤 회신이 왔으므로 여기에 사실을 덧붙인다. 지적 가운데 둘이 범위 안이었고 사실로 확인되어 이어서 고쳤다.
- 보정 하나: **새로 만든 `safe_optional_float` 이 이미 있던 함수의 사본이었다.** `app/routes/kr_market_signal_common.py:86` 의 `_safe_optional_float` 이 같은 목적으로 이미 있었고 `kr_market_jongga_grade_helpers.py` 가 쓰고 있었다. 게다가 두 사본은 동작이 달랐다. 기존 것은 `"475,500원"` 을 475500.0 으로 읽지만 NaN·inf 를 막지 않고, 새 것은 그 반대였다. 이 항목이 payload 생성기에서 없애려던 「같은 것을 가리키는 두 벌이 서로 다르게 동작한다」를 헬퍼 층에 다시 만든 셈이다. ponytail 의 사다리 2 번(이미 이 저장소에 있는가)을 건너뛴 결과이며, 첫 `/ponytail-review` 도 잡지 못했다. 기존 함수가 수치 변환을 `engine.pandas_utils_safe.safe_optional_float` 에 맡기고 통화 표기 제거만 제 몫으로 남기게 고쳤다. 그 덕에 종가베팅 등급 경로도 NaN·inf 를 막게 되었다.
- 보정 둘: **화면과 프롬프트의 `vcp_score` 가 아직 어긋났다.** `_build_vcp_signal_from_row` 가 `_safe_int(vcp_score, default=0)` 으로 소수를 버렸다. 프롬프트는 실수 원값을 싣도록 고쳤으므로 보조 점수가 16.5 인 행은 화면이 `16.0`, AI 사유가 `16.5점` 을 말하게 된다. 첫 커밋이 TODO 의 「VCP 점수의 출처를 표·차트와 같은 자리로 통일」 체크박스를 `[x]` 로 표시했으나 실제로는 절반만 닫혀 있었다. `safe_optional_float` 로 바꾸고 기본값 `0` 도 없앴다. 그 기본값 때문에 값이 없는 행까지 `0.0` 으로 그려져 화면의 `?? '-'` 갈래에 영영 닿지 못했고 `frontend/src/lib/api.ts` 의 `vcp_score?: number` 선언과도 어긋나 있었다.
- 재검증: 사본의 SK 행 `vcp_score` 를 16.5 로 두고 다시 실측해 API 응답·상세 패널 카드·프롬프트·배치 payload 가 모두 `16.5` 임을 확인했다. 고치기 전이라면 카드가 `16.0` 이었다. pytest 1746 통과·2 스킵 exit 0 · vitest 49 파일 305 통과 exit 0 · type-check exit 0(첫 시도에서 새 검사의 `market` 리터럴 타입 오류를 잡아 고쳤다) · 새 검사 둘을 `16f8b2b` 코드에 걸어 실패를 확인 · 원본 `data/signals_log.csv` 해시 그대로.
- 이월: 폴백의 `contraction_ratio` 기본값이 `1.0` 이라 키 제거로 매수 관문이 함께 바뀐다는 지적은 `[VCP-023]` 에 덧붙였다. 그 함수가 위험 경로 안이기 때문이다.
- 절차에 관해 남길 것: **예비 리뷰어를 띄운 판단 자체는 낭비였다.** 지정 도구인 `feature-dev:code-reviewer` 가 `idle` 로 보이면서도 결과를 늦게 batched 로 보냈을 뿐인데, 유실로 단정하고 두 번째를 띄웠다. 그런데 그 두 번째가 첫 번째가 놓친 두 가지를 잡았다. 다음 라운드에서 `idle` 을 유실로 읽지 않는 것과, 리뷰를 하나만 두는 것 사이의 균형은 다시 볼 필요가 있다.

## [INFRA-036] 백로그 현행화 — 해소된 항목을 지우고 같은 원인의 항목을 합친다
- 완료 2026-09-07 15:45 | 티어 문서 | 커밋 16e75e9
- 변경: docs/dev-cycle/TODO.md (+525 -902, 실행 코드 변경 없음)
- 리뷰: superpowers:brainstorming(bounded, 정리안을 제거·판단 필요·병합·정정 네 묶음으로 제시하고 AskUserQuestion 두 건 승인 — 「적용 범위」에 「1·3·4 만 적용」, 「P1 순서」에 「보안 2건을 맨 앞으로」) · 항목별 현행성 대조(101개 전부를 읽고 grep·ls·vitest 로 코드·자료·로그와 대조. 방법과 결과는 메모)
- 검증: 문서만 변경해 동적 QA 제외(tier-rules §1-1) · `git diff --cached --check` exit 0 · TODO 안의 중복 ID 0건 · 제거·흡수된 33개 ID 를 남은 항목이 「흡수」 문맥 밖에서 참조하는 곳 0건 · 남은 66개 항목 전부에 카테고리·티어 줄 존재 · 연속 빈 줄 3개 이상 0곳
- 메모: 사용자 질문은 「왜 이렇게 많이 남았는가」였다. 101개 가운데 감사(`AUDIT-*`)에서 온 것은 6개, 옛 계획 문서 이관이 2개이고 **나머지 93개는 라운드마다 붙은 code-review·`/qa-only` 가 만든 범위 밖 발견**이다. 77건을 완료하는 동안 라운드당 평균 1.2개가 새로 들어와 닫는 속도보다 빨리 자랐고, 발견 하나가 항목 하나가 되면서 같은 파일·같은 원인이 여러 ID 로 쪼개졌다. 종가베팅의 「빈 상태 안내가 영어」는 다섯 항목(FE-025, JONGGA-023, JONGGA-027, JONGGA-034, JONGGA-035)에 겹쳐 적혀 있었다. P2 의 51개가 T1 인데 T1 도 설계 승인·리뷰·QA·아카이브를 전부 거치므로 한 줄짜리 수정에 한 라운드가 통째로 들었다. **제거 9건은 전부 근거를 확인했다.** INFRA-031 은 본문 스스로 해소를 적고 있었고 grep 으로 중복 0건, CHAT-011 은 챗봇 vitest 에서 `unique "key"` 경고 0건, CHAT-006 은 `kr_market_chatbot_stream_helpers.py:87` 의 청크가 모델 문자열·`Dict[str, int]` 토큰 수·오류 문자열뿐이라 NaN 불가, INFRA-026 은 `data/*.json` 에 `"NaT"` 0건, VCP-016 은 `logs/backend.log` 에 429 가 0건이다. INFRA-002·FLOW-002 는 결함도 범위도 없는 일반론과 요청 없는 기능이고, INFRA-009 는 INFRA-008 뒤 필요 시 재등록, CHAT-028 은 확신도 낮음에 이중 실패 조건이라 닫았다. **병합 26건**은 살아남는 항목의 본문을 다시 써서 흡수한 항목의 실측값·행 번호·체크박스를 옮겼고 「2026-09-07 백로그 정리에서 `[X]` 를 흡수했습니다」 한 줄을 남겼다. 목록은 월별 요약의 「백로그 정리」 절에 있다. **정정 셋 가운데 INFRA-035 가 가장 크다.** 처음 등록할 때 「`cmp_dev_*` 이며 저장소 밖 도구」라고 적었으나 `.db-wal` 11,647개 가운데 11,642개가 `paper_trading_test_*` 이고 출처는 `tests/services/test_paper_trading_service.py:49` 의 픽스처다. 가장 최근 파일이 이날 14:58 이라 pytest 한 번에 두 파일씩 지금도 새고 있다. INFRA-021 은 `get_performance_report` 의 테스트 밖 호출자가 없음을 확인해 「지운다」로, FE-021 은 StrictMode 로 설명되는 이중 요청을 빼고 `refreshKey` 가 기간 버튼과 묶인 한 가지로 좁혔다. **사용자가 남기기로 한 것**: 관찰된 결함 없이 규모만 큰 리팩터링 세 묶음(INFRA-032+CHAT-012, INFRA-018, FLOW-006)은 제거 후보로 제시했으나 「1·3·4 만 적용」을 골라 P2 에 그대로 있다. **정리 도중 셈이 한 번 틀렸다.** 사용자에게 「병합 27건」이라고 보고했으나 실제 목록은 26건이었고 파일을 만든 뒤 세어 잡았다. 결과는 101개 → 66개(P1 18 · P2 48)다. **제거·병합된 ID 는 재사용하지 않는다.** `archive-format.md` §2 의 다음 번호 절차가 `archive/` 전체를 grep 하므로 월별 요약에 목록을 두는 것이 그 보장이다. 이 항목 자신은 문서 항목이라 첫 커밋에 TODO 항목을 유지했다가 이 아카이브 커밋에서 뺐다.

## [INFRA-025] `/api/system/env` 가 인증 없이 서버 `.env` 를 읽고 쓴다
- 완료 2026-09-07 17:24 | 티어 T3 | 커밋 95b022c
- 변경: services/common_env_service.py, app/routes/common_update_routes.py, frontend/src/app/api/system/env/route.ts (신규), frontend/src/lib/adminEmails.ts (신규), services/admin_helpers.py, frontend/src/app/components/SettingsModal.tsx, frontend/next.config.js (실행 코드 +211 -141) · tests/app/test_system_env_admin_gate.py·frontend/src/app/api/system/env/route.test.ts·frontend/src/app/components/SettingsModal.admin-gate.test.tsx (신규) 외 4개 (검사 +356 -57) · .env.example, .gitignore, AGENTS.md, CLAUDE.md, README.md, docs/dev-cycle/TODO.md, docs/dev-cycle/qa/INFRA-025.md (신규), docs/superpowers/plans/2026-09-07-infra-025-system-env-admin-gate.md (신규) (문서 +1179 -9)
- 리뷰: superpowers:brainstorming(architectural, AskUserQuestion 두 건 승인 — 「차단 방식」에 「Next 세션 게이트 + Flask 공유 비밀」, 「마스킹 구멍」에 「함께 고친다」) · superpowers:writing-plans(계획 932줄, Task 6개) · oh-my-claudecode:critic(판정 `REVISE`, 지적 6건 전부 반영. 가장 큰 것은 `hmac.compare_digest` 가 비ASCII str 인자에 `TypeError` 를 던져 403 대신 500 이 나가는 결함) · frontend-skills(번들 문서 `01-app/…/12-route-handlers-and-middleware.md` 와 `05-server-and-client-components.md` — 새 라우트 핸들러가 서버 전용임을 확인. Next 16.3.4 에서 GET 라우트 핸들러는 기본 동적이고 `getServerSession` 이 쿠키를 읽어 정적화가 애초에 불가능하다) · tier-rules §2(`.env` 로 시작하는 파일과 인증 경계 양쪽에 닿아 T3) · ponytail-review(지적 3건 전부 반영 — `get_admin_api_token`·`ADMIN_TOKEN_HEADER`·`_reject_unless_admin` 이 호출자 하나짜리 간접층이라 인라인해 17줄 삭제) · feature-dev:code-reviewer `infra025-reviewer`(네 항목 결함 없음, pytest·vitest·type-check 독립 재실행으로 통과 확인. 지적 1건은 이월 기록과 실제 항목의 불일치였고 `[INFRA-037]` 로 해소) · oh-my-claudecode:security-reviewer(`%2F` 우회가 rewrite 를 빠져나가 Flask 에 닿는 것을 실측으로 확인, 캐시 헤더·`FLASK_BASE` 한정·마스킹 임계값 24 를 반영. `[INFRA-037]`·`[INFRA-038]` 이월) · /review(critical 0건. Enum & Value Completeness 를 diff 밖까지 대조해 `SettingsModal.tsx` 가 쓰는 키 12개와 `EDITABLE_ENV_KEYS` 12개가 정확히 일치함을 확인. informational 2건은 `[FE-041]` 로 이월) · qa-only·qa(필수 12/12, 선택 2 중 1 통과)
- 검증: pytest 1756 통과·2 스킵 exit 0(기준 1746) · vitest 52 파일 316 통과 exit 0(기준 49 파일 305) · type-check exit 0 · npm run build 성공 · QA 시나리오 필수 12/12 통과(5·7 실패 → 워커 재기동 후 고침), 선택 2건 중 14 통과·13 은 `[INFRA-017]` 범위 · 인증 없는 `GET` 이 45개 키를 내주던 것 → 403 · 토큰이 있어도 10개만 반환하며 `ADMIN_EMAILS`·`ADMIN_API_TOKEN`·`GOOGLE_APPLICATION_CREDENTIALS`·`NEXTAUTH_SECRET` 미포함 · 허용 목록 밖 키 `POST` 전후 `.env` SHA-256 동일
- 메모: **공격 사슬 네 단계가 실측으로 이어졌다.** ① 인증 없는 `GET` 이 45개 키를 반환하고 그 안에 `ADMIN_EMAILS` 가 평문으로 있었다. 마스킹이 키 이름에 `KEY`·`SECRET` 같은 단어가 있는지로만 판정해 그 단어가 없는 변수 37개를 그대로 흘렸기 때문이다. ② 그 이메일이 새면 `common_admin_routes.py:24` 의 `/api/admin/check` 가 이메일 문자열만으로 판정하므로 관리자 화면이 열린다. ③ `[INFRA-027]` 의 `X-User-Email` 헤더가 검증 없이 신원이 되어 쿼터까지 뚫린다. ④ `POST` 로 `.env` 를 덮어쓴다. **신뢰의 근거를 Flask 바깥에 두었다.** Next 라우트 핸들러가 NextAuth 세션으로 관리자를 판정하고 `ADMIN_API_TOKEN` 을 붙여 넘기며, Flask 는 그 토큰만 상수 시간으로 확인한다. Flask 는 NextAuth 세션을 알지 못하고 앞으로도 알 필요가 없다. **Next 의 rewrite 제외만으로는 부족하다.** 배열을 돌려주는 `rewrites()` 는 `afterFiles` 라 파일시스템 라우트가 앞서지만, `/api/system%2Fenv` 는 부정 전방탐색을 빠져나가 rewrite 를 타고 Flask 에 닿고 WSGI 가 `PATH_INFO` 를 디코딩해 라우트에 매칭된다. 그때 요청을 세우는 것은 Flask 쪽 토큰 검사뿐이며, 보안 리뷰가 이것을 실측으로 확인했다. **마스킹 판정을 허용 목록으로 뒤집었다.** 화면이 실제로 읽고 쓰는 12개 키만 오간다. 그 과정에서 기존 결함 하나가 함께 드러났다. `AI_PROVIDER` 는 `PROVIDER` 안의 `ID` 가 `SENSITIVE_KEYWORDS` 에 걸려 `******` 로 반환되고 있었고, 그래서 공급자 버튼이 어느 것도 선택되지 않았다. 계획 검토자가 「`AI_PROVIDER` 는 어떤 키워드에도 걸리지 않는다」고 한 것은 사실과 달랐고 새 검사가 그것을 잡았다. 마스킹 임계값도 8 에서 24 로 올렸다. 8 이면 16자인 Google 앱 비밀번호가 앞뒤 4자씩을 그대로 흘리는데 `SMTP_HOST` 와 `SMTP_USER` 가 같은 응답에 함께 실린다. **`.gitignore` 가 새 라우트를 통째로 숨기고 있었다.** `env/`·`ENV/` 에 앞 `/` 가 없어 하위 디렉터리에도 걸렸고, macOS 는 `core.ignorecase` 가 true 라 `ENV/` 가 `env/` 와도 일치한다. 그래서 `frontend/src/app/api/system/env/` 가 무시되어 배포에서 빠질 뻔했다. 루트 한정(`/env/`·`/ENV/`)으로 고쳤고, 그 뒤 무시 대상에서 빠지는 디렉터리는 이 라우트뿐임을 `find` 로 확인했다. **워커 재기동을 또 건너뛰었다.** 마스킹 임계값을 16:43:51 에 고쳤는데 워커는 16:41:12 기동이라 QA 5·7 이 실패했다. `gunicorn-needs-hup-before-qa` 학습을 QA 문서 첫머리에 적어 놓고도 같은 자리에 걸렸다. 이번 라운드에서 서버를 네 번 재기동했다(노출 차단 · 토큰 재발급 · 마스킹 반영 · QA). **화면 시나리오 11·12 는 첫 시도에서 hydration 이 되지 않아 막혔다가 나중에 통과시켰다.** `next dev` 가 첫 요청에서 라우트를 컴파일하는 동안 스크립트가 아직 실행되지 않은 상태를 잡았던 것으로 보인다. 시간을 두고 다시 열자 `__reactFiber$…` 가 붙어 있었고 클릭이 정상 동작했다. 탭 버튼이 「일반」 하나뿐이고 `/api/system/env` 요청이 0건임을 네트워크 로그와 `performance.getEntriesByType('resource')` 두 경로로 확인했다. **같은 날 `.env` 의 자격 증명 11개가 비워진 사고가 있었다.** 옛 `FACTORY_RESET_SENSITIVE_KEYS` 14개 가운데 `.env` 에 값이 있던 11개가 정확히, 그리고 그것만 비워져 `reset_sensitive_env_and_user_data` 의 실행이 특정된다. `data/` 파일의 부재도 같은 함수가 설명한다. 다만 HTTP 로 실행된 것은 아니다. 활동 로그의 `DELETE` 두 건은 모두 `MethodNotAllowed`(405)이고 성공한 `DELETE`·`POST` 는 없다. 파이썬에서 직접 부른 것이며 **호출 주체는 재구성할 수 없다.** 활동 로거는 HTTP 만 기록하고 `logs/backend.log` 는 `restart_all.sh:80` 이 `>` 로 덮어쓰므로 재기동 이전 기록이 남지 않았다. 이번 라운드가 그 함수와 라우트를 지웠으므로 같은 일이 다시 일어날 경로는 없다. `.env.bak.20260901_084454` 에서 11개를 복구했고 되돌리기 전 상태를 `.env.bak.20260907_164102` 에 남겼다. **이 사고에서 두 번 틀렸다.** 처음에는 시각의 근접만으로 보안 리뷰 에이전트를 원인으로 지목했고, 반박을 받은 뒤에는 반대로 「원인 미확인, `data/` 피해 없음」으로 물러섰다. 리뷰어가 제안한 개수 대조 한 번으로 결론이 났으므로 그 대조를 처음부터 했어야 했다. 교훈 둘을 `AGENTS.md` 의 「되돌릴 수 없는 조작」 절에 적었다. 서브에이전트에게 「읽기 전용」이라고만 지시하면 실행 중인 서버에 요청을 보내는 것을 막지 못한다는 것, 자료가 사라진 것을 발견했을 때 활동 로그와 오류 로그를 먼저 읽어야 한다는 것이다. **설정 화면으로 자격 증명을 바꿔도 요청을 처리한 워커의 `os.environ` 만 바뀐다.** `services/notifier.py` 와 `engine/messenger_config.py` 가 객체를 만들 때 `os.getenv` 로 읽으므로 나머지 워커는 재기동 전까지 옛 값을 쓴다. 유출된 키를 이 화면으로 교체했다면 반드시 워커를 모두 재기동해야 한다. 이 사실을 `CLAUDE.md` 에 적었다. 이월: `/api/notification/send` 가 여전히 무인증으로 저장된 자격 증명을 써서 발송하는 것은 새 `[INFRA-037]`(P1), 500 응답 본문이 서버 내부 경로를 흘리는 것은 새 `[INFRA-038]`(P1), 알림 테스트가 `.env` 저장 실패를 삼키고 「발송 성공」으로 표시하는 것은 새 `[FE-041]`(P2). `DELETE` 가 405 대신 500 을 내는 것은 기존 `[INFRA-017]` 에 사례를 덧붙였다.

## [INFRA-027] 요청 헤더 한 줄이 그대로 신원이 되어 유료 쿼터를 우회한다

- 완료 2026-09-07 18:52 | 티어 T3 | 커밋 ab347b5, 2382a73, d6b66f3
- 변경: services/identity_helpers.py (신규), frontend/src/proxy.ts (신규), frontend/src/lib/identity.ts (신규), app/__init__.py, app/routes/common_update_routes.py, app/routes/kr_market.py, app/routes/kr_market_chatbot_http_routes.py, app/routes/kr_market_chatbot_routes.py, app/routes/kr_market_quota_http_routes.py, services/kr_market_chatbot_request_helpers.py, services/kr_market_quota_runtime_service.py, services/kr_market_quota_service.py, frontend/src/app/components/{ChatWidget,SettingsModal,Sidebar}.tsx, frontend/src/app/components/chatHelpers.ts (실행 코드 +334 -79) · tests/services/test_identity_helpers.py·tests/services/test_quota_recharge_daily_limit.py·tests/app/test_identity_gate.py·frontend/src/lib/identity.test.ts·frontend/src/proxy.test.ts (신규) 외 5개 (검사 +685 -27) · .env.example, docs/dev-cycle/TODO.md, docs/dev-cycle/qa/INFRA-027.md (신규), docs/superpowers/plans/2026-09-07-infra-027-identity-signature-gate.md (신규) (문서 +2157 -6)
- 리뷰: superpowers:brainstorming(architectural, AskUserQuestion 두 건 승인 — 「신원 확정」에 「Next 미들웨어 + HMAC 서명」, 「익명 처리」에 「익명 유지 + 쿼터 경로만 로그인 필수」, 「범위」에 「신원 확정 + recharge 차단」, 「충전 버튼」에 「하루 1회 제한으로 남김」) · superpowers:writing-plans(계획 1724줄, Task 8개) · oh-my-claudecode:critic `infra027-critic`(판정 `REVISE`, CRITICAL 1건·MAJOR 5건·MEDIUM 4건·MINOR 3건 전부 반영) · frontend-skills(번들 문서 `01-app/03-api-reference/03-file-conventions/proxy.md` — Next 16 에서 `middleware.js` 가 폐지되고 `proxy.js` 로 바뀐 것, 실행 순서가 `Proxy(3) → afterFiles rewrites(6)` 인 것, Proxy 가 Node.js 런타임 기본인 것을 확인. §2 표의 스킬 조건에는 해당 없음) · tier-rules §2(`.env` 로 시작하는 파일과 인증 경계 양쪽에 닿아 T3) · ponytail-review(지적 4건 전부 반영, 17줄 삭제 — 중복 `int()` 변환·상수 배열과 루프·호출자 하나짜리 상수·세 파일에 반복된 같은 docstring) · feature-dev:code-reviewer `infra027-reviewer`(코드 결함 없음. 지적 2건 반영 — `ChatWidget.tsx` 의 죽은 `useSession`·`userEmail`, M-4 실측 요구. pytest 80건·vitest 22건 독립 재실행과 두 언어 서명 바이트 대조로 확인) · /review(**CRITICAL 1건** — Enum & Value Completeness 가 익명 ID 생성 자리 셋의 형식 불일치를 잡았다) · oh-my-claudecode:security-reviewer `infra027-security`(차단 등급 없음. 다섯 지적 중 셋 반영·둘 이월) · qa-only·qa(필수 12/12)
- 검증: pytest 1783 통과·2 스킵 exit 0(기준 1756) · vitest 54 파일 328 통과 exit 0(기준 52 파일 316) · type-check exit 0 · npm run lint 오류 0(경고 204는 기존) · npm run build 성공 · QA 시나리오 필수 12/12 통과(S-9 는 `data/` 읽기 전용 제약에 따라 단위·라우트 검사 11건으로 대체) · 시크릿 확인 다섯 가지 통과 · **핵심 가정 실측**: 세션 쿠키를 실은 요청에서 `identity_present=True resolved=True`
- 메모: **브라우저에는 이미 검증된 신원이 있는데 서버로 보낼 때 쓰지 않고 있었다.** `chatHelpers.ts:43` 의 `getAuthHeaders` 가 `localStorage` 의 `user_profile.email` 을 헤더에 실었고, 그 값은 설정 모달의 자유 입력 칸에서 왔다. Google 로그인을 해도 세션 이메일이 서버로 가지 않았다. **신원 확정을 `proxy.ts` 한 자리로 옮겼다.** Next 16 에서 `middleware.js` 규약이 폐지되고 `proxy.js` 로 바뀐 것이 설계에 직접 영향을 주었다. Proxy 가 Node.js 런타임 기본이라 `node:crypto` 와 `getToken` 을 그대로 쓸 수 있었고, 실행 순서가 rewrite 보다 앞서므로 `next.config.js` 를 건드리지 않았다. **계획 검토가 게이트를 통째로 무력화하는 우회를 잡았다.** `X-Session-Id: victim@example.com` 한 줄이면 됐다. 챗봇 `owner_id` 와 쿼터 키가 검증된 이메일과 익명 ID 를 같은 문자열 공간에 담기 때문이고, 관리자 이메일을 넣으면 `is_admin_email` 판정까지 통과해 쿼터가 무제한이 된다. **`/review` 가 로그인 상태로는 절대 보이지 않는 결함을 잡았다.** 그 우회를 막으려 `anon_` 접두사 화이트리스트를 썼는데, 익명 ID 를 만드는 자리가 셋이고 `chatbot/storage.py:295` 가 소유자 불일치 때 배정하는 것은 접두사가 없다. VCP 화면이 그 값을 저장해(`page.tsx:1101`) 다음 요청의 `X-Session-Id` 로 쓰므로, 그대로 두었으면 비로그인 사용자의 VCP 채팅이 영구히 끊겼다. 로그인 사용자는 이메일이 소유자라 멀쩡해 보여 로그인한 채로 QA 를 돌렸다면 통과했을 것이다. 판정을 「이메일 모양이면 버린다」로 바꿨다가, 보안 리뷰의 지적을 받아 `[A-Za-z0-9_-]{1,128}` 로 다시 좁혔다. `recharge_day:` 접두사 충돌과 `user_quota.json` 무한 증식이 같은 줄에서 함께 닫힌다. **이 라운드 전체가 가정 하나 위에 서 있었다.** 「proxy 가 붙인 request 헤더가 afterFiles rewrite 의 외부 목적지까지 전달된다」가 그것이다. 틀렸다면 로그인 사용자 전원이 조용히 익명이 되는데 pytest 는 헤더를 스스로 만들어 넣고 vitest 는 문자열 형식만 보므로 어떤 자동 검사도 실패하지 않는다. 상태 코드 200 도 증거가 되지 않는다. 익명 응답도 200 이다. Flask 에 임시 프로브를 넣고 `next-auth/jwt` 의 `encode` 로 세션 쿠키를 만들어 `identity_present=True resolved=True` 를 확인한 뒤 프로브를 지웠다. Google 로그인을 하지 않아 자격 증명을 다루지 않았다. **판정이 한쪽에서만 강제되고 있었다.** `verify_identity_header` 가 돌려주는 이메일에 `@` 를 요구하지 않아, 「검증된 신원에는 `@` 가 있고 익명 ID 에는 없다」는 불변식이 반쪽만 지켜졌다. 서명자가 이메일이 아닌 식별자를 넣기 시작하면 조용히 무너지므로 대칭을 맞췄다. **`proxy.ts` 에 검사가 하나도 없었다.** 계획서에 헤더 삭제 검사가 있었는데 구현에서 빠뜨렸고 보안 리뷰가 잡았다. 7건을 더했고, 삭제를 세션 확인 뒤로 옮기는 돌연변이를 주입해 정확히 그 검사가 실패하는 것으로 실효성을 확인했다. **`.env.production` 에 비밀이 없었다.** 다른 세 키는 값이 있는데 이것만 빠져 있었고, 실패 방향은 안전하지만 예외도 경고도 없이 「로그인했는데 대화 목록이 비어 있다」로만 나타난다. 사용자 승인을 받아 두 파일에 각각 다른 값을 넣었다. 저장소에서 `.env.production` 을 읽는 배포 절차를 찾지 못했으므로, 그 파일이 쓰이지 않는다면 운영 `.env` 에 따로 넣어야 한다. **작업 트리를 두 번 더럽혔다.** `frontend/` 밖에서 `npx vitest` 를 돌려 루트에 `node_modules/` 가 생겼고(`.gitignore` 는 `frontend/node_modules/` 만 있다), `cd frontend` 가 남은 상태로 다음 명령을 보내 경로를 두 번 틀렸다. 둘 다 그 자리에서 정리했다. 이월: replay 방어가 망 구성에 기대는데 네 자리가 모두 `0.0.0.0` 에 바인딩하는 것은 새 `[INFRA-039]`(P1, `app.run(debug=True)` 포함), 인증이 쿠키 파생으로 바뀌며 생긴 CSRF 노출은 새 `[INFRA-040]`(P1, 보안 리뷰가 「이번 변경이 새로 만든 유일한 위험」으로 분류), `/api/admin/check` 가 검증된 신원을 쓰지 않는 것은 새 `[INFRA-041]`(P2). 익명이 `browser_session_id` 를 지우면 챗봇 쿼터가 초기화되는 한계는 승인 범위 밖이라 코드 주석에 남겼다.

## [INFRA-037] `/api/notification/send` 가 인증 없이 저장된 자격 증명으로 발송한다
- 완료 2026-09-07 20:20 | 티어 T2 | 커밋 b34a4ac
- 변경: app/routes/common_notification_routes.py (+33 -7),
  tests/app/test_notification_admin_gate.py 신규, tests/app/test_common_routes_refactor.py
  (테스트 +242 -9, 문서 +226 -9)
- 리뷰: superpowers:brainstorming(bounded 대화 설계. TODO 가 적어 둔 `X-Admin-Token` 방식 대신
  `[INFRA-027]` 의 `g.user_email` 을 쓰는 방식과 관리자 전용 권한 수준을 사용자가 선택) ·
  ponytail-review(3건 중 2건 반영, net -20 제안. 인코딩 우회 주석 두 줄과 가짜 Messenger 의
  쓰지 않는 telegram·email 제거. 위조 서명 테스트 삭제 제안은 서명 부재와 다른 층이라 미반영) ·
  feature-dev:code-reviewer(`infra037-reviewer`, 차단 결함 0건. 권고 3건 중 2건 반영 —
  `_PLATFORM_SPECS` 판정을 `Messenger()` 앞으로, 오설정 두 방향의 403 고정 테스트 2건.
  1건은 `[INFRA-042]` 이월. 선택 사항 2건도 반영) ·
  oh-my-claudecode:security-reviewer(`infra037-security`, 우회로 0건, 새 보안 위험 0건.
  `get_json()` 에 `silent=True` 가 없는 것이 CSRF 를 막고 있다는 지적으로 그 줄에 이유를 적음.
  나머지는 `[INFRA-042]`·`[INFRA-043]`·`[INFRA-044]` 이월, 인접 2건은 `[INFRA-039]` 에 덧붙임)
- 검증: pytest 1792 통과 · 2 skip · vitest 328 통과 · tsc --noEmit exit 0 ·
  게이트를 임시 제거하면 새 검사 5건이, 플랫폼 판정 순서를 되돌리면 1건이 실패함을 대조 ·
  시크릿 3항(추적 env 는 .env.example 뿐, NEXT_PUBLIC_ 둘 다 비밀 아님, 403 본문·로그에 자격 증명 없음) ·
  git diff --cached --check exit 0 · QA 시나리오 9/9 통과
- 메모: **리뷰 중 사고가 있었다.** 코드 리뷰 에이전트가 경계 조건을 실측하다가 관리자 신원으로
  요청을 통과시켜 운영 디스코드 채널에 `[Test] DISCORD Notification` 한 건이 실제로 도착했다.
  원인은 이 세션의 「알림 테스트 발송을 하지 않는다」 제약을 리뷰 프롬프트에 적지 않은 것이다.
  이 저장소에서 발송 라우트를 실측할 때는 존재하지 않는 플랫폼 이름(`carrier-pigeon`)을 보내
  `400 Unknown platform` 으로 게이트 통과만 확인한다. `NOTIFICATION_ENABLED=false` 로는 막히지
  않으며 그 사실은 `[INFRA-043]` 에 적어 두었다. 그리고 이 항목이 닫은 것은
  `/api/notification/send` 하나뿐이다. 같은 자격 증명으로 발송하는 `/api/kr/jongga-v2/message`
  와 `/run` 은 여전히 열려 있고 `[INFRA-042]` 가 다룬다.

## [INFRA-039] Flask 가 모든 인터페이스에 바인딩해 신원 서명의 전제가 관례에 머문다
- 완료 2026-09-07 21:25 | 티어 T3 | 커밋 aaf1c53
- 변경: config.py, restart_all.sh, tests/app/test_flask_binding.py 외 7개 (+87 -16, 문서 포함 +608 -23)
- 리뷰: ponytail-review(같은 이유가 다섯 파일에 중복 → .env.example 을 정본으로 두고 net -10) ·
  critic 계획 검토(**REVISE**, 지적 8건 전부 반영) · feature-dev:code-reviewer(다섯 확인 항목
  결함 없음, 승인 범위 문구 불일치 1건 지적 → 고침) · /review(restart_all.sh 의 --bind 에
  따옴표가 없어 값의 공백이 인자를 가르는 것을 고침. Procfile 의 # 주석이 표준 파서에서
  무시되는 것을 검증) · security-reviewer(4건 반영. Procfile 예외가 구멍이라는 지적으로
  `[INFRA-046]` 을 만들고, identity_helpers 주석의 과한 보증을 약화했다) ·
  browser-notes(agent-browser 실측에 적용, 함정 두 가지를 그 문서에 추가)
- 검증: pytest 1794 통과 · 2 skip (신규 2건) · vitest 328 통과 · tsc --noEmit exit 0 ·
  시크릿 3항(추적 env 는 .env.example 뿐, diff 에 값 없음, NEXT_PUBLIC_ 변수 없음) ·
  git diff --cached --check exit 0 · QA 시나리오 필수 10/10 통과 (S-11 은 선택, 미실행)
- 메모: **계획 검토가 내 실측 해석을 뒤집었고 그 덕분에 잘못된 승인을 되돌렸다.** 처음에는
  「Next.js 가 X-Forwarded-For 를 붙이지 않으니 `_resolve_real_ip` 를 지워도 무해하다」고
  판단해 사용자 승인까지 받았는데, 그 실측이 틀렸다. `app/__init__.py:187` 의
  `_should_skip_activity_logging` 이 `GET` 을 전부 건너뛰는데 `GET` 으로 재고 로그의 마지막
  줄들을 읽은 것이 원인이었다. 그 줄들은 앞선 라운드가 남긴 다른 요청의 기록이었다.
  `POST /api/system/log-event` 로 다시 재니 Next 는 헤더를 붙이고 있었고
  (`base-server.js:612` 의 `??=`), 클라이언트가 보낸 값을 덮어쓰지 않아 위조가 그대로
  통과했다. 승인 전제가 거짓이었으므로 다시 여쭈어 이월로 정했다(`[INFRA-045]`).
  **감사 로그를 재는 실측은 반드시 `POST` 로 한다.**
- 메모: **바인딩 변경은 `kill -HUP` 으로 반영되지 않는다.** gunicorn 마스터가 리스닝 소켓을
  잡고 워커가 그것을 상속하므로 HUP 은 워커만 새로 띄운다. 파이썬 코드 변경에는 HUP 이
  충분하지만 이 종류의 QA 는 `stop_all.sh` 와 `restart_all.sh` 로 완전히 재기동해야 한다.
- 메모: **브라우저 실측은 `localhost:3500` 으로 연다.** `127.0.0.1:3500` 으로 열면 Next.js 의
  `allowedDevOrigins` 가 API 를 막아 화면이 로딩에 갇히는데, 콘솔에 오류가 남지 않아 결함으로
  오판하기 쉽다. S-6 을 실패로 판정할 뻔했다. `curl` 은 그 검사에 걸리지 않으므로 curl 이
  통과했다는 사실이 브라우저도 된다는 뜻이 아니다. `references/browser-notes.md` 에 적었다.
- 메모: `Procfile` 이 Flask 만 띄운다는 것을 보안 리뷰가 짚었다. `[INFRA-027]` 이후 신원
  확정은 `proxy.ts` 가 맡으므로, 그 파일로 배포하면 모든 요청이 익명이 되면서 동시에 Flask 가
  인터넷에 직접 노출된다. 지금 아키텍처에서 성립하지 않는 배포 방식이며 `[INFRA-046]` 이다.

## [INFRA-040] 인증이 쿠키 파생으로 바뀌면서 생긴 CSRF 노출을 닫는다
- 완료 2026-09-07 22:35 | 티어 T3 | 커밋 e6313bc
- 변경: frontend/src/proxy.ts, frontend/src/proxy.test.ts,
  .claude/skills/dev-cycle/references/tier-rules.md 외 3개 (+695 -15, 구현만 +44 -7)
- 리뷰: ponytail-review(net -9, next-auth 기본값 설명 4줄과 세는 명령 6줄) ·
  critic(REVISE, 7건 중 6건 반영) · review(1건 자동 수정, 소문자 메서드) ·
  code-reviewer(결함 없음, 확신도 낮은 관찰 1건은 이 사이클이 별도 수행) ·
  security-reviewer(**실제 결함 1건**, 검사 위치를 옮김)
- **리뷰 둘이 같은 자리에 반대 판정을 냈다.** `code-reviewer` 는 검사가 `if (secret)`
  안쪽에 있는 것을 「타당하다」고 확신도 높음으로 판정했고, `security-reviewer` 는 같은
  자리를 「실제로 뚫린다」고 판정했다. 전자는 proxy 안에서만 보아 「비밀이 없으면 신원
  게이트가 꺼져 CSRF 대상이 없다」고 옳게 추론했고, 후자는 그 비밀을 보지 않는 다른 인증
  경로(`resolveAdminToken`)가 살아 있다는 것을 보았다. 시야가 넓은 쪽이 맞았다.
  **한 리뷰가 「타당하다」고 한 자리를 다른 리뷰가 뒤집을 수 있다.** 확신도가 높다는 것이
  범위가 넓다는 뜻은 아니다.
- 검증: pytest 1794 통과 · 2 skip · vitest 335 통과(54 파일) · tsc --noEmit 종료 코드 0 ·
  QA 시나리오 15/15 통과 (선택 S-16 미실행)
- 메모: **CSRF 차단을 신원 서명 블록 안에 두면 안 된다.** `INTERNAL_IDENTITY_SECRET` 은
  Flask 로 신원을 넘기는 용도일 뿐 「이 요청이 인증되었는가」와 무관하다. 안쪽에 두면 그
  값이 빈 배포에서 차단만 꺼지는데, `app/api/system/env/route.ts:26` 의 `resolveAdminToken`
  은 그 값을 보지 않고 NextAuth 세션만 보므로 살아 있고 `.env` 쓰기까지 닿는다. 기본값이
  빈 문자열이라 개발기와 신규 배포에서 현실적인 상태다. 보안 리뷰가 잡았다.
- 메모: **세션 쿠키가 필요한 경로는 로그인 없이도 실측할 수 있다.** `frontend` 를 cwd 로
  두고 `node --input-type=module -e` 로 `next-auth/jwt` 의 `encode` 를 불러 QA 토큰을 만든다.
  스크래치패드에 스크립트를 두면 모듈을 찾지 못한다. 생성 성공은 길이가 아니라 JWE 조각
  수(점 네 개로 나뉜 다섯 조각)로 확인한다. 길이로 재다가 1113자짜리
  `ERR_MODULE_NOT_FOUND` 를 토큰으로 통과시켰다.
- 메모: **실측 결과가 통째로 달라지면 코드보다 토큰 만료를 먼저 의심한다.** `maxAge: 300`
  토큰이 11분 뒤 만료되어 여섯 경우가 전부 500 으로 나왔고 구현 결함으로 오판할 뻔했다.
  차단이 부분적으로 실패하면 코드 문제이고, 막아야 할 것과 막지 말아야 할 것이 함께
  무너지면 신원이 사라진 것이다.
- 메모: 사파리는 16.4(2023-03)부터 `Sec-Fetch-Site` 를 보낸다. 계획 검토가 「헤더 부재를
  차단하면 iOS 16.3 이하에서 로그인 사용자의 쓰기가 전부 막힌다」며 `Origin` 예비 대조를
  제안했고 사실이었다. 사용자에게 다시 여쭈어 **승인대로 헤더 부재 차단을 유지**하기로
  했으며, 문의가 들어오면 무엇을 더하면 되는지 코드 주석에 남겼다.
- 메모: `[INFRA-027]` 이 `proxy.ts` 와 `identity_helpers.py` 를 만들면서 `tier-rules.md` §4
  의 목록 갱신을 이행하지 않아 이 항목의 티어가 T2 로 적혀 있었다. §2 에 「신원 확정」 절을
  만들어 넣고 머리글 수치를 63개·23,357줄에서 66개·23,695줄로 고쳤다.

## [INFRA-041] 관리자 판정을 요청자가 정한 쿼리에서 검증된 신원으로 옮긴다
- 완료 2026-09-07 23:07 | 티어 T1 | 커밋 85c4fd7
- 변경: `app/routes/common_admin_routes.py`, `frontend/src/hooks/useAdmin.ts`,
  `tests/app/test_common_routes_refactor.py` (+35 -10)
- 리뷰: ponytail-review(네 건, net -11) · security-reviewer(지적 1 반영, 2 이월, 3·4 조치 없음)
- 검증: pytest 1795 통과 · 2 skip · vitest 335 통과(54 파일) · `tsc --noEmit` exit 0 ·
  QA 필수 11/11 통과(선택 S-11 미실행)
- 이월: `[INFRA-048]`(`INTERNAL_IDENTITY_SECRET` 이 비면 관리자 화면이 말없이 사라진다)
- 메모: `GET /api/admin/check` 가 `request.args.get("email")` 로 판정해 **누구든 이메일을
  하나씩 넣어 `ADMIN_EMAILS` 명단을 확인할 수 있었다.** 실제 권한 게이트는 Next 쪽
  `resolveAdminToken` 이라 권한 상승은 아니었고, 닫은 것은 그 오라클과 브라우저 쪽 관리자
  UI 게이트다. **이로써 저장소에서 요청자가 정한 값으로 신원을 판정하는 자리가 없어졌다.**
  `[INFRA-027]` 이 만든 `g.user_email` 을 쓰지 않는 마지막 자리였다.
- 메모: **보안 리뷰가 이번 변경이 만든 결함 하나를 잡았다.** 판정 근거가 URL 에서 헤더로
  옮겨지면서 캐시 키가 요청자마다 갈리던 성질이 함께 사라졌다. 종전에는 URL 이 `?email=`
  로 달라 어떤 HTTP 캐시도 사용자마다 키를 나눴으나, 이제 URL 은 모두에게 같고 본문만
  신원에 따라 다르다. 앞단에 공유 캐시가 놓이면 관리자의 `true` 응답이 다른 사람에게
  재사용된다. 라우트에 `Cache-Control: no-store`, 훅에 `cache: 'no-store'` 를 붙였다.
  `frontend/src/app/api/system/env/route.ts:55-59` 가 같은 이유로 같은 조치를 이미 하고
  있었는데 그 선례를 처음에 보지 못했다. **판정 근거를 URL 에서 헤더로 옮기면 캐시 성질이
  함께 바뀐다는 것을 다음 사이클이 먼저 확인한다.**
- 메모: **막는 것만 확인하면 통과 판정이 무의미하다.** 「신원과 무관하게 언제나 false」를
  돌려주는 코드도 회귀 시나리오 셋을 통과한다. S-3 과 S-6 이 신원이 있을 때 `true` 가
  나오는 것을 함께 재도록 시나리오를 짰다. 두 방향이 함께 맞아야 옳다.
- 메모: **zsh 는 파라미터 확장 결과를 단어 분리하지 않아 curl 인자가 통째로 하나가 된다.**
  QA 하네스의 `${1:+-H "Cookie: ..."}` 가 인자 하나로 전달되어 신원이 붙은 네 경우가 전부
  400 이 났고 코드 결함으로 오판할 뻔했다. `if [ -n "$1" ]` 조건 분기로 고쳤다.
  `browser-notes.md` 가 agent-browser 에 대해 적어 둔 함정과 같은 성질이며 curl 에도
  그대로 해당한다. `[INFRA-040]` 의 토큰 만료 오판과 판별 요령이 같다. **막아야 할 것과
  막지 말아야 할 것이 함께 무너지면 코드가 아니라 요청을 먼저 의심한다.**
- 메모: Flask 는 `SIGHUP` 으로 워커만 교체해 반영했다. `restart_all.sh` 를 통째로 돌리면
  Next 까지 재기동되는데, `useAdmin.ts` 변경은 Next 가 자동으로 다시 컴파일하므로 그럴
  이유가 없다. 반영 여부는 `?email=` 없는 요청이 400 을 내는지로 갈린다.
- 메모: `/api/admin/check` 는 `app/__init__.py` 의 `NOISY_ACTIVITY_PATHS` 에 들어 있어
  `logs/backend.log` 에 한 줄도 남지 않는다. **URL 에서 이메일이 사라진 효과는 접근 로그로
  확인할 수 없고 코드로만 확인된다.** 다음 사이클이 같은 확인을 반복하지 않도록 적어 둔다.
- 메모: 프론트엔드에는 검사를 더하지 않았다. 이 저장소의 훅 검사 관례는
  `useChatStream.test.ts` 처럼 순수 함수를 뽑아 보는 것이고, URL 문자열 한 줄에
  `renderHook` 과 `next-auth/react` 모킹을 세우는 배관이 검사보다 크다. 보안 회귀는 백엔드
  검사 둘이 막고, 프론트가 다시 쿼리를 붙여도 서버가 무시한다.
- 메모: 보안 리뷰 보고가 지적 4 의 중간에서 잘려 두 번 회신을 요청했고, **마감 직후에
  나머지가 도착했다.** 결론은 갈리지 않았다. 질문 5 에 대해 리뷰도 「`common_admin_routes.py`
  가 마지막이었고 이제 남은 자리가 없다」고 판정했으며, 세는 자리가 열(내 확인은 여덟)인
  것은 `kr_market_chatbot_http_routes.py` 에 네 자리가 몰려 있어 파일 단위로 세면 줄기
  때문이다. 지적 4 의 나머지에서 `[INFRA-042]` 목록에 없던
  `POST /api/system/start-update` 하나와 각 라우트의 화면 쪽 대응 위치를 얻어 그 항목에
  보강했다. 서버 측 관리자 게이트가 두 자리뿐이고 **둘의 판정 방식이 서로 다르다는**
  사실도 함께 적었다. 데코레이터를 만들 때 표준을 먼저 정해야 한다.
- 메모: 리뷰가 짚은 `X-Forwarded-For` 두 자리는 `[INFRA-045]` 가 이미 세 자리로 더 넓게
  잡고 있어 보탤 것이 없었다. **리뷰가 새로 찾은 것처럼 보여도 백로그를 먼저 대조한다.**

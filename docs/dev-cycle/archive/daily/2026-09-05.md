# 2026-09-05

## [FLOW-012] 참조 수급 자료를 믿기 전에 쓸 만한 값인지 본다
- 완료 2026-09-05 10:10 | 티어 T3 | 커밋 1db2212
- 변경: services/investor_trend_5day_service.py, tests/services/test_investor_trend_5day_service.py
  (+372 -6, 문서 +574 -31)
- 리뷰: superpowers:writing-plans(T3 계획 문서 작성) · ponytail-review(중첩 함수와
  독스트링 분량을 그대로 두는 편이 짧다고 판정) · feature-dev:code-reviewer(`None` 반환
  경로에서 `discarded_references` 가 실리지 못한다는 관찰, 확신도 낮음) · 테스트 리뷰
  (`zero_total` 과 `extreme_abs_total` 의 판정 단위가 달라 생기는 사각지대 확신도 7,
  고정 날짜의 시효 확신도 6) · /review 2회 — 지적 3건은 이유를 적어 미반영
- 검증: pytest 1634 통과 2 스킵 · vitest 237 통과 · QA 시나리오 8/8 통과 (헬스 98)
- 메모:
  **오늘 자료에서 `discarded_references` 는 한 건도 채워지지 않는다.** pykrx 가 `None` 을
  돌려주면 `_accept_reference` 가 `discarded` 에 넣지 않고 `False` 만 돌려주기 때문이다.
  거부 판정 자체가 트리거되지 않았으므로, 이번 사이클이 더한 방어 장치는 실데이터에서
  아직 발동한 적이 없다. 단위 검사 11건이 그 경로를 담당한다.

  **CSV 이상징후는 2002 종목 가운데 24건이다.** `stale_csv` 21건과 `single_day_spike`
  3건이며, `stale_csv` 21종목은 pykrx 와 Toss 참조가 둘 다 `None` 이라 `source=csv` 로
  남는다. 실제로 참조가 CSV 를 대체하는 것은 `single_day_spike` 3종목(022100 포스코DX,
  047050 포스코인터내셔널, 316140 우리금융지주)뿐이고, 이 셋은 QA 시나리오 S-3 이
  값까지 고정해 두었다.

  **`reference_only` 에 감점을 두지 않기로 했다.** 기본 참조인 pykrx 는 KRX 공식 자료라
  표식이 붙었다는 사실만으로 값이 덜 정확하다고 볼 근거가 없고, 감점을 넣으려면 등급으로
  바로 이어지는 `engine/screener_scoring_helpers.py` 의 계수를 관측 자료 없이 정해야 한다.
  표식이 실제로 얼마나 붙는지 관측한 뒤에 정한다.

  **QA 1단계가 찾은 발견 셋이 모두 범위 밖이었다.** 종목 상세 모달의 EPS 와 순이익이
  반대 부호를 가리키는 것을 `[JONGGA-033]`, 음수 PER 을 설명하지 못하는 툴팁을
  `[FE-035]` 로 올렸다. 모달 접근성은 `[FE-030]` 이 이미 같은 내용을 실측 근거와 함께
  담고 있어 새 항목을 만들지 않았다.

  **QA 2단계에서 시나리오 기대값 하나를 실측에 맞춰 고쳤다.** 1단계 리포트가 「S급 이상」
  필터에서 카드가 1건으로 줄었다고 적었으나, 오늘 시그널 아홉 건의 등급은 A 1건과 B 8건
  이고 S 는 없다. 「S급 이상」은 0건이 옳고 1건은 「A급 이상」의 결과다. 앱의 동작이 아니라
  기대값 쪽의 오류라 소스는 건드리지 않았다.

## [FLOW-013] SQLite 캐시에서 되살린 수급 항목이 5일치를 갖췄는지 확인한다
- 완료 2026-09-05 10:35 | 티어 T3 | 커밋 f13b23b
- 변경: services/investor_trend_5day_service.py, tests/services/test_investor_trend_5day_service.py
  (+92 -18, 문서 +369 -19)
- 리뷰: superpowers:writing-plans(T3 계획 문서 작성) · ponytail-review(독스트링 축소와
  `isinstance` 중첩 뒤집기와 로그 접두사 단축 셋을 반영해 6줄 감소, `len(details) != 5` 의
  상수화는 리뷰 쪽에서도 반대) · feature-dev:code-reviewer(다섯 항목 모두 타당 판정,
  독스트링의 「영영 수급 없음」 표현이 부정확하다는 지적을 반영해 정정) · /review
  (`OverflowError` 미처리 CRITICAL 확신도 10/10 — 실측으로 재현한 뒤 수정하고 회귀 검사
  추가) — 미반영 2건은 커밋 메시지에 이유를 적음
- 검증: pytest 1639 통과 2 스킵 · vitest 237 통과 · QA 시나리오 8/8 통과 (헬스 97)
- 메모:
  **`TODO.md` 가 적어 둔 T2 를 T3 으로 올렸다.** 대상 파일이 티어 규칙 §2 의 「수급 집계」
  위험 경로에 있기 때문이다. 규칙이 「티어를 낮추지 않는다」이므로 리뷰 세 종을 모두 돌렸고,
  그 셋째인 `/review` 가 아래의 `OverflowError` 결함을 찾았다. 티어를 그대로 두었으면
  놓쳤을 결함이다.

  **`OverflowError` 는 이번 변경 이전부터 있던 결함이다.** 이 페이로드는
  `load_json_payload_from_sqlite` 가 `json.loads` 로 복원하는데 JSON 은 `Infinity` 를
  표현할 수 있고 `int(float("inf"))` 는 `ValueError` 가 아니라 `OverflowError` 를 던진다.
  호출자 `_get_or_build_trend_map` 이 이 함수를 `try` 의 `else:` 절 안에서 부르므로 그
  예외는 잡히지 않고 종목 상세 요청 하나를 통째로 500 으로 만든다. 같은 파일의
  `_safe_int`(`:87`)가 이미 `OverflowError` 를 잡고 있었는데 새 코드가 그 교훈을 빠뜨렸다.
  이번 변경이 「손상 처리」를 명시적 책임으로 삼았으므로 함께 고쳤다.

  **손상 스냅숏은 재빌드 후 제자리에서 교체된다.** 실제 캐시로 확인했다.
  `investor_trend_5day_unified::latest` 스냅숏에서 로보티즈의 `details` 를 네 건으로 줄이고
  Flask 를 재기동한 뒤 수급 서비스를 부르니 경고를 남기고 버린 다음 CSV 에서 다시 만들어
  정확한 값을 돌려주었고, 같은 키의 스냅숏이 그 자리에서 갱신되어 2002 종목 전부 `details`
  가 다섯 건으로 복구되었다. `save_json_payload_to_sqlite` 가 `filepath` 를 키로
  `INSERT ... ON CONFLICT DO UPDATE` 를 쓰기 때문이며, 그래서 무한 캐시 미스가 생기지 않는다.

  **이 동작은 종목 상세 API 로 관찰할 수 없다.** 그 경로는
  `data/.kr_stock_detail_cache/runtime_cache.db` 에 종목별 응답 스냅숏을 시간 버킷 키로
  따로 두고 있어서 수급 조회 자체가 매 요청마다 일어나지 않는다. 수급 캐시 동작을 실측하려면
  `services.investor_trend_5day_service` 의 함수를 직접 호출해야 한다.

  **`_serialize_trend_map` 은 건드리지 않았다.** 그 함수의 입력은 언제나 `_build_trend_map`
  의 산출물이고 그 값이 이미 `int` 이므로 `continue` 분기에 도달할 수 없다.
  `feature-dev:code-reviewer` 도 호출자가 하나임을 grep 으로 확인해 같은 판단을 냈다.

  **`[INFRA-031]` 의 ID 충돌이 셋 해소되었다.** `[FLOW-011]`, `[FLOW-012]`, `[FLOW-013]` 의
  나중 항목이 차례로 완료되어 파일에서 빠졌으므로 남은 충돌은 `[INFRA-018]` 하나다.
  그 사실을 항목 본문에 반영했다.

## [INFRA-018] 휴장일 목록이 해를 넘기면 조용히 비어 버린다
- 완료 2026-09-05 11:30 | 티어 T2 | 커밋 cdf3f34
- 변경: engine/market_schedule.py, tests/engine/test_market_schedule_refactor.py,
  docs/dev-cycle/TODO.md, docs/dev-cycle/qa/INFRA-018.md (+206 -17)
- 리뷰: ponytail-review(net -21 제안, 셋 반영) · feature-dev:code-reviewer(다섯 건 지적,
  셋 반영) · 프론트엔드 스킬 해당 없음(`frontend/` 미변경)
- 검증: pytest 1641 통과 · vitest 237 통과(참고값, 미변경) ·
  QA 시나리오 9/9 통과 (헬스 99)
- 메모: **같은 번호의 항목이 둘이므로 이것은 「휴장일 목록」 쪽이다.** 1135번째 줄의
  `[INFRA-018] numpy 2.x 승격` 은 별개 항목이며 아직 백로그에 남아 있다.

  `_fallback_holidays` 가 다루는 마지막 해를 `_fallback_holiday_last_year` 상수로 두고,
  그 해를 지난 연도가 조회되면 경고를 남긴다. `known_holidays()` 가 종목마다 도는
  판정에서 불리므로 `functools.lru_cache` 로 워커마다 연도당 한 번만 내보낸다.
  gunicorn 을 `--workers 2` 로 띄우므로 「연도마다 한 번」이 아니라 「워커마다 연도당
  한 번」이다.

  **2027년 휴장일을 미리 채우지 않기로 했다.** 음력 공휴일과 대체공휴일은 공고로
  확정되므로 추정한 날짜를 넣으면 틀린다. pykrx 로 한 해치를 받아 캐시하는 경로도
  택하지 않았다. `known_holidays()` 의 「네트워크 없이 아는 만큼만」이라는 설계 전제와
  충돌하기 때문이다. 그 전제는 `is_market_open` 이 하루씩만 판정하는 것과 짝을 이룬다.

  **목록과 상수가 어긋났는지 검사하려던 단언을 지웠다.** 지운 것은
  `max(_fallback_holidays).year == _fallback_holiday_last_year` 였다. 「목록을 채우고
  상수 올리기를 잊은 실수」와 「한 해를 부분만 채운 의도된 상태」가 `max().year >
  last_year` 로 똑같이 관측되어 하나의 단언으로 가를 수 없다. 상수를 따로 둔 이유가
  바로 후자를 허용하기 위해서이므로, 그 검사는 상수의 존재 이유 자체를 금지한다.
  `feature-dev:code-reviewer` 가 2027년 신정만 넣고 상수를 2026 에 남긴 상태를 실제로
  만들어 검사 실패를 보였다. 그 계약은 상수 주석의 마지막 문장으로 옮겼다.

  **검사 단계에서 목록이 낡는 것을 잡을 장치는 이제 없다.** 오늘 날짜와 상수를
  비교하는 검사를 두면 2027년 1월에 CI 가 실패하는데, 그것은 테스트 정책이 지우라고
  지정한 「특정 시점의 값을 고정 검사해 지금은 항상 실패하는 테스트」에 정확히
  해당한다. 운영 로그의 경고가 유일한 통로인 것이 이 항목의 의도된 설계다.

  **두 리뷰가 충돌해 정확성을 택한 자리가 있다.** ponytail-review 는 경고 함수의
  `last_year` 인자를 없애고 본문에서 클래스 상수를 읽으라고 했다. 그대로 반영했으나
  code-reviewer 가 그러면 상수가 `lru_cache` 키에서 빠져 검사가 조용히 통과한다는 것을
  `CacheInfo` 실측으로 보였다(상수를 2020 에서 2019 로 바꿔도 두 번째 호출이 캐시에
  적중). 인자를 `(last_year, asked_year)` 로 되돌려 캐시 키 문제와 클래스 이름
  하드코딩을 함께 해소했다.

  **반영하지 않은 지적 셋.** ① `np.busday_count` 구간의 아래쪽 끝인 `latest_date` 가
  채우지 않은 해에 걸리면 여전히 조용하다. 목록을 누적하는 전제라 지금은 구멍이 없고,
  오래된 해를 정리하는 작업이 생길 때 다룰 문제다. ② `date.today()` 가 시스템 로컬
  시간을 읽어 UTC 서버에서 경고가 아홉 시간 늦는다. 같은 모듈의
  `_current_validation_token` 도 `datetime.now()` 를 쓰므로 일관성을 택했다.
  ③ `_fallback_holidays` 가 `_market_schedule_sqlite_cache_context` 의 서명 계산에도
  쓰여(`:104`) 목록에 새 해를 더하면 기존 캐시가 전부 무효가 된다. 목록을 갱신하면
  캐시가 새로 채워지는 것이 옳은 동작이다.

  **`[INFRA-031]` 의 ID 충돌이 모두 해소되었다.** 이 항목이 완료되어 빠지면서
  `[INFRA-018]` 도 하나만 남았다. 그 사실을 항목 본문에 반영했다.

  **QA 가 이월한 발견 둘.** 종가베팅 화면에서 필터로 결과가 0건이 되면
  「TRENDING THEMES」까지 함께 비고(`[JONGGA-034]`), 빈 상태 안내만 영어로 남아 있다
  (`[JONGGA-035]`). 둘 다 이번 항목이 고치기로 한 자리가 아니라 백로그로 올렸다.

## [FE-007] 모달 셸을 통합하고 대형 클라이언트 컴포넌트를 나눈다
- 완료 2026-09-05 12:55 | 티어 T3 | 커밋 5fedb18
- 변경: `Modal.tsx`, `BuyStockModal.tsx`, `SellStockModal.tsx`, `ConfirmationModal.tsx`,
  `StockTradeHistoryModal.tsx`, `PaperTradingModal.tsx`, `page.tsx` 수정 +
  `PaperTradingAssetChart.tsx`, `PaperTradingAccountActions.tsx`, `ScoringLogicTabs.tsx`,
  `paperTradingFormat.ts`, `ModalShell.test.tsx` 신규 (+1845 -1491, 순 +354)
- 리뷰: `frontend-skills.md` §2(`frontend/src/app` 을 건드리므로 번들 문서 배정) ·
  next 번들 문서 `05-server-and-client-components.md`(프롭으로 넘긴 서버 컴포넌트가
  클라이언트 모듈 그래프에 들어가지 않는다는 근거를 여기서 확인) ·
  ponytail-review(net -32 제안, 다섯 반영) · feature-dev:code-reviewer(여덟 건, 넷 반영) ·
  /review(Pass 1 해당 없음, informational 3건)
- 검증: pytest 1641 통과(백엔드 미변경) · vitest 248 통과 · `tsc --noEmit` 통과 ·
  `npm run build` exit 0 · QA 시나리오 9/9 통과
- 메모: 모달 다섯 벌이 저마다 갖고 있던 오버레이와 Escape 와 ARIA 속성을 `ModalShell`
  하나로 모았다. 종전에는 갖춘 것이 서로 달라 매수·매도 모달에서 Escape 가 듣지 않았고
  확인 대화상자에는 배경 판이 없었다. `PaperTradingModal` 은 1,154줄에서 552줄로 줄었고,
  랜딩 `page.tsx` 는 `'use client'` 없이 684줄이 되었다.

  **리팩터링이 만든 회귀 셋을 이번 사이클 안에서 모두 없앴다.** ① 차트를 떼어내자 탭을
  벗어날 때 언마운트되어 기간과 이동평균선 선택이 사라졌다. React 19.2 의 `<Activity>`
  로 감쌌다. 상태는 지키고 숨은 동안 effect 는 정리한다. code-reviewer 가 임시 검사로
  다섯 가지 동작을 확인해 이 선택이 성립함을 확정했다. ② 그 경계가 `loading` 분기 안에
  있으면 입금 경로에서 무효가 되므로 `fetchPortfolio` 가 첫 조회에만 스피너를 띄우게
  했다. ③ 셸을 모으면서 매수·매도 모달에도 Escape 가 생겼는데, 리스너가 셸마다
  `document` 에 붙어 Escape 한 번에 겹친 두 모달이 함께 닫혔다. 브라우저 실측에서
  찾았고, 마운트 순서를 쌓아 맨 위의 셸만 반응하게 고쳤다. 판정 줄을 임시로 무력화해
  새 검사 두 건이 실제로 실패하는 것까지 확인했다.

  **`/qa-only` 가 콘솔 오류 하나를 찾았다.** 서버 컴포넌트가 프롭으로 넘긴 세 패널이
  배열 자식으로 취급되어 React 가 `key` 를 요구했다. 각 패널의 최상위 요소에 `key` 를
  주어 없앴다. 같은 리포트가 High 로 올린 「매수 버튼이 거래 내역 모달을 연다」는
  결함이 아니었다. `browse` 의 접근성 트리 참조가 부모 행에 매핑된 도구 아티팩트이며,
  셀렉터 클릭으로 재검증해 정상 동작을 확인했다.

  **반영하지 않은 지적 셋.** ① `backdropClassName` 프롭을 없애고 배경 어둡기를 하나로
  통일하라는 것 — 시각이 달라지면 이번 리팩터링이 지키려는 「보이는 것은 그대로」가
  무너진다. ② `ConfirmationModal` 의 `role="alertdialog"` 와 ③ 여섯 모달의 초점 관리는
  옳지만 이번 항목이 정한 다섯 가지에 없다. 배경 스크롤 잠금과 함께 `[FE-030]` 에
  넣었다. 그 항목은 종전부터 있었고 이번 사이클이 그중 Escape 닫기와 `aria-labelledby`
  둘을 이미 해결했으므로 새 항목을 만들지 않고 갱신했다.

  **`next-best-practices` 가 폐지된 스킬임을 확인해 규약을 고쳤다**(커밋 386cf27).
  `frontend-skills.md` §2 가 그 스킬을 예외 없이 읽으라고 규정하고 있었으나 설치되어
  있지 않았다. 미설치가 아니라 상류에서 없어진 것이었다. `vercel-labs/next-skills` 는
  안내문만 남은 저장소가 되었고 그 README 가 세 스킬의 행선지를 적어 두었다. 대체 경로인
  `frontend/node_modules/next/dist/docs/01-app/` 를 규약에 직접 적고, 무엇을 건드릴 때
  어느 문서를 열지 여섯 줄짜리 표로 정했다. 같은 규정이 있던 `CLAUDE.md` 와
  `dev-workflow` 에이전트와 `archive-format.md` §5 예시도 함께 고쳤다.

  **QA 가 이월한 발견 하나.** 모달 진입 애니메이션 클래스 다섯 가지가 컴파일된 CSS 에
  없어 아무 일도 하지 않는다(`[FE-036]`). `tailwindcss-animate` 가 설치되어 있지 않고
  `tailwind.config.ts` 의 `plugins` 가 비어 있다. 실제로 정의된 것은
  `globals.css:117` 의 `animate-fade-in` 하나뿐이다.

## [JONGGA-006] 종가베팅 페이지에서 범용 컴포넌트와 정적 모달 분리
- 완료 2026-09-05 17:45 | 티어 T3 | 커밋 eb6decb
- 변경: frontend/src/app/dashboard/kr/closing-bet/page.tsx, frontend/src/app/components/Tooltip.tsx,
  frontend/src/app/dashboard/kr/closing-bet/displayPrimitives.tsx 외 8개 (코드 +385 -385 · 문서 +515 -16)
- 리뷰: next 번들 문서 05-server-and-client-components(새 파일 둘의 `'use client'` 가 엄밀히는
  불필요함을 확인했으나 `CLAUDE.md` Code Style 과 `ClosingBetCriteriaModal` 선례를 따라 붙였다) ·
  vercel-composition-patterns(`wide` 불리언을 `size` 열거형으로 바꾸는 근거가 되었다.
  다만 `patterns-explicit-variants` 의 변형 컴포넌트 분리는 따르지 않았다) ·
  ponytail-review(2건 적용, net -6) · feature-dev:code-reviewer(결함 0건, 정리 2건 적용) ·
  review(지적 없음)
- 검증: pytest 1641 통과 · vitest 252 통과 · tsc 통과 · npm run build 통과 · ESLint 오류 0건 ·
  QA 시나리오 10/10 통과 (헬스 98)
- 메모: **TODO 가 적은 이동처와 조사 결과가 갈렸다.** TODO 는 `PriceRangeBar`·`StatBox`·`ScoreBar`
  를 「컴포넌트 디렉터리로 이동」이라 적었으나, `ScoreBar` 는 저장소 전체에서 부르는 자리가 0건인
  죽은 코드였고 `PriceRangeBar` 는 같은 디렉터리의 `displayHelpers.isPositivePrice` 에 의존했다.
  공용 디렉터리로 올리면 공용 컴포넌트가 특정 라우트의 모듈을 가로질러 가져오게 되어 의존 방향이
  뒤집히므로 `closing-bet/displayPrimitives.tsx` 로 옮겼다. 그 디렉터리에는 `displayHelpers.ts`
  라는 선례가 이미 있다.

  **툴팁 통합의 기본값을 호출 건수로 정했다.** 사본 세 벌의 시각이 서로 달라 그냥 갈아치우면
  화면이 바뀐다. 작은 시각 52건 대 큰 시각 17건이므로 기본값을 `sm` 으로 두고 큰 시각 17곳에만
  `size="lg"` 를 붙였다. 반대로 했다면 51곳의 툴팁이 커지고 왼쪽 정렬로 바뀌어 리팩터링이 시각
  변경을 포함하게 된다. QA 의 S-1 과 S-3 이 네 화면에서 이 판단을 검증했다.

  **`vercel-composition-patterns` 의 규칙을 그대로 따르지 않았다.** `patterns-explicit-variants`
  는 불리언 모드 대신 변형 컴포넌트를 나누라고 하지만, 그 규칙이 겨냥하는 것은 `isThread` 처럼
  렌더 트리 구조가 갈리는 불리언이다. 세 사본은 트리가 동일하고 CSS 클래스 문자열만 다르므로
  변형을 나누면 통합의 목적이 사라진다. 규칙의 취지인 불리언 제거만 취해 열거형으로 갔다.

  **QA 가 이월한 발견 둘.** ① 툴팁이 뷰포트와 스크롤 컨테이너의 경계를 보지 않아 종목 상세 모달의
  「시세 정보」 툴팁이 위쪽 46px 잘리고, 모바일 폭 375px 에서는 툴팁 200개 가운데 67개가 화면 밖으로
  나간다(`[FE-037]`). 통합 전 세 사본도 같은 배치 규칙과 같은 폭을 썼으므로 이번 변경이 만든 것이
  아니며, 사본이 하나로 모인 지금은 한 곳을 고치면 세 화면이 함께 고쳐진다. ② 종목 상세 모달에
  `role="dialog"` 가 없는 것은 이미 `[FE-030]` 의 체크박스에 있어 그 항목에 실측 근거만 보탰다.

  **`[FE-007]` 이 남긴 ESLint 오류 하나를 분리 커밋으로 고쳤다**(커밋 7d1dab2). 검사의 지역
  컴포넌트 이름이 한글이라 `react-hooks/rules-of-hooks` 가 컴포넌트로 알아보지 못했다. 규칙은
  대문자로 시작할 것을 요구하는데 한글에는 대소문자가 없다. vitest 는 lint 를 돌리지 않으므로
  그 사이클의 검증을 통과했다.

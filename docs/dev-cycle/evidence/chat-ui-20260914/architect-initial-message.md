# 독립 아키텍처 중간 원문

ARCHITECTURE 현재 판정 BLOCK. 핵심 3건: (1) 실측 확인된 off-by-one: VCP UI가 client-only 환영 메시지를 chatHistory에 넣은 최초 대화에서 버튼은 UI i를 그대로 DELETE index로 전달(page.tsx:1982-2000,2052-2055→405-410), 서버는 저장 history의 순수 0-based index를 삭제(request_helpers.py:75-87; storage.py:322-333). preflight before DB user/model 2개, after user만 남음(preflight-history-before.json:2-20, after.json:2-11)이라 질문 버튼이 모델을 삭제. (2) 중복가드는 modal confirm에만 있음(page.tsx:437-455); /clear는 delete primitive를 직접 호출(1080-1086), primitive 자체엔 guard 없음(405-435). 세 진입점 공통 중복방지 미충족. (3) 모바일 drawer window Escape(챗봇 page:202-211)는 drawer 안 세션 삭제가 confirmation을 열어도 drawer가 열린 채(page:621-623), ModalShell document Escape(Modal.tsx:52-62)와 같은 이벤트에 둘 다 반응해 한 번에 모달+drawer를 닫음. QA S-2에 중첩모달 Escape가 없음. 추가로 streaming 중 삭제 시 DELETE 성공 후 기존 SSE가 sessionKey/chatHistory를 다시 쓸 수 있음(vcp page:1094-1105,1130-1162,1205-1236 vs delete:422-430); QA S-7은 지연 DELETE/종목전환만 있어 send+delete race 미포함. 수정 후 재리뷰 필요.

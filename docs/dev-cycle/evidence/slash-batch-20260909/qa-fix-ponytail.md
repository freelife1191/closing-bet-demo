Lean already.

`Sidebar.tsx:87`은 기존 `getAuthHeaders()`를 재사용한 1줄 수정입니다. 회귀 테스트도 최초 조회와 갱신 이벤트라는 서로 다른 경계를 직접 검증합니다.

net 0 lines possible. 매니페스트 9개와 루트 `package.json` 해시가 일치합니다.

Ponytail verdict: Ship.

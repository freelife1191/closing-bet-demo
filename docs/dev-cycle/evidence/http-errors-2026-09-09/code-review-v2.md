INFRA-017·038 revision2 narrow 결과 — ponytail: APPROVE(과잉0), code-reviewer: APPROVE(issues0), confidence0.97. review-input revision2 scope4 SHA 일치. `common_update_routes.py:83-90`의 HTTPException 별도except+type-only warning+raise는 wrapper가 malformedJSON400/form415를 500으로 삼키는 root cause를 3줄로 직접 수정; fallback/새helper 없음. `test_common_routes_refactor.py:354-364`는 실제 log-event wrapper에서 JSON malformed400/form415, external internal-error 없음, ERROR log 없음을 검증. 작은 앱에는 global handler가 없어 wrapper 자체를 분리검증하고 v1 `test_global_error_contract.py` actual factory HTTPException tests와 중복 아님. 일반 예외 default fixed500/internal raw log 유지. 테스트/서버 실행 안 함; v2 fullpytest 진행중이라 완료 주장 안 함. code finding 없음.

아키텍처 핵심 발견: BLOCK 후보. app/routes/common_update_routes.py:183-185의 request.get_json()이 malformed JSON에서 BadRequest(HTTPException)를 내지만, 같은 파일 :83-87 공용 _execute_update_route의 except Exception이 먼저 잡아 고정 500으로 변환합니다. 따라서 app/__init__.py:268-270의 HTTPException 보존 분기까지 도달하지 않아 'HTTPException original response/status' 수용조건이 common log-event에서 깨집니다. 이 공용 helper는 이번 범위(env GET/log-event shared errors) 안이므로 except HTTPException: raise 후 일반 Exception만 500 처리 권고. 신규 tests는 global 400/429/405만 있고 이 wrapper 우회 케이스가 없음(test_global_error_contract.py:47-63; test_common_routes_refactor.py:316-351).

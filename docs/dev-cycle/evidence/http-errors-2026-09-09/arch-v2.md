v2 좁은 재검토 결과: Architectural Status CLEAR. common_update_routes.py:83-90에서 HTTPException을 type-only WARNING 후 재발생시켜 global/default Flask 처리로 위임하고, 일반 Exception만 고정500 처리합니다. 이전 BLOCK 원인인 log-event request.get_json(:186-188) BadRequest/UnsupportedMediaType 선점이 해소됐습니다. tests/app/test_common_routes_refactor.py:354-364가 malformed JSON 400/form 415 및 ERROR 로그 부재를 고정합니다. revision2 manifest의 6개 SHA 모두 현재 파일과 일치. custom response는 app/__init__.py:268-270의 error 객체 반환 + Werkzeug HTTPException.get_response가 self.response를 그대로 반환(venv/.../werkzeug/exceptions.py:162-163)하므로 구조적으로 보존됩니다. warning 로그는 공개 log-event의 malformed 요청마다 1건 발생할 수 있어 운영 노이즈 tradeoff는 있으나 type만 기록해 노출/계약 blocker는 아님. QA 문서 행렬 갱신과 동적 probe/full pytest는 실행 주체의 최종 증거로 남음; 본 검토자는 지시대로 실행 안 함.

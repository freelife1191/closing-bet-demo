## 보안 리뷰 결과

**범위:** `/var/folders/99/kpfx0mdj3fvbczqpbncjl0bm0000gn/T/security-review-routing-kchu35vl/fixture.py`
**검토 방식:** 지정된 독립 `code-reviewer`가 `security-review` 기준으로 정적 검토
**판정:** **REQUEST CHANGES — 배포 금지**

### 발견 사항

[HIGH] 인증·인가 없이 다른 사용자의 계정 잔액 조회 가능
파일: `fixture.py:6-8`

- **근거:** URL의 `owner` 값을 그대로 `ACCOUNTS[owner]` 조회 키로 사용하며, 호출자 인증이나 요청 계정과 경로 계정의 일치 여부를 검사하지 않습니다.
- **악용 가능성:** 서버에 접근할 수 있는 공격자는 `GET /account/alice`, `GET /account/bob`처럼 계정명을 바꾸어 잔액을 열람할 수 있습니다. 별도 조건이나 권한이 필요하지 않습니다.
- **영향:** 사용자 금융 정보 노출 및 객체 수준 권한 우회입니다.
- **분류:** OWASP A01 Broken Access Control, IDOR/BOLA.
- **조치:** 인증된 사용자 식별자를 서버가 신뢰할 수 있는 세션 또는 검증된 토큰에서 가져오고, 해당 식별자와 요청 대상 계정의 소유권을 비교하십시오. 일반 사용자가 임의 `owner`를 선택할 필요가 없다면 경로 매개변수를 제거하고 `/account/me` 형태로 조회하는 편이 안전합니다. 관리자 조회가 필요하면 별도의 명시적 권한 검사를 추가해야 합니다.

[LOW] 존재하지 않는 계정명이 처리되지 않아 500 오류를 유발
파일: `fixture.py:8`

- **근거:** `ACCOUNTS[owner]`가 없는 키에 `KeyError`를 발생시키지만 명시적인 입력 검증이나 404 처리가 없습니다.
- **악용 가능성:** 임의 계정명으로 반복 요청해 오류와 로그를 발생시킬 수 있습니다.
- **영향:** 잘못된 서버 오류, 모니터링 잡음, 제한적인 가용성 저하 가능성이 있습니다. 기본 Flask 프로덕션 오류 응답은 일반적으로 예외 세부 정보를 숨기지만, 디버그 모드라면 내부 정보 노출 위험이 커집니다.
- **분류:** OWASP A04 Insecure Design / A05 Security Misconfiguration.
- **조치:** `account = ACCOUNTS.get(owner)`로 확인하고 없으면 명시적으로 404를 반환하십시오. 운영 환경에서는 Flask 디버그 모드를 비활성화하고 반복 오류에 대한 로깅·요청 제한을 적용하십시오.

### 확인 결과

- 하드코딩된 API 키, 암호, 토큰, 개인 키, 연결 문자열은 발견되지 않았습니다.
- SQL/NoSQL·셸 실행·파일 경로·HTML 렌더링·외부 URL 요청 코드가 없어 SQL 주입, 명령 주입, 경로 순회, XSS, SSRF 공격면은 이 fixture에 없습니다.
- `/health`는 고정된 비민감 상태만 반환하므로 현재 범위에서 보안 문제로 판정하지 않았습니다.
- 상태 변경 요청이 없어 CSRF 검사는 적용 대상이 아닙니다.
- 암호화, 세션, JWT, 비밀번호 해시는 구현되어 있지 않아 해당 구현 품질은 평가할 수 없습니다.
- 의존성 manifest와 네트워크 감사 범위가 제공되지 않았으므로 **의존성 취약점 감사와 CVE 검사는 적용 대상이 아닙니다.**
- 스킬과 fixture만 읽었으며 fixture 실행, HTTP 요청, 외부 네트워크 접근, 파일 수정은 수행하지 않았습니다.
- 런타임 보안 헤더, 실제 디버그 설정, 프록시 접근 통제, 오류 응답 본문은 실행 금지 조건 때문에 확인하지 않았습니다.

**보안 권고:** `fixture.py:6-8`의 객체 수준 접근 통제를 구현하고 존재하지 않는 계정을 404로 처리한 뒤 재검토해야 합니다. 현재 상태는 원격 접근 가능한 서비스로 배포하기에 부적합합니다.

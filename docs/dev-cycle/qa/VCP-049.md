# [VCP-049] 스크리너 실패가 「시그널 없음」으로 처리되어 그 날짜의 기존 시그널 행을 지운다 — QA 기록

- 대상: `engine/screener.py` 의 `run_screening`. 결과를 만들지 못한 실패는 예외로 올리고, 조건 충족 0건만 빈 프레임으로 돌려준다
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-24 19:31 | 실행 2026-09-24 19:33:08~19:33:23, 실패 원인 로그 재확인 19:33
- 검증 기준 커밋: `0946595` (이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `6d1ba81`. 사본은 각 커밋의 `git archive` 이며 실행 중 범위 파일은 바뀌지 않았다
- 구성 근거: 설계 승인(대화 19:29), 호출 경로 추적(스케줄러·관리자 「Refresh VCP」 → `create_signals_log` → `SmartMoneyScreener.run_screening`)
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 화면 진입점인 관리자 「Refresh VCP」는 실제 수집과 LLM 호출로 이어지는 금지 조작이다. 결과는 `signals_log.csv` 의 행 보존 여부와 함수 반환값이다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본 `signals_log.csv`(2026-05-05 15행)·`korean_stocks_list.csv`·`all_institutional_trend_data.csv`·`daily_prices.csv` 를 복사한다. 소켓 연결을 막아 네트워크 호출을 차단한다. `run_ai=False` 라 LLM 호출이 없다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp049qa_harness.py`

## 시나리오

### S-1. Market Gate 예외에서 그 날짜의 기존 행이 남고 실패로 끝난다 (하네스, 필수)
- 범위: 실제 `MarketGate.analyze` 는 내부 예외를 잡아 기본값(Open)을 돌려주므로 운영에서 Gate 데이터 실패는 여기까지 오지 않는다. 이 시나리오는 `analyze` 자체가 예외를 내는 방어 경로를 확인한다
- 조작: `MarketGate.analyze` 가 예외를 내게 하고 `create_signals_log(target_date="2026-05-05", run_ai=False)`
- 기대: 수정 뒤 `False`, 15행 그대로(`same_content=True`). 수정 전 사본은 `True`, 2026-05-05 행 0건
- 실제: 수정 뒤 `result=False`, 15행 그대로(`same_content=True`), 로그 「스크리닝 실패: QA: market gate down」→「VCP 분석 오류 - 기존 로그 보존, 빈 최신 결과 저장」. 수정 전 사본은 `result=True`, 행 0건. 통과

### S-2. 가격 파일이 헤더만 있을 때 기존 행이 남고 실패로 끝난다 (하네스, 필수)
- 조작: 사본 `daily_prices.csv` 를 헤더만 남기고 같은 호출
- 기대: 수정 뒤 `False`, 15행 그대로. 수정 전 사본은 `True`, 행 0건
- 실제: 수정 뒤 `result=False`, 15행 그대로, 로그 「스크리닝 실패: 필수 데이터(종목 목록·일별 가격)가 없거나 비어 있습니다.」. 수정 전 사본은 `result=True`, 행 0건. 통과

### S-3. 조건 충족 종목이 실제로 없으면 종전처럼 그 날짜를 정리한다 (하네스, 필수, 회귀 확인)
- 조작: Market Gate 정상, 종목 분석이 결과를 내지 않게 하고 같은 호출
- 기대: 수정 전후 모두 `True`, 2026-05-05 행 0건
- 실제: 수정 전후 모두 `result=True`, 2026-05-05 행 0건. 통과

## 정리
- 실제: 사본 둘(`0946595`·`6d1ba81`) 삭제 뒤 없음 확인. 서버를 띄우지 않았고 3500·5501 리스너 0. 원본 `data/signals_log.csv`(1789994506)·`data/daily_prices.csv`(1789994444) 수정 시각 전후 같음(읽기 복사만). 소켓 차단·`run_ai=False` 로 네트워크·LLM 호출 없음. 결과: 필수 3/3 통과

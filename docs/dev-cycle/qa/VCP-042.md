# [VCP-042] 실패 재분석 저장이 티커 앞자리 0 을 지우지 않는다 — QA 기록

- 대상: `services/kr_market_vcp_reanalysis_service.py`(`write_vcp_signals_csv_atomic` 이 `to_csv` 직전에 `ticker` 를 6자리로 맞추고 결측은 빈 칸으로 둠)
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-24 17:21 | 실행 2026-09-24 17:23:16~17:23:19(1차), 17:23:38~17:23:40(2차)
- 검증 기준 커밋: `a374076` (이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `8405e74`. 사본은 각 커밋의 `git archive` 이며 실행 중 범위 파일은 바뀌지 않았다
- 구성 근거: 설계 승인(대화 17:18), 호출 경로 추적(관리자 「AI 재분석」 → `kr_market_data_signals_routes` 가 공유 로더 `load_csv_file` 로 usecols 읽기 → `execute_vcp_failed_ai_reanalysis` → 잠금 안에서 `write_vcp_signals_csv_atomic` 이 같은 로더로 전체를 다시 읽어 병합·저장)
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 화면 진입점인 관리자 「AI 재분석」 버튼은 LLM 을 부르는 금지 조작이다. 결과는 파일 원문의 티커 표기라 화면에서 보이지 않는다(읽는 쪽이 모두 zfill 한다)
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본 `signals_log.csv` 만 복사한다. 로더의 SQLite 스냅샷(`runtime_cache.db`)은 CSV 옆, 곧 사본 `data/` 에 생긴다. 분석기는 가짜(`analyze_batch` 가 000660 에만 유효한 Gemini 판정을 돌려줌). LLM·네트워크 호출 없음. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp042qa_harness.py`. 1회차는 CSV 직접 읽기, 2회차는 메모리 캐시를 비워 SQLite 스냅샷(`read_json`) 복원 경로를 탄다
- 실행 1차(17:23:16) 하네스 보정: 2회차의 SQLite 적중이 `False` 였다. 1회차 저장으로 파일 시그니처가 바뀌어 새 시그니처의 스냅샷이 없었기 때문이다(코드 결함 아님). 기대값은 바꾸지 않고, 2회차 전에 usecols·전체 읽기를 한 번씩 해 스냅샷을 만든 뒤 메모리 캐시만 비우도록 하네스를 고쳐 2차(17:23:38)로 다시 돌렸다. S-1·S-3 결과는 1차와 2차가 같다. 아래 실제는 2차 결과다

## 시나리오

### S-1. 재분석 저장 뒤 모든 행의 티커가 6자리로 남는다 (하네스, 필수)
- 조작: 1회차 재분석(2026-05-05) 뒤 `signals_log.csv` 원문의 티커 열을 읽는다
- 기대: 수정 뒤 6자리 미만 티커 0건. 수정 전 사본은 `000660`→`660` 처럼 앞자리 0 이 빠진 티커가 생긴다
- 실제: 수정 뒤 `status=200`, 「실패 15건 중 1건 재분석 완료」, `short tickers: []`(15종목 모두 6자리). 수정 전 사본은 같은 응답에 `short tickers: ['660', '3550', '5380', '5930', '28260', '32830', '34730', '64350']`. 통과

### S-2. SQLite 스냅샷 복원 경로에서도 앞자리 0 이 유지된다 (하네스, 필수)
- 조작: 메모리 캐시를 비우고 2회차 재분석, SQLite 적중 여부와 원문 티커를 본다
- 기대: 전체 읽기가 SQLite 에서 적중하고(수정 뒤), 6자리 미만 티커 0건. 수정 전 사본은 짧은 티커가 남는다
- 실제: 수정 뒤 `sqlite loads (usecols, hit): [((usecols), True), (None, True)]`, 「실패 14건 중 0건 재분석 완료」(갱신 0건에도 파일을 다시 씀), `short tickers: []`. 수정 전 사본도 두 읽기 모두 적중했고 짧은 티커 8건이 그대로 남았다. 통과
- 부수 관찰(범위 밖, 수정 전후 동일): SQLite 경로로 다시 쓰면 `1447000.0`·`14.0` 같은 실수 표기가 `1447000`·`14` 로 바뀐다. 값은 같아 TODO 로 올리지 않았다

### S-3. 갱신 대상 행의 AI 값은 그대로 반영된다 (하네스, 필수, 회귀 확인)
- 조작: 1회차 뒤 000660 행 원문을 읽는다
- 기대: 수정 전후 모두 `BUY`, 70, `VCP042-QA 수축 뒤 돌파 확인`
- 실제: 수정 뒤 원문 `000660,SK하이닉스,2026-05-05,…,BUY,70.0,VCP042-QA 수축 뒤 돌파 확인,…`. 수정 전 사본은 같은 값에 티커만 `660`. 통과

## 정리
- 실제: 사본 둘(`a374076`·`8405e74`) 삭제 뒤 없음 확인. 서버를 띄우지 않았고 3500·5501 리스너 0. 원본 `data/signals_log.csv` 수정 시각 전후 같음(1789994506, 읽기 복사만), 원본 `data/runtime_cache.db` 수정 시각은 09-23 17:05 그대로(스냅샷은 사본 `data/` 에만 생김). `analyze_batch` 는 가짜라 LLM·네트워크 호출 없음. 결과: 필수 3/3 통과

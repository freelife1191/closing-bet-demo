# [VCP-045] Z.ai 폴백의 `zai_disabled_reason` 도 워커 수명 동안 풀리지 않는다 — QA 기록

- 대상: `engine/vcp_ai_analyzer.py` 의 `_expire_session_blocks`·`_analyze_with_zai`, 프로세스 싱글톤 `get_vcp_analyzer()`
- 단계(phase): 운영 로그 확인 대기(운영자) | 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24 10:46 | 실행 (미실행)
- 구성 근거: 설계 승인(10:43), critic 계획 검토 ACCEPT-WITH-RESERVATIONS(동시성 상한·`analyze_stock` 경로 테스트 반영), 호출 경로 추적(`analyze_stock` → 오케스트레이션 → `_analyze_with_gpt` → `_fallback_from_gpt` → `_analyze_with_zai`, `:1026` 에서 플래그를 켬)
- 검증 기준 커밋: (첫 커밋 뒤 기록)
- QA 엔진(engine): Claude Code. Z.ai 결과는 두 번째 자리(`gpt_recommendation`)로 저장되고 VCP 화면(`/dashboard/kr/vcp`)의 GPT 열이 그것을 읽으므로 browser_applicability: required(browser_driver: agent-browser)
- 하네스: 첫 커밋의 `git archive` 사본, `secrets/`·`data/`·`.env` 삭제 뒤 `data/` 에 공개 시장 자료(`daily_prices.csv`·`korean_stocks_list.csv`·`signals_log.csv`)만 복사한다. LLM 키는 넣지 않는다. 한 프로세스가 오래 사는 워커를 흉내 낸다: `VCP_AI_PROVIDERS=gemini,gpt,zai` 로 싱글톤을 만든 뒤 `gemini_client`·`gpt_client`·`zai_client` 만 가짜로 바꾸고 `second_provider="gpt"` 로 둔다. GPT 는 `VCP_GPT_MODEL` 을 빈 값으로 두어 매번 Z.ai 로 넘어가게 한다(쿼터 플래그를 켜지 않으므로 Z.ai 플래그만 관찰된다). 가짜 Gemini 는 늘 HOLD, 가짜 Z.ai 는 모드 echo = 메타 응답 텍스트, 모드 ok = `{"action":"BUY","confidence":88,"reason":"VCP045-QA zai ok ..."}` 이며 호출 수를 센다. 경과 시간은 모듈 `time` 에만 오프셋을 더해 흉내 낸다. 수집은 `create_signals_log("2026-09-21", run_ai=True, signal_limit=2)`
- 원본 `data/`·`logs/`·3500·5501·운영 주소는 건드리지 않고, 실제 LLM·발송은 하지 않는다
- 필수 여부(required): S-1~S-4 예
- 반복(iteration): (실행 뒤 기록)
- 결과: (실행 뒤 기록)
- 증거: (실행 뒤 기록)
- 정리(cleanup): (실행 뒤 기록)

## 시나리오

### S-1. 메타 응답 반복으로 Z.ai 가 꺼지고 시각이 기록된다 (회귀)
- 조작: 가짜 Z.ai echo 모드, 오프셋 0 에서 수집 1회
- 기대: `zai_disabled_reason == "prompt-echo responses"`, 수집 뒤 `_session_blocks_since` 가 None 이 아님(수정 전 코드는 None), 캐시의 `gpt_recommendation.reason` 에 「VCP045-QA zai ok」 없음

### S-2. TTL 안에서는 꺼진 상태를 유지한다 (대조)
- 조작: ok 모드, 오프셋 +300초에서 수집 1회
- 기대: 가짜 Z.ai 호출 0회, `zai_disabled_reason` 유지

### S-3. 10분이 지나면 Z.ai 를 다시 부른다 (핵심)
- 조작: ok 모드, 오프셋 +601초에서 수집 1회
- 기대: 가짜 Z.ai 호출 ≥ 2(종목당 1), `zai_disabled_reason` None, 캐시 두 종목 `gpt_recommendation.action` BUY·「VCP045-QA zai ok」, 로그 「VCP 분석기 세션 차단 해제: … zai=prompt-echo responses」

### S-4. VCP 화면 GPT 열 (브라우저)
- 조작: S-3 뒤 사본에서 gunicorn·Next dev 를 띄우고 `http://localhost:<port>/dashboard/kr/vcp` 를 넓은 뷰포트로 연다
- 기대: `/api/kr/signals` 두 종목의 `gpt_recommendation.action` BUY, 화면 두 행의 GPT 열에 매수 배지, 콘솔 오류 없음

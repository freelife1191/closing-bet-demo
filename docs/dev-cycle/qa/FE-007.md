# [FE-007] 모달 셸 통합과 대형 클라이언트 컴포넌트 분할 — QA 시나리오

- 대상 화면: http://localhost:3500/ 와 http://localhost:3500/dashboard/kr/closing-bet
- 구성 근거: /qa-only 리포트 (`.gstack/qa-reports/qa-report-localhost-3500-2026-09-05.md`)
  + 이번 사이클의 변경 13개 파일 + agent-browser 실측
- 구성 2026-09-05 12:35 | 실행 (미실행)

## 시나리오

### S-1. 겹친 모달에서 Escape 한 번이 위의 것만 닫는다 (회귀)

- 조작: 종가베팅 대시보드에서 「모의투자」를 눌러 모달을 연다. 「보유 종목」 탭으로 가서
  삼성증권 행의 「매수」를 눌러 매수 대화상자를 겹쳐 연다. Escape 를 한 번 누르고 화면을
  본 뒤, 다시 한 번 누른다.
- 기대: 첫 Escape 뒤에는 「모의 투자 매수」만 사라지고 「모의투자 포트폴리오」는 그대로
  열려 있다. 두 번째 Escape 에 모의투자 모달이 닫힌다.
- 근거: 이번 사이클이 만든 회귀다. 셸을 하나로 모으면서 매수·매도 모달에도 Escape 가
  생겼는데, 셸마다 `document` 에 리스너를 달기 때문에 Escape 한 번에 두 모달이 함께
  닫혔다. 2026-09-05 12:10 실측으로 확인해 맨 위 셸만 반응하도록 고쳤다.
- 결과:

### S-2. 랜딩 탭을 오가도 콘솔에 오류가 남지 않는다 (회귀)

- 조작: 랜딩 페이지를 연다. 「Scoring Logic Detail」 아래에서 「VCP 분석」 → 「수급 점수」
  → 「종가베팅」 → 「VCP 분석」 순으로 네 번 누른다. 브라우저 콘솔을 확인한다.
- 기대: 오류 0건. Next.js 개발 오버레이의 「N Issue」 배지가 화면 좌하단에 나타나지
  않는다.
- 근거: `/qa-only` 리포트의 ISSUE-002 다. 이번 사이클이 만든 `ScoringLogicTabs` 에서
  `Each child in a list should have a unique "key" prop` 이 났다. 서버 컴포넌트가 프롭으로
  넘긴 세 패널이 배열 자식으로 취급되므로 각 패널의 최상위 `div` 에 `key` 를 주어 고쳤다.
- 결과:

### S-3. 수익 차트의 기간과 이동평균선 선택이 탭을 오간 뒤에도 남는다 (회귀)

- 조작: 모의투자 모달에서 「수익 차트」 탭을 연다. 기간을 「1개월」로 바꾸고 이동평균선
  「3일」을 체크한다. 「자산 개요」 탭으로 갔다가 다시 「수익 차트」로 돌아온다.
- 기대: 기간은 「1개월」이 선택된 채(분홍 배경)이고 이동평균선은 「3일」과 「10일」 둘 다
  체크되어 있다. 차트도 다시 그려진다. 기본값인 「1년」과 「10일」만으로 되돌아가지 않는다.
- 근거: 차트를 별도 컴포넌트로 뗀 뒤 탭을 벗어나면 언마운트되어 선택이 초기화되었다.
  React 19.2 의 `<Activity>` 로 감싸 상태를 지키면서 effect 는 정리하도록 고쳤다.
- 결과:

### S-4. 다른 탭에 있을 때 차트 요소가 겹쳐 보이지 않는다 (회귀)

- 조작: 모의투자 모달의 「자산 개요」 탭에서 화면을 본다. 기간 버튼(1개월·3개월·6개월·
  1년·전체)과 이동평균선 체크박스가 보이는지 확인한다.
- 기대: 하나도 보이지 않는다. `Activity` 가 숨긴 차트의 최상위 `div` 에 인라인
  `display: none` 이 걸려 있고, 그 안의 요소는 `offsetParent` 가 `null` 이며 접근성
  트리에도 나오지 않는다.
- 근거: `<Activity mode="hidden">` 은 자식을 언마운트하지 않고 DOM 에 남긴다. 숨김이
  실제로 걸리지 않으면 다른 탭에 차트의 조작 요소가 겹쳐 보이게 된다.
- 결과:

### S-5. 모달 닫기 세 경로가 모두 동작한다 (인접)

- 조작: 모의투자 모달과 「기준표」 모달 각각에 대해 Escape 키, 배경 어두운 판 클릭,
  헤더의 `×` 버튼 세 가지로 닫아 본다.
- 기대: 세 경로 모두에서 모달이 닫힌다. 모의투자 모달의 오버레이는
  `z-[110] p-4`, 배경 판은 `bg-black/80 backdrop-blur-sm` 이고, 기준표 모달의 오버레이는
  `z-[100]` 에 200ms 페이드가 걸려 있다.
- 근거: 종전에는 다섯 벌의 셸이 갖춘 기능이 서로 달라 매수·매도 모달에서 Escape 가 듣지
  않았고 확인 대화상자에는 배경 판이 없었다. 셸을 모은 것이 이번 항목의 첫 체크박스다.
- 결과:

### S-6. 랜딩의 탭 셋이 각자의 패널로 정확히 갈린다 (인접)

- 조작: 랜딩 페이지의 「Scoring Logic Detail」에서 세 탭을 차례로 누른다.
- 기대: 「종가베팅」이 기본 활성(파란 배경)이고 패널 제목은 「종가베팅이란?」이다.
  「VCP 분석」을 누르면 「VCP 패턴이란?」과 「감지 로직 상세 (100점 만점)」이,
  「수급 점수」를 누르면 「수급 분석이란?」과 「분석 가중치 (Total 100점)」이 나온다.
  누른 탭만 회색 배경으로 활성 표시된다.
- 근거: 랜딩 페이지에서 `'use client'` 를 걷어내고 탭 상태만 `ScoringLogicTabs` 로
  떼어냈다. 세 패널은 서버 컴포넌트인 채로 프롭으로 넘어간다.
- 결과:

### S-7. 보유 종목의 매수·매도 버튼이 그 종목의 주문 대화상자를 연다 (인접)

- 조작: 모의투자 모달의 「보유 종목」 탭에서 삼성증권(016360) 행의 「매수」를 누르고 닫은
  뒤 「매도」를 누른다. 행의 다른 곳(종목명 근처)도 눌러 본다. **주문 실행 버튼은 누르지
  않는다.**
- 기대: 「매수」에서는 「모의 투자 매수」가, 「매도」에서는 「모의 투자 매도」가 열린다.
  행의 다른 곳을 누르면 「삼성증권 거래 내역」이 열린다. 셋이 서로 섞이지 않는다.
- 근거: `/qa-only` 리포트가 ISSUE-001 로 「매수를 눌러도 거래 내역이 열린다」를 High 로
  올렸으나, agent-browser 의 셀렉터 클릭으로 재검증한 결과 매수 대화상자가 정상적으로
  열렸다. `browse` 의 접근성 트리 참조가 부모 행에 매핑된 도구 아티팩트다. 다만 이 세
  경로가 실제로 갈리는지는 확인할 값이므로 시나리오로 남긴다.
- 결과:

### S-8. 충전 팝오버의 가산 버튼이 금액을 정확히 더한다 (인접)

- 조작: 모의투자 모달 헤더의 예수금 옆 「+」를 눌러 충전 팝오버를 연다. 입력 칸의 초기값을
  읽고 「+1억」을 두 번 누른다. **「충전하기」는 누르지 않는다.**
- 기대: 초기값 `10,000,000` 에서 `110,000,000`, 다시 `210,000,000` 으로 정확히 1억씩
  늘어난다. 「+100만」과 「+1천만」 버튼도 함께 있다.
- 근거: 입금과 계정 초기화를 `PaperTradingAccountActions.tsx` 로 떼어내면서 금액만 다른
  세 벌의 버튼 코드를 배열 하나로 합쳤다. 합치는 과정에서 금액이 어긋나지 않았는지 본다.
- 결과:

### S-9. 모달을 두 번째로 열면 스피너 없이 옛 값이 먼저 보인다 (인접)

- 조작: 모의투자 모달을 열어 「총 평가 자산」과 「예수금」을 읽는다. 닫았다가 곧바로 다시
  연다. 여는 직후의 화면을 본다.
- 기대: 두 번째로 열 때 전체 스피너가 뜨지 않고 앞서 본 값(총 평가 자산 102,082,600원,
  예수금 91,733,700원)이 곧바로 보이며 조회가 끝나면 갱신된다. **의도한 변화다.**
- 근거: 차트 탭에서 입금했을 때 기간과 이동평균선 선택이 사라지는 문제를 없애려고
  `fetchPortfolio` 가 첫 조회에만 스피너를 띄우게 했다. 갱신 때마다 콘텐츠 영역을 통째로
  갈면 그 안의 `Activity` 경계까지 언마운트되기 때문이다. 그 대가로 두 번째 열림에서
  잠깐 옛 값이 보인다.
- 결과:

## 이월한 발견

- `/qa-only` ISSUE-003 「모달이 열려도 배경 페이지가 스크롤된다」와 ISSUE-004 「모달에
  포커스 트랩이 없어 Tab 이 배경으로 새어 나간다」 → `[FE-030]`. 둘 다 이번 변경이 만든
  것이 아니라 종전 다섯 벌의 셸에도 없던 기능이고, 이번 항목이 정한 다섯 가지에 들어
  있지 않다. `ModalShell` 한 자리에서 함께 해결되므로 한 항목으로 묶었다.
- 리뷰가 지적한 `ConfirmationModal` 의 `role="alertdialog"` 도 같은 이유로 `[FE-030]` 에
  넣었다. `[FE-030]` 은 종전부터 있던 항목이고 이번 사이클이 그중 Escape 닫기와
  `aria-labelledby` 둘을 이미 해결했으므로 새 항목을 만들지 않고 그 항목을 갱신했다.
- 모달 진입 애니메이션 클래스(`animate-in`, `fade-in`, `zoom-in-95`,
  `slide-in-from-bottom-4`, `animate-scale-in`)가 컴파일된 CSS 에 없어 아무 일도 하지
  않는다 → `[FE-036]`. 이 저장소에 `tailwindcss-animate` 가 설치되어 있지 않고
  `tailwind.config.ts` 의 `plugins` 가 비어 있다. 실제로 정의된 것은
  `globals.css:117` 의 `animate-fade-in` 하나뿐이다.
- `/qa-only` ISSUE-001 「매수 버튼이 거래 내역 모달을 연다」는 이월하지 않았다.
  agent-browser 로 재검증한 결과 정상 동작했고, `browse` 의 참조 매핑이 부모 행에 닿은
  도구 아티팩트였다. 검사 자체는 S-7 로 남겼다.

## 실행 결과

(미실행)

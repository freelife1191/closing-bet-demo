# [VCP-052] VCP AI JSON 의 읽기·병합·저장과 가격 동기화에 파일 잠금 — QA 기록

- 대상: `services/common_update_ai_analysis_service.py` 의 `_write_ai_analysis_files`(읽기·병합·저장 전체를 `data/kr_ai_analysis.json.lock` 안에), `scripts/init_data.py` 의 `update_kr_ai_analysis_prices`(같은 잠금, `atomic_write_text` 저장)
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-25 09:23 | 실행 2026-09-25 09:29:53~09:30:05
- 검증 기준 커밋: `4ffb9065`(이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `3e24dc31`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 09:19), 호출 경로(수동 갱신 `run_ai_analysis_step`·수집 `create_signals_log`·재분석 `update_vcp_ai_cache_files` → `_write_ai_analysis_files`, 시세 갱신 `update_vcp_signals_recent_price` → `update_kr_ai_analysis_prices` → `data/kr_ai_analysis*.json`)
- QA 엔진(engine): Claude Code. 결함은 워커 둘이 같은 파일을 겹쳐 쓸 때의 유실이고 화면 조작으로 겹침을 재현할 수 없으므로 browser_applicability: not_applicable. CLI 하네스(스크래치 `v052_harness.py`)가 실제 함수를 별도 프로세스 둘로 실행한다. 병합 쪽은 읽은 직후 0.8초 지연을 넣어 겹침을 강제하고, 둘째 프로세스는 0.3초 뒤에 시작한다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지운다. 하네스가 사본 `data/` 에 AI JSON 을 새로 만든다. 네트워크·LLM·원본 `data/` 없음
- 금지 조작: Refresh VCP, AI 재분석, 챗봇 전송, 모의 매수, 설정 저장
- 필수 여부(required): S-1·S-2 예
- 반복(iteration): 1회
- 결과: 통과 (필수 2/2)
- 증거: 아래 「실제」 줄. 사본 `services/common_update_ai_analysis_service.py` 의 `VCP-052` 표시는 수정 전 0건·수정 뒤 1건으로 두 사본이 서로 다른 커밋임을 확인. 두 실행 모두 하네스 exit 0, 각 프로세스 exit `[0, 0]`

## 시나리오

### S-1. 두 프로세스가 서로 다른 종목을 같은 날짜 파일에 병합해도 둘 다 남는다 (필수, CLI)
- 조작: 두 사본 루트에서 `venv/bin/python <스크래치>/v052_harness.py` 를 실행한다. 005930 병합과 000660 병합을 겹친다
- 기대: 수정 뒤 `kr_ai_analysis_20260922.json` 종목 `['000660', '005930']`. 수정 전은 한 종목만 남는다(결함 재현). 두 프로세스 exit 0
- 실제: 수정 뒤(`q052new`) 두 병합이 09:30:01·09:30:03 에 차례로 끝나고(둘째가 잠금을 기다림) 종목 `['000660', '005930']`. 수정 전(`q052old`) 두 병합이 모두 09:29:56 에 끝나고 종목 `['000660']`(005930 판정 유실, 결함 재현). 통과

### S-2. 병합과 가격 동기화가 겹쳐도 가격 동기화가 사라지지 않는다 (필수, CLI)
- 조작: S-1 과 같은 실행. 최신 파일(`signal_date` 2026-09-22)에 035420 병합(느림)과 005930=110 가격 동기화를 겹친다
- 기대: 수정 뒤 가격 동기화가 병합을 기다린 뒤 적용되어 005930 `current_price` 110. 수정 전은 가격 동기화가 먼저 쓰고 병합이 옛 내용으로 덮어 `current_price` 없음(결함 재현). 두 사본 모두 최신 파일에 035420 이 있다
- 실제: 수정 뒤 병합 09:30:05 완료 뒤 가격 동기화 09:30:05 완료, 최신 파일 종목 `['000660', '005930', '035420']`, 005930 `110 / 0.0`(수익률 0.0 은 병합이 날짜 파일 내용으로 최신 파일을 덮어 `entry_price` 가 빠진 결과이며 `[VCP-059]` 의 종전 동작). 수정 전 가격 동기화 09:29:57 가 병합 09:29:58 보다 먼저 끝나고 최신 파일 종목 `['000660', '035420']`, 005930 가격 `None / None`(동기화 유실, 결함 재현). 통과

## 정리
- 실제: 사본 둘(`q052old`·`q052new`) 리터럴 경로로 삭제 뒤 없음 확인(처음 변수 경로 `rm -rf` 는 안전 검사로 실행되지 않아 리터럴 경로로 다시 함), 사본 경로 프로세스 0, 서버 기동 없음. 원본 `data/kr_ai_analysis.json`(1788316016)·`signals_log.csv`(1789994506) 수정 시각 그대로, 원본 `data/` 에 `kr_ai_analysis.json.lock` 없음. 네트워크·LLM 없음. 결과: 필수 2/2 통과

# [INFRA-110] 데이터 상태 화면의 늦게 도착한 마운트 조회가 새 진행 폴링을 끊는 문제 — QA 기록

- 대상: `frontend/src/app/dashboard/data-status/page.tsx` 의 `pollUpdateStatus`·`startPolling`·`handleUpdateAll`(`pollSessionRef` 번호를 `startPolling` 과 전체 업데이트 클릭이 올리고, 요청 시점과 번호가 다른 응답은 성공·실패 모두 반영하지 않음)
- 단계(phase): 시나리오 구성 완료 | 실행 전
- 구성 2026-09-25 12:45 | 실행 (미실행)
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 대조는 수정 전 커밋 `a1dff279`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 12:34), 리뷰 low 1 을 범위에 넣는 확인(대화, 분 단위 시각은 재지 않았다. 12:34 뒤·수정 전 재실행 12:44:15 전). 호출 경로(마운트 `pollUpdateStatus()` 1회 → 「전체 데이터 업데이트」 클릭 → `POST /api/system/start-update` → `startPolling()` 500ms interval → `fetchAPI('/api/system/update-status')`). 백엔드 `api_start_update` 는 응답 전에 `start_update` 로 실행 중을 저장하므로 문제가 되는 것은 시작 전에 보낸 조회뿐이다
- QA 엔진(engine): Claude Code. browser_applicability: required(browser_driver: agent-browser). 사용자가 보는 것은 진행 표시와 버튼 문구다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지운다. 사본 Next dev(58102)의 `API_URL` 은 세션 스크래치의 가짜 백엔드 `infra110qa_stub.py`(127.0.0.1:58101)다. 실제 Flask 의 시작 요청은 수집·LLM 을 실제로 돌리므로 쓰지 않는다. 가짜 백엔드는 첫 `update-status` 를 도착 시점의 상태로 읽고 4초 뒤 응답하며, `start-update` 는 상태만 실행 중으로 바꾼다(S-4 는 `start-update` 응답을 4초 늦춘다). 더미 `NEXTAUTH_SECRET`·`INTERNAL_IDENTITY_SECRET`·`ADMIN_EMAILS=qa-admin@example.com` 을 환경 변수로만 주고 같은 더미 비밀로 만든 세션 토큰을 쿠키로 넣는다. 원본 3500·5501·`data/`·`.env`·운영 주소는 쓰지 않는다
- 금지: 원본·운영의 시작 클릭, 중지 버튼, 메시지 발송, 설정 저장, Refresh, AI 재분석, 챗봇 전송, 모의 매수
- 필수 여부(required): S-1~S-4 예
- 실행: 세션 스크래치의 `infra110qa_run.sh <사본> <표식>`(agent-browser `--namespace infra110qa`), 두 사본을 차례로

## 시나리오

### S-1. 마운트 조회가 시작보다 늦게 `isRunning:false` 로 도착해도 진행 폴링과 표시가 남는다 (브라우저, 필수)
- 조작: 화면을 열고 1.5초 뒤(마운트 조회가 가짜 백엔드에 잡혀 있는 동안) 「전체 데이터 업데이트」를 누른다. 5초 뒤 화면과 조회 수를 읽고, 2초 뒤 조회 수를 다시 센다
- 기대: 수정 뒤 「Daily Prices 업데이트 중...」과 「업데이트 중...」 버튼이 보이고, 2초 동안 조회 수가 늘어난다. 가짜 백엔드의 시작 요청 1건. 수정 전은 진행 표시가 사라지고 조회가 늘지 않는다(결함 재현)
- 실제:

### S-2. 시작하지 않으면 늦게 도착한 마운트 조회 뒤 폴링이 붙지 않는다 (브라우저, 필수, 회귀)
- 조작: 화면을 열고 아무것도 누르지 않은 채 6초 뒤와 그 2초 뒤의 조회 수를 센다
- 기대: 수정 전후 모두 조회 1건에서 늘지 않고 「전체 데이터 업데이트」 버튼이 보이며 진행 표시가 없다
- 실제:

### S-3. 실행이 끝나면 폴링이 멈추고 진행 표시가 사라진다 (브라우저, 필수, 회귀)
- 조작: S-1 에 이어 가짜 백엔드를 끝난 상태로 바꾸고 3초 뒤 화면을 읽은 뒤, 2초 동안 조회 수를 센다
- 기대: 수정 뒤 진행 표시가 사라지고 「전체 데이터 업데이트」 버튼이 돌아오며 조회 수가 늘지 않는다. 가짜 백엔드 로그에 예상 밖 POST 0건
- 실제:

### S-4. 시작 요청의 응답보다 먼저 도착한 `isRunning:false` 도 진행 상태를 지우지 않는다 (브라우저, 필수)
- 조작: 가짜 백엔드의 시작 응답을 4초 늦춘 뒤 화면을 열고 1.5초 뒤 「전체 데이터 업데이트」를 누른다. 마운트 응답 뒤·시작 응답 전(클릭 약 2.7초 뒤)에 화면을 읽고, 3초 뒤 다시 읽는다
- 기대: 수정 뒤 첫 읽기에서 「업데이트 중...」 버튼 1·「전체 데이터 업데이트」 0, 두 번째 읽기에서 진행 표시 1. 가짜 백엔드 로그의 순서가 마운트 응답 → 화면 읽기 → 시작 응답이어야 유효하다. 수정 전은 첫 읽기에서 「전체 데이터 업데이트」 가 다시 보인다(결함 재현)
- 실제:

## 정리
- 실제:

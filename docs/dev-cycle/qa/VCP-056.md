# [VCP-056] 가격 값이 빈 프레임을 분석 불가로 센다 — QA 기록

- 대상: `engine/screener.py` 의 `_prepare_stock_analysis`(사전 검사가 원래 행 수 대신 `_normalize_price_frame` 이 남긴 행 수, 곧 high·low·close·volume 이 모두 숫자인 행 수를 센다)
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-25 08:25
- 검증 기준 커밋: (이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `6801501e`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 08:19), 호출 경로(스케줄러 17:00 체인과 관리자 수동 업데이트의 VCP 단계 → `create_signals_log` → `SmartMoneyScreener.run_screening` → `_analyze_candidates` → `_prepare_stock_analysis`), `[VCP-053]` 의 「분석 가능한 종목이 없습니다」 예외 갈래
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 화면 진입점(데이터 상태 화면의 업데이트)은 실제 KRX·Toss 수집과 LLM 분석을 일으키는 금지 조작이고, 결함은 가격 파일의 값이 체계적으로 빌 때만 드러나 실제 출처로는 재현을 고를 수 없다. 화면이 읽는 `signals_log.csv` 의 계약은 바꾸지 않았다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지운다. 하네스는 사본 경로(`/scratchpad/`)가 아니면 실행을 거부하고 소켓 연결을 막는다. 가짜는 `MarketGate.analyze` 하나(외부 지수 조회 회피)이며 나머지는 사본의 실제 코드다. `run_ai=False` 라 LLM 호출이 없다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp056qa_harness.py <모드>`. 매 실행 때 사본 `data/` 에 종목 목록(000001~000003), 일별 가격(종목마다 09-22 까지 영업일 30행), 수급 파일, 기존 시그널 한 행(09-22, 000009)이 든 `signals_log.csv` 를 새로 쓴다. 대상일은 09-22
- 필수 여부(required): S-1~S-3 모두 예
- 반복(iteration): 1회
- 결과:
- 증거:

## 시나리오

### S-1. 전 종목의 high 가 비면 기존 시그널을 지우지 않는다 (하네스, 필수)
- 조작: 두 사본에서 `nan_high`. 전 종목 30행의 high 가 빈 칸
- 기대: 수정 전은 반환 `True`, 09-22 행 없음(기존 000009 삭제), 「VCP 조건 충족 종목 없음」(결함 재현). 수정 뒤는 스크리닝이 「분석 가능한 종목이 없습니다」 예외로 끝나 09-22 의 000009 가 남는다
- 실제:

### S-2. 일부 행만 비고 유효 행이 20 이상이면 종전처럼 분석한다 (하네스, 필수, 회귀)
- 조작: 두 사본에서 `partial`. 전 종목 앞 10행의 high 만 빈 칸
- 기대: 두 사본의 결과가 같다. 반환 `True`, 09-22 행 없음, 「VCP 조건 충족 종목 없음」(평평한 가격이라 조건 충족 종목이 실제로 없다)
- 실제:

### S-3. 결측이 없으면 종전과 같다 (하네스, 필수, 회귀)
- 조작: 두 사본에서 `normal`
- 기대: 두 사본의 결과가 같다. 반환 `True`, 09-22 행 없음, 「VCP 조건 충족 종목 없음」
- 실제:

## 정리
- 실제:

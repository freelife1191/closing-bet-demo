# [VCP-018] legacy 분석 파일의 GPT 추천 보강 — QA 시나리오

- 대상 화면: http://localhost:3500/dashboard/kr/vcp (백엔드 http://localhost:5501)
- 구성 근거: /qa-only 리포트 (`.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-018.md`)
  + 이번 사이클의 변경 두 파일 + code-review 가 찾은 회귀
- 구성 2026-09-07 11:30 | 실행 (미기재)
- QA 엔진(engine): Claude Code `/qa-only` → `/qa`
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 반복(iteration): 1회
- baseline 상태: 기준값 수집 완료 (`.gstack/qa-reports/baseline-vcp-018.json`)
- 필수 여부(required): 예
- 결과: (미기재)
- 증거: `.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-018.md`
- 정리(cleanup): (미기재)

## 검사 대상에 관한 전제

이번 변경은 legacy 분석 파일로 오늘 분석의 빈 자리를 채우는 함수 하나를 바꿉니다. 세
프로바이더 필드를 한 상수로 묶어 대칭을 맞추었고, `gpt_recommendation` 이 그 목록에서
빠져 있던 것이 이 항목의 결함입니다.

현재 `data/` 의 `ai_analysis_results_20260505.json` 과 `kr_ai_analysis.json` 은 내용이
같습니다. 두 파일을 같은 실행이 함께 쓰기 때문입니다. 두 파일이 같으면 보강할 빈 자리가
없으므로 결함도 수정도 드러나지 않습니다.

그래서 원본 `data/` 를 심볼릭 링크로 건 사본 디렉터리를 scratchpad 에 만들고 세 파일만
실제 사본으로 바꾸어, 그 디렉터리를 작업 디렉터리로 삼아 백엔드를 다시 띄웁니다. 바꾼
내용과 이유는 `/qa-only` 리포트의 「검사 자료에 관한 전제」 절에 표로 있습니다. 쓰기가
일어나는 SQLite 캐시는 링크를 지워 사본 디렉터리에 새로 만들어지게 합니다. 코드에는 손대지
않고 원본 `data/` 는 읽기만 하며, 실행이 끝나면 사본과 임시 프로세스를 지웁니다.

「고치기 전」 값은 `git worktree` 로 `ccd15d5` 를 별도 디렉터리에 체크아웃하고 그 코드로
백엔드를 띄워 읽습니다. `PYTHONPATH` 만 바꾸는 방법은 gunicorn 에서 적용되지 않아 원본
코드가 그대로 적재되므로, worktree 디렉터리 자체를 작업 디렉터리로 삼고 자료 디렉터리를
사본에 연결합니다. 그 코드가 실제로 적재되었는지는 임시 표식 로그로 확인합니다.

## 시나리오

### S-1. 오늘 분석에 GPT 판정이 없으면 legacy 판정이 응답에 실린다 (회귀)
- 조작: 오늘 분석 파일에서 `gpt_recommendation` 을 뺀 사본으로 백엔드를 띄우고
  `GET /api/kr/signals?date=2026-05-05` 를 부른다.
- 기대: `count=10` 이고 네 종목(034730·000660·005930·064350)의 `gpt_recommendation.action`
  이 `BUY` 다. 그 사유는 `[legacy 판정]` 으로 시작한다. 같은 자료를 고치기 전 코드로 읽으면
  네 종목 모두 `gpt_recommendation` 이 없다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-2. 표의 GPT 배지가 그 판정을 보인다 (회귀)
- 조작: 같은 자료를 읽는 백엔드에서 VCP 화면을 열고 「과거」 로 2026-05-05 를 선택한 뒤
  표를 오른쪽 끝까지 본다.
- 기대: 네 종목의 GPT 열에 「▲ 매수」 배지가 있고 툴팁이 `[legacy 판정]` 으로 시작한다.
  분석 파일에 없는 여섯 종목은 `-` 다. 고치기 전에는 열 종목 모두 GPT 열이 `-` 다.
  콘솔 오류는 0 건이다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-3. 종목 상세 모달의 GPT 탭이 살아난다 (회귀)
- 조작: 표에서 SK(034730) 종목명을 눌러 상세 모달을 열고 AI 탭 구성을 읽는다.
- 기대: 「GPT」와 「Gemini」 두 탭이 있고, GPT 탭 본문이 `[legacy 판정]` 으로 시작하는
  legacy 사유를 보인다. 고치기 전에는 탭이 「Gemini」 하나뿐이다. 화면의 `getAIBadge` 는
  `aiData` 로 폴백하지만 이 자료에서 `/api/kr/ai-analysis` 는
  `jongga_v2_integrated_history` 갈래를 타고 VCP 종목을 하나도 담지 않으므로, 백엔드
  보강이 유일한 GPT 출처다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-4. 오늘 판정이 legacy 판정에 밀려나지 않는다 (회귀)
- 조작: legacy 사본의 `gemini_recommendation` 을 `SELL` 로 바꾸고 사유 앞에
  `[legacy 판정]` 을 붙인 상태로 S-1 과 S-2 를 다시 확인한다. 이어서 오늘 분석 파일의
  `gemini_recommendation` 을 실패 기록(`action` 이 `N/A`, `reason` 이 `분석 실패`)으로
  바꾼 사본으로도 확인한다.
- 기대: 두 경우 모두 화면의 Gemini 배지가 오늘 자 판정을 유지하고 툴팁에 `[legacy 판정]`
  표식이 없다. 두 번째 경우에는 CSV 가 만든 판정이 그대로 보인다. code-review 가 찾은
  회귀이며, 보강 조건을 「비어 있을 때만」 으로 되돌려 막았다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-5. 오늘 분석이 온전하면 기존 동작이 그대로다 (인접)
- 조작: 오늘 분석 파일을 원래 내용으로 되돌린 사본으로 같은 조회를 한다.
- 기대: 네 종목의 `gpt_recommendation.action` 이 `HOLD` 이고 사유에 `[legacy 판정]` 표식이
  없다. 오늘 분석의 판정이 그대로 쓰이며 legacy 는 개입하지 않는다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-6. Perplexity 탭 판정이 그대로다 (인접)
- 조작: 같은 화면에서 AI 탭 목록과 표의 두 번째 AI 열 이름을 읽는다.
- 기대: Perplexity 탭이 나타나지 않고 두 번째 AI 열이 GPT 다. 이 자료에 perplexity 판정이
  없으므로 `decideSecondaryAI` 가 GPT 를 고르는 것이 정상이다. 세 필드가 같은 규칙으로
  다뤄지는지는 `tests/app/test_kr_market_vcp_signal_helpers_refactor.py` 의
  `test_legacy_merge_fills_the_same_three_provider_fields` 가 고정한다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-7. 정적 검증 전체가 통과한다 (회귀)
- 조작: 저장소 루트에서 `pytest`, `frontend/` 에서 `npx vitest run` 을 돌린다. 이어서
  새로 더한 검사 세 개를 종전 구현과 회귀가 있던 구현에 각각 걸어 본다.
- 기대: pytest 가 1711 건 통과하고 종료 코드가 0 이다. vitest 는 45 파일 288 건이
  통과하고 종료 코드가 0 이다. 종전 구현에서는 「세 필드 대칭 보강」이 실패하고, 유효성
  판정 구현에서는 「실패 기록 유지」와 「오늘 CSV 판정 보존」이 실패한다.
- 필수 여부(required): 예
- 실제: (미기재)
- 결과: (미기재)

### S-8. 같은 오리진 링크와 로딩 성능이 성하다 (리포트)
- 조작: 화면의 같은 오리진 링크 7 개에 요청을 보내고 `perf` 로 로딩 시간을 읽는다.
- 기대: 전부 200 이고 `load` 가 2 초를 넘지 않는다.
- 필수 여부(required): 아니오
- 실제: (미기재)
- 결과: (미기재)

## 이월한 발견

- ISSUE-001 「VCP 종목 상세 모달이 Escape 로 닫히지 않고 `role`·`aria-modal`·닫기 버튼
  이름이 없다」 → 기존 `[FE-030]` 과 `[FE-017]` 에 새 사실로 덧붙입니다. 화면 코드는 이번에
  한 줄도 바꾸지 않았으므로 범위 밖입니다.
- ISSUE-002 「모바일에서 빈 상태 문구가 표 밖에 놓인다」 → 기존 `[FE-015]`.

## 실행 결과

(미기재)

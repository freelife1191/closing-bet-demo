# UltraQA Report — FE-022

- Goal: FE-022·FE-039 공통 프로필 표시·갱신 및 저장된 직무 복원.
- engine: ultraqa
- lifecycle: app-adapted
- phase: complete
- iteration: 1
- same_failure_count: 0
- baseline: pytest 2281 통과/2 skip(exit0), Vitest439 통과/62파일(exit0), typecheck exit0, lint exit0(199 warnings)
- browser_applicability: required — Sidebar·챗봇·헤더가 여는 실제 SettingsModal 사용자 흐름
- browser_driver: agent-browser
- namespace/session: profile-batch-20260909 / qa
- 대상: http://127.0.0.1:57371/chatbot 및 /dashboard/kr/closing-bet
- 소스 기준: 99ba5ef — 실제 checkout 8파일 SHA 일치
- cleanup: complete — cleanup.json
- UltraQA Report: [FE-022.md](FE-022.md)

## 범위와 완료 조건

기존 Next UI를 격리 체크아웃에서 실행하고, 외부 효과가 나는 HTTP 백엔드만 합성 대역으로 둔다.
실제 인증 제공자·서버 저장·LLM 동작의 E2E 성공을 주장하지 않는다. 원본 3500/5501과 live 주소,
.env 및 data, 사용자 소유 루트 package.json을 변경하지 않는다. 실제 자격 증명은 복사하지 않는다.
필수 행·정적 검사·cleanup 모두 통과해야 완료한다. 같은 실패 3회 또는 전체 5회에서 중단한다.

## 시나리오 행렬

모든 행 필수. 사용자 모델은 합성 일반 사용자이며, 특이 문자열은 실행 지시가 아닌 데이터다.

| ID | 의도·분류 | setup / 조작 | 기대 신호 | 실제·결과 | 수정 | 증거 | cleanup |
|---|---|---|---|---|---|---|---|
| S-1 | 회귀: 기본 사용자 | 새 격리 브라우저, 비로그인 응답으로 챗봇·사이드바·설정 열기 | User 기본 이름이 동일하고 흑기사 없음 | 새 무쿠키 화면의 Sidebar User와 챗봇 안녕하세요, User님, 설정 User·빈 직접입력 확인 / 통과 | 구현 수정 후 첫 동적 실행 | anonymous-before.txt, anonymous-settings.txt, anonymous-settings.png | 완료 |
| S-2 | 인접: 로그인 표시 | 합성 로그인 이름과 다른 로컬 이름으로 UI 로드 | 두 표시 영역은 세션 이름 우선, 설정의 편집값은 로컬 프로필 보존 | 합성 쿠키 적용 후 두 화면 일반 사용자, 편집 이름 이름만 수정 및 user@example.com 유지 / 통과 | 구현 수정 후 첫 동적 실행 | auth-display.txt, auth-editable.txt, auth-editable.png | 완료 |
| S-3 | 회귀: 커스텀 직무·진입 경로 | UI로 이름/직무 저장 후 본문 아바타·사이드바·헤더로 설정 열기 | 세 경로 모두 같은 편집 이름, 직접 입력과 저장된 직무 문자열 | 챗봇 저장→Sidebar 설정→헤더 설정에서 같은 저장 직무 복원; 이름 변경도 전달 / 통과 | 구현 수정 후 첫 동적 실행 | custom-saved.txt, sidebar-reopened.txt, header-reopened.txt, header-reopened.png | 완료 |
| S-4 | 적대적: 취소·재개·다른 필드 저장 | 커스텀 직무 편집 후 취소·다시 열기, 이름만 바꿔 저장 | 취소 편집은 폐기, 마지막 저장 직무가 유지되고 POST 본문 일치 | 임시 직무 입력 후 취소→Sidebar 재열기에서 저장 원문 복원; 이름만 수정 POST도 persona 원문 일치 / 통과 | 구현 수정 후 첫 동적 실행 | sidebar-reopened.txt, auth-editable.txt, profile-posts.jsonl.gz | 완료 |
| S-5 | 인접: 알려진 직무 | 리서치 선택·저장·다시 열기 | 리서치 선택 유지, 직접 입력창 숨김 | 리서치 선택·저장 후 새로고침/헤더 재열기에서 리서치 선택, 직접입력창 없음 / 통과 | 구현 수정 후 첫 동적 실행 | known-selected.txt, known-reopened.txt, known-reopened.png | 완료 |
| S-6 | 적대적: 오래된 캐시·특이 문자열 | 변경 이벤트와 캐시 부재/잘못된 JSON은 회귀 테스트, Unicode 커스텀 직무는 UI로 입력 | 기본값 복원/무충돌 및 문자열 그대로 표시·보존 | 손상 캐시·이벤트 reset 회귀 통과. UI에서 리서치→리서치 분석가 🧪 타이핑해도 직접입력 유지. Unicode 원문 저장 확인 / 통과 | 구현 수정 후 첫 동적 실행 | green.log.gz, custom-prefix.txt, custom-prefix-completed.txt, custom-visible.png, profile-posts.jsonl.gz | 완료 |
| S-7 | 검증 무결성·dirty 보존 | 테스트 종료 코드·검사 수, 브라우저 오류와 Next MCP, 원본 파일 해시 확인 | 필수 실패 없음, root package.json 불변, 임시 서버/세션 종료 | baseline 통과, 페이지/콘솔 오류 없음, Next runtime 오류 없음, 소유 프로세스·포트·fixture 정리 및 package 해시 불변 / 통과 | 구현 수정 후 첫 동적 실행 | static-results.json, errors-final.json, errors-prefix.json, console-prefix.json, get_errors.txt, cleanup.json | 완료 |

Prompt injection·경로 문자열은 저장 직무 데이터로만 취급한다. CLI flag parser와 OMX 취소 명령은
이번 제품 변경 대상이 아니므로 적용 불가다. 무한 요청/강제 중단은 테스트 및 서버 기동 상한으로
관리한다. 실패 로그를 지워 성공으로 바꾸지 않으며, 하네스 오류는 제품 결함과 구분한다.

## 명령과 증거

증거 디렉터리: `../evidence/profile-batch-20260909/`.
정적 검사: pytest, Vitest 전체, type-check, lint. 각 종료 코드와 검사 수를 실행 후 기록한다.
리뷰는 ponytail 이후 code-reviewer·architect 독립 검토. Native OMX 상태는 활성화하지 않는다.

## 실행 결과

- 필수 통과: 7/7
- 미통과 필수: 없음
- 재개 판정: 완료 가능

## 정적 검증 이력

새 테스트의 미등록 toHaveValue 타입으로 build smoke 3건이 실패했다. typed input.value 검사로 수정한 후 전체439건과 타입검사를 통과했다. 초기 회귀 RED에는 fixture currentModel 오류도 포함되므로 제품 결함 assertion과 구분한다. 원문은 evidence의 red.log 및 full-vitest-red.log에 보존한다.

## 실행·진단·정리

- 실행 명령: agent-browser 전용 namespace/session의 open → snapshot → ref click/fill/select/type → snapshot/screenshot → errors/console → close. 두 진입 URL 모두 소유 localhost57371.
- profile POST 3회(200), Sidebar log-event POST 2회(200). 나머지는 allowlisted GET만 있었고 모두200. 환경·LLM·거래·발송·삭제 요청 없음.
- 첫1280×577 스크린샷에서 custom 입력 일부가 footer 아래에 있어1280×900에서 실제 입력과 전체 문자열을 다시 확인했다. Modal 스크롤 구조 변경은 이 범위에 없다.
- Next get_errors는 configErrors/sessionErrors 모두 비었다. get_compilation_issues는 webpack에서 도구 없음(-32602)으로 실행 불가다. 이를 성공으로 세지 않고 Next 컴파일 로그와 통과한 build/type smoke를 보강 증거로 사용했다.
- 하네스 setup awk 정규식 오류는 기동 전에 재현·수정했다. cleanup은 SIGTERM 뒤 killpg(0) 조회가 PermissionError로 끝나, 별도 ps에서 소유 두 그룹의 구성원이0명이고57371/57372가 비었음을 확인했다. 추가 kill 없이 소유 scratch만 제거했다.
- 원본 사용자 package.json SHA 불변. 브라우저와 임시 서버는 종료했다. 실제 서비스 백엔드·외부 로그인 공급자의 검증을 주장하지 않는다.
- 실행 코드 변경 없는 첫 동적 iteration1에서 필수7/7 통과. 정적 초기 실패 로그는 보존했다.

ULTRAQA COMPLETE: Goal met after 1 cycles

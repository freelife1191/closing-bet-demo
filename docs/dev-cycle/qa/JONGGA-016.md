# [JONGGA-016] 자료가 「관망」이라고 적은 종목이 화면에서 「매수」로 보인다 — QA 시나리오

- 대상 화면: http://localhost:3500/dashboard/kr/closing-bet
- 구성 근거: /qa-only 리포트 (.gstack/qa-reports/qa-report-localhost-3500-2026-09-03-jongga-016.md)
  + 이번 사이클의 변경 세 파일
- 구성 2026-09-03 | 실행 2026-09-03

`TODO.md` 의 `- QA 시나리오:` 줄은 「카드의 추천 배지가 자료의 `ai_evaluation.action` 과
같다. 판정이 없는 자료는 추천 자리에도 「미산출」처럼 값이 없음을 드러낸다」였습니다. 그
줄과 실측이 어긋난 자리는 없었고, 실측으로 기대값을 채워 아래 시나리오로 옮겼습니다.

## 시나리오

### S-1. 최상위 판정이 비어 있어도 카드가 자료의 판정을 그린다 (회귀)
- 조작: 화면을 연 뒤 날짜 선택을 `2026-02-11` 로 바꾼다. 카드마다 추천 배지의 문구를 읽어
  종류별로 센다.
- 기대: 카드 4장이 나오고 배지는 `BUY` 2장과 `HOLD` 2장이다. 응답의 최상위
  `ai_evaluation` 은 9건 전부 `null` 이고 판정은 `score.ai_evaluation` 에만 있다.
  고치기 전에는 이 날짜에서 배지가 전부 `BUY` 로 나왔다.
- 결과: 통과. 카드 4장에 `BUY` 2장(LG전자, 메리츠금융지주)과 `HOLD` 2장(아크릴,
  현대제철)이 나왔다.

### S-2. 같은 카드의 확신도가 판정과 같은 원천에서 나온다 (회귀)
- 조작: `2026-02-11` 의 카드 4장에서 「확신도」 오른쪽의 백분율을 순서대로 읽는다.
- 기대: `80%`, `60%`, `80%`, `60%` 이며 각 값 왼쪽에 진행 막대가 함께 그려진다.
  `HOLD` 배지가 붙은 카드의 확신도가 `60%` 두 개다.
- 결과: 통과. `80%`, `60%`, `80%`, `60%` 순서로 나왔고 네 장 모두 진행 막대가 함께
  그려졌다. `HOLD` 배지가 붙은 두 장의 확신도가 `60%` 로 같다.

### S-3. 판정 건수가 많은 날짜에서도 분포가 자료와 같다 (회귀)
- 조작: 날짜를 `2026-02-19` 로 바꾸고 배지를 종류별로 센다.
- 기대: 카드 19장에 `HOLD` 12장과 `BUY` 7장이다. 이 날짜도 최상위 `ai_evaluation` 이
  19건 전부 `null` 이다.
- 결과: 통과. 카드 19장에 `HOLD` 12장과 `BUY` 7장이다.

### S-4. 판정이 없는 자료는 네 자리가 모두 값 없음을 드러낸다 (회귀)
- 조작: 날짜를 `2026-02-20` 으로 바꾸고 카드 하나에서 추천 배지, 확신도, 모델 배지,
  AI 분석 리포트 본문 네 자리를 읽는다.
- 기대: 카드 18장 전부가 배지 「AI 분석 대기」, 확신도 「미산출」이며 진행 막대가 그려지지
  않는다. 모델 이름 배지는 아예 없고 리포트 본문은 「AI 분석 대기 중입니다」로 시작한다.
  고치기 전에는 이 자리에 모델 이름이 실제 자료 없이 표시되었다.
- 결과: 통과. 카드 18장 전부가 배지 「AI 분석 대기」, 확신도 「미산출」이고 진행 막대를
  그린 카드가 한 장도 없다. 모델 이름 배지도 18장 모두 없으며 리포트 본문은
  「AI 분석 대기 중입니다」로 시작한다.

### S-5. 모델 이름 배지가 자료의 모델과 같다 (회귀)
- 조작: 날짜를 `2026-02-11` 과 `2026-02-23` 으로 차례로 바꿔 카드의 모델 이름 배지를
  읽는다.
- 기대: `2026-02-11` 은 `Gemini 2.5 flash`, `2026-02-23` 은 `Gemini 2.0 flash lite` 다.
  날짜마다 다른 이름이 나오며 고정된 기본값이 나오지 않는다.
- 결과: 통과. `2026-02-11` 은 네 장 모두 `Gemini 2.5 flash`, `2026-02-23` 은 판정이 있는
  23장이 모두 `Gemini 2.0 flash lite` 다. 같은 날짜의 나머지 3장은 판정이 없어 모델
  배지도 없다.

### S-6. 최상위 판정이 채워진 자료는 그대로 유지된다 (회귀)
- 조작: 날짜를 `2026-05-05` 로 바꾸고 배지를 종류별로 센다.
- 기대: 카드 9장에 `BUY` 6장과 `HOLD` 3장이며 모델 배지는
  `Gemini 3.1 flash lite preview` 다. 이 날짜는 최상위 `ai_evaluation` 이 9건 모두
  객체이므로 이번 변경 전후로 값이 같아야 한다.
- 결과: 통과. 카드 9장에 `BUY` 6장과 `HOLD` 3장이고 모델 배지는
  `Gemini 3.1 flash lite preview` 다.

### S-7. 날짜를 바꿔도 앞 날짜의 배지가 남지 않는다 (인접)
- 조작: 날짜를 `2026-02-20` 으로 바꿔 전부 「AI 분석 대기」인 것을 확인한 뒤, 곧바로
  `2026-02-19` 로 바꾼다.
- 기대: 전환 즉시 19장 전부가 판정 배지(`HOLD` 12, `BUY` 7)로 바뀌고 「AI 분석 대기」가
  한 장도 남지 않는다.
- 결과: 통과. 전환 즉시 19장 전부가 판정 배지(`HOLD` 12, `BUY` 7)로 바뀌었고
  「AI 분석 대기」가 한 장도 남지 않았다.

### S-8. 등급 필터를 걸어도 배지가 유지된다 (인접)
- 조작: 날짜를 `2026-02-24` 로 바꾼 뒤 등급 필터를 「S급 이상」으로 바꾼다.
- 기대: 카드가 39장에서 3장으로 줄고, 남은 카드의 추천 배지와 확신도와 모델 배지가 필터를
  걸기 전과 같은 값이다.
- 결과: 통과. 39장에서 3장으로 줄었고 남은 세 장의 배지와 확신도와 모델 배지가 필터를
  걸기 전과 같다. 삼성SDI `BUY`/`65%`, SK하이닉스 `BUY`/`85%`, 현대차 `HOLD`/`65%`
  이며 셋 다 `Gemini 2.0 flash lite` 다.

### S-9. 점수 필터를 원복하면 카드 수가 돌아온다 (인접)
- 조작: 날짜를 `2026-02-19` 로 바꾸고 점수 필터를 「8점 이상」으로 바꿨다가 다시 원래
  값으로 되돌린다.
- 기대: 19장 → 13장 → 19장으로 움직이며, 원복한 뒤의 배지 분포가 S-3 과 같은
  `HOLD` 12장과 `BUY` 7장이다.
- 결과: 통과. 19장 → 13장 → 19장으로 움직였고 원복한 뒤의 배지 분포가 S-3 과 같은
  `HOLD` 12장과 `BUY` 7장이다.

### S-10. 신호가 없는 날짜는 빈 상태를 그린다 (인접)
- 조작: 날짜를 `2026-05-22` 로 바꾼다.
- 기대: 카드가 한 장도 없고 「No Signals Found」 빈 상태 안내가 나온다. 배지가 붙은 빈
  카드나 오류 화면이 나오지 않는다.
- 결과: 통과. 카드가 한 장도 없고 「💤 No Signals Found」 빈 상태 안내가 나온다. 배지가
  붙은 빈 카드도 오류 화면도 나오지 않았다.

## 이월한 발견

- ISSUE-001 「뷰포트 폭이 정확히 1280px 일 때 `HOLD` 배지가 붙은 카드의 「확신도」 라벨이
  두 줄로 깨진다」 → `[JONGGA-029]`. 이번 항목이 고치기로 한 것은 체크박스 셋이 적은 대로
  판정 원천을 하나로 정하는 일과 판정이 없는 자료의 표현이며, 배지 행의 줄바꿈은 그 셋
  어디에도 닿지 않아 범위 밖입니다. 값 자체는 정확하게 그려지므로 심각도도 Low 입니다.

## 실행 결과

- 통과 : 10건 가운데 10건
- 실패한 시나리오: 없음
- 시나리오 밖에서 새로 발견: 없음. 날짜 7회 전환과 필터 4회 조작을 거치는 동안
  콘솔 오류가 0건이고 200·204·304 가 아닌 네트워크 응답도 0건이다.

실행 2026-09-03, 뷰포트 1440x900, 검증 대상 커밋 `d7a2ec8`. 실패가 없어 `/qa` 가
수정을 만들지 않았고 셋째 커밋도 생기지 않았다. 리포트는
`.gstack/qa-reports/qa-report-localhost-3500-2026-09-03-jongga-016-scenarios.md` 다.

측정은 카드의 추천 배지를 DOM 에서 직접 세는 방식으로 했다. 확신도를 읽을 때는
요소를 복제해 `div.absolute`(툴팁 상자)를 지운 뒤 텍스트를 읽어야 한다. 확신도
툴팁에 「값이 없으면 미산출로 표시합니다」가 들어 있어서, 지우지 않으면 값이 있는
카드도 「미산출」로 잘못 읽힌다.

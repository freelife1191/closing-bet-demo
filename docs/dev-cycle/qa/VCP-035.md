# [VCP-035] signals_log.csv 쓰기 잠금 — QA 시나리오

- 대상: `git archive` 로 만든 코드 사본 두 개. 기준은 `1735afb`(변경 전)이고, 검증 대상은 첫 커밋(아래 「검증 기준 커밋」)이다. 사본마다 scratchpad 의 하네스 `vcp035_harness.py` 를 `env -i` 로 실행한다. 프로세스는 `multiprocessing` spawn 으로 띄운다. 이렇게 해야 gunicorn 워커 둘이나 스케줄러와 CLI 처럼 서로 다른 프로세스가 같은 파일을 쓰는 상황이 된다
- 구성 근거: 승인 범위(TODO `[VCP-035]` 「설계 승인」)와 계획 Task 5. 기대값은 하네스가 직접 쓴 행 수이므로 원본 `data/` 를 읽지 않는다
- 브라우저 적용 여부(browser_applicability): not-applicable(정책상 차단). 이 쓰기 경로에 화면으로 들어가는 길은 Refresh VCP, 실패 AI 재분석, 관리자 데이터 갱신 셋이다. 셋 모두 `create_signals_log(run_ai=True)` 나 AI 재분석을 부르므로 금지 조작(LLM 비용)에 해당한다. 그래서 `closing-bet-verify` 의 서비스 하네스 등급으로 검사한다. 화면이 읽는 CSV 형식이 바뀌지 않았다는 것은 S-3 의 바이트 비교로 대신한다
- QA 엔진(engine): Claude Code, 서비스 하네스. 원본 3500/5501·live 주소·원본 `.env`·원본 `data/` 는 쓰지 않는다. 사본에 `.env` 와 `secrets/` 는 없고, 추적 파일인 `.env.example` 만 있다
- 금지 조작: 실제 LLM 호출, 네트워크 시세 조회(pykrx 는 하네스가 가짜로 바꾼다), 서버 기동, 원본 파일 쓰기
- 검증 기준 커밋: (첫 커밋 후 기록)
- 단계(phase): 시나리오 구성 완료
- 반복(iteration): 0
- 결과: (실행 후 기록)
- 필수 여부(required): 예
- 읽은 정본: `.claude/skills/closing-bet-python/SKILL.md`, `.claude/skills/closing-bet-verify/SKILL.md`
- baseline 상태: 원본 트리 전체 `pytest -q` 는 2709 passed, 2 skipped, exit 0 이다. frontend 변경이 없어 type-check 와 vitest 는 생략했다

## 시나리오

### S-1. 프로세스 넷이 동시에 행을 더해도 하나도 사라지지 않는다 (핵심: 읽기·병합·교체 직렬화)
- 조작: `python vcp035_harness.py <사본> <작업 디렉터리> 10` 을 실행한다. 한 회차에서 프로세스 네 개가 같은 CSV 에 `SignalTracker._append_to_log` 로 서로 다른 티커를 25개씩 하나씩 더한다.
- 기대: 검증 사본에서는 10회 모두 100 이다. 기준 사본에서는 100 보다 작은 회차가 나와 결함이 재현된다.
- 필수 여부(required): 예
- 실제:
- 결과:

### S-2. 시세 갱신이 조회하는 동안 더해진 행을 지우지 않는다 (재읽기 후 티커 기준 적용)
- 조작: `python vcp035_harness.py <사본> <작업 디렉터리> 5 price` 를 실행한다. 한 프로세스는 가짜 pykrx(종목당 0.05초, 종가 110)로 `update_vcp_signals_recent_price` 를 돌리고, 다른 프로세스는 그동안 25행을 더한다.
- 기대: 검증 사본에서는 5회 모두 `(appended 25, priced 20)` 이다. 기준 사본에서는 appended 가 25 보다 작은 회차가 나온다.
- 필수 여부(required): 예
- 실제:
- 결과:

### S-3. 동시성이 없을 때 시세 갱신의 저장 바이트가 변경 전과 같다 (형식 보존)
- 조작: 같은 시드 CSV(20행, 선행 0 티커, BOM 없음)에 대해 두 사본에서 `update_vcp_signals_recent_price` 를 하나의 프로세스로 실행한다. 결과 파일을 `cmp` 로 비교한다.
- 기대: 바이트가 같다(utf-8-sig BOM, `index=False`, 수치 표기 동일).
- 필수 여부(required): 예
- 실제:
- 결과:

### S-4. 정리 (인접)
- 조작: 하네스가 끝난 뒤 사본 경로의 프로세스를 조회하고, 사본 디렉터리를 지운다. 원본 `data/`·`logs/` 에서 수정 시각이 바뀐 파일을 센다.
- 기대: 남은 프로세스 0, 사본 삭제, 원본 변화 0, 작업 트리 변화 없음.
- 필수 여부(required): 예
- 실제:
- 결과:

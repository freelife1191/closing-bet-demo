# [FE-008] 대시보드 진입 시의 하이드레이션 불일치 제거 — QA 시나리오

- 대상 화면: http://localhost:3500/dashboard/kr 과 http://localhost:3500/chatbot
- 구성 근거: /qa-only 리포트 (.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-fe-008.md)
  + 설계 단계 실측 원본 (.gstack/qa-reports/fe008-hydration-measure-2026-09-07.log)
  + 이번 사이클의 변경. 실행 코드 변경은 없고 TODO 의 근거 인용과 실측 기록만 고쳤다
- 구성 2026-09-07 07:34 | 실행 (미실행)
- QA 엔진(engine): Claude Code `/qa-only` → `/qa`
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 반복(iteration): 0회
- baseline 상태: 기준값 수집 완료 (`.gstack/qa-reports/baseline-fe-008.json`, 건강 98)
- 필수 여부(required): 예
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): (실행 후 기록)

브라우저는 gstack 번들 헤드리스 Chromium(`browse`)이다. 판정 전마다 `console --clear` 로 콘솔을
비우고 `goto` 로 다시 읽는다(`browser-notes.md`). 챗봇 화면에서는 「보내기」·추천 질문 카드·
빠른 조회 버튼·삭제 계열을 누르지 않는다(`AGENTS.md` 「되돌릴 수 없는 조작」).

## 시나리오

### S-1. 대시보드 전체 로드에 하이드레이션 경고가 없다 (회귀)
- 조작: `console --clear` → `goto http://localhost:3500/dashboard/kr` → `wait --networkidle` →
  `console --errors` 와 `console` 을 읽는다. 5회 반복한다.
- 기대: 5회 모두 `console --errors` 가 `(no console errors)` 이고, `console` 전체에서
  `hydrat`·`did not match`·`didn't match`·`mismatch` 가 0건이며, 남는 줄은 React DevTools 안내와
  `[HMR] connected` 두 줄뿐이다.
- 필수 여부(required): 예
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-2. 사이드바 클라이언트 전환에도 경고가 없다 (회귀)
- 조작: `/dashboard/kr` 에서 사이드바 「AI 상담」→「Overview」를 3회 왕복하고, 이어서
  「VCP 시그널」→「종가베팅」→「누적 성과」→「Overview」를 한 바퀴 돈다. 전환마다
  `console --clear` 뒤 클릭, `wait --networkidle`, `url`, `console --errors` 를 읽는다.
- 기대: 전환 10회 모두 URL 이 누른 링크의 경로와 같고 `console --errors` 가
  `(no console errors)`, 하이드레이션 검색 0건이다.
- 필수 여부(required): 예
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-3. 챗봇 첫 렌더의 인사말에 사용자 이름이 있고 h1 이 하나다 (회귀)
- 조작: `console --clear` → `goto http://localhost:3500/chatbot` 직후(0초)에 `js` 로
  `h1` 의 문구·개수와 `[hidden]` 조상 안의 개수, `button` 개수를 읽고, `wait --networkidle` 뒤
  다시 읽는다. 5회 반복한다.
- 기대: 5회 모두 0초에 `h1` 이 하나이고 문구가 「안녕하세요, 흑기사님」이다(저장된 프로필이
  없는 브라우저의 기본값). 유휴 뒤에도 같고 버튼은 25개다. 콘솔 오류 0건.
- 필수 여부(required): 예
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-4. 대시보드에서 챗봇으로 클라이언트 전환해도 인사말이 같다 (인접)
- 조작: `/dashboard/kr` 에서 사이드바 「AI 상담」을 눌러 전환한 뒤 S-3 의 값을 읽는다. 3회.
- 기대: 3회 모두 `h1` 하나 「안녕하세요, 흑기사님」, 버튼 25, 콘솔 오류 0건.
- 필수 여부(required): 예
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-5. 0초 DOM 의 초과분은 전부 숨김 스트리밍 세그먼트 안에 있다 (리포트)
- 조작: S-1·S-3 의 0초 읽기에서 `button` 개수가 유휴 값보다 크면, 초과분이
  `div[hidden][id^="S:"]` 조상 안에 있는지 `e.closest("[hidden]")` 로 센다. 같은 시점의
  `screenshot` 을 찍어 화면에 인사말이 두 벌 보이지 않는지 본다.
- 기대: 초과분 전부가 숨김 조상 안이고(예: 대시보드 27 = 보이는 15 + 숨김 12), 화면에는
  인사말·카드가 한 벌만 보인다. 초과분이 없는 회차는 그 자체로 통과다.
- 필수 여부(required): 아니오 (09-05 관찰의 설명이며 완료 조건은 S-1·S-3 이 맡는다)
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-6. 모바일 뷰포트에서도 두 화면이 경고 없이 열린다 (인접)
- 조작: `viewport 375x812` 로 두 화면을 전체 로드하고 S-1·S-3 의 값을 읽는다. 끝나면
  `viewport 1440x900` 으로 되돌린다.
- 기대: 두 화면 모두 콘솔 오류 0건, 챗봇 `h1` 하나 「안녕하세요, 흑기사님」.
- 필수 여부(required): 예
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 뷰포트 복원

### S-7. 프레임워크 관점에서도 오류가 없다 (리포트)
- 조작: 브라우저가 두 화면을 연 뒤 `/_next/mcp` 에 `get_errors` 와 `get_compilation_issues` 를
  호출한다(`frontend-skills.md` §3 의 명령).
- 기대: `{"configErrors":[],"sessionErrors":[]}` 와 `{"issues":[]}`.
- 필수 여부(required): 예
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-8. 사이드바·헤더 링크가 모두 살아 있다 (인접)
- 조작: `/dashboard/kr` 의 동일 출처 링크를 `js` 의 HEAD 요청으로 검사한다.
- 기대: `/`, `/dashboard/kr`, `/dashboard/kr/vcp`, `/dashboard/kr/closing-bet`,
  `/dashboard/kr/cumulative`, `/chatbot`, `/dashboard/data-status` 일곱 개가 모두 200.
- 필수 여부(required): 아니오
- 실제: (실행 후 기록)
- 결과: (실행 전)
- 증거: (실행 후 기록)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

## 이월한 발견

1단계 리포트의 네 건은 모두 백로그에 이미 있어 새로 올린 항목이 없다.

- ISSUE-001 「챗봇 모바일 사이드바 열기 버튼에 이름이 없다」 → 기존 `[CHAT-007]`
- ISSUE-002 「헤더 홈 링크에 이름이 없다」 → 기존 `[FE-029]`
- ISSUE-003 「사이드바 User 와 챗봇 흑기사 표기가 다르다」 → 기존 `[FE-022]`
- ISSUE-004 「챗봇 안내 말풍선이 NASDAQ 카드에 겹친다」 → 기존 `[FE-010]`

## 실행 결과

(실행 후 기록)

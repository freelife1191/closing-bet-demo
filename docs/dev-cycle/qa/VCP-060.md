# [VCP-060] 가격 동기화의 청산 전용 종목 시세 조회 제외 — QA 기록

- 대상: `scripts/init_data.py` 의 `update_vcp_signals_recent_price`(조회 대상을 CLOSED 가 아닌 행의 티커로 좁힘)
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-25(설계 승인 13:41 뒤, 실행 전, 분 단위 시각은 기록하지 않음) | 실행 2026-09-25 13:48:20~13:48:22
- 검증 기준 커밋: `43effaea`(이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `8b82b010`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 13:41). 가격 동기화는 백그라운드 VCP 파이프라인(`services/kr_market_vcp_background_service.py:90`)과 `init_data.py update-prices` 가 인자 없이 부르며 반환값을 쓰지 않는다. 적용 루프는 `status != 'CLOSED'` 행만 갱신한다([VCP-051]). CLOSED 를 쓰는 코드는 `engine/signal_tracker_log_helpers.py:186` 의 대문자 리터럴 하나다
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 바뀐 것은 배치가 외부 시세를 조회하는 대상뿐이고 화면 코드·API 응답·파일 형식은 바뀌지 않는다. 실제 시세 조회는 외부 호출이므로 `pykrx` 와 `fetch_stock_price` 를 조회 티커를 기록하는 가짜로 바꾼다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고 빈 `data/` 를 만든다. 하네스는 소켓 연결을 막고 사본의 함수를 직접 부른다. 서버와 포트는 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp060qa_harness.py <사본>`(사본 경로가 scratchpad 가 아니면 거부)
- 필수 여부(required): S-1~S-4 예
- 반복(iteration): 1회
- 결과: 통과 (필수 4/4)
- 증거: 아래 「실제」 줄(하네스 `RESULT` 줄 원문, scratchpad `qa-vcp060-old.txt`·`qa-vcp060-new.txt`), 두 실행 모두 exit 0, stderr 소켓 차단 예외 0건

## 시나리오

### S-1. 청산 전용 종목은 조회하지 않고 OPEN 행이 있는 종목만 조회·갱신한다 (하네스, 필수)
- 조작: 005930 CLOSED·005930 OPEN·000660 CLOSED 세 행을 두고 가격 동기화를 부른다. 가짜 시세는 005930 110원, 000660 220원이다
- 기대: 수정 뒤 조회 `["005930"]`. 수정 전 `["005930", "000660"]`(결함 재현). 로그는 수정 전후 모두 005930 CLOSED (90, -10.0)·005930 OPEN (110, 10.0)·000660 CLOSED (180, -10.0)
- 실제: 수정 뒤 조회 `["005930"]`, 수정 전 `["005930", "000660"]`(결함 재현). 로그는 수정 전후 모두 005930 CLOSED (90, -10.0)·005930 OPEN (110, 10.0)·000660 CLOSED (180, -10.0). 통과

### S-2. status 결측 행은 청산이 아니므로 조회·갱신한다 (하네스, 필수)
- 조작: 035420 status 빈 칸·000660 CLOSED 두 행을 두고 가격 동기화를 부른다. 가짜 시세는 035420 330원이다
- 기대: 수정 뒤 조회 `["035420"]`, 수정 전 `["035420", "000660"]`. 로그는 수정 전후 모두 035420 결측 (330, 10.0)·000660 CLOSED (180, -10.0)
- 실제: 수정 뒤 조회 `["035420"]`, 수정 전 `["035420", "000660"]`. 로그는 수정 전후 모두 035420 결측 (330, 10.0)·000660 CLOSED (180, -10.0). 통과

### S-3. status 열이 없는 로그는 전 티커를 조회·갱신한다 (하네스, 필수, 회귀)
- 조작: status 열 없이 005930·000660 두 행을 두고 가격 동기화를 부른다
- 기대: 수정 전후 모두 조회 `["005930", "000660"]`, 로그 005930 (110, 10.0)·000660 (220, 10.0)
- 실제: 수정 전후 모두 조회 `["005930", "000660"]`, 로그 005930 (110, 10.0)·000660 (220, 10.0). 통과

### S-4. 모든 행이 청산이면 외부 조회가 0건이고 로그 값은 그대로다 (하네스, 필수)
- 조작: 005930·000660 두 CLOSED 행을 두고 가격 동기화를 부른다
- 기대: 수정 뒤 조회 `[]`, 수정 전 `["005930", "000660"]`. 로그는 수정 전후 모두 (90, -10.0)·(180, -10.0)
- 실제: 수정 뒤 조회 `[]`, 수정 전 `["005930", "000660"]`. 로그는 수정 전후 모두 (90, -10.0)·(180, -10.0). 통과

## 정리
- 실제: 두 사본(`q060old`·`q060new`)을 리터럴 경로로 삭제(사본의 `venv` 는 원본 venv 를 가리키는 링크이며 링크만 지워졌고 원본 venv 는 남아 있음), 하네스 프로세스 0. 원본 `data/` 에서 QA 직전(13:48:15) 뒤 수정된 파일 0개(`find data -newermt`, 13:48:30 확인). 서버·포트·브라우저는 쓰지 않았다. 원본 `data/`·`.env` 사용 없음. 결과: 필수 4/4 통과

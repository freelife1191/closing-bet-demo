# [FE-047] 루트 레이아웃 `lang` 을 한국어로 — QA 시나리오

- 대상 화면: http://localhost:3747/, http://localhost:3747/privacy, http://localhost:3747/dashboard/kr (격리 Next dev)
- 구성 근거: `[FE-047]` 의 QA 줄(아무 화면에서나 문서의 `lang` 속성이 `ko`). 기대값은 바뀐 `layout.tsx` 의 값이다
- 구성 2026-09-23 10:59 | 실행 2026-09-23 11:01~11:04 (1회차)
- 검증 기준 커밋: `bc7ac3d` (첫 커밋). 사본은 이 커밋 직후의 깨끗한 작업 트리에서 rsync 로 만들었고 사본의 `layout.tsx:18` 이 `lang="ko"` 임을 확인했다
- QA 엔진(engine): Claude Code, 브라우저는 gstack `browse`. 원본 3500/5501·live 주소·원본 `.env` 는 쓰지 않는다. 작업 트리를
  scratchpad `qa-fe047/` 로 복사(`.env*`·`.git`·`venv`·`logs` 제외, `frontend/node_modules` 는 APFS clone, `secrets/` 즉시 삭제)하고
  Next 만 3747 에 더미 `NEXTAUTH_SECRET`·`INTERNAL_IDENTITY_SECRET` 와 `API_URL=http://127.0.0.1:5747` 로 띄운다.
  Flask 는 띄우지 않는다. `lang` 은 서버가 그리는 `<html>` 에 있어 API 응답과 무관하기 때문이다. 그래서 API 호출의 연결 실패는
  이 항목의 판정 대상이 아니다
- 단계(phase): 시나리오 구성 완료 | 실행 완료
- 반복(iteration): 1회
- 결과: 통과 (필수 2/2)
- 증거: 각 시나리오의 「실제」 줄(`curl`, browse `js`·`console --errors`, `/_next/mcp get_errors`) · 스크린샷 scratchpad
  `fe047-privacy.png`(방침 전체, 열어 확인) · 격리 로그 `qa-fe047-next.log`
- 정리(cleanup): browse 서버 정지, 격리 Next(3747) 종료 뒤 3747/5747/3500/5501 리스너 0, 사본 경로 프로세스 0, 사본 `qa-fe047/`
  삭제, 저장소 루트 `node_modules/.vite` 없음, 원본 작업 트리 변화 없음. 원본 3500/5501 은 처음부터 떠 있지 않았다
- 필수 여부(required): 예
- browser_applicability: required. 화면 낭독기와 브라우저 번역 제안이 읽는 문서 언어이며 모든 화면의 진입 흐름에 걸린다.
  browser_driver: gstack `browse`
- 읽은 정본: `.claude/skills/closing-bet-nextjs/SKILL.md`, `.claude/skills/closing-bet-verify/SKILL.md`,
  `frontend/node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md`(Root Layout)

## 시나리오

### S-1. 서버가 보낸 HTML 의 `<html>` 이 `lang="ko"` 다 (핵심)
- 조작: 세 경로를 `curl` 로 받아 `<html` 여는 태그를 본다.
- 기대: 세 응답 모두 `<html lang="ko"`. `lang="en"` 은 없다.
- 필수 여부(required): 예
- 실제: `/`·`/privacy`·`/dashboard/kr` 세 응답의 여는 태그가 모두 `<html lang="ko">` 였다. `lang="en"` 은 없었다
- 결과: 통과

### S-2. 브라우저에서 문서 언어가 `ko` 로 읽힌다 (핵심)
- 조작: browse 로 세 화면을 차례로 열고 `document.documentElement.lang` 을 읽는다. `/_next/mcp get_errors` 를 조회한다.
- 기대: 세 화면 모두 `ko`. 하이드레이션 불일치 오류 없음(`get_errors` 의 `sessionErrors` 에 hydration 항목 없음).
- 필수 여부(required): 예
- 실제: 세 화면의 `document.documentElement.lang` 이 모두 `ko` 였다(본문 길이 2791·6602·642자로 화면이 그려진 뒤 읽음).
  `get_errors` 는 `configErrors:[]` 이고, `sessionErrors` 에는 `/dashboard/kr` 의 `API Error: 500` 4건과 JSON 파싱 오류 1건만 있었다.
  모두 Flask 를 띄우지 않아 생긴 API 연결 실패이며(`qa-fe047-next.log` 의 `ECONNREFUSED` 66건), 계획에서 판정 대상 밖으로 정했다.
  `get_errors`·Next 로그·browse 콘솔 오류 어디에도 hydration 관련 항목은 0건이었다
- 결과: 통과

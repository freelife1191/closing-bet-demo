# [INFRA-093] 수급 Toss 백필이 pykrx 로 저장된 정확한 순매수거래대금을 근사값으로 덮는다 — QA 기록

- 대상: `scripts/init_data.py` 의 `_merge_save_csv(keep=...)` 와 `_backfill_institutional_trend_from_toss`(`keep="first"`)
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24(첫 커밋 시각) | 실행 (미실행)
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 대조는 수정 전 커밋 `0a7b84e`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 21:55), 호출 경로(스케줄러 17:00·관리자 수동 갱신 → `create_institutional_trend` → pykrx 빈 응답이면 `latest_existing < expected` 분기에서 `_backfill_institutional_trend_from_toss` → `_merge_save_csv`). 다른 호출자 셋(가격 파일 두 곳, pykrx 수급)은 기본값 `keep="last"`
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 이 경로는 배치 수집이며 화면 진입점(관리자 갱신)은 실제 KRX·Toss 수집을 일으키는 금지 조작이다. 결과는 저장된 CSV 로 판정한다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고 빈 `data/` 를 만든다. 하네스가 종목 목록(005930·000660)과 수급 CSV(09-18 005930, 09-21 005930·000660·069500, 값은 억 단위 정확값)를 사본 `data/` 에 쓴다. 소켓 연결을 막고 가짜 pykrx·Toss 모듈을 넣으며 `get_last_trading_date` 를 09-22 로 고정한다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `infra093qa_harness.py <backfill|pykrx>`

## 시나리오

### S-1. Toss 백필이 기존 행을 덮지 않고 빈 칸만 채운다 (하네스, 필수)
- 조작: 두 사본에서 `backfill` 모드로 실행. pykrx 는 빈 응답이고, 수집 도중 다른 실행이 (09-22, 005930) 정확값 `777000000/-888000000` 을 저장한다. Toss 는 두 종목 모두 09-22·09-21·09-18 에 수량 7·9, 종가 1000 을 준다
- 기대: 수정 전은 파일에 있던 005930 의 09-18·09-21·09-22 와 000660 의 09-21 이 근사값 `7000/9000` 으로 바뀐다. 수정 뒤는 이 넷이 원래 값(`111000000/-222000000`, `333000000/444000000`, `777000000/-888000000`, `-555000000/0`)을 유지하고, 파일에 없던 000660 의 09-18·09-22 만 `7000/9000` 으로 채워진다. 069500 행은 둘 다 그대로. 두 쪽 모두 `result=true`
- 실제:

### S-2. pykrx 정상 경로의 결과는 수정 전과 같다 (하네스, 필수, 회귀 확인)
- 조작: 새 사본 `data/` 에서 두 사본을 `pykrx` 모드로 실행하고 JSON 출력을 `cmp` 로 비교한다
- 기대: 두 출력이 같다. 09-22 행은 pykrx 값(`10/10`, `20/20`, `30/30`)이다
- 실제:

## 정리
- 실제:

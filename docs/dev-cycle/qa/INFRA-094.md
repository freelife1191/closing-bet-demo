# [INFRA-094] 수급 근사·부분 날짜를 pykrx 로 다시 받는다 — QA 기록

- 대상: `scripts/init_data.py` 의 `create_institutional_trend`(창 안 재수집 날짜 선정, Skip 제외, 두 프레임 교집합 가드, 저장 뒤 최신일 확인과 백필·stale 꼬리, 중단 가드)와 `_collect_toss_trend_rows_for_ticker`(`source="toss"`)
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-24 22:20:37(첫 커밋 시각) | 실행 2026-09-24 22:21:01~22:21:12
- 검증 기준 커밋: `1498f99` (이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `debb932`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 22:06), 호출 경로(스케줄러 17:00·관리자 수동 갱신 → `create_institutional_trend`), 계획 Review Focus
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 이 경로는 배치 수집이며 화면 진입점(관리자 갱신)은 실제 KRX·Toss 수집을 일으키는 금지 조작이다. 결과는 저장된 CSV 로 판정한다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고 빈 `data/` 를 만든다. 하네스가 종목 목록(005930·000660, 수집기가 069500 을 더함)과 수급 CSV 를 사본 `data/` 에 쓴다. 소켓 연결을 막고 가짜 pykrx(주식 두 종목만, ETF 069500 없음)·Toss 모듈을 넣으며 `get_last_trading_date` 를 고정한다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `infra094qa_harness.py <approx|latest|oneside|plain>`
- 계약 변경(리뷰 반영, 의도함): (1) 수집한 날짜가 있어도 최신일이 기대일보다 이르면 수정 전은 `True`, 수정 뒤는 Toss 백필을 시도하고 실패하면 `False`(stale). 주 호출자 `run_institutional_trend_step` 는 원래 stale 을 따로 검증해 실패로 봤다. (2) 다시 받는 날짜는 두 프레임에 모두 있는 종목만 저장한다. 그 날짜에 Toss 행이 남아 있으면 창을 벗어날 때까지 매 실행 다시 묻는다(날짜당 2회). (3) 사용자 중단 뒤 백필이 필요한 경우 `False`

## 시나리오

### S-1. 창 안의 Toss 근사 날짜를 pykrx 정확값으로 바꾼다 (하네스, 필수)
- 조작: 두 사본에서 `approx` 모드. 09-17·09-18 정확값, 09-21·09-22 세 종목 모두 근사값(`7000/9000`, `source=toss`), 기대일 09-22
- 기대: 수정 전은 「이미 최신」으로 pykrx 호출 없이 `True`, 근사값 그대로. 수정 뒤는 pykrx 를 09-21·09-22 에 부르고 005930·000660 이 `10/20`·`20/40`(source 빈 칸)으로 바뀐다. pykrx 가 주지 않는 069500 은 근사값 `source=toss` 로 남는다. Toss 호출 0, `True`
- 실제: 22:21 실행, 두 사본 모두 exit 0·Traceback 0·`True`·Toss 호출 0. 수정 전(`debb932`)은 pykrx 호출 없음(`[]`), 09-21·09-22 여섯 행이 모두 `7000/9000`·`toss` 그대로. 수정 뒤(`1498f99`)는 로그 「근사·부분 날짜 2개를 다시 수집합니다」, pykrx 호출 `['20260921', '20260922']`, 두 날짜의 005930 `10/20`·000660 `20/40`(source 빈 칸), 069500 은 `7000/9000`·`toss` 로 남음. 통과

### S-2. 과거 근사 날짜를 받고 최신일이 빈 응답이면 Toss 백필로 채운다 (하네스, 필수)
- 조작: `latest` 모드. 09-22 근사, 기대일 09-23 에서 pykrx 가 빈 응답
- 기대: 수정 뒤는 pykrx 를 09-22·09-23 에 부르고, 09-22 의 두 주식은 정확값, 09-23 은 세 종목 `3000/4000`(source=toss), `True`. 수정 전은 09-22 를 다시 받지 않아 근사값이 남고 09-23 만 백필된다(source 없음)
- 실제: 두 사본 모두 exit 0·Traceback 0·`True`·Toss 호출 3. 수정 전은 pykrx `['20260923']`, 09-22 세 행 `7000/9000`·`toss` 유지, 09-23 세 행 `3000/4000`(source 없음). 수정 뒤는 pykrx `['20260922', '20260923']`, 09-22 의 005930 `10/20`·000660 `20/40`(정확값), 069500 은 근사값 유지(`keep="first"`), 09-23 세 행 `3000/4000`·`toss`, 로그 「Toss 백필」. 통과

### S-3. 다시 받는 날짜에서 한쪽 프레임이 비면 기존 값을 0 으로 덮지 않는다 (하네스, 필수)
- 조작: `oneside` 모드. 09-22 근사, 기관합계 프레임 빈 응답
- 기대: 수정 뒤 pykrx 를 09-22 에 부르지만 저장 행이 없어 CSV 가 수정 전 출력과 같다(근사값 유지, `inst_buy` 0 없음). 두 쪽 모두 `True`, Toss 호출 0
- 실제: 두 사본 모두 exit 0·Traceback 0·`True`·Toss 호출 0. 수정 전 pykrx `[]`, 수정 뒤 `['20260922']`. 저장된 12행이 두 사본에서 같음(`rows_equal True`), 09-22 세 행 모두 `7000/9000`·`toss` 로 남았고 `inst_buy` 0 으로 덮인 행 없음. JSON 전체의 차이는 pykrx 호출 목록뿐. 통과

### S-4. 재수집 대상이 없으면 결과가 수정 전과 같다 (하네스, 필수, 회귀 확인)
- 조작: `plain` 모드. 09-17·09-18·09-21 정확값, 기대일 09-22
- 기대: 두 사본의 JSON 출력이 `cmp` 로 같다. pykrx 호출은 09-22 하나
- 실제: 두 사본 모두 exit 0·Traceback 0·`True`. 두 JSON 출력이 `cmp` 로 같음(`PLAIN_IDENTICAL`), pykrx 호출 `['20260922']`, 09-22 에 005930 `10/20`·000660 `20/40` 추가(069500 은 가짜 pykrx 에 없어 행 없음). 통과

## 정리
- 실제: 사본 둘(`q094a`·`q094b`)을 지우고 없음 확인. 첫 시도에서 zsh 가 `set -- $pair` 를 나누지 않아 이름에 공백이 든 빈 디렉터리 둘이 생겼고, 내용이 비어 있음을 확인한 뒤 `rmdir` 로 지움(삭제 명령은 그 빈 디렉터리 안에서만 실행됨). 서버를 띄우지 않았고 3500·5501 리스너 0. 원본 `data/all_institutional_trend_data.csv` 수정 시각(1789994453, 09-21 21:40) 그대로. 소켓 차단으로 네트워크 없음, KRX 로그인·LLM 호출 없음, 여덟 실행 로그 Traceback 0. 결과: 필수 4/4 통과

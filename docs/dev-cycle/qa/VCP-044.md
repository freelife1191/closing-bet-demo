# [VCP-044] VCP 재분석 결과가 캐시에 없는 종목을 캐시에 넣지 않고, 날짜 없는 캐시 파일을 날짜 확인 없이 읽고 쓴다 — QA 기록

- 대상: `services/kr_market_vcp_cache_update_service.py` 의 `update_vcp_ai_cache_files`(수집용 `_write_ai_analysis_files` 재사용), `services/kr_market_vcp_reanalysis_service.py` 의 `load_vcp_ai_cache_map`(날짜 없는 파일의 `signal_date` 확인)
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24 11:03
- 구성 근거: 설계 승인(10:57), 호출 경로 추적(재분석 라우트 → `execute_vcp_failed_ai_reanalysis` → `load_vcp_ai_cache_map` 으로 대상 선정 → 분석 → `update_cache_files` = `app/routes/kr_market.py` 의 `_update_vcp_ai_cache_files` → `update_vcp_ai_cache_files`), 화면 조회 경로(`services/kr_market_vcp_payload_service.py` 가 `ai_analysis_results_<날짜>.json` 을 읽어 VCP 표의 GEMINI·GPT 열을 채움)
- 검증 기준 커밋: 첫 커밋(해시는 커밋 뒤 기록)
- QA 엔진(engine): Claude Code. 재분석 결과는 VCP 화면(`/dashboard/kr/vcp`)의 GPT 열로 보이므로 browser_applicability: required(browser_driver: agent-browser)
- 하네스: 첫 커밋의 `git archive` 사본, `secrets/`·`data/`·`.env` 삭제 뒤 `data/` 에 공개 시장 자료(`daily_prices.csv`·`korean_stocks_list.csv`·`signals_log.csv`)만 복사한다. LLM 키는 넣지 않는다. scratchpad `vcp044qa_harness.py` 가 한 프로세스에서 싱글톤 분석기의 클라이언트만 가짜로 바꾼다(`VCP_AI_PROVIDERS=gemini,gpt,zai`, `VCP_GPT_MODEL` 빈 값이라 GPT 자리는 Z.ai 가 채움). 수집 단계에서는 Z.ai 클라이언트를 비우고(두 번째 자리가 값을 내지 못함) 이름에 「KT」가 든 프롬프트(030200)만 Gemini 가 예외를 낸다. Z.ai 는 호출이 실패해도 규칙 기반 판정을 돌려주므로 종목이 캐시에서 통째로 빠지는 것은 이 경우다. 재분석 단계에서는 Z.ai 클라이언트를 되돌리고 모두 성공한다(Gemini HOLD, Z.ai BUY, 사유에 「VCP044-QA … reanalyze」). 수집은 `create_signals_log("2026-09-21", run_ai=True, signal_limit=2)`, 재분석은 라우트와 같은 인자로 `execute_vcp_failed_ai_reanalysis(target_date="2026-09-21")`(자동 모드)
- 대조(수정 전 커밋 `677d61f` 의 사본, 같은 하네스):
  - 대조 1회차(11:04, 수집 때 Z.ai 가 예외로 실패하도록 둔 초기 하네스): KT 는 Gemini 가 비고 GPT 칸이 규칙 기반 HOLD 로 캐시에 들어갔다. 재분석은 09-18 날짜 없는 파일의 KT Gemini(SELL, 「STALE-0918」)를 09-21 캐시로 읽어 KT 를 채워진 것으로 보고 `failed_targets=0` 으로 끝났다. **읽기 쪽 결함 재현**(날짜 다른 날짜 없는 파일이 빈 칸을 가림). 로그 `vcp044qa-before-run1.log`
  - 대조 2회차(11:06, 현재 하네스): 수집 뒤 09-21 날짜 파일 둘에 `000088` 만 있다. 재분석은 두 종목을 대상으로 잡아 성공했으나(`failed_targets=2`, `updated_count=2`) 09-21 날짜 파일에는 여전히 `030200` 이 없고, 대신 09-18 날짜 없는 파일의 `030200` 이 「VCP044-QA … reanalyze」로 덮였다(`stale undated unchanged: False`). **쓰기 쪽 결함 두 가지 재현.** 로그 `vcp044qa-before.log`
- 원본 `data/`·`logs/`·3500·5501·운영 주소는 건드리지 않고, 실제 LLM·발송은 하지 않는다
- 필수 여부(required): S-1~S-4 예
- 반복(iteration): (실행 대기)
- 결과: (실행 대기)
- 증거: (실행 대기)
- 정리(cleanup): (실행 대기)

## 시나리오

### S-1. 수집 때 캐시에 못 들어간 종목을 재분석이 캐시에 넣는다 (핵심)
- 조작: 수집 뒤 재분석(자동 모드) 1회
- 기대: 수집 뒤 `ai_analysis_results_20260921.json` 에 `000088` 만 있고 `030200` 은 없다. 재분석은 `030200` 을 대상으로 잡고(`failed_targets` ≥ 1) 성공한다. 재분석 뒤 같은 파일과 `kr_ai_analysis_20260921.json` 에 `030200` 이 `gemini_recommendation` HOLD·`gpt_recommendation` BUY(「VCP044-QA zai reanalyze」)로 들어가고, `000088` 의 수집 결과(「… collect」)는 그대로다. 수정 전 사본에서는 `030200` 이 캐시에 없다
- 실제: (실행 대기)

### S-2. 날짜 파일이 없던 날도 재분석이 날짜 파일을 만든다
- 조작: 새 사본에서 `QA_FAIL_ALL=1` 로 수집(두 종목 모두 모든 AI 실패, 날짜 파일이 만들어지지 않음) 뒤 재분석 1회
- 기대: 두 날짜 파일이 새로 생기고 `signal_date` 가 `2026-09-21` 이며 두 종목이 들어 있다. 날짜 없는 파일은 분석 날짜(09-21)가 오늘(09-24)이 아니므로 새로 만들지 않는다
- 실제: (실행 대기)

### S-3. 다른 날짜의 날짜 없는 캐시가 겹치는 종목을 가려도 재분석 대상에서 빠지지 않고 그 파일을 고치지 않는다
- 조작: S-1 의 재분석 직전에 `ai_analysis_results.json` 을 `signal_date: 2026-09-18`·`030200` SELL(「STALE-0918」)로 만든다
- 기대: `030200` 이 여전히 재분석 대상이 되고(S-1 과 같은 결과), `ai_analysis_results.json` 내용이 바이트 단위로 그대로다. 수정 전 사본에서는 09-21 재분석 결과가 이 09-18 파일의 `030200` 에 쓰인다(CSV 에서 실패로 기록된 행은 캐시와 무관하게 대상이 되므로, 읽기 쪽 차이는 단위 테스트 `test_load_vcp_ai_cache_map_ignores_undated_cache_of_other_date` 로 확인한다)
- 실제: (실행 대기)

### S-4. VCP 화면 GPT 열 (브라우저)
- 조작: S-1 뒤 사본에서 gunicorn·Next dev 를 띄우고 `http://localhost:<port>/dashboard/kr/vcp` 를 넓은 뷰포트로 연다
- 기대: `/api/kr/signals` 의 `030200` 행 `gpt_recommendation.action` BUY, 화면 KT 행의 GPT 열에 「▲ 매수」, GEMINI 열에 「■ 관망」, 콘솔 오류 없음
- 실제: (실행 대기)

# KRX 적용 후 추가 운영 확인

engine: ultraqa; lifecycle: app-adapted; phase: stopped-with-findings; iteration: 1.
범위: 사용자 추가 확인 요청. 제품 변경 없이 기존 스케줄 작업 실행 경로와 데이터/AI/화면을 검증한다.
실제 시세·AI 사용량 및 결과 저장은 승인된 범위다. 알림·거래·설정 변경은 실행하지 않는다.

| ID | 검사 | 방법 | 기대 | 결과 |
|---|---|---|---|---|
| A | KRX 적용 전체 분석 | 기존 scheduled chain 실제 수동호출 | 가격/수급/종가 단계 성공 | FAIL: 종목코드 빈 값으로 전원 탈락. 기존12건 복구 |
| B | VCP 신규 분석 | 동일 체인의 create_signals_log(run_ai=True) | 오늘 시그널 및 AI 결과 또는 정상 조건미달 확인 | 오늘0건. 과거1종목 실제Gemini/GPT별도응답 성공, 최신저장안함 |
| C | 스케줄러 | 등록 로그·실제 job 함수 순서/상태복구 | 중복 실행 없음, 단계실행, 최종idle | 순서·종료상태 PASS, 결과오류를 성공으로 오인함 |
| D | 숫자 일치 | 원자료와 AI 근거 문장 대조 | 허위 단위/값 없음 | FAIL: 금액2건, 전략수치2건 이상 발견 |
| E | 실제 화면 | ego-browser 결과조회 | 날짜·내용·오류 확인 | 빈VCP/복구된12건 표시·JS오류0 확인 |

마지막 알림 함수만 no-op으로 대체하며 실제 분석 함수는 변경하지 않았다. 미래 예약 시각의 자동 실행을 관측한 것으로 주장하지 않는다. 기존결과는 logs/post-krx-backup-20260921/에 백업. 실행로그는 메모리에서 시크릿을 지운 뒤 로컬저장한다. 실패하면 결과/원인을 기록하고 미통과를 성공으로 바꾸지 않는다.


## 실행과 판정

실제 등록은 Asia/Seoul 매일17:00, 다음 실행2026-09-22 17:00이다. 이번에는 같은 `run_daily_closing_analysis()`를 수동 호출했다. 마지막 알림 callback만 no-op으로 교체했고 가격·수급·VCP·종가 함수는 실제로 수행했다. 전체154.4초, 단계13.4/9.5/52.4/78.3초, 최종상태IDLE. 알림콜백에 도달했지만 메시지는 보내지 않았다. 다음 예약 시각의 실제 자동 발화는 관측하지 않았다.

가격·수급 갱신은 성공했으며 VCP는 오늘조건0건으로 저장됐다. 오늘 VCP AI 호출은 없어 기존2026-05-05 현대로템1건을 `analyze_batch`로 실제호출했다. Gemini/GPT의 비어있지않은분석근거를 확인했고 현재결과파일에는 쓰지 않았다. 오늘신규VCP시그널 성공이라고 세지 않는다.

종가베팅은 KRX조회후 KOSPI139/KOSDAQ236개 후보를 가져왔지만, 모두1차탈락하고0건을 저장했다. `engine/collectors/krx_data_mixin.py`의 reset_index→rename(index=ticker)가 이름있는티커인덱스를 처리하지 못한다. input index.name=티커, value005930인 DataFrame을 통과시키면 StockData.code가빈문자열이됨을합성재현했다. 잘못된후보를정상조건미달로분류해기존결과까지덮는 결함이다. 이번에만든빈latest/daily를실행전백업12건으로복구했고브라우저에서확인했다. 잘못된캐시도후속수정대상이다.

독립 숫자검토는 updated_at2026-09-21T21:22:36.852076의12건을대상으로했다. 삼성전자9조2089억원→AI92조89억원, SK하이닉스8조46.7억원→AI80조47억원 오기를확인했다. 억단위프롬프트를 모델이조단위로재표현하는과정의오류로추정하되내부인과를확정하지않았다. 현재가보다높은손절제안등2건도후속검토대상이다. 세부근거는numeric-audit.md.

## 정리

브라우저Space30은 finish keep[]1회로종료했고PNG2장을직접확인했다. 임시실행스크립트2개제거, 서비스3500/5501유지. 설정/인증변경없음. 실제데이터수집이므로가격/수급/캐시는갱신됐으며원본data무변경을주장하지않는다. 테스트를위한메신저발송/거래는없었다. 원시로그는자격증명값을지운뒤로컬logs에만보관했다.

ULTRAQA BLOCKED: 전체정상판정불가. INFRA-072와JONGGA-038을TODO에등록했고완료아카이브를만들지않았다. 이번요청의추가확인은완료했지만발견한제품결함수정은미완료다.

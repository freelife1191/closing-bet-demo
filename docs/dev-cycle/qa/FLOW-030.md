# [FLOW-030]·[FLOW-031]·[JONGGA-044] 오늘 키 참조를 캐시하지 않음과 기준일 CSV 창 판정 — QA 시나리오

- 대상: 세 항목을 한 라운드로 묶은 T3(`docs/dev-cycle/TODO.md` 의 `[FLOW-030]` 「설계 승인」 줄과 `[FLOW-031]` 「범위 변경 승인」 두 줄)
- 대상 화면: http://localhost:3757/dashboard/kr/closing-bet (격리 Next dev) → 카드의 「상세 분석 보기」 모달의 「투자자 동향」 절
- 구성 근거: 모달은 기준일 없는 최신 창이다. 카드 클릭 → `/api/kr/stock-detail/<code>` → `append_investor_trend_5day` →
  `get_investor_trend_5day_for_ticker(verify_with_references=True)` → CSV 에 없으면(`missing_csv`) `_get_reference_trend_cached(pykrx)`(키는
  `_resolve_pykrx_latest_market_date`) → 거부되면 `_get_reference_trend_cached(toss)`(키는 달력상 오늘). `[FLOW-030]` 의 Toss 참조 비저장은
  이 흐름에서 드러나므로 브라우저 실측이 필수다
- 브라우저가 닿지 않는 흐름: 기준일 실행(`KRXCollector.get_supply_data(target_date=...)`, 17시 체인과 스크리너)의 오늘 키 pykrx 참조와
  `[FLOW-031]` 의 기준일 CSV 창 판정은 화면에서 가를 수 없어 CLI 하네스로 검사한다(H-1~H-5). 오늘 키 판정은 `datetime.now()` 에 달려 있으므로
  하네스가 서비스 모듈의 `datetime` 을 고정 시각으로 바꾼다(테스트의 `_freeze_now` 와 같은 방식)
- `[JONGGA-044]` 는 테스트 파일 한 줄만 바꿔 동적 QA 제외다(`tier-rules.md` §1-1). 증거는 vitest 전체 104 files·701 passed 와 TODO 의 RED 기록이다
- 휴장일 목록(`engine/market_schedule.py`)에 2026-09-24·25(추석)가 들어가 고정 자료의 마지막 거래일은 2026-09-23(수)이다. 창은 09-17·18·21·22·23 이다
- 검증 기준 커밋: 새 코드는 이 문서를 담은 첫 커밋, 대조용 수정 전 코드는 `f6708cbc`(첫 커밋의 부모). 사본은 각 커밋의 `git archive` 다
- QA 엔진(engine): Claude Code, 브라우저는 gstack `browse`. `[FLOW-029]` 와 같은 방식으로 시나리오를 직접 구성하고 `/qa-only`·`/qa` 스킬 본체의
  전체 앱 탐색은 하지 않는다
- browser_applicability: required(S-1~S-3). browser_driver: gstack `browse`(새 코드만). 수정 전 코드는 API 응답과 캐시 행만 대조한다
- 격리: 각 커밋의 `git archive` 를 scratchpad `flow030/qa/new/`·`flow030/qa/old/` 에 풀고 `secrets/`·`data/`·`.env*` 를 지운 뒤 사본마다 `data/` 에
  고정 자료(생성기 `flow030/qa/make_fixture.py`)만 둔다. 격리 gunicorn 은 새 코드 5757·수정 전 5758, Next 는 3757 이다. CLI 하네스는 실행마다 새
  자료 디렉터리를 쓴다(H-1 의 두 단계와 H-5 의 두 코드는 한 디렉터리를 공유). 원본 3500/5501·live 주소·원본 `data/`·`.env` 는 쓰지 않는다
- 대역: Flask 는 QA 전용 진입점 `flow030/qa/qa_flow030_app.py`(사본에만 복사), CLI 는 `flow030/qa/qa_cli.py`. 둘 다 loopback 이 아닌 소켓 연결을 막아
  `QA-BLOCKED` 로 남기고 `sys.modules` 에 가짜 `pykrx` 를 넣는다. 가짜 지수(`get_index_ohlcv_by_date`)는 09-23 으로 끝나 모달의 pykrx 키가 실행 시각과
  무관하게 `20260923` 이다. Flask 진입점은 `TossCollector._safe_request` 를 900271·900272 의 종목 정보와 900271 의 투자자 동향만 응답하게 바꾼다.
  LLM·발송 경로는 부르지 않는다. 기동 환경에 `KRX_ID=`·`KRX_PW=` 빈 값
- 구성 2026-09-26 01:16(첫 커밋 `46f569a8` 에 포함) | 실행 01:24:03(사본 구성)~01:25:14(마지막 API 대조), 정리 확인 01:25:56
- 단계(phase): 시나리오 구성 완료 | 실행 완료
- 반복(iteration): 1회
- baseline 상태: 수정 전 코드 사본에 같은 고정 자료로 같은 API·하네스를 돌려 대조한다. 회귀 검사는 수정 전에 실패했다(TODO `[FLOW-030]` 의 RED 기록)
- 필수 여부(required): S-1~S-3, H-1~H-5 예
- 결과: 통과 (필수 8/8)
- 증거: 각 시나리오의 「실제」 줄. 모두 scratchpad `flow030/qa/` 아래에 있다. CLI 출력 `qa-cli.txt`(01:24:17~01:24:30), 모달 글자·콘솔·네트워크
  `qa-browser.txt`(스크립트 `browse_run.sh`), 스크린샷 `flow030-QA전날끝-modal.png`·`flow030-QA과거키-modal.png`(열어 확인), API 응답·캐시 행·로그 집계
  `qa-api.txt`(원문 `raw-<포트>-<종목>.json`), 격리 로그 `qa-new-flask.log`·`qa-old-flask.log`·`qa-new-next.log`. CLI 의 `QA-CODE` 줄과 로그의
  `QA-PYKRX`·`QA-TOSS` 줄로 각 사본이 대역과 자기 코드를 거쳤음을 확인했다
- 구성 중 차이(계획과 다름): 계획은 CLI 하네스가 모두 `KRXCollector.get_supply_data(target_date=...)` 를 부른다고 적었다. 그 경로는 기준일이 있으면
  pykrx 참조를 먼저 보고(`get_pykrx_trend_5day`) 실패할 때만 CSV 로 가므로 H-2~H-4 의 CSV 창 판정이 가려진다. 그래서 `today`(H-1)는 수집기 경로(17시 체인)를,
  `csv`(H-2~H-4)는 스크리너의 과거 기준일 경로(`engine/screener.py` 의 `_calculate_supply_score_csv`, `get_investor_trend_5day_for_ticker(target_datetime=..., verify_with_references=True)`)를
  그대로 부르게 했다. 기대값은 바꾸지 않았다. CSV 는 자료 디렉터리에 없을 때만 쓰게 해 같은 디렉터리를 잇는 H-1 B·H-5 에서 수정 시각이 바뀌지 않게 했다
- 추가 실행(필수 아님): H-5 (3) 수정 전 사본으로 `today A`(09-23 17:05 장중 값 5억을 저장) 뒤 같은 디렉터리에 새 사본으로 `today B`. 배포 당일 남은 장중 값을 새 코드가 읽지 않는지 본다
- 정리(cleanup): browse 서버 정지(「Server stopped」). 격리 gunicorn 마스터 둘과 Next 의 npm·run-next·node·next-server 를 PID 와 작업 디렉터리(사본)를 확인한 뒤 종료했다.
  01:25:40 에 3757/5757/5758/3500/5501 리스너 0, 사본 경로 프로세스 0. 사본 `new/`·`old/` 와 CLI 자료 디렉터리 열셋을 리터럴 절대 경로로 삭제했다. 원본 `data/` 에서
  사본 구성 전 표식 이후 바뀐 파일 0, 저장소 루트 `node_modules/.vite` 없음, `git status` 는 깨끗했다(이 기록 전). 원본 3500/5501 은 처음부터 떠 있지 않았다
- 관찰(판정 밖): `qa_cli.py` 가 캐시 행이 없을 때 줄바꿈 없이 「QA-ROWS pykrx none」을 찍어 다음 줄 `exit=0` 과 붙었다. 하네스 출력 형식만의 문제다
- 읽은 정본: `.claude/skills/closing-bet-python/SKILL.md`, `.claude/skills/closing-bet-verify/SKILL.md`. frontend 변경은 테스트 파일뿐이라 Next 번들 문서는 읽지 않았다

## 고정 자료

- 화면용 수급 CSV: 900264(09-15~09-23, 하루 2억·3억) 한 종목만 둔다. 카드 두 종목은 CSV 에 없어(`missing_csv`) 참조로 간다
- 카드: 900271 QA전날끝(pykrx 가 09-22 로 끝나 거부 → Toss 참조), 900272 QA과거키(pykrx 가 09-23 으로 끝나 채택)
- Flask 가짜 pykrx 거래대금: 900271 은 09-16·17·18·21·22, 900272 는 09-17·18·21·22·23, 둘 다 하루 외국인 1억·기관 2억. 그 밖은 빈 프레임
- Flask 가짜 Toss 투자자 동향(900271): 09-16~09-22 다섯 날, 종가 10,000, 외국인 −20,000주·기관 −40,000주 → 5일 합 외국인 −10억·기관 −20억

CLI 하네스(1억 = 100,000,000, 값은 `[외국인 5일, 기관 5일]`). 기준일은 모두 `20260923` 이다.

| 시나리오 | 자료 디렉터리의 CSV | 가짜 pykrx(09-17~09-23 또는 표기한 날) | 고정 시각 |
|---|---|---|---|
| H-1 단계 A | 900264 만(09-15~09-23), 900281 없음 | 900281 하루 1억·1억 | 2026-09-23 17:05 |
| H-1 단계 B | 같은 디렉터리 | 900281 09-23 만 3억·3억, 나머지 1억·1억 | 2026-09-24 09:00 |
| H-2 | 900291 09-15~09-22 하루 1억·2억(CSV 전체가 09-22 로 끝남) | 900291 이 09-16~09-22(전날로 끝남) | 2026-09-24 10:00 |
| H-3 | 900292·900293·900295·900296·900297 09-15~09-22 하루 1억·2억, 09-23 은 900292 한 줄(5억·6억) | 900293 하루 3억·3억 | 2026-09-24 10:00 |
| H-4 | 900294 09-15~09-23 하루 1억·2억 | 없음 | 2026-09-24 10:00 |

## 시나리오

### S-1 [FLOW-030] 모달의 Toss 참조는 그려지지만 오늘 키로 저장되지 않는다 (필수)
- 조작: 900271 QA전날끝 카드의 「상세 분석 보기」
- 기대(새 코드): 「투자자 동향」 외국인 「-10억」·기관 「-20억」(Toss 참조). API 응답 `investorTrend5Day` 가 −1,000,000,000·−2,000,000,000.
  사본의 `data/.investor_trend_reference_cache/toss/runtime_cache.db` 에 900271 행 없음(파일이 없거나 행 0), pykrx 캐시에도 900271 행 없음
- 기대(수정 전, API): 같은 값, Toss 캐시에 `900271__<실행 당일 YYYYMMDD>` 행 있음
- 실제: 통과. 모달 「투자자 동향」 외국인 「-10억」·기관 「-20억」·개인 0(스크린샷). 두 코드의 API `investorTrend5Day` 모두 {−1,000,000,000, −2,000,000,000}.
  새 사본 toss 캐시 DB 는 행 0, pykrx 캐시에는 `900272__20260923`·`latest_market_date__20260926` 뿐이고 900271 행 없음. 수정 전 사본 toss 캐시에 `900271__20260926`.
  두 로그 모두 `QA-PYKRX trading_value 900271 20260909-20260923` 1줄, `QA-TOSS trading-trend 900271` 2줄(상세의 Toss 합계와 Toss 참조). 새 코드에서 브라우저 2회와
  API 1회의 상세 요청이 Toss 를 다시 부르지 않은 것은 15분 상세 캐시가 응답을 준 것이다(계획의 「알려진 동작 변화」 첫 줄의 범위)

### S-2 [FLOW-030] 지난 날짜 키의 pykrx 참조는 계속 저장된다 (필수)
- 조작: 900272 QA과거키 카드의 「상세 분석 보기」
- 기대(두 코드): 「투자자 동향」 외국인 「5억」·기관 「10억」, API `investorTrend5Day` 500,000,000·1,000,000,000.
  pykrx 캐시에 `900272__20260923` 행 있음(키 09-23 은 실행 당일보다 앞선다). 새 코드와 수정 전 코드의 서명 값은 다르다(`v3`·`v2`)
- 실제: 통과. 모달 외국인 「+5억」·기관 「+10억」(스크린샷, 개인은 가짜 pykrx 에 없어 「자료 없음」). 두 코드의 API 모두 {500,000,000, 1,000,000,000}.
  pykrx 캐시에 `900272__20260923` 행이 두 사본 모두 있고 서명 두째 값이 새 코드 1120597248(`pykrx:v3`), 수정 전 1120597247(`pykrx:v2`)이다

### S-3 콘솔·네트워크 (필수)
- 기대: S-1·S-2 동안 콘솔 오류 0, 상세 요청 둘 다 200, `QA-BLOCKED` 0줄
- 실제: 통과. `console --errors` 「no console errors」(01:25:04). 네트워크 `stock-detail/900271`·`stock-detail/900272` 가 각각 2회, 전부 200.
  두 Flask 로그 `QA-BLOCKED` 0줄, Traceback 0줄

### H-1 [FLOW-030] 오늘 키 pykrx 참조는 값을 주지만 저장하지 않고, 다음 날 새 프로세스가 확정 값을 저장한다 (필수)
- 명령: `qa_cli.py <사본> <자료> today A` 뒤 같은 자료로 `today B`(새 코드끼리, 수정 전 코드끼리 각각)
- 기대(새 코드): 단계 A `value=[500000000, 500000000] pykrx_calls=1`, 단계 A 뒤 pykrx 캐시 행 없음,
  단계 B `value=[700000000, 700000000] pykrx_calls=1`, 단계 B 뒤 `900281__20260923` 행 있음
- 기대(수정 전): 단계 A 같은 값, 단계 A 뒤 행 있음, 단계 B `value=[500000000, 500000000] pykrx_calls=0`(09-23 장중 값이 남음)
- 실제: 통과. 새 코드 A `value=[500000000, 500000000] pykrx_calls=1`, 「QA-ROWS pykrx none」. B `value=[700000000, 700000000] pykrx_calls=1`,
  `900281__20260923` 행(서명 1120597248, 7억·7억). 수정 전 A 같은 값·행 있음(5억·5억, 서명 1120597247), B `value=[500000000, 500000000] pykrx_calls=0`

### H-2 [FLOW-031] CSV 가 기준일 전날로 끝나고 pykrx 도 전날로 끝나면 None 이다 (필수)
- 명령: `qa_cli.py <사본> <자료> csv b`
- 기대(새 코드): 900291 `value=None pykrx_calls=1`, 로그 「기준일 2026-09-23 전 2026-09-22 로 끝나 영업일 1일이 비었다」 1줄
- 기대(수정 전): 900291 `value=[500000000, 1000000000] pykrx_calls=0`(09-16~09-22 CSV 5일)
- 실제: 통과. 새 코드 `value=None pykrx_calls=1`, 경고 「수급 CSV 가 기준일 2026-09-23 전 2026-09-22 로 끝나 영업일 1일이 비었다. 기준일 5일 값을 CSV 로 만들지
  않는다」 1줄, pykrx 캐시 행 없음(거부 값). 수정 전 `value=[500000000, 1000000000] pykrx_calls=0`

### H-3 [FLOW-031] 기준일이 부분 수집일이어도 그날 행이 있는 종목은 CSV 값, 없는 종목은 참조로 간다 (필수)
- 명령: `qa_cli.py <사본> <자료> csv c`
- 기대(새 코드): 900292 `value=[900000000, 1400000000] pykrx_calls=0`(09-17~09-23), 900293 `value=[1500000000, 1500000000] pykrx_calls=1`
- 기대(수정 전): 부분 수집일 09-23 을 빼 900292·900293 모두 `value=[500000000, 1000000000] pykrx_calls=0`(09-16~09-22)
- 실제: 통과. 새 코드 900292 `value=[900000000, 1400000000] pykrx_calls=0`, 900293 `value=[1500000000, 1500000000] pykrx_calls=1`(행 `900293__20260923`).
  수정 전 900292·900293 모두 `value=[500000000, 1000000000] pykrx_calls=0`

### H-4 [FLOW-031] CSV 에 기준일이 온전히 있으면 두 코드 모두 CSV 값이다 (필수)
- 명령: `qa_cli.py <사본> <자료> csv d`
- 기대(두 코드): 900294 `value=[500000000, 1000000000] pykrx_calls=0`
- 실제: 통과. 새 코드와 수정 전 모두 900294 `value=[500000000, 1000000000] pykrx_calls=0`

### H-5 배포 전 캐시(v2 참조 행·`unified_v2` 맵)를 새 코드가 읽지 않는다 (필수)
- 명령: (1) 수정 전 사본으로 `csv b` 를 돌린 자료 디렉터리에 새 사본으로 `csv b` (2) 수정 전 사본으로 `today B` 를 돌린 자료 디렉터리에 새 사본으로 `today B`
- 기대: (1) 새 코드 900291 `value=None pykrx_calls=1`(옛 맵의 CSV 값을 쓰지 않음) (2) 새 코드 900281 `value=[700000000, 700000000] pykrx_calls=1`,
  같은 키의 행 서명이 바뀐다
- 실제: 통과. (1) 수정 전 `csv b` 가 `[500000000, 1000000000] pykrx_calls=0` 을 내고 맵 행 `…::investor_trend_5day_unified_v2::20260923`(본문 167바이트)을
  남긴 디렉터리에서 새 코드는 `value=None pykrx_calls=1` 과 경고 1줄을 내고, 맵 행 `…unified_v3::20260923`(본문 11바이트, 빈 맵)을 따로 저장했다.
  (2) 수정 전 `today B` 뒤 새 코드 `today B` 는 `value=[700000000, 700000000] pykrx_calls=1`, 같은 키 `900281__20260923` 의 서명이 1120597247 → 1120597248.
  (3, 추가) 수정 전 `today A`(5억·5억 저장) 뒤 새 코드 `today B` 도 `value=[700000000, 700000000] pykrx_calls=1`

## 알려진 동작 변화와 비용(검사 대상 아님)

- 오늘 키의 Toss 참조는 저장하지 않으므로 15분 상세 슬롯 캐시와 60초 실패 캐시 사이에서 다시 묻는다(계획의 항목 (4), 의도한 동작)
- 스크리너 과거 기준일 배치에서 창 끝과 기준일 사이가 비면 전 종목이 pykrx 를 한 번씩 조회한다. 과거 키라 한 번 뒤에는 저장된다(계획 검토 m1)
- 비거래일 기준일은 참조의 끝 날짜 대조에서 늘 실패해 None 이다(`/review` M-2). 휴장일 목록 09-24~12-31 은 KRX 공지와 대조하지 않았다

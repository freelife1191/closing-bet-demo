# [VCP-041] VCP 분석기가 429/503 을 받은 Gemini 모델을 워커 수명 동안 영구 제외한다 — QA 기록

- 대상: `engine/vcp_ai_analyzer.py` 의 `_analyze_with_gemini`·`gemini_blocked_models`, 프로세스 싱글톤 `get_vcp_analyzer()`
- 단계(phase): 원인 확정 2 완료 | 원인 확정 1(운영 로그) 대기(운영자) | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-24 09:00 | 실행 2026-09-24 09:06~09:10 (1회차)
- 구성 근거: 설계 승인(08:54), critic 계획 검토 REVISE(F1 배치 마지막 종목), 호출 경로 추적(`analyze_batch` → `analyze_stock`, 재분석 `background: true` → `run_async_analyzer_batch_with_control` → `analyze_stock` 직접)
- 검증 기준 커밋: `7470475` (첫 커밋). 사본은 이 커밋의 `git archive HEAD` 이며 실행 중 범위 파일은 바뀌지 않았다
- QA 엔진(engine): Claude Code. VCP 화면(`/dashboard/kr/vcp`)의 Gemini 열이 이번 수정이 바꾸는 저장 자료를 읽으므로 browser_applicability: required
- 하네스: 첫 커밋의 `git archive HEAD` 사본, `secrets/` 삭제, `data/` 에는 공개 시장 자료 세 파일만 복사, 사본 `.env` 는 `FLASK_PORT=58171`·`FRONTEND_PORT=58172` 두 줄. LLM 키는 환경에 넣지 않는다. 오래 사는 워커를 한 프로세스로 흉내 낸다: `VCP_AI_PROVIDERS=gemini` 로 싱글톤을 만든 뒤 `gemini_client` 만 가짜로 바꾼다(모드 fail = 모든 모델 `429 resource_exhausted`, 모드 ok = `{"action":"BUY","confidence":71,"reason":"VCP041-QA gemini ok"}`, 호출 수를 센다). 10분·하루 경과는 `engine.vcp_ai_analyzer.time.monotonic` 에 오프셋을 더해 흉내 낸다(외부 효과의 가장 바깥 경계인 LLM 클라이언트와 시계만 대역). 수집은 `create_signals_log("2026-09-21", run_ai=True, signal_limit=2)`, 재분석은 같은 프로세스의 Flask test client(더미 비밀 서명)
- 원본 `data/`·`logs/`·3500·5501·운영 주소는 건드리지 않고, 실제 LLM·발송은 하지 않는다
- 시계 대역 방식: 모듈의 `time` 만 오프셋을 더하는 대리 객체로 바꿨다(전역 `time`·asyncio 시계는 그대로). fail 모드 수집은 실제 백오프(약 80초)를 그대로 기다렸다
- 필수 여부(required): 예
- 반복(iteration): 1회. 기대값 정정 둘(결함 아님): (가) S-1 제외 모델 수를 7로 적었으나 사본에는 모델 설정이 없어 기본값 `gemini-3.7-flash` 가 체인 안의 같은 모델과 겹쳐 6개다(운영 `gemini-3.8-flash` 면 7개). (나) S-4 응답을 200 으로 적었으나 `background: true` 는 설계상 202 `started` 다(`app/routes/kr_market_data_signals_routes.py:398`)
- 결과: 통과 (필수 5/5)
- 증거: 아래 각 시나리오의 「실제」 줄(하네스 출력 원문), 로그 scratchpad `vcp041qa-harness.log`, 스크린샷 `vcp041qa-s5.png`(세션 임시). 기준 상태: 사본 `data/` 는 공개 시장 자료 세 파일뿐, `secrets/` 없음, venv inode 원본 407776753 / 사본 906793751, 시작 전 58171·58172 리스너 0
- 정리(cleanup): browse 서버 중지, cwd 가 사본인 프로세스 8개(gunicorn 마스터·워커, `npm run dev`·Next 자식)를 TERM 으로 내림(남은 것 0). 58171·58172 리스너 0, 사본 삭제 뒤 `ls` 로 없음 확인. 원본 `data/signals_log.csv` 는 09-21 21:41 그대로, 3500·5501 리스너 0(처음부터 없음)

## 시나리오

### S-1. 배치 마지막 종목에서 켠 차단이 시각을 남긴다 (회귀·F1)
- 조작: 가짜 Gemini fail 모드, 오프셋 0 에서 수집 1회
- 기대: 두 종목 CSV `N/A`·「분석 실패」, 분석기 `gemini_blocked_models` 가 체인 전체(사본 6개), 수집 뒤 `_session_blocks_since` 가 None 이 아님
- 실제: 09:06:08 시작. 가짜 Gemini 호출 6회, `blocked=6`, `since=5434.24`(None 아님), CSV `033530`·`108230` 모두 `ai_action` 결측(`N/A` 를 pandas 가 NaN 으로 읽음)·「분석 실패」, 캐시 파일 없음(유효 추천이 없는 종목은 넣지 않음). 통과

### S-2. TTL 안에서는 차단을 유지한다 (대조)
- 조작: 가짜 Gemini ok 모드로 바꾸고 오프셋 +300초에서 수집 1회
- 기대: 가짜 Gemini 호출 0회, CSV 는 여전히 `N/A`, 제외 집합 유지
- 실제: 09:07:29 시작. 호출 0회, `blocked=6`, `since` 가 S-1 과 같음, CSV 두 종목 여전히 「분석 실패」. 통과

### S-3. 다음 날 수집은 Gemini 를 다시 부른다 (핵심)
- 조작: ok 모드, 오프셋 +86,400초에서 수집 1회
- 기대: 가짜 Gemini 호출 ≥ 2(종목당 1), CSV 두 종목 `BUY`·71·「VCP041-QA gemini ok」, 캐시 `gemini_recommendation.action` BUY, 제외 집합 빈 값
- 실제: 09:07:37 시작. 호출 2회, `blocked=0`, `since=None`, CSV `033530`·`108230` 모두 `BUY`·71·「VCP041-QA gemini ok」, 캐시 `kr_ai_analysis_20260921.json` 두 종목 `gemini_recommendation.action` BUY. 통과

### S-4. 재분석 진행률 경로(analyze_stock 직접)도 풀린다
- 조작: fail 모드로 수집 1회(차단 재현, 오프셋 +90,000초) → ok 모드, 오프셋 +90,601초 → `POST /api/kr/signals/reanalyze-failed-ai` `{"target_date":"2026-09-21","background":true}` 후 상태 조회로 완료 대기
- 기대: http 202 `started`(정정), 완료 상태 success, 가짜 Gemini 호출 ≥ 1, CSV 두 종목 `BUY`·71
- 실제: 차단 재현 수집(09:07:45) 뒤 `blocked=6`·CSV 「분석 실패」. 09:08:56 재분석 POST → http 202 「실패 AI 재분석을 백그라운드에서 시작했습니다.」, `/api/kr/signals/status` 는 `running false`·`status success`·progress 100·「실패 2건 중 2건 재분석 완료」. 호출 2회, `blocked=0`, CSV 두 종목 `BUY`·71·「VCP041-QA gemini ok」. 통과. 재분석 저장 뒤 CSV 티커가 `033530` → `33530` 으로 바뀐 것은 기존 `[VCP-042]` 이다

### S-5. VCP 화면 Gemini 열 (브라우저, browser_driver: gstack browse)
- 조작: S-4 뒤 사본에서 gunicorn(58171)·Next dev(58172) 기동, `http://localhost:58172/dashboard/kr/vcp` 를 넓은 뷰포트로 연다
- 기대: 두 종목 행의 Gemini 열에 BUY 판정이 보이고 콘솔 오류 없음, `/api/kr/signals` 응답의 `gemini_recommendation.action` BUY
- 실제: `curl http://127.0.0.1:58171/api/kr/signals` 에서 `033530`·`108230` 의 `gemini_recommendation` 이 BUY·「VCP041-QA gemini ok」. browse `viewport 2000x900` → `goto http://localhost:58172/dashboard/kr/vcp`(200). 화면 텍스트에 「SJG세종 033530 … - ▲ 매수」「톱텍 108230 … - ▲ 매수」(GPT 열 `-`, GEMINI 열 매수), 콘솔 error 0건. 스크린샷을 열어 두 행 GEMINI 열의 초록 「▲ 매수」 배지를 확인. 통과

## 원인 확정

- 승인: 2026-09-24 08:48 KST 사용자 선택 「실호출 1회 승인」(격리 사본, 종목 하나, `_analyze_with_gemini` 1회, 체인은 설정 모델 하나)
- 기준 커밋: `3e87a08`. 사본은 `git archive HEAD` 를 scratchpad 에 풀고 `data/` 를 지웠다(`secrets/` 는 아카이브에 없음). 원본 `.env` 값은 실행 프로세스의 환경 변수로만 넘겼고 출력하지 않았다. Vertex 자격 증명은 원본 파일을 절대 경로로 가리켰다(복사하지 않음). 종목 입력은 원본 `data/signals_log.csv` 에서 읽기 전용으로 가져왔다(로컬 원본 최신일 2026-05-05, 000660 SK하이닉스, score 82)
- 하네스: 새 `VCPMultiAIAnalyzer()` 를 만들고 `build_gemini_retry_model_chain` 을 설정 모델 하나만 돌려주도록 바꿨다. `gemini_client.models.generate_content` 를 기록용 프록시로 감싸 응답 원문·`finish_reason`·사용량·예외를 남겼다. 호출 인자와 분석기 코드는 바꾸지 않았다
- 시도 1(08:49:40, 요청 미전송): 프록시로 교체하면서 원래 `genai.Client` 참조를 잃어 객체가 수거되었고, 내부 HTTP 클라이언트가 닫혔다. `RuntimeError: Cannot send a request, as the client has been closed.`(0.38초). 요청이 나가지 않았으므로 승인한 1회에 세지 않았다. 하네스 결함이며 운영 경로와 무관하다(운영 분석기는 `self.gemini_client` 를 계속 쥔다)
- 시도 2(실측, 08:49:52, 1회 호출): 설정 `VCP_GEMINI_MODEL=gemini-3.8-flash`, Vertex `global`, `config` 인자 없음
  - 응답: 5.2초, 후보 1개, `finish_reason=STOP`, `prompt_feedback=None`
  - 사용량: prompt 540, candidates 215, thoughts 303, total 1058
  - `response.text`: 400자. 코드펜스 없이 `{"action": "BUY", "confidence": 80, "reason": "[핵심 투자 포인트]…"}` 로 시작해 `}` 로 끝난다
  - `_parse_json_response`: 성공(BUY 80). `_analyze_with_gemini` 반환도 같다
  - 배치 뒤 `gemini_blocked_models`: 빈 집합
- 판정: 새 프로세스에서 운영과 같은 모델 이름·Vertex `global` 로 호출하면 정상 JSON 이 온다. 따라서 대안 가설 셋(JSON 파싱 실패, `response.text` None, 리전 404)은 **이 모델·설정에서는 재현되지 않았다**. `[VCP-040]` 실행 1(08:10, 두 종목 정상)과 합쳐 새 프로세스 3회 호출이 모두 정상이다. 「새 프로세스에서는 정상, 오래 산 워커에서만 null」이라는 형태는 영구 제외 가설과 들어맞는다
- 남은 불확실성: 로컬 `.env` 와 운영 `.env` 의 `VCP_GEMINI_MODEL`·Vertex 설정이 같은지는 확인하지 않았다. 운영에서 실제로 429·503 이 발생했는지는 원인 확정 1(운영 `backend.log`)이 있어야 확정된다
- 증거 원문: scratchpad `vcp041_probe.py`·`vcp041_probe_out.json`(세션 임시)
- 정리: 사본 트리 삭제 뒤 `ls` 로 없음 확인, `vcp041_probe.py` 프로세스 0. 원본 `data/signals_log.csv` 는 09-21 21:41 그대로다. 서버를 띄우지 않았으므로 리스너는 없다

## 운영자 확인 (2026-09-25 06:5x, 사용자가 운영 서버에서 실행한 결과를 전달)

- 명령: `grep -nE '사용 가능한 모델이 없습니다|세션에서 제외합니다|JSON 파싱 실패|분석 실패 \(Final\)' logs/backend.log | tail -20`
- 결과: 한 줄. `8977:2026-09-24 17:01:25,700 - engine.vcp_ai_analyzer - WARNING - [Gemini] SK아이이테크놀로지 gemini-3.8-flash 모델을 429/503 계열 오류로 세션에서 제외합니다.`
- 판정: **원인 확정.** 운영 17:00 실행에서 429/503 계열 오류로 모델 영구 제외가 실제로 발동했다. 운영 모델명은 `gemini-3.8-flash` 로 로컬과 다르다(남은 불확실성의 앞부분). 남은 단계는 배포·재기동과 첫 17:00 실행 뒤 확인이다

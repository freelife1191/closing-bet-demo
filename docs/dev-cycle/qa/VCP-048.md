# [VCP-048] GPT 클라이언트가 없으면 두 번째 자리를 비운다 — QA 기록

- 대상: `engine/vcp_ai_provider_init_helpers.py`(`resolve_effective_second_provider` 의 `gpt_ready`), `engine/vcp_ai_analyzer.py`(생성자가 `gpt_client is not None` 을 넘김), `services/kr_market_vcp_reanalysis_service.py`(Second 강제 503 안내문), `.env.example` 주석
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24 15:54
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 사본은 그 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 15:52), 호출 경로 추적(기동 → `VCPMultiAIAnalyzer.__init__` → `init_gpt_client`(키 없으면 None) → `resolve_effective_second_provider`, 재분석 → `execute_vcp_failed_ai_reanalysis` → `analyzer.second_provider` → 필수 칸 판정)
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 확정된 두 번째 자리는 화면에 표시되지 않고, 그 효과가 드러나는 관리자 「AI 재분석」 버튼은 금지 조작이다. 표의 두 번째 열은 수정 전에도 GPT 가 돌지 않아 비어 있었으므로 화면 차이가 없다
- 격리: 첫 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본의 `signals_log.csv`·`ai_analysis_results_20260505.json` 만 복사한다. 하네스는 `env -u OPENAI_API_KEY` 로 돌리며 사본에는 `.env` 가 없다. Gemini·Z.ai 초기화만 가짜로 막고 GPT 는 실제 `init_gpt_client` 를 탄다. LLM·네트워크 호출 없음. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 합성 자료(사본만): `seed` 가 05-05 행을 2026-09-22 로 복제하고(`ai_action=HOLD`, `is_vcp=True`) `ai_analysis_results_20260922.json` 을 Gemini 판정만 든 모양(GPT 칸 없음)으로 만든다. 키 없는 운영에서 생기는 캐시 모양이다
- 대조: 같은 하네스를 수정 전 커밋 `f9653bc` 사본에서 돌린다
- 하네스: 세션 스크래치의 `vcp048qa_harness.py`

## 시나리오

### S-1. OPENAI_API_KEY 가 없으면 두 번째 자리가 비고 원인이 경고로 남는다 (하네스, 필수)
- 조작: `VCP_AI_PROVIDERS=gemini,gpt`, `VCP_SECOND_PROVIDER=gpt`, 키 없음으로 분석기를 만든다
- 기대: 수정 뒤 `gpt_client=None`, `second_provider=None`, 경고에 「OPENAI_API_KEY가 설정되지 않아」와 「GPT 클라이언트를 만들지 못해(OPENAI_API_KEY·GPT 초기화 로그 확인)」. 수정 전 사본은 `second_provider='gpt'`
- 실제:

### S-2. 자동 재분석이 채워질 수 없는 GPT 칸 때문에 행을 다시 잡지 않는다 (하네스, 필수)
- 조작: S-1 설정으로 `execute_vcp_failed_ai_reanalysis(target_date="2026-09-22")` 자동 모드, `analyze_batch` 는 대상만 기록하는 가짜
- 기대: 수정 뒤 `status=200`, `failed_targets=0`, 「재분석이 필요한 실패/누락 항목이 없습니다」, `analyze_batch` 호출 없음, 캐시 파일 바이트 그대로. 수정 전 사본은 합성 네 종목이 GPT 누락으로 대상이 된다
- 실제:

### S-3. Second 강제 재분석은 원인을 알리는 503 을 돌려준다 (하네스, 필수)
- 조작: 같은 설정으로 `force_provider="second"`
- 기대: 수정 뒤 `status=503`, 안내문에 `OPENAI_API_KEY`, `analyze_batch` 호출 없음. 수정 전 사본은 스코프 네 종목을 대상으로 `analyze_batch` 를 부른다(두 번째 자리를 gpt 로 믿으므로)
- 실제:

## 정리
- 실제:

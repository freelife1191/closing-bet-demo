# [VCP-055] Toss 수급 조회가 200 빈 응답을 받으면 5일 순매수 0 으로 저장·캐시한다 — QA 기록

- 대상: `engine/toss_collector_metric_parsers.py` 의 `parse_investor_trend`(유효 행이 없으면 `None`)
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24 21:4x(실행 때 실측 시각으로 고친다)
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 대조는 수정 전 커밋 `c585b00`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 21:38), 호출 경로 추적(스케줄러·관리자 「Refresh VCP」 → `SmartMoneyScreener._calculate_supply_score` → `calculate_supply_score_with_toss` → `TossCollector.get_investor_trend` → `parse_investor_trend`. 폴백은 `_calculate_supply_score_csv` → `get_investor_trend_5day_for_ticker` 이고, CSV 에 이상징후가 있으면 pykrx·Toss 참조(`_fetch_toss_reference_trend`)를 조회한다). 다른 호출자는 `scripts/init_data.py:357` Toss 백필과 `engine/toss_collector.py:189` 종목 상세(둘 다 `or {}`), 수동 스크립트 `scripts/verify_toss_supply.py:24`(`if trend:`)이며 None 을 이미 처리한다
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 이 경로는 배치 스크리닝이며 화면 진입점인 「Refresh VCP」는 실제 수집과 LLM 호출로 이어지는 금지 조작이다. 결과는 스크리너 수급 결과(`foreign_5d`·`inst_5d` 등)와 캐시로 판정한다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본 `all_institutional_trend_data.csv`(마지막 날짜 2026-09-21) 한 파일만 복사한다. 하네스는 소켓 연결을 막고 `TossCollector._safe_request` 를 가짜로 바꿔 수급 URL 에 빈 body 또는 5행 정상 body 를 준다. LLM 키는 없다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp055qa_harness.py <empty|normal> <티커들>`. 티커는 CSV 에 있는 `005930,000660,950250`

## 시나리오

### S-1. Toss 빈 body 에서 스크리너가 0 대신 CSV 폴백 값을 쓴다 (하네스, 필수)
- 조작: 두 사본에서 `empty` 모드로 실행
- 기대: 수정 전은 세 종목 모두 `foreign_5d=0`·`inst_5d=0` 이다. 수정 뒤는 CSV 5일 합계에서 나온 값이거나, 폴백이 참조를 얻지 못했을 때 결측(`None`)이며 Toss 의 0 이 아니다. 수정 뒤 값은 원본 CSV 의 마지막 5거래일 합계와 대조한다
- 실제:

### S-2. 빈 응답이 15분 캐시와 Toss 참조 캐시에 남지 않는다 (하네스, 필수)
- 조작: S-1 과 같은 실행에서 메모리 캐시 항목 수, 메모리를 비운 뒤 SQLite 스냅샷 적중 여부, `_fetch_toss_reference_trend` 결과를 기록한다
- 기대: 수정 전은 메모리 3항목·SQLite 적중 `True`·Toss 참조가 0 값 dict 다. 수정 뒤는 메모리 0항목·SQLite 적중 `False`·Toss 참조 `None` 이다
- 실제:

### S-3. 정상 body 의 결과는 수정 전과 같다 (하네스, 필수, 회귀 확인)
- 조작: 두 사본에서 `normal` 모드로 실행하고 JSON 출력을 비교한다
- 기대: 두 출력의 `rows`·캐시·참조 값이 같다
- 실제:

## 정리
- 실제:

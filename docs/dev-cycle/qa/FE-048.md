# [FE-048] 상세 모달의 Toss 5일 순매수 결측과 부분 합계 — QA 시나리오

- 대상 화면: http://localhost:3748/dashboard/kr/closing-bet (격리 Next dev) → 카드의 「상세 분석 보기」 모달의 「투자자 동향」 절
- 구성 근거: 설계 승인(대화 2026-09-25 16:07, TODO [FE-048] 절). 호출 경로: 카드 클릭 → `fetch('/api/kr/stock-detail/<code>')`
  → Flask `fetch_stock_detail_payload` → `TossCollector.get_full_stock_detail` → `parse_investor_trend` → `build_toss_detail_payload`
  → `append_investor_trend_5day`(확정 집계. CSV 가 없으면 pykrx·Toss 참조를 조회하고, `_reference_reject_reason` 이 버리면 키를 넣지 않는다)
  → 모달 `mapTossDataToDetail` → `FlowAmount`. 모달이 Toss 합계로 물러서는 것은 확정 집계 키가 없을 때뿐이므로, 그 조건을 실제 판정 코드로 만든다
- 구성 2026-09-25(설계 승인 16:07 뒤, 첫 커밋 전) | 실행: 미실행
- 검증 기준 커밋: 첫 커밋(이 문서를 담은 커밋). 사본은 그 커밋의 `git archive` 다
- QA 엔진(engine): Claude Code, 브라우저는 gstack `browse`(`~/.claude/skills/gstack/browse/dist/browse`)
- browser_applicability: required. 사용자가 보는 것은 모달의 글자다. browser_driver: gstack `browse`
- 격리: 첫 커밋의 `git archive` 를 scratchpad `qa-fe048/` 에 풀고 `frontend/node_modules` 는 APFS clone. 사본에서 `secrets/`·`data/`·`.env*` 를 지운 뒤
  `data/` 를 새로 만들어 QA 고정 자료 `jongga_v2_latest.json`(신호일 2026-09-24, 다섯 종목) 하나만 둔다. 원본 3500/5501·live 주소·원본 `data/`·`.env` 는 쓰지 않는다
- 대역(QA 전용 진입점 `qa_fe048_app.py`, 사본에만 둔다): `TossCollector._safe_request` 를 URL 별 고정 응답으로 바꾼다(종목 정보·투자자 동향만 응답, 나머지 None).
  `services.investor_trend_5day_service._fetch_pykrx_reference_trend`·`_resolve_pykrx_latest_market_date` 를 None 으로 바꿔 KRX 에 닿지 않게 한다.
  loopback 이 아닌 소켓 연결은 모두 막는다. LLM·발송 경로는 부르지 않는다
- 기동: Flask `SCHEDULER_ENABLED=false gunicorn qa_fe048_app:app --bind 127.0.0.1:5748 --workers 1 --threads 4 --keep-alive 0`,
  Next `PORT=3748 API_URL=http://127.0.0.1:5748 TZ=Asia/Seoul npm run dev`(더미 `NEXTAUTH_SECRET`·`INTERNAL_IDENTITY_SECRET`·`NEXTAUTH_URL`)
- 단계(phase): 시나리오 구성 완료
- 반복(iteration): 0회
- baseline 상태: 기대값은 고정 자료에서 계산했다(아래 각 시나리오). 수정 전 동작은 vitest 회귀 테스트가 실패로 확인했다(`page.regression-fe-048.test.tsx` 3건 RED)
- 필수 여부(required): S-1~S-5 예. S-5 는 리뷰(closing-bet-reviewer, 2026-09-25 medium 지적 1)로 추가했다
- 결과: 미실행
- 증거: 미실행
- 정리(cleanup): 미실행
- 읽은 정본: `.claude/skills/closing-bet-nextjs/SKILL.md`, `.claude/skills/closing-bet-python/SKILL.md`, `.claude/skills/closing-bet-verify/SKILL.md`,
  `frontend/node_modules/next/dist/docs/01-app/01-getting-started/05-server-and-client-components.md`

고정 자료의 Toss 투자자 동향(종가·외국인·기관 수량, 다섯 행)과 판정 경로는 다음과 같다.

| 종목 | 행 | 파서 결과 | 확정 집계 | 모달 기대 |
|---|---|---|---|---|
| 900001 QA결측 | 5행 종가 1,000, 외국인·기관 수량 모두 빈 값 | 합계 둘 다 None, 일수 0·0 | Toss 참조 정규화 실패(None) → 키 없음 | 외국인 「자료 없음」, 기관 「자료 없음」 |
| 900002 QA부분 | 5행 종가 10,000, 외국인 [빈, 10,000, 20,000, 30,000, 40,000], 기관 모두 빈 값 | 외국인 1,000,000,000·4일, 기관 None·0일 | 기관 결측으로 정규화 실패 → 키 없음 | 외국인 「+10억(4일)」, 기관 「자료 없음」 |
| 900003 QA4일 | 가격 있는 4행 종가 10,000(외국인 5,000·기관 -2,500) + 종가 0 인 1행 | 외국인 200,000,000·4일, 기관 -100,000,000·4일 | 4행이라 `insufficient_days` 로 버림 → 키 없음 | 외국인 「+2억(4일)」, 기관 「-1억(4일)」 |
| 900004 QA정상 | 5행 종가 10,000, 외국인 10,000·기관 -10,000 | 외국인 500,000,000·5일, 기관 -500,000,000·5일 | Toss 참조 채택 → 키 있음(5억·-5억) | 외국인 「+5억」, 기관 「-5억」, 일수 표시 없음 |
| 900005 QA실패 | 추세 요청 실패(대역이 None) | 파서 None → 수집기 `{}` | Toss 참조도 None → 키 없음 | 외국인 「자료 없음」, 기관 「자료 없음」 |

## 시나리오

### S-1. 확정 집계도 Toss 합계도 없으면 「자료 없음」을 그린다 (회귀)
- 조작: `curl` 로 `/api/kr/stock-detail/900001` 을 받아 `investorTrend`·`investorTrend5Day` 를 읽는다. 브라우저에서 QA결측 카드의 「상세 분석 보기」를 눌러 외국인·기관 칸의 글자를 읽는다.
- 기대: API 는 `investorTrend.foreign`·`institution` 이 null, `foreignDays`·`institutionDays` 가 0, `investorTrend5Day` 키 없음. 화면은 외국인·기관 모두 「자료 없음」이고 「-」나 「0」이 아니다.
- 필수 여부(required): 예
- 실제: 미실행
- 결과: 미실행

### S-2. 한쪽만 결측이면 다른 쪽은 부분 일수를 붙여 그린다 (회귀)
- 조작: 900002 에 같은 조작을 한다.
- 기대: API 는 `foreign` 1000000000·`foreignDays` 4, `institution` null·`institutionDays` 0, `investorTrend5Day` 키 없음. 화면은 외국인 「+10억(4일)」, 기관 「자료 없음」.
- 필수 여부(required): 예
- 실제: 미실행
- 결과: 미실행

### S-3. 가격 있는 날이 4일이면 두 칸 모두 「(4일)」을 붙인다 (회귀)
- 조작: 900003 에 같은 조작을 한다.
- 기대: API 는 `foreign` 200000000·`foreignDays` 4, `institution` -100000000·`institutionDays` 4, `investorTrend5Day` 키 없음. 화면은 외국인 「+2억(4일)」, 기관 「-1억(4일)」.
- 필수 여부(required): 예
- 실제: 미실행
- 결과: 미실행

### S-4. 확정 집계가 있으면 일수를 붙이지 않고 기존처럼 그린다 (인접)
- 조작: 900004 에 같은 조작을 한다. 다섯 모달을 연 뒤 `console --errors` 와 `/_next/mcp` `get_errors` 를 읽는다.
- 기대: API 는 `investorTrend5Day` 가 `{foreign: 500000000, institution: -500000000}`, `foreignDays`·`institutionDays` 5. 화면은 외국인 「+5억」, 기관 「-5억」이고 「일)」 글자가 없다. 이번 변경에서 나온 콘솔·런타임 오류 없음.
- 필수 여부(required): 예
- 실제: 미실행
- 결과: 미실행

### S-5. Toss 추세 요청이 실패하면 0 이 아니라 「자료 없음」을 그린다 (회귀)
- 조작: 900005 에 같은 조작을 한다.
- 기대: API 는 `foreign`·`institution` 이 null, `foreignDays`·`institutionDays` 가 null, `investorTrend5Day` 키 없음. 화면은 외국인·기관 모두 「자료 없음」.
- 필수 여부(required): 예
- 실제: 미실행
- 결과: 미실행

## 실행 결과

- 미실행

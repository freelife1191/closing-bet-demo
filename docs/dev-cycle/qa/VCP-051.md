# [VCP-051] 시그널 가격 갱신이 청산(CLOSED) 행의 수익률을 덮는 문제 — QA 기록

- 대상: `scripts/init_data.py` 의 `update_vcp_signals_recent_price` 적용 루프(`status == 'CLOSED'` 행 건너뜀)
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-25 09:13 | 실행 2026-09-25 09:14:03~09:14:04
- 검증 기준 커밋: `4779f00e`(이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `648864cc`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 09:10), 호출 경로(스케줄러·수동 VCP 갱신 → `services/kr_market_vcp_background_service.py:90` → `init_data.update_vcp_signals_recent_price` → `data/signals_log.csv`)
- QA 엔진(engine): Claude Code. 이 함수의 결과를 직접 보이는 화면이 없고(청산 행은 VCP 화면에서 빠진다, `[VCP-034]`) 결과는 CSV 값이므로 browser_applicability: not_applicable. CLI 하네스로 실제 함수를 실행한다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지운다. 하네스(스크래치 `v051_harness.py`)가 사본 `data/signals_log.csv` 를 새로 만들고 `pykrx` 를 가짜 모듈로 바꿔 시세를 넣는다(005930=110, 000660=180, 035420=55). 네트워크·LLM·원본 `data/` 없음
- 입력 행: 005930 CLOSED(현재가 93, 수익률 -7.0, 청산가 93, 청산일 09-10) · 005930 OPEN(진입 100) · 000660 CLOSED(현재가 230, 수익률 15.0) · 035420 status 빈 칸(진입 50)
- 금지 조작: Refresh VCP, AI 재분석, 챗봇 전송, 모의 매수, 설정 저장
- 필수 여부(required): S-1·S-2 예
- 반복(iteration): 1회
- 결과: 통과 (필수 2/2)
- 증거: 아래 「실제」 줄. 사본 `scripts/init_data.py` 의 `VCP-051` 표시는 수정 전 0건·수정 뒤 1건으로 두 사본이 서로 다른 커밋임을 확인. 두 실행 모두 exit 0

## 시나리오

### S-1. 청산 행의 현재가·수익률·청산가·청산일은 그대로다 (필수, CLI)
- 조작: 두 사본 루트에서 `venv/bin/python <스크래치>/v051_harness.py` 를 실행하고 출력 표를 비교한다
- 기대: 수정 뒤 005930 CLOSED `93 / -7.0 / 93 / 2026-09-10`, 000660 CLOSED `230 / 15.0`. 수정 전은 두 행이 `110 / 10.0`, `180 / -10.0` 으로 덮임(결함 재현)
- 실제: 수정 뒤(`q051new`, 09:14:04) 005930 CLOSED `93.0 / -7.0 / 93.0 / 2026-09-10`, 000660 CLOSED `230.0 / 15.0 / 230.0 / 2026-09-12`. 수정 전(`q051old`, 09:14:03) 005930 CLOSED `110.0 / 10.0`, 000660 CLOSED `180.0 / -10.0`(청산가·청산일은 남고 현재가·수익률만 덮임, 결함 재현). 통과

### S-2. OPEN 행과 status 빈 행은 종전대로 갱신된다 (필수, CLI, 회귀)
- 조작: S-1 과 같은 출력
- 기대: 두 사본 모두 005930 OPEN `110 / 10.0`, 035420 빈 status `55 / 10.0`. 로그의 갱신 건수는 수정 뒤 2건, 수정 전 4건
- 실제: 두 사본 모두 005930 OPEN `110.0 / 10.0`, 035420(status NaN) `55.0 / 10.0`. 로그 「VCP 시그널 가격 업데이트 완료」 수정 전 4건·수정 뒤 2건, 두 사본 모두 「3개 종목 현재가 확보」. 통과

## 정리
- 실제: 사본 둘(`q051old`·`q051new`) 리터럴 경로로 삭제 뒤 없음 확인, 사본 경로 프로세스 0, 서버 기동 없음. 원본 `data/signals_log.csv`(1789994506)·`kr_ai_analysis.json`(1788316016) 수정 시각 그대로. 네트워크·LLM 없음(pykrx 가짜 모듈, 폴백 미도달). 결과: 필수 2/2 통과

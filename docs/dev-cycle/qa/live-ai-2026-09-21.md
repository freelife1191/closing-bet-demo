# 실제 신규 AI 분석 실행 결과

- 사용자 요청: 신규 AI 분석 실행. 기존 서비스 적용 후 실제 외부 API 호출 승인.
- 실행 기준: 42ee9ae. 제품 소스 변경 없음.
- 실행: 기존 venv의 engine.generator.run_screener(), 기본 전체 KOSPI/KOSDAQ. 웹 wrapper의 메신저 발송은 호출하지 않았다. 인증 헤더를 위조하거나 관리자 검증을 우회하지 않았다.
- 결과: 95.6초, 시세 후보424개(KOSPI174/KOSDAQ250), 1차 통과12개(KOSPI9/KOSDAQ3), 뉴스 수집0, 최종 시그널0.
- 프로세스 exit0이나 전체 분석은 실패다. 시장별 예외를 상위가 삼킨 뒤 빈 결과를 저장했으므로 성공으로 판정하지 않는다.
- KRX 로그인에 필요한 KRX_ID/KRX_PW 미설정 메시지를 확인했다. pykrx 응답 해석/로깅 오류도 발생했다. 토스 후보 수집은 성공했다. 뉴스0의 세부 원인은 미확정이며 KRX 자격 증명 부재와 같은 원인이라고 단정하지 않는다.
- 엔진 실행이 최신 리포트를 빈 결과로 교체해, 실행 전 logs/live-ai-backup-20260921 백업과 이번 실행의 date/signals를 대조한 뒤 기존 latest를 복구했다. 이번 실행에서 생성한 daily 빈 결과는 logs 백업으로 옮겨 보존하고 사용자 데이터에서는 제거했다. 스케줄러/실행 상태는 IDLE 확인.
- 실제 LLM 별도 검사: 기존2026-09-09 자료 중 SK하이닉스1건을 기존 analyze_news_batch_jongga로 Gemini(Vertex) gemini-3.7-flash에 전달해 분석1건·비어 있지 않은 근거를 받았다(exit0). 모의 응답이 아니다. 오늘 시장 분석으로 표시하지 않았으며 리포트에 덮어쓰지 않았다. 응답 원문은 logs/live-ai-reanalysis-result-20260921.json에 보관했다.
- 서비스 latest API: date2026-09-09, signals18개. ego-browser space27로 기존 날짜 안내와 SK하이닉스 카드 복구 확인, finish keep[]1회 완료.
- 데이터 수집 캐시·Market Gate 자료는 엔진 정상 경로로 갱신될 수 있다. 전체 data 무변경을 주장하지 않는다. 시크릿 설정 변경, 수동 매매, 알림 발송은 하지 않았다.
- 증거: ../evidence/live-ai-20260921/result.json, actual-ai-call.json, browser-restored.json. 원시 실행 로그는 시크릿 노출 방지를 위해 Git 증거에 복사하지 않고 logs/live-ai-20260921.log 및 logs/live-ai-probe-20260921.log에 유지.
- 임시 실행 스크립트2개 제거. 기존 서비스3500/5501 유지.

판정: 실제 AI 연결/응답 검사는 PASS. 오늘 신규 전체 분석은 BLOCKED(뉴스 수집0). INFRA-071로 원인 해결과 실패 시 기존 결과 보존 검증을 추적한다.

# [VCP-057] VCP 수급 결측의 남은 0 기본값과 실제 0 의 색 표기 — QA 기록

- 대상: `frontend/src/app/dashboard/kr/vcp/page.tsx` 의 `supplyColorClass`(실제 0 을 `text-gray-300`), `engine/screener_scoring_helpers.py` 의 `_score_supply_core`(1일 값 결측을 None), `engine/screener.py` 의 미사용 `ScreenerResult` 삭제, 수동 스크립트 `tests/test_vcp.py`·`scripts/diagnose_screener.py` 의 None 출력
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-25 08:43 | 실행 2026-09-25 08:49:22~08:49:44
- 검증 기준 커밋: `72e688af`(이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `20ca79b8`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 08:40), 호출 경로(VCP 화면 `/dashboard/kr/vcp` → `krAPI.getSignals` → `/api/kr/signals` → `signals_log.csv` 의 `foreign_5d`·`inst_5d` → 표 수급 열의 `supplyColorClass`), `[VCP-054]` QA 의 격리 구성
- QA 엔진(engine): Claude Code. 결과가 VCP 화면의 표에 보이므로 browser_applicability: required(browser_driver: agent-browser). (2)는 현재 두 details 생성 경로가 키를 항상 채워 화면으로 도달할 수 없으므로 pytest 로만 검증하고, (1)·(3)은 삭제와 수동 스크립트 출력이라 동적 QA 대상이 아니다(정적 검증과 값별 서식 확인으로 대신함)
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본 `signals_log.csv`·`korean_stocks_list.csv`·`daily_prices.csv` 를 복사한다(원본은 읽기만). 사본의 `signals_log.csv` 만 고친다: 15행 모두 `is_vcp=True`, SK(034730)는 `foreign_5d` 빈 칸·`inst_5d` 0, LG(003550)는 `foreign_5d` 음수·`inst_5d` 빈 칸, 나머지는 그대로. LLM 키는 넣지 않고 인증 값은 가짜다. gunicorn 은 `SCHEDULER_ENABLED=false` 로 loopback 임시 포트에, Next dev 는 `API_URL` 로 사본 Flask 를 가리켜 다른 임시 포트에 띄운다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 금지 조작: Refresh VCP, AI 재분석, 챗봇 전송, 모의 매수, 설정 저장
- 필수 여부(required): S-1·S-2 예
- 반복(iteration): 1회
- 결과: 통과 (필수 2/2)
- 증거: 아래 「실제」 줄. 표 칸은 agent-browser `eval` 로 각 행 수급 칸의 글자·색 클래스·화살표 클래스를 JSON 으로 모아(스크래치 `v057_cells_<포트>.json`) 스크립트로 비교했다. 스크린샷 `vcp057qa-58572-table.png`(수정 전)·`vcp057qa-58574-table.png`(수정 뒤). 사본 `page.tsx` 의 `VCP-057` 표시는 수정 전 0건·수정 뒤 1건으로 두 사본이 서로 다른 커밋임을 확인. 사본 Flask(`58573`) `GET /api/kr/signals` 08:49:22 응답 15행, SK `foreign_5d=None`·`inst_5d=0.0`, LG `-3500000000.0`·`None`

## 시나리오

### S-1. 실제 0 은 중립색, 결측은 회색 `-`, 매도는 빨강이다 (필수, 브라우저)
- 조작: 두 사본의 `http://localhost:<port>/dashboard/kr/vcp` 를 넓은 뷰포트로 열고 SK·LG 행의 수급 칸 클래스와 글자를 읽는다
- 기대: 수정 뒤 SK 기관 칸 `0`·`text-gray-300`·화살표 없음, SK 외국인 칸 `-`·`text-gray-500`, LG 외국인 칸 음수 금액·`text-red-400`·아래 화살표, LG 기관 칸 `-`·`text-gray-500`. 수정 전 SK 기관 칸은 `0`·`text-red-400`(결함 재현). 콘솔 오류 없음
- 실제: 뷰포트 2000×900, 「최신 저장분(2026-05-05)」 안내와 15행. 수정 뒤(Next dev `58574`, 08:49:44): SK 외국인 `-`·`text-gray-500`·화살표 없음, SK 기관 `0`·`text-gray-300`·화살표 없음, LG 외국인 `-35억`·`text-red-400`·`fa-arrow-down`, LG 기관 `-`·`text-gray-500`. 수정 전(`58572`, 08:49:37): SK 기관 `0`·`text-red-400`(결함 재현), 나머지 SK·LG 칸은 수정 뒤와 같음. 두 화면 `errors` 출력 없음. 스크린샷에서 SK 기관 `0` 이 빨간 매도 금액과 다른 밝은 회색임을 눈으로 확인. 통과

### S-2. 다른 행의 표기는 수정 전과 같다 (필수, 브라우저, 회귀)
- 조작: S-1 과 같은 화면에서 SK·LG 를 뺀 행의 수급 칸 글자·클래스를 두 사본에서 비교한다
- 기대: 두 사본이 같다
- 실제: 15행 모두 두 사본에 있고(종목 집합 같음) 칸별 비교에서 다른 곳은 SK 기관 칸 하나뿐이다(`diff` 1건). 매수(초록·위 화살표)·매도(빨강·아래 화살표, 예: 휴젤 기관 `-130억`) 표기는 두 사본이 같다. 통과

## 정리
- 실제: agent-browser 세션 둘 종료, 사본 gunicorn 둘(58571·58573)과 Next dev 둘(58572·58574)을 TERM 으로 내림(첫 `kill` 은 zsh 가 PID 목록을 나누지 않아 실패해 `xargs` 와 사본 경로 `pkill` 로 다시 함). 58571~58574 리스너 0, 사본 경로 프로세스 0. 사본 둘(`q057old`·`q057new`) 리터럴 경로로 삭제 뒤 없음 확인. 원본 `data/signals_log.csv`(1789994506)·`daily_prices.csv`(1789994444)·`korean_stocks_list.csv`(1770785642) 수정 시각 그대로(읽기 복사만), 3500·5501 리스너 0. 두 gunicorn 로그 Traceback 0. LLM 키 없음, Refresh·재분석·챗봇 전송·모의 매수 없음. 결과: 필수 2/2 통과

# [INFRA-095] 수급 수집이 결측을 0 으로 저장하지 않는다 — QA 기록

- 대상: `scripts/init_data.py` 의 `create_institutional_trend`(pykrx 두 프레임 교집합, NaN 값 종목 건너뜀, 한쪽 프레임만 비면 WARNING)와 `_collect_toss_trend_rows_for_ticker`(빈 종가·수량 행 버림, 실제 0 저장)
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-25 08:10 | 실행 2026-09-25 08:15:24~08:15:34
- 검증 기준 커밋: `47e61791`(이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `67bda5e9`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 07:56), 호출 경로(스케줄러 17:00 체인과 관리자 수동 업데이트의 「Institutional Trend」 단계 → `create_institutional_trend` → pykrx 실패·최신일 누락 시 `_backfill_institutional_trend_from_toss`), 계획의 Review Focus
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 화면 진입점(데이터 상태 화면의 업데이트)은 실제 KRX·Toss 수집과 LLM 분석을 일으키는 금지 조작이고, 결함은 외부 응답의 결측 모양에서만 드러나 실제 출처로는 재현을 고를 수 없다. 읽는 쪽은 바꾸지 않았고 파일 계약(모든 값이 숫자)도 그대로다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고 빈 `data/` 를 만든다. 하네스는 사본 경로(`/scratchpad/`)가 아니면 실행을 거부하고 소켓 연결을 막는다. 가짜는 pykrx 의 `get_market_net_purchases_of_equities_by_ticker` 와 Toss 의 `get_investor_trend` 둘뿐이며 나머지는 사본의 실제 코드다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `infra095qa_harness.py <모드>`. 매 실행 때 하네스가 사본 `data/` 의 종목 목록(000001·000002)과 수급 파일(09-21 행 3개, 000001·000002·069500)을 새로 쓴다(다른 파일은 두 파일을 읽지 않음). 기준일은 09-22
- 필수 여부(required): S-1~S-5 모두 예
- 반복(iteration): 1회
- 결과: 통과 (필수 5/5)
- 증거: 아래 「실제」 줄(하네스 출력 RESULT 원문의 요약, 스크래치 `qa095_<사본>_<모드>.out`). 사본의 `[INFRA-095]` 표시는 수정 전 0건·수정 뒤 2건으로 두 사본이 서로 다른 커밋임을 확인했다. 10회 실행 모두 exit 0·Traceback 없음

## 시나리오

### S-1. 한쪽 프레임에만 있는 종목은 저장하지 않는다 (하네스, 필수)
- 조작: 두 사본에서 `one_side`. 외국인 프레임에 000001·000002·069500, 기관 프레임에 000001·069500
- 기대: 수정 전은 09-22 에 000002 가 `inst_buy` 0 으로 저장된다(결함 재현). 수정 뒤는 09-22 행이 000001(30, 60)·069500(50, 70) 둘이고 000002 가 없다, 반환 `True`
- 실제: 08:15 실행. 수정 전: 09-22 행 000001(30, 60)·000002(40, 0)·069500(50, 70), 반환 `True`, 경고 없음. 000002 의 기관 결측이 0 으로 저장됨(결함 재현). 수정 뒤: 09-22 행 000001(30, 60)·069500(50, 70), 000002 없음, 반환 `True`, 경고 없음. 통과

### S-2. Toss 의 빈 필드는 버리고 실제 0 은 저장한다 (하네스, 필수)
- 조작: 두 사본에서 `toss_blank`. pykrx 가 비어 Toss 백필로 간다. 000001 은 외국인 수량 0(실제 0), 000002 는 종가 `None`, 069500 은 외국인 수량 빈 문자열
- 기대: 수정 전은 000002 가 (0, 0), 069500 이 (0, 2000) 으로 저장된다(결함 재현). 수정 뒤는 09-22 행이 000001(0, 3000) 하나다, 반환 `True`(최신일이 채워짐)
- 실제: 08:15 실행. 수정 전: 09-22 행 000001(0, 3000)·000002(0, 0)·069500(0, 2000), 백필 「총 6행」, 반환 `True`(결함 재현). 수정 뒤: 09-22 행 000001(0, 3000) 하나, 백필 「총 4행」, 반환 `True`. 실제 0 수량(000001 외국인)은 저장됨. 통과

### S-3. 한쪽 조회가 통째로 비면 저장하지 않고 경고한다 (하네스, 필수)
- 조작: 두 사본에서 `empty_side`. 기관 프레임이 빈 `DataFrame`
- 기대: 수정 전은 세 종목이 모두 `inst_buy` 0 으로 저장된다(결함 재현). 수정 뒤는 09-22 행이 없고, 경고에 「한쪽 투자자 프레임만 비어 저장 생략」과 stale ERROR 가 있고, 반환 `False`
- 실제: 08:15 실행. 수정 전: 09-22 행 세 종목이 모두 `inst_buy` 0(30, 0)·(40, 0)·(50, 0), 반환 `True`, 경고 없음(결함 재현). 수정 뒤: 09-22 행 없음, 경고 「[Supply Trend] 2026-09-22 한쪽 투자자 프레임만 비어 저장 생략」과 「수급 데이터 stale 감지: latest=2026-09-21, expected>=2026-09-22」, 반환 `False`. 통과

### S-4. 결측이 없으면 종전과 같다, 실제 0 포함 (하네스, 필수, 회귀)
- 조작: 두 사본에서 `normal`. 두 프레임이 세 종목을 모두 주고 000002 외국인·069500 기관이 실제 0
- 기대: 두 사본의 결과가 같다. 09-22 행 000001(30, 60)·000002(0, 70)·069500(50, 0), 반환 `True`, 경고 없음
- 실제: 08:15 실행. 두 사본 모두 09-22 행 000001(30, 60)·000002(0, 70)·069500(50, 0), 반환 `True`, 경고 없음. 결과 동일. 통과

### S-5. 휴장일은 종전처럼 끝난다 (하네스, 필수, 회귀)
- 조작: 두 사본에서 `holiday`. 두 프레임이 모두 빈 `DataFrame`
- 기대: 두 사본의 결과가 같다. 09-22 행 없음, 「한쪽 투자자 프레임」 경고 없음, stale ERROR 와 반환 `False`(Toss 도 빈 응답)
- 실제: 08:15 실행. 두 사본 모두 09-22 행 없음, 「한쪽 투자자 프레임」 경고 없음, stale ERROR 1줄, 반환 `False`. 결과 동일. 통과

## 정리
- 실제: 사본 둘(`q095old`·`q095new`)과 변이·리뷰용 스크래치 사본 `m095`·`i095`(모두 `.env`·`secrets/` 없음, `data/` 에는 하네스·테스트가 만든 파일뿐)를 리터럴 경로로 지우고 없음 확인. 하네스 프로세스 0, 서버를 띄우지 않았고 3500·5501 리스너 0. 원본 `data/all_institutional_trend_data.csv` 수정 시각(09-21 21:40) 그대로, 원본 `data/` 에 `all_institutional_trend_data.csv.lock` 없음. 소켓 차단으로 네트워크 없음, KRX 로그인·Toss 실조회·LLM 호출 없음. 결과: 필수 5/5 통과

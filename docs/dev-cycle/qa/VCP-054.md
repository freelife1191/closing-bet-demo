# [VCP-054] VCP 화면·챗봇 데이터가 빈 수급을 0 으로 읽는다 — QA 기록

- 대상: `app/routes/kr_market_vcp_signal_helpers.py` 의 `_build_vcp_signal_from_row`(`foreign_5d`·`inst_5d` 를 `_safe_optional_float` 로), `services/kr_market_vcp_signals_cache.py` 의 캐시 스키마 버전(5→6, `closing-bet-reviewer` medium 반영), `frontend/src/lib/api.ts` 의 `KRSignal`(`number | null`), `frontend/src/app/dashboard/kr/vcp/page.tsx` 의 표 수급 열과 상세 문구
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24 21:10
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 대조는 수정 전 커밋 `75d892f`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 21:03), 호출 경로 추적(VCP 화면 `/dashboard/kr/vcp` → `krAPI.getSignals` → `/api/kr/signals` → `services/kr_market_vcp_payload_service.py` 가 `signals_log.csv` 를 읽음 → `_build_vcp_signals_from_dataframe` → `_build_vcp_signal_from_row`)
- QA 엔진(engine): Claude Code. 결과가 VCP 화면의 표와 상세 패널에 보이므로 browser_applicability: required(browser_driver: agent-browser)
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본 `signals_log.csv`(2026-05-05 15행)·`korean_stocks_list.csv`·`daily_prices.csv` 를 복사한다. 사본의 `signals_log.csv` 만 고친다: SK(034730)는 `foreign_5d` 빈 칸·`inst_5d` 0, LG(003550)는 두 값 모두 빈 칸이고 나머지 13행은 그대로 둔다. LLM 키는 넣지 않는다. gunicorn 은 `SCHEDULER_ENABLED=false` 로 loopback 임시 포트에, Next dev 는 `API_URL` 로 사본 Flask 를 가리켜 다른 임시 포트에 띄운다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 금지 조작: Refresh VCP, AI 재분석, 챗봇 전송, 모의 매수, 설정 저장. 행 클릭(상세 패널 열기)은 조회만 한다

## 시나리오

### S-1. API 가 결측은 null, 실제 0 은 0 으로 준다 (필수)
- 조작: 사본 Flask 에 `GET /api/kr/signals`
- 기대: 수정 뒤 SK `foreign_5d=null`·`inst_5d=0`, LG 두 값 `null`, 나머지 13행의 두 값은 원본 CSV 와 같은 수. 수정 전 사본은 SK·LG 의 빈 칸이 `0` 이다
- 실제:

### S-2. 표가 결측을 회색 `-` 로 그린다 (필수, 브라우저)
- 조작: `http://localhost:<port>/dashboard/kr/vcp` 를 넓은 뷰포트로 연다
- 기대: SK 외국인 칸은 회색 `-` 이고 화살표가 없으며, 기관 칸은 `0` 이다(실제 0 의 기존 표기 유지). LG 두 칸은 회색 `-` 이다. 다른 행(예: 삼성전자)은 금액·색·화살표가 수정 전 사본과 같다. 콘솔 오류가 없다
- 실제:

### S-3. 상세 문구가 결측에 `원` 을 붙이지 않는다 (필수, 브라우저)
- 조작: SK 행과 삼성전자 행을 차례로 눌러 상세 패널을 연다
- 기대: SK 는 「외국인 5일 순매수 - · 기관 5일 순매수 0원」, 삼성전자는 두 값 모두 금액 뒤에 「원」이 붙는다. 수정 전 사본은 SK 에서 「0원」 두 개를 표시한다
- 실제:

### S-4. 수정 전 서버가 남긴 캐시가 있어도 수정 뒤 서버가 null 을 준다 (필수)
- 조작: 두 사본의 `signals_log.csv` 를 수정 시각까지 같게(`cp -p`) 두고, 수정 전 사본에서 S-1 을 호출해 생긴 `data/runtime_cache.db` 를 수정 뒤 사본 `data/` 에 복사한 다음 수정 뒤 서버를 띄워 `GET /api/kr/signals` 를 호출한다
- 기대: 수정 뒤 응답의 SK `foreign_5d=null`, LG 두 값 `null` 이다(버전 6 서명이 달라 옛 payload 를 쓰지 않음). 스키마 버전만 5 로 되돌린 대조 실행에서는 같은 캐시에서 SK·LG 가 `0` 으로 나온다
- 실제:

## 정리
- 실제:

# [VCP-058] `vcp_signals_latest.json` 원자적 저장 — QA 기록

- 대상: `scripts/init_data.py` 의 `_write_vcp_signals_latest_payload`(`atomic_write_text` 로 교체)와 `create_signals_log` 바깥 except 갈래의 최신 파일 재저장(`OSError` 보호 뒤 `False`)
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-25(설계 승인 13:00·범위 추가 13:02 뒤, 실행 전, 분 단위 시각은 기록하지 않음)
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 대조는 수정 전 커밋 `6028c098`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 13:00), 범위 추가(대화 13:02). 이 파일은 VCP 스크리너 배치(`create_signals_log`, 스케줄러와 관리자 VCP 실행이 부름)가 쓰고, `_count_latest_signals`(백그라운드 VCP 완료 메시지)와 `_extract_json_data_metadata`(데이터 상태 화면의 VCP 날짜)가 읽는다
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 바뀐 것은 배치의 파일 저장 방식과 실패 반환값뿐이고 화면 코드와 API 응답 형식은 바뀌지 않는다. 실제 VCP 실행은 외부 조회를 부르므로 스크리너를 가짜로 바꾼다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고 빈 `data/` 를 만든다. 하네스는 소켓 연결을 막고 사본의 `init_data`·읽는 쪽 함수를 직접 부른다. 서버와 포트는 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp058qa_harness.py <사본>`(사본 경로가 scratchpad 가 아니면 거부)
- 필수 여부(required): S-1~S-4 예
- 반복(iteration): (실행 뒤 기록)
- 결과: (실행 뒤 기록)
- 증거: (실행 뒤 기록)

## 시나리오

### S-1. 저장이 도중에 실패해도 기존 파일이 남는다 (하네스, 필수)
- 조작: 시그널 1건짜리 기존 최신 파일을 두고, 직렬화할 수 없는 값이 두 번째 시그널에 든 저장을 부른다
- 기대: 수정 뒤 예외 `TypeError`, 파일 바이트 불변, `_count_latest_signals` 1, 임시 파일 0. 수정 전은 파일이 잘려 읽는 쪽이 0 을 받는다(결함 재현). 저장 함수 하나의 보존이다. `create_signals_log` 전체에서는 바깥 except 갈래가 `[VCP-028]` 설계대로 빈 결과를 다시 쓰므로 「이전 결과가 계속 보인다」의 증거가 아니다(심층 리뷰 Minor 1)
- 실제: (실행 뒤 기록)

### S-2. 쓰는 동안 읽어도 빈 파일이나 잘린 JSON 을 보지 않는다 (하네스, 필수)
- 조작: 시그널 3,000건 파일을 150번 다시 쓰는 동안 다른 스레드가 두 읽는 함수를 계속 부른다
- 기대: 수정 뒤 시그널 수가 3,000 이 아닌 읽기 0, 날짜를 못 읽은 경우 0, 임시 파일 0. 수정 전은 잘못 읽은 횟수가 0 보다 크다(타이밍에 따라 달라지므로 수정 전 수치는 참고)
- 실제: (실행 뒤 기록)

### S-3. 정상 저장의 내용은 수정 전후가 같다 (하네스, 필수, 회귀)
- 조작: numpy 정수·실수·불리언, `date`, `None` 이 든 시그널을 저장하고 `generated_at` 을 뺀 내용과 두 읽는 함수의 결과를 비교한다
- 기대: 내용·시그널 수·날짜가 수정 전후 같다. 파일 권한은 수정 전 umask 기준, 수정 뒤 0600(설계에서 수용한 부수 효과)
- 실제: (실행 뒤 기록)

### S-4. 분석 실패 갈래에서 최신 파일 저장까지 실패해도 `False` 를 돌려준다 (하네스, 필수)
- 조작: (a) 스크리너가 예외를 내고 최신 파일 자리가 디렉터리라 저장이 `OSError` 로 실패하게 한다. (b) 스크리너만 예외를 내고 저장은 정상이다
- 기대: (a) 수정 뒤 `False`, `signals_log.csv` 바이트 불변, 임시 파일 0. 수정 전은 예외가 함수 밖으로 나온다(결함 재현). (b) 수정 전후 모두 `False`, 최신 파일은 날짜 2026-09-25·시그널 `[]`, 로그 불변
- 실제: (실행 뒤 기록)

## 정리
- 실제: (실행 뒤 기록)

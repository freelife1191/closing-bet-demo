# [VCP-040] VCP AI 판정 병합이 Gemini 추천만 보고 다른 프로바이더의 성공 결과를 버린다 — QA 기록

- 대상: VCP 수집 병합(`create_signals_log`), 자동 실패 재분석 라우트(`POST /api/kr/signals/reanalyze-failed-ai`), 화면 API(`GET /api/kr/signals`·`/api/kr/ai-analysis`), VCP 화면(`/dashboard/kr/vcp`)의 GPT·Gemini 열
- 구성 근거: 설계 승인(08:13)·범위 추가 승인(08:22), critic 계획 검토 REVISE, closing-bet-reviewer 지적 1(강제 Second 뒤 라벨), critic 심층 리뷰 유보(캐시 미등재 종목, `[VCP-044]` 이월)
- 구성 2026-09-24 | 실행: 아래 「실행 결과」
- 검증 기준 커밋: 첫 커밋(실행 전에 해시를 적는다). 사본은 그 커밋의 `git archive HEAD` 다
- QA 엔진(engine): Claude Code. 사용자 진입 흐름이 VCP 화면이므로 브라우저 실측(gstack `browse`)을 필수로 둔다. browser_applicability: required. 하네스:
  첫 커밋 뒤 `git archive HEAD` 로 scratchpad 에 사본을 만들고 `secrets/` 를 지운다. `data/` 에는 공개 시장 자료 세 파일만 복사한다. 사본 `.env` 에는 `FLASK_PORT=58171`, `FRONTEND_PORT=58172` 두 줄만 둔다. `venv`·`frontend/node_modules` 는 `cp -c -R` 로 clone 한다. LLM 키는 환경에 넣지 않는다.
  자료 적재: 사본 cwd 에서 `create_signals_log("2026-09-21", run_ai=True, signal_limit=3)` 을 가짜 분석기로 돌린다. 가짜 분석기는 원인 확정 실행 1 의 **실제 응답**을 돌려주되, 033530 은 gemini 를 None 으로 바꾸고, 108230 은 그대로 둔다(gemini HOLD 65, gpt HOLD 68). 세 번째 종목은 세 칸 모두 None 으로 준다.
  backend 는 `env -i` 로 `SCHEDULER_ENABLED=false`, 더미 `INTERNAL_IDENTITY_SECRET`·`ADMIN_EMAILS` 만 주고 `venv/bin/gunicorn flask_app:app --bind 127.0.0.1:58171 --workers 2 --threads 8 --timeout 120 --keep-alive 0` 으로 띄운다. Next 는 `PORT=58172 API_URL=http://127.0.0.1:58171 npm run dev` 로 띄운다.
  재분석은 별도 프로세스에서 Flask test client 로 관리자 라우트를 부르고(더미 비밀로 서명), 분석기는 가짜로 끼운다(세 번째 종목: gemini None·gpt 유효 / 033530: Gemini 재시도 실패).
  원본 `data/`·`logs/`·3500·5501·운영 주소는 건드리지 않고, LLM·발송은 하지 않는다
- 기대값 출처: 원인 확정 실행 1 의 실제 응답(`vcp040-raw.json`), 승인 범위, `page.tsx:1779-1782`(GPT·Gemini 열)
- 단계(phase): 원인 확정 완료 | 시나리오 구성 완료 | 실행 전
- 필수 여부(required): 예

## 원인 확정

- 승인: 2026-09-24 사용자 선택 「수집+재분석 모두」(격리 사본, 시그널 상한 2, 실제 LLM 호출 약 4~8회 허용)
- 기준 커밋: `8b6ce18`. 사본은 `git archive HEAD` 를 scratchpad 에 풀고 `secrets/` 를 지웠다. `data/` 에는 공개 시장 자료 세 파일(`korean_stocks_list.csv`, `daily_prices.csv`, `all_institutional_trend_data.csv`, 원본 최신일 2026-09-21)만 복사했다. 사본 `.env` 에는 포트 두 줄(58161·58162)만 두었다. 원본 `.env` 값은 실행 프로세스의 환경 변수로만 넘겼고, Vertex 자격 증명은 원본 파일을 절대 경로로 가리켰다(복사하지 않음). 설정은 `VCP_AI_PROVIDERS=gemini,gpt,z.ai`, `VCP_SECOND_PROVIDER=gpt`, `VCP_GEMINI_MODEL=gemini-3.8-flash`, Vertex `global` 이다
- 실행 1(실측, 08:09:52~08:10:36): 한 프로세스에서 `create_signals_log(target_date="2026-09-21", run_ai=True, signal_limit=2)` 을 부르고, 이어서 관리자 라우트 `POST /api/kr/signals/reanalyze-failed-ai`(`{"target_date":"2026-09-21"}`)를 Flask test client 로 호출했다. 서명 헤더에는 더미 비밀을 썼다. `VCPMultiAIAnalyzer.analyze_batch` 의 반환값은 동작을 바꾸지 않는 관찰용 래퍼로 기록했다
  - 스크리너: 53개 감지, 상위 2개(033530 SJG세종, 108230 톱텍)
  - 추천 원자료: 두 종목 모두 `gemini_recommendation` 성공(BUY 68 / HOLD 65), `gpt_recommendation` 성공(HOLD 72 / HOLD 68, `gpt-5.6-luna`), `perplexity_recommendation` None. 배치 뒤 `gemini_blocked_models` 는 빈 집합, providers 는 `['gemini','gpt','zai']`
  - CSV: 두 행 모두 Gemini 판정(BUY 68, HOLD 65)이다. **운영 증상(Gemini null)은 로컬에서 재현되지 않았다**
  - 재분석: http 200, 「재분석이 필요한 실패/누락 항목이 없습니다」, `failed_targets 0`
  - 부수 관찰: 지수 조회 `get_index_ohlcv` 가 `None of [Index(['TRD_DD', ...])] are in the [columns]` 로 두 번 실패했다(KRX 로그인 환경, `datetime.now()`=09-24 장 시작 전). `[INFRA-089]` 에 옮긴다
- 실행 2(대체 절차, LLM 호출 없음, 08:11:17): TODO 가 정한 대로, 실행 1 의 실제 결과에서 `gemini_recommendation` 만 None 으로 바꾼 값을 돌려주는 가짜 분석기로 같은 두 진입점을 새 사본 상태 위에서 다시 돌렸다. gpt 결과는 실행 1 의 실제 응답 그대로다
  - 수집 병합 뒤 CSV: `033530 ai_action=N/A ai_confidence=0 ai_reason=분석 실패`, `108230` 도 같음. 같은 입력에 유효한 `gpt_recommendation`(HOLD 72 / HOLD 68)이 있었다 → `scripts/init_data.py:1557-1576` 원인 **확정**
  - 재분석: http 200, `failed_targets 2`, `updated_count 0`, `still_failed_count 2`, 「실패 2건 중 0건 재분석 완료」. 가짜 분석기는 두 종목으로 호출되어 gpt 성공 결과를 돌려주었으나 행이 갱신되지 않았다 → `_extract_vcp_ai_recommendation`(Gemini 만 봄) 경로 원인 **확정**
  - 캐시(`kr_ai_analysis_20260921.json`)에는 실행 1 에서 쓴 Gemini 값이 남아 있었다. 캐시 쓰기가 기존 유효값을 보존한 결과이며 이 항목의 판정에는 쓰지 않는다
- 원인 판정: 가설과 일치한다. 수정 계획은 바꾸지 않는다
- 범위 밖 발견: 실행 2 의 재분석 저장 뒤 사본 `signals_log.csv` 원문의 티커가 `033530` → `33530` 으로 바뀌었다(갱신 0건에서도 파일을 다시 씀). `[VCP-042]` 로 등록했다
- 증거 원문: scratchpad `vcp040-run.log`·`vcp040-raw.json`·`vcp040-replay.log`(세션 임시)
- 정리: 사본 트리 삭제 뒤 `ls` 로 없음 확인, `pgrep -fl vcp040` 0건, 58161·58162 리스너 0. 원본 `data/signals_log.csv` 는 09-21 21:41 그대로이고 작업 트리 변경은 이 문서와 TODO 뿐이다

## 시나리오

### S-1. 수집이 Gemini 가 빈 종목에 GPT 판정을 쓴다 (회귀·핵심)
- 조작: 하네스의 자료 적재를 실행하고 사본 `signals_log.csv` 원문을 읽는다.
- 기대: 033530 `HOLD`·72·GPT 사유. 108230 `HOLD`·65·Gemini 사유(Gemini 우선). 세 번째 종목은 `N/A`·확신도 공란·「분석 실패」.
- 필수 여부(required): 예

### S-2. 캐시의 Gemini 칸에 GPT 값이 들어가지 않는다
- 조작: 사본 `kr_ai_analysis_20260921.json` 의 세 추천 칸을 읽는다.
- 기대: 033530 `gemini_recommendation` null, `gpt_recommendation` HOLD 72. 108230 두 칸 모두 원래 값. 세 번째 종목은 캐시에 없음(`_write_ai_analysis_files` 는 유효 추천이 없는 종목을 넣지 않는다).
- 필수 여부(required): 예

### S-3. 화면 API 가 GPT 판정을 Gemini 칸에 싣지 않는다
- 조작: `GET http://127.0.0.1:58171/api/kr/signals?date=2026-09-21`
- 기대: 033530 `gemini_recommendation` null, `gpt_recommendation.action` HOLD. 108230 `gemini_recommendation.action` HOLD. 세 번째 종목은 `gemini_recommendation` null(실패 행).
- 필수 여부(required): 예

### S-4. VCP 화면의 GPT 열과 Gemini 열 (브라우저)
- 조작: `browse` 로 `http://127.0.0.1:58172/dashboard/kr/vcp` 를 열고 2026-09-21 을 고른 뒤 표를 읽고 스크린샷을 찍는다. 조작은 조회만 한다(Refresh·재분석·챗봇 버튼은 누르지 않는다).
- 기대: 033530 행의 GPT 열에 HOLD 배지, Gemini 열에 판정 배지 없음. 108230 행은 두 열 모두 HOLD. 콘솔 오류 없음.
- 필수 여부(required): 예

### S-5. 자동 재분석이 GPT 로 채우고, 유효한 기존 판정은 지킨다
- 조작: 하네스의 재분석을 실행한 뒤 CSV·캐시·`/api/kr/signals` 를 다시 읽는다.
- 기대: 세 번째 종목 CSV 가 GPT 판정으로 갱신(`updated_count` ≥ 1). 033530 CSV 는 `HOLD`·72·GPT 사유 그대로. 캐시 033530 `gemini_recommendation` 은 여전히 null.
- 필수 여부(required): 예

### S-6. 캐시에 없는 종목의 재분석 결과 라벨 (관측, 알려진 한계)
- 조작: S-5 뒤 `/api/kr/signals` 에서 세 번째 종목의 두 칸을 읽는다.
- 기대: 판정 대상이 아닌 관측이다. critic 심층 리뷰 유보대로 GPT 판정이 `gemini_recommendation` 에 실리고 `gpt_recommendation` 이 비면 `[VCP-044]` 의 근거로 그대로 기록한다.
- 필수 여부(required): 아니오

# [FE-044] 개인정보처리방침과 서비스 약관 페이지 — QA 시나리오

- 대상 화면: `http://localhost:<소유한 격리 포트>/` 와 `/privacy`, `/terms`
- 구성 근거: 이번 사이클의 신규 네 파일과 `frontend/src/app/page.tsx` 푸터 변경분
- 구성 2026-09-22 14:22 | 실행 2026-09-22 14:24 | 리뷰 반영 후 재실행 2026-09-22 14:29
- QA 엔진(engine): Claude Code `/qa-only` → `/qa`
- 단계(phase): 시나리오 구성 완료 | 실행 완료
- 반복(iteration): 2회(리뷰 반영 후 재실행)
- baseline 상태: 기준값 수집 완료(변경 전에는 두 주소가 404 였다)
- 필수 여부(required): 예
- 결과: 통과 7/7
- 증거: 1차 `.gstack/qa-reports/screenshots/fe044-privacy.png`, `fe044-terms.png`,
  `fe044-privacy-mobile.png`. 2차 `fe044-privacy-r2.png`, `fe044-terms-r2.png`,
  `fe044-privacy-mobile-r2.png`
- 정리(cleanup): 격리 Next 서버(포트 3521) 종료 완료, 변이 실험 사본 복구 완료

## 시나리오

### S-1. 두 문서가 각자의 주소에서 열린다
- 조작: 격리 서버의 `/privacy` 와 `/terms` 를 차례로 연다.
- 기대: 두 주소 모두 200 으로 응답하고 각각 「개인정보처리방침」과 「서비스 약관」이
  가장 큰 제목으로 보인다. 라우트 그룹 `(legal)` 은 주소에 나타나지 않는다.
- 필수 여부(required): 예
- 실제: `/privacy` 200, `/terms` 200, `/` 200. 각 화면의 `h1` 이 「개인정보처리방침」과
  「서비스 약관」이었고 브라우저 탭 제목은 각각 `개인정보처리방침 | Smart Money Bot` 과
  `서비스 약관 | Smart Money Bot` 이었다. 주소에 `(legal)` 이 나타나지 않았다.
- 결과: 통과

### S-2. 랜딩 페이지 푸터의 두 링크가 각 문서로 간다
- 조작: 첫 화면을 열어 맨 아래로 내린 뒤 「개인정보처리방침」 링크를 누른다. 뒤로 가서
  「서비스 약관」 링크를 누른다.
- 기대: 각각 `/privacy` 와 `/terms` 로 이동한다. 이 확인이 필요한 이유는 Google OAuth
  동의 화면에 등록할 주소가 실제로 열려야 하기 때문이다.
- 필수 여부(required): 예
- 실제: 푸터의 두 링크가 각각 `/privacy` 와 `/terms` 를 가리켰고, 첫 링크를 눌러
  `http://localhost:3521/privacy` 로 이동했다.
- 결과: 통과

### S-3. 개인정보처리방침의 필수 조항 열 개가 모두 보인다
- 조작: `/privacy` 를 열어 제목을 위에서 아래로 읽는다.
- 기대: 1번 수집 항목부터 10번 방침의 변경까지 번호가 빠짐없이 이어진다. 문의 이메일
  `freeopen1191@gmail.com` 이 링크로 보이고 시행일이 표시된다.
- 필수 여부(required): 예
- 실제: 1번부터 10번까지 열 개 제목이 순서대로 렌더되었다. 본문에 「시행일 2026년 9월
  22일」과 문의 이메일이 표시되었다.
- 결과: 통과

### S-4. 서비스 약관의 투자 면책이 눈에 띈다
- 조작: `/terms` 를 열어 첫 화면에서 스크롤 없이 보이는 범위를 확인하고, 제7조까지
  내려간다.
- 기대: 상단 안내 상자에 투자 권유가 아니라는 문구가 보이고, 제7조에 투자자문업자가
  아니라는 서술과 손익이 이용자에게 귀속된다는 서술이 있다.
- 필수 여부(required): 예
- 실제: 안내 상자의 화면 위치가 205px 로 스크롤 없이 보였다. 제7조 제목이
  「제7조 (투자 정보의 성격과 면책)」이었고 투자자문업자 미등록 서술과 손익 귀속 서술이
  모두 있었다. 무료 제공과 모의투자 서술도 확인했다.
- 결과: 통과

### S-5. 문서 사이를 머리글 메뉴로 오간다
- 조작: `/privacy` 상단의 「서비스 약관」을 누르고, 그 화면 상단의 「개인정보처리방침」을
  누른다. 좌측 상단의 서비스 이름을 눌러 첫 화면으로 돌아간다.
- 기대: 세 번 모두 의도한 화면으로 이동한다.
- 필수 여부(required): 예
- 실제: `/privacy` → `/terms` → `/privacy` → `/` 순으로 세 번 모두 이동했다.
- 결과: 통과

### S-6. 좁은 화면에서 표가 넘치지 않는다
- 조작: 브라우저 폭을 390px 로 좁혀 `/privacy` 의 표 세 개를 확인한다.
- 기대: 가로 스크롤이 생기지 않고 표의 두 열이 모두 읽힌다.
- 필수 여부(required): 예
- 실제: 뷰포트 390px 에서 문서 폭이 390px 로 같아 가로 넘침이 없었고, 표 세 개가 모두
  폭 342px 로 화면 안에 들어왔다. 화면 캡처로도 두 열이 모두 읽힘을 확인했다.
- 결과: 통과

### S-7. 로그인하지 않은 상태에서도 열린다 (인접)
- 조작: 로그인하지 않은 브라우저로 두 주소를 연다.
- 기대: 로그인 화면으로 넘어가지 않고 본문이 그대로 보인다. 법적 고지는 누구나 볼 수
  있어야 하며, Google 이 동의 화면 심사에서 확인하는 조건이기도 하다.
- 필수 여부(required): 예
- 실제: `/api/auth/session` 이 빈 객체를 돌려주어 비로그인 상태임을 확인한 뒤에도 두
  화면의 본문이 그대로 보였다.
- 결과: 통과

## 정적 검증

- vitest 전체: 87개 파일 652건 통과
- `npm run type-check`: 통과
- 계약 테스트 변이 실험 5건
  - 조항 제목 변경, 문의 이메일 변경, 투자자문 면책 삭제, 푸터 링크 주소 변경 → 모두 실패로 잡힘
  - 활동 로그 보관 기간 서술 삭제 → **살아남음**. 「30일」이 6항과 10항에도 나와 단언이
    느슨했다. 단언을 「기록한 날부터 30일」로 좁혀 다시 실험했고 실패로 잡혔다.
  - 다섯 건 모두 원본을 복구했고 파일 동일성을 `diff` 로 확인했다.

## 리뷰 반영 후 재실행 (2026-09-22 14:29)

두 리뷰의 지적을 반영해 개인정보처리방침의 조항이 열 개에서 열두 개로 늘었고, 서비스
약관의 면책 조항 세 자리에 고의·중과실 유보가 들어갔다. 구조가 바뀌었으므로 화면 실측을
다시 했다.

- S-1, S-3: `/privacy` 의 제목을 다시 읽어 1번부터 12번까지 순서대로 렌더됨을 확인했다.
  `h1` → `h2` → `h3` 단계에 건너뜀이 없었다. 결과: 통과
- S-6: 뷰포트 390px 에서 문서 폭 390px, 표 네 개가 모두 폭 342px 로 가로 넘침이 없었다.
  표가 세 개에서 네 개로 늘었으나 결과는 같다. 결과: 통과
- S-4: `/terms` 의 상단 안내 상자와 제7조를 다시 확인했다. 결과: 통과
- 국외 이전 표에 이전 국가(미국·미국·중국)와 연락처, 보유·이용 기간이 표시됨을 화면
  캡처로 확인했다.

## 정적 검증 (리뷰 반영 후)

- vitest 전체: 87개 파일 654건 통과
- `npm run type-check`: 통과
- 계약 테스트: 8건에서 10건으로 늘었고 전부 통과

## 2차 리뷰 반영 (2026-09-22 14:30)

두 리뷰 모두 같은 결함 하나를 지적했다. 1차 반영에서 새로 넣은 「라. 이용자의 브라우저에만
저장되는 정보」가 사실과 달랐다. 세 항목 가운데 둘이 서버로도 전송된다.

- 관심 종목: `frontend/src/app/components/ChatWidget.tsx:226-231` 과
  `frontend/src/app/chatbot/useChatStream.ts:189-220` 이 채팅 요청마다 본문에 실어 보낸다.
- 표시 이름과 대화 성향: `frontend/src/app/components/chatHelpers.ts` 의 `saveUserProfile`
  이 `/api/kr/chatbot/profile` 로 먼저 POST 한 뒤 로컬에 복사한다.

법적 고지가 수집 범위를 실제보다 좁게 알리는 방향의 오류이므로 차단 사유로 받아들여
고쳤다. 제목에서 「~에만」을 빼고 「서버로 보내지 않고」와 「운영자는 이 값을 읽을 수
없습니다」를 지웠으며, 두 항목이 서버로 가는 경로를 본문에 적었다. 1항 가의 이름 서술과
5항 Google 이전 행도 함께 고쳤다.

계약 테스트에 단언 넷을 더했다. 「서버로 보내지 않고」가 다시 들어오면 실패하고, 서버 전송
사실을 적은 두 문장과 Google 이전 행의 관심 종목 문구가 사라져도 실패한다.

- 화면 재확인: `/privacy` 에서 「요청에 함께 실려 서버로」가 보이고 「서버로 보내지 않고」는
  없으며, 이름 관련 새 서술이 표시됨을 확인했다.
- 계약 테스트 11건 통과, 전체 vitest 655건 통과, type-check 통과.

## 3차 리뷰 반영과 마감 (2026-09-22 14:31)

코드 리뷰가 차단하지 않는 관찰로 남긴 한 가지를 반영했다. 「라」 목의 세 항목 가운데 둘에만
서버 전송 단서가 붙어 있어 세 번째인 브라우저 식별자를 브라우저에만 있는 값으로 오해할
여지가 있었다. 그 항목도 서버에 저장된다는 문장을 보탰다.

두 리뷰의 최종 판정이다.

- code-review(feature-dev:code-reviewer): APPROVE. 3라운드. CHANGES REQUESTED 두 번을 거쳤다.
- review(oh-my-claudecode:critic): ACCEPT. 3라운드. REJECT 두 번을 거쳤다.

심층 리뷰가 판정을 막지 않는 유보 두 가지를 남겼다. 첫째, 방침의 보관 기간 문안과 시스템의
간격은 `[FE-046]` 이 닫을 때까지 열려 있다. 둘째, 개인정보 보호책임자의 성명은 사용자
결정 사항이며 개인정보 보호법 제30조 제1항 제6호가 직접 요구하는 항목이라는 점을 사용자
보고에 포함한다.

최종 검증이다. vitest 87개 파일 655건 통과, `npm run type-check` 무오류, `npm run lint` 의
신규 파일 경고 0건(저장소 기존 경고 184건은 이번 변경과 무관), QA 시나리오 7/7 통과.

- 결과: 통과
- 정리: 격리 Next 서버(포트 3521)를 종료했고 변이 실험 사본 다섯 건을 모두 복구해 `diff` 로
  동일성을 확인했다. scratchpad 의 임시 스크립트와 로그는 세션 디렉터리에 남는다.

# [VCP-030] signals_log.csv 원자적 쓰기와 빈 파일 회복 — QA 시나리오

- 대상
  - 스케줄러와 「Refresh VCP」가 부르는 `scripts/init_data.py` 의 `create_signals_log`
  - 원자성 주장은 `create_signals_log` 의 쓰기에만 해당한다. `SignalTracker` 의 쓰기 세 곳은 `[VCP-034]` 의 범위다(코드 리뷰 지적 1)
  - 그 결과를 읽는 VCP 화면 `/dashboard/kr/vcp` 와 `GET /api/kr/signals`·`GET /api/kr/signals/dates`
- 구성 근거
  - TODO 의 범위 줄: 원자적 쓰기, 0바이트 파일 회복, 열 수가 어긋난 파일은 보존 + False 유지
  - 계획 검토 R1: 병합과 당일 정리 두 갈래의 원자성
  - 설계 승인 줄: 격리 사본, 가짜 스크리너 하네스, 브라우저 확인
- 가짜 스크리너의 동작: `run_screening` 이 「QA종목030」(ticker `999030`) 한 행을 돌려준다. `MarketGate.analyze` 는 고정값을 돌려준다. LLM(`run_ai=False`)·시세 조회·메시지 발송은 부르지 않고, 하네스는 loopback 이외의 소켓 연결을 막는다
- 쓰기 계측: `atomic_write_text` 와 `pandas.DataFrame.to_csv` 를 감싸서, `signals_log.csv` 에 대한 쓰기를 방식별로 센다
- 구성 2026-09-23 | 실행 (미실행)
- 검증 기준 커밋: (첫 커밋 뒤 기록). 대조군은 `git archive <첫 커밋>~1` 로 같은 하네스를 돌린다
- QA 엔진(engine): Claude Code, 브라우저는 gstack `browse`
- 금지 사항
  - 원본 3500·5501 포트, live 주소, 원본 `.env`, 원본 `data/` 에 쓰기
  - 실제 LLM 호출, 실제 메시지 발송
  - 설정 저장, Refresh 계열, 재분석, 모의 매수, 삭제
- 격리 구성
  - 코드 사본 `code/` 는 `git archive <첫 커밋>` 으로 만들고 `.env`·`secrets/` 는 두지 않는다
  - 원본 `data/` 의 JSON·CSV 를 `code/data/` 에 읽기 전용으로 복사한다
  - cwd 는 `code/` 로 두어 운영처럼 BASE_DIR 과 cwd 를 같게 한다
  - gunicorn 은 `env -i`, `SCHEDULER_ENABLED=false`, 더미 `INTERNAL_IDENTITY_SECRET` 으로 5755 포트에 띄운다
  - Next 는 `API_URL=http://127.0.0.1:5755` 로 3755 포트에 띄운다
- 단계(phase): 시나리오 구성 완료
- 반복(iteration): 0회
- 결과: (미실행)
- 필수 여부(required): 예
- browser_applicability: required. 사용자는 이 파일을 VCP 화면의 날짜 목록과 시그널 표로 본다. browser_driver: gstack `browse`

## 시나리오

### S-1. 0바이트 로그에서 회복하고, 원자적으로 한 번 쓴다 (핵심)
- 조작: 사본의 `data/signals_log.csv` 를 0바이트로 만든다. 가짜 스크리너와 쓰기 계측을 건 상태에서 `create_signals_log(target_date="2026-09-23", run_ai=False)` 를 부른다
- 기대
  - 반환값이 True 다
  - `signals_log.csv` 쓰기는 `atomic` 1회이고 `to_csv` 로 직접 쓴 것은 0회다
  - 파일이 BOM 으로 시작하고 「QA종목030」 한 행을 담는다
  - 대조군(변경 전 커밋)은 같은 조작에서 False 를 돌려주고 파일이 0바이트로 남는다
- 필수 여부(required): 예
- 실제:
- 결과:

### S-2. 쓰기 도중 실패해도 기존 로그가 잘리지 않는다 (계획 검토 R1)
- 조작: 원본에서 복사한 로그가 있는 상태에서 `os.fsync` 를 실패시킨다. 이 상태로 시그널이 있는 경우(병합 갈래)와 없는 경우(당일 정리 갈래)를 각각 실행한다
- 기대: 두 경우 모두 반환값이 False 이고, 로그 파일의 SHA-256 이 실행 전과 같다. `signals_log.csv.*` 임시 파일은 남지 않는다
- 필수 여부(required): 예
- 실제:
- 결과:

### S-3. 열 수가 어긋난 로그는 보존하고 False 를 돌려준다 (`[VCP-029]` 회귀)
- 조작: 둘째 행에 필드를 하나 더 넣은 로그를 두고 가짜 스크리너로 실행한다
- 기대: 반환값이 False 이고 로그 바이트가 그대로다. 경고 로그에 「기존 로그 병합 실패」가 남는다
- 필수 여부(required): 예
- 실제:
- 결과:

### S-4. 화면에 회복된 로그가 보인다
- 조작: S-1 을 다시 실행해 로그를 「QA종목030」 상태로 만든 뒤 격리 서버를 띄우고 `/dashboard/kr/vcp` 를 연다
- 기대
  - `GET /api/kr/signals/dates` 에 `2026-09-23` 이 있다
  - `GET /api/kr/signals?date=2026-09-23` 이 200 이고 「QA종목030」 을 돌려준다
  - 화면에 「QA종목030」 이 보인다
  - 콘솔 오류와 5xx 가 0 이다
- 필수 여부(required): 예
- 실제:
- 결과:

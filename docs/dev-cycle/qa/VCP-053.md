# [VCP-053] 과거 날짜 재분석에서 분석 가능한 종목이 하나도 없어도 「시그널 없음」으로 그 날짜 행을 지운다 — QA 기록

- 대상: `engine/screener.py` 의 `run_screening`(분석된 종목 수 집계, 0개면 `RuntimeError`)과 `_detect_vcp_pattern`(예외 삼킴 제거)
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 구성 2026-09-24 20:58 | 실행
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 대조는 수정 전 커밋 `1050731`. 사본은 각 커밋의 `git archive` 다
- 구성 근거: 설계 승인(대화 20:53 이전), 호출 경로 추적(스케줄러·관리자 「Refresh VCP」 → `create_signals_log` → `SmartMoneyScreener.run_screening` → 과거 날짜는 `_analyze_candidates` 의 CSV 배치 경로 → `_prepare_stock_analysis` 에서 가격 20행 미만·판정 예외는 None → 빈 결과면 「시그널 없음」 갈래, 예외면 `[VCP-028]` 보존 갈래)
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 화면 진입점인 관리자 「Refresh VCP」는 실제 수집과 LLM 호출로 이어지는 금지 조작이고, 결과는 `signals_log.csv` 의 행과 반환값으로 판정한다
- 격리: 각 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고, 사본 `data/` 에 원본 `signals_log.csv`(2026-05-05 15행뿐)·`korean_stocks_list.csv`·`daily_prices.csv`(2025-11-13~2026-09-21)·`all_institutional_trend_data.csv` 를 복사한다. 소켓 연결을 막고 `run_ai=False` 이며 Market Gate 는 고정값(중립·Open)이다. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 하네스: 세션 스크래치의 `vcp053qa_harness.py`. `create_signals_log(target_date=<날짜>, run_ai=False, signal_limit=100)` 한 번이며, `_analyze_candidates` 를 감싸 후보 수와 분석된(결과가 None 이 아닌) 종목 수를 센다. `seed` 는 사본 `signals_log.csv` 에 원본 05-05 15행을 대상 날짜로 복제해 넣는다(원본에 가격 창 시작 부근 날짜의 행이 없으므로)

## 시나리오

### S-1. 가격 창 시작 부근 날짜는 실패로 끝나고 기존 행이 남는다 (하네스, 필수)
- 조작: `seed` 로 2025-11-20(가격 6거래일째, 전 종목 20행 미만) 실행
- 기대: 수정 뒤 `False`, 분석 종목 0, 11-20 15행과 05-05 15행이 그대로(`signals_log.csv` 내용 동일). 최신 payload 는 보존 갈래도 빈 값으로 덮는다(기존 설계, 리뷰 low 2)이므로 판정 밖이다. 수정 전 사본은 `True` 와 11-20 행 삭제
- 실제:

### S-2. 정상 날짜의 출력이 수정 전과 같다 (하네스, 필수, 회귀 확인)
- 조작: 2026-09-21 실행, 출력 `signals_log.csv` 를 `cmp` 로 비교
- 기대: 두 커밋 모두 `True`, 분석 종목 수 같음, 출력 바이트 동일
- 실제:

### S-3. 2026-05-05 의 분석 종목 수를 실측한다 (하네스, 기록)
- 조작: 원본 그대로 2026-05-05 실행
- 기대: 분석 종목 수를 기록한다. 0 이면 수정 뒤 `False`·15행 보존, 1 이상이면 두 커밋 모두 정상 0건 판정(삭제)이며 이번 범위의 결함이 아니다
- 실제:

## 정리
- 실제:

# [VCP-046] Perplexity 프로바이더를 코드·설정·화면에서 제거한다 — QA 기록

- 대상: `engine/vcp_ai_provider_init_helpers.py`(`drop_removed_providers`, `resolve_effective_second_provider` 의 perplexity→gpt 대체), `engine/vcp_ai_analyzer.py`(Perplexity 경로 삭제), `engine/vcp_ai_orchestration_helpers.py`(`VCP_AI_WRITTEN_FIELDS`), `services/kr_market_vcp_reanalysis_service.py`(두 번째 칸은 `gpt_recommendation`), `services/common_env_service.py`(`PERPLEXITY_API_KEY` 편집 제외), frontend(설정 모달·랜딩·VCP 기준 모달·개인정보 5절·`decideSecondaryAI` GPT 우선)
- 단계(phase): 시나리오 구성 완료 | 실행 전
- 구성 2026-09-24 13:37
- 검증 기준 커밋: 이 문서를 담은 첫 커밋. 사본은 그 커밋의 `git archive` 로 만든다
- 구성 근거: 설계 승인(대화 12:46, 문서 13:02), 계획 `docs/superpowers/plans/2026-09-24-vcp-046-perplexity-removal.md` 의 Review Focus 다섯 항목, 호출 경로 추적(기동 → `get_vcp_analyzer` → `VCPMultiAIAnalyzer.__init__` → `resolve_effective_second_provider`, 재분석 → `execute_vcp_failed_ai_reanalysis` → `analyzer.second_provider` → `resolve_vcp_second_recommendation_key`, VCP 화면 → `/api/kr/signals` legacy 보강 → `page.tsx` 의 `decideSecondaryAI`, 설정 모달 → `GET /api/system/env` → `read_masked_env_vars`)
- QA 엔진(engine): Claude Code. browser_applicability: required(browser_driver: agent-browser). VCP 표의 두 번째 열, 관리자 설정 모달, 개인정보·랜딩 문구가 사용자가 보는 화면이다. 분석기 기동과 재분석 대상 판정은 화면에 드러나지 않으므로 하네스로 본다
- 격리: 첫 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지운다. 사본 `data/` 에는 원본의 공개 시장 자료(`signals_log.csv`, `daily_prices.csv`, `korean_stocks_list.csv`)와 `ai_analysis_results_20260505.json`·`kr_ai_analysis_20260505.json` 을 복사하고, 아래 합성 캐시를 만든다. 사본 `.env` 는 더미 값만 담는다(`PERPLEXITY_API_KEY=pplx-qa-dummy-…`, `VCP_SECOND_PROVIDER=perplexity`, `VCP_AI_PROVIDERS=gemini,perplexity,gpt`, 더미 `NEXTAUTH_SECRET`·`INTERNAL_IDENTITY_SECRET`·`ADMIN_API_TOKEN`, `ADMIN_EMAILS=qa-admin@example.com`, `SCHEDULER_ENABLED=false`). 관리자 화면은 `[VCP-031]` 과 같이 더미 `NEXTAUTH_SECRET` 으로 `next-auth/jwt` 의 `encode` 가 만든 세션 토큰을 쿠키로 넣는다. 원본 3500·5501, 원본 `data/`·`.env`, 운영 주소는 쓰지 않는다
- 합성 캐시(사본만): `signals_log.csv` 의 05-05 행을 복제해 `signal_date` 를 2026-09-21·2026-09-22 로 바꾼 행을 덧붙이고, `ai_analysis_results_20260921.json` 은 두 번째 판정을 `perplexity_recommendation` 에만(과거 캐시 모양), `ai_analysis_results_20260922.json` 은 `perplexity_recommendation`·`gpt_recommendation` 둘 다에 담는다. 사유 문자열에 「VCP046-QA pplx」「VCP046-QA gpt」를 넣어 화면에서 출처를 가린다
- 금지: 설정 저장, Refresh, AI 재분석 실행 버튼, 챗봇 전송, 모의 매수, 실제 LLM 호출. 하네스의 재분석은 분석기의 `analyze_batch` 를 대상 기록용 가짜로 바꿔 부른다
- 대조: S-1 은 수정 전 커밋 `67bc1d0` 사본에서 같은 하네스를 돌린다(키가 있으면 perplexity 를 그대로 확정하던 동작)
- 필수 여부(required): S-1~S-5 예
- 반복(iteration):
- 결과:
- 증거:
- 정리(cleanup):

## 시나리오

### S-1. 운영 `.env` 에 perplexity 설정이 남아 있어도 분석기는 GPT 로 기동한다 (하네스, 핵심)
- 조작: 사본에서 클라이언트 초기화만 가짜로 바꾸고 `VCPMultiAIAnalyzer()` 를 세 설정으로 만든다. (가) `VCP_AI_PROVIDERS=gemini,perplexity,gpt`, `VCP_SECOND_PROVIDER=perplexity`, `PERPLEXITY_API_KEY` 있음 (나) `VCP_AI_PROVIDERS=gemini,perplexity`, `VCP_SECOND_PROVIDER=perplexity` (다) 기본값(`gemini,gpt`, `gpt`)
- 기대: (가) `providers == ["gemini","gpt"]`, `second_provider == "gpt"`, 경고 두 줄(목록의 perplexity 대신, 두 번째 자리 gpt 대체). (나) `providers == ["gemini","gpt"]`(gpt 보충), `second_provider == "gpt"`, 경고 두 줄. (다) 경고 없음, `second_provider == "gpt"`. 세 경우 모두 분석기에 `perplexity_*` 속성과 `_analyze_with_perplexity` 가 없다. 수정 전 사본의 (가)는 `second_provider == "perplexity"`
- 실제:

### S-2. 과거 캐시만 있는 날짜의 재분석 대상은 GPT 칸을 기준으로 정한다 (하네스)
- 조작: S-1 (가) 설정의 사본에서 `execute_vcp_failed_ai_reanalysis(target_date="2026-09-21")` 를 자동 모드로 부르고, `analyze_batch` 를 받은 종목만 기록하는 가짜로 바꾼다. 이어 2026-09-22 로 한 번 더 부른다
- 기대: 09-21 은 Gemini 가 있고 두 번째 판정이 `perplexity_recommendation` 에만 있는 종목이 GPT 누락으로 대상이 된다(요청 건수 = 합성 종목 수). 09-22 는 GPT 가 있으므로 대상 0 건이고 `analyze_batch` 가 불리지 않는다. 가짜 `analyze_batch` 는 빈 결과를 돌려주므로 캐시 파일은 바이트 단위로 그대로다(유효 결과가 있으면 `generated_at` 이 바뀌어 파일 전체가 다시 쓰이므로 이 기대는 빈 결과에서만 성립한다)
- 실제:

### S-3. VCP 표의 두 번째 열: 과거 캐시는 Perplexity, GPT 가 있으면 GPT (브라우저)
- 조작: 사본 gunicorn·Next dev 를 띄우고 `/dashboard/kr/vcp` 의 날짜 선택에서 2026-09-21, 2026-09-22, 2026-05-05 를 차례로 연다. 첫 행을 눌러 상세 탭을 본다
- 기대: 09-21 두 번째 열 머리글 「Perplexity」, 배지 사유 「VCP046-QA pplx」. 09-22 는 머리글 「GPT」, 배지는 GPT 판정이고 상세 탭에 GPT·Perplexity 탭이 모두 있다. 05-05 는 「GPT」. 콘솔 오류 없음. `/api/kr/signals?date=2026-09-21` 응답에 `perplexity_recommendation` 이 실려 있다
- 실제:

### S-4. 관리자 설정 모달에 Perplexity 키 입력란이 없고, 서버 `.env` 에 남은 키를 돌려주지 않는다 (브라우저)
- 조작: 더미 관리자 세션으로 설정 모달의 「API & 기능」 탭을 연다. 저장 버튼은 누르지 않는다
- 기대: `OPENAI_API_KEY` 입력란은 있고 `PERPLEXITY_API_KEY` 입력란과 「Perplexity」 절은 없다. 탭이 부른 `GET /api/system/env` 응답에 `PERPLEXITY_API_KEY` 키가 없다(사본 `.env` 에는 더미 값이 있음)
- 실제:

### S-5. 개인정보 5절과 랜딩·VCP 기준 모달 문구 (브라우저)
- 조작: `/privacy`, `/`(랜딩), VCP 화면의 기준 모달을 연다
- 기대: `/privacy` 5절 표에 `OpenAI, L.L.C.` 행이 있고 「Perplexity」 문자열이 없으며 시행일은 2026년 9월 24일. 거부 방법 문장은 「OpenAI 와 Z.ai 로 가는 VCP 분석은」. 랜딩과 기준 모달에 「Perplexity」 문자열이 없다. 랜딩 AI 카드 격자는 두 장이 한 줄에 놓인다
- 실제:

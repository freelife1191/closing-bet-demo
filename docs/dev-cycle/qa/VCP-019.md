# [VCP-019] 날짜를 지정하지 않는 시그널 조회의 빈 이유 안내 — QA 시나리오

- 대상 화면: http://localhost:3500/dashboard/kr/vcp (백엔드 http://localhost:5501)
- 구성 근거: /qa-only 리포트 (`.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-019.md`)
  + 이번 사이클의 변경 두 파일
- 구성 2026-09-07 10:05 | 실행 (미실행)
- QA 엔진(engine): Claude Code `/qa-only` → `/qa`
- 단계(phase): 시나리오 구성 완료 | 실행 대기
- 반복(iteration): 1회
- baseline 상태: 기준값 수집 완료 (`.gstack/qa-reports/baseline-vcp-019.json`)
- 필수 여부(required): 예
- 결과: (미실행)
- 증거: `.gstack/qa-reports/qa-report-localhost-3500-2026-09-07-vcp-019.md`
- 정리(cleanup): (미실행)

## 검사 대상에 관한 전제

이번 변경은 안내 문구를 정하는 갈래 하나를 바꿉니다. 갈래는 최신 저장 날짜가 오늘보다
과거인지, 오늘 그 자체인지, 미래인지로 나뉩니다. 현재 `data/signals_log.csv` 는 15행 전부
`signal_date` 가 2026-05-05 이므로 실환경에서는 첫 갈래만 나옵니다.

나머지 두 갈래를 화면과 API 에서 보려면 오늘 또는 미래 날짜를 가진 자료가 필요합니다.
그 상태를 화면 조작으로 만들려면 「Refresh VCP」 나 「실패 AI 재분석」 을 눌러야 하는데
둘 다 `AGENTS.md` 가 금지한 되돌릴 수 없는 조작이고, `data/` 는 읽기 전용입니다.

그래서 원본 파일의 `signal_date` 열만 바꾼 사본을 scratchpad 에 만들고, 그 디렉터리를
작업 디렉터리로 삼아 백엔드를 다시 띄웁니다. `app/routes/kr_market.py:98` 의 `DATA_DIR` 이
`'data'` 상대 경로이므로 작업 디렉터리를 바꾸면 그 사본을 읽습니다. 코드에는 손대지 않고,
원본 `data/` 는 읽기만 합니다. 실행이 끝나면 사본과 임시 프로세스를 지웁니다.

## 시나리오

### S-1. 오늘 자 행만 있고 시그널이 0건이면 그 사실을 알린다 (회귀)
- 조작: `signal_date` 를 2026-09-07 로 바꾼 사본으로 백엔드를 띄우고
  `GET /api/kr/signals` 를 부른다.
- 기대: `count=0` 이고 `stale_warning` 이 정확히
  `"오늘(2026-09-07) 기준 VCP 시그널이 없습니다."` 다. 최신 저장 날짜가 오늘 그 자체이므로
  「최신 저장 데이터는 …입니다」 뒷절은 붙지 않는다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 사본과 임시 백엔드를 지운다

### S-2. 그 상태에서 화면 배너가 같은 문구를 보인다 (회귀)
- 조작: 같은 사본을 읽는 백엔드를 5501 포트에 띄우고 VCP 화면을 새로 로드한다.
- 기대: 상단 경고 배너에 `오늘(2026-09-07) 기준 VCP 시그널이 없습니다.` 가 그대로 보이고,
  표에는 `No signals found.` 만 있으며, 콘솔 오류는 0건이다. 고치기 전에는 이 상태에서
  배너 자체가 렌더되지 않아 사용자가 이유를 알 수 없었다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 5501 을 원본 자료 백엔드로 되돌린다

### S-3. 최신 저장 날짜가 미래이면 그 날짜를 감추지 않는다 (회귀)
- 조작: `signal_date` 를 2026-12-01 로 바꾼 사본으로 `GET /api/kr/signals` 를 부른다.
- 기대: `count=0` 이고 `stale_warning` 이
  `"오늘(2026-09-07) 기준 VCP 시그널이 없습니다. 최신 저장 데이터는 2026-12-01입니다."` 다.
  같은 판정을 쓰는 날짜 목록이 미래 날짜를 드롭다운에 보이므로 배너도 그 날짜를 말해야 한다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 사본과 임시 백엔드를 지운다

### S-4. 최신 저장 날짜가 과거인 기존 문구가 그대로다 (인접)
- 조작: 원본 자료를 읽는 백엔드에서 `GET /api/kr/signals` 를 부른다.
- 기대: `count=0` 이고 `stale_warning` 이
  `"오늘(2026-09-07) 기준 VCP 시그널이 없습니다. 최신 저장 데이터는 2026-05-05입니다."` 로
  기준값과 같다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-5. 날짜를 지정한 조회의 문구가 그대로다 (인접)
- 조작: 원본 자료 백엔드에 `?date=2026-05-05`, `?date=20260505`, `?date=not-a-date` 를
  각각 보낸다.
- 기대: 앞의 둘은 `"2026-05-05에 저장된 VCP 시그널이 없습니다."`, 마지막은
  `"요청한 날짜에 저장된 VCP 시그널이 없습니다."` 로 `[VCP-008]` 이 만든 문구와 같다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-6. 날짜 목록과 드롭다운이 그대로다 (인접)
- 조작: 원본 자료 백엔드에 `GET /api/kr/signals/dates` 를 보내고, 화면의 「과거」 버튼을
  눌러 드롭다운을 연다.
- 기대: 응답이 `[]` 이고 드롭다운에 「데이터 파일 없음」 이 보인다. 날짜 버튼은 0개다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): `Escape` 로 드롭다운을 닫는다

### S-7. 정적 검증 전체가 통과한다 (회귀)
- 조작: 저장소 루트에서 `pytest`, `frontend/` 에서 `npx vitest run` 을 돌린다.
- 기대: pytest 가 1708건 통과하고 종료 코드가 0 이다. vitest 는 45 파일 288건이 통과하고
  종료 코드가 0 이다. 새로 더한 검사 둘이 그 안에 포함된다.
- 필수 여부(required): 예
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

### S-8. 같은 오리진 링크와 로딩 성능이 성하다 (리포트)
- 조작: 화면의 같은 오리진 링크 7개에 요청을 보내고 `perf` 로 로딩 시간을 읽는다.
- 기대: 전부 200 이고 `load` 가 2초를 넘지 않는다.
- 필수 여부(required): 아니오
- 실제: (미실행)
- 결과: (미실행)
- 증거: (미실행)
- 정리(cleanup): 이 시나리오가 소유한 임시 자료 없음

## 이월한 발견

- ISSUE-001 「헤더 홈 링크에 접근 가능한 이름이 없다」 → 기존 `[FE-029]`. 화면 공통 헤더의
  문제이며 이번 변경 범위 밖입니다.
- ISSUE-002 「모바일에서 빈 상태 문구가 화면 밖에 놓인다」 → 기존 `[FE-015]`. 표의 최소 폭을
  다루어야 하므로 이번 범위 밖입니다. 다만 이번 변경으로 배너가 뜨는 경우에는 배너 문구가
  모바일에서도 보이므로 이유를 아예 알 수 없는 상태는 줄었습니다.
- ISSUE-003 「무료 사용량 「+」 버튼의 이름이 기호 하나뿐이다」 → 기존 `[FE-029]` 의 메모.

## 실행 결과

(미실행)

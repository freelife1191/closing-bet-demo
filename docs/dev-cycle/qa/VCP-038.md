# [VCP-038] 메뉴 2 의 ai_reason 원문 저장 — QA 시나리오

- 대상
  - `run.py` 메뉴 2 의 저장 갈래(y). 출력은 50자로 자르고 `signals_log.csv` 에는 원문을 저장한다
  - 그 로그를 읽는 `GET /api/kr/signals` 의 `gemini_recommendation` 과 VCP 화면 `/dashboard/kr/vcp` 의 AI 사유 표시
- 구성 근거
  - 설계 승인 줄: 자르기는 출력용 복사본에만 적용하고 `_append_to_log(analyzed_df)` 는 원래 값으로 저장한다
  - 사유가 결측이면 API 는 추천을 싣지 않는다(`app/routes/kr_market_signal_common.py:46` `_is_meaningful_ai_reason`, `app/routes/kr_market_vcp_signal_helpers.py:208-220`). 변경 전에는 결측이 문자열 "nan..." 으로 저장되어 유효한 사유로 통과한다
- 하네스: 격리 사본 `code/` 를 cwd 로 두고 `run.main()` 을 입력 `2`·`y`·`""` 로 실행한다. `scan_today_signals` 는 원본에서 복사한 자료로 실제로 돌리고, LLM 을 부르는 `SignalTracker.analyze_signals_with_ai` 만 가짜로 바꾼다(첫 행 사유 80자 이상, 둘째 행 NaN, 나머지 짧은 사유). loopback 이외의 소켓 연결은 막는다
- 구성 2026-09-23 16:30 | 실행 2026-09-23 16:39~16:46 (1회차)
- 검증 기준 커밋: `d933bfe` (첫 커밋, `code/` 는 `git archive d933bfe3`). 대조군은 `git archive d933bfe3~1` 로 같은 하네스를 돌렸다
- QA 엔진(engine): Claude Code, 브라우저는 gstack `browse`
- 금지 사항
  - 원본 3500·5501 포트, live 주소, 원본 `.env`, 원본 `data/` 에 쓰기
  - 실제 LLM 호출, 실제 메시지 발송
  - 설정 저장, Refresh 계열, 재분석, 모의 매수, 삭제
- 격리 구성
  - 코드 사본은 `.env`·`secrets/` 없이 만들고, 원본 `data/` 의 JSON·CSV 를 `code/data/` 에 읽기 전용으로 복사한다
  - gunicorn 은 `env -i`, `SCHEDULER_ENABLED=false`, 더미 `INTERNAL_IDENTITY_SECRET` 으로 5757 포트에 띄운다
  - Next 는 `API_URL=http://127.0.0.1:5757` 로 3757 포트에 띄운다
- 단계(phase): 시나리오 구성 완료 | 실행 완료
- 반복(iteration): 1회
- 결과: 통과 (필수 2/2)
- 증거: 하네스 출력 scratchpad `vcp038-harness.out`, 새 코드 사본의 저장 로그 `vcp038-signals-log-new.csv`, 격리 gunicorn access·error 로그 `vcp038-access-5757.log`·`vcp038-access-5758.log`·`vcp038-error-5757.log`·`vcp038-error-5758.log`, Next 로그 `vcp038-next.log`·`vcp038-next-base.log`, 스크린샷 `vcp038-s2.png`(열어 확인)·`vcp038-s2-base.png`, browse `js`·`console --errors` 출력
- 정리(cleanup): browse 서버 정지, 격리 gunicorn 두 개(5757·5758) 마스터 TERM, 격리 Next 프로세스 TERM. 3757·5757·5758 와 원본 3500·5501 에 리스너 0, 사본 경로 프로세스 0, 사본 `qa-vcp038/`·대조군 `qa-vcp038-base/` 삭제. 사본 구성 시각 이후 원본 `data/`·`logs/` 에서 수정 시각이 바뀐 파일 0개. 루트 `node_modules/.vite` 없음, 작업 트리 변화 없음
- 구성 중 차이: 가짜 AI 는 점수 1위 행(010120 LS ELECTRIC)에 89자 사유, 2위 행(009150 삼성전기)에 NaN 을 넣었다. 대조군 화면을 보려고 5758 에 대조군 gunicorn 을 하나 더 띄우고 같은 Next 를 그쪽으로 다시 띄웠다. Next 를 `NEXT_PUBLIC_API_URL` 을 준 명령으로 처음 띄우고 `127.0.0.1:3757` 로 열었을 때는 Next dev 가 `/_next` 자원의 교차 출처 요청을 막아 클라이언트 코드가 돌지 않았다(표 0행). `[VCP-037]` 과 같은 기동 명령으로 다시 띄우고 `localhost:3757` 로 열어 해결했다. 새 코드 사본에서 하네스를 실수로 한 번 더 실행했으나 같은 날짜 행이 병합으로 대체되어 로그는 64행·오늘 49행·중복 0 그대로였다
- 관찰: 대조군 화면 첫 로드에서 Next 프록시가 `GET /api/kr/signals`·`/api/kr/signals/status` 를 `read ECONNRESET` 으로 잃고 500 을 냈다(gunicorn access 는 모두 200, 재조회로 표는 채워짐). Next 를 다시 띄운 직후, gunicorn 에 약 50초 요청이 없던 뒤였다. `[INFRA-080]` 과 같은 현상이며 그 항목에 관찰로 더했다
- 필수 여부(required): 예
- browser_applicability: required. 사용자는 메뉴 2 로 저장한 AI 사유를 VCP 화면의 추천 배지와 상세 패널에서 본다. browser_driver: gstack `browse`

## 시나리오

### S-1. 메뉴 2 의 y 갈래가 ai_reason 원문을 저장한다 (핵심)
- 조작: 하네스로 `run.main()` 을 입력 `2`·`y`·`""` 로 실행한다
- 기대
  - 출력 표의 사유는 50자 + "..." 로 잘려 있다
  - 로그의 오늘 자 첫 행 `ai_reason` 이 가짜 원문과 같고(50자 초과), 둘째 행은 비어 있다
  - 대조군은 첫 행이 50자 + "..." 이고 둘째 행이 "nan..." 이다
- 필수 여부(required): 예
- 실제: 출력 표는 사유를 50자 + "..." 로 잘라 보였다. 로그 64행(기존 15 + 오늘 49), 010120 의 `ai_reason` 이 가짜 원문 89자와 같음(`long_row_equal=True`), 009150 은 `nan`(결측, `isna=True`). 대조군은 010120 이 「QA 원문 사유: … 외국인과 기관이 ...」(50자 + "..."), 009150 이 문자열 'nan...'
- 결과: 통과

### S-2. API 와 화면이 원문 사유를 보이고 결측 사유 행에는 추천을 붙이지 않는다
- 조작: S-1 을 마친 사본으로 격리 서버를 띄워 날짜 없는 `GET /api/kr/signals` 를 읽고 `/dashboard/kr/vcp` 에서 첫 행 종목을 열어 AI 사유를 본다. 대조군 로그로도 API 를 읽는다
- 기대
  - 첫 행 종목의 `gemini_recommendation.reason` 이 원문 전체이고, 화면의 사유 문구에 51자 이후 부분이 보인다
  - 둘째 행 종목은 `gemini_recommendation` 이 없다(null)
  - 대조군은 첫 행 사유가 "..." 으로 끝나고 둘째 행 사유가 "nan..." 인 추천이 실린다
  - 콘솔 오류와 5xx 는 0 이다
- 필수 여부(required): 예
- 실제: 새 코드 API 는 `source=signals_log.csv`·`count=20`, 010120 의 `gemini_recommendation.reason` 이 원문 전체(「… 끝부분까지 보이면 원문 저장.」), 009150 은 `gemini_recommendation: null`. 화면 표 20행, LS ELECTRIC 추천 배지의 title 이 원문 전체. 삼성전기 배지는 시그널 추천이 없어 화면이 `/api/kr/ai-analysis` 의 추천으로 대체한 실제 AI 사유(「① 뉴스/재료 분석: …」)를 보였다(`page.tsx:1085-1094`). 대조군 API 는 010120 사유가 "..." 으로 끝나고 009150 에 사유 "nan..." 인 BUY 추천이 실렸으며, 대조군 화면의 삼성전기 배지 title 이 「nan...」 이었다. 새 코드 쪽 `console --errors` 「no console errors」, gunicorn access 두 로그 32건 모두 200, error 로그의 Traceback·ERROR 0건. 대조군 쪽 콘솔 오류 5건은 위 관찰의 Next 프록시 ECONNRESET 500 이다
- 결과: 통과

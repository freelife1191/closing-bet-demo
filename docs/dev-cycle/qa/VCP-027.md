# [VCP-027] 실패 AI 재분석의 날짜 없는 스코프를 화면의 「최신」 날짜와 맞춘다 — QA 시나리오

- 대상 화면: http://localhost:3749/dashboard/kr/vcp (격리 Next). 백엔드는 격리 gunicorn http://127.0.0.1:5749
- 구성 근거: `[VCP-027]` 의 근거 줄(2026-09-10 에 유효 행이 있고 2026-09-15 행이 전부 CLOSED 면 화면은 09-10, 재분석 스코프는
  09-15) + 이번 변경(날짜 없는 갈래가 `_filter_signals_dataframe_by_date` 를 쓰고, 라우트가 `status`·`is_vcp` 를 읽음).
  기대값은 아래 변형 자료와 오늘(2026-09-23)에서 나온다
- 구성 2026-09-23 12:05 | 실행 (미실행)
- 검증 기준 커밋: (첫 커밋 뒤 기입). 비교 대상은 수정 전 커밋 `c42077b`
- QA 엔진(engine): Claude Code, 브라우저는 gstack `browse`. 원본 3500/5501·live 주소·원본 `.env`·원본 `data/` 는 쓰지 않는다.
  `browser-notes.md` 「공통」 절에 따라 원본 PYTHONPATH 의 앱을 scratch cwd 로 띄우지 않는다
  - 코드 사본 `code-before/`(`git archive c42077b`)·`code-after/`(`git archive <첫 커밋>`). `.env`·`secrets/` 없음
  - 실행 디렉터리 `run/`: 원본 `data/` 의 JSON·CSV 를 읽기 전용으로 복사하고 `signals_log.csv` 만 아래 변형으로 바꾼다
  - 변형 `signals_log.csv`(원본 헤더 그대로 네 행): `005930` 2026-09-10 OPEN `is_vcp=True` AI 실패(`N/A`·「분석 실패」),
    `000660` 2026-09-10 CLOSED `is_vcp=True`, `035420` 2026-09-15 CLOSED `is_vcp=True`, `051910` 2026-09-30 OPEN `is_vcp=True`(미래)
  - gunicorn: `code-after` 를 `--pythonpath`, cwd `run/`, `SCHEDULER_ENABLED=false`, 더미 `INTERNAL_IDENTITY_SECRET`, 1 worker,
    `127.0.0.1:5749`. Next: 작업 트리 `frontend/` 사본, `PORT=3749 API_URL=http://127.0.0.1:5749`, 더미 비밀, `npm run dev -- -p 3749`
  - 서비스 하네스: 각 코드 사본을 `PYTHONPATH` 로, cwd `run/` 에서 라우트와 같은 `load_csv_readonly(load_csv_file,
    "signals_log.csv", usecols=_VCP_REANALYSIS_SIGNAL_USECOLS)` 로 읽은 뒤 `prepare_vcp_signals_scope(df, None)` 의 날짜와 행을 출력
- 금지 조작: 실패 AI 재분석·GEMINI 재분석 단추와 `POST /api/kr/signals/reanalyze-failed-ai`(AI 비용), Refresh VCP, 설정 저장, 챗봇
  전송, 모의 매수, 삭제 계열. 화면은 GET 조회만 한다
- 단계(phase): 시나리오 구성 완료
- 반복(iteration): 0회
- 결과: (미실행)
- 필수 여부(required): 예
- browser_applicability: required. 관리자가 「최신」 탭에서 보이는 날짜를 보고 재분석을 누르는 흐름이다. 누르는 조작은 금지 조작이므로
  브라우저로는 화면의 날짜를 읽고, 재분석이 쓸 날짜는 같은 자료·같은 읽기 경로의 하네스로 계산해 대조한다. 이 대조는 하네스 증거이며
  재분석 실행을 관측한 것이 아니다. browser_driver: gstack `browse`
- 읽은 정본: `.claude/skills/closing-bet-python/SKILL.md`, `.claude/skills/closing-bet-verify/SKILL.md`,
  `.claude/skills/dev-cycle/references/browser-notes.md`

## 시나리오

### S-1. 수정 전 코드는 화면과 다른 날짜를 스코프로 잡는다 (대조군)
- 조작: `code-before` 로 하네스를 돌린다.
- 기대: 판정 없는 최댓값이라 날짜가 2026-09-30(미래 행)이고 행은 `051910` 하나다. 화면 날짜(S-2)와 다르면 대조군이 재현된 것이다.
- 필수 여부(required): 예 (재현되지 않으면 S-2 의 일치가 수정의 증거가 되지 못한다)
- 실제:
- 결과:

### S-2. 수정 후 스코프 날짜가 화면의 「최신」 날짜와 같다 (핵심)
- 조작: 격리 서버로 `/dashboard/kr/vcp` 「최신」 탭을 열어 표의 종목과 날짜·대체 안내를 읽는다. `code-after` 로 하네스를 돌린다.
- 기대: 화면은 2026-09-10 의 `005930` 한 행과 「오늘(2026-09-23) 기준 … 최신 저장분(2026-09-10)」 안내를 보인다. 하네스는 날짜
  2026-09-10, 행 `005930`·`000660`(CLOSED 포함, 결정 「날짜만 맞춤」)을 낸다. 미래 행과 09-15 는 들어가지 않는다.
- 필수 여부(required): 예
- 실제:
- 결과:

### S-3. 화면 회귀 없음
- 조작: S-2 화면에서 과거 탭의 날짜 목록을 열고, 콘솔 오류·`/_next/mcp get_errors`·gunicorn access 로그의 5xx 를 확인한다.
- 기대: 날짜 목록에 2026-09-10 이 있고 5xx·콘솔 오류·프레임워크 오류 0. 이번 변경과 무관한 오류는 원인과 함께 따로 적는다.
- 필수 여부(required): 예
- 실제:
- 결과:

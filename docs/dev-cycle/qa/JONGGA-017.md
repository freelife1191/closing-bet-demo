# [JONGGA-017] 영문자가 든 종목코드가 조회 경로에 따라 다른 코드가 된다 — QA 시나리오

- 대상 화면: http://localhost:3500/dashboard/kr/closing-bet
- 구성 근거: `/qa-only` 리포트 (`.gstack/qa-reports/qa-report-closing-bet-2026-09-04.md`)
  + 이번 사이클의 변경 세 파일
- 구성 2026-09-04 06:55 | 실행 2026-09-04 07:10 (7건 전부 통과, 고친 것 없음)

이번 사이클이 고친 것은 `app/routes/kr_market_signal_common.py` 의 `_normalize_ticker` 입니다.
그 함수가 숫자만 남기는 바람에 영문자가 든 국내 종목코드가 다른 종목의 코드로 바뀌었습니다.
아크릴 `0007C0` 이 `000070` 이 되고, 아로마티카 `0015N0` 이 `000150` 이 되며,
삼성에피스홀딩스 `0126Z0` 이 `001260` 이 됩니다. 세 값 모두 실재하는 다른 종목의 코드입니다.
이력 조회는 이 정규화를 거치지 않고 최신 조회만 거치므로, 최신 자료에 이런 종목이 들어오는
날 두 경로가 서로 다른 회사를 가리켰습니다.

기대값은 2026-09-04 브라우저 실측값입니다.

## 시나리오

### S-1. 아크릴의 코드가 카드와 모달과 외부 링크에서 같다 (회귀)
- 조작: 날짜 선택 상자에서 `2026-02-11` 을 고른다. 「아크릴」 카드의 종목명 아래 코드를
  읽고, 그 카드의 「상세 분석 보기」를 눌러 모달 머리글의 코드를 읽는다. 모달을 Escape 로
  닫고 그 카드의 「N 네이버」와 「토스」 링크 주소를 읽는다.
- 기대: 네 자리 모두 `0007C0` 이다. 카드가 `0007C0`, 모달 머리글이 `아크릴 / 0007C0 /
  KOSDAQ`, 네이버가 `https://m.stock.naver.com/domestic/stock/0007C0/main`, 토스가
  `https://tossinvest.com/stocks/0007C0` 다. `000070` 은 어디에도 나타나지 않는다.
- 결과: 통과. 네 자리가 모두 `0007C0` 이다. 카드가 `B GRADE / #2 / 아크릴 / 0007C0`,
  모달 머리글이 `아크릴 / 0007C0 / KOSDAQ`, 네이버가
  `https://m.stock.naver.com/domestic/stock/0007C0/main`, 토스가
  `https://tossinvest.com/stocks/0007C0` 다. 이 날짜의 문서 전체에서 `0007C0` 이 여덟 번
  나오고 `000070` 은 한 번도 나오지 않는다.

### S-2. 아로마티카의 코드가 카드와 모달과 외부 링크에서 같다 (회귀)
- 조작: 날짜를 `2026-02-20` 으로 바꾼다. 「아로마티카」 카드에 S-1 과 같은 절차를 밟는다.
- 기대: 네 자리 모두 `0015N0` 이다. 모달 머리글이 `아로마티카 / 0015N0 / KOSDAQ` 이다.
  아로마티카 카드 안에는 `000150` 이 나타나지 않는다. 그 코드는 두산의 것이고, 이 날짜에는
  두산 카드도 함께 뜨므로 화면 전체에서는 `000150` 이 두산 카드 몫으로 나타난다.
- 결과: 통과. 네 자리가 모두 `0015N0` 이고 모달 머리글이 `아로마티카 / 0015N0 / KOSDAQ`
  이다. 다만 기대에 적은 「`000150` 은 나타나지 않는다」는 이 화면에 들어맞지 않는다.
  2026-02-20 에는 두산이 열세 번째 카드로 실재하며 그 카드의 코드가 `000150` 이기
  때문이다. 그래서 문서 전체에서 `000150` 이 여덟 번 나온다. 두 카드를 따로 떼어 세어 보면
  아로마티카 카드 안에는 `0015N0` 이 여덟 번, `000150` 이 0번이고, 두산 카드 안에는
  `000150` 이 여덟 번, `0015N0` 이 0번이다. 서로 섞이지 않았다. 오히려 이 날짜가 가장
  강한 회귀 증거다. 고치기 전이었다면 아로마티카가 `000150` 으로 바뀌어 같은 화면에
  `000150` 을 단 카드가 두 장 생겼을 것이다. 기대 문구는 「아로마티카 카드 안에
  `000150` 이 나타나지 않는다」로 읽어야 정확하다.

### S-3. 삼성에피스홀딩스의 코드가 카드와 모달과 외부 링크에서 같다 (회귀)
- 조작: 날짜를 `2026-02-23` 으로 바꾼다. 「삼성에피스홀딩스」 카드에 S-1 과 같은 절차를
  밟는다.
- 기대: 네 자리 모두 `0126Z0` 이다. 모달 머리글이 `삼성에피스홀딩스 / 0126Z0 / KOSPI` 다.
  `001260` 은 나타나지 않는다.
- 결과: 통과. 네 자리가 모두 `0126Z0` 이고 모달 머리글이
  `삼성에피스홀딩스 / 0126Z0 / KOSPI` 다. 문서 전체에서 `0126Z0` 이 여덟 번 나오고
  `001260` 은 한 번도 나오지 않는다.

### S-4. 숫자만으로 된 코드가 이번 변경에 영향받지 않는다 (회귀)
- 조작: 날짜를 `2026-02-11` 로 두고 네 카드의 등급·종목명·코드·총점을 읽는다. 그다음
  날짜를 `Latest Report` 로 바꿔 세 카드의 코드를 읽고 각 카드의 네이버 링크와 대조한다.
- 기대: 2026-02-11 은 `S LG전자 066570 17점`, `B 아크릴 0007C0 16점`,
  `B 메리츠금융지주 138040 8점`, `B 현대제철 004020 8점` 네 장이다.
  Latest Report 는 `한화생명 088350`, `LG에너지솔루션 373220`, `삼성중공업 010140` 세 장이며
  카드 코드와 네이버 링크의 코드가 순서까지 같다. 앞을 0 으로 채우는 동작이 살아 있어
  `10140` 이 아니라 `010140` 이다.
- 결과: 통과. 2026-02-11 은 `S LG전자 066570 17점`, `B 아크릴 0007C0 16점`,
  `B 메리츠금융지주 138040 8점`, `B 현대제철 004020 8점` 네 장이다. Latest Report 는
  `B 한화생명 088350 10점`, `B LG에너지솔루션 373220 8점`, `B 삼성중공업 010140 7점`
  세 장이며, 일곱 장 모두 카드 코드와 네이버 링크 코드와 토스 링크 코드가 순서까지
  같다. 삼성중공업이 `10140` 이 아니라 `010140` 이므로 앞을 0 으로 채우는 동작이 살아
  있다.

### S-5. 날짜를 오가도 코드가 유지된다 (인접)
- 조작: `2026-02-11` → `2026-02-23` → `Latest Report` → `2026-02-11` 순서로 날짜를 바꾸며
  매번 아크릴 또는 삼성에피스홀딩스의 카드 코드를 읽는다.
- 기대: 마지막에 되돌아온 `2026-02-11` 의 아크릴 코드가 처음과 같은 `0007C0` 이다. 날짜를
  오가는 사이에 코드가 숫자로 바뀌거나 카드가 사라지지 않는다.
- 결과: 통과. `2026-02-11`(4장, 아크릴 `0007C0`) → `2026-02-23`(26장,
  삼성에피스홀딩스 `0126Z0`) → `Latest Report`(3장) → `2026-02-11`(4장, 아크릴
  `0007C0`) 로 오갔다. 되돌아온 2026-02-11 의 아크릴 코드가 처음과 같은 `0007C0` 이고
  카드 수도 4장 그대로다. 오가는 사이에 코드가 숫자로 바뀌거나 카드가 사라지지
  않았다.

### S-6. 필터를 걸어도 영문자 코드 카드가 제 값을 유지한다 (인접)
- 조작: 날짜를 `2026-02-24` 로 바꾸고 「총점」 필터를 `10점 이상` 으로 건다. 남은 카드의
  코드를 순서대로 읽고 네이버 링크의 코드와 대조한다.
- 기대: 13장이 남고 코드 순서는 `006400, 000660, 005380, 489790, 006340, 009150, 008930,
  010130, 096770, 005070, 0126Z0, 298040, 222080` 이다. 열한 번째가 `0126Z0` 이며 네이버
  링크의 코드가 카드와 순서까지 같다.
- 결과: 통과. 필터를 걸기 전 39장이 걸고 나서 13장이 되었고, 코드 순서가
  `006400, 000660, 005380, 489790, 006340, 009150, 008930, 010130, 096770, 005070,
  0126Z0, 298040, 222080` 으로 기대와 완전히 같다. 열한 번째가 삼성에피스홀딩스
  `0126Z0` 이며 열세 장 모두 카드 코드와 네이버·토스 링크 코드가 일치한다.

### S-7. 열세 개 날짜 전부에서 카드 코드와 두 외부 링크가 일치한다 (인접)
- 조작: 날짜 선택 상자의 옵션을 처음부터 끝까지 하나씩 골라, 날짜마다 카드에 적힌 코드
  목록과 네이버 링크의 코드 목록과 토스 링크의 코드 목록 세 벌을 순서까지 맞춰 대조한다.
- 기대: 열세 날짜 모두 세 목록이 완전히 같다. 카드 수는 순서대로 3, 2, 2, 0, 9, 13, 35,
  39, 26, 18, 19, 6, 4 이며, 영문자 코드는 2026-02-26·02-24·02-23 에 `0126Z0`,
  2026-02-20 에 `0015N0`, 2026-02-11 에 `0007C0` 다. 자료가 없는 2026-05-22 는 카드가
  0장이다.
- 결과: 통과. 카드 수가 순서대로 3, 2, 2, 0, 9, 13, 35, 39, 26, 18, 19, 6, 4 로
  기대와 같다. 열세 날짜에 걸친 176장 전부에서 카드 코드와 네이버 링크 코드와 토스 링크
  코드 세 벌이 완전히 같아, 불일치가 한 건도 없다. 영문자 코드는 2026-02-26·02-24·02-23
  에 `0126Z0`, 2026-02-20 에 `0015N0`, 2026-02-11 에 `0007C0` 로 기대한 자리에만
  나타난다. 자료가 없는 2026-05-22 는 카드가 0장이고 빈 상태 문구가 뜬다.

## 이월한 발견

`/qa-only` 리포트가 지적한 넷은 모두 `[JONGGA-017]` 의 범위 밖입니다. 이 항목이 고치기로 한
것은 종목코드 정규화이고, 넷은 표기와 기준일과 호출 횟수의 문제입니다. 셋은 이미 백로그에
있어 새로 올리지 않았습니다.

- ISSUE-001 「카드의 「종가」 타일이 날짜에 따라 다른 날의 종가를 가리킨다」 →
  `[JONGGA-031]` 로 새로 올렸습니다. 기존 `[JONGGA-023]` 은 툴팁 문구를 맞추는 항목이고
  이것은 같은 타일이 날짜에 따라 다른 필드를 그리는 동작 문제여서 별개입니다.
- ISSUE-002 「점수표의 매매 원칙(+9%/-5%)과 카드의 전략 포인트(+5%/-3%)가 어긋난다」 →
  이미 `[JONGGA-020]` 에 같은 사실이 적혀 있습니다. 새 항목을 만들지 않았습니다.
- ISSUE-003 「자료 없는 날짜의 빈 상태가 영어이고 안내가 사실과 다르다」 → 이미
  `[JONGGA-023]` 과 `[FE-025]` 가 담고 있습니다. `[FE-025]` 의 첫 체크박스가 「필터로 0장이
  된 것인지 자료가 없는 것인지 구분되게 안내 문구를 나눔」이어서 이번에 찾은 자료 없는
  날짜의 경우까지 포함합니다. 새 항목을 만들지 않았습니다.
- ISSUE-004 「화면 진입 시 `dates` 와 `latest` API 를 각각 두 번 호출한다」 → 백로그에
  올리지 않았습니다. 개발 모드의 React Strict Mode 가 효과를 두 번 실행시키는 동작일 수
  있어 결함인지 자체가 미확정입니다. 운영 빌드에서 같은 기록이 나오는지 확인한 뒤에
  판정할 사안입니다.

## 실행 결과

2026-09-04 07:10 에 `/qa` 로 일곱 건을 번호 순서대로 실행했고 전부 통과했습니다. 실패한
시나리오가 없으므로 이 단계에서 고친 코드는 없습니다.

- 검사 범위: 날짜 옵션 열세 개에 걸친 카드 176장. 각 카드마다 카드에 적힌 코드와 네이버
  링크 코드와 토스 링크 코드 세 벌을 대조했고, 영문자 코드를 가진 세 종목은 상세 모달
  머리글까지 확인했습니다.
- 코드 불일치: 0건.
- 콘솔 오류: 0건. 네트워크 요청 101건이 모두 200 입니다.
- 증거 그림: `.gstack/qa-reports/screenshots/jongga017-qa-s1-acryl-2026-02-11.png`(카드),
  `jongga017-qa-s1-acryl-modal.png`(모달 머리글).

이번 실행에서 시나리오 문구 하나를 바로잡았습니다. S-2 의 기대에 적은 「`000150` 은
나타나지 않는다」가 화면 전체를 두고 한 말처럼 읽히는데, 2026-02-20 에는 두산이 실재하며
그 코드가 `000150` 입니다. 화면 전체가 아니라 아로마티카 카드 안을 두고 한 말이어야
맞습니다. 이 자리는 결함이 아니라 오히려 이번 수정을 가장 잘 드러내는 자리입니다. 고치기
전이었다면 아로마티카가 두산과 같은 코드로 바뀌어, 한 화면에 `000150` 을 단 카드가 두 장
생겼을 것이기 때문입니다.

같은 이유로 회귀 검사에 남길 값을 하나 더 확인했습니다. 아로마티카 카드 안에서 `000150`
이 0번, 두산 카드 안에서 `0015N0` 이 0번입니다. 두 카드가 서로의 코드를 조금도 침범하지
않습니다.

# [VCP-043] signal_tracker 경로의 AI 추천 선택을 수집·재분석 규칙으로 통일 — QA 기록

- 대상: `engine/signal_tracker_ai_helpers.py`(`apply_ai_results` 가 `_extract_vcp_ai_recommendation` 에 선택을 맡기고 `ai_provider` 열을 쓰지 않음), `README.md` 계약 설명
- 단계(phase): 완료 | 시나리오 구성 완료 | 실행 완료
- 구성 2026-09-24 17:12 | 실행 2026-09-24 17:13:06~17:13:08
- 검증 기준 커밋: `c856c69` (이 문서를 담은 첫 커밋). 대조는 수정 전 커밋 `43037d0`. 사본은 각 커밋의 `git archive` 이며 실행 중 범위 파일은 바뀌지 않았다
- 판독 주의: 하네스의 `pd.read_csv` 는 기본값으로 문자열 `N/A` 를 결측으로 읽어 표에 NaN 으로 보인다. 아래 실제는 저장된 CSV 원문 행으로 판정했다
- 구성 근거: 설계 승인(대화 17:07), 호출 경로 추적(`run.py` 메뉴 2 → `SignalTracker.analyze_signals_with_ai` → `apply_ai_results` → 저장 확인 뒤 `_append_to_log` → `signals_log.csv`)
- QA 엔진(engine): Claude Code. browser_applicability: 하네스 대체. 이 경로는 대화형 CLI 이고 화면·API 진입점이 없다. 실제 `run.py` 메뉴 2 는 스캔과 LLM 호출이 붙어 있어 실행하지 않고, 같은 순서(`analyze_signals_with_ai` → `_append_to_log`)를 하네스로 부른다
- 격리: 기준 커밋의 `git archive` 사본에서 `secrets/`·`data/`·`.env` 를 지우고 사본 `data/` 에 원본 `signals_log.csv` 만 복사한다. 분석기는 `get_vcp_analyzer` 를 가짜로 바꿔 고정 결과를 돌려준다. LLM·네트워크 호출 없음. 원본 `data/`·`.env`·3500·5501 은 쓰지 않는다
- 합성 자료(하네스 안): 종목 900001(Gemini 실패 dict + GPT `buy`/72), 900002(둘 다 실패 dict), 900003(Gemini HOLD/55 + GPT SELL)
- 하네스: 세션 스크래치의 `vcp043qa_harness.py`

## 시나리오

### S-1. Gemini 가 실패 dict 여도 뒤의 유효한 GPT 판정이 저장된다 (하네스, 필수)
- 조작: 하네스 실행 뒤 `signals_log.csv` 에서 900001 행을 읽는다
- 기대: 수정 뒤 `ai_action=BUY`, `ai_confidence=72`, `ai_reason=수급 개선과 수축 확인`. 수정 전 사본은 `N/A`·0·`분석 실패`
- 실제: 수정 뒤 원문 `…,BUY,72.0,수급 개선과 수축 확인,…`. 수정 전 사본 원문 `…,N/A,0.0,분석 실패,…,gemini`. 통과

### S-2. 실패 dict 만 있으면 확신도 없이 실패로 저장되고, 유효한 Gemini 는 그대로 우선한다 (하네스, 필수)
- 조작: 같은 실행에서 900002·900003 행을 읽는다
- 기대: 900002 는 `N/A`·확신도 빈 칸·`분석 실패`, 900003 은 `HOLD`·55·`돌파 전 관망`. 수정 전 사본의 900002 는 확신도 0(실패 dict 의 값)
- 실제: 수정 뒤 900002 원문 `…,N/A,,분석 실패,…`(확신도 빈 칸), 900003 원문 `…,HOLD,55.0,돌파 전 관망,…`. 수정 전 사본 900002 는 `N/A,0.0,분석 실패`, 900003 은 같은 HOLD/55. 통과

### S-3. 저장 파일에 `ai_provider` 열이 새로 생기지 않는다 (하네스, 필수)
- 조작: 저장 전후 `signals_log.csv` 머리글을 비교한다
- 기대: 수정 뒤 전후 모두 `ai_provider` 없음, 행 수는 3 늘어남. 수정 전 사본은 저장 뒤 `ai_provider` 열이 생긴다
- 실제: 수정 뒤 `rows: 15 -> 18`, `ai_provider in header before/after: False False`. 수정 전 사본은 `False True` 이고 세 행 모두 `gemini`(Gemini 실패 dict 를 고른 900001·900002 포함). 통과

## 정리
- 실제: 사본 둘(`c856c69`·`43037d0`) 삭제 뒤 없음 확인. 서버를 띄우지 않았고 3500·5501 리스너 0. 원본 `data/signals_log.csv` 수정 시각 전후 같음(1789994506, 읽기 복사만). `analyze_batch` 는 가짜라 LLM·네트워크 호출 없음. 결과: 필수 3/3 통과

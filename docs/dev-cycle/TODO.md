# TODO

> 백로그의 단일 관리 지점입니다. 형식은
> `.claude/skills/dev-cycle/references/archive-format.md` 를 따릅니다.
> 완료된 항목은 아카이브로 옮기고 이 파일에서 제거합니다.
> 진행은 `/dev-cycle next` 로 시작합니다.
>
> 2026-09-01 여섯 카테고리 감사(`[INFRA-004]`)로 30개 항목이 들어왔습니다. 각 항목의
> 근거에 적힌 `AUDIT-*` 문서는 `docs/dev-cycle/audits/` 에 있으며, 항목마다 위치를
> 절 번호까지 적어 두었으므로 사이클을 시작할 때 그 절을 먼저 읽습니다.

## P0 — 즉시

비어 있습니다. `[CHAT-021]` 을 마지막으로 P0 이 정리되었습니다.


## P1 — 이번 주기

### [INFRA-033] 개발 라운드 설계와 Codex UltraQA 흐름을 연결한다
- 카테고리: 인프라 | 티어: 문서 | 근거: 2026-09-06 사용자 요청과 전체 워크플로 리뷰
- 설계 승인: 2026-09-06 | 승인 확인: 2026-09-06T20:32:31+09:00 | 경로: architectural | 범위: dev-cycle·dev-workflow 규약과 격리 실제 검증 | 근거: 현재 대화의 설계 제안에 대한 사용자 「진행해」
- 설계: docs/superpowers/specs/2026-09-06-dev-cycle-brainstorming-ultraqa-design.md
- 상태: RESUME — Codex App 필수 8/8 통과·독립 검수 APPROVE·정리 완료, 최종 아카이브 대기
- 검증 기록: docs/dev-cycle/qa/INFRA-033-recheck.md (App 필수 8/8 PASS). 초기 STOPPED 6/9 이력은 INFRA-033.md에 보존
- [x] brainstorming 설계와 사용자 승인 확인
- [x] 라운드·재개·승인·필수 QA 실패·감사 읽기 전용 계약 반영
- [x] Codex UltraQA와 App 상태 대응, Claude Code 기존 도구 매핑 정리
- [x] 격리 저장소 실제 흐름과 적대적 시나리오 실행 (필수 전체 통과 아님)
- [x] 독립 리뷰와 정적 검사, 증거·정리 확인 (임시 app-server 13개 종료, fixture 제거)
- [x] 구현 커밋 `4eaba62` 기록
- [x] T3 리뷰의 제한·검토 증거 재사용과 전용 호출·큰 입력 검증을 보완한 새 평가 계획: docs/dev-cycle/qa/INFRA-033-recheck.md
- [x] 필수 미통과 원인 수리, 실제 T3 전체 마감·전용 역할·입력/실패/커밋 게이트 회귀 통과
- [x] 소유 app-server 19개 종료·임시 worktree/fixture 제거, 원본 OMX 상태와 미추적 package.json 보존
- [ ] 최종 아카이브 커밋에서만 TODO 항목 제거


### [INFRA-034] 감사 초안과 설계의 경계를 밝히고 oh-my-claudecode 를 사이클에 연결한다
- 카테고리: 인프라 | 티어: 문서 | 근거: 2026-09-06 사용자 요청과 oh-my-claudecode 5.3.0 스킬·에이전트·도구·훅 대조 검토
- 설계 승인: 2026-09-06 | 승인 확인: 2026-09-06T23:20:38+09:00 | 경로: bounded | 범위: `dev-workflow` 초안·설계 경계와 다음 단계, 실행 환경 표에 `critic`·`security-reviewer`·`trace`·`session_search` 연결, OMC 키워드 감지 주의 | 근거: 현재 대화의 설계 제안에 대한 사용자 선택 응답 (네 후보 모두 적용, 커밋은 사용자가 수행)
- 검토 결과: 스킬 29개·에이전트 19개·MCP 도구 11묶음·훅 10이벤트 가운데 위 넷만 채택. `autopilot`·`ralph`·`team`·`ultragoal`·`self-improve`·`launch` 는 설계 승인 게이트나 단일 백로그와 충돌, `wiki`·`remember`·`planner`·`notepad` 는 Git 미추적 `.omc/` 에만 기록, `code-simplifier`·`git-master` 는 `/ponytail-review` 와 두 커밋 구조에 충돌해 제외
- [ ] `dev-workflow.md` 에 초안·설계 경계와 저장 뒤 다음 단계 기록
- [ ] `SKILL.md` 실행 환경 표와 [0] 3번·[1] 4번·[3] 8번에 네 도구 연결, OMC 키워드 주의 문단
- [ ] `tier-rules.md` §1 시크릿 문단에 `security-reviewer` 보강
- [ ] `CLAUDE.md` 개발 사이클 절과 `codex-setup.md` §3 대응표 교차 반영
- [ ] 문서 검증: 참조 경로·절·도구 실재 확인, `git diff --stat` 코드 무변경, 네 자리 교차 확인, TODO 티어 재판정 불필요 확인
- [ ] 첫 커밋과 완료 아카이브 (사용자 커밋 뒤)


### [CHAT-022] 메모리 전체 동기화가 다른 워커가 저장한 행을 지운다
- 카테고리: 챗봇 | 티어: T2 | 근거: 2026-09-05 `[CHAT-017]` 사이클의 코드 리뷰
- `chatbot/storage_memory_manager.py` 의 `_save()` 가 `save_memories_to_sqlite` 에 자기
  스냅샷을 통째로 넘기고, `_delete_stale_memory_rows_cursor` 가 그 스냅샷에 없는 행을
  전부 지웁니다. 그런데 `self.memories` 는 `__init__` 에서 한 번 적재한 뒤 다시 읽지
  않습니다. `_load` 를 부르는 곳이 생성자뿐입니다.
- gunicorn 워커 두 개가 각자 다른 스냅샷을 들고 있으므로, 워커 B 에서 단건 저장이 실패해
  `_save()` 로 넘어가면 워커 A 가 저장한 다른 사용자의 행이 사라집니다. 워커 A 에서
  `/memory add` 한 값이 워커 B 로 간 요청의 `/memory view` 에 보이지 않는 문제도 같은
  원인입니다.
- 이 구조 자체는 `[CHAT-017]` 이 만든 것이 아닙니다. 다만 종전에 지워지던 것은 다시 만들 수
  있는 캐시 두 건이었고, 이제는 사용자가 직접 입력한 개인 메모리입니다. 피해의 성격이
  달라졌습니다.
- 티어 판정: `chatbot/storage.py` 의 히스토리 쪽이 `_mark_session_changed` 로 델타만 반영하는
  구조를 이미 갖고 있습니다. 메모리도 같은 방식으로 바꿀지, 아니면 `_save()` 의 stale 정리
  범위를 자기가 건드린 소유자로 좁힐지 정해야 합니다.
- [ ] 전체 동기화가 다른 워커의 행을 지우지 않도록 범위를 좁힘
- [ ] 워커가 다른 워커의 저장을 읽을 수 있는지 확인하고 필요하면 재적재 경로를 둠
- [ ] 두 워커를 흉내 낸 회귀 검사를 pytest 로 고정


### [FE-008] 대시보드 진입 시의 하이드레이션 불일치 제거
- 카테고리: 프론트엔드 공통 | 티어: T2 | 근거: QA 리포트 2026-09-02 ISSUE-001
- 티어 판정: 원인 위치를 아직 특정하지 못했습니다. 서버와 클라이언트가 서로 다른 값을
  그리는 자리를 찾아야 하며, 후보는 `마지막 업데이트` 처럼 로케일 시각을 그리는 표시입니다.
  건드릴 파일 수가 확정되지 않아 `T2` 로 두고, 실제 범위가 넓어지면 상향합니다.
- 2026-09-05 `[CHAT-003]` 사이클의 `/qa` 가 챗봇 화면에서도 같은 성격의 중간 상태를
  실측했습니다. `/chatbot` 을 연 직후 약 3초 동안 인사말이 「안녕하세요, 님」으로 사용자
  이름이 빠진 채 보이고, 그 순간 `h1` 이 두 개이며 DOM 의 버튼이 43개로 부풀어 있습니다.
  3초 뒤에 「안녕하세요, 흑기사님」과 `h1` 하나, 버튼 25개로 정착합니다. 이 항목이 대시보드를
  고칠 때 챗봇 화면도 함께 봅니다.
- [ ] 하이드레이션 경고를 내는 서브트리를 특정 (React DevTools 또는 경고 스택 추적)
- [ ] 서버와 클라이언트가 같은 값을 그리도록 수정 (시각 표시는 마운트 후 렌더 등)
- [ ] `/dashboard/kr` 진입 시 콘솔 오류 0건 확인
- [ ] `/chatbot` 진입 직후 인사말에 사용자 이름이 빠지지 않는지 확인
- [ ] `/qa-only` 실행

### [CHAT-007] 사이드바 대화 항목을 키보드로 열 수 있게 만든다
- 카테고리: 챗봇 | 티어: T1 | 근거: QA 리포트 2026-09-02 ISSUE-002
- 티어 판정: `frontend/src/app/chatbot/page.tsx` 의 사이드바 목록 렌더 두 곳만 바꿉니다.
  `div` 를 `button` 으로 바꾸거나 `role`·`tabIndex`·키 핸들러를 붙이는 정도라 50줄 아래입니다.
  `[CHAT-004]` 가 이 파일을 쪼개기 전에 끝내는 편이 충돌이 적습니다.
- 2026-09-05 `[CHAT-003]` 사이클의 `/qa-only` ISSUE-001 을 여기에 합쳤습니다. 같은 파일의
  같은 성격(스크린 리더가 읽을 이름)이라 따로 세우면 한 파일을 두 번 건드리게 됩니다.
  하단 입력 영역의 버튼 넷이 접근성 트리에서 이름이 비어 있습니다. 보내기
  (`fas fa-paper-plane`)와 모바일 사이드바 열기(`fas fa-bars`)는 `title` 조차 없고,
  파일 첨부(`fas fa-plus-circle`)와 음성 입력(`fas fa-microphone`)은 `title` 만 있어
  접근 가능한 이름으로 계산되지 않습니다. 보내기 버튼을 식별하지 못하면 질문을 입력하고도
  보낼 수단을 찾지 못하고, 모바일에서는 햄버거가 사이드바를 여는 유일한 수단입니다.
- 2026-09-05 `[CHAT-004]` 사이클의 `/qa-only` 가 같은 상태를 다시 확인했고, 대상이 하나 더
  있다는 것을 찾았습니다. 답변 위에 있는 탭 둘(「스마트머니봇」과 사용자 이름)도 대화 항목과
  똑같이 `div` 에 `onclick` 만 달려 있고 `tabindex`·`role`·`onkeydown` 이 모두 없습니다.
  `[CHAT-004]` 가 `page.tsx` 를 쪼갠 뒤이므로 이제 대화 항목은 `page.tsx` 에 그대로 있고
  답변 영역은 `ChatMessage.tsx` 로 옮겨 갔습니다. 어느 파일을 고칠지 확인하고 시작합니다.
- 2026-09-05 `[CHAT-016]` 사이클의 `/qa-only` 가 이 결함의 대가를 실제로 치렀습니다.
  대화 목록이 있을 때 접근성 트리에 이름 없는 버튼 다섯 개(`[button] ""`)만 보여 어느 것이
  대화 항목이고 어느 것이 삭제 버튼인지 구분되지 않습니다. 첫 항목을 대화로 알고 눌렀더니
  삭제 확인 모달이 떴습니다(취소해 아무것도 지우지 않았습니다). 대화를 열려면 접근성
  트리에 없는 커서 요소를 눌러야 했습니다. 되돌릴 수 없는 삭제와 일반 항목이 보조 기술에서
  똑같이 보이는 상태이므로, 아래 첫 항목에 **대화 제목을 접근 가능한 이름으로 노출**하는
  것과 **삭제 버튼에 `aria-label` 을 붙이는** 것을 함께 넣습니다.
- 2026-09-05 `[CHAT-017]` 사이클의 `/qa-only` 가 다시 확인했는데 범위가 줄었습니다. 파일
  첨부와 음성 입력은 `title` 이 접근 가능한 이름으로 계산되어 이제 이름이 있습니다. 버튼
  25개 가운데 이름이 없는 것은 모바일 사이드바 열기(`fas fa-bars`) 하나뿐입니다. 보내기
  버튼은 입력이 비어 있을 때 렌더되지 않아 이번 실측에서는 확인하지 못했습니다.
- [ ] 대화 항목에 포커스가 가고 Enter 로 열리도록 수정. 제목이 접근 가능한 이름이 되게 함
- [ ] 삭제 버튼에 `aria-label` 을 붙여 항목과 구분되게 함
- [ ] 답변 위 탭 둘도 같은 방식으로 키보드 조작이 되도록 수정
- [ ] 삭제 버튼이 항목 안에 중첩되지 않도록 마크업 정리 (버튼 안의 버튼은 무효한 HTML)
- [ ] 입력 영역의 버튼 넷에 `aria-label` 을 붙임 (보내기·햄버거·파일 첨부·음성 입력)
- [ ] `/qa-only` 실행

### [VCP-008] 히스토리 날짜 목록과 시그널 조회가 서로 다른 자료를 본다
- 카테고리: VCP 시그널 | 티어: T2 | 근거: [JONGGA-002] 사이클의 qa-only 실측
- 관찰: `GET /api/kr/signals/dates` 는 `["2026-05-05"]` 를 돌려주는데 같은 날짜로
  `GET /api/kr/signals?date=2026-05-05` 를 부르면 `{"count": 0, "source": "no_data"}`
  가 돌아옵니다. `data/` 에 `jongga_v2_results_20260505.json` 은 있고
  `vcp_signals_results_20260505.json` 은 없습니다. 날짜 목록은 종가 계열 파일에서
  만들어지고 시그널 조회는 VCP 파일을 찾습니다.
- 화면에서는 유일하게 제시된 히스토리 날짜를 눌러도 `No signals found.` 만 보이고,
  왜 비었는지 알려 주는 안내가 없습니다. 오늘 탭에 있는 안내가 히스토리 탭에는 없습니다.
- [ ] 날짜 목록을 만드는 근거를 VCP 시그널 파일 존재 여부와 맞춤
- [ ] 히스토리 탭에서도 데이터가 없는 이유를 알리는 안내 표시
- [ ] 두 응답이 어긋나지 않는지 확인하는 회귀 검사 추가
- [ ] agent-browser 로 히스토리 날짜 선택 결과를 실측

### [VCP-018] legacy 분석 파일의 GPT 추천이 VCP 화면에 닿지 못한다
- 카테고리: VCP 시그널 | 티어: T2 | 근거: `[VCP-004]` 사이클의 code-review
- 관찰: `app/routes/kr_market_vcp_signal_helpers.py:316-336` 의
  `_merge_legacy_ai_fields_into_map` 은 `perplexity_recommendation` 과
  `gemini_recommendation` 두 필드만 보강합니다. 그런데 실제 legacy 파일인
  `data/kr_ai_analysis.json` 을 열어 보면 네 항목 모두 `gpt_recommendation` 을 갖고 있고
  `perplexity_recommendation` 은 한 건도 없습니다. 보강 대상이 자료와 정반대로 어긋나
  있습니다.
- 영향: `frontend/src/app/dashboard/kr/vcp/page.tsx:430` 이 GPT 탭을 보여줄지 판정하고
  `:950` 과 `:1694` 가 그 값을 읽습니다. 그러므로 오늘 분석 파일에 GPT 판정이 없고 legacy
  에만 있는 종목은 화면에서 GPT 탭 자체가 사라집니다. 같은 자리를 지나는
  `_merge_ai_data_into_vcp_signals`(`:352`)는 세 프로바이더를 모두 내보내므로, 비대칭은
  보강 함수 한 곳에만 있습니다.
- 확인할 것: 이 비대칭이 의도인지 먼저 가립니다. legacy 파일이 만들어지던 시점에 GPT
  프로바이더가 없었다면 뒤에 추가된 필드를 보강 목록에 넣지 않은 누락이고, VCP 화면이
  legacy 의 GPT 판정을 일부러 배제하기로 한 것이라면 `perplexity` 쪽이 죽은 코드입니다.
  어느 쪽이든 지금 상태는 둘 다 아닙니다.
- [ ] `data/kr_ai_analysis.json` 을 만드는 경로를 찾아 세 필드가 언제 채워지는지 확인
- [ ] 보강 목록을 자료와 화면이 실제로 쓰는 필드에 맞춤
- [ ] 세 프로바이더의 보강 규칙이 같은지 검사하는 회귀 테스트 추가
- [ ] agent-browser 로 VCP 화면의 AI 탭 구성을 실측

### [JONGGA-008] 백엔드가 확신도 없음을 0 으로 표현하는 자리를 정리
- 카테고리: 종가베팅 | 티어: T2 | 근거: 2026-09-02 JONGGA-004 진행 중 발견
- `[JONGGA-004]` 에서 화면 쪽은 확신도가 없는 상태를 값 없음으로 표현하고 "미산출" 로
  그리도록 고쳤습니다. 그런데 백엔드는 값이 없을 때 여전히 0 을 채워 보내므로, 그 응답을
  받으면 화면은 "AI 가 0% 확신한다" 로 읽히는 막대를 그립니다. AI 가 실제로 0 을 낸
  경우와 값이 아예 없는 경우가 구분되지 않습니다.
- 해당하는 자리는 다섯 곳입니다. 마지막 두 곳은 `[JONGGA-004]` 의 code-review 가 짚었습니다.
  - `app/routes/kr_market_jongga_normalize_helpers.py:131` 의 `signal.get("ai_confidence", 0)`
  - `app/routes/kr_market_jongga_ai_payload_helpers.py:37` 의 사유가 문자열일 때 붙는 `confidence: 0`
  - `app/routes/kr_market_vcp_signal_helpers.py:178` 의 `_safe_int(..., default=0)`
  - `app/routes/kr_market_jongga_reanalysis_helpers.py:123` 의 `matched_result.get("confidence", 0)`
  - `engine/llm_analyzer_parsers.py:102` 의 `item.get("confidence", 0)`. 여기는 형 변환도
    없어서 LLM 이 낸 문자열이 그대로 흐릅니다. `engine/vcp_ai_analyzer_helpers.py:221` 의
    `_normalize_confidence_value` 가 같은 일을 이미 하고 있으므로 그것을 재사용합니다
- 티어 판정: `_normalize_confidence_value` 를 그대로 호출만 하면 T2 이지만, 재사용하려고
  `engine/vcp_ai_analyzer_helpers.py` 를 한 줄이라도 고치면 §2 의 「VCP 판정」에 닿아 T3 이
  됩니다. 계획 단계에서 어느 쪽인지 먼저 정합니다. 2026-09-02 `[INFRA-013]` 의 백로그
  대조에서 확인했습니다.
- [ ] 다섯 자리가 값 없음을 `None` 으로 돌려주도록 수정하고, 값이 있으면 숫자로 정규화
- [ ] `tests/app/test_vcp_failed_ai_filter.py` 가 `ai_confidence: 0` 을 실패 판정의 입력으로
      쓰고 있으므로 판정 기준이 어긋나지 않는지 확인
- [ ] VCP 화면의 확신도 표시도 값 없음을 처리하는지 확인하고 필요하면 함께 수정
- [ ] pytest·vitest 전체 통과와 두 화면 실측

### [FE-015] 모바일에서 VCP 표의 빈 상태 문구가 화면 밖에 놓인다
- 카테고리: 프론트엔드 | 티어: T1 | 근거: 2026-09-02 VCP-009 마감 qa-only
- 빈 상태 문구를 담은 `td` 의 폭이 995px 인데 그것을 감싼 가로 스크롤 영역의 표시 폭은
  341px 입니다. 문구가 995px 안에서 가운데 정렬되므로 중심이 x 좌표 약 514 에 놓이고,
  375px 폭의 화면에서는 완전히 밖으로 밀려납니다.
- 최신 화면에서는 표 위의 경고 배너가 비어 있는 이유를 알려 줍니다. 그러나 `과거` 에서
  날짜를 고르면 그 배너가 사라지므로, 모바일에서는 아무 설명 없는 빈 상자만 남습니다.
  데이터가 없는 상황과 화면이 고장 난 상황을 구분할 수 없습니다.
- [ ] 빈 상태 문구를 스크롤 영역 밖으로 옮기거나 뷰포트 폭에 맞춰 고정
- [ ] 375×812 에서 최신과 과거 날짜 양쪽 모두 문구가 보이는지 확인

### [VCP-011] VCP 상세의 AI 요약이 표·차트와 다른 수축비율과 점수를 말한다
- 카테고리: VCP 시그널 | 티어: T2 | 근거: 2026-09-02 VCP-010 진행 중 브라우저 실측
- `2026-05-05` 의 SK(034730) 상세에서 표와 차트 하단은 수축비율 `0.41` 을 보여 주는데,
  같은 패널의 Gemini 요약문은 "수축 비율 1.8811" 이라고 적습니다. 점수도 어긋나서,
  `VCP SCORE` 카드는 `100.0` 인데 요약문은 "VCP 패턴 보조 점수가 3.0점으로 다소 낮게
  측정되었으나" 라고 말합니다.
- `data/kr_ai_analysis_20260505.json` 의 해당 항목에는 `contraction_ratio` 와
  `vcp_score` 가 모두 `None` 이고, `1.8811` 은 `gemini_recommendation` 의 본문 안에만
  있습니다. 즉 AI 를 호출할 때 넘긴 값이 `signals_log.csv` 의 값과 달랐습니다.
- [ ] AI 프롬프트에 싣는 수축비율과 VCP 점수의 출처를 표·차트와 같은 자리로 통일
- [ ] 프롬프트에 넘긴 값이 응답 저장분에도 남는지 확인하는 검사 추가

### [VCP-012] 차트 조회 응답의 도착 순서를 보장하지 않는다
- 카테고리: VCP 시그널 | 티어: T1 | 근거: 2026-09-02 VCP-010 마감 code-review
- `frontend/src/app/dashboard/kr/vcp/page.tsx` 의 `openChart` 에 요청 순번 가드가 없어서,
  먼저 보낸 요청의 응답이 늦게 도착하면 나중에 연 종목의 차트를 덮어씁니다. 종목 A 의
  상세를 열고 응답을 기다리는 동안 닫고 종목 B 를 열면, A 의 응답이 B 의 화면에 실립니다.
- `[VCP-010]` 이전에도 있던 문제이지만, 구간이 특정 날짜에 고정되면서 어긋난 화면이
  더 뚜렷하게 드러납니다.
- [ ] `AbortController` 나 요청 순번으로 늦게 온 응답을 버림
- [ ] 늦은 응답이 화면을 덮지 않는지 확인하는 검사 추가

### [FE-018] 모바일에서 VCP 상세 모달의 글자들이 서로 겹친다
- 카테고리: 프론트엔드 | 티어: T2 | 근거: 2026-09-02 VCP-010 마감 qa-only ISSUE-002
- 375×812 에서 상세 모달을 열면 세 영역이 한 자리에 포개집니다. 이동평균선 토글 바가
  AI 요약 본문을 덮고, 하단 정보 막대(`수축비율`·`전반부`·`후반부`)가 그 위에 다시
  겹쳐 두 글자가 서로 관통합니다. 차트 아래쪽 캔들과 `GPT / Gemini` 탭도 같은 높이에서
  만납니다.
- 읽을 수 있는 글자가 사실상 없으므로, 모바일에서는 상세 화면이 없는 것과 같습니다.
  1280px 에서는 차트와 AI 패널이 좌우로 나뉘어 문제가 드러나지 않습니다.
- [ ] 375px 폭에서 차트·이동평균선 토글·정보 막대·AI 패널이 세로로 쌓이도록 배치 수정
- [ ] 375×812 에서 각 영역의 글자가 겹치지 않는지 확인

### [JONGGA-012] 시그널을 저장할 때의 목표가·손절가 폭을 백테스트와 맞춘다
- 카테고리: 종가베팅 | 티어: T3 예상 | 근거: 2026-09-02 JONGGA-007 마감 qa-only ISSUE-001
- 화면이 실제로 그리는 폭은 진입가 대비 `+5.0% / -3.0%` 입니다. 진입가 134,400 원에
  목표가 141,120 원과 손절가 130,368 원이고, 2026-05-05 의 시그널 9건도 모두 같은
  비율입니다. 백테스트가 승패를 재는 `JONGGA_TARGET_PCT` 0.09 및 `JONGGA_STOP_PCT`
  0.05 와 다릅니다.
- JONGGA-007 이 맞춘 자리는 목표가가 비어 있을 때 채워 넣는 대체값이었습니다. 저장된
  시그널이 목표가를 항상 담고 있어서 그 경로는 실행되지 않습니다. 따라서 화면과
  백테스트의 기준 불일치는 시그널을 저장하는 시점의 폭을 봐야 해소됩니다.
- 사용자는 화면의 목표가를 보고 매매하고 누적 성과에서 승률을 확인하는데, 두 값이
  서로 다른 기준으로 계산되고 있습니다.
- 신호를 만드는 경로를 건드리면 `tier-rules.md` §2 의 「신호와 등급 결정」에 닿으므로
  T3 로 봅니다. 계획 단계에서 실제 수정 파일로 다시 판정합니다.
- [ ] 저장 시점에 목표가·손절가를 만드는 자리를 찾아 폭의 출처를 확인
- [ ] 폭을 공용 상수로 통일할지, 두 기준을 분리해 화면에 밝힐지 방향을 정함
- [ ] 정한 방향대로 반영하고 폭이 한 곳에서만 결정되는지 확인하는 검사 추가

### [INFRA-019] README 와 .env.example 의 스케줄러 서술이 실제 동작과 어긋난다
- 카테고리: 인프라 | 티어: T3 | 근거: 2026-09-02 INFRA-012 진행 중 실측
- `JONGGA_SCHEDULE_TIME` 을 읽는 파이썬 코드가 저장소에 없습니다. 15:20 단독 잡은
  커밋 0e0f895 「기존 중복 스케줄 제거」로 걷어내 17:00 체인에 합쳤고, `[INFRA-005]`
  가 복구를 검토한 끝에 철회했습니다. 그런데 문서 여섯 자리가 여전히 그 잡이 도는
  것처럼 적고 있습니다.
- 대상: `README.md:105`, `README.md:399`, `README.md:1434`, `README.md:1465`,
  `README.md:2049`, `.env.example:123`
- 운영자가 `.env` 에 `JONGGA_SCHEDULE_TIME` 을 적어 넣어도 아무 일이 일어나지 않는데,
  안내 문서는 그 값이 동작한다고 말합니다.
- 방향은 `[INFRA-005]` 가 이미 정했습니다. 15:20 은 단일가 시간대라 시그널 0건이 되기
  쉽고, 그러면 「신호 없음」 알림이 기준일 가드를 잠가 17:00 의 실제 알림을 그날 통째로
  막습니다. 따라서 잡을 되살리지 않고 문서에서 걷어냅니다.
- Market Gate 간격도 값이 서로 다릅니다. 코드 기본값은 `engine/config.py:280` 의 30분인데
  `.env.example:47` 은 5분이고, README 는 네 자리(`430`, `1433`, `1464`, `2048`)에서
  「기본 5분」이라고 적습니다.
- `.env.example` 을 건드리므로 `tier-rules.md` §2 의 「시크릿과 인증」에 해당해 T3 이며
  §1 의 시크릿 확인 세 가지를 검증에 더합니다.
- [ ] README 다섯 자리에서 15:20 단독 실행 서술을 걷어냄
- [ ] `.env.example:123` 의 `JONGGA_SCHEDULE_TIME` 줄 처리
- [ ] Market Gate 간격의 기본값 표기를 `engine/config.py:280` 과 맞춤
- [ ] 시크릿 확인 세 가지 수행


### [INFRA-017] 존재하지 않는 API 경로가 404 대신 500 을 돌려준다
- 카테고리: 인프라 | 티어: T2 | 근거: [FE-004] 사이클의 실측, [FE-002] 마감 qa-only ISSUE-001
- 관찰: `curl http://localhost:5501/api/health` 가 500 과 함께 본문에
  `"error": "Internal Server Error", "message": "404 Not Found: ..."` 를 돌려줍니다.
  존재하지 않는 경로이므로 404 가 나가야 하는데, 전역 예외 처리기가 `NotFound` 까지
  삼켜 500 으로 바꾸고 `logs/backend.log` 에 `CRITICAL SERVER ERROR` 로 남깁니다.
- 한 경로만의 문제가 아닙니다. 2026-09-02 실측에서 `/api/chat/sessions`,
  `/api/chatbot/sessions`, `/api/chat/history`, `/api/chat/session`,
  `/api/kr/chat/sessions` 다섯 개가 모두 같은 형태로 500 을 돌려주는 것을
  확인했습니다. 응답의 `type` 필드에는 `NotFound` 가 그대로 실려 나옵니다.
- 재현: `curl -i http://localhost:5501/api/chat/sessions` 를 실행하면 첫 줄이
  `HTTP/1.1 500 INTERNAL SERVER ERROR` 이고 본문에 `"type": "NotFound"` 가 실립니다.
- 호출하는 쪽이 「경로가 없음」과 「서버가 고장남」을 구분할 수 없습니다. 프론트엔드의
  재시도 로직은 5xx 를 일시적 장애로 보고 무의미하게 반복하게 되고, 운영 환경의
  모니터링에서는 경로 오타 하나가 서버 장애로 집계됩니다.
- 전역 오류 처리를 바꾸면 모든 API 응답의 상태 코드에 영향을 주므로 T2 로 봅니다.
  계획 단계에서 실제 수정 파일로 다시 판정합니다.
- [ ] 전역 예외 처리기가 `werkzeug.exceptions.HTTPException` 을 그대로 통과시키도록 수정
- [ ] 오탐 로그가 사라지는지 `logs/backend.log` 로 확인
- [ ] 없는 경로가 404 를, 실제 서버 오류가 500 을 돌려주는지 구분하는 회귀 검사 추가

### [JONGGA-014] 파이프라인 Phase 1 호출의 죽은 TypeError 폴백을 걷어낸다
- 카테고리: 종가베팅 | 티어: T3 | 근거: `[JONGGA-001]` 사이클에서 관찰
- 관찰: `engine/phases_pipeline.py:51-54` 가 Phase 1 을 호출할 때 `target_date` 를 받지
  못하는 구현을 대비해 `try` / `except TypeError` 폴백을 두고 있습니다. 예외를 잡으면
  `target_date` 없이 `self.phase1.execute(candidates)` 를 한 번 더 호출합니다.
- 걷어내야 하는 이유가 둘입니다. 첫째, `Phase1Analyzer.execute` 는 지금 `target_date` 를
  정식 인자로 받으므로 첫 호출이 `TypeError` 로 끝날 일이 없습니다. 죽은 코드입니다.
  둘째, `except TypeError` 는 시그니처 불일치뿐 아니라 페이즈 안쪽에서 발생한
  `TypeError` 까지 함께 삼킵니다. 그러면 후보 수백 개의 수집과 점수 계산을 통째로 다시
  실행하고, 두 번째 시도도 같은 오류로 실패합니다. 실행 시간이 두 배가 되고, 밖으로
  나가는 예외가 두 번째 호출의 것으로 바뀌어 원인을 추적하기 어려워집니다.
- `engine/phases_pipeline.py` 는 `tier-rules.md` §2 의 위험 경로이므로 T3 입니다.
- [ ] `Phase1Analyzer.execute` 의 시그니처를 확인해 폴백이 정말 죽은 코드인지 확정
- [ ] `try` / `except TypeError` 를 걷어내고 단일 호출로 정리
- [ ] 페이즈 안쪽에서 난 `TypeError` 가 삼켜지지 않고 그대로 올라오는지 확인하는 검사를
      `tests/engine/test_phases_pipeline_refactor.py` 에 추가

### [CHAT-008] 무료 사용량이 모델을 부르지 않는 슬래시 명령에도 차감된다
- 카테고리: 챗봇 | 티어: T2 | 근거: 2026-09-02 CHAT-002 마감 qa-only ISSUE-001
- `/status`, `/help`, `/clear` 는 모델을 호출하지 않습니다. 세 명령의 활동 로그 항목은 모두
  `token_usage: {}` 이고 백엔드 로그에도 모델 호출 흔적이 없습니다. 그런데도 한도 10회짜리
  무료 사용량이 명령마다 1회씩 줄어듭니다. `/help` 를 보내기 전후로 쿼터 API 가
  `remaining` 7 에서 6 으로, `usage` 3 에서 4 로 바뀌는 것을 확인했습니다.
- `/status` 와 `/help` 는 `[CHAT-002]` 이후 기록에도 남지 않으므로, 사용자가 인터페이스를
  익히려고 두 명령을 눌러 보면 흔적도 없이 무료 한도의 20% 가 사라집니다.
- 차감 지점은 `app/routes/kr_market.py` 의 `[QUOTA]` 로그를 남기는 자리입니다. 위험 경로가
  아니지만 과금 판정을 바꾸므로 실측이 필요해 T2 로 봅니다.
- [ ] 모델을 호출하지 않은 응답을 차감 대상에서 제외하는 판정 지점을 정함
- [ ] 슬래시 명령 다섯 종과 일반 질문의 차감 여부를 검사하는 pytest 추가
- [ ] `/status` 를 연속으로 보내도 쿼터가 줄지 않는지 브라우저로 실측

### [FLOW-011] 누적 수익률이라는 한 이름 아래 값이 두 개 존재한다
- 카테고리: 수급·백테스트 | 티어: T1 | 근거: 2026-09-03 FLOW-004 사이클의 `/qa-only` 실측
- 누적 성과 화면의 `누적 수익률 (S+A+B 합산)` 카드는 `+71.8%` 를 보여줍니다. 이 값은
  `kpi.roiByGrade` 의 등급별 `totalRoi` 를 더한 것입니다. 그런데 같은 응답에는
  `kpi.totalRoi = 50.8` 이라는 다른 값이 함께 들어 있고, 카드의 툴팁 제목은
  `누적 수익률 (Total ROI)` 이라 그 필드와 같은 이름을 내걸고 있습니다.
- 두 값의 차이가 21.0 포인트인데 화면만 보고는 어느 쪽이 이 화면이 말하려던 누적
  수익률인지 알 수 없습니다. `[FLOW-010]` 과 같은 파일을 건드리므로 함께 처리해도 됩니다.
- [ ] 두 값의 정의 차이를 확인하고 화면이 보여줄 값을 하나로 정함
- [ ] 쓰지 않기로 한 쪽은 응답에서 빼거나 이름을 구분되게 바꿈

### [FLOW-013] 「누적 추천수 180」인데 표에는 173건만 나온다
- 카테고리: 수급·백테스트 | 티어: T3 | 근거: 2026-09-03 FLOW-010 사이클의 `/qa-only` ISSUE-001
- QA 시나리오: 페이지 크기를 `200개` 로 바꾸면 표의 행 수가 「누적 추천수」와 같다
- 백엔드는 `grade="D"` 인 거래 7건을 포함해 180건을 보내는데, 화면은 S·A·B 세 등급만
  그려서 173행만 나옵니다. 마지막 페이지가 30행이어야 하는데 23행에서 끝납니다.
- 등급 카드 세 개의 합(16+26+131=173)도 「누적 추천수 180」과 7건 어긋납니다. 등급 필터에
  `D` 버튼이 없어 필터로도 꺼내 볼 수 없습니다.
- 빠진 7건은 2026-02-11~12 의 거래이며 6건 LOSS, 1건 WIN 입니다.
- 먼저 정할 것이 있습니다. **D 등급을 화면에 드러낼 것인가, 아니면 누적 성과의 집계
  대상에서 빼고 「누적 추천수」도 173 으로 맞출 것인가.** 앞을 고르면 등급 카드가 네 장이
  되고, 뒤를 고르면 `totalSignals` 를 세는 자리를 함께 고쳐야 합니다.
- [ ] 두 갈래 가운데 하나를 정하고 근거를 적음
- [ ] `aggregate_cumulative_kpis` 의 등급 집계와 `totalSignals` 를 그 결정에 맞춤
- [ ] 응답 구조가 바뀌면 `_CUMULATIVE_CACHE_SCHEMA_VERSION` 을 함께 올림
- [ ] 화면의 표와 등급 필터를 맞추고, 행 수와 「누적 추천수」가 같은 것을 vitest 로 고정

### [INFRA-027] 요청 헤더 한 줄이 그대로 신원이 되어 유료 쿼터를 우회한다
- 카테고리: 인프라 | 티어: T3 | 근거: 2026-09-04 `[INFRA-006]` 사이클의 /review
  보안 스페셜리스트 지적(확신도 90). 코드를 직접 열어 확인했다.
- `app/__init__.py:175` 가 `g.user_email = request.headers.get('X-User-Email')` 로
  헤더 값을 검증 없이 신원으로 삼는다. 서명도 만료도 확인하지 않는다.
- `services/kr_market_route_service.py:153-165` 는 그 값 하나로 두 가지를 판정한다.
  값이 없으면 401 을 돌려주고, 있으면 `usage_tracker.check_and_increment(user_email)` 로
  무료 10회 쿼터를 집계한다. 그래서 헤더 문자열만 바꾸면 쿼터가 새로 시작되어 Gemini
  분석을 무제한으로 부를 수 있고, 남의 이메일을 넣으면 그 사람의 쿼터를 대신 소진시킨다.
- 티어 근거: 인증 경계를 바꾸는 작업이고 `app/__init__.py` 의 `before_request` 를
  건드리므로 `tier-rules.md` §2 의 위험 경로에 닿는다.
- 2026-09-06 `[CHAT-021]` 사이클의 코드 리뷰가 같은 원인이 챗봇 데이터에도 열려 있음을
  확인했다. `services/kr_market_chatbot_request_helpers.py:16` 의 `resolve_chatbot_owner_id`
  가 `X-User-Email` 과 `X-Session-Id` 를 검증 없이 소유자로 삼는다. 그 이메일은
  `frontend/src/app/components/SettingsModal.tsx:376` 의 자유 입력 필드에서 오고 NextAuth
  세션과 대조하지 않으며, `frontend/next.config.js:11` 이 `/api/:path((?!auth).*)` 를
  Flask 로 그대로 넘겨 중간에 검사하는 자리도 없다. 그래서 남의 이메일을 아는 사람은
  헤더 하나로 그 사람의 대화와 프로필을 읽고 덮어쓸 수 있다. `[CHAT-017]`·`[CHAT-018]`·
  `[CHAT-021]` 이 막은 것은 모든 사용자가 한 벌을 공유하던 사고였고 고의 접근은 그대로
  열려 있다. 그 사실을 `resolve_chatbot_owner_id` 에 `# ponytail:` 주석으로 적어 두었다.
- [ ] `g.user_email` 을 신원 근거로 쓰는 자리를 전수 조사 (`app/__init__.py:186` 포함)
- [ ] `resolve_chatbot_owner_id` 도 같은 방식으로 신원을 확정하게 함
- [ ] 서버가 서명을 검증할 수 있는 자격 증명으로 신원을 확정하는 방식을 정한다
- [ ] 당장 도입이 어려우면 이 엔드포인트에 IP 단위 속도 제한을 먼저 건다
- [ ] 위조한 헤더가 쿼터를 우회하지 못함을 고정하는 검사 추가
- [ ] pytest 전체 통과 확인

### [INFRA-025] `/api/system/env` 가 인증 없이 서버 `.env` 를 읽고 쓴다
- 카테고리: 인프라 | 티어: T3 | 근거: 2026-09-04 FE-006 사이클의 code-review
- `app/routes/common_update_routes.py:221` 의 `manage_env` 에 권한 검사가 없습니다.
  `GET` 은 마스킹한 환경 변수 전체를, `POST` 는 `.env` 쓰기를, `DELETE` 는 민감 값과
  사용자 자료의 초기화를 아무 요청자에게나 허용합니다. `README.md` 는 이 경로를 관리자
  전용으로 서술하고 있어 문서와 코드가 어긋납니다.
- 설정 모달은 `Sidebar.tsx:371` 에서 모든 방문자에게 렌더되므로, 로그인하지 않은
  방문자도 화면을 통해 이 경로에 닿습니다.
- `.env` 로 시작하는 파일을 다루므로 `tier-rules.md` §2 「시크릿과 인증」에 따라 T3 이며,
  §1 의 시크릿 확인 세 가지를 검증에 더합니다.
- [ ] `useAdmin` 이 쓰는 `/api/admin/check` 와 같은 판정을 서버에서 하도록 데코레이터 추가
- [ ] 세 메서드 각각에 어떤 권한을 요구할지 정함. `GET` 도 마스킹된 값이나마 변수 이름
      전체를 노출하므로 함께 막을지 판단
- [ ] 권한이 없는 요청이 403 을 받는 것을 확인하는 테스트 추가
- [ ] 화면이 403 을 받았을 때 무엇을 보여줄지 정함

### [FE-032] 마스킹되어 돌아온 설정 값이 입력 칸에 값으로 들어가 저장이 조용히 무시된다
- 카테고리: 프론트엔드 공통 | 티어: T2 | 근거: 2026-09-04 FE-006 사이클의 code-review
- `[FE-006]` 이 API 키 두 필드에만 `apiKeyFieldProps` 를 적용했습니다. 같은 문제를 가진
  나머지 아홉 필드는 그대로입니다.
- `services/common_env_service.py` 의 `SENSITIVE_KEYWORDS` 가 부분 문자열로 매칭하므로
  「알림 센터」 탭의 여덟 필드 가운데 일곱이 마스킹되어 돌아오고, `SettingsModal.tsx` 는
  그 문자열을 입력 칸의 값으로 그립니다. 사용자가 뒤쪽만 고치면 `*` 가 남고,
  `update_env_file` 이 `*` 를 보고 기존 값을 유지하므로 저장한 줄 알지만 아무것도
  바뀌지 않습니다.
- 2026-09-04 브라우저 실측입니다. `SMTP_PORT` 만 `PORT` 가 `SENSITIVE_KEYWORDS` 에 없어
  마스킹되지 않습니다.

  | 라벨 | 입력 타입 | 값 길이 | 화면 노출 |
  |---|---|---:|---|
  | `TELEGRAM_BOT_TOKEN` | password | 46 | 점 처리 |
  | `TELEGRAM_CHAT_ID` | text | 14 | **별표가 평문으로 보임** |
  | `DISCORD_WEBHOOK_URL` | password | 121 | 점 처리 |
  | `SMTP_HOST` | text | 14 | **별표가 평문으로 보임** |
  | `SMTP_PORT` | text | 3 | `587` (마스킹 없음) |
  | `SMTP_USER` | email | 23 | **별표가 평문으로 보임** |
  | `SMTP_PASSWORD` | password | 21 | 점 처리 |
  | `EMAIL_RECIPIENTS` | text | 18 | **별표가 평문으로 보임** |

- `AI_PROVIDER` 와 `GOOGLE_SEARCH_ENGINE_ID` 는 **지금 `.env` 에 없어서 응답에 나오지
  않습니다.** 그래서 오늘 화면에서는 재현되지 않습니다. `.env` 에 넣는 순간
  `AI_PROVIDER` 는 `PROVIDER` 안의 `ID` 에 걸려 마스킹되고, 값 `gemini` 가 `******` 가
  되어 `SettingsModal.tsx:906` 의 `(envVars['AI_PROVIDER'] || 'gemini') === 'gemini'` 가
  늘 거짓이 되며, 공급자 버튼 셋 가운데 어느 것도 선택 표시가 되지 않습니다. 잠복
  결함이므로 함께 고칩니다.
- [ ] `SENSITIVE_KEYWORDS` 의 부분 문자열 매칭을 좁힐지, 아니면 화면에서 일괄로 다룰지 결정
- [ ] 여덟 필드를 `apiKeyFieldProps` 와 같은 경로로 보냄. 함수 이름이 `apiKey` 로
      좁으므로 이름도 함께 맞춤
- [ ] `AI_PROVIDER` 선택 표시가 값과 일치하는 것을 확인
- [ ] 마스킹 값이 입력 칸의 값으로 새어 나오지 않는 것을 vitest 로 고정

### [FE-033] 설정 모달의 라벨과 삭제 버튼에 접근 가능한 이름이 없다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-04 FE-006 사이클의 /qa-only 리포트
  ISSUE-002·003·004
- `SettingsModal.tsx` 의 `<label>` 열 개가 `htmlFor` 도 없고 입력 칸을 감싸지도 않습니다.
  입력 칸 쪽에도 `id` 와 `aria-label` 이 없어 연결이 0/10 입니다. 스크린 리더가 입력 칸을
  「편집 텍스트」로만 읽고, 라벨을 눌러 포커스를 주는 동작도 되지 않습니다.
- API 키 삭제 버튼 두 개는 아이콘만 있고 `aria-label` 이 없습니다. `title="API Key 삭제"`
  만 있는데 `title` 은 접근 가능한 이름의 마지막 대체 수단이라 스크린 리더에 따라 읽지
  않습니다. 누르면 되돌릴 수 없는 삭제가 일어나므로 이름이 확실해야 합니다. `[FE-024]` 가
  아이콘 버튼 열 개에 `aria-label` 을 붙일 때 이 둘이 빠졌습니다. 같은 모달의
  「닫기」·「메뉴 열고 닫기」·「검색」·「설정 열기」·「AI 상담」은 모두 갖고 있습니다.
- 라벨 문구가 `OPENAI_API_KEY` 처럼 환경 변수 이름 그대로입니다. 절 제목은 「OpenAI」로
  읽기 쉬운데 라벨만 그렇습니다. 알림 탭은 `SMTP Host` 와 `TELEGRAM_BOT_TOKEN` 이 섞여
  있습니다. 이 모달은 관리자 전용이 아니라 모든 방문자에게 열립니다.
- `[FE-032]` 가 같은 파일의 같은 입력 칸들을 건드리므로 그 뒤에 처리하면 충돌이 적습니다.
- QA 시나리오: 「API & 기능」 탭에서 Tab 키로 입력 칸에 포커스를 옮겼을 때 접근 가능한
  이름이 라벨 문구와 같다. 휴지통 버튼도 이름이 읽힌다.
- [ ] 라벨과 입력 칸을 `htmlFor`/`id` 로 연결하거나 라벨이 입력을 감싸도록 변경
- [ ] 삭제 버튼 두 개에 `aria-label` 추가
- [ ] 라벨 문구를 사람이 읽는 이름으로 바꾸고 변수 이름은 보조 설명으로 옮길지 결정
- [ ] 연결 상태를 vitest 로 고정

### [FE-034] 종가베팅 화면에 `h1` 이 없고 빵부스러기가 시맨틱 목록이 아니다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-04 `[FLOW-005]` 사이클의 /qa-only
  리포트 ISSUE-003·004
- 화면에서 가장 큰 제목이 `h2` 「종가 베팅」(48px)이고 `h1` 이 없습니다. 그 아래가 `h3`
  「Trading Tips & Strategy」와 종목명, `h4` 「AI 분석 리포트」로 이어집니다. 스크린 리더
  사용자가 제목 단축키로 화면의 주제에 바로 갈 수 없고, `h2` 부터 시작하는 구조는 계층을
  건너뛴 것으로 읽힙니다.
- 상단의 「/대시보드/국내 시장/종가베팅」은 눈으로는 빵부스러기인데 `nav` 로 감싸이지
  않았고 `ol`/`li` 구조도 아닙니다. 현재 위치를 나타내는 `aria-current` 도 없어서 보조
  기술이 탐색 경로로 인식하지 못하고 링크 셋이 흩어져 읽힙니다.
- 빵부스러기는 대시보드 전체가 공유하는 레이아웃에 있을 가능성이 높으므로, 고치기 전에
  어느 파일이 그리는지 확인하고 한 자리에서 바꿉니다.
- QA 시나리오: 종가베팅 화면에서 `h1` 이 하나 있고 그 내용이 「종가 베팅」이다.
  빵부스러기가 `nav[aria-label]` 안의 `ol` 이고 마지막 항목에 `aria-current="page"` 가 있다.
- [ ] 화면 제목을 `h1` 으로 올리고 그 아래 계층을 한 단계씩 맞춤
- [ ] 빵부스러기를 `nav` + `ol`/`li` 로 감싸고 마지막 항목에 `aria-current="page"` 추가
- [ ] 대시보드의 다른 화면에도 같은 구조가 쓰이는지 확인하고 함께 정리
- [ ] 제목 계층과 빵부스러기 구조를 vitest 로 고정

### [FLOW-014] 자료가 얇은 기준일에서 수급 조회가 종목 수만큼 pykrx 왕복을 낸다
- 카테고리: 수급·백테스트 | 티어: T2 | 근거: `[FLOW-011]` 사이클의 code-reviewer 지적과 실측
- `[FLOW-011]` 이 호출자를 `verify_with_references=True` 한 번 호출로 바꾸면서, CSV 에
  5거래일이 모이지 않은 종목이 참조 조회 대상이 되었습니다. 예전에는 그 자리에서 값 없이
  끝났으므로 조회가 없었습니다. 정확성을 얻고 시간을 내준 맞바꿈입니다.
- 대상 종목 수가 기준일에 따라 크게 달라집니다. `_get_or_build_trend_map` 과
  `_detect_csv_anomaly_flags` 를 직접 돌려 2026-09-04 자료로 잰 값이며, 전체는 1997 종목
  입니다. **「신규」만 이번 변경이 만든 비용입니다.** 「기존」은 플래그가 붙어 예전에도
  둘째 호출이 참조를 받아 오던 종목이라 비용이 달라지지 않았습니다.

  | target_date | 신규 (`missing_csv`) | 기존 (플래그 있음) |
  |---|---|---|
  | 최신 (`target_date=None`) | **0** | 20 |
  | 2026-02-23 | 57 | 7 |
  | 2026-01-15 (CSV 시작일 부근) | **1997** | 0 |

- 비용의 모양이 비대칭입니다. 자료가 정상이면 정확히 0 이고, 자료가 얇아진 순간에만 전
  종목으로 튑니다. 평소에 재면 아무 문제가 없어 보이는 것이 이 항목의 어려운 점입니다.

- 최악의 경로는 `python scripts/init_data.py vcp-signal <과거날짜>` 입니다. `max_stocks`
  기본값이 600(`engine/constants_market_system.py`)이고 `engine/screener.py:227` 의 순회가
  순차이므로, 왕복 0.3~1초로 잡으면 한 번에 3~10분이 붙습니다. 스케줄러와 `all` 경로는
  `target_date=None` 이라 Toss 우선 경로로 가므로 해당하지 않습니다.
- 최신 창(`target_datetime=None`)에서는 왕복이 종목당 두 번이 될 수 있습니다.
  `_resolve_best_payload:823` 의 `if not pykrx_ref and is_latest_reference_window` 때문에
  pykrx 가 빈손이면 Toss 까지 부릅니다. 위 표의 「왕복 × 종목 수」 계산은 과거 기준일에서만
  정확합니다. 과거 기준일은 `is_latest_reference_window` 가 거짓이라 Toss 를 건너뜁니다.
- 상세 조회 라우트(`app/routes/kr_market_data_backtest_stock_routes.py:54` 의
  `get_stock_detail`)는 끝까지 동기 경로입니다. 그래서 막히는 것은 이벤트 루프가 아니라
  gunicorn 워커 스레드(`--workers 2 --threads 8`)입니다. 15분 슬롯 캐시가 앞에 있어 평소
  노출은 좁지만, CSV 가 4영업일 넘게 낡아 `stale_csv` 가 전 종목에 붙으면 모든 상세 조회가
  이 경로로 들어갑니다.
- 참조 조회 실패는 캐시되지 않습니다. `services/investor_trend_5day_service.py:763` 이
  「miss(None)를 장시간 캐시하지 않아 일시 장애 후 재시도를 허용한다」고 명시하며
  `test_reference_cache_does_not_pin_miss_result` 가 그 동작을 고정합니다. 그래서 pykrx
  에도 자료가 없는 종목은 매 호출마다 왕복을 새로 냅니다.
- 요청 처리 경로도 범위에 들어갑니다. Toss 참조는 `timeout=10` 에 재시도 3회라 최악
  33초가량 워커를 붙잡고, pykrx 쪽은 이 저장소가 시간 제한을 걸지 않습니다. 종목 상세
  페이로드 캐시가 슬롯마다 한 번으로 눌러 주고 기존에도 `get_full_stock_detail` 이 왕복
  6회를 내므로 증폭 자체는 크지 않습니다. `[FLOW-011]` 사이클의 security 리뷰가 짚었습니다.
- [ ] 실패를 짧게(분 단위) 캐시할지, 아니면 호출자가 대량 경로임을 알릴 수단을 둘지 정함
- [ ] 요청 처리 경로에는 참조 조회의 전체 시간 예산을 두고 초과하면 CSV 로 내려가는 편이
      나은지 검토. 대량 배치 경로와 요구가 다릅니다
- [ ] 정한 방식으로 과거 기준일 스크리닝 한 번의 소요 시간을 재고 개선폭을 기록
- [ ] 실패 캐시를 넣는다면 `test_reference_cache_does_not_pin_miss_result` 의 의도와
      충돌하지 않는지 확인하고, 충돌하면 그 검사의 겨냥점을 옮김
- [ ] 참조 조회 횟수를 고정하는 검사를 넣을지 정함. 지금은 `_detect_csv_anomaly_flags`
      의 판정을 넓혔을 때 종목마다 네트워크가 붙기 시작해도 알려 줄 검사가 없습니다.
      `test_get_investor_trend_5day_for_ticker_skips_reference_when_csv_is_normal` 이
      한 종목으로 같은 불변식을 보고 있으므로, 겹치지 않는 형태를 찾아야 합니다

### [INFRA-030] 레거시 `engine/collectors.py` 와 모듈형 `engine/collectors/` 가 공존해 죽은 코드가 남는다
- 카테고리: 인프라 | 티어: T3 | 근거: `[FLOW-011]` 사이클(수급 호출자 통합)의 실측
- `engine/collectors.py` 가 `__path__` 를 스스로 지정해 모듈이면서 패키지처럼 동작합니다
  (`:31-34`). 파일 끝(`:2486-2501`)에서 `EnhancedNewsCollector` 와 `NaverFinanceCollector`
  만 모듈형으로 덮어쓰고 `KRXCollector` 는 덮어쓰지 않습니다.
- 그래서 같은 일을 하는 코드가 두 벌 있고 한 벌만 실행됩니다. 실측으로 확인한 것은
  다음 둘입니다.
  - `engine/collectors.py` 의 `NaverFinanceCollector._get_investor_trend` 는 모듈형에
    덮여 실행되지 않습니다. `from engine.collectors import NaverFinanceCollector` 가
    모듈형을 가져옵니다
  - `engine/collectors/krx_local_data_mixin.py` 의 `get_supply_data` 는
    `engine.collectors.krx.KRXCollector` 를 임포트하는 실행 코드가 없어 테스트에서만
    돕니다. 실행되는 것은 `engine/collectors.py` 의 `KRXCollector` 입니다
- 두 벌이 갈라지면 어느 쪽을 고쳤는지 알 수 없습니다. `[FLOW-011]` 이 판정 헬퍼를
  정리할 때 실행되지 않는 자리까지 함께 고쳐야 했습니다.
- 같은 자리에 개인 수급의 일관성 문제가 하나 겹쳐 있습니다. 서비스는 개인 수급을 돌려주지
  않으므로 `engine/collectors/krx_local_data_mixin.py:1237` 은 정상 경로에서 `retail_buy_5d=0`
  을 넣고 `engine/collectors/naver_pykrx_mixin.py:453` 은 `individual` 을 0 으로 둡니다.
  이상징후로 pykrx 경로에 빠질 때만 실값이 채워집니다. 같은 필드가 어느 경로를 거쳤느냐에
  따라 0 이기도 실값이기도 합니다. `[FLOW-011]` 사이클의 code-reviewer 가 짚었습니다.
- [ ] 어느 구현을 남길지 정하고 나머지를 지움
- [ ] `__path__` 조작을 없앨 수 있는지 확인 (`engine/collectors/__init__.py` 로 대체)
- [ ] 지운 쪽만 검사하던 테스트를 남긴 쪽으로 옮기거나 지움
- [ ] 개인 수급을 늘 0 으로 둘지, 서비스가 함께 돌려주게 할지 정하고 두 경로를 맞춤

### [INFRA-031] 백로그에 같은 ID 를 가진 항목이 여덟 개 있다
- 카테고리: 인프라 | 티어: T1 | 근거: `[FLOW-011]` 사이클에서 새 ID 를 고르다 발견
- `TODO.md` 안에 `[FLOW-011]`, `[FLOW-012]`, `[FLOW-013]`, `[INFRA-018]` 이 각각 두 벌씩
  있었습니다. 나중에 추가된 쪽이 이미 쓰이던 번호를 다시 썼습니다. 그 뒤 사이클이 하나씩
  완료되면서 충돌이 넷 모두 해소되어, 지금 이 파일에는 겹치는 ID 가 남아 있지 않습니다.
  - `[INFRA-018]`: 휴장일 목록(2026-09-04) ↔ numpy 2.x 승격(그 이전).
    앞의 것이 2026-09-05 에 완료되어 빠졌으므로 남은 것은 하나입니다
  - `[FLOW-011]`: 수급 호출자 통합(2026-09-01 감사) ↔ 누적 수익률 두 값(2026-09-03).
    앞의 것이 완료되어 이 파일에서 빠졌으므로 남은 것은 하나입니다
  - `[FLOW-012]`: 참조 수급 자료의 신뢰성(2026-09-04) ↔ 「최근 10건 승률」(그 이전).
    앞의 것이 완료되어 빠졌으므로 남은 것은 하나입니다
  - `[FLOW-013]`: SQLite 캐시의 5일치 불변식(2026-09-04) ↔ 「누적 추천수 180」(2026-09-03).
    앞의 것이 완료되어 빠졌으므로 남은 것은 하나입니다
- 아카이브는 아직 오염되지 않았습니다. 완료 기록에 겹치는 ID 가 없습니다. 다만 충돌한
  항목 가운데 하나라도 완료되면 일별 상세와 월별 요약에서 어느 항목인지 알 수 없게 됩니다.
- 나중에 추가된 쪽의 번호를 바꾸는 것이 안전합니다. 쓰이지 않은 번호는 `FLOW-014` 이후와
  `INFRA-032` 이후입니다.
- [ ] 나중에 추가된 셋의 ID 를 빈 번호로 바꿈
- [ ] 바꾼 ID 를 참조하는 문서가 있는지 확인 (`docs/dev-cycle/qa/`, `docs/superpowers/plans/`)
- [ ] ID 를 새로 고를 때 쓰던 번호인지 확인하는 절차를 `archive-format.md` 에 한 줄로 적음

### [JONGGA-033] 종목 상세 모달의 EPS 와 순이익이 서로 반대 부호를 가리킨다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-05 `[FLOW-012]` 사이클의 `/qa-only` ISSUE-001
- QA 시나리오: 상세 모달의 EPS 부호와 「재무 정보」 순이익 부호가 같은 방향을 가리킨다
- 같은 모달 안의 두 섹션이 부호가 반대인 값을 나란히 보여 줍니다. 로보티즈(108490)는
  EPS 가 `-266` 인데 순이익이 `26억` 이고, 레인보우로보틱스(277810)는 EPS 가 `87` 인데
  순이익이 `-7091만` 입니다. S-Oil·한미반도체·LG전자 셋은 서로 일치합니다.
- EPS 툴팁이 자기를 「순이익 ÷ 발행주식수」로 설명하므로, 두 값을 대조한 사용자는 한쪽이
  틀렸다고 읽습니다. 로보티즈는 EPS 만 보면 적자이고 순이익만 보면 흑자입니다.
- 두 값 모두 Toss 응답에서 오며 서로 다른 필드입니다.
  `services/kr_market_stock_detail_service.py:502` 의 `indicators.eps` 와 같은 함수
  `financials.net_income` 입니다. EPS 가 최근 4분기 합산이고 순이익이 다른 기간일
  가능성이 높으므로, 먼저 Toss 원본에서 두 필드의 기준 기간을 확인해야 합니다.
- [ ] Toss 응답에서 두 필드의 기준 기간이 서로 다른지 확인
- [ ] 기간이 다르다면 화면에 그 기준을 함께 적거나 같은 기간으로 맞춤

### [FE-022] 사이드바와 AI 상담 화면의 사용자 표기가 서로 다르다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-03 FLOW-004 사이클의 `/qa-only` 실측
- 같은 세션인데 사이드바 하단은 이니셜 `U` 에 이름 `User`, 등급 `Free Tier Plan` 을
  보여주고, `/chatbot` 본문은 `안녕하세요, 흑기사님` 에 아바타 `흑기` 를 보여줍니다.
- **2026-09-04 FE-005 사이클의 `/qa-only` 가 진단 방향을 뒤집었습니다.** 앞서 이 자리에는
  「챗봇 화면은 실제 계정 이름을 알고 있는데 사이드바만 자리표시자」라고 적혀 있었으나
  그 전제가 틀렸습니다. 같은 브라우저에서 `/api/auth/session` 을 조회하면 `user` 자체가
  없고, 사용량 조회도 `?email=&session_id=anon_…` 로 나가 비로그인 상태임을 확인해
  줍니다. 즉 사이드바의 `User` 가 맞고 챗봇의 `흑기사님` 이 자리표시자입니다.
- 그래서 고칠 방향은 사이드바를 챗봇에 맞추는 것이 아니라 그 반대입니다. 로그인하지 않은
  방문자에게 특정 이름으로 인사하면 다른 사람의 계정으로 들어온 것처럼 보입니다.
- **2026-09-06 `[CHAT-026]` 의 코드 리뷰가 같은 것을 다시 지적했습니다.** 그 사이클이
  자리표시자를 `chatHelpers.ts` 의 `DEFAULT_USER_PROFILE` 상수로 모았으므로, 이제 챗봇 쪽
  이름을 바꾸는 것은 그 상수 한 곳을 고치는 일입니다. 사이드바 쪽 `useState({ name: 'User',
  … })` 는 그대로 남아 있고, `localStorage` 에 캐시가 없는 첫 방문자에게 기본값으로
  갱신하는 분기가 챗봇 화면과 달리 사이드바에는 없다는 것도 함께 확인했습니다.
- **2026-09-06 `[CHAT-026]` 의 브라우저 실측이 영향 범위가 넓어진 것을 확인했습니다.**
  그 사이클이 두 저장 경로를 `saveUserProfile` 하나로 모으면서, 사이드바가 보여 주는
  자리표시자가 이제 서버 프로필까지 갱신합니다. 같은 `/chatbot` 화면에서 본문 아바타로 연
  설정 모달은 이름에 `흑기사` 를, 사이드바 사용자 메뉴와 대시보드 헤더 「설정 열기」로 연
  모달은 `User` 를 담고 있습니다. 세 경로가 여는 모달의 생김새와 「저장」 버튼이 같으므로,
  사용자가 어느 버튼으로 열었는지에 따라 저장되는 이름이 갈립니다. 종전에는 사이드바
  경로가 `localStorage` 만 건드려서 이 차이가 화면 안에 머물렀습니다.
- 그래서 P2 에서 P1 로 올립니다. 화면 표기의 불일치였던 것이 서버에 남는 값의 문제가
  되었습니다.
- [ ] 챗봇 화면이 세션 없는 방문자에게 쓰는 이름을 정함. 자리표시자 `흑기사` 를 걷어냄
- [ ] 사이드바와 챗봇이 같은 출처에서 사용자 정보를 읽는지 확인함
- [ ] 이름을 아직 모를 때의 표시 문구를 두 자리에서 동일하게 정함


## P2 — 대기


### [CHAT-027] 프로필이 메모리 영역에 들어오면서 생긴 어긋남 셋
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-06 `[CHAT-021]` 사이클의 코드 리뷰
- `[CHAT-021]` 이 `user_profile` 을 소유자 버킷으로 옮기면서 메모리와 같은 취급을 받게
  되었고, 그 결과 세 자리가 어긋납니다. 셋 다 사소하고 같은 주변이라 한 항목으로 묶습니다.
- `chatbot/command_service.py:119` 의 `render_memory_view` 가 소유자 버킷을 그대로 보여
  주므로 사용자가 `/memory` 로 넣은 적 없는 `user_profile` 이 `/memory view` 에 나타나고
  `/memory remove user_profile` 로 지울 수도 있습니다.
- `frontend/src/app/chatbot/page.tsx:197` 의 `/clear all` 설명이 「내 대화 전체 초기화」인데
  실제로는 `chatbot/command_service.py:52` 가 메모리도 지우므로 프로필까지 사라집니다.
  서버 응답 문구는 정확하고 화면의 명령어 목록만 어긋납니다.
- `chatbot/runtime_setup_service.py:162` 의 `init_user_profile_from_env` 는 가드가
  `not memory.memories` 인데 저장소가 2단이 된 뒤로는 어느 소유자에게든 행이 하나라도
  있으면 거짓입니다. 공용에 캐시 두 행이 이미 있어 영영 발동하지 않으므로 `USER_PROFILE`
  환경 변수는 어떤 화면에도 영향을 주지 못합니다.
- [ ] `/memory view` 에서 프로필을 가리거나 별도 표기로 구분
- [ ] `/clear all` 설명 문구를 실제 동작에 맞춤
- [ ] `init_user_profile_from_env` 를 지우거나 발동하도록 고침

### [CHAT-024] 창 높이가 낮으면 빠른 조회 버튼이 추천 질문 카드를 가린다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-017]` 사이클의 `/qa-only` ISSUE-001
- 1280×577 뷰포트에서 `/chatbot` 을 열면 추천 질문 카드 넷의 아래쪽 절반이 잘리고 그 위로
  「시장 현황 / VCP 추천 / 종가 베팅 / 뉴스 분석 / 내 관심종목」 버튼 줄이 겹쳐 뜹니다.
  카드 영역이 스크롤되지 않아 사용자가 잘린 부분을 볼 방법이 없습니다. 노트북 화면이나
  개발자 도구를 연 상태에서 흔히 나오는 높이입니다.
- 1440×900 에서는 여백이 충분해 겹치지 않고, 375×812 모바일에서는 카드가 2×2 로 재배치되고
  버튼이 두 줄로 감겨 역시 겹치지 않습니다. 중간 높이 구간에서만 나타납니다.
- 티어 판정: `frontend/src/app/chatbot/page.tsx` 의 빈 화면 레이아웃 한 곳입니다. 버튼 줄을
  카드 영역 바깥으로 빼거나 카드 영역에 최소 높이와 스크롤을 주는 정도입니다.
- `[CHAT-010]` 「창을 좁히면 사이드바가 챗봇 본문을 덮은 채 남는다」와는 다른 결함입니다.
  그쪽은 폭이 원인이고 이쪽은 높이가 원인입니다.
- [ ] 낮은 높이에서 버튼 줄과 카드가 겹치지 않도록 레이아웃 수정
- [ ] 1280×577, 1440×900, 375×812 세 뷰포트에서 확인


### [CHAT-023] 공용 메모리의 추천 질문 캐시가 상한 없이 쌓인다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-017]` 사이클의 코드 리뷰
- `chatbot/daily_suggestions_service.py:65` 가 `daily_suggestions_<persona>_<watchlist>` 키로
  추천 질문을 공용 영역에 저장합니다. `chatbot/data_service.py:133` 의 조회는 1시간 신선도만
  보고 낡은 행을 지우지는 않습니다.
- `[CHAT-017]` 이 공용 영역을 사용자 명령에서 도달 불가능하게 만들었으므로 `/clear all` 도
  `/memory clear` 도 이 행들에 닿지 않습니다. `save_memories_to_sqlite` 의 stale 정리는
  스냅샷에 있는 행을 남기므로 정리 수단이 아닙니다.
- 캐시 키가 (페르소나 × 관심종목) 조합이고 관심종목이 사용자 입력이라 조합에 상한이 없습니다.
  조합마다 한 행씩 영구히 쌓입니다.
- 티어 판정: 만료된 행을 지우는 자리 하나를 정하면 됩니다. 저장 직후에 같은 접두사의 낡은 행을
  지우는 방법과, 다른 캐시들이 쓰는 `prune_rows_by_updated_at_if_needed`(`services/sqlite_utils.py`)를
  쓰는 방법이 있습니다. 후자를 쓰면 기존 프루닝 규약에 맞습니다.
- [ ] 만료된 공용 캐시 행을 지우는 자리를 정하고 구현
- [ ] 상한이 실제로 걸리는지 pytest 로 고정


### [CHAT-020] VCP 화면의 대화 삭제가 서버 응답을 보지 않는다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-018]` 사이클의 code-review (확신도 높음)
- `frontend/src/app/dashboard/kr/vcp/page.tsx` 의 삭제 경로 세 곳(메시지 삭제, 대화 삭제,
  `/clear`)이 `await fetch(...)` 만 하고 `res.ok` 를 보지 않습니다. 화면은 낙관적으로 먼저
  지우므로, 서버가 404 를 돌려주어도 사용자는 지워졌다고 봅니다.
- 소유자가 바뀌어 저장된 세션 ID 가 죽었을 때 드러납니다. 다음 조회에서 404 를 받으면
  `[CHAT-016]` 이 넣은 분기가 `localStorage` 를 정리하므로 결국 복구되지만, 그때까지는
  화면과 서버 상태가 어긋납니다.
- 같은 화면의 조회 경로는 `[CHAT-016]` 이 이미 404 를 다루도록 고쳤습니다. 삭제 세 곳만
  같은 기준으로 맞추면 됩니다.
- [ ] 삭제 응답이 404 이면 저장된 세션 ID 를 버리고 대화를 다시 불러오도록 고침
- [ ] 세 경로가 같은 방식으로 응답을 다루는지 확인

### [CHAT-019] 슬래시 명령 자동완성이 가려지고 설명이 화면마다 다르다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-016]` 사이클의 `/qa-only`
  ISSUE-001, ISSUE-003
- 두 가지가 같은 UI 에 있어 한 항목으로 묶습니다.
- **가림**: `/chatbot` 입력창에 `/` 를 입력하면 뜨는 「사용 가능한 명령어」 팝업의 둘째 행
  `/clear all` 설명이 하단 빠른 조회 버튼(「시장 현황」 등 다섯)에 덮입니다. 첫째 행은
  보입니다. DOM 에는 있고 시각적으로만 가리므로 겹침 문제입니다. **되돌릴 수 없는 명령의
  설명이 보이지 않습니다.** VCP 화면의 채팅 위젯에서는 가려지지 않습니다.
- **문구 불일치**: 같은 `/clear` 의 설명이 `/chatbot` 에서는 「현재 대화 초기화」,
  VCP 위젯에서는 「화면 청소」입니다. 실제 동작은 현재 세션에 저장된 메시지를 서버에서
  비우는 것이므로 「화면 청소」는 실제보다 가볍게 들립니다. 어느 쪽이 맞는 설명인지 정하고
  두 화면을 맞춥니다.
- [ ] 팝업이 하단 버튼 위에 그려지도록 겹침을 정리
- [ ] `/clear` 설명 문구를 두 화면에서 하나로 맞춤

### [CHAT-010] 창을 좁히면 사이드바가 챗봇 본문을 덮은 채 남는다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-003]` 사이클의 `/qa-only` ISSUE-002
- QA 시나리오: 1280px 에서 연 챗봇 화면을 375px 로 줄여도 사이드바가 접히고 본문이 가려지지
  않는다
- 뷰포트를 1280×720 으로 두고 `/chatbot` 을 연 다음 새로 고치지 않고 375×812 로 줄이면,
  사이드바가 오버레이로 남아 추천 질문 카드와 빠른 조회 버튼과 입력창을 덮습니다. 사이드바
  자체도 왼쪽으로 밀려 잘려서 「스마트 머니 봇」이 「마트 머니 봇」으로 보입니다.
- 같은 폭에서 페이지를 새로 로드하면 정상입니다. 사이드바가 접히고 왼쪽 위에 햄버거 버튼이
  나옵니다. 초기 렌더는 반응형 분기를 옳게 타고, 폭이 바뀌는 순간에만 열림 상태가 정리되지
  않습니다.
- 새로 고치면 풀리므로 막다른 상태는 아니지만, 그 사이에는 입력창에 손이 닿지 않습니다.
- [ ] 뷰포트가 `lg` 아래로 내려가면 사이드바 열림 상태를 닫도록 수정
- [ ] 1280px → 375px 리사이즈와 375px 초기 로드를 모두 실측

### [CHAT-011] 대화 목록 렌더에서 React key 경고가 난다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-003]` 사이클의 `/qa-only` ISSUE-003
- QA 시나리오: 대화가 한 건 이상 있는 챗봇 화면에서 콘솔 경고가 0건이다
- 대화가 하나도 없으면 콘솔이 완전히 깨끗합니다. 대화가 한 건이라도 있으면
  `Each child in a list should have a unique "key" prop` 경고가 두 번 납니다. 즉 대화
  목록을 그리는 자리에서 `key` 가 빠졌습니다.
- 화면에 드러나는 오작동은 아직 없습니다. 다만 `key` 없는 목록은 항목을 지우거나 순서를
  바꿀 때 React 가 잘못된 항목을 재사용할 수 있고, 이 화면에는 「이 메시지 삭제」와
  「이 질문과 답변 함께 삭제」가 있습니다.
- `[CHAT-004]` 가 `frontend/src/app/chatbot/page.tsx` 를 쪼개므로 그보다 먼저 끝내는 편이
  충돌이 적습니다.
- [ ] 경고를 내는 목록 렌더 두 곳에 안정적인 `key` 를 부여
- [ ] 대화가 있는 상태에서 콘솔 경고 0건 확인

### [CHAT-012] 챗봇 저장소의 스키마 준비 골격을 공용 게이트로 흡수
- 카테고리: 챗봇 | 티어: T2 | 근거: 2026-09-05 `[CHAT-003]` 사이클의 code-reviewer 지적
- QA 시나리오: 챗봇 대화 목록과 메시지 이력이 통합 전과 같게 조회된다
- `chatbot/storage_sqlite_common.py:95-194` 의 `ensure_chatbot_storage_schema` 가
  `[CHAT-003]` 이 만든 `services/sqlite_ready_gate.py` 의 `SqliteReadyGate` 와 같은
  골격입니다. 줄 단위로 대응이 맞습니다. 진입 `:103`↔`:66`, 대기 루프 `:111-116`↔`:72-77`,
  in-progress 등록 `:118`↔`:79`, `finally` 재진입 `:180`↔`:94`, `add_bounded_ready_key`
  `:183-187`↔`:97-101`, `notify_all` `:190`↔`:104` 입니다.
- 차이는 `force_recheck: bool = False` 인자 하나뿐입니다. 그 인자를 게이트의 `ensure` 에
  더하면 흡수됩니다.
- `[CHAT-003]` 이 이 파일을 범위에서 뺀 것은 그 항목이 정한 대상이 캐시 두 모듈이었고,
  이 파일은 캐시가 아니라 세션·메시지·메모리 저장소이기 때문입니다.
- [ ] `SqliteReadyGate.ensure` 에 `force_recheck` 를 더함
- [ ] `ensure_chatbot_storage_schema` 를 게이트 위임으로 교체
- [ ] 기존 회귀 테스트가 그대로 통과하는지 확인

### [INFRA-032] 나머지 열다섯 모듈의 SQLite 준비 골격을 공용 게이트로 흡수
- 카테고리: 인프라 | 티어: T3 | 근거: 2026-09-05 `[CHAT-003]` 사이클의 code-reviewer 지적
- QA 시나리오: 종가베팅·VCP·실시간 시세·누적 성과 화면이 통합 전과 같은 값을 그린다
- `services/sqlite_utils.py` 의 `add_bounded_ready_key` 를 쓰는 비테스트 모듈이 열여섯
  개이고, 그 가운데 `[CHAT-003]` 이 두 개를, `[CHAT-012]` 가 한 개를 가져갑니다. 남는
  것은 다음 열다섯 개입니다.

      services/kr_market_data_cache_sqlite_payload.py
      services/kr_market_vcp_signals_cache.py
      services/kr_market_jongga_payload_helpers.py
      services/kr_market_realtime_price_cache.py
      services/kr_market_realtime_latest_close_cache.py
      services/kr_market_realtime_market_map_cache.py
      services/kr_market_backtest_summary_cache.py
      services/kr_market_cumulative_cache.py
      services/kr_market_data_cache_jongga.py
      services/common_update_status_service.py
      services/file_row_count_cache.py
      services/paper_trading_db_setup.py
      engine/kr_ai_stock_info_cache.py
      engine/signal_tracker_source_cache.py
      engine/signal_tracker_analysis_source_cache.py

- `[CHAT-003]` 은 두 모듈을 옮기면서 코드 줄이 13줄 늘었습니다(주석 제외 820 → 833).
  중복 로직 354줄이 사라진 자리를 위임 호출의 인자 전달이 채웠기 때문입니다. 나머지가
  따라오면 순감으로 돌아섭니다.
- 열다섯 개를 한 사이클에 다 옮기면 규모가 통제를 벗어나므로, 화면 단위로 서너 개씩 나누어
  진행할지 이 항목을 시작할 때 정합니다.
- [ ] 옮길 순서를 화면 단위로 묶어 확정
- [ ] 각 묶음마다 기존 회귀 테스트가 그대로 통과하는지 확인
- [ ] 옮긴 뒤 주석을 뺀 코드 줄 수가 실제로 줄었는지 측정


### [FE-037] 툴팁이 화면과 스크롤 컨테이너의 경계를 넘어 잘린다
- 카테고리: 프론트엔드 공통 | 티어: T2 | 근거: 2026-09-05 `[JONGGA-006]` 사이클의 `/qa-only` ISSUE-001, ISSUE-002
- QA 시나리오: 화면 가장자리와 스크롤 컨테이너의 첫 줄에 있는 툴팁이 전문을 보여 준다
- `frontend/src/app/components/Tooltip.tsx` 는 툴팁을 늘 트리거의 위쪽 가운데에 띄웁니다.
  뜰 자리가 있는지 재지 않으므로 두 자리에서 글자가 잘립니다.
- **종목 상세 모달의 「시세 정보 (실시간)」 툴팁**은 그 제목이 모달 스크롤 컨테이너의 첫
  줄이라 위쪽에 공간이 없습니다. 툴팁 상자가 `top: 93px` 인데 컨테이너는 `top: 139px` 에서
  시작해 상단 46px 이 잘리고, 「당일의 가격 움직임(1일 범위)과 최근 52주간 최저·최고가를
  견주어 지금의 가격 위치를 보여」까지가 사라진 채 「줍니다.」부터 보입니다.
- **모바일 폭 375px 에서는 종가베팅 화면 툴팁 200개 가운데 67개**가 화면 밖으로 나갑니다.
  왼쪽 40개와 오른쪽 27개이며, 「등급」 툴팁은 왼쪽으로 64px 잘려 각 줄의 앞글자를 읽을 수
  없습니다. 툴팁은 `pointer-events: none` 이라 가로 스크롤은 생기지 않고 읽히지 않기만
  합니다.
- 통합 전 세 사본도 같은 배치 규칙과 같은 폭을 썼으므로 `[JONGGA-006]` 이 만든 것이
  아닙니다. 사본이 하나로 모인 지금은 한 곳을 고치면 세 화면이 함께 고쳐집니다.
- `position` 프롭에 `bottom` 이 이미 있으므로, 뜰 자리가 없을 때 아래로 뒤집고 가로로는
  뷰포트 안쪽으로 당기는 처리를 공용 컴포넌트에 넣으면 해소됩니다.
- [ ] 위쪽 공간이 모자라면 아래로 뒤집도록 함
- [ ] 가로로 화면을 넘으면 뷰포트 안쪽으로 당기도록 함
- [ ] 종목 상세 모달과 모바일 폭에서 두 경우를 실측으로 확인

### [JONGGA-034] 필터로 결과가 비면 「TRENDING THEMES」까지 함께 사라진다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-05 `[INFRA-018]` 사이클의 `/qa-only` FINDING-001
- QA 시나리오: 거래대금 필터를 「1조 이상」으로 바꿔 카드가 0건이 되어도 화면 오른쪽 위의
  테마 목록은 그날 리포트의 테마를 그대로 보여 준다
- 거래대금 필터를 「1조 이상」으로 두면 카드가 0건이 되는데, 그때 「TRENDING THEMES」
  상자가 「데이터 갱신 후 표시됩니다」로 바뀝니다. 필터를 되돌리면 테마도 함께 돌아오므로
  자료가 사라진 것은 아니고, 보이는 카드에서 테마를 뽑고 있어서 생기는 현상입니다.
- 테마 목록은 그날 리포트 전체의 성질이지 지금 보이는 카드의 성질이 아닙니다. 필터는
  무엇을 볼지 좁히는 조작인데 그 조작이 리포트의 요약까지 지우는 것은 어긋납니다.
- [ ] 테마 목록을 필터 적용 전의 리포트 전체에서 뽑도록 고침

### [JONGGA-035] 빈 상태 안내만 영어로 남아 있다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-05 `[INFRA-018]` 사이클의 `/qa-only` FINDING-002
- QA 시나리오: 필터로 카드가 0건이 되었을 때 표시되는 안내 문구가 한국어다
- 카드가 0건이면 「No Signals Found」와 「Try adjusting filters or wait for market
  conditions to improve.」가 표시됩니다. 같은 화면의 다른 안내는 모두 한국어입니다.
- 이 화면은 `CANDIDATES`, `FILTERED`, `DATA STATUS`, `OLD DATA` 처럼 영어 라벨을 일부러
  섞어 씁니다. 다만 그것들은 짧은 라벨인 데 비해 이 안내는 사용자에게 다음 행동을 알려
  주는 완결된 문장이라서 성격이 다릅니다. 라벨은 그대로 두고 이 문장만 옮기면 됩니다.
- [ ] 빈 상태 제목과 안내 문장을 한국어로 옮김

### [FE-036] 모달 진입 애니메이션 클래스가 컴파일된 CSS 에 존재하지 않는다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-05 `[FE-007]` 사이클에서 발견
- QA 시나리오: 모달을 열 때 지정한 애니메이션이 실제로 재생되거나, 재생되지 않는다면
  그 클래스가 마크업에서 사라진다
- 모달 다섯 벌이 `animate-in`, `fade-in`, `zoom-in-95`, `slide-in-from-bottom-4`,
  `animate-scale-in` 을 쓰고 있으나 **이 클래스들은 컴파일된 CSS 에 없습니다.** 브라우저가
  알지 못하는 이름이므로 아무 일도 일어나지 않습니다.
- 원인은 `tailwindcss-animate` 플러그인이 설치되지 않은 것입니다.
  `frontend/tailwind.config.ts` 의 `plugins` 가 비어 있습니다. 앞의 넷은 그 플러그인이
  제공하는 이름이고 `animate-scale-in` 은 어디에도 정의가 없습니다.
- 실제로 정의된 것은 `frontend/src/app/globals.css:117` 의 `animate-fade-in` 하나뿐입니다.
  이름이 비슷해 구별이 어렵습니다.
- 고르는 길이 둘입니다. 플러그인을 설치해 클래스를 살리거나, 죽은 클래스를 지우고
  `animate-fade-in` 으로 통일하는 것입니다. 뒤의 것이 의존성을 늘리지 않고 지금 화면과
  같은 결과를 냅니다.
- [ ] 죽은 클래스가 붙은 자리를 전부 찾음
- [ ] 한 방향을 골라 반영함
- [ ] 모달 여섯 벌을 열어 보며 진입 모습이 의도대로인지 실측함

### [FE-035] PER 툴팁이 음수 PER 을 설명하지 못한다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-05 `[FLOW-012]` 사이클의 `/qa-only` ISSUE-003
- QA 시나리오: PER 이 음수인 종목의 툴팁이 그 값을 어떻게 읽어야 하는지 알려 준다
- 로보티즈(108490)의 PER 은 `-1147.60` 인데 옆의 툴팁은 「주가수익비율. 주가 ÷ 주당순이익.
  낮을수록 저평가, 업종 평균과 비교 필요.」라고만 적혀 있습니다. 음수 PER 은 주당순이익이
  음수, 즉 적자라는 뜻이라 저평가와 무관합니다.
- 툴팁대로 읽으면 `-1147.60` 이 가장 저평가된 값으로 해석되므로, 적자 기업을 저평가
  기업으로 오독할 수 있습니다.
- 「음수면 적자를 뜻하므로 비교 대상이 아닙니다」 한 줄을 툴팁에 더하면 해소됩니다.
- [ ] PER 툴팁에 음수 사례 설명을 더함

### [INFRA-026] 공용 JSON 인코더가 결측 날짜를 `"NaT"` 문자열로 저장한다
- 카테고리: 인프라 | 티어: T2 | 근거: 2026-09-04 `[INFRA-006]` 사이클의 code-review 관찰(확신도 30)
- `numpy_json_encoder.py:44-45` 의 `isinstance(obj, (datetime, date))` 분기에 pandas 의
  `NaT` 도 걸린다. `NaTType` 이 `datetime` 을 상속하기 때문이다. `NaT.isoformat()` 은
  `"NaT"` 라는 문자열을 돌려주므로, 결측 날짜가 값처럼 JSON 에 실린다.
- 이 인코더를 쓰는 모듈은 여덟 개다. `engine/generator_result_storage.py`,
  `engine/market_gate.py`, `services/kr_market_data_cache_jongga.py`,
  `services/common_update_status_service.py`, `services/kr_market_backtest_summary_cache.py`,
  `services/kr_market_cumulative_cache.py`, `services/common_update_ai_analysis_service.py`
  이며 `[INFRA-006]` 이 `scripts/init_data.py` 를 여기에 더했다.
- 티어 근거: `numpy_json_encoder.py` 는 위험 경로 목록에 없으나 여덟 모듈의 저장 경로가
  모두 이 파일 하나를 지난다. 고칠 때 `tier-rules.md` §2 에 추가할지 함께 판정한다.
- [ ] `data/` 아래 저장된 JSON 에 `"NaT"` 문자열이 실제로 들어 있는지 확인
- [ ] 들어 있으면 `numpy_json_encoder.py` 가 `pd.isna` 를 먼저 보고 `None` 을 돌려주게 고친다
- [ ] 화면이 그 자리를 어떻게 그리는지 확인. `[JONGGA-019]` 가 값 없음을 값처럼 그리던 문제를
      고쳤으므로 같은 처리가 필요한지 본다
- [ ] 회귀 검사 추가. `numpy_json_encoder.py` 에는 지금 전용 검사가 하나도 없다
- 같은 파일의 numpy 분기도 함께 본다. 삭제한 `scripts/init_data.py` 의 사본은
  `np.integer` 와 `np.floating` 이라는 추상 기반 클래스로 판정했으나 공용 구현은 구체
  타입을 열거한다. 그래서 `np.longdouble` 이 직렬화되지 않는다(실행으로 확인). 다만
  추상 기반 클래스로 넓히면 `np.timedelta64` 까지 걸려 단위 없는 정수로 조용히 바뀌므로,
  `NaT` 와 같은 성격의 판단이 필요하다. 지금 자료 경로는 float64 만 만들어 실제 피해는 없다.
- `np.float_` 는 numpy 2.0 에서 없어진 이름이라 그때 이 파일이 임포트 단계에서 깨진다.
  `[INFRA-018]` 의 numpy 핀과 함께 판정한다.
- [ ] `np.longdouble` 을 열거 목록에 더할지, 추상 기반 클래스로 바꿀지 판정

### [INFRA-028] Gemini 재분석 요청의 날짜 목록에 형식 검사도 개수 상한도 없다
- 카테고리: 인프라 | 티어: T2 | 근거: 2026-09-04 `[INFRA-006]` 사이클의 /review
  보안 스페셜리스트 지적(확신도 70, 60, 40)
- `services/kr_market_route_service.py:113` 의 `parse_target_dates` 는 신뢰 경계를 넘어온
  값을 문자열로 바꾸고 공백만 없앤다. 날짜 형식도 목록 길이도 보지 않는다.
- 그 값이 `scripts/init_data.py` 의 파일명 조립부까지 그대로 닿는다. 지금 경로 탈출이
  성립하지 않는 이유는 파일명 조립부의 방어가 아니라 그보다 앞의
  `df['signal_date'] == str(t_date)` 필터가 실재하지 않는 날짜를 걸러 내기 때문이다.
  방어가 우연히 다른 자리에 놓여 있어 필터 조건이 느슨해지는 순간 취약해진다.
- 같은 값이 필터를 적용하기 전에 `log(f"Deep Analysis for date: {t_date}")` 로 기록되므로
  개행 문자를 넣으면 로그에 가짜 항목을 만들 수 있다. 이 로그를 파싱하는 소비자는 확인하지
  못했으므로 영향은 제한적이다.
- Flask 의 `MAX_CONTENT_LENGTH` 가 설정되어 있지 않아 항목이 수만 개인 배열도 받는다.
- [ ] `parse_target_dates` 에서 `\d{4}-\d{2}-\d{2}` 형식과 목록 길이 상한을 확정
- [ ] 형식에 맞지 않는 값을 이 한 자리에서 걸러 파일명 조립부까지 닿지 않게 한다
- [ ] `MAX_CONTENT_LENGTH` 를 설정할지 판정
- [ ] 잘못된 형식이 거부됨을 고정하는 검사 추가
- [ ] pytest 전체 통과 확인

### [INFRA-029] 장 마감 정기 분석의 완료 로그가 실패한 단계를 덮는다
- 카테고리: 인프라 | 티어: T2 | 근거: 2026-09-04 `[INFRA-006]` 사이클의 /review
  유지보수성 스페셜리스트 지적(확신도 45)
- `services/scheduler_jobs.py:102-115` 의 일별 주가, 수급, VCP 세 단계는 실패해도 오류만
  기록하고 계속 진행하는데, 121 행의 완료 로그는 그 결과를 보지 않는다.
- 게다가 이 로그는 122 행에서 `jongga_ok` 를 판정하기 전에 나오므로, 종가베팅이 실패해
  126 행의 오류가 뒤따르는 경우에도 「완료」가 먼저 찍힌다. 사람이 지켜보지 않는 야간
  작업이라 로그가 유일한 판단 근거인데, 완료로 grep 하면 실패한 밤을 성공으로 읽는다.
- [ ] `prices_ok`, `inst_ok`, `vcp_ok`, `jongga_ok` 를 모아 완료와 부분 실패를 나눠 기록
- [ ] 최소한 완료 로그를 `jongga_ok` 판정 이후로 옮긴다
- [ ] 부분 실패 시 완료 로그가 나오지 않음을 고정하는 검사 추가
- [ ] pytest 전체 통과 확인

### [FE-031] 국내 시장 화면이 갱신 실패를 콘솔에만 남긴다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-04 FE-005 사이클의 code-reviewer
  지적(확신도 55, 25)
- QA 시나리오: Flask 를 내린 상태로 「Market Gate 새로고침」을 누르면 화면에 실패 사유가
  뜬다. 화면이 아무 반응도 하지 않는 상태로 남지 않는다
- `frontend/src/app/dashboard/kr/page.tsx:364` 는 `krAPI.updateMarketGate` 가 던진 예외를
  `console.error` 로만 처리합니다. 버튼을 눌러도 화면에는 아무것도 바뀌지 않아서 사용자는
  갱신이 됐는지 실패했는지 알 수 없습니다.
- `[FE-005]` 가 `fetchAPI` 로 하여금 백엔드가 적어 보낸 사유를 `Error.message` 로 올리게
  했지만, 이 화면이 그 값을 버리므로 사용자에게 닿는 경로가 아직 없습니다. 감사 AUDIT-FE
  §2.1 이 노린 「사용자가 실패 사유를 본다」는 이 항목에서 닫힙니다.
- 같은 파일 377행의 `/api/kr/refresh` 는 아직 raw `fetch` 라 타임아웃이 걸리지 않습니다.
  `[FE-005]` 는 `krAPI` 와 `paperTradingAPI` 안쪽으로 범위를 그었기 때문에 남았습니다.
- [ ] `updateMarketGate` 실패를 화면에 알림. 기존 오류 표시 방식이 이 화면에 있으면 그것을 씀
- [ ] `/api/kr/refresh` 호출을 `fetchAPI` 로 옮김
- [ ] 같은 화면에 실패를 삼키기만 하는 `catch` 가 더 있는지 확인하고 범위를 정함
- [ ] 「Refresh Market Gate Only」 버튼에 접근 가능한 이름을 붙임. 2026-09-04 실측에서
      이 화면의 이름 없는 버튼은 이것 하나였고, `title` 속성만 있어 accname 명세상
      최후 폴백이라 스크린 리더가 읽지 않을 수 있음. `[FE-024]` 가 종가베팅 화면만
      대상으로 삼아 남았음

### [FE-029] 선택 상자 다섯 개와 헤더 홈 링크에 접근 가능한 이름이 없다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-04 FE-024 사이클의 code-reviewer 지적 5번
  + 같은 사이클의 `/qa-only` ISSUE-001
- QA 시나리오: 다섯 선택 상자와 헤더의 집 모양 링크를 스크린 리더로 훑으면 각각 무엇을
  고르는 상자이고 어디로 가는 링크인지 읽힌다
- `frontend/src/app/dashboard/kr/closing-bet/page.tsx` 의 거래대금·상승률·등급·총점·날짜
  선택 상자가 `<label htmlFor>` 도 `aria-label` 도 갖고 있지 않습니다. 레이블 문구는 형제
  `<div>` 에 있을 뿐이어서 접근성 트리로 이어지지 않고 「콤보 상자」로만 읽힙니다.
- `frontend/src/app/components/Header.tsx:23` 의 `<a href="/">` 도 같은 상태입니다. 안에
  `fas fa-home` 아이콘 하나뿐이어서 「링크」라고만 읽히고 어디로 가는지 말하지 못합니다.
  화면의 링크 23개 가운데 이름이 없는 것은 이 하나입니다.
- `[FE-024]` 가 고친 버튼과 같은 등급의 WCAG 4.1.2 실패입니다. 그 항목이 `button` 만
  대상으로 삼았기 때문에 둘 다 남았습니다.
- 회귀 검사도 `button` 만 훑습니다. 고칠 때 `page.regression-fe-024.test.tsx` 의
  `unnamedButtons` 를 `button, select, a` 로 넓히면 함께 고정됩니다.
- [ ] 다섯 선택 상자에 `aria-label` 또는 `<label htmlFor>` 를 붙임
- [ ] 헤더의 홈 링크에 `aria-label` 을 붙임
- [ ] 회귀 검사의 훑는 범위를 `select` 와 `a` 까지 넓힘
- [ ] 다른 화면의 선택 상자와 아이콘 링크도 같은 상태인지 확인하고 범위를 정함

### [FE-030] 모달이 열려도 초점이 옮겨 가지 않고 배경이 살아 있다
- 카테고리: 프론트엔드 공통 | 티어: T2 | 근거: 2026-09-04 FE-024 사이클의 code-reviewer 지적 8번
- QA 시나리오: 모달을 열면 초점이 모달 안으로 들어가고 Tab 이 배경으로 새지 않는다
- `frontend/src/app/components/Modal.tsx` 와 `ConfirmationModal.tsx` 가 열릴 때
  초점을 옮기지 않습니다. 초점이 방아쇠 버튼에 그대로 남습니다.
- 배경에 `inert` 나 `aria-hidden` 이 없어서 가상 커서로 뒤쪽 내용을 계속 읽을 수 있고,
  Tab 이 모달 밖으로 나갑니다.
- `[FE-024]` 가 세운 지출 확인 절차가 이 컴포넌트 위에 서 있습니다. 확인 모달이 열린
  사실이 스크린 리더 사용자에게 전달되지 않으므로, 그 사용자에게는 안전장치가 조용히
  열립니다. 파괴적 조작에서는 「취소」에 초기 초점을 두는 것이 관례입니다.
- **2026-09-05 `[FE-007]` 이 이 항목의 일부를 이미 해결했습니다.** 모달 여섯 벌의 셸을
  `Modal.tsx` 의 `ModalShell` 하나로 모으면서 Escape 닫기와 배경 클릭 닫기와
  `role="dialog"`·`aria-modal`·`aria-labelledby` 셋이 어느 모달에서든 함께 따라옵니다.
  `ConfirmationModal` 이 Escape 로 닫히지 않던 것과 `aria-labelledby` 가 없던 것이 그
  사이클에서 사라졌습니다. 남은 것은 초점 관리와 배경 비활성화입니다.
- **배경 스크롤도 잠기지 않습니다.** 모달이 열려 있어도 `body` 와 `html` 의 `overflow` 가
  `visible` 이어서 모달 위에서 휠을 굴리면 뒤 페이지가 그대로 움직이고, 닫고 나면 처음
  보던 자리가 아닙니다. 2026-09-05 `[FE-007]` 사이클의 `/qa-only` ISSUE-003 으로
  실측했습니다. 초점 가두기와 같은 자리에서 해결되므로 함께 다룹니다.
- **`ConfirmationModal` 은 `role="alertdialog"` 를 써야 합니다.** 되돌릴 수 없는 조작을
  확인받는 대화상자이므로 낭독기가 그 사실을 알려야 합니다. `[FE-007]` 사이클의
  code-reviewer 지적이며 그 항목의 범위 밖이라 이월했습니다.
- 앱 전체의 모달이 이 둘을 쓰므로 한 곳을 고치면 모든 화면이 함께 바뀝니다. 그래서 T2 이고
  화면별 확인이 필요합니다.
- **공용 컴포넌트를 쓰지 않는 모달이 하나 더 있습니다.** 종가베팅 카드의 「상세 분석 보기」로
  여는 종목 상세 모달은 `page.tsx` 가 직접 그리는 `fixed inset-0 z-[100]` 오버레이여서
  `role="dialog"` 도 `aria-modal` 도 `aria-labelledby` 도 없습니다. 2026-09-04 사이클의
  `/qa-only` ISSUE-002 로 실측했습니다. 공용 둘을 고쳐도 이 모달은 그대로이므로 함께
  다뤄야 합니다. Escape 로 닫히는 것은 확인했습니다.
- **같은 화면 안에서 두 모달이 갈립니다.** 「종가베팅 점수표」 버튼이 여는 모달에는
  `role="dialog"` 가 있고 상세 모달에는 없습니다. 2026-09-05 `[JONGGA-006]` 사이클의
  `/qa-only` 도 같은 화면에서 「기준표」 모달과 상세 모달을 대조해 같은 결과를 얻었습니다. 다만 초점을 옮기지 않는 것과 배경
  `overflow` 가 `visible` 로 남는 것은 **둘 다 같습니다.** 그래서 `role` 만 맞추면 나머지
  절반이 남습니다. `[FLOW-011]` 사이클의 `/qa-only` 가 한 화면에서 둘을 대조했습니다.
- [ ] `ModalShell` 에 초기 초점과 초점 가두기를 넣음
- [ ] `ModalShell` 에 배경 스크롤 잠금을 넣음
- [ ] `ConfirmationModal` 을 `role="alertdialog"` 로 바꾸고 「취소」에 초기 초점을 둠
- [ ] 종가베팅 상세 모달에 `role="dialog"` 와 `aria-modal` 과 `aria-labelledby` 를 더함
- [ ] 모달을 쓰는 화면 전부를 훑어 초점이 튀지 않는지 확인
- [x] `ConfirmationModal` 에 Escape 닫기와 `aria-labelledby` 를 더함 — `[FE-007]` 에서 완료

### [JONGGA-032] 종가베팅 상태 폴링이 화면을 떠난 뒤에도 계속 돈다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-04 FE-024 사이클의 code-reviewer 지적 10번
- `frontend/src/app/dashboard/kr/closing-bet/page.tsx` 의 `pollStatus` 가 2초 간격
  `setInterval` 과 350초 `setTimeout` 을 걸지만 언마운트 정리가 없습니다.
- 스크리너 전체 업데이트를 실행한 뒤 다른 화면으로 옮기면, 인터벌이 계속 돌면서 이미
  사라진 컴포넌트에 `setState` 를 부릅니다.
- 회귀 검사 `page.regression-fe-024.test.tsx` 의 「실행을 눌러야 스크리너 전체 업데이트가
  나간다」가 이 경로를 실제로 밟습니다. 지금은 통과하지만 타이머가 검사 뒤에도 남습니다.
- [ ] 인터벌 id 를 `useRef` 에 담고 언마운트 시 정리
- [ ] 350초 안전장치 타이머도 함께 정리
- [ ] 회귀 검사가 타이머를 남기지 않는지 확인

### [FE-028] 헤더의 모바일 검색 버튼이 아무 동작도 하지 않는다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-04 FE-024 사이클의 실측
- QA 시나리오: 좁은 화면에서 헤더의 돋보기 버튼을 누르면 검색할 수단이 나타난다
- `frontend/src/app/components/Header.tsx:76` 의 버튼에는 `onClick` 이 없습니다. 눌러도
  아무 일이 일어나지 않습니다. `lg` 미만 폭에서만 보이므로 데스크톱에서는 드러나지
  않습니다.
- 바로 위 `lg` 이상에서 보이는 검색 입력 상자에도 `onChange` 나 `onSubmit` 이 없어서
  글자를 넣어도 아무 데도 전달되지 않습니다. 옆의 `⌘K` 표시도 그 단축키를 듣는 곳이
  없습니다. 즉 헤더의 검색은 화면에만 있고 기능이 없습니다.
- `[FE-024]` 가 이 버튼에 `aria-label="검색"` 을 붙였습니다. 이름이 붙어 스크린 리더가
  읽어 주게 되었으므로, 눌러도 아무 일이 없다는 사실이 오히려 더 드러납니다.
- 결정할 것은 검색 기능을 만들지, 아니면 셋을 모두 지울지입니다. 지우기로 하면 이
  항목의 티어는 T1 이고, 만들기로 하면 새 항목으로 다시 잡습니다.
- [ ] 검색 기능을 만들지 지울지 결정
- [ ] 결정에 따라 버튼과 입력 상자와 `⌘K` 표시를 함께 처리
- [ ] 헤더에 이름 없는 버튼이 남지 않는 것을 vitest 로 확인

### [JONGGA-030] 가격 서비스의 티커 정규화가 영문자 코드를 모른다
- 카테고리: 종가베팅 | 티어: T3 | 근거: 2026-09-04 JONGGA-017 사이클의 리뷰
- QA 시나리오: 영문자 코드를 가진 종목의 카드에 현재가와 수익률이 그려진다
- `services/kr_market_realtime_price_service.py:42` 의 `normalize_ticker` 는
  `str(ticker).zfill(6)` 한 줄입니다. 대소문자를 정규화하지 않으므로 소문자가 섞인
  `0007c0` 이 들어오면 가격 맵의 키가 `0007c0` 이 됩니다. 조회하는 쪽인
  `_normalize_ticker` 는 `[JONGGA-017]` 이후 `0007C0` 을 내므로 두 값이 어긋나
  가격이 붙지 않습니다.
- 같은 파일의 `_is_normalized_unique_ticker_list` 는 `ticker.isdigit()` 를 요구합니다.
  영문자가 든 코드는 늘 「정규화 안 됨」으로 판정되어, 이미 정규화된 목록을 그대로
  돌려주는 최적화 경로를 타지 못하고 매번 다시 정규화합니다.
- 종가베팅 카드의 현재가가 붙는 경로가 여기입니다.
  `services/kr_market_csv_utils.py:228` 의 `build_latest_close_map_from_prices_df` 가
  `df_prices["ticker"].astype(str).str.zfill(6)` 로 가격 맵의 키를 만듭니다. 조회하는
  쪽은 `_normalize_ticker` 를 거치므로, 두 쪽이 같은 함수를 공유하는 것이 아니라
  결과가 우연히 같은 두 함수입니다. `daily_prices.csv` 에 소문자나 시장 표시가 섞이는
  날 어긋납니다.
- 같은 `zfill(6)` 방식을 `engine/` 과 `services/` 의 여러 파일이 각자 정의해 쓰고
  있습니다. `services/paper_trading_sync_service.py:14`,
  `services/paper_trading_price_fetchers.py:19`,
  `services/kr_market_realtime_price_cache.py:62`,
  `engine/collectors/krx_data_mixin.py:22`,
  `services/kr_market_vcp_cache_update_service.py:17` 이 그렇습니다. 이름이 모두
  `_normalize_ticker` 여서 한 함수처럼 보이지만 서로 다른 구현입니다.
- 사용자가 겪는 일: 지금은 자료가 대문자로만 들어와 드러나지 않습니다. 어긋나더라도
  가격이 붙지 않을 뿐 남의 가격이 붙지는 않으므로 `[JONGGA-017]` 보다 덜 급합니다.
- 티어 근거: 가격 캐시 계열 파일이라 §2 저장소 스키마에 닿습니다.
- [ ] 가격 서비스의 티커 정규화를 `_normalize_ticker` 와 같은 규약으로 맞춤
- [ ] `_is_normalized_unique_ticker_list` 의 판정에 영문자 코드를 반영
- [ ] 소문자 코드가 들어와도 가격이 붙는지 검사로 고정

### [JONGGA-031] 카드의 「종가」 타일이 날짜에 따라 다른 날의 종가를 가리킨다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-04 JONGGA-017 사이클의 `/qa-only` ISSUE-001
- QA 시나리오: 카드의 「종가」가 어느 거래일의 값인지 화면에서 알 수 있고, 같은 카드의
  「매수가」와 기준일이 다르면 그 사실이 드러난다
- 최신 리포트에서 카드의 「종가」 타일이 `current_price` 를, 「전략 포인트」의 「매수가」가
  `entry_price` 를 그립니다. 두 값이 다르면 같은 카드 안에 두 숫자가 나란히 놓이는데
  「매수가」에만 날짜가 붙어 있습니다. 2026-09-04 실측: LG에너지솔루션 `373220` 이
  「종가 ₩365,500」과 「매수가 ₩362,000 (2026-09-03 종가)」, 삼성중공업 `010140` 이
  「종가 ₩21,650」과 「매수가 ₩21,450 (2026-09-03 종가)」입니다. 한화생명 `088350` 은
  두 값이 같아 드러나지 않습니다.
- 지난 날짜를 고르면 두 값이 언제나 같습니다. 이력 응답
  (`/api/kr/jongga-v2/history/<날짜>`)이 `current_price` 와 `entry_price` 를 같은 값으로
  돌려주기 때문입니다. 그래서 같은 타일이 최신 리포트에서는 오늘 종가를, 지난 날짜에서는
  신호일 종가를 가리킵니다.
- 툴팁은 「가장 최근 거래일의 종가입니다」 한 가지 뜻만 설명합니다. 지난 날짜에서는 그
  설명이 맞지 않습니다.
- 사용자가 겪는 일: 최신 리포트의 카드 한 장 안에서 상승률(신호일 기준), 거래대금(신호일
  확정), 외인·기관 5일(신호일까지)이 모두 신호일 기준인데 종가만 오늘 기준입니다. 타일에
  날짜가 없어 이 차이를 알아챌 방법이 없습니다.
- `[JONGGA-023]` 과 겹치지 않습니다. 그 항목은 툴팁 네 개의 시점 어휘를 맞추는 문구
  작업이고, 이것은 같은 타일이 조회 경로에 따라 다른 필드를 그리는 동작 문제입니다.
- [ ] 「종가」 타일이 어느 거래일의 값인지 화면에 밝힘
- [ ] 최신 리포트와 지난 날짜에서 그 타일의 뜻이 같아지도록 정리
- [ ] 툴팁을 정리한 동작에 맞춤

### [JONGGA-024] 카드의 미니차트가 주가 흐름이 아니라 상승·하락 두 모양만 그린다
- 카테고리: 종가베팅 | 티어: T2 | 근거: 2026-09-03 JONGGA-021 사이클의 `/qa-only` ISSUE-001
- QA 시나리오: 같은 화면의 카드 여러 장에서 미니차트의 모양이 종목마다 다르다
- `page.tsx:2237` 의 `polyline` 이 `signal.change_pct` 의 부호만 보고 고정 좌표 두 벌
  가운데 하나를 고릅니다. 그래서 상승한 종목은 전부 같은 곡선이 되고 하락한 종목도 전부
  같은 곡선이 됩니다.
- 2026-02-11 자료의 네 종목에서 `points` 속성을 읽어 확인했습니다. LG전자(+23.3%),
  아크릴(+26.8%), 메리츠금융지주(+7.4%), 현대제철(+7.3%) 넷 다
  `5,35 15,30 25,28 35,25 45,22 55,18 65,15 75,12 85,8 95,5` 로 문자 하나까지 같습니다.
  선 색깔만 `url(#gradient-<종목코드>)` 로 갈립니다.
- 사용자가 겪는 일: 카드 목록을 훑으며 흐름을 그림으로 읽으려 하면 모든 종목이 똑같이
  완만하게 우상향한 것으로 보입니다. 그림이 아무 정보도 주지 않으면서 정보인 척합니다.
- 상승률 오버레이가 이미 방향을 숫자로 말하고 있고, `[JONGGA-021]` 이 「크게 보기」에
  실제 차트를 붙여 흐름을 확인할 경로가 생겼으므로 긴급도는 낮습니다.
- 같은 자리에 `{/* Chart Area */}` 주석이 `page.tsx:2216` 과 `2217` 두 줄로 겹쳐 있습니다.
- [ ] 미니차트를 실제 자료로 그릴지 차트 모양을 걷어낼지 정함
- [ ] 자료로 그리기로 하면 어느 응답 필드에서 시계열을 얻을 수 있는지 확인
- [ ] 겹친 `{/* Chart Area */}` 주석 정리

### [JONGGA-025] 모바일에서 「크게 보기」 차트의 가격 축 숫자를 읽을 수 없다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-03 JONGGA-021 사이클의 `/qa-only` ISSUE-002
- QA 시나리오: 375x812 에서 차트를 열었을 때 가격 축의 숫자를 판독할 수 있다
- `[JONGGA-021]` 이 붙인 네이버 금융 차트 이미지는 원본이 700×289 인데, 375px 뷰포트에서
  모달 폭에 맞춰 309×128 로 줄어듭니다. 가로가 44% 가 되면서 오른쪽 가격 축(41,974 /
  38,455 / 35,336 …)과 아래 날짜 축의 숫자가 뭉개집니다.
- 캔들의 전체 흐름은 알아볼 수 있으므로 방향은 읽히지만, 어느 가격대인지는 알 수 없습니다.
  모달 아래 네이버 금융·토스 증권 링크로 나가면 확인할 수 있어 막다른 길은 아닙니다.
- 네이버가 이 크기 하나만 제공하므로 더 큰 이미지를 받을 수는 없습니다. 가로 스크롤을
  허용해 원본 크기로 보게 하거나, 좁은 화면에서는 이미지 대신 링크만 안내하는 방식 가운데
  하나를 정해야 합니다.
- [ ] 좁은 화면에서 어떤 표시 방식을 쓸지 정함
- [ ] 정한 방식을 적용하고 375x812 에서 확인

### [FE-027] 모달을 열어도 화면 맨 위 32px 이 가려지지 않는다
- 카테고리: 프론트엔드 공통 | 티어: T2 | 근거: 2026-09-03 JONGGA-021 사이클의 `/qa` 시나리오 밖 발견
- QA 시나리오: 모달이 열린 동안 화면의 어느 지점을 눌러도 모달 뒤의 요소가 반응하지 않는다
- 종가베팅 화면의 모달 오버레이는 `fixed inset-0 z-[100] bg-black/80` 인데, 실제로 그리는
  영역은 `top=32, height=688` 로 화면 맨 위 32px 이 빠집니다. 뷰포트가 1280×720 일 때
  아래쪽 경계는 720 에 정확히 맞으므로 위쪽만 어긋납니다.
- 그 결과 모달이 열려 있어도 상단 32px 띠에 걸친 사이드바 로고와 상단 검색창이 어두워지지
  않고 그대로 노출되며, `document.elementFromPoint(20, 20)` 이 오버레이가 아니라 그
  링크(`href="http://localhost:3500/"`)를 돌려줍니다. 그 자리를 누르면 모달을 닫지 않은 채
  다른 화면으로 이동합니다.
- 「크게 보기」 차트 모달과 「상세 분석 보기」 모달 **둘 다** 같은 값(`top=32, height=688`)이
  나옵니다. 뒤엣것은 `[JONGGA-021]` 이 건드리지 않은 모달이므로 이번 변경이 만든 것이
  아니라 두 모달이 공유하는 오버레이 구조의 문제입니다.
- 원인은 아직 찾지 못했습니다. `getComputedStyle` 은 `position: fixed`, `inset: 0px`,
  `transform: none` 이고, 조상 가운데 `transform` `filter` `backdrop-filter` `perspective`
  `contain` `will-change` `container-type` 을 가진 요소가 하나도 없어 containing block 이
  바뀐 흔적이 없습니다. `visualViewport` 도 `offsetTop=0` `scale=1` 로 정상입니다.
  새로고침해도 같은 값이 나오므로 렌더 타이밍 문제도 아닙니다.
- 다른 화면의 모달도 같은지는 확인하지 않았습니다. 원인을 찾으면 범위가 정해집니다.
- [ ] 오버레이가 32px 밀리는 원인을 찾음
- [ ] 다른 화면의 모달도 같은 현상인지 확인해 고칠 범위를 정함
- [ ] 모달이 열린 동안 상단 32px 의 요소가 클릭되지 않는 것을 확인

### [JONGGA-023] 종가베팅 화면에 남은 표기 어긋남 세 가지
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-03 JONGGA-015 사이클의 `/qa-only`
  ISSUE-004 · ISSUE-005 · ISSUE-006
- QA 시나리오: 카드의 지표 툴팁 네 개가 모두 같은 시점 어휘를 쓰고, 빈 상태 안내가
  한국어이며, 상단 카운터와 화면의 카드 수가 어긋날 때 그 이유를 알 수 있다
- 셋 다 `frontend/src/app/dashboard/kr/closing-bet/page.tsx` 의 문구 문제여서 한 항목으로
  묶었습니다.
- **툴팁의 시점 어휘가 갈립니다.** `[JONGGA-015]` 가 「상승률」을 「신호가 나온 거래일의
  상승률입니다」로, 「종가」를 「가장 최근 거래일의 종가입니다」로 고쳤는데 나머지 둘은
  그대로입니다. 「거래량 배수」는 "최근 20일 평균 거래량 대비 **오늘** 거래량의 배수",
  「거래대금」은 "**오늘** 하루 동안 거래된 총 금액" 이라고 적습니다. 화면 상단이 「오늘
  (2026-09-03) 기준 종가베팅 데이터가 없습니다. 최신 저장 데이터는 2026-09-02입니다」라고
  안내하는 바로 아래에서 같은 값을 「오늘」이라고 부릅니다.
- **빈 상태 안내만 영어입니다.** 필터로 결과가 0건이 되면 `No Signals Found` 와
  `Try adjusting filters or wait for market conditions to improve.` 가 나옵니다. 화면의
  다른 안내는 모두 한국어입니다.
- **상단 카운터가 필터를 반영하지 않습니다.** 거래대금 `1조 이상` 이나 등급 `S급 이상` 을
  걸어 카드가 0장이 되어도 CANDIDATES 와 FILTERED 는 「2」로 남습니다. 두 값의 툴팁은 각각
  「시장에서 1차 필터링된 후보 종목 수」와 「AI 조건에 의해 최종 선별된 종목 수」로 엔진
  단계를 가리키므로 의도된 동작일 수 있습니다. 다만 「FILTERED 2」 바로 아래에 「No Signals
  Found」가 나오면 사용자는 둘을 같은 것으로 읽습니다.
- [ ] 카드 지표 툴팁 네 개의 시점 어휘를 하나로 맞춤
- [ ] 빈 상태 안내를 한국어로 바꿈
- [ ] 카운터가 무엇을 세는지 화면에서 구분되게 하거나 필터를 반영하게 함

### [JONGGA-020] 목표가·손절가의 대체 계산 비율이 실제 자료와 다르다
- 카테고리: 종가베팅 | 티어: T3 | 근거: 2026-09-03 JONGGA-015 사이클의 계획 단계 조사
- QA 시나리오: 목표가와 손절가가 빠진 자료에서도 카드가 다른 자료와 같은 비율을 적용한다
- 자료에 `target_price` 나 `stop_price` 가 없으면
  `app/routes/kr_market_jongga_normalize_helpers.py:104-107` 이 대신 계산합니다. 그때 쓰는
  상수는 `services/kr_market_backtest_common.py:68-69` 의 `JONGGA_TARGET_PCT = 0.09` 와
  `JONGGA_STOP_PCT = 0.05` 입니다.
- 그런데 실제 자료에 담긴 값은 매수가의 `+5%` 와 `-3%` 입니다. 2026-09-03 실측: 태웅
  `37,200 → 39,060 / 36,084`, 메리츠금융지주 `129,800 → 136,290 / 125,906` 으로 네 값 모두
  원 단위까지 그 비율입니다.
- 사용자가 겪는 일: 두 필드가 빠진 자료가 섞이면 그 카드만 `+9%` 와 `-5%` 를 제시합니다.
  `[JONGGA-015]` 가 목표가·손절가 옆에 매수가 대비 비율을 적게 했으므로, 이제 그 어긋남이
  화면에 숫자로 드러납니다.
- 2026-09-03 `/qa-only` 가 더 큰 사실을 찾았습니다. **이 두 상수는 자료가 빠졌을 때만 쓰이는
  대체 계산에 그치지 않고, 「종가베팅 점수표」 모달의 「매매 원칙 (Exit Strategy)」에서
  사용자에게 직접 보입니다.** 그 모달은 「익절 목표 +9%」와 「손절 기준 -5%」를 적고 "누적
  성과 백테스트(backtest_trade_helpers)는 익절(+9%)·손절(-5%) 도달 시 청산하며, 미도달 시
  OPEN 상태로 추적합니다" 라고 밝힙니다. 바로 옆 카드는 `+5.0%` 와 `-3.0%` 를 제시하므로,
  같은 화면에서 두 기준이 나란히 어긋난 채 보입니다.
- 「Trading Tips & Strategy」 아코디언은 세 번째 기준을 댑니다. 「익일 시초 30분 내 +3%
  발생 시 50% 분할 익절」과 「전일 종가 또는 5일선 이탈 시 나머지 전량 매도」입니다.
  고칠 때 이 세 자리를 함께 봐야 합니다.
- 티어 근거: 두 상수는 백테스트가 함께 씁니다. 값을 바꾸면 `services/` 의 백테스트 집계가
  같이 달라지므로 어느 쪽을 기준으로 삼을지부터 정해야 합니다.
- [ ] 목표가와 손절가의 기준 비율을 한 곳에서 정하고 그 출처를 밝힘
- [ ] 대체 계산과 엔진이 실제로 쓰는 비율을 맞춤
- [ ] 백테스트가 그 상수를 어떻게 쓰는지 확인하고 영향을 정리
- [ ] 점수표 모달과 Trading Tips 아코디언의 문구를 정한 기준에 맞춤
- [ ] 두 필드가 빠진 자료로 회귀 검사를 고정

### [JONGGA-018] 가산점이 화면이 밝힌 상한 7점을 넘는다
- 카테고리: 종가베팅 | 티어: T3 | 근거: 2026-09-03 JONGGA-005 사이클의 `/qa` NEW-002
- QA 시나리오: 카드 점수표의 「보너스 (가산점)」이 그 카드가 밝힌 상한을 넘지 않는다
- 카드가 "가산점 (Max 7)" 이라고 적으면서 그보다 큰 값을 표시합니다. 총점도 그만큼
  부풀려집니다.
- 2026-09-03 실측: `2026-02-11` 자료에서 아크릴이 `+9/7`, LG전자가 `+8/7` 입니다. 아크릴의
  총점 16점은 기본 7점과 보너스 9점의 합입니다. 원본 자료
  (`data/jongga_v2_results_20260211.json`)의 `score_details.bonus_score` 가 이미 9와 8이므로
  화면은 자료를 그대로 그린 것이며, 계산은 `engine/scorer.py` 의 `_calculate_bonus` 가
  합니다.
- 티어 근거: 고칠 자리인 `engine/scorer.py` 와 `engine/scorer_scoring_mixin.py` 는 종가베팅
  점수와 등급을 결정합니다. §2 의 「신호와 등급 결정」 절이 다루는 일과 같으므로 T3 으로
  판정합니다. 두 파일은 아직 그 목록에 없으니, 착수하는 사이클의 첫 커밋에서 §4 에 따라
  목록에 추가합니다.
- [ ] 가산점 세 갈래(거래량 급증 5, 장대양봉 1, 상한가 1)의 합이 상한을 넘을 수 있는
      경로를 찾음
- [ ] 상한을 강제할 것인지 화면의 상한 표기를 실제에 맞출 것인지 정함
- [ ] 상한을 넘는 입력으로 검사를 고정
- [ ] 이미 저장된 자료를 어떻게 다룰지 정함. 다시 계산할 것인가 그대로 둘 것인가

### [FE-025] 필터 결과가 0장이어도 상단은 「FILTERED 2」라고 말한다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-03 JONGGA-005 사이클의 `/qa-only` ISSUE-003
- QA 시나리오: 필터로 카드가 0장이 되면 상단 숫자와 빈 상태 안내가 서로 맞는다
- 상승률 `15% 이상`, 등급 `A급 이상`, 거래대금 `5000억 이상` 가운데 무엇을 걸어도 카드가
  0장이 되는데 상단은 계속 `CANDIDATES 2 / FILTERED 2` 입니다.
- `FILTERED` 툴팁이 "AI 조건에 의해 최종 선별된 종목 수입니다" 라고 하므로 사용자 필터와
  무관하게 고정인 것은 설계로 보입니다. 다만 화면에 카드가 없는데 바로 위에 「2」가 적혀
  있으면 목록이 잘못 그려진 것으로 읽힙니다.
- 빈 상태 안내가 영어입니다. `No Signals Found` 와
  `Try adjusting filters or wait for market conditions to improve.` 뿐이며, 화면의 나머지
  안내는 모두 한국어입니다.
- [ ] 필터로 0장이 된 것인지 자료가 없는 것인지 구분되게 안내 문구를 나눔
- [ ] 빈 상태 문구를 한국어로 바꿈
- [ ] 상단 숫자가 무엇을 세는지 화면에서 알 수 있게 함

### [FE-026] 모바일에서 종가베팅 필터 줄의 버튼 문구가 접힌다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-03 JONGGA-005 사이클의 `/qa-only` ISSUE-004
- QA 시나리오: 375x812 에서 필터 줄의 버튼 문구가 두 줄을 넘지 않는다
- 375x812 에서 `종가베팅 전체 10주 매수` 가 세 줄로, `종가베팅 점수표` 가 두 줄로
  접힙니다. 날짜 선택 상자는 `Latest Report` 를 담지 못해 `Lates` 에서 잘립니다.
- 가로 스크롤은 생기지 않고 값도 데스크톱과 같습니다. 읽기 어려울 뿐 동작에는 문제가
  없어 우선순위를 낮게 둡니다.
- [ ] 좁은 폭에서 버튼 문구를 줄이거나 배치를 바꿈
- [ ] 날짜 선택 상자가 선택된 값을 다 보여주도록 함

### [JONGGA-026] 조 단위 금액이 1000억 단위로 뭉개진다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-03 JONGGA-022 사이클의 `/code-review`
- QA 시나리오: 5일 순매수가 1조를 넘는 종목의 카드와 모달에서 1000억 단위 차이가 구분된다
- `[JONGGA-022]` 가 카드와 모달의 금액 표기를 `displayHelpers.ts` 의 `formatBigNumber`
  하나로 합치면서, 카드가 쓰던 `1조 2400억` 형식이 `1.2조` 로 바뀌었습니다. 소수 한
  자리라 1000억 단위 해상도가 되어, 1조 2400억과 1조 2600억이 사실상 같은 문자열이
  됩니다. 외국인·기관 순매수 금액은 투자자가 자릿수까지 보는 값입니다.
- 지금 자료의 5일 순매수는 최대 414억(메리츠금융지주 기관)이라 조 단위에 닿지 않습니다.
  그래서 `[JONGGA-022]` 에서는 고치지 않고 이월했습니다.
- 고치려면 `formatBigNumber` 의 조 단위 갈래를 `${jo}조 ${uk}억` 으로 되돌리면 됩니다.
  다만 이 함수는 시가총액과 재무 3종(매출·영업이익·순이익)도 함께 그리므로 그 다섯
  자리의 표기가 같이 바뀝니다. 좁은 칸에서 길어지는 것을 견딜 수 있는지 함께 봅니다.
- [ ] 조 단위 표기의 해상도를 억 단위로 되돌릴지 판단
- [ ] 시가총액·재무 표기가 함께 바뀌는 것이 읽기에 문제되지 않는지 확인

### [VCP-017] VCP 화면의 금액 포맷이 종가베팅과 다른 반올림 규칙을 쓴다
- 카테고리: VCP | 티어: T1 | 근거: 2026-09-03 JONGGA-022 사이클의 `/code-review`
- QA 시나리오: VCP 화면이 같은 금액을 두 자리에서 보여줄 때 두 값의 문자열이 같다
- `frontend/src/app/dashboard/kr/vcp/page.tsx:928` 의 `formatFlow` 가
  `Math.floor`(내림)를 쓰는 반면, `[JONGGA-022]` 가 만든
  `closing-bet/displayHelpers.ts` 의 `formatBigNumber` 는 `toFixed(0)`(반올림)을
  씁니다. 같은 금액 3,276,004,650 을 하나는 32억, 다른 하나는 33억으로 적습니다.
- `[JONGGA-022]` 가 종가베팅에서 고친 것과 정확히 같은 결함 클래스입니다. VCP 화면이
  아직 같은 금액을 두 자리에서 보여주지 않아 사용자에게 드러나지는 않았습니다.
- 두 함수를 하나로 합치면 `closing-bet/displayHelpers.ts` 를 VCP 가 가져다 쓰는 모양이
  되므로, 합칠 때는 `kr/` 아래 공용 위치로 올리는 편이 맞습니다.
- [ ] 두 포맷 함수의 반올림 규칙을 하나로 맞춤
- [ ] 합친 함수를 두 화면이 함께 볼 수 있는 위치에 둠

### [INFRA-023] 가격 맵이 티커 없는 행을 `000000` 이라는 가짜 종목으로 만든다
- 카테고리: 인프라 | 티어: T2 | 근거: `[JONGGA-005]` 사이클의 변이 검사
- `services/kr_market_csv_utils.py:237` 이 `df_prices["ticker"].astype(str).str.zfill(6)`
  로 티커를 만듭니다. CSV 에 티커가 빈 행이 있고 그 행의 종가가 유효하면 `"000000"`
  이라는 키가 가격 맵에 들어갑니다. 국내 시장에 그런 종목코드는 없으므로 이 키는
  결측을 여섯 자리로 채운 자리채움입니다.
- `services/kr_market_data_cache_prices.py:41` 과 `:63` 의 SQLite 직렬화·역직렬화도
  같은 `zfill(6)` 을 하며, 바로 다음 줄의 `if not ticker_key: continue` 는 zfill 뒤라
  결코 참이 되지 않는 죽은 검사입니다.
- 종가베팅 경로는 `[JONGGA-005]` 가 `_normalize_ticker` 에서 전부 0 인 코드를 빈
  문자열로 돌리도록 고쳐 막았습니다. 남은 것은 맵을 만드는 쪽이며, 이 맵을 함께 쓰는
  VCP 경로가 `"000000"` 을 조회하는지는 확인하지 않았습니다.
- [ ] 빈 티커 행을 맵에서 제외할지, 아니면 `zfill` 앞에서 걸러 낼지 정하고 한쪽으로 정리
- [ ] `if not ticker_key: continue` 두 곳의 죽은 검사 정리
- [ ] VCP 가격 반영 경로가 이 키를 조회하는지 확인하고, 그렇다면 함께 막음
- [ ] 빈 티커가 섞인 가격 DataFrame 에 대한 검사를 남김

### [VCP-016] 두 번째 AI 프로바이더의 GPT 레이트 리밋을 운영 데이터로 관측한다
- 카테고리: VCP 시그널 | 티어: T1 | 근거: `[VCP-015]` 사이클에서 닫지 못한 체크박스
- `[VCP-003]` 이 두 번째 자리에 GPT 폴백을 넣은 뒤로 종목당 LLM 호출이 두 배가 됩니다.
  `[VCP-015]` 가 캐시 판정을 실행 결과와 맞췄으므로 이미 채워진 종목은 재분석에서
  건너뜁니다. 그래서 두 배 호출은 최초 1회 분석에만 남습니다.
- 다만 그 최초 1회에서 GPT 레이트 리밋에 걸리는지는 실측하지 못했습니다. 확인하려면
  실제 LLM API 를 호출해야 하고 비용이 발생하므로, 사용자가 지출을 승인한 자리에서만
  진행합니다.
- 관측 방법: `logs/backend.log` 에서 장 마감 분석이 돌아간 날의 GPT 호출 실패와 429 응답을
  세고, 종목 수 대비 실패율을 본다. 실패율이 유의미하면 호출 간 대기를 넣는다.
- [ ] 운영 로그에서 GPT 429 발생 여부를 확인한다
- [ ] 실패율이 유의미하면 대응 방법을 정하고 별도 항목으로 등록한다

### [FE-020] 랜딩 페이지의 AI 프로바이더 설명을 실제 동작과 맞춘다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: `[VCP-015]` 사이클의 유지보수성 리뷰
- `frontend/src/app/page.tsx:256` 은 GPT 가 "provider 우선순위(gemini→gpt→perplexity)에서
  두 번째로 채택됩니다" 라고 설명합니다. 실제로는 고정 우선순위가 없고
  `VCP_SECOND_PROVIDER` 설정이 두 번째 자리를 정합니다.
- 같은 파일 `:264` 는 Perplexity 가 "Gemini·GPT 모두 응답하지 않을 때 최종 fallback 으로
  동작합니다" 라고 설명하는데, 실제 방향이 반대입니다. Perplexity 는 설정이 지정하면 두
  번째 자리를 차지하는 선택지이고, 호출이 실패하면 오히려 GPT 와 Z.ai 로 폴백합니다.
  `engine/vcp_ai_analyzer._build_perplexity_fallback_chain` 이 그 체인을 만듭니다.
- `[VCP-015]` 가 같은 계열의 오기를 `VCPCriteriaModal.tsx` 에서 고쳤습니다. 이 두 문단은
  대시보드가 아니라 랜딩 페이지라 그 사이클의 범위 밖이었습니다.
- [ ] 두 문단을 실제 동작에 맞게 고친다

### [FE-014] 떠 있는 채팅 위젯이 여러 화면의 본문을 가린다
- 카테고리: 프론트엔드 | 티어: T1 | 근거: 2026-09-02 VCP-007 마감 qa-only,
  2026-09-02 VCP-009 마감 qa-only 로 대상 범위 확대
- 오른쪽 아래에 고정된 채팅 위젯(`div.fixed bottom-3 right-3`, z-index 120)이 VCP 표의
  오른쪽 아래와 겹칩니다. 1280×828 에서는 아래 두 행의 GEMINI 배지가, 375×812 에서는
  CONT. 와 ENTRY 값이 덮입니다. 위젯이 `fixed` 라서 문서 스크롤로 해소되지 않습니다.
- 가리는 자리는 VCP 화면 하나가 아닙니다. `궁금한 건 채팅으로 문의하세요` 말풍선이
  종가베팅의 `Trading Tips & Strategy` 카드, 누적 성과의 표 오른쪽, 데이터 상태의
  `Market Gate` 카드 본문도 각각 덮습니다. 위젯이 모든 대시보드 화면에 공통으로 얹히므로
  VCP 화면만 손보면 나머지 세 화면이 그대로 남습니다.
- [ ] 본문 아래쪽에 위젯 높이만큼 여백을 두거나 위젯을 본문 밖으로 옮김
- [ ] 네 화면(VCP·종가베팅·누적 성과·데이터 상태)에서 가려지는 자리가 없는지 확인
- [ ] 두 해상도에서 VCP 표 마지막 행의 오른쪽 셀이 보이는지 확인

### [FE-016] 모바일에서 `VCP 전체 10주 매수` 버튼이 왼쪽으로 잘린다
- 카테고리: 프론트엔드 | 티어: T1 | 근거: 2026-09-02 VCP-009 마감 qa-only
- 375×812 에서 버튼의 좌우 좌표가 -47 부터 99 까지입니다. 왼쪽 47px 이 화면 밖에 있어
  `VCP 전체 10주 매수` 가 `CP 전체 10주 매수` 로 읽힙니다. 문서의 `scrollWidth` 는 375 로
  뷰포트와 같으므로 가로로 밀어서 되돌릴 수도 없습니다.
- 버튼의 오른쪽 절반은 화면 안에 있어 누를 수는 있습니다. 다만 이 버튼은 실제 매수
  주문을 넣는 자리이므로, 이름을 끝까지 읽지 못한 채 누르게 되는 상태는 위험합니다.
- 2026-09-02 `[FE-013]` 마감 qa-only 에서는 같은 뷰포트에서 좌우 좌표가 10 부터 157 로
  나와 재현되지 않았습니다. 착수하기 전에 어떤 조건에서 잘리는지 먼저 특정합니다.
- 2026-09-02 `[VCP-010]` 마감 qa-only 에서 다시 -47 부터 99 로 재현되었습니다. 재현된 두
  경우는 모두 버튼 줄에 `과거` 날짜 버튼이 함께 놓인 상태였고, 재현되지 않은 `[FE-013]`
  때는 최신 탭이라 그 버튼이 없었습니다. 이 차이부터 확인하면 조건이 좁혀집니다.
- [ ] 잘리는 조건을 먼저 재현하고 그 조건을 항목에 적는다
- [ ] 버튼 행이 좁은 화면에서 줄바꿈되거나 가로로 스크롤되도록 배치 수정
- [ ] 375×812 에서 모든 버튼의 이름이 잘리지 않고 보이는지 확인

### [CHAT-006] 챗봇 SSE 스트림이 NaN 방어를 우회하는지 점검
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-01 VCP-006 의 code-review 지적
- `[VCP-006]` 이 붙인 JSON provider 는 `jsonify` 를 지나는 응답만 덮습니다. 챗봇 스트림은
  `services/kr_market_chatbot_stream_helpers.py:87,91` 에서 `json.dumps` 로 직접 직렬화하므로
  provider 를 지나지 않습니다. 청크에 NaN 이 실리면 프론트엔드의 `JSON.parse` 가 그 줄에서
  예외를 던져 스트림 처리가 끊깁니다.
- 다만 청크에 담기는 값은 모델이 준 문자열과 `usage_metadata` 의 토큰 수라서 NaN 이 생길
  여지가 좁습니다. 실재 여부를 먼저 확인하고, 없으면 항목을 닫습니다.
- [ ] 청크에 담기는 값의 출처를 확인해 NaN 이 실릴 수 있는지 판단
- [ ] 실릴 수 있으면 `sanitize_for_json` 을 거치도록 고치고 회귀 테스트 추가

### [VCP-005] `vcp_ai_analyzer.py` 의 죽은 폴백 코드와 중복 헬퍼 정리
- 카테고리: VCP 시그널 | 티어: T3 | 근거: AUDIT-VCP §3.1, §2.1
- 티어 근거: `engine/vcp_ai_analyzer.py` 는 `tier-rules.md` §2 의 "VCP 판정" 위험 경로에
  올라 있으므로 줄 수와 무관하게 T3 입니다. 리뷰는 `/ponytail-review` → `/code-review` →
  `/review` 순서를 지킵니다. 실행 코드를 바꾸므로 `/qa-only` 는 돌립니다. 화면이 바뀌지는
  않으므로 agent-browser 로 값을 대조할 자리는 없습니다.
- [ ] 호출자가 없는 `_fallback_to_zai` 제거
- [ ] `_resolve_perplexity_fallback_providers` 와 `_build_perplexity_fallback_chain` 의
      실행되지 않는 분기 정리
- [ ] `_extract_status_code` 세 사본을 하나로 통합
- [ ] `max_parse_attempts = 1` 로 죽어 있는 재시도 구조 정리
- [ ] 기존 27건의 Z.ai 테스트가 그대로 통과하는지 확인

### [CHAT-005] 죽은 프롬프트 상수와 레거시 경로 정리
- 카테고리: 챗봇 | 티어: T2 | 근거: AUDIT-CHAT §3.1, §3.2
- 티어 판정: 삭제 위주이며 위험 경로에 닿지 않습니다. 170줄에서 250줄 사이로 보지만,
  구현 후 `git diff --stat` 이 300줄을 넘으면 `tier-rules.md` §3-6 에 따라 `T3` 으로
  올립니다. 위임 믹스인 다섯 개를 평탄화하는 작업은 규모가 따로여서 이 항목에 넣지
  않았습니다.
- [ ] `INTENT_PROMPTS`, `VCP_EXPERT_PERSONA`, `VCP_EXPERT_SUGGESTIONS` 제거
- [ ] 호출자 없는 래퍼 세 개(`_fetch_mock_data`, `_detect_stock_query_from_stock_map`,
      `_fallback_response`) 처리 방향 결정 후 정리
- [ ] `close()` 에 남은 초기화 잔재 제거 (종료 시 종목 맵 재로드 중단)
- [ ] `_CompatGenerativeModel` 과 `_run_legacy_model_chat` 이 아직 필요한지 확인 후 정리
- [ ] 삭제한 심볼을 참조하던 테스트 정리 및 pytest 전체 통과 확인

### [FLOW-006] 백테스트 재노출 전용 계층을 걷어낸다
- 카테고리: 수급·백테스트 | 티어: T2 | 근거: AUDIT-FLOW §3.1
- [ ] `..._service`, `..._calculators`, `..._cumulative`, `..._signal_stats` 네 파일의
      외부 호출자를 확인한 뒤 남길 진입점 하나를 결정
- [ ] 나머지 재노출 계층 제거하고 `app/routes/kr_market_backtest_helpers.py` 의 import 정리
- [ ] `tests/services/test_kr_market_backtest_service.py` 의 import 경로 갱신
- [ ] pytest 전체 통과 확인

### [FLOW-007] 티커 패딩 헬퍼를 공용 유틸로 통합한다
- 카테고리: 수급·백테스트 | 티어: T1 | 근거: AUDIT-FLOW §2.2
- [ ] `..._scenario_helpers` 와 `..._trade_helpers` 의 자체 구현을
      `services.kr_market_csv_utils.get_ticker_padded_series` 로 교체
- [ ] 캐시 컬럼(`_ticker_padded`) 동작이 교체 전후로 같은지 확인
- [ ] `tests/services/test_kr_market_backtest_service.py` 통과 확인

### [INFRA-008] init_data 의 죽은 진입점 정리
- 카테고리: 인프라 | 티어: T3 | 근거: AUDIT-INFRA §3.1, §3.2
- `scripts/init_data.py` 가 위험 경로에 있어 T3 입니다.
- [ ] `create_market_gate`(1993-2096)를 삭제하고 Market Gate 생성 경로가
      `engine/market_gate.MarketGate` 하나임을 확인
- [ ] `reset_cache`(529-534)의 존치 여부를 판단하고 불필요하면 삭제
- [ ] `assign_grade`(91-145)를 실제 등급 판정 경로에 연결하거나,
      `tests/test_grading_logic.py` 와 함께 폐기
- [ ] pytest 전체 통과 확인

### [INFRA-009] init_data 를 책임 단위로 분리
- 카테고리: 인프라 | 티어: T3 | 근거: AUDIT-INFRA §4.1, §5.2
- 선행 조건: `[INFRA-008]` 로 죽은 코드를 걷어낸 뒤 남은 규모로 분리 범위를 다시
  잡습니다. `scripts/init_data.py` 가 위험 경로에 있어 T3 입니다.
- [ ] 열세 가지 책임을 묶어 분리 단위를 확정 (수집 / 생성 / 알림 / CLI 를 후보로 검토)
- [ ] 한 번에 300줄을 넘기지 않도록 여러 사이클로 나누어 진행할 순서를 결정
- [ ] `scripts/debug_details.py:13` 의 깨진 import 를 정리 대상에 포함
- [ ] 분리 대상 함수 가운데 테스트가 없는 `create_jongga_v2_latest` 등에 회귀 테스트 추가
- [ ] 이동한 파일 경로를 `tier-rules.md` §2 위험 경로 목록에 반영

### [FLOW-002] 수급 데이터 분석 고도화
- 카테고리: 수급·백테스트 | 티어: T3 | 근거: docs/plans/TO_DO_LIST.md 이관
- 티어 판정: `services/investor_trend_5day_service.py` 는 `tier-rules.md` §2 의 「수급 집계」에
  있습니다. 경계를 정리하려면 그 파일을 수정하게 되므로 T3 입니다. 2026-09-02 `[INFRA-013]`
  의 백로그 대조에서 T2 오기를 발견해 정정했습니다.
- [ ] 섹터별 수급 집계 설계
- [ ] 기존 `services/investor_trend_5day_service.py` 와의 경계 정리

### [INFRA-002] 타입 힌트 보강
- 카테고리: 인프라 | 티어: T2 | 근거: CLAUDE.md 이관
- [ ] 타입 힌트가 없는 공개 함수 범위 확정
- [ ] 우선순위가 높은 모듈부터 보강
- [ ] 한 번에 300줄을 넘기지 않도록 여러 항목으로 나누어 진행

### [FE-010] 챗봇 안내 말풍선이 본문 카드를 가린다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: [JONGGA-002] 사이클의 qa-only 실측
- 관찰: `/dashboard/kr/closing-bet` 에서 "궁금한 건 채팅으로 문의하세요" 말풍선이
  `Trading Tips & Strategy` 카드의 오른쪽 아래 모서리와 겹쳐 그려지고, 말풍선 자체도
  위아래가 잘려 보입니다.
- 추가 관찰([INFRA-001] 사이클의 qa-only): 뷰포트 폭 900px 에서는 같은 말풍선이 필터 줄
  오른쪽 끝의 새로고침 버튼을 완전히 덮어 클릭 자체를 막습니다. 가리는 데 그치지 않고
  조작을 막으므로 처음 기록한 것보다 심각합니다. 폭 1280px 에서는 사이드바가 펼쳐지며
  배치가 달라져 겹치지 않으므로, 사이드바가 접히는 중간 폭 구간에서만 나타납니다.
- [ ] 말풍선의 위치와 z-index 를 조정해 카드와 겹치지 않게 수정
- [ ] 말풍선이 잘리지 않는지 화면에서 확인
- [ ] 폭 900px 에서 필터 줄의 버튼을 모두 클릭할 수 있는지 확인

### [INFRA-018] numpy 2.x 승격 — 제거된 별칭 참조와 pykrx 상한을 함께 푼다
- 카테고리: 인프라 | 티어: T2 | 근거: [INFRA-001] 사이클의 실측
- numpy 2.x 로 올리는 것을 막는 것이 두 가지입니다. 하나만 풀면 나아가지 못합니다.
  - `numpy_json_encoder.py:38` 이 numpy 2.0 에서 제거된 `np.float_` 을 참조합니다.
  - `pykrx==1.2.3` 이 `numpy<2.0,>=1.24.0` 을 요구합니다. PyPI 메타데이터 실측 결과
    1.2.6 까지가 `numpy<2.0` 이고 1.2.7 부터 `numpy>=2.0` 으로 뒤집힙니다. 즉 pykrx 를
    1.2.7 이상으로 올리는 일과 numpy 승격은 한 묶음이며, 어느 한쪽만 올리면 pip 이
    의존성을 해결하지 못합니다.
- `np.float_` 은 numpy 1.x 에서도 `np.float64` 의 별칭이었으므로, 이미 같은 줄에 있는
  `np.float64` 만 남기면 두 버전에서 모두 동작합니다.
- pykrx 1.2.3 에서 1.2.7 로 네 단계를 건너뛰므로 시세 조회 API 의 반환 형태가 그대로인지
  확인해야 합니다. `engine/collectors.py` 등 19곳이 이 패키지를 씁니다.
- [ ] `np.float_` 참조 제거
- [ ] `pykrx` 를 numpy 2.x 를 허용하는 버전으로 올리고 시세 조회 경로 회귀 확인
- [ ] `requirements.txt` 의 numpy·pykrx 핀과 두 주석을 함께 갱신
- [ ] numpy 2.x 를 설치한 격리 환경에서 pytest 전체 통과 확인

### [FE-011] `npm run test` 가 감시 모드라 자동화에서 멈춘다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: [INFRA-001] 사이클의 실측
- `frontend/package.json:10` 의 `"test": "vitest"` 는 감시 모드로 실행됩니다. 파이프로
  연결해 돌리면 188건을 다 끝낸 뒤에도 워커가 남아 파일 변경을 기다리고, 출력이 버퍼에
  갇혀 아무것도 나오지 않습니다. `[INFRA-001]` 검증에서 실제로 7분을 대기하다가
  프로세스를 끝내고 `npx vitest run` 으로 다시 돌려야 했습니다.
- `CLAUDE.md:53` 과 dev-cycle 검증 절차가 모두 `npm run test` 를 부르므로, 사이클마다
  같은 자리에서 멈춥니다.
- [ ] `test` 를 `vitest run` 으로 바꾸고 감시 모드는 `test:watch` 로 분리
- [ ] `CLAUDE.md` 의 테스트 명령 안내가 그대로 유효한지 확인

### [FE-017] VCP 화면의 아이콘 전용 버튼에 접근 가능한 이름이 없다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-02 FE-013 마감 qa-only
- `/dashboard/kr/vcp` 의 버튼 17개 가운데 7개가 텍스트도 `aria-label` 도 갖고 있지
  않습니다. 그중 모바일 메뉴 토글, 검색, 알림, 사이드바 접기 네 개는 `title` 조차 없어서
  접근성 트리에 이름이 빈 문자열로 올라갑니다. 스크린 리더 사용자는 이 버튼들이 무엇을
  하는지 알 수 없습니다.
- 나머지 세 개는 표의 행별 매수 버튼입니다. `[FE-013]` 이 붙인 `title` 하나에만 기대고
  있는데, `title` 은 HTML-AAM 에서 최후 수단이라 보조 기술 조합에 따라 읽히지 않을 수
  있고 터치 기기에서는 툴팁 자체가 뜨지 않습니다. 비활성 사유를 툴팁으로만 알리므로
  터치 사용자는 왜 눌리지 않는지 알 방법이 없습니다.
- 2026-09-02 `[JONGGA-009]` 마감 qa-only 에서 종가베팅 화면도 같은 상태였습니다.
  버튼 20개 중 8개가 이름 없음이고 그 8개 모두 `title` 이 없습니다. 이름 없는 버튼
  가운데 모바일 메뉴 토글·검색·알림·사이드바 접기는 공용 레이아웃이 만드는 것이라
  한 곳을 고치면 모든 화면이 함께 낫습니다. 제목의 범위를 VCP 화면에서 공용 레이아웃과
  카드 버튼 전체로 넓혀서 착수합니다.
- 2026-09-02 `[JONGGA-010]` 마감 qa-only 에서 종가베팅 카드의 `모의 매수` 버튼도 같은
  상태임을 확인했습니다. 이제 이 버튼이 날짜 조건으로 막히는데, 막힌 사유는 `title`
  로만 전달되므로 375×812 에서는 읽을 방법이 없습니다. 같은 화면 상단의 경고 배너가
  같은 정보를 전달하고 있어서 사유를 전혀 알 수 없는 상태는 아닙니다.
- 2026-09-03 `[VCP-002]` 마감 qa-only 에서 이름 없는 컨트롤 두 개를 더 찾았습니다.
  브레드크럼의 홈 링크(`fas fa-home`)와 떠 있는 채팅 위젯의 열기 버튼입니다. 앞의 것은
  `button` 이 아니라 `a` 여서 버튼만 세던 이전 조사에서 빠져 있었습니다. 이번에는
  `aria-label` 과 `title` 과 텍스트가 모두 빈 `button` 과 `a` 를 함께 조회했고 VCP
  화면에서 다섯 개가 나왔습니다. 나머지 셋은 모바일 메뉴 토글과 검색과 알림입니다.
- [ ] 이름 없는 버튼 네 개에 `aria-label` 을 붙인다
- [ ] 브레드크럼 홈 링크와 채팅 위젯 열기 버튼에도 `aria-label` 을 붙인다
- [ ] 행별 매수 버튼에 `aria-label` 을 붙여 `title` 의존을 줄인다
- [ ] 접근성 트리에서 이 화면의 `button` 과 `a` 가 모두 이름을 갖는지 확인

### [JONGGA-011] 매수 모달이 저장된 과거 시세를 `실시간 시세 적용` 으로 표기한다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-02 JONGGA-009 마감 qa-only ISSUE-002
- 종가베팅 카드에서 `모의 매수` 를 누르면 열리는 모달이 `현재가 (매수가) 134,400원`
  아래에 `● 실시간 시세 적용` 이라고 적습니다. 그런데 이 값은 2026-09-01 저장분의
  현재가와 같아서 오늘 시세가 아닙니다.
- 모달이 실제로 시세를 다시 조회하는지, 아니면 카드가 넘겨준 값을 그대로 쓰는지부터
  확인합니다. 후자라면 문구를 고치고, 전자라면 조회 실패 시의 표기를 정합니다.
- [ ] 모달이 표시하는 값의 출처를 확인한다
- [ ] 출처에 맞게 문구 또는 조회 경로를 바로잡는다

### [VCP-013] 3개월 차트에 거래일이 20일치뿐인데 공백 표시가 없다
- 카테고리: VCP 시그널 | 티어: T1 | 근거: 2026-09-02 VCP-010 마감 qa-only ISSUE-001
- `?period=3m&end=2026-05-05` 응답에 실린 날짜는 `2026-02-04 … 2026-03-06`, `2026-04-30`,
  `2026-05-04` 로 20건입니다. 3월 6일과 4월 30일 사이 약 8주가 통째로 비어 있습니다.
- 차트는 캔들을 균등 간격으로 그리므로 화면에서는 두 캔들이 이웃한 거래일처럼 붙어
  보입니다. 사용자가 두 달 떨어진 두 지점을 연속된 움직임으로 읽게 됩니다.
- 원본 일봉 자료의 공백이므로 차트만 고쳐서는 해결되지 않습니다. 자료를 채우는 방향과
  화면에 공백을 표시하는 방향 가운데 어느 쪽으로 갈지 먼저 정합니다.
- [ ] 원본 자료의 공백이 특정 종목만의 문제인지 전 종목 공통인지 확인
- [ ] 정한 방향에 따라 자료 보충 또는 공백 표시를 구현

### [VCP-014] VCP 상세 모달에 차트 기간을 바꾸는 조작 수단이 없다
- 카테고리: VCP 시그널 | 티어: T1 | 근거: 2026-09-02 VCP-010 마감 qa-only ISSUE-005
- 모달 안의 버튼을 모두 훑으면 이동평균선 토글 여섯 개와 `GPT / Gemini` 탭뿐입니다.
  차트는 항상 3개월로 고정되며 사용자가 바꿀 방법이 없습니다.
- 백엔드는 이미 `?period=` 를 받습니다. `period=1y&end=2026-05-05` 로 76건이 돌아옵니다.
  프론트엔드의 `openChart` 도 기간 인자를 받도록 되어 있으므로, 화면 쪽 조작 수단만
  없는 상태입니다.
- [ ] 상세 모달에 기간 전환 버튼 배치
- [ ] 기간을 바꿔도 선택한 과거 날짜가 유지되는지 확인하는 검사 추가

### [JONGGA-013] AI 분석 리포트 본문의 목표가가 전략 포인트와 어긋난다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-02 JONGGA-007 마감 qa-only ISSUE-002
- SK이노베이션 카드의 AI 분석 리포트 본문은 "130,000원 선을 손절 기준으로 설정하고
  단기 목표가 142,000원 도달 시 분할 익절"이라고 적는데, 바로 아래 전략 포인트는
  `목표가 ₩141,120`, `손절가 ₩130,368` 입니다.
- 한쪽은 LLM 이 문장으로 쓴 어림값이고 다른 쪽은 계산된 값이라 출처가 다릅니다. 두
  숫자가 나란히 놓여 있어서 사용자가 어느 쪽을 따라야 할지 알 수 없습니다.
- [ ] 두 값을 나란히 보여줄지, 본문의 숫자를 계산값으로 맞출지 방향을 정함
- [ ] 정한 방향대로 화면 또는 프롬프트를 조정

### [FE-019] 모바일에서 매수 버튼이 막힌 사유를 읽을 수 없다
- 카테고리: 프론트엔드 | 티어: T1 | 근거: 2026-09-02 JONGGA-010 마감 qa-only ISSUE-001,
  2026-09-02 JONGGA-007 마감 qa-only ISSUE-003 으로 재확인
- 막힌 사유가 `title` 속성으로만 전달됩니다. `title` 은 마우스 커서를 올려야 뜨므로
  터치 환경에서는 읽을 방법이 없습니다.
- 같은 화면 상단의 경고 배너가 같은 정보를 전달하고 있어서 사용자가 이유를 전혀 알 수
  없는 상태는 아닙니다. 두 사이클 연속으로 관찰되었기에 판단을 미루지 않고 등록합니다.
- [ ] 터치 환경에서도 읽히는 사유 표시 방식으로 교체
- [ ] 375×812 에서 사유 문구가 실제로 보이는지 확인

### [CHAT-009] 상태 변경 슬래시 명령으로 시작한 대화의 제목이 명령어로 붙는다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-02 CHAT-002 마감 qa-only ISSUE-002
- 세션 제목은 첫 사용자 메시지에서 만들어집니다. `[CHAT-002]` 가 `/clear` 를 기록에 남기도록
  고친 뒤로, `/clear` 로 시작한 대화는 이후에 어떤 질문을 하든 제목이 `/clear` 로 고정됩니다.
  실측에서 `/clear` 다음에 `1+1은? 숫자만 답해.` 를 보냈는데 사이드바 항목이 `/clear` 로
  표시되었고 저장소의 `title` 도 `/clear` 였습니다.
- `/clear` 를 습관적으로 먼저 누르는 사용자일수록 `최근 대화` 목록이 전부 같은 이름이 되어
  어떤 대화였는지 구분할 수 없습니다.
- 제목을 만들 때 첫 메시지 대신 첫 비명령 메시지를 쓰면 해소됩니다. 기록에 남기는 판정과
  목록에 표시하는 판정이 이미 나뉘어 있으므로 제목 판정도 같은 기준을 쓸 수 있습니다.
- [ ] 제목을 만드는 자리가 첫 비명령 메시지를 고르도록 수정
- [ ] `/clear` 로 시작한 세션의 제목이 뒤이은 질문에서 만들어지는지 검사하는 pytest 추가

### [FLOW-008] mock 라우트가 확정한 백테스트 어휘 밖의 상태를 낸다
- 카테고리: 수급·백테스트 | 티어: T1 | 근거: 2026-09-03 FLOW-004 사이클의 code-review
- `app/routes/common_market_mock_routes.py:94-113` 이 `/kr/backtest-summary` 로 `"OK"` 를
  내보냅니다. `[FLOW-004]` 에서 상태 어휘를 여섯 가지로 확정하며 없앤 값입니다.
- 지금은 도달할 수 없습니다. `app/__init__.py:239-240` 이 `kr_bp` 를 `/api/kr` 로 먼저
  등록하므로 같은 주소에서 실제 라우트가 이깁니다. 그래서 사용자 영향은 없고, 어휘와
  어긋난 채 남은 죽은 코드입니다.
- 등록 순서가 언젠가 바뀌면 화면의 확인 아이콘 판정이 조용히 어긋나므로 지금 정리합니다.
- [ ] 이 mock 라우트를 지울지 어휘에 맞출지 결정하고 반영
- [ ] 같은 파일의 다른 mock 라우트도 도달 가능한지 함께 확인

### [FLOW-009] 화면이 전패 구간과 미집계 구간을 구분해 보여주지 않는다
- 카테고리: 수급·백테스트 | 티어: T1 | 근거: 2026-09-03 FLOW-004 사이클의 code-review
- `[FLOW-004]` 가 백엔드에서 전패(`BAD`)와 미집계(`PENDING`)를 갈라 놓았지만, 화면에서는
  둘 다 승률 0% 에 `미흡` 배지가 붙습니다(`frontend/src/app/dashboard/kr/page.tsx:437-441`).
  달라지는 것은 확인 아이콘의 유무뿐입니다.
- 성적이 가장 나쁜 구간과 아직 판정하지 못한 구간을 사용자가 같은 화면에서 구별할 수
  없으므로, 갈라 놓은 어휘가 화면까지 닿지 않은 상태입니다.
- 같은 사이클의 `/review` 가 범위를 더 넓혀 관찰했습니다. 확정한 여섯 어휘 가운데 화면이
  구분하는 것은 셋뿐입니다. `Accumulating` 은 종가베팅 카드에만 분기가 있고 VCP 카드에는
  없으며(`page.tsx:932, 947`), `OK (New)` 와 `PENDING` 은 어느 카드에서도 별도 표시가
  없어 둘 다 승률 0% 로만 보입니다.
- [ ] `PENDING` 일 때 승률 대신 집계 전임을 알리는 표시로 바꿈
- [ ] `OK (New)` 와 `Accumulating` 을 두 카드에서 같은 방식으로 처리
- [ ] 여섯 어휘가 각각 어떤 표시로 이어지는지 vitest 로 고정

### [INFRA-021] `get_performance_report` 의 손익비가 JSON 으로 옮길 수 없는 값을 낸다
- 카테고리: 인프라 | 티어: T1 | 근거: 2026-09-03 FLOW-004 사이클의 code-review
- `engine/signal_tracker_analysis_mixin.py:474` 가 손실이 0일 때 `float("inf")` 를 손익비로
  돌려주고 `:489` 에서 그대로 dict 에 담습니다. JSON 으로 내보내면 `Infinity` 가 되어
  `JSON.parse` 가 깨지는 형태입니다.
- 지금은 이 함수를 테스트 밖에서 부르는 곳이 없어 노출되지 않습니다. 그래서 우선순위는
  낮지만, `[FLOW-004]` 가 같은 결함을 고친 `aggregate_cumulative_kpis` 와 계산이 같으므로
  한쪽만 고친 채 두면 다음에 이 함수를 쓰는 사람이 같은 함정을 밟습니다.
- [ ] 이 함수를 살릴지 지울지 먼저 판단 (테스트 밖 호출자가 없음)
- [ ] 살린다면 `aggregate_cumulative_kpis` 와 같이 `None` 을 돌려주도록 맞춤

### [FE-021] 누적 성과 화면이 마운트마다 같은 요청을 두 번 보낸다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-03 FLOW-004 사이클의 `/qa-only` 실측
- `/dashboard/kr/cumulative` 에 진입할 때마다
  `GET /api/kr/closing-bet/cumulative?page=1&limit=50` 이 두 번 나갑니다. 응답 크기가
  3232B 로 같아 조건이 다른 요청이 아니라 같은 요청입니다.
- 개발 모드의 React StrictMode 가 effect 를 두 번 실행하는 동작일 수 있으므로, 먼저
  프로덕션 빌드에서 재현되는지 확인한 뒤에 고칠지 판단합니다. 재현되지 않으면 닫습니다.
- 2026-09-04 FE-005 사이클의 `/qa-only` 가 같은 현상을 다른 화면에서도 확인했습니다.
  `/api/kr/user/quota` 가 화면 하나를 여는 동안 두 번에서 네 번 나갑니다. 국내 시장·VCP·
  종가베팅·챗봇 네 화면 모두 그렇습니다. 응답이 109B 로 작고 1~15ms 에 돌아와 체감
  영향은 없지만 원인은 같아 보입니다. 재측정할 때 이 요청도 함께 셉니다.
- 2026-09-04 FE-005 사이클의 `/qa` 가 두 자리를 더 찾았습니다. 모의투자 모달의 수익 차트에서
  기간 버튼 「3개월」을 누르면 `history/asset?days=90` 과 함께 기간과 무관한
  `GET /api/portfolio` 가 다시 나갑니다. 그리고 `/chatbot` 을 열면
  `chatbot/models` 와 `chatbot/sessions` 가 각각 두 번씩 나갑니다. 앞의 것은 StrictMode 로
  설명되지 않으므로 기간 상태가 바뀔 때 조회 함수 둘이 함께 걸려 있는지 확인해야 합니다.
- [ ] `npm run build` 후 프로덕션 서버에서 요청 횟수를 재측정. 누적 성과 화면의
      `cumulative` 요청, 네 화면의 `user/quota` 요청, 챗봇의 `models`·`sessions` 요청을
      함께 셈
- [ ] 수익 차트의 기간 버튼이 `GET /api/portfolio` 를 함께 부르는지 확인. 프로덕션에서도
      그렇다면 기간 변경과 무관한 조회를 떼어냄
- [ ] 프로덕션에서도 두 번 나가면 호출 지점을 한 번으로 모음

### [FLOW-012] 「최근 10건 승률」이 전체 기간이 아니라 현재 페이지의 앞 10건을 본다
- 카테고리: 수급·백테스트 | 티어: T2 | 근거: 2026-09-03 FLOW-010 사이클의 `/review`
- 누적 성과 화면의 결과 분포 툴팁(`CumulativeClientPage.tsx` 의 `renderDistributionTooltip`)이
  「최근 10건 승률」과 「연속 손실」을 현재 페이지의 앞 10행으로 계산합니다. 표는 페이지
  단위로 끊기므로, 2페이지를 열면 그 페이지의 앞 10건이 「최근」이 됩니다.
- `[FLOW-010]` 이 등급 카드에서 고친 것과 같은 종류의 결함이며 같은 화면에 남아 있습니다.
  다만 이쪽은 백엔드가 값을 보내 주지 않아 프론트엔드만으로는 고칠 수 없습니다.
  `aggregate_cumulative_kpis` 가 최근 N건 승률과 연속 손실을 함께 집계해야 합니다.
- 코드에 `We assume 'trades' passed here are the current page's trades` 라는 주석으로
  한계가 이미 적혀 있습니다.
- [ ] 최근 N건의 기준을 정함. 거래 목록의 정렬 기준이 추천일인지 확인하고 N 값을 고정
- [ ] `aggregate_cumulative_kpis` 가 최근 N건 승률과 연속 손실을 함께 집계
- [ ] 응답 구조가 바뀌므로 `_CUMULATIVE_CACHE_SCHEMA_VERSION` 을 함께 올림
- [ ] 툴팁이 그 값을 읽도록 바꾸고 페이지를 넘겨도 바뀌지 않는 것을 vitest 로 고정

### [FE-023] 「평균 수익률」 카드의 등급별 세부가 카드 밖으로 넘친다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-03 FLOW-010 사이클의 `/qa-only` ISSUE-003
- QA 시나리오: 1280x720 에서 이 카드 안에 `S` `A` `B` 세 값이 모두 보인다
- 1280x720 뷰포트에서 카드 제목 「평균 수익률 (S+A+B 합산)」이 두 줄로 접히면서 안쪽 공간이
  모자라, 등급별 세부가 카드 경계 아래로 흘러나오고 `B +0.2%` 는 아예 보이지 않습니다.
- 바로 아래 「누적 수익률 (S+A+B 합산)」 카드는 같은 형태의 세 값을 한 줄에 담고 있어서
  두 카드가 나란히 다르게 보입니다.
- [ ] 카드 폭에 맞게 세부 표기를 배치하고 1280x720 에서 세 값이 모두 보이는 것을 확인

### [JONGGA-028] AI 판정을 담는 세 자리가 화면마다 다르게 읽혀 서로 다른 값을 말한다
- 카테고리: 종가베팅 | 티어: T2 | 근거: 2026-09-03 JONGGA-016 사이클의 code-review 관찰
- QA 시나리오: 한 종목을 재분석한 직후 종가베팅 카드의 추천 배지와 AI 분석 화면의 추천이
  같다. 사유 본문도 같은 판정의 사유를 적는다
- 종가베팅 응답은 AI 판정을 최상위 `ai_evaluation`, `score.ai_evaluation`,
  `score_details.ai_evaluation` 세 자리에 담습니다. `[JONGGA-016]` 이 카드 쪽을
  「최상위 → score」 순으로 정리했는데, 백엔드의 정식 추출기
  `app/routes/kr_market_jongga_ai_payload_helpers.py:16` 의 `_extract_jongga_ai_evaluation`
  은 `score_details → score → 최상위` 로 정반대 순서를 씁니다.
- 재분석(`app/routes/kr_market_jongga_reanalysis_helpers.py:121`)은 새 판정을 최상위에만
  넣고 `score` 에 옛 판정을 남깁니다. 그래서 재분석 직후 카드는 새 판정을, AI 분석 화면은
  낡은 판정을 말합니다. 어느 쪽이 옳은지는 재분석이 최상위를 갱신한다는 사실이 정합니다.
- 사유 본문도 같은 뿌리로 어긋납니다. `frontend/src/app/dashboard/kr/closing-bet/page.tsx`
  의 AI 분석 리포트 본문은 `signal.score.llm_reason` 한 자리만 읽는데,
  `app/routes/kr_market_jongga_normalize_helpers.py:109` 는 `ai_action` 형태의 시그널에서
  사유를 `ai_evaluation.reason` 에만 넣고 `score.llm_reason` 을 채우지 않습니다. 그런
  자료에서는 배지가 판정과 확신도를 그리는 동안 본문이 「AI 분석 대기 중입니다...」라고
  적습니다.
- 백엔드 추출기 자신도 반대 방향의 같은 어긋남을 주석으로 인정하고 있습니다
  (`kr_market_jongga_ai_payload_helpers.py:42`).
- `[JONGGA-008]` 이 다루는 확신도 0 문제와 같은 응답의 다른 필드입니다. 함께 볼 것.
- [ ] 세 자리 가운데 무엇이 정본인지 정하고 백엔드 추출기와 화면이 같은 순서를 쓰게 함
- [ ] 사유 본문이 판정과 같은 원천을 읽도록 맞춤
- [ ] 재분석 직후의 두 화면을 실측으로 대조

### [JONGGA-027] 필터가 카드를 모두 걸러 내면 빈 화면이 자료를 못 받은 것처럼 말한다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-03 JONGGA-019 사이클의 `/qa-only` ISSUE-001, ISSUE-002
- QA 시나리오: 등급 필터로 카드를 모두 걸러 낸 상태에서 빈 화면의 안내가 한국어이고,
  자료를 받지 못한 상황이 아니라 필터 때문임을 밝힌다
- 두 자리가 같은 상황에서 어긋납니다. 2026-09-03 실측: 등급 필터를 「S급 이상」으로 바꾸면
  카드가 0장이 되는데, DATA STATUS 는 `UPDATED 오후 05:03` 이고 CANDIDATES 와 FILTERED 가
  모두 3 이라 자료 자체는 이미 받아 둔 상태입니다.
- 카드 자리에 나오는 안내만 영어입니다. 「No Signals Found」와 「Try adjusting filters or
  wait for market conditions to improve.」인데, 같은 화면의 필터 라벨과 「표시 0 / 전체 3」
  요약과 「필터 초기화」 버튼은 모두 한국어입니다.
- TRENDING THEMES 절이 「데이터 갱신 후 표시됩니다」로 바뀝니다. 자료를 못 받은 상황을
  가리키는 문구인데 실제로는 필터만 걸린 상태이므로, 사용자가 재조회를 눌러야 한다고
  잘못 판단하게 됩니다. 필터를 초기화하면 테마가 그대로 돌아옵니다.
- [ ] 빈 상태 안내를 한국어로 바꾸고 필터 때문임을 밝힘
- [ ] 테마 절이 필터로 비어 있는 경우와 자료를 못 받은 경우를 구분
- [ ] 두 표현이 필터 상태에 따라 갈리는 것을 vitest 로 고정

### [JONGGA-029] 뷰포트 폭 1280px 에서 카드의 「확신도」 라벨이 두 줄로 깨진다
- 카테고리: 종가베팅 | 티어: T1 | 근거: 2026-09-03 JONGGA-016 사이클의 `/qa-only` ISSUE-001
- QA 시나리오: 뷰포트 폭을 1280px 로 맞춘 상태에서 `HOLD` 배지가 붙은 카드의 「확신도」
  라벨이 한 줄로 그려진다
- 2026-09-03 실측: 뷰포트 폭이 **정확히 1280px** 일 때만 재현되며, 그 폭에서는 `HOLD` 배지가
  붙은 카드 전부가 「확신」과 「도」 두 줄로 나뉩니다. 1280px 은 Tailwind 의 `xl`
  브레이크포인트여서 카드가 3열 그리드로 바뀌는 자리입니다.
- 배지 행의 폭이 199px 이면 깨지고 200px 이면 들어가는 1px 차이입니다. 뷰포트 1250px 은
  1열이라 888px 이고, 1285px 은 200px, 1300px 은 204px, 1440px 은 219px 로 모두 정상입니다.
- 배지 자체의 폭은 `BUY` 40px, `HOLD` 48px, 「AI 분석 대기」 69px 입니다. 「AI 분석 대기」는
  배지가 가장 넓은데도 깨지지 않는데, 확신도가 「미산출」이라 16px 짜리 진행 막대를 그리지
  않아 오른쪽 묶음이 그만큼 짧기 때문입니다. 그래서 배지 폭만 줄이는 방향으로는 풀리지
  않고 배지 행 전체의 줄바꿈 규칙을 봐야 합니다.
- 값 자체는 정확하게 그려지므로 심각도는 Low 입니다.
- [ ] 1280px 에서 배지 행이 199px 로 좁아지는 원인을 찾음
- [ ] 확신도 라벨이 줄바꿈되지 않도록 고침
- [ ] 뷰포트 1280px 과 그 앞뒤 폭을 실측으로 대조

### [CHAT-013] 스트리밍 도중의 전송과 세션 전환이 서로를 덮어쓴다
- 카테고리: 챗봇 | 티어: T2 | 근거: 2026-09-05 `[CHAT-004]` 사이클의 code-review 와 `/review`
- 세 현상이 한 뿌리에서 나옵니다. `useChatStream` 이 SSE 분기에 들어가자마자
  `setIsLoading(false)` 를 부르기 때문입니다(`frontend/src/app/chatbot/useChatStream.ts:171`).
  이 동작은 `[CHAT-004]` 이전의 `page.tsx` 에도 그대로 있었으므로 새로 생긴 결함이 아닙니다.
  - 첫 바이트가 도착한 뒤에는 「답변 중단」 버튼이 화면에서 사라집니다. `page.tsx:1022` 의
    삼항이 `isBusy` 로 갈리는데 그때 거짓이 되고, 입력 칸도 비어 있어 다른 가지의 조건도
    빗나가 아무 버튼도 그려지지 않습니다. `handleStop` 을 누를 방법이 없습니다
  - 같은 이유로 `:107` 의 전송 가드도 풀립니다. Enter 로 두 번째 질문을 넣으면 첫 스트림이
    중단되지 않은 채 `abortControllerRef` 만 덮이고, 두 스트림이 같은 마지막 메시지에
    델타를 섞어 씁니다
  - 응답이 흐르는 도중 다른 세션을 열면 `applyToLastMessage` 가 그 세션 기록의 마지막
    메시지에 델타를 붙입니다. 기존 회귀 검사
    `page.regression-chat-001.test.tsx` 의 세 번째 시나리오는 `localStorage` 값만 보고
    메시지 내용을 보지 않아 이 자리를 잡지 못합니다
- 티어 판정: `useChatStream.ts` 와 `page.tsx` 의 버튼 조건 한 곳입니다. 마지막 메시지의
  `isStreaming` 을 판정에 더하는 정도라 300줄 아래로 봅니다.
- [ ] 스트리밍이 진행 중인 동안 중단 버튼이 계속 보이도록 판정을 고침
- [ ] 스트리밍 중 두 번째 전송을 막거나, 앞 스트림을 중단하고 이어받도록 정함
- [ ] 세션이 바뀐 뒤 도착한 델타를 버리도록 스트림에 소속 세션 검사를 더함
- [ ] 위 셋을 각각 붙잡는 vitest 검사 추가

### [CHAT-014] 모델이 낸 추천 질문에 개수와 길이 상한이 없다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-004]` 사이클의 `/review`
- `frontend/src/app/chatbot/chatMessageParser.ts:112-116` 이 「[추천 질문]」 뒤의 모든 줄을
  버튼으로 만듭니다. 빈 줄만 거르고 개수도 길이도 제한하지 않습니다. 모델이 긴 목록을 내면
  답변 아래가 버튼으로 채워집니다.
- React 가 텍스트를 이스케이프하고 `react-markdown` 이 원시 HTML 을 렌더하지 않으므로
  스크립트가 실행되는 경로는 없습니다. 화면이 망가지는 문제입니다.
- 이 동작은 `[CHAT-004]` 이전에도 같았습니다.
- [ ] 추천 질문의 개수 상한과 한 줄 길이 상한을 정하고 적용
- [ ] 상한을 넘는 입력에서 무엇이 잘리는지 검사로 고정

### [FE-038] 인증 헤더를 만드는 코드가 네 곳에 흩어져 규칙이 서로 다르다
- 카테고리: 프론트엔드 공통 | 티어: T2 | 근거: 2026-09-05 `[CHAT-004]` 사이클의 code-review
- `[CHAT-004]` 이 챗봇 쪽 `getAuthHeaders` 를
  `frontend/src/app/components/chatHelpers.ts` 로 올렸습니다. 그런데 같은 헤더를
  `ChatWidget.tsx:248`, `Sidebar.tsx:110`, `dashboard/kr/vcp/page.tsx:1026` 이 각자 만들고
  있고 규칙이 다릅니다. 그쪽은 이메일이 없을 때 빈 문자열을 보내며 `user@example.com`
  자리표시자도 거르지 않습니다.
- 영향: 같은 사용자가 어느 화면에서 요청했는지에 따라 서버가 보는 신원이 달라집니다.
  `[INFRA-027]` 이 헤더 한 줄로 쿼터를 우회하는 문제를 다루고 있어 그 항목과 함께 보면
  좋습니다.
- 2026-09-05 `[CHAT-016]` 이 VCP 화면 몫을 먼저 해소했습니다. 그 화면이 읽던
  `localStorage` 의 `user_email` 키는 저장소 어디에서도 기록되지 않아 언제나 `null` 이었고,
  그래서 로그인한 사용자가 만든 세션을 본인도 열지 못했습니다. 전송·조회·삭제 다섯 자리를
  `getVcpChatHeaders` 하나로 모아 `chatHelpers.getAuthHeaders()` 를 쓰게 했습니다.
  **남은 것은 `ChatWidget.tsx` 와 `Sidebar.tsx` 두 곳입니다.**
- [ ] 남은 두 곳(`ChatWidget.tsx`, `Sidebar.tsx`)이 `chatHelpers` 의 `getAuthHeaders` 를
      쓰도록 통일
- [x] VCP 화면의 죽은 `user_email` 키를 없애고 전송·조회·삭제 다섯 곳을 함께 고침
      (`[CHAT-016]` 에서 완료)
- [ ] 이메일이 없을 때와 자리표시자일 때의 규칙을 한 곳에서 정함
- [ ] 네 경로가 같은 헤더를 만드는지 vitest 로 고정

### [CHAT-015] 답변의 소제목이 빈 제목과 번호 목록으로 쪼개진다
- 카테고리: 챗봇 | 티어: T1 | 근거: 2026-09-05 `[CHAT-004]` 사이클의 `/qa-only` ISSUE-001
- 모델이 `### 1. 시장 환경 및 섹터 강도` 같은 소제목을 쓰면, 화면에서는 내용이 빈 `h3`
  하나와 그 뒤의 별도 번호 목록으로 갈라집니다. 답변 하나에 이런 제목이 셋 있으면 빈 `h3`
  도 셋 생깁니다.
- 원인은 `frontend/src/app/chatbot/chatMessageParser.ts:160` 의 치환입니다.

      processed = processed.replace(/(?<=\S)\s+(?=(?:\*\*|__)?\d+\.\s)/g, '\n\n');

  「빽빽한 문장 안의 번호 목록 앞에 줄바꿈을 넣는다」는 뜻인데, `###` 의 마지막 `#` 이
  `(?<=\S)` 에 걸리고 그 뒤 공백과 `1. ` 이 나머지 조건을 채웁니다. 그래서
  `### 1. 제목` 이 `###\n\n1. 제목` 이 됩니다. Node 로 확인했습니다.
- `:172` 의 `cleanReasoning` 에도 같은 치환이 있으므로 추론 영역에서도 같은 일이
  일어납니다. 한 군데만 고치면 나머지가 남습니다.
- 영향: 답변의 단락을 나누던 소제목이 본문과 같은 크기의 목록 항목으로 보여 답변을
  훑어보기 어렵습니다. 스크린 리더에는 이름 없는 제목 셋이 읽혀 제목으로 건너뛰는 이동이
  무력해집니다.
- 이 동작은 `[CHAT-004]` 이전에도 같았습니다. 그 사이클은 `preprocessMarkdown` 과
  `extractSuggestions` 를 `page.tsx` 에서 이 파일로 **옮기기만 하고 로직을 고치지
  않았습니다**.
- 티어 판정: 한 파일의 정규식 두 곳입니다. 앞이 `#` 으로 끝나는 경우를 제외하는 조건을
  더하는 정도라 50줄 아래입니다.
- [ ] 제목 줄(`#` 으로 시작하는 줄)을 이 치환의 대상에서 제외
- [ ] 본문과 추론 두 곳에 같은 규칙을 적용
- [ ] `### 1. 제목` 이 `h3` 하나로 렌더되는지, 빽빽한 문장 속 번호 목록은 여전히 갈라지는지
      vitest 로 고정

### [FE-039] 설정 모달의 「직무 / 역할」이 저장된 값을 반영하지 않는다
- 카테고리: 프론트엔드 공통 | 티어: T1 | 근거: 2026-09-06 `[CHAT-026]` 사이클의 `/qa-only` ISSUE-003
- QA 시나리오: 목록에 없는 페르소나가 저장된 상태에서 설정 모달을 열면 「직접 입력」이
  선택되고 그 값이 입력창에 보인다
- 「직무 / 역할」은 여섯 옵션(`엔지니어링` `리서치` `디자인` `트레이딩` `학생` `직접 입력`)을
  가진 `select` 입니다. 저장된 페르소나가 그 목록에 없으면 `select` 의 값 매칭이 실패해
  첫 옵션인 `엔지니어링` 이 선택된 것처럼 보입니다.
- 실측에서 서버의 `/api/kr/chatbot/profile` 은 `주식 투자를 배우고 있는 열정적인 투자자` 를
  돌려주는데 세 경로 모두 화면에는 `엔지니어링` 이 떴습니다. 사용자는 자신이 무엇을 골라
  두었는지 화면에서 확인할 수 없고, 그 상태로 저장하면 페르소나가 `엔지니어링` 으로
  바뀝니다.
- 「직접 입력」 옵션이 이미 있으므로 목록에 없는 값을 그 자리로 보내면 됩니다.
- [ ] 저장된 페르소나가 목록에 없을 때 「직접 입력」이 선택되게 함
- [ ] 그때 입력창에 저장된 문자열이 채워지는지 확인
- [ ] vitest 로 목록 밖 값의 표시를 고정

# [FLOW-030] [FLOW-031] [JONGGA-044] 오늘 키 참조 비저장·기준일 행 없는 CSV 창 버림·테스트 시각 고정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 확정 전일 수 있는 오늘 키 수급 참조를 저장하지 않고, 기준일 행이 없는 CSV 창을 기준일 5일 값으로 쓰지 않으며, 자정 전후에 흔들리는 vitest 회귀 검사를 고정한다.

**Architecture:** `_get_reference_trend_cached` 의 저장 갈래에 「키 날짜 < 오늘」 조건 하나를 더하고 참조 SQLite 서명을 v3 로 올린다. `_build_trend_map` 은 기준일을 받았을 때 창 끝 다음 날부터 기준일까지 빈 영업일이 있으면 빈 맵을 돌려준다. vitest 파일은 `beforeEach` 에서 `vi.setSystemTime` 으로 KST 정오에 고정한다.

**Tech Stack:** Python 3.11, pandas, numpy(`busday_count`), pytest, vitest 4.1.11

**Spec:** 별도 설계 문서 없음(bounded). 승인 범위는 `docs/dev-cycle/TODO.md` 의 세 항목 `설계 승인` 줄과 2026-09-26 00:25 대화(「승인 A로 해」).

## Global Constraints

- 운영 코드는 `services/investor_trend_5day_service.py` 한 파일만 바꾼다. Ruling(2026-09-26 01:0x, `/review` M-1 뒤 사용자 선택): `engine/market_schedule.py` 의 `_fallback_holidays` 에 2026 하반기 휴장일 9일을 더한다.
- pytest 는 `KRX_ID= KRX_PW= venv/bin/python -m pytest -q -p no:cacheprovider` 로 돌린다(KRX 로그인 금지).
- 원본 `data/` 는 읽기 전용이다. 실제 KRX·Toss·LLM 호출, 서버 재기동을 하지 않는다.
- 결측과 실제 0 을 가른다(`.claude/skills/closing-bet-python/SKILL.md`).
- 새 백로그 항목을 만들지 않는다. 남는 한계는 아카이브 메모에 적는다.

## Review Focus

1. 오늘 키 값이 메모리에 남으면 오래 도는 워커에서 다음 날 같은 기준일 조회가 확정 전 값을 받는다. 메모리와 SQLite 둘 다 막혔는지 본다.
2. 최신 창(기준일 없음)의 pykrx 키는 `_resolve_pykrx_latest_market_date` 로 정해진다. 평일에는 오늘, 주말에는 금요일이라 기존 저장 검사가 요일에 따라 갈린다. 검사는 날짜를 고정해야 한다.
3. 기준일이 토요일이고 CSV 가 금요일로 끝나면 버리지 않아야 한다. 창 끝과 기준일이 같으면 빈 영업일 수는 0 이어야 한다.
4. ~~창 끝 뒤의 부분 날짜(`recent_trading_dates` 가 뺀 날)가 기준일이면 그 날도 빈 영업일로 세어 버린다.~~ Ruling(2026-09-26 00:5x, `closing-bet-reviewer` F1 뒤 사용자 선택): 기준일을 준 호출은 끝쪽 부분 수집일을 빼지 않고 CSV 의 마지막 5개 날짜를 창으로 쓴다. 그날 행이 없는 종목만 빠진다. 최신 창은 트리밍을 유지한다.
5. vitest 의 `Date` 고정이 렌더 중 `findByRole`(실제 타이머)을 멈추지 않아야 한다.

---

### Task 1: [FLOW-030] 오늘 키 참조를 저장하지 않고 서명을 v3 로 올린다

**Files:**
- Modify: `services/investor_trend_5day_service.py` (`_reference_sqlite_context` 의 서명, `_get_reference_trend_cached` 의 저장 갈래, `get_investor_trend_5day_for_ticker` docstring)
- Test: `tests/services/test_investor_trend_5day_service.py`

**Interfaces:**
- Consumes: `_get_reference_trend_cached(*, data_dir, source, ticker, target_datetime)`, `_reference_sqlite_context(...)`, 기존 헬퍼 `_complete_reference(value)`
- Produces: 테스트 헬퍼 `_freeze_now(monkeypatch, moment: datetime) -> None` (Task 2 는 쓰지 않음)

- [ ] **Step 1: 실패하는 검사를 쓴다**

```python
def _freeze_now(monkeypatch, moment: datetime) -> None:
    class _Frozen(datetime):
        @classmethod
        def now(cls, tz=None):
            return moment

    monkeypatch.setattr(trend_service, "datetime", _Frozen)


def test_a_reference_keyed_today_is_returned_but_not_cached(monkeypatch, tmp_path):
    """[FLOW-030] 오늘 키 참조는 장중·시간외 부분 값일 수 있다. 조회한 호출에만 주고, 다음 날 같은 키를 처음 묻는 호출이 저장한다."""
    trend_service.clear_investor_trend_5day_memory_cache()
    calls = []
    monkeypatch.setattr(
        trend_service, "_fetch_pykrx_reference_trend", lambda **_k: calls.append(1) or _complete_reference(100 * len(calls)),
    )
    kwargs = {"data_dir": str(tmp_path), "source": "pykrx", "ticker": "005930", "target_datetime": "2026-02-24"}
    sqlite_key, signature = trend_service._reference_sqlite_context(**kwargs)

    _freeze_now(monkeypatch, datetime(2026, 2, 24, 17, 5))
    assert trend_service._get_reference_trend_cached(**kwargs)["foreign"] == 500
    assert trend_service._get_reference_trend_cached(**kwargs)["foreign"] == 1_000  # 두지 않아 다시 묻는다
    assert sqlite_payload_cache.load_json_payload_from_sqlite(filepath=sqlite_key, signature=signature)[0] is False

    _freeze_now(monkeypatch, datetime(2026, 2, 25, 9, 0))
    assert trend_service._get_reference_trend_cached(**kwargs)["foreign"] == 1_500
    trend_service.clear_investor_trend_5day_memory_cache()
    assert trend_service._get_reference_trend_cached(**kwargs)["foreign"] == 1_500  # SQLite 에서 읽는다
    assert len(calls) == 3
    trend_service.clear_investor_trend_5day_memory_cache()


def test_toss_reference_is_not_cached_because_its_key_is_always_today(monkeypatch, tmp_path):
    """[FLOW-030] Toss 는 달력상 오늘 키로만 묻는다. 아침 스냅숏이 그날 내내 남지 않게 두지 않는다."""
    trend_service.clear_investor_trend_5day_memory_cache()
    _freeze_now(monkeypatch, datetime(2026, 2, 24, 9, 30))
    calls = []
    monkeypatch.setattr(
        trend_service, "_fetch_toss_reference_trend",
        lambda **_k: calls.append(1) or {**_complete_reference(100), "source": "toss"},
    )
    kwargs = {"data_dir": str(tmp_path), "source": "toss", "ticker": "005930", "target_datetime": None}
    sqlite_key, signature = trend_service._reference_sqlite_context(**kwargs)

    assert trend_service._get_reference_trend_cached(**kwargs)["foreign"] == 500
    assert trend_service._get_reference_trend_cached(**kwargs)["foreign"] == 500
    assert len(calls) == 2
    assert sqlite_payload_cache.load_json_payload_from_sqlite(filepath=sqlite_key, signature=signature)[0] is False
    trend_service.clear_investor_trend_5day_memory_cache()
```

기존 `test_a_reference_saved_under_the_v1_signature_is_not_read` 를 옛 서명 둘로 매개변수화한다.

```python
@pytest.mark.parametrize("old_version", ["pykrx", "pykrx:v2"])
def test_a_reference_saved_under_an_old_signature_is_not_read(monkeypatch, tmp_path, old_version):
    """[FLOW-029] v1 에는 NaN 을 0 으로 저장한 값과 끝 날짜가 어긋난 값이, [FLOW-030] v2 에는 오늘 키로 저장한 확정 전 값이 있을 수 있어 읽지 않는다."""
    trend_service.clear_investor_trend_5day_memory_cache()
    sqlite_key, _ = trend_service._reference_sqlite_context(
        data_dir=str(tmp_path), source="pykrx", ticker="005930", target_datetime="2026-02-24",
    )
    old_signature = (trend_service._stable_token_to_int("20260224"), trend_service._stable_token_to_int(old_version))
    sqlite_payload_cache.save_json_payload_to_sqlite(filepath=sqlite_key, signature=old_signature, payload=_complete_reference(999))
    monkeypatch.setattr(trend_service, "_fetch_pykrx_reference_trend", lambda **_k: _complete_reference(300))

    result = trend_service._get_reference_trend_cached(
        data_dir=str(tmp_path), source="pykrx", ticker="005930", target_datetime="2026-02-24",
    )

    assert result["foreign"] == 1_500
    trend_service.clear_investor_trend_5day_memory_cache()
```

- [ ] **Step 2: 실패를 확인한다**

Run: `KRX_ID= KRX_PW= venv/bin/python -m pytest -q -p no:cacheprovider tests/services/test_investor_trend_5day_service.py -k "keyed_today or always_today or old_signature"`
Expected: FAIL 3건 — 오늘 키 검사는 두 번째 호출이 `500`(메모리 재사용), Toss 검사는 `len(calls) == 1`, `pykrx:v2` 는 `4_995`.

- [ ] **Step 3: 최소 구현**

`_reference_sqlite_context`:

```python
    # [FLOW-029] v1 에는 NaN 을 0 으로 저장한 값(930525be 이전), 끝 날짜가 기준일이 아닌 값, 거부될 값이 남아 있을 수 있다.
    # [FLOW-030] v2 에는 오늘 키로 저장한 확정 전 값이 남아 있을 수 있다. 다음 날에는 확정 값처럼 읽힌다.
    # SQLite 에서 읽은 값은 거부되어도 다시 조회하지 않으므로 _reference_reject_reason 이나 저장 조건을 엄격하게 바꾸면 이 버전도 올린다
    signature = (_stable_token_to_int(token), _stable_token_to_int(f"{source}:v3"))
```

`_get_reference_trend_cached` 의 `finally` 저장 갈래:

```python
                # [FLOW-029] 거부될 값도 실패로 다룬다. 저장하면 같은 키로 다시 묻지 않는다
                if result is not None and _reference_reject_reason(result) is None:
                    # [FLOW-030] 키 날짜가 오늘이면 장중·시간외 부분 값이거나 아침 스냅숏일 수 있다(Toss 는 늘 오늘 키).
                    # 조회한 호출과 기다린 호출에만 주고 두지 않는다. 다음 날 같은 키를 처음 묻는 호출이 확정 값을 저장한다
                    if target_key < datetime.now().strftime("%Y%m%d"):
                        _REFERENCE_CACHE[cache_key] = copy.deepcopy(result)
                        ...(기존 메모리·SQLite 저장 그대로 한 단계 들여씀)
                elif fatal is None:
```

`get_investor_trend_5day_for_ticker` docstring 의 캐시 문단 끝에 한 문장을 붙인다: 「키 날짜가 오늘인 참조(기준일이 오늘인 실행, 장중 최신 창, 늘 오늘 키인 Toss)는 어디에도 두지 않아 호출마다 다시 받는다([FLOW-030]).」

- [ ] **Step 4: 통과를 확인한다**

Run: Step 2 와 같은 명령
Expected: PASS 4건(매개변수 둘 포함)

- [ ] **Step 5: 요일에 기대는 기존 저장 검사를 고정한다**

평일 시각으로 고정해 파일 전체를 돌리는 탐침을 한 번 실행해, 최신 창 키가 오늘이 되어 깨지는 기존 검사를 찾는다(오늘 2026-09-26 은 토요일이라 그냥 돌리면 드러나지 않는다).

Run: 파일 끝에 임시 autouse 픽스처 `_freeze_now(monkeypatch, datetime(2026, 9, 23, 10, 0))` 를 붙여 `... pytest -q -p no:cacheprovider tests/services/test_investor_trend_5day_service.py` 실행 후 임시 픽스처 제거
Expected: 최신 창 저장을 확인하는 검사(예: `test_get_investor_trend_5day_for_ticker_reuses_reference_sqlite_after_memory_clear`)가 실패한다. 그 검사들에 `monkeypatch.setattr(trend_service, "_resolve_pykrx_latest_market_date", lambda **_k: datetime(2026, 2, 24))` 를 넣어 키를 과거 날짜로 고정한다. 기대값은 바꾸지 않는다. 고친 뒤 탐침을 다시 붙여 전부 통과하는지 보고 제거한다. 판단 기준(critic m3): 고칠 대상은 최신 창 키가 오늘이 되어 저장을 단언하는 검사뿐이다. `_Frozen` 하위 클래스가 `isinstance(cached_value, datetime)` 를 바꾸는 등 저장과 무관한 탐침 산물은 고치지 않고 ledger 에 적는다.

- [ ] **Step 6: 관련 파일을 돌린다**

Run: `KRX_ID= KRX_PW= venv/bin/python -m pytest -q -p no:cacheprovider tests/services/test_investor_trend_5day_service.py tests/engine/test_collectors_unified_supply_service_refactor.py tests/engine/test_screener_supply_batch.py`
Expected: PASS

### Task 2: [FLOW-031] 기준일 행이 없는 CSV 창을 기준일 5일 값으로 쓰지 않는다

**Files:**
- Modify: `services/investor_trend_5day_service.py` (`_build_trend_map`)
- Test: `tests/services/test_investor_trend_5day_service.py`

**Interfaces:**
- Consumes: `_build_trend_map(trend_df, *, target_datetime)`, `get_investor_trend_5day_for_ticker(...)`, `MarketSchedule.known_holidays()`
- Produces: 없음(반환 계약은 그대로 `dict`, 해당 경우 `{}`)

- [ ] **Step 1: 실패하는 검사를 쓴다**

```python
_MARCH_WEEK = ["2026-03-03", "2026-03-04", "2026-03-05", "2026-03-06", "2026-03-09"]


def _one_ticker_csv(dates: list[str]) -> pd.DataFrame:
    return pd.DataFrame([{"ticker": "005930", "date": day, "foreign_buy": 1, "inst_buy": 2} for day in dates])


@pytest.mark.parametrize(
    ("dates", "target", "kept"),
    [
        (_MARCH_WEEK, "2026-03-10", False),  # 기준일 화요일 행이 없어 창이 전날로 끝난다
        (_MARCH_WEEK, "2026-03-09", True),
        (["2026-03-09", "2026-03-10", "2026-03-11", "2026-03-12", "2026-03-13"], "2026-03-14", True),  # 토요일 기준일, 금요일 끝
        (["2026-02-09", "2026-02-10", "2026-02-11", "2026-02-12", "2026-02-13"], "2026-02-18", True),  # 2/16~18 은 목록의 휴장일
    ],
)
def test_a_csv_window_that_misses_a_business_day_up_to_the_target_is_not_used(dates, target, kept):
    """[FLOW-031] 기준일까지 빈 영업일이 있는 창은 기준일의 5거래일이 아니다. 전날 끝 값을 기준일 값으로 내보내지 않는다."""
    assert bool(trend_service._build_trend_map(_one_ticker_csv(dates), target_datetime=target)) is kept


def test_a_target_run_without_the_target_day_in_csv_gets_the_reference_or_none(monkeypatch, tmp_path):
    """[FLOW-031] 기준일 참조도 없으면 None 이다. 종가베팅은 그 종목을 수급 결측으로 본다."""
    _one_ticker_csv(_MARCH_WEEK).to_csv(tmp_path / "all_institutional_trend_data.csv", index=False)
    trend_service.clear_investor_trend_5day_memory_cache()
    monkeypatch.setattr(trend_service, "_fetch_pykrx_reference_trend", lambda **_k: None)

    assert trend_service.get_investor_trend_5day_for_ticker(
        ticker="005930", data_dir=str(tmp_path), target_datetime="2026-03-10",
    ) is None

    monkeypatch.setattr(trend_service, "_fetch_pykrx_reference_trend", lambda **_k: {**_complete_reference(300), "latest_date": "2026-03-10"})
    trend_service.clear_investor_trend_5day_memory_cache()
    result = trend_service.get_investor_trend_5day_for_ticker(
        ticker="005930", data_dir=str(tmp_path), target_datetime="2026-03-10",
    )
    assert (result["source"], result["foreign"], result["quality"]["reference_only"]) == ("pykrx", 1_500, True)
    trend_service.clear_investor_trend_5day_memory_cache()
```

- [ ] **Step 2: 실패를 확인한다**

Run: `KRX_ID= KRX_PW= venv/bin/python -m pytest -q -p no:cacheprovider tests/services/test_investor_trend_5day_service.py -k "misses_a_business_day or without_the_target_day"`
Expected: FAIL 2건 — 첫 매개변수 경우 `True is False`, 통합 검사는 CSV 값(`foreign == 5`)이 나와 `is None` 실패. 나머지 세 매개변수 경우는 PASS.

추세 맵 SQLite 접미사 검사(critic M1)도 쓴다.

```python
def test_a_trend_map_saved_under_the_v2_suffix_is_not_read(tmp_path):
    """[FLOW-031] v2 스냅숏에는 기준일 행 없이 전날로 끝난 창이 들어 있을 수 있어 읽지 않는다."""
    _one_ticker_csv(_MARCH_WEEK).to_csv(tmp_path / "all_institutional_trend_data.csv", index=False)
    trend_service.clear_investor_trend_5day_memory_cache()
    filepath, signature = trend_service._resolve_trend_file_context(data_dir=str(tmp_path), filename="all_institutional_trend_data.csv")
    stale_map = trend_service._build_trend_map(_one_ticker_csv(_MARCH_WEEK), target_datetime="2026-03-09")
    sqlite_payload_cache.save_json_payload_to_sqlite(
        filepath=f"{filepath}::investor_trend_5day_unified_v2::20260310", signature=signature,
        payload=trend_service._serialize_trend_map(stale_map),
    )

    assert trend_service._get_or_build_trend_map(
        data_dir=str(tmp_path), filename="all_institutional_trend_data.csv", target_datetime="2026-03-10",
    ) == {}
    trend_service.clear_investor_trend_5day_memory_cache()
```

Expected: FAIL — v2 스냅숏을 읽어 비어 있지 않은 맵.

- [ ] **Step 3: 최소 구현**

`_SQLITE_KEY_SUFFIX` 를 v3 로 올린다(critic M1, `[FLOW-025]` 가 `_build_trend_map` 계약을 바꾸며 v2 로 올린 선례).

```python
# [FLOW-025] 종목별 마지막 5행으로 만든 스냅숏, [FLOW-028] 영문자 코드가 숫자 코드와 섞인 스냅숏을 다시 쓰지 않도록 v2 로 바꿨다.
# [FLOW-031] 기준일 행 없이 전날로 끝난 창을 기준일 키로 둔 스냅숏을 읽지 않도록 v3 로 바꿨다
_SQLITE_KEY_SUFFIX = "::investor_trend_5day_unified_v3"
```

`_build_trend_map` 에서 `window_dates = recent_trading_dates(working["date"])` 바로 뒤:

```python
        if normalized_target_datetime is not None:
            # [FLOW-031] 창 끝 다음 날부터 기준일까지 빈 영업일이 있으면 그 창은 기준일의 5거래일이 아니다(수집 실패·부분 수집일).
            # 빈 맵을 돌려주면 종목마다 missing_csv 로 기준일 참조를 묻고, 참조도 없으면 None 이다. 휴장일 목록에 없는 휴장일을
            # 기준일로 주면 여기서 버려진다. 정규 실행의 기준일은 실제 최근 거래일이라 해당하지 않는다
            from engine.market_schedule import MarketSchedule

            missing_days = int(np.busday_count(
                (window_dates[-1] + pd.Timedelta(days=1)).date(),
                (normalized_target_datetime + timedelta(days=1)).date(),
                holidays=MarketSchedule.known_holidays(),
            ))
            if missing_days:
                logger.warning(
                    "수급 CSV 가 기준일 %s 전 %s 로 끝나 영업일 %d일이 비었다. 기준일 5일 값을 CSV 로 만들지 않는다",
                    normalized_target_datetime.strftime("%Y-%m-%d"), window_dates[-1].strftime("%Y-%m-%d"), missing_days,
                )
                return {}
```

- [ ] **Step 4: 통과를 확인한다**

Run: Step 2 와 같은 명령
Expected: PASS 6건

- [ ] **Step 5: 기준일을 주는 기존 검사를 돌린다**

Run: `KRX_ID= KRX_PW= venv/bin/python -m pytest -q -p no:cacheprovider tests/services tests/engine -k "trend or supply or screener"`
Expected: PASS. 실패가 있으면 기준일이 CSV 끝보다 뒤인 고정 자료가 원인인지 먼저 본다. 그 검사가 전날 끝 창을 기대한 것이면 계약이 바뀐 것이므로 기대값을 고치고 ledger 에 Ruling 으로 남긴다. 다른 원인이면 구현을 고친다.

### Task 3: [JONGGA-044] vitest 회귀 검사의 시각을 KST 정오에 고정한다

**Files:**
- Modify: `frontend/src/app/dashboard/kr/closing-bet/page.regression-jongga-037.test.tsx:46-50`

**Interfaces:**
- Consumes: 파일의 `TODAY()`(KST 오늘 12:00 +09:00 문자열)
- Produces: 없음

- [ ] **Step 1: 자정 전 실패를 재현한다**

`beforeEach` 첫 줄에 임시로 `vi.setSystemTime(new Date('2026-09-25T23:58:00+09:00'));` 를 넣는다.

Run: `cd frontend && npx vitest run src/app/dashboard/kr/closing-bet/page.regression-jongga-037.test.tsx`
Expected: FAIL 1건 「안전 timeout 뒤 늦은 상태 응답은 화면이나 polling을 되살리지 않는다」(`UPDATED` 를 찾지 못함)

- [ ] **Step 2: 고정 줄을 넣는다**

임시 줄 바로 아래에 다음을 넣는다.

```ts
  // [JONGGA-044] 고정 자료의 KST 날짜와 가짜 타이머가 앞당긴 시각이 자정을 넘어 어긋나지 않게 KST 정오에 고정한다.
  // 가짜 타이머 전이라 Date 만 고정되고, 뒤이은 useFakeTimers 가 이 시각에서 시작하며 afterEach 의 useRealTimers 가 되돌린다
  vi.setSystemTime(new Date(TODAY()));
```

Run: Step 1 과 같은 명령
Expected: PASS 5건(23:58 에서 시작해도 TODAY() 가 그날 정오로 고정)

- [ ] **Step 3: 임시 줄을 지우고 다시 돌린다**

Run: Step 1 과 같은 명령, 이어서 `cd frontend && npx vitest run`
Expected: 파일 5 passed, 전체 exit 0

---

## 정적 검증과 첫 커밋

- pytest 전체, vitest 전체, `git diff --check`.
- 첫 커밋 대상: 서비스·두 테스트 파일·이 계획·QA 문서·TODO.

## QA 초안(`docs/dev-cycle/qa/FLOW-030.md` 에서 확정)

- 격리 사본 두 벌(새 코드와 `f6708cbc`), `secrets/`·`data/`·`.env*` 삭제, 포트 3500·5501 금지, 외부 소켓 차단, pykrx·Toss 대역.
- 브라우저: 상세 모달(CSV 이상 징후 종목)의 최신 창이 Toss 참조로 그려지고, 새 코드는 `.investor_trend_reference_cache/toss` 에 행이 없고 수정 전 코드는 오늘 키 행이 생긴다.
- CLI: (a) 오늘을 기준일로 한 `get_supply_data` 는 값을 주지만 SQLite 행이 없고, 다음 날 시각의 새 프로세스가 다시 물어 저장한다(수정 전은 첫 프로세스가 저장하고 다음 날 묻지 않음) (b) 기준일 행 없는 CSV 와 전날 끝 pykrx 에서 `get_supply_data` 가 None(수정 전은 전날 끝 CSV 값) (c) CSV 에 기준일 행이 있으면 두 코드 모두 CSV 값.
- 비용 메모(critic m1, 아카이브에 기록): 스크리너 과거 기준일 배치에서 창 끝과 기준일 사이가 비면(끝쪽 부분 수집일 포함) 전 종목이 pykrx 를 한 번씩 조회한다. 과거 키라 한 번 뒤에는 캐시된다. 오늘 기준일 VCP 에서 Toss 가 실패해 CSV 로 폴백하는 종목은 pykrx 를 조회하고 그 값은 캐시되지 않는다.
- JONGGA-044 는 테스트 파일만 바꿔 동적 QA 제외(vitest 결과가 증거).

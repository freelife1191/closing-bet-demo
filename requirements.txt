# 직접 애플리케이션·테스트 의존성은 검증된 버전으로 고정한다.
# 기존 전이 의존성·설치 도구의 보안 하한도 표준 설치에 함께 적용한다.
# 전이 의존성 전체를 고정한 lock은 아니다. 검증 기록: docs/dev-cycle/qa/INFRA-070.md
-r requirements-security.txt

flask==3.1.3
flask-cors==6.0.2
gunicorn==25.0.3

pandas==2.3.3
# ponytail: pykrx 1.2.3의 numpy<2.0 제약으로 1.x 유지. 인코더의 np.float_ 참조는 제거했다.
# NumPy 2 전환은 pykrx 인증 쿠키의 타 출처 전달 문제가 해결된 뒤 검증한다([INFRA-018]).
numpy==1.26.4

# 1.2.7 부터 numpy>=2.0 으로 뒤집히므로 numpy 핀과 같이 움직인다([INFRA-018]).
pykrx==1.2.3
yfinance==1.3.0
finance-datareader==0.9.102

requests==2.33.0
httpx==0.28.1
beautifulsoup4==4.14.3

# google-genai 2.21.0 은 이 저장소가 쓰는 API 표면이 1.62.0 과 같고 필수 의존성도
# 10개로 동일하다. 다만 실제 호출 동작은 Vertex 과금 호출 없이 확인할 수 없어
# 승격을 분리한다([INFRA-001]). 아래 openai 핀에는 해당하지 않는다.
google-genai==1.62.0
openai==2.24.0

python-dotenv==1.2.2
schedule==1.2.2

# 개발과 검증. CLAUDE.md 가 이 파일 하나로 환경을 세우도록 안내한다.
pytest==9.0.3
